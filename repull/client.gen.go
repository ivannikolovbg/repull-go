// Package repull provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package repull

import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"strings"
	"time"

	"github.com/oapi-codegen/runtime"
	openapi_types "github.com/oapi-codegen/runtime/types"
)

// RequestEditorFn is the function signature for the RequestEditor callback function
type RequestEditorFn func(ctx context.Context, req *http.Request) error

// Doer performs HTTP requests.
//
// The standard http.Client implements this interface.
type HttpRequestDoer interface {
	Do(req *http.Request) (*http.Response, error)
}

// Client which conforms to the OpenAPI3 specification for this service.
type Client struct {
	// The endpoint of the server conforming to this interface, with scheme,
	// https://api.deepmap.com for example. This can contain a path relative
	// to the server, such as https://api.deepmap.com/dev-test, and all the
	// paths in the swagger spec will be appended to the server.
	Server string

	// Doer for performing requests, typically a *http.Client with any
	// customized settings, such as certificate chains.
	Client HttpRequestDoer

	// A list of callbacks for modifying requests which are generated before sending over
	// the network.
	RequestEditors []RequestEditorFn
}

// ClientOption allows setting custom parameters during construction
type ClientOption func(*Client) error

// Creates a new Client, with reasonable defaults
func NewClient(server string, opts ...ClientOption) (*Client, error) {
	// create a client with sane default values
	client := Client{
		Server: server,
	}
	// mutate client and add all optional params
	for _, o := range opts {
		if err := o(&client); err != nil {
			return nil, err
		}
	}
	// ensure the server URL always has a trailing slash
	if !strings.HasSuffix(client.Server, "/") {
		client.Server += "/"
	}
	// create httpClient, if not already present
	if client.Client == nil {
		client.Client = &http.Client{}
	}
	return &client, nil
}

// WithHTTPClient allows overriding the default Doer, which is
// automatically created using http.Client. This is useful for tests.
func WithHTTPClient(doer HttpRequestDoer) ClientOption {
	return func(c *Client) error {
		c.Client = doer
		return nil
	}
}

// WithRequestEditorFn allows setting up a callback function, which will be
// called right before sending the request. This can be used to mutate the request.
func WithRequestEditorFn(fn RequestEditorFn) ClientOption {
	return func(c *Client) error {
		c.RequestEditors = append(c.RequestEditors, fn)
		return nil
	}
}

// The interface specification for the client above.
type ClientInterface interface {

	// BatchUpdateAvailabilityWithBody Update availability across many properties
	//
	// Applies ONE settings object across up to 500 properties and pushes the result to every connected channel.
	//
	// Ownership is checked before anything is written: a batch containing a property from another workspace is refused as a whole and names the offending ids, rather than being partially applied.
	//
	// Per-property *different* values are separate calls — presenting them as one request would be a false claim about atomicity.
	//
	// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /v1/availability/batch (the `BatchUpdateAvailability` operationId).
	BatchUpdateAvailabilityWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BatchUpdateAvailability Update availability across many properties
	//
	// Applies ONE settings object across up to 500 properties and pushes the result to every connected channel.
	//
	// Ownership is checked before anything is written: a batch containing a property from another workspace is refused as a whole and names the offending ids, rather than being partially applied.
	//
	// Per-property *different* values are separate calls — presenting them as one request would be a false claim about atomicity.
	//
	// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /v1/availability/batch (the `BatchUpdateAvailability` operationId).
	BatchUpdateAvailability(ctx context.Context, body BatchUpdateAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAvailability Get property availability
	//
	// Channel-agnostic day-by-day availability calendar for a property over a date window. Returns a thin per-date shape — `{ date, available, price, minNights }` — projected from the property calendar.
	//
	// The `from` and `to` query params are **required** (ISO `YYYY-MM-DD`, inclusive) — omitting or malforming either returns 422. The window is capped at 366 days; longer ranges are truncated to the first 366 days.
	//
	// **`days` contains only the dates we actually hold calendar data for.** Requested dates with no calendar row are listed in `coverage.missingDates` — their availability is unknown. Never treat a missing date as bookable: this endpoint deliberately does not synthesise availability, because a fabricated open date can be double-booked. A property with no calendar still returns a real 200 (`days: []`, every date in `coverage.missingDates`), never a 404 — 404 means the property id does not exist or belongs to a different workspace.
	//
	// This endpoint is read-only, and the projected per-date shape carries **availability, price, and min-nights only** — it does NOT expose max-stay, closed-to-arrival (CTA), closed-to-departure (CTD), or the dedicated stop-sell flag. To read or write that full restriction set on Booking.com use the channel routes: `GET`/`PUT /v1/channels/booking/availability` (with the room + rate ids from `GET /v1/channels/booking/properties/{id}/rooms`). To **write** calendar values use `PUT /v1/availability/{propertyId}` (or `PATCH /v1/availability/batch`), which updates the property calendar and pushes to its connected channels; channel-only settings stay on the channel routes.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/availability/{propertyId} (the `GetAvailability` operationId).
	GetAvailability(ctx context.Context, propertyId int, params *GetAvailabilityParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAvailabilityWithBody Set prices, block or unblock dates
	//
	// Writes the calendar for one property AND pushes to every connected channel in the same step. A write that only changed our copy would leave the OTA calendars stale and eventually double-book a guest.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/availability/{propertyId} (the `UpdateAvailability` operationId).
	UpdateAvailabilityWithBody(ctx context.Context, propertyId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAvailability Set prices, block or unblock dates
	//
	// Writes the calendar for one property AND pushes to every connected channel in the same step. A write that only changed our copy would leave the OTA calendars stale and eventually double-book a guest.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/availability/{propertyId} (the `UpdateAvailability` operationId).
	UpdateAvailability(ctx context.Context, propertyId int, body UpdateAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateBillingCheckoutWithBody Create checkout session
	//
	// Redirect user to Stripe checkout.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/billing (the `CreateBillingCheckout` operationId).
	CreateBillingCheckoutWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateBillingCheckout Create checkout session
	//
	// Redirect user to Stripe checkout.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/billing (the `CreateBillingCheckout` operationId).
	CreateBillingCheckout(ctx context.Context, body CreateBillingCheckoutJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbAlterations List Airbnb alterations
	//
	// List reservation alteration requests for Airbnb reservations in this workspace. **Pure DB read** from the local `reservation_alterations` mirror — never calls Airbnb upstream — scoped to your workspace via the reservations join.
	//
	// Default returns only pending alterations; pass `?type=all` for the full history. Filter to a single reservation with `?reservation_code=<confirmation code>`. Every response carries the `dataFreshness` envelope.
	//
	// Each row carries the proposed change in its `new*` fields. A **listing transfer** shows up as `newListingId` (Repull listing id) and `newAirbnbListingId` (Airbnb's own id); both are `null` when the alteration does not move the reservation.
	//
	// Alterations of reservations on inactive listings are left out. Filtering by a reservation on an inactive listing (`reservation_code`) returns `403 listing_inactive`.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Corresponds with GET /v1/channels/airbnb/alterations (the `ListAirbnbAlterations` operationId).
	ListAirbnbAlterations(ctx context.Context, params *ListAirbnbAlterationsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateAirbnbAlterationWithBody Create Airbnb alteration
	//
	// Propose a change to an existing Airbnb reservation: new dates, a new guest count, a new total price, or a move to a different listing. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host for the workspace, else `404 no_connection`.
	//
	// The body is validated before anything reaches Airbnb. `confirmation_code` is required and **at least one** of `check_in`, `check_out`, `number_of_guests`, `total_price` or `listing_id` must be sent with it — an alteration that changes nothing is `422 invalid_params`, not a request Airbnb is asked to act on. Unknown fields are refused rather than ignored, so a misspelling can never look like a successful write.
	//
	// **Listing transfer.** `listing_id` moves the reservation to another listing in your workspace. Send the **Repull** listing id (the `id` from `GET /v1/properties`); Repull checks you own it, that it is active and connected to Airbnb, translates it to the Airbnb listing id and sends it upstream. Callers who hold the Airbnb-side id instead may send `airbnb_listing_id`. **Airbnb decides** whether to honour a listing change on an alteration — Repull sends it and reports Airbnb’s answer; a refusal comes back as `422 airbnb_rejected` carrying Airbnb’s own message.
	//
	// Returns `403 listing_inactive` when the reservation’s listing, or the listing it is being moved to, is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/alterations (the `CreateAirbnbAlteration` operationId).
	CreateAirbnbAlterationWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateAirbnbAlteration Create Airbnb alteration
	//
	// Propose a change to an existing Airbnb reservation: new dates, a new guest count, a new total price, or a move to a different listing. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host for the workspace, else `404 no_connection`.
	//
	// The body is validated before anything reaches Airbnb. `confirmation_code` is required and **at least one** of `check_in`, `check_out`, `number_of_guests`, `total_price` or `listing_id` must be sent with it — an alteration that changes nothing is `422 invalid_params`, not a request Airbnb is asked to act on. Unknown fields are refused rather than ignored, so a misspelling can never look like a successful write.
	//
	// **Listing transfer.** `listing_id` moves the reservation to another listing in your workspace. Send the **Repull** listing id (the `id` from `GET /v1/properties`); Repull checks you own it, that it is active and connected to Airbnb, translates it to the Airbnb listing id and sends it upstream. Callers who hold the Airbnb-side id instead may send `airbnb_listing_id`. **Airbnb decides** whether to honour a listing change on an alteration — Repull sends it and reports Airbnb’s answer; a refusal comes back as `422 airbnb_rejected` carrying Airbnb’s own message.
	//
	// Returns `403 listing_inactive` when the reservation’s listing, or the listing it is being moved to, is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/alterations (the `CreateAirbnbAlteration` operationId).
	CreateAirbnbAlteration(ctx context.Context, body CreateAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbAlteration Get Airbnb alteration
	//
	// Fetch a single Airbnb reservation alteration by its Airbnb alteration id. **Pure DB read**, workspace-scoped via the reservations join. Returns `404 not_found` when no alteration matches the id in your workspace.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/alterations/{id} (the `GetAirbnbAlteration` operationId).
	GetAirbnbAlteration(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AcceptAirbnbAlterationWithBody Accept Airbnb alteration
	//
	// Accept a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to approve the proposed date / guest-count / price change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/accept (the `AcceptAirbnbAlteration` operationId).
	AcceptAirbnbAlterationWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AcceptAirbnbAlteration Accept Airbnb alteration
	//
	// Accept a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to approve the proposed date / guest-count / price change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/accept (the `AcceptAirbnbAlteration` operationId).
	AcceptAirbnbAlteration(ctx context.Context, id string, body AcceptAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CancelAirbnbAlterationWithBody Cancel Airbnb alteration
	//
	// Withdraw an alteration you proposed, before the other side has answered it. **Write-side** — calls Airbnb upstream (`respondToAlteration` with status `canceled`). Use this when you sent the wrong dates, guest count, price or listing: the alteration stops being pending instead of sitting there until the guest acts on it.
	//
	// This is the third of Airbnb's three answers to a pending alteration, alongside `accept` and `decline`, and behaves identically to them: requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Airbnb decides whether an alteration can still be withdrawn — one that has already been accepted or declined is refused upstream, and Airbnb's own reason comes back in `message`.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/cancel (the `CancelAirbnbAlteration` operationId).
	CancelAirbnbAlterationWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CancelAirbnbAlteration Cancel Airbnb alteration
	//
	// Withdraw an alteration you proposed, before the other side has answered it. **Write-side** — calls Airbnb upstream (`respondToAlteration` with status `canceled`). Use this when you sent the wrong dates, guest count, price or listing: the alteration stops being pending instead of sitting there until the guest acts on it.
	//
	// This is the third of Airbnb's three answers to a pending alteration, alongside `accept` and `decline`, and behaves identically to them: requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Airbnb decides whether an alteration can still be withdrawn — one that has already been accepted or declined is refused upstream, and Airbnb's own reason comes back in `message`.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/cancel (the `CancelAirbnbAlteration` operationId).
	CancelAirbnbAlteration(ctx context.Context, id string, body CancelAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeclineAirbnbAlterationWithBody Decline Airbnb alteration
	//
	// Decline a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to reject the proposed change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/decline (the `DeclineAirbnbAlteration` operationId).
	DeclineAirbnbAlterationWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeclineAirbnbAlteration Decline Airbnb alteration
	//
	// Decline a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to reject the proposed change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/decline (the `DeclineAirbnbAlteration` operationId).
	DeclineAirbnbAlteration(ctx context.Context, id string, body DeclineAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbConnection Get Airbnb connection state
	//
	// Returns the workspace's Airbnb host connection state in one envelope. Use this instead of inferring connection health from per-listing 401s on `GET /v1/channels/airbnb/listings` — that's noisy (every per-listing call has to fail before you know) and ambiguous (a single 5xx looks identical to a deauth).
	//
	// Pure DB read — does NOT touch Airbnb's API, so it's cheap to poll from a status-page surface.
	//
	// The response includes one row per Airbnb host the workspace has linked. Each row carries `isConnected`, `lastSyncedAt`, `deactivatedAt`, and `lastDisconnectReason` (most recent non-backfill row in `airbnb_host_events`).
	//
	// A self-serve `fixUrl` is included whenever `status` is anything other than `connected` — points at the dashboard where the host re-authorizes (or initiates the first OAuth flow for `never_connected` workspaces).
	//
	// Corresponds with GET /v1/channels/airbnb/connection (the `GetAirbnbConnection` operationId).
	GetAirbnbConnection(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbListings List Airbnb listings
	//
	// List every Airbnb listing this workspace has access to via the connected Airbnb account. **Pure DB read — never calls Airbnb upstream.** The connect flow is what populates the local cache; the API serves what's already there. Customers with a disconnected host still see their last-synced data, with the top-level `dataFreshness` envelope flagging the staleness and pointing at the reconnect URL.
	//
	// Pass `?include=amenities` to enrich each connection with its locally-cached amenity set. Returns `null` per connection when the cache is empty.
	//
	// Pass `?include=thumbnail` to add `thumbnailUrl` to each listing — one extra column on the query that already runs, so a selection screen renders from a single request instead of one call per listing. `null` when the listing has no thumbnail stored. Combine comma-separated, e.g. `?include=amenities,thumbnail`.
	//
	// **Can this listing be written to?** Every connection carries `syncCategory` — Airbnb's own per-listing API sync decision (`sync_all`, `sync_rates_and_availability`, or `none`) — and `writable`, which is `false` exactly when that category is `none`. Airbnb authorises sync one listing at a time, so a connected account can still hold listings Airbnb refuses every write to; a write to one of those returns `403 listing_not_api_connected` before anything is sent, and reconnecting the account does not change it (the host must switch the listing on in Airbnb). Check `writable` here before a portfolio-wide push instead of discovering it one 403 at a time.
	//
	// Inactive listings are left out; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Corresponds with GET /v1/channels/airbnb/listings (the `ListAirbnbListings` operationId).
	ListAirbnbListings(ctx context.Context, params *ListAirbnbListingsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// MapAirbnbListingWithBody Map an Airbnb listing to a Repull listing
	//
	// Link an existing Airbnb listing to a canonical Repull listing/property. **API-key-scoped** (unlike the Booking room mapping, which is Connect-session-scoped).
	//
	// Discover the `airbnbId` (+ `hostId`) via `GET /v1/channels/airbnb/listings`, then re-point it at the `listingId` of your choice — the dedup / consolidation case where the Airbnb sync auto-created its own listing but you want the inventory under an existing property.
	//
	// Repoints both the Airbnb record and its platform link to the target listing in one transaction. Idempotent — re-mapping to the same listing is a 200 no-op (`alreadyMapped: true`). Scope is enforced against your workspace on both the target listing and the existing Airbnb record; a listing that already links a different Airbnb listing returns 409.
	//
	// Returns `403 listing_inactive` when the target listing, or the listing the Airbnb listing is mapped to now, is inactive; nothing is changed.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/listings/map (the `MapAirbnbListing` operationId).
	MapAirbnbListingWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// MapAirbnbListing Map an Airbnb listing to a Repull listing
	//
	// Link an existing Airbnb listing to a canonical Repull listing/property. **API-key-scoped** (unlike the Booking room mapping, which is Connect-session-scoped).
	//
	// Discover the `airbnbId` (+ `hostId`) via `GET /v1/channels/airbnb/listings`, then re-point it at the `listingId` of your choice — the dedup / consolidation case where the Airbnb sync auto-created its own listing but you want the inventory under an existing property.
	//
	// Repoints both the Airbnb record and its platform link to the target listing in one transaction. Idempotent — re-mapping to the same listing is a 200 no-op (`alreadyMapped: true`). Scope is enforced against your workspace on both the target listing and the existing Airbnb record; a listing that already links a different Airbnb listing returns 409.
	//
	// Returns `403 listing_inactive` when the target listing, or the listing the Airbnb listing is mapped to now, is inactive; nothing is changed.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/listings/map (the `MapAirbnbListing` operationId).
	MapAirbnbListing(ctx context.Context, body MapAirbnbListingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbListing Get Airbnb listing
	//
	// Fetch all Airbnb connection rows for a single Vanio listing id. A property may be linked from multiple Airbnb hosts — every match is returned. Pass `?include=amenities` to enrich each row with its current Airbnb amenities.
	//
	// Each row carries `syncCategory` — Airbnb's own per-listing API sync decision (`sync_all`, `sync_rates_and_availability`, or `none`) — and `writable`, which is `false` exactly when that category is `none`, meaning Airbnb refuses every write to the listing and Repull returns `403 listing_not_api_connected` without sending anything. `GET /v1/channels/airbnb/listings` reports both fields for the whole portfolio in one call.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id} (the `GetAirbnbListing` operationId).
	GetAirbnbListing(ctx context.Context, id string, params *GetAirbnbListingParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AirbnbListingActionWithBody Listing action (delete/push/publish/unlist/relist)
	//
	// Apply a state action to a listing by id. The path `id` is the canonical Repull listing id.
	//
	// **`delete` here never touches Airbnb. Read this before you call it.**
	//
	// | | `action: "delete"` (this endpoint) | `action: "unlist"` (this endpoint) |
	// |---|---|---|
	// | What it changes | The Repull record | The live Airbnb listing |
	// | Calls Airbnb | **No. Never.** | Yes |
	// | The guest-facing listing | Stays live and keeps taking bookings | **Goes down** and stops taking bookings |
	// | Billing and plan limits | No longer billed, no longer counts toward the cap | Unchanged |
	// | API access to the listing | `403 listing_inactive` until reactivated | Unchanged — you can still read and write it |
	// | Reverse it with | `PATCH /v1/listings/{id}` `{ "active": true }` | `action: "relist"` |
	// | Data kept | Yes, and it keeps syncing | Yes |
	//
	// Neither one deletes anything on Airbnb. **There is no endpoint on this API that deletes an Airbnb listing** — the word `delete` on this route means "deactivate the Repull record" and nothing else. (Main vanio's internal listing-sync layer has a same-named action that DOES hard-delete on Airbnb; it is not exposed here, by any endpoint, deliberately. If you have read that code, note that the two names do not mean the same thing.)
	//
	// `delete` is idempotent. To take a listing off the market on every channel at once — Airbnb and Booking.com together — use `POST /v1/listings/{id}/offline`.
	//
	// `unlist` calls Airbnb and **takes the live listing down**: it is deactivated with a valid deactivation reason and then READ BACK, so "Airbnb accepted the call but the listing is still live" is reported as a failure rather than a success. Requires `airbnbConnectionId` — a listing can be connected to more than one Airbnb listing, and taking down the wrong one is not undoable through this API. `relist` puts it back up (re-enables sync and makes the listing available again); it does not push content.
	//
	// `relist` goes through the channel-publish billing gate and `unlist` does not, so on a workspace whose subscription has lapsed a listing can be taken down and not put back until billing is sorted out. That refusal comes back as `402` with the action that fixes it — never as an Airbnb error, because retrying and reconnecting Airbnb do nothing for it.
	//
	// `push` / `publish` push the listing's content to Airbnb via the same host-side sync orchestrator as `POST /v1/listings/{id}/publish/airbnb` — pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create + publish a new one under that host. `force` re-pushes every field, ignoring dirty-field tracking. The result is per-section: see `AirbnbPublishResult`.
	//
	// Any other action (e.g. `pull`) returns a structured 422 naming the supported actions.
	//
	// Returns `403 listing_inactive` for `push`/`publish`/`unlist`/`relist` when the listing is inactive. `delete` (deactivation) is always accepted.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id} (the `AirbnbListingAction` operationId).
	AirbnbListingActionWithBody(ctx context.Context, id string, params *AirbnbListingActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AirbnbListingAction Listing action (delete/push/publish/unlist/relist)
	//
	// Apply a state action to a listing by id. The path `id` is the canonical Repull listing id.
	//
	// **`delete` here never touches Airbnb. Read this before you call it.**
	//
	// | | `action: "delete"` (this endpoint) | `action: "unlist"` (this endpoint) |
	// |---|---|---|
	// | What it changes | The Repull record | The live Airbnb listing |
	// | Calls Airbnb | **No. Never.** | Yes |
	// | The guest-facing listing | Stays live and keeps taking bookings | **Goes down** and stops taking bookings |
	// | Billing and plan limits | No longer billed, no longer counts toward the cap | Unchanged |
	// | API access to the listing | `403 listing_inactive` until reactivated | Unchanged — you can still read and write it |
	// | Reverse it with | `PATCH /v1/listings/{id}` `{ "active": true }` | `action: "relist"` |
	// | Data kept | Yes, and it keeps syncing | Yes |
	//
	// Neither one deletes anything on Airbnb. **There is no endpoint on this API that deletes an Airbnb listing** — the word `delete` on this route means "deactivate the Repull record" and nothing else. (Main vanio's internal listing-sync layer has a same-named action that DOES hard-delete on Airbnb; it is not exposed here, by any endpoint, deliberately. If you have read that code, note that the two names do not mean the same thing.)
	//
	// `delete` is idempotent. To take a listing off the market on every channel at once — Airbnb and Booking.com together — use `POST /v1/listings/{id}/offline`.
	//
	// `unlist` calls Airbnb and **takes the live listing down**: it is deactivated with a valid deactivation reason and then READ BACK, so "Airbnb accepted the call but the listing is still live" is reported as a failure rather than a success. Requires `airbnbConnectionId` — a listing can be connected to more than one Airbnb listing, and taking down the wrong one is not undoable through this API. `relist` puts it back up (re-enables sync and makes the listing available again); it does not push content.
	//
	// `relist` goes through the channel-publish billing gate and `unlist` does not, so on a workspace whose subscription has lapsed a listing can be taken down and not put back until billing is sorted out. That refusal comes back as `402` with the action that fixes it — never as an Airbnb error, because retrying and reconnecting Airbnb do nothing for it.
	//
	// `push` / `publish` push the listing's content to Airbnb via the same host-side sync orchestrator as `POST /v1/listings/{id}/publish/airbnb` — pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create + publish a new one under that host. `force` re-pushes every field, ignoring dirty-field tracking. The result is per-section: see `AirbnbPublishResult`.
	//
	// Any other action (e.g. `pull`) returns a structured 422 naming the supported actions.
	//
	// Returns `403 listing_inactive` for `push`/`publish`/`unlist`/`relist` when the listing is inactive. `delete` (deactivation) is always accepted.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id} (the `AirbnbListingAction` operationId).
	AirbnbListingAction(ctx context.Context, id string, params *AirbnbListingActionParams, body AirbnbListingActionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbListingAmenities List Airbnb amenities
	//
	// List an Airbnb listing's amenities. **Pure DB read** from the local `listings_airbnb_amenities` cache — never calls Airbnb upstream. The response splits amenities into `amenities` (regular) and `accessibility_amenities` (step-free access, wide doorways, grab rails, disabled parking, wheelchair, accessible-height fixtures, hoists, etc). Both are arrays (`[]` when none). Consult `dataFreshness` to disambiguate "never synced" from "fresh and genuinely empty". Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/amenities (the `ListAirbnbListingAmenities` operationId).
	ListAirbnbListingAmenities(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingAmenitiesWithBody Update Airbnb amenities
	//
	// Set amenities on an Airbnb listing. **Write-side** — calls Airbnb upstream.
	//
	// **Partial by design**: only the amenities you name change, so turning one off is a one-line body and nothing else on the listing moves. Ids are the `id` values `GET /amenities` returns (e.g. `wireless_internet`, `ac`, `kitchen`); case is ignored. Airbnb refuses ids outside its vocabulary — that comes back as `422 airbnb_rejected` carrying Airbnb's own message.
	//
	// `accessibility_amenities` go to Airbnb's separate accessibility resource, which has **no read side at all** — Airbnb offers no endpoint to fetch them back, and the combined amenities GET 404s on production listings. What you can read back is our own copy: this endpoint updates it on success, and `GET /amenities` returns it under `accessibilityAmenities`. Airbnb may also hold an accessibility claim for review until photo evidence is attached; pass `photo_ids` to supply it.
	//
	// Our Airbnb copy is updated on success so a read straight after this write returns the new values. The platform-neutral copy behind `GET /v1/listings/{id}?include=amenities` uses a different amenity vocabulary and is refreshed by the next sync, except where an id happens to be identical in both.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/amenities (the `UpdateAirbnbListingAmenities` operationId).
	UpdateAirbnbListingAmenitiesWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingAmenities Update Airbnb amenities
	//
	// Set amenities on an Airbnb listing. **Write-side** — calls Airbnb upstream.
	//
	// **Partial by design**: only the amenities you name change, so turning one off is a one-line body and nothing else on the listing moves. Ids are the `id` values `GET /amenities` returns (e.g. `wireless_internet`, `ac`, `kitchen`); case is ignored. Airbnb refuses ids outside its vocabulary — that comes back as `422 airbnb_rejected` carrying Airbnb's own message.
	//
	// `accessibility_amenities` go to Airbnb's separate accessibility resource, which has **no read side at all** — Airbnb offers no endpoint to fetch them back, and the combined amenities GET 404s on production listings. What you can read back is our own copy: this endpoint updates it on success, and `GET /amenities` returns it under `accessibilityAmenities`. Airbnb may also hold an accessibility claim for review until photo evidence is attached; pass `photo_ids` to supply it.
	//
	// Our Airbnb copy is updated on success so a read straight after this write returns the new values. The platform-neutral copy behind `GET /v1/listings/{id}?include=amenities` uses a different amenity vocabulary and is refreshed by the next sync, except where an id happens to be identical in both.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/amenities (the `UpdateAirbnbListingAmenities` operationId).
	UpdateAirbnbListingAmenities(ctx context.Context, id string, body UpdateAirbnbListingAmenitiesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbListingAvailability Get Airbnb availability
	//
	// Read the per-day availability calendar for an Airbnb listing. Returns one row per day including price overrides, min-stay, and blocked status.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/availability (the `GetAirbnbListingAvailability` operationId).
	GetAirbnbListingAvailability(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingAvailabilityWithBody Update Airbnb availability
	//
	// Push availability + restrictions to Airbnb. `type: "calendar"` writes per-date restrictions — min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`) — via a batch of operations that each target either a date range or an explicit date list. `type: "rules"` writes listing-level availability rules (default min/max nights, booking lead time, turnover days, seasonal/day-of-week min nights). Restrictions never leak across channels — this endpoint writes only to Airbnb.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
	//
	// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
	//
	// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/availability (the `UpdateAirbnbListingAvailability` operationId).
	UpdateAirbnbListingAvailabilityWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingAvailability Update Airbnb availability
	//
	// Push availability + restrictions to Airbnb. `type: "calendar"` writes per-date restrictions — min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`) — via a batch of operations that each target either a date range or an explicit date list. `type: "rules"` writes listing-level availability rules (default min/max nights, booking lead time, turnover days, seasonal/day-of-week min nights). Restrictions never leak across channels — this endpoint writes only to Airbnb.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
	//
	// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
	//
	// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/availability (the `UpdateAirbnbListingAvailability` operationId).
	UpdateAirbnbListingAvailability(ctx context.Context, id string, body UpdateAirbnbListingAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbBookingSettings Get Airbnb booking settings
	//
	// Read how an Airbnb listing takes bookings and what happens when a guest cancels: booking mode, Instant Book state, the good-track-record requirement, check-in/check-out times, advance notice, preparation time, booking window, the short-stay and long-stay cancellation policies, and the non-refundable option.
	//
	// **This is Repull's stored copy, not a live call to Airbnb.** Values come from the local Airbnb mirror that the sync workers fill, so the response always carries `dataFreshness` — check `dataFreshness.stale` (and `dataFreshness.accounts[]` when the workspace has several Airbnb accounts) before treating a value as current.
	//
	// **Not exposed by Airbnb.** The pre-reservation message and automatic stay extension have no field on Airbnb's `booking_settings` resource, so neither can be read or written here; set the pre-reservation message in the Airbnb host dashboard, and handle extensions through `/v1/channels/airbnb/alterations`. Airbnb also expresses the same-day cutoff only as whole hours of advance notice, so `advanceNotice.hours` is as precise as the cutoff gets.
	//
	// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive — an inactive listing keeps syncing but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/booking-settings (the `GetAirbnbBookingSettings` operationId).
	GetAirbnbBookingSettings(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbBookingSettingsWithBody Update Airbnb booking settings
	//
	// Set any subset of a listing's booking settings on Airbnb. Partial — a field you do not send is left as it is.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id; Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb, so a bad value is a `422` naming the field rather than a failed upstream call. Unknown fields are refused rather than dropped.
	//
	// **Two groups, applied in order.** Instant Book, check-in/out and the cancellation fields go to Airbnb's booking-settings resource. Advance notice, preparation time and booking window go to Airbnb's availability rules — Airbnb replaces that whole document, so Repull reads the current rules first and merges your change onto them, which is why setting a preparation time does not blank the listing's min/max nights. The response's `applied` array names the groups that were written.
	//
	// **Non-refundable is a percentage here, a factor on Airbnb.** Airbnb stores `non_refundable_price_factor` between 0.7 and 1.0; send `cancellation.nonRefundable.discountPercent` (0-30) and Repull converts — 10% becomes 0.9. `enabled: false` sets the factor to 1.0.
	//
	// **Not exposed by Airbnb:** `preReservationMessage` and `automaticStayExtension`. Sending either returns a `422` explaining where to set it instead.
	//
	// **Errors:** `403 connection_reauth_required` — Airbnb no longer accepts the connection for this listing (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 invalid_params` — the body is wrong; `field` names it. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/booking-settings (the `UpdateAirbnbBookingSettings` operationId).
	UpdateAirbnbBookingSettingsWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbBookingSettings Update Airbnb booking settings
	//
	// Set any subset of a listing's booking settings on Airbnb. Partial — a field you do not send is left as it is.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id; Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb, so a bad value is a `422` naming the field rather than a failed upstream call. Unknown fields are refused rather than dropped.
	//
	// **Two groups, applied in order.** Instant Book, check-in/out and the cancellation fields go to Airbnb's booking-settings resource. Advance notice, preparation time and booking window go to Airbnb's availability rules — Airbnb replaces that whole document, so Repull reads the current rules first and merges your change onto them, which is why setting a preparation time does not blank the listing's min/max nights. The response's `applied` array names the groups that were written.
	//
	// **Non-refundable is a percentage here, a factor on Airbnb.** Airbnb stores `non_refundable_price_factor` between 0.7 and 1.0; send `cancellation.nonRefundable.discountPercent` (0-30) and Repull converts — 10% becomes 0.9. `enabled: false` sets the factor to 1.0.
	//
	// **Not exposed by Airbnb:** `preReservationMessage` and `automaticStayExtension`. Sending either returns a `422` explaining where to set it instead.
	//
	// **Errors:** `403 connection_reauth_required` — Airbnb no longer accepts the connection for this listing (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 invalid_params` — the body is wrong; `field` names it. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/booking-settings (the `UpdateAirbnbBookingSettings` operationId).
	UpdateAirbnbBookingSettings(ctx context.Context, id string, body UpdateAirbnbBookingSettingsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbCheckinGuide Get Airbnb check-in guide
	//
	// Return every published locale variant of an Airbnb listing's check-in guide. **Pure DB read** from `listings_airbnb_check_in_guides`. Pass `?locale=en` to filter to one locale (prefix match). Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/checkin-guide (the `GetAirbnbCheckinGuide` operationId).
	GetAirbnbCheckinGuide(ctx context.Context, id string, params *GetAirbnbCheckinGuideParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbCheckinGuide Upsert Airbnb check-in guide
	//
	// Upsert the check-in guide for one locale on an Airbnb listing. **Write-side** — calls Airbnb upstream; the DB mirror is reconciled by the sync worker once the upstream call returns. Target the locale with `?locale=en` (defaults to `en`). Requires a connected Airbnb host, else `404 no_connection`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/checkin-guide (the `UpdateAirbnbCheckinGuide` operationId).
	UpdateAirbnbCheckinGuide(ctx context.Context, id string, params *UpdateAirbnbCheckinGuideParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbCheckoutGuide Get Airbnb checkout guide
	//
	// Return the checkout tasks an Airbnb listing shows guests at departure. **Pure DB read** from `listings_airbnb_checkout_tasks`. Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/checkout-guide (the `GetAirbnbCheckoutGuide` operationId).
	GetAirbnbCheckoutGuide(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbListingDescriptions List Airbnb descriptions
	//
	// List an Airbnb listing's per-locale content (name, summary, house rules, etc). **Pure DB read** from `listings_airbnb_descriptions`. Filter to one locale with `?locale=en` (the legacy `?country=` param is accepted as a soft alias). Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/descriptions (the `ListAirbnbListingDescriptions` operationId).
	ListAirbnbListingDescriptions(ctx context.Context, id string, params *ListAirbnbListingDescriptionsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingDescriptionWithBody Update an Airbnb description for one locale
	//
	// Write one locale's copy to the live Airbnb listing.
	//
	// Airbnb keeps a SEPARATE description per locale (`PUT /v2/listing_descriptions/{listingId}/{locale}`), which is why `locale` is part of the request and not a guess: a listing can carry twelve of them, and writing Italian copy into the English row is how a translation gets lost. Only the fields you send are written; Airbnb keeps the rest. `GET /v1/channels/airbnb/listings/{id}/settings?type=locales` lists the locales already synced for the listing.
	//
	// `description` is not an accepted field: Airbnb composes the public description from the sections (`summary`, `space`, `access`, …) and ignores a directly-supplied one.
	//
	// **A 200 does not by itself mean the change was applied.** On an established listing Airbnb LOCKS host-managed description fields — the write returns 200, reports them as locked, and applies nothing for them. The response reports `blockedFields`: the fields YOU sent that Airbnb dropped. `blockedFields: []` is what a landed write looks like; a non-empty list is still a 200 (the other fields really were written) with a `message` naming what was not. Reporting that as a clean success is the bug behind "the description does not push to Airbnb".
	//
	// This writes to AIRBNB. To write Repull's own canonical copy — the content a later publish distributes — use `PUT /v1/listings/{id}/content` with `locale`.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Returns `403 listing_inactive` when the listing is inactive.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/descriptions (the `UpdateAirbnbListingDescription` operationId).
	UpdateAirbnbListingDescriptionWithBody(ctx context.Context, id string, params *UpdateAirbnbListingDescriptionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingDescription Update an Airbnb description for one locale
	//
	// Write one locale's copy to the live Airbnb listing.
	//
	// Airbnb keeps a SEPARATE description per locale (`PUT /v2/listing_descriptions/{listingId}/{locale}`), which is why `locale` is part of the request and not a guess: a listing can carry twelve of them, and writing Italian copy into the English row is how a translation gets lost. Only the fields you send are written; Airbnb keeps the rest. `GET /v1/channels/airbnb/listings/{id}/settings?type=locales` lists the locales already synced for the listing.
	//
	// `description` is not an accepted field: Airbnb composes the public description from the sections (`summary`, `space`, `access`, …) and ignores a directly-supplied one.
	//
	// **A 200 does not by itself mean the change was applied.** On an established listing Airbnb LOCKS host-managed description fields — the write returns 200, reports them as locked, and applies nothing for them. The response reports `blockedFields`: the fields YOU sent that Airbnb dropped. `blockedFields: []` is what a landed write looks like; a non-empty list is still a 200 (the other fields really were written) with a `message` naming what was not. Reporting that as a clean success is the bug behind "the description does not push to Airbnb".
	//
	// This writes to AIRBNB. To write Repull's own canonical copy — the content a later publish distributes — use `PUT /v1/listings/{id}/content` with `locale`.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Returns `403 listing_inactive` when the listing is inactive.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/descriptions (the `UpdateAirbnbListingDescription` operationId).
	UpdateAirbnbListingDescription(ctx context.Context, id string, params *UpdateAirbnbListingDescriptionParams, body UpdateAirbnbListingDescriptionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbListingDetails Get Airbnb listing details
	//
	// What kind of property Airbnb thinks this is — property type group and category, room type, capacity — plus the check-in method (`checkInOption`), whether the listing is live (`hasAvailability`), and **`lockedFields`: the attributes Airbnb refuses to change on this listing**.
	//
	// **Pure DB read** from the local mirror, one entry per Airbnb connection.
	//
	// Read `lockedFields` before a content write. Airbnb does not refuse a write to a locked attribute: it returns 200, reports the attribute as locked, and applies nothing — which is why a write can look successful and change nothing. 1,180 of 5,917 synced listings carry at least one locked attribute.
	//
	// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/details (the `GetAirbnbListingDetails` operationId).
	GetAirbnbListingDetails(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingDetailsWithBody Update property type, room type, quiet hours or check-in method
	//
	// Change what kind of property the Airbnb listing is, when its quiet hours are, or how the guest gets in. Partial: only the fields you send are written. At least one required; an unknown field is refused by name rather than dropped.
	//
	// This is the UPDATE path for fields that previously had none. `POST /v1/listings` accepts a `propertyType` when a listing is CREATED and nothing could change it afterwards, so a listing mis-typed at import stayed mis-typed; the check-in method was mirrored and never exposed at all.
	//
	// **A 200 does not by itself mean the change was applied.** `property_type_category`, `property_type_group` and `check_in_option` are among the attributes Airbnb locks on established listings: the write returns 200, and Airbnb applies nothing for the locked ones. The response reports `blockedFields` — the fields YOU sent that Airbnb dropped — and `blockedFields: []` is what a landed write looks like. `GET …/details` reports the same list as `lockedFields` so you can check first.
	//
	// Canonical property type (the value Repull keeps and republishes) is set with `PUT /v1/listings/{id}/content` under `details`; this endpoint writes straight to Airbnb.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/details (the `UpdateAirbnbListingDetails` operationId).
	UpdateAirbnbListingDetailsWithBody(ctx context.Context, id string, params *UpdateAirbnbListingDetailsParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingDetails Update property type, room type, quiet hours or check-in method
	//
	// Change what kind of property the Airbnb listing is, when its quiet hours are, or how the guest gets in. Partial: only the fields you send are written. At least one required; an unknown field is refused by name rather than dropped.
	//
	// This is the UPDATE path for fields that previously had none. `POST /v1/listings` accepts a `propertyType` when a listing is CREATED and nothing could change it afterwards, so a listing mis-typed at import stayed mis-typed; the check-in method was mirrored and never exposed at all.
	//
	// **A 200 does not by itself mean the change was applied.** `property_type_category`, `property_type_group` and `check_in_option` are among the attributes Airbnb locks on established listings: the write returns 200, and Airbnb applies nothing for the locked ones. The response reports `blockedFields` — the fields YOU sent that Airbnb dropped — and `blockedFields: []` is what a landed write looks like. `GET …/details` reports the same list as `lockedFields` so you can check first.
	//
	// Canonical property type (the value Repull keeps and republishes) is set with `PUT /v1/listings/{id}/content` under `details`; this endpoint writes straight to Airbnb.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/details (the `UpdateAirbnbListingDetails` operationId).
	UpdateAirbnbListingDetails(ctx context.Context, id string, params *UpdateAirbnbListingDetailsParams, body UpdateAirbnbListingDetailsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbListingPermits List Airbnb permits and licences
	//
	// The regulatory permits, licences and registration numbers attached to an Airbnb listing.
	//
	// **DB-only by default.** `?source=cache` (the default) returns the permits as last mirrored by the sync worker — regulatory body, regulation type, status, permit number — with no upstream call.
	//
	// **`?source=live` also returns the QUESTIONS.** The mirror stores the RESULT of a permit, not what Airbnb asks for it, so a caller that is about to write needs `?source=live` once: it returns each permit's `flows[]` with the `answer_key`, `type` and `choices` of every question, and the answers already on file. Airbnb refuses an `answer_key` it did not ask for on this listing, so this is not optional guesswork you can skip.
	//
	// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/permits (the `ListAirbnbListingPermits` operationId).
	ListAirbnbListingPermits(ctx context.Context, id string, params *ListAirbnbListingPermitsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingPermitsWithBody Answer Airbnb permit questions
	//
	// Answer the regulatory permit questions for a listing — the licence or registration number a city requires to keep the listing up.
	//
	// Read the questions first with `GET …/permits?source=live`. For each permit, pick one of its `flows[]` and send its `slug` as `flow_slug`; key every answer by the question's `answer_key`, and let the question's `type` decide the value field (`text_value`, `attestation_value`, `radio_value`, `date_value` or `selected_options_value`). Answers are forwarded verbatim — nothing is defaulted or inferred, because a wrong licence number can take a listing down in a regulated city.
	//
	// Send `Idempotency-Key`: a timeout here leaves you unable to tell "never arrived" from "arrived, response lost", and this is a compliance filing.
	//
	// Airbnb refusing the answers (an unknown `answer_key`, a malformed licence number) is `422 airbnb_rejected` carrying Airbnb's own reason. An expired or revoked Airbnb connection is `403 connection_reauth_required`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/permits (the `UpdateAirbnbListingPermits` operationId).
	UpdateAirbnbListingPermitsWithBody(ctx context.Context, id string, params *UpdateAirbnbListingPermitsParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingPermits Answer Airbnb permit questions
	//
	// Answer the regulatory permit questions for a listing — the licence or registration number a city requires to keep the listing up.
	//
	// Read the questions first with `GET …/permits?source=live`. For each permit, pick one of its `flows[]` and send its `slug` as `flow_slug`; key every answer by the question's `answer_key`, and let the question's `type` decide the value field (`text_value`, `attestation_value`, `radio_value`, `date_value` or `selected_options_value`). Answers are forwarded verbatim — nothing is defaulted or inferred, because a wrong licence number can take a listing down in a regulated city.
	//
	// Send `Idempotency-Key`: a timeout here leaves you unable to tell "never arrived" from "arrived, response lost", and this is a compliance filing.
	//
	// Airbnb refusing the answers (an unknown `answer_key`, a malformed licence number) is `422 airbnb_rejected` carrying Airbnb's own reason. An expired or revoked Airbnb connection is `403 connection_reauth_required`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/permits (the `UpdateAirbnbListingPermits` operationId).
	UpdateAirbnbListingPermits(ctx context.Context, id string, params *UpdateAirbnbListingPermitsParams, body UpdateAirbnbListingPermitsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteAirbnbListingPhoto Delete an Airbnb photo
	//
	// Remove a single photo from an Airbnb listing. Pass the Airbnb-side photo id as `?photoId=`. **Write-side** — calls Airbnb upstream.
	//
	// The photo is proven to belong to the listing named in the path first; a photo from another listing returns `404`. Airbnb refuses to delete a listing's last photo. Both stored copies drop the photo on success — `stored` reports whether that succeeded.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with DELETE /v1/channels/airbnb/listings/{id}/photos (the `DeleteAirbnbListingPhoto` operationId).
	DeleteAirbnbListingPhoto(ctx context.Context, id string, params *DeleteAirbnbListingPhotoParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbListingPhotos List Airbnb photos
	//
	// List photos attached to an Airbnb listing in display order. Returns the public CDN URL plus Airbnb-side metadata (id, caption, room).
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/photos (the `ListAirbnbListingPhotos` operationId).
	ListAirbnbListingPhotos(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingPhotoWithBody Update an Airbnb photo
	//
	// Change one photo's caption, its position in the tour, the room it is filed under, or its metadata. **Write-side** — calls Airbnb upstream.
	//
	// Airbnb's photo endpoints are keyed by photo id alone, so the photo is proven to belong to the listing named in the path before anything is sent; a photo from another listing returns `404`, the same answer a photo that does not exist gets.
	//
	// On success both stored copies are updated — the Airbnb mirror `GET /photos` serves AND the canonical photo tour behind `GET /v1/listings/{id}` — so a read straight after this write returns the new value instead of waiting for the next sync. `stored` says whether that succeeded; `false` means Airbnb accepted the change but our copy will only catch up at the next sync.
	//
	// To move several photos at once use `PUT /photos/order`: it is one call instead of N, it validates the whole order before writing anything, and it reports exactly what landed if Airbnb refuses part-way.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /v1/channels/airbnb/listings/{id}/photos (the `UpdateAirbnbListingPhoto` operationId).
	UpdateAirbnbListingPhotoWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingPhoto Update an Airbnb photo
	//
	// Change one photo's caption, its position in the tour, the room it is filed under, or its metadata. **Write-side** — calls Airbnb upstream.
	//
	// Airbnb's photo endpoints are keyed by photo id alone, so the photo is proven to belong to the listing named in the path before anything is sent; a photo from another listing returns `404`, the same answer a photo that does not exist gets.
	//
	// On success both stored copies are updated — the Airbnb mirror `GET /photos` serves AND the canonical photo tour behind `GET /v1/listings/{id}` — so a read straight after this write returns the new value instead of waiting for the next sync. `stored` says whether that succeeded; `false` means Airbnb accepted the change but our copy will only catch up at the next sync.
	//
	// To move several photos at once use `PUT /photos/order`: it is one call instead of N, it validates the whole order before writing anything, and it reports exactly what landed if Airbnb refuses part-way.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /v1/channels/airbnb/listings/{id}/photos (the `UpdateAirbnbListingPhoto` operationId).
	UpdateAirbnbListingPhoto(ctx context.Context, id string, body UpdateAirbnbListingPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UploadAirbnbListingPhotosWithBody Upload photos to Airbnb
	//
	// Upload one or more photos to an Airbnb listing.
	//
	// `image` is base64 image DATA, not a url — a `data:image/jpeg;base64,…` prefix is accepted and stripped, and the decoded image must be under 25 MB. (This operation previously documented public image urls that Airbnb would fetch. It never did: a url arrived at Airbnb as a ~60-byte image.)
	//
	// Airbnb assigns the photo id and CDN urls, so the newly uploaded photos appear in `GET /photos` after the next sync. Caption, order and room assignment can be set straight away with `PATCH /photos`, `PUT /photos/order` and `PUT /photos/cover`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id}/photos (the `UploadAirbnbListingPhotos` operationId).
	UploadAirbnbListingPhotosWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UploadAirbnbListingPhotos Upload photos to Airbnb
	//
	// Upload one or more photos to an Airbnb listing.
	//
	// `image` is base64 image DATA, not a url — a `data:image/jpeg;base64,…` prefix is accepted and stripped, and the decoded image must be under 25 MB. (This operation previously documented public image urls that Airbnb would fetch. It never did: a url arrived at Airbnb as a ~60-byte image.)
	//
	// Airbnb assigns the photo id and CDN urls, so the newly uploaded photos appear in `GET /photos` after the next sync. Caption, order and room assignment can be set straight away with `PATCH /photos`, `PUT /photos/order` and `PUT /photos/cover`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id}/photos (the `UploadAirbnbListingPhotos` operationId).
	UploadAirbnbListingPhotos(ctx context.Context, id string, body UploadAirbnbListingPhotosJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetAirbnbListingCoverPhotoWithBody Set the Airbnb cover photo
	//
	// Choose which photo leads the listing. **Write-side** — calls Airbnb upstream.
	//
	// Airbnb has no "cover" field: the cover is the first photo of the tour, so this is a position write. Usually it is a single upstream request — the chosen photo takes a position below the current first one and nothing else moves. When the tour already starts at position 1 and there is no room below it, the tour is renumbered instead, one request per photo whose position actually changes, with the same stop-at-first-failure reporting as `PUT /photos/order` (`applied`, `failed_photo_id`, `not_attempted`; re-send the identical body to finish).
	//
	// The listing thumbnail — what every list view renders — is repointed at the new cover, so the change is not visible only inside the photo tour.
	//
	// The photo must already be in our cached copy of the tour; one uploaded since the last sync returns `404`, and `PATCH /photos` can set its position in the meantime.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/cover (the `SetAirbnbListingCoverPhoto` operationId).
	SetAirbnbListingCoverPhotoWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetAirbnbListingCoverPhoto Set the Airbnb cover photo
	//
	// Choose which photo leads the listing. **Write-side** — calls Airbnb upstream.
	//
	// Airbnb has no "cover" field: the cover is the first photo of the tour, so this is a position write. Usually it is a single upstream request — the chosen photo takes a position below the current first one and nothing else moves. When the tour already starts at position 1 and there is no room below it, the tour is renumbered instead, one request per photo whose position actually changes, with the same stop-at-first-failure reporting as `PUT /photos/order` (`applied`, `failed_photo_id`, `not_attempted`; re-send the identical body to finish).
	//
	// The listing thumbnail — what every list view renders — is repointed at the new cover, so the change is not visible only inside the photo tour.
	//
	// The photo must already be in our cached copy of the tour; one uploaded since the last sync returns `404`, and `PATCH /photos` can set its position in the meantime.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/cover (the `SetAirbnbListingCoverPhoto` operationId).
	SetAirbnbListingCoverPhoto(ctx context.Context, id string, body SetAirbnbListingCoverPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReorderAirbnbListingPhotosWithBody Reorder the Airbnb photo tour
	//
	// Set the order of a listing's photo tour in one call. **Write-side** — calls Airbnb upstream.
	//
	// Send the photo ids in the order you want them shown, first photo first. Ids you leave out keep their current relative order behind the ones you list, so moving one photo to the front is `{"photo_ids": ["<id>"]}`. Positions are then written as a dense run starting at 1.
	//
	// Airbnb has no bulk photo endpoint — order is one `sort_order` per photo — so this saves a loop of up to 200 requests against your rate limit and makes the partial-failure case reportable. How much is atomic:
	//
	// - Everything is validated before anything is written. A duplicate id, an id that is not on this listing, an inactive listing or a missing connection all fail with **zero** upstream writes.
	// - Only photos whose position actually changes are written; re-sending the order you already have writes nothing.
	// - On the first upstream failure the run stops — nothing after it is attempted. The error carries `applied`, `failed_photo_id` and `not_attempted`, uses Airbnb's own status (`422` rejected / `403` reauth / `429` / `502`), and the operation is idempotent: re-send the identical body to finish the run.
	// - Our stored copy records what actually landed, never the intent.
	//
	// Validation is against our cached copy of the tour, so a listing whose photos have never synced returns `404` — use `PATCH /photos` (which needs no cache) until the first sync lands.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/order (the `ReorderAirbnbListingPhotos` operationId).
	ReorderAirbnbListingPhotosWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReorderAirbnbListingPhotos Reorder the Airbnb photo tour
	//
	// Set the order of a listing's photo tour in one call. **Write-side** — calls Airbnb upstream.
	//
	// Send the photo ids in the order you want them shown, first photo first. Ids you leave out keep their current relative order behind the ones you list, so moving one photo to the front is `{"photo_ids": ["<id>"]}`. Positions are then written as a dense run starting at 1.
	//
	// Airbnb has no bulk photo endpoint — order is one `sort_order` per photo — so this saves a loop of up to 200 requests against your rate limit and makes the partial-failure case reportable. How much is atomic:
	//
	// - Everything is validated before anything is written. A duplicate id, an id that is not on this listing, an inactive listing or a missing connection all fail with **zero** upstream writes.
	// - Only photos whose position actually changes are written; re-sending the order you already have writes nothing.
	// - On the first upstream failure the run stops — nothing after it is attempted. The error carries `applied`, `failed_photo_id` and `not_attempted`, uses Airbnb's own status (`422` rejected / `403` reauth / `429` / `502`), and the operation is idempotent: re-send the identical body to finish the run.
	// - Our stored copy records what actually landed, never the intent.
	//
	// Validation is against our cached copy of the tour, so a listing whose photos have never synced returns `404` — use `PATCH /photos` (which needs no cache) until the first sync lands.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/order (the `ReorderAirbnbListingPhotos` operationId).
	ReorderAirbnbListingPhotos(ctx context.Context, id string, body ReorderAirbnbListingPhotosJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbListingPricing Get Airbnb pricing
	//
	// Read the current pricing config (base price, weekend uplift, length-of-stay discounts, smart-pricing bounds) for an Airbnb listing.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/pricing (the `GetAirbnbListingPricing` operationId).
	GetAirbnbListingPricing(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingPricingWithBody Update Airbnb pricing
	//
	// Push pricing changes to Airbnb. The `type` discriminator selects the sub-resource (model, standard settings, LOS, rate-plan, fees, currency, rule, or per-date `calendar`). `type: "calendar"` carries the full per-date restriction set — nightly price, min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`). For settings sub-resources the full object is replaced — GET first, mutate locally, then PUT the whole object.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
	//
	// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
	//
	// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/pricing (the `UpdateAirbnbListingPricing` operationId).
	UpdateAirbnbListingPricingWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingPricing Update Airbnb pricing
	//
	// Push pricing changes to Airbnb. The `type` discriminator selects the sub-resource (model, standard settings, LOS, rate-plan, fees, currency, rule, or per-date `calendar`). `type: "calendar"` carries the full per-date restriction set — nightly price, min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`). For settings sub-resources the full object is replaced — GET first, mutate locally, then PUT the whole object.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
	//
	// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
	//
	// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/pricing (the `UpdateAirbnbListingPricing` operationId).
	UpdateAirbnbListingPricing(ctx context.Context, id string, body UpdateAirbnbListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbListingQuality Get Airbnb listing quality
	//
	// Return an Airbnb listing's quality signals — standards, reservation issues, and monthly quality stats. **Pure DB read** from the local quality mirrors. Scope the response with `?type=all|standards|issues|stats` (default `all`, which returns `{ standards, issues }`). Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/quality (the `GetAirbnbListingQuality` operationId).
	GetAirbnbListingQuality(ctx context.Context, id string, params *GetAirbnbListingQualityParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteAirbnbListingRoom Delete an Airbnb room
	//
	// Delete a room from an Airbnb listing, and its beds with it. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=`. Requires a connected Airbnb host, else `404 no_connection`.
	//
	// The room is proven to belong to the listing named in the path first; a room from another listing returns `404`. Both stored copies drop the room on success — `stored` reports whether that succeeded.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with DELETE /v1/channels/airbnb/listings/{id}/rooms (the `DeleteAirbnbListingRoom` operationId).
	DeleteAirbnbListingRoom(ctx context.Context, id string, params *DeleteAirbnbListingRoomParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbListingRooms List Airbnb rooms
	//
	// List the rooms configured on an Airbnb listing, ordered by room number, each with its sleeping arrangement in `beds`. **Pure DB read** from `listings_airbnb_rooms` + `listings_airbnb_beds`. Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/rooms (the `ListAirbnbListingRooms` operationId).
	ListAirbnbListingRooms(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateAirbnbListingRoomWithBody Create an Airbnb room
	//
	// Create a new room on an Airbnb listing, with its sleeping arrangement. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host, else `404 no_connection`.
	//
	// The response is the room object as Airbnb returned it. The new room is also seeded into our own copy, so the very next `GET /rooms` shows it rather than waiting for the sync.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id}/rooms (the `CreateAirbnbListingRoom` operationId).
	CreateAirbnbListingRoomWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateAirbnbListingRoom Create an Airbnb room
	//
	// Create a new room on an Airbnb listing, with its sleeping arrangement. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host, else `404 no_connection`.
	//
	// The response is the room object as Airbnb returned it. The new room is also seeded into our own copy, so the very next `GET /rooms` shows it rather than waiting for the sync.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id}/rooms (the `CreateAirbnbListingRoom` operationId).
	CreateAirbnbListingRoom(ctx context.Context, id string, body CreateAirbnbListingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingRoomWithBody Update an Airbnb room
	//
	// Change a room's type, number, privacy or sleeping arrangement. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=` and send only the fields you want to change.
	//
	// `beds` REPLACES the room's whole arrangement — that is Airbnb's semantics for the field — so send every bed the room has, not just the changed one.
	//
	// Airbnb's room endpoints are keyed by room id alone, so the room is proven to belong to the listing named in the path before anything is sent; a room from another listing returns `404`, the same answer a room that does not exist gets.
	//
	// On success both stored copies are rebuilt to match, so a read straight after this write returns the new arrangement. `stored` says whether that succeeded.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/rooms (the `UpdateAirbnbListingRoom` operationId).
	UpdateAirbnbListingRoomWithBody(ctx context.Context, id string, params *UpdateAirbnbListingRoomParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingRoom Update an Airbnb room
	//
	// Change a room's type, number, privacy or sleeping arrangement. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=` and send only the fields you want to change.
	//
	// `beds` REPLACES the room's whole arrangement — that is Airbnb's semantics for the field — so send every bed the room has, not just the changed one.
	//
	// Airbnb's room endpoints are keyed by room id alone, so the room is proven to belong to the listing named in the path before anything is sent; a room from another listing returns `404`, the same answer a room that does not exist gets.
	//
	// On success both stored copies are rebuilt to match, so a read straight after this write returns the new arrangement. `stored` says whether that succeeded.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/rooms (the `UpdateAirbnbListingRoom` operationId).
	UpdateAirbnbListingRoom(ctx context.Context, id string, params *UpdateAirbnbListingRoomParams, body UpdateAirbnbListingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbListingSafetyDisclosures List guest-safety disclosures
	//
	// What a guest is told about the property before they book — exterior security cameras, a decibel noise monitor, pets on the property, stairs, a pool with no fence, weapons, shared spaces, limited parking. Airbnb calls them `listing_expectations_for_guests` and shows them at booking time.
	//
	// **Pure DB read** from the local mirror. EVERY supported disclosure type is returned, including the ones this listing has not declared (`value: false`), so "does this property have cameras?" has an answer rather than a missing key — `declared` tells you whether Airbnb holds an explicit answer. Types Airbnb returns that are not in the documented set are passed through rather than dropped.
	//
	// Where a listing is connected to several Airbnb listings, a disclosure declared on any of them is reported as true of the property.
	//
	// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/safety-disclosures (the `ListAirbnbListingSafetyDisclosures` operationId).
	ListAirbnbListingSafetyDisclosures(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingSafetyDisclosuresWithBody Update guest-safety disclosures
	//
	// Declare or retract the guest-safety disclosures on the live Airbnb listing.
	//
	// **This is a MERGE, not a replacement.** Airbnb keeps one value per disclosure type: a type you leave out keeps the value it has, and to retract one you send it with `value: false`. A full replacement would let a partial request silently un-declare a security camera — a guest-safety statement, not a preference.
	//
	// Only the disclosures are sent upstream. The same Airbnb endpoint carries the cancellation policy and instant-book settings, and this endpoint never touches them.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Airbnb refusing the change is `422 airbnb_rejected` with Airbnb's own reason; an expired or revoked connection is `403 connection_reauth_required`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/safety-disclosures (the `UpdateAirbnbListingSafetyDisclosures` operationId).
	UpdateAirbnbListingSafetyDisclosuresWithBody(ctx context.Context, id string, params *UpdateAirbnbListingSafetyDisclosuresParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbListingSafetyDisclosures Update guest-safety disclosures
	//
	// Declare or retract the guest-safety disclosures on the live Airbnb listing.
	//
	// **This is a MERGE, not a replacement.** Airbnb keeps one value per disclosure type: a type you leave out keeps the value it has, and to retract one you send it with `value: false`. A full replacement would let a partial request silently un-declare a security camera — a guest-safety statement, not a preference.
	//
	// Only the disclosures are sent upstream. The same Airbnb endpoint carries the cancellation policy and instant-book settings, and this endpoint never touches them.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Airbnb refusing the change is `422 airbnb_rejected` with Airbnb's own reason; an expired or revoked connection is `403 connection_reauth_required`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/safety-disclosures (the `UpdateAirbnbListingSafetyDisclosures` operationId).
	UpdateAirbnbListingSafetyDisclosures(ctx context.Context, id string, params *UpdateAirbnbListingSafetyDisclosuresParams, body UpdateAirbnbListingSafetyDisclosuresJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbListingSettings Get Airbnb listing settings
	//
	// Return an Airbnb listing's host roles, published locales, and regulatory permits. **Pure DB read** — host roles from `listings_airbnb_details.host_roles`, locales from distinct `listings_airbnb_descriptions.locale`, permits from `listings_airbnb_permits`. Scope with `?type=all|hosts|permits|locales` (default `all`, which returns `{ hosts, locales }`). Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/settings (the `GetAirbnbListingSettings` operationId).
	GetAirbnbListingSettings(ctx context.Context, id string, params *GetAirbnbListingSettingsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbThreads List Airbnb message threads
	//
	// List Airbnb message threads (one per guest conversation). Cursor-paginated. Each thread includes a preview of the latest message.
	//
	// Threads on inactive listings are left out; they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Corresponds with GET /v1/channels/airbnb/messaging (the `ListAirbnbThreads` operationId).
	ListAirbnbThreads(ctx context.Context, params *ListAirbnbThreadsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbThread Get Airbnb thread
	//
	// Fetch a single Airbnb message thread by its Airbnb thread id. **Pure DB read** from the local `message_threads` mirror, workspace-scoped. Returns `404 not_found` when no thread matches. For the messages within a thread use `GET /v1/channels/airbnb/messaging/{threadId}/messages`.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/messaging/{threadId} (the `GetAirbnbThread` operationId).
	GetAirbnbThread(ctx context.Context, threadId string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbThreadMessages Get Airbnb messages
	//
	// Messages stored for an Airbnb thread, as recorded rows (not the unified `Message` shape — use `GET /v1/conversations/{id}/messages` for that). By default returns 50 per page, newest first; walk older pages with `?cursor=` (the `pagination.nextCursor` of the previous page) until `pagination.hasMore` is `false`. `?all=true` returns up to 1000 rows oldest-first in one response, with no `pagination`.
	//
	// Each row carries `attachments` — photos and other files on that message, inbound or outbound — in the same shape as the unified endpoint.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/messaging/{threadId}/messages (the `ListAirbnbThreadMessages` operationId).
	ListAirbnbThreadMessages(ctx context.Context, threadId string, params *ListAirbnbThreadMessagesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SendAirbnbMessageWithBody Send Airbnb message
	//
	// Send a message in an Airbnb thread as the host. Airbnb enforces content rules (no off-platform contact info, no external URLs) — violating messages are rejected upstream and surface as `422 airbnb_rejected` carrying Airbnb's own reason. Resending the same text is refused again; edit it first. `502 airbnb_error` is the other answer and means something else entirely: Airbnb did not complete the send, so retry it unchanged.
	//
	// ### Sending a photo or video (`mediaUrl`)
	//
	// Airbnb only accepts media uploaded to a signed URL it issues, one file per message and no text on the same message. With `mediaUrl`, Repull downloads the file (public `https://` only, 10 MB max), reads its real type from the bytes (JPEG, PNG, GIF, WebP — converted to JPEG — or MP4/QuickTime), uploads it to Airbnb and sends it; `message`, if given, follows as a separate message. This is the same flow as `POST /v1/conversations/{id}/messages` with `attachments` — prefer that endpoint, which also takes several files per request. The response is a `SendMessageResponse`, the send is recorded in the conversation, and failures are the 422 codes documented there (`attachment_type_not_supported`, `attachment_too_large`, `message_not_sent` for a pre-booking thread, …). The thread must already be synced to Repull (`GET /v1/conversations` lists them), otherwise `404`.
	//
	// Text-only sends (no `mediaUrl`) go straight to Airbnb and return Airbnb's message object.
	//
	// The `{threadId}` is the Airbnb thread id — the `externalThreadId` field on a unified `Conversation` (`GET /v1/conversations`).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/messaging/{threadId}/messages (the `SendAirbnbMessage` operationId).
	SendAirbnbMessageWithBody(ctx context.Context, threadId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SendAirbnbMessage Send Airbnb message
	//
	// Send a message in an Airbnb thread as the host. Airbnb enforces content rules (no off-platform contact info, no external URLs) — violating messages are rejected upstream and surface as `422 airbnb_rejected` carrying Airbnb's own reason. Resending the same text is refused again; edit it first. `502 airbnb_error` is the other answer and means something else entirely: Airbnb did not complete the send, so retry it unchanged.
	//
	// ### Sending a photo or video (`mediaUrl`)
	//
	// Airbnb only accepts media uploaded to a signed URL it issues, one file per message and no text on the same message. With `mediaUrl`, Repull downloads the file (public `https://` only, 10 MB max), reads its real type from the bytes (JPEG, PNG, GIF, WebP — converted to JPEG — or MP4/QuickTime), uploads it to Airbnb and sends it; `message`, if given, follows as a separate message. This is the same flow as `POST /v1/conversations/{id}/messages` with `attachments` — prefer that endpoint, which also takes several files per request. The response is a `SendMessageResponse`, the send is recorded in the conversation, and failures are the 422 codes documented there (`attachment_type_not_supported`, `attachment_too_large`, `message_not_sent` for a pre-booking thread, …). The thread must already be synced to Repull (`GET /v1/conversations` lists them), otherwise `404`.
	//
	// Text-only sends (no `mediaUrl`) go straight to Airbnb and return Airbnb's message object.
	//
	// The `{threadId}` is the Airbnb thread id — the `externalThreadId` field on a unified `Conversation` (`GET /v1/conversations`).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/messaging/{threadId}/messages (the `SendAirbnbMessage` operationId).
	SendAirbnbMessage(ctx context.Context, threadId string, body SendAirbnbMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbMessageWithBody Edit / react to / mark an Airbnb message
	//
	// Act on a single message in an Airbnb thread. **Write-side** — calls Airbnb upstream. The `action` discriminator selects the operation:
	//
	// - `edit` — replace message text (requires `message`).
	// - `unsend` — retract the message.
	// - `read` — mark the message as read.
	// - `react` — add a reaction (requires `reaction`).
	//
	// Requires a connected Airbnb host, else `404 no_connection`.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /v1/channels/airbnb/messaging/{threadId}/messages/{messageId} (the `UpdateAirbnbMessage` operationId).
	UpdateAirbnbMessageWithBody(ctx context.Context, threadId string, messageId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateAirbnbMessage Edit / react to / mark an Airbnb message
	//
	// Act on a single message in an Airbnb thread. **Write-side** — calls Airbnb upstream. The `action` discriminator selects the operation:
	//
	// - `edit` — replace message text (requires `message`).
	// - `unsend` — retract the message.
	// - `read` — mark the message as read.
	// - `react` — add a reaction (requires `reaction`).
	//
	// Requires a connected Airbnb host, else `404 no_connection`.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /v1/channels/airbnb/messaging/{threadId}/messages/{messageId} (the `UpdateAirbnbMessage` operationId).
	UpdateAirbnbMessage(ctx context.Context, threadId string, messageId string, body UpdateAirbnbMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// WithdrawAirbnbOffer Withdraw Airbnb special offer
	//
	// Withdraw a special offer the guest has not booked. **Write-side** — calls Airbnb upstream. Pass the Airbnb offer id as `?offerId=`. The Repull-id equivalent is `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
	//
	// Corresponds with DELETE /v1/channels/airbnb/offers (the `WithdrawAirbnbOffer` operationId).
	WithdrawAirbnbOffer(ctx context.Context, params *WithdrawAirbnbOfferParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbOffer Get Airbnb special offer
	//
	// Read a pre-approval or special offer from Airbnb by its Airbnb id. **Live read** — calls Airbnb upstream. Pass the id as `?offerId=`. The Repull-id equivalent is `GET /v1/conversations/{id}/special-offers/{offerId}`, which also confirms the offer belongs to that conversation.
	//
	// Corresponds with GET /v1/channels/airbnb/offers (the `GetAirbnbOffer` operationId).
	GetAirbnbOffer(ctx context.Context, params *GetAirbnbOfferParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateAirbnbOfferWithBody Create Airbnb special offer or pre-approval
	//
	// Create a pre-approval or a special offer on an Airbnb thread, addressed by **Airbnb** ids. **Write-side** — calls Airbnb upstream. The Repull-id equivalents, which also update the inquiry in Vanio, are `POST /v1/conversations/{id}/pre-approval` and `POST /v1/conversations/{id}/special-offers` — prefer those unless you only hold Airbnb ids.
	//
	// - `type: "preapproval"` — let the guest book the dates and price they asked about. Requires `thread_id`; optional `block_instant_booking`.
	// - `type: "offer"` — your own terms. Requires `thread_id`, `listing_id` (the **Airbnb** listing id, as a string), `start_date`, `nights`, `total_price` (whole stay, listing currency) and `guest_details` with `number_of_guests` (or `number_of_adults`; Airbnb counts adults + children).
	//
	// The body is validated before anything reaches Airbnb (a `422 invalid_params` names the field), and unknown fields are refused. The legacy spellings `threadId` and `blockInstantBooking` still work. The request is sent as the Airbnb account that owns the thread or listing.
	//
	// Airbnb refusals are mapped rather than returned as a 500: `409 inquiry_no_longer_open` / `inquiry_expired` when the inquiry moved on, `422 airbnb_rejected` with Airbnb’s reason otherwise, `403 connection_reauth_required` when the grant does not allow it.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/offers (the `CreateAirbnbOffer` operationId).
	CreateAirbnbOfferWithBody(ctx context.Context, params *CreateAirbnbOfferParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateAirbnbOffer Create Airbnb special offer or pre-approval
	//
	// Create a pre-approval or a special offer on an Airbnb thread, addressed by **Airbnb** ids. **Write-side** — calls Airbnb upstream. The Repull-id equivalents, which also update the inquiry in Vanio, are `POST /v1/conversations/{id}/pre-approval` and `POST /v1/conversations/{id}/special-offers` — prefer those unless you only hold Airbnb ids.
	//
	// - `type: "preapproval"` — let the guest book the dates and price they asked about. Requires `thread_id`; optional `block_instant_booking`.
	// - `type: "offer"` — your own terms. Requires `thread_id`, `listing_id` (the **Airbnb** listing id, as a string), `start_date`, `nights`, `total_price` (whole stay, listing currency) and `guest_details` with `number_of_guests` (or `number_of_adults`; Airbnb counts adults + children).
	//
	// The body is validated before anything reaches Airbnb (a `422 invalid_params` names the field), and unknown fields are refused. The legacy spellings `threadId` and `blockInstantBooking` still work. The request is sent as the Airbnb account that owns the thread or listing.
	//
	// Airbnb refusals are mapped rather than returned as a 500: `409 inquiry_no_longer_open` / `inquiry_expired` when the inquiry moved on, `422 airbnb_rejected` with Airbnb’s reason otherwise, `403 connection_reauth_required` when the grant does not allow it.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/offers (the `CreateAirbnbOffer` operationId).
	CreateAirbnbOffer(ctx context.Context, params *CreateAirbnbOfferParams, body CreateAirbnbOfferJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbReservations List Airbnb reservations
	//
	// Cursor-paginated list of reservations sourced directly from Airbnb. Use this when you need Airbnb-specific fields (guest payout split, cancellation policy snapshot) that the unified `/v1/reservations` endpoint flattens away.
	//
	// Walk pages with `?cursor=<pagination.nextCursor>` until `pagination.hasMore` is `false`. The cursor is opaque — never construct or parse it client-side.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. Internally this walks upstream Airbnb cursor pages to skip rows, so deep offsets cost N/limit upstream round-trips; cursor remains the better choice for deep pagination.
	//
	// When `status` is omitted, all statuses are returned (Airbnb defaults to `accepted` only on its own surface, but this endpoint normalises to "all"). Pass `?status=accepted` to scope.
	//
	// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Corresponds with GET /v1/channels/airbnb/reservations (the `ListAirbnbReservations` operationId).
	ListAirbnbReservations(ctx context.Context, params *ListAirbnbReservationsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAirbnbReservation Get Airbnb reservation
	//
	// Fetch a single Airbnb reservation by Airbnb confirmation code (e.g. `HMABCDEF12`).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/airbnb/reservations/{code} (the `GetAirbnbReservation` operationId).
	GetAirbnbReservation(ctx context.Context, code string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AirbnbReservationActionWithBody Accept, decline or cancel an Airbnb reservation
	//
	// Act on an Airbnb reservation by its Airbnb confirmation code. **Write-side** — calls Airbnb upstream, as the Airbnb account that owns the booking.
	//
	// - `accept` — accept a pending booking request.
	// - `decline` — decline a pending booking request. Requires `reason` (one of Airbnb's decline reasons) and `message` (sent to the guest, at most 500 characters).
	// - `cancel` — cancel a confirmed booking as the host. Requires `reason` (one of Airbnb's host-cancellation reasons). **Host cancellations carry Airbnb penalties.**
	//
	// The body is validated before anything reaches Airbnb; unknown fields are refused. There is no `pre-approve` action: a pre-approval answers an inquiry, which has no confirmation code — use `POST /v1/conversations/{id}/pre-approval`. For accept/decline, `POST /v1/reservations/{id}/accept` and `/decline` do the same by Repull id and keep the reservation in Repull in sync.
	//
	// Airbnb refusals are mapped rather than returned as a 500: a request that already moved on is `409 request_no_longer_pending` (do not retry), an expired one `409 request_expired`, any other refusal `422 airbnb_rejected` with Airbnb's reason.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/reservations/{code} (the `AirbnbReservationAction` operationId).
	AirbnbReservationActionWithBody(ctx context.Context, code string, params *AirbnbReservationActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AirbnbReservationAction Accept, decline or cancel an Airbnb reservation
	//
	// Act on an Airbnb reservation by its Airbnb confirmation code. **Write-side** — calls Airbnb upstream, as the Airbnb account that owns the booking.
	//
	// - `accept` — accept a pending booking request.
	// - `decline` — decline a pending booking request. Requires `reason` (one of Airbnb's decline reasons) and `message` (sent to the guest, at most 500 characters).
	// - `cancel` — cancel a confirmed booking as the host. Requires `reason` (one of Airbnb's host-cancellation reasons). **Host cancellations carry Airbnb penalties.**
	//
	// The body is validated before anything reaches Airbnb; unknown fields are refused. There is no `pre-approve` action: a pre-approval answers an inquiry, which has no confirmation code — use `POST /v1/conversations/{id}/pre-approval`. For accept/decline, `POST /v1/reservations/{id}/accept` and `/decline` do the same by Repull id and keep the reservation in Repull in sync.
	//
	// Airbnb refusals are mapped rather than returned as a 500: a request that already moved on is `409 request_no_longer_pending` (do not retry), an expired one `409 request_expired`, any other refusal `422 airbnb_rejected` with Airbnb's reason.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/reservations/{code} (the `AirbnbReservationAction` operationId).
	AirbnbReservationAction(ctx context.Context, code string, params *AirbnbReservationActionParams, body AirbnbReservationActionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbReviews List Airbnb reviews
	//
	// List reviews left by guests on Airbnb listings in this workspace. Includes both reviews of the host and reviews of the guest (where the host has not yet submitted theirs).
	//
	// Reviews of inactive listings are left out; they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Corresponds with GET /v1/channels/airbnb/reviews (the `ListAirbnbReviews` operationId).
	ListAirbnbReviews(ctx context.Context, params *ListAirbnbReviewsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RespondAirbnbReviewLegacy Respond to / submit Airbnb review (legacy)
	//
	// Legacy action-based shape. Body `{ action: "respond"|"submit", reviewId, response?, review? }`. Kept for backwards compatibility — prefer `PUT /v1/channels/airbnb/reviews/{id}` (edit) and `POST /v1/channels/airbnb/reviews/{id}/respond` (reply) for new integrations.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with POST /v1/channels/airbnb/reviews (the `RespondAirbnbReviewLegacy` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	RespondAirbnbReviewLegacy(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// EditAirbnbReviewWithBody Edit Airbnb host review
	//
	// Edit a host-side review for an Airbnb stay. Airbnb collapses POST + PUT into the same upstream call (`PUT /v2/listing_reviews/{id}`), so this endpoint covers both initial submit and subsequent edits while the review window is open.
	//
	// Body is a partial `AirbnbReview` — pass the fields you want to change (rating, public review, private feedback, category ratings).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/reviews/{id} (the `EditAirbnbReview` operationId).
	EditAirbnbReviewWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// EditAirbnbReview Edit Airbnb host review
	//
	// Edit a host-side review for an Airbnb stay. Airbnb collapses POST + PUT into the same upstream call (`PUT /v2/listing_reviews/{id}`), so this endpoint covers both initial submit and subsequent edits while the review window is open.
	//
	// Body is a partial `AirbnbReview` — pass the fields you want to change (rating, public review, private feedback, category ratings).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/airbnb/reviews/{id} (the `EditAirbnbReview` operationId).
	EditAirbnbReview(ctx context.Context, id string, body EditAirbnbReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RespondAirbnbReviewWithBody Respond to Airbnb review
	//
	// Post a public host response to a guest review. Airbnb allows one response per review — repeated POSTs return 409. Response text is capped at 1000 characters.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/reviews/{id}/respond (the `RespondAirbnbReview` operationId).
	RespondAirbnbReviewWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RespondAirbnbReview Respond to Airbnb review
	//
	// Post a public host response to a guest review. Airbnb allows one response per review — repeated POSTs return 409. Response text is capped at 1000 characters.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/reviews/{id}/respond (the `RespondAirbnbReview` operationId).
	RespondAirbnbReview(ctx context.Context, id string, body RespondAirbnbReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListAirbnbTransactions List Airbnb transactions
	//
	// List Airbnb host transactions (reservation earnings, payouts, resolution adjustments) for this workspace, newest first. **Pure DB read** — customer-facing reads never call Airbnb upstream; they serve the `airbnb_transactions` mirror. Each row carries the genuine host- and guest-side financial breakdown (accommodation subtotal, cleaning fee, host + guest service fees split base/VAT, tax buckets, expected/actual host payout with settlement status). Trigger a refresh with `POST` on this path. When the mirror is empty or the host disconnected, `dataFreshness.stale = true` with a `reason` (`never_synced`, `host_disconnected_<iso>`, `sync_lag_>_24h`).
	//
	// Transactions of reservations on inactive listings are left out; payout rows, which belong to no listing, are always included.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Corresponds with GET /v1/channels/airbnb/transactions (the `ListAirbnbTransactions` operationId).
	ListAirbnbTransactions(ctx context.Context, params *ListAirbnbTransactionsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SyncAirbnbTransactionsWithBody Sync Airbnb transactions
	//
	// Refresh the Airbnb transactions mirror for this workspace by pulling from Airbnb upstream and upserting the breakdown that `GET` serves. Optional JSON body `{ start_date, end_date, transaction_type }` (`transaction_type` is `COMPLETED` or `UPCOMING`; both are synced when omitted). Returns `{ synced, count }`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/airbnb/transactions (the `SyncAirbnbTransactions` operationId).
	SyncAirbnbTransactionsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SyncAirbnbTransactions Sync Airbnb transactions
	//
	// Refresh the Airbnb transactions mirror for this workspace by pulling from Airbnb upstream and upserting the breakdown that `GET` serves. Optional JSON body `{ start_date, end_date, transaction_type }` (`transaction_type` is `COMPLETED` or `UPCOMING`; both are synced when omitted). Returns `{ synced, count }`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/airbnb/transactions (the `SyncAirbnbTransactions` operationId).
	SyncAirbnbTransactions(ctx context.Context, body SyncAirbnbTransactionsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetBookingAvailability Read current Booking.com rates/availability/restrictions
	//
	// Read the current rate, availability, and restriction state for a Booking.com property so you can reconcile before writing with the PUT on this path. Keyed by `property_id` (the Booking hotel id), symmetric with the PUT.
	//
	// Proxies Booking's `getRoomRateAvailability` — the returned fields (price, rooms-to-sell, min/max stay, closed-to-arrival/departure, stop-sell) are whatever Booking.com emits for the window. A listing-id-keyed equivalent is available at `GET /v1/channels/booking/listings/{id}/pricing`.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/booking/availability (the `GetBookingAvailability` operationId).
	GetBookingAvailability(ctx context.Context, params *GetBookingAvailabilityParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateBookingAvailabilityWithBody Update Booking.com rates/availability
	//
	// Write rates, availability and restrictions to a Booking.com property. `type` selects the write:
	//
	// - `rates` — nightly prices, plus any length-of-stay / arrival restrictions sent with them.
	// - `availability` — inventory (`availableRooms`), the stop-sell flag (`closed`), and restrictions. Omit `availableRooms` and `closed` for a restriction-only write.
	// - `derived-pricing` — occupancy-derived pricing rules.
	//
	// **Dates are inclusive at both ends.** `{ "start": "2026-11-04", "end": "2026-11-04" }` is exactly one night.
	//
	// **A rate amount needs an occupancy.** Booking.com stores the amount against the party size the rate plan prices: sent above that number it declines the price in silence, sent below it it answers 400. Send `occupancy`, or omit it and Repull resolves it from Booking.com's own data and echoes the value and its `source` back in `occupancy[]`. If it cannot be resolved the write is refused with `422` naming `updates[N].occupancy`.
	//
	// **Restrictions are sent in the same call, on their own wire.** A price and a minimum stay are two writes on Booking.com's side. Send them together and the response reports each separately: `price` and `restrictions` carry their own state, their own read-back, and — when refused — Booking.com's own reason. The top-level `applied` is `partial` when they disagree, so a price that landed is never reported as a failure. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` because Booking.com's notification has no element for them — set those on the rate plan in the Extranet. Nothing you send is ever silently ignored.
	//
	// **Inventory is not part of a rate update.** `roomsToSell` on a `rates` update returns `422 inventory_not_in_rate_update`; send it as `type: "availability"` instead.
	//
	// **The response says what is known.** Booking.com acknowledges a write with no per-date status, so the nights are read back — prices and restrictions out of the same read: `applied` is `verified`, `mismatch`, `partial`, `rejected` or `unverified` (send `verify: false` to skip the read-back). A bare acknowledgement is never reported as "all updates applied". Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
	//
	// Restrictions never leak across channels — this endpoint writes only to Booking.com. When Booking.com refuses a write outright, their own reason comes back as `422 booking_rejected` with `booking_ruid`; a genuine outage on their side is `502 booking_error`.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/booking/availability (the `UpdateBookingAvailability` operationId).
	UpdateBookingAvailabilityWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateBookingAvailability Update Booking.com rates/availability
	//
	// Write rates, availability and restrictions to a Booking.com property. `type` selects the write:
	//
	// - `rates` — nightly prices, plus any length-of-stay / arrival restrictions sent with them.
	// - `availability` — inventory (`availableRooms`), the stop-sell flag (`closed`), and restrictions. Omit `availableRooms` and `closed` for a restriction-only write.
	// - `derived-pricing` — occupancy-derived pricing rules.
	//
	// **Dates are inclusive at both ends.** `{ "start": "2026-11-04", "end": "2026-11-04" }` is exactly one night.
	//
	// **A rate amount needs an occupancy.** Booking.com stores the amount against the party size the rate plan prices: sent above that number it declines the price in silence, sent below it it answers 400. Send `occupancy`, or omit it and Repull resolves it from Booking.com's own data and echoes the value and its `source` back in `occupancy[]`. If it cannot be resolved the write is refused with `422` naming `updates[N].occupancy`.
	//
	// **Restrictions are sent in the same call, on their own wire.** A price and a minimum stay are two writes on Booking.com's side. Send them together and the response reports each separately: `price` and `restrictions` carry their own state, their own read-back, and — when refused — Booking.com's own reason. The top-level `applied` is `partial` when they disagree, so a price that landed is never reported as a failure. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` because Booking.com's notification has no element for them — set those on the rate plan in the Extranet. Nothing you send is ever silently ignored.
	//
	// **Inventory is not part of a rate update.** `roomsToSell` on a `rates` update returns `422 inventory_not_in_rate_update`; send it as `type: "availability"` instead.
	//
	// **The response says what is known.** Booking.com acknowledges a write with no per-date status, so the nights are read back — prices and restrictions out of the same read: `applied` is `verified`, `mismatch`, `partial`, `rejected` or `unverified` (send `verify: false` to skip the read-back). A bare acknowledgement is never reported as "all updates applied". Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
	//
	// Restrictions never leak across channels — this endpoint writes only to Booking.com. When Booking.com refuses a write outright, their own reason comes back as `422 booking_rejected` with `booking_ruid`; a genuine outage on their side is `502 booking_error`.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/booking/availability (the `UpdateBookingAvailability` operationId).
	UpdateBookingAvailability(ctx context.Context, body UpdateBookingAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetBookingCharges Get Booking.com charges
	//
	// Fetch the extra-charge set (cleaning fee, resort fee, city tax, etc.) configured for a Booking.com property. Pass the Booking.com `property_id` as a query param — required.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/booking/charges (the `GetBookingCharges` operationId).
	GetBookingCharges(ctx context.Context, params *GetBookingChargesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateBookingChargesWithBody Set Booking.com charges
	//
	// Replace the extra-charge set for a Booking.com property. The body carries the target `property_id` and the full `charges` array — Booking treats the write as a full replacement, so include every charge you want to keep.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/booking/charges (the `UpdateBookingCharges` operationId).
	UpdateBookingChargesWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateBookingCharges Set Booking.com charges
	//
	// Replace the extra-charge set for a Booking.com property. The body carries the target `property_id` and the full `charges` array — Booking treats the write as a full replacement, so include every charge you want to keep.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/booking/charges (the `UpdateBookingCharges` operationId).
	UpdateBookingCharges(ctx context.Context, body UpdateBookingChargesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetBookingContent Get Booking.com content
	//
	// Read one kind of content for a Booking.com property, straight from Booking.com.
	//
	// | `type` | What it is |
	// |---|---|
	// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
	// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
	// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
	// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
	// | `policies` | Cancellation and prepayment policies. |
	// | `licences` | The region's licence rules and the licence on file. |
	// | `checkin_methods` | How guests get in (holiday homes). |
	// | `contacts` | Who Booking.com contacts about the property. |
	//
	// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/booking/content (the `GetBookingContent` operationId).
	GetBookingContent(ctx context.Context, params *GetBookingContentParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateBookingContentWithBody Update Booking.com content
	//
	// Write one kind of content to a Booking.com property only. Nothing on the canonical listing or on Airbnb changes. To send the listing's own content to every channel instead, use `PUT /v1/listings/{id}/content` and publish.
	//
	// | `type` | What it is |
	// |---|---|
	// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
	// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
	// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
	// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
	// | `policies` | Cancellation and prepayment policies. |
	// | `licences` | The region's licence rules and the licence on file. |
	// | `checkin_methods` | How guests get in (holiday homes). |
	// | `contacts` | Who Booking.com contacts about the property. |
	//
	// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
	//
	// What each `type` takes:
	//
	// - `description`: `text`, optional `language` (default `en`).
	// - `facilities`: `facilities: [{ facility_id | room_facility_id, state: "PRESENT" | "MISSING", instances? }]`. Facilities you do not send stay as they are.
	// - `photos`: `photos: [{ url }]`, uploaded in the background. With `room_id`, send `photo_ids` instead to add photos that have finished processing to that room.
	// - `settings`: `settings: { <block>: {…} }`, for example `{ "pets": { "pets_allowed": "PETS_ALLOWED" } }`. Each block is written separately and reported in `results`.
	// - `policies`: `policyCode` (152 = free cancellation at any time, 1 = non-refundable, …), optional `prepaymentRequired`; add `policyId` to change an existing policy. A property holds at most 7 policies and none can be deleted.
	// - `licences`: `variantId` and `contentData: [{ name, value }]`, from the rules `GET ?type=licences` returns; optional `room_id`.
	// - `checkin_methods`: `methods: [{ checkin_method }]`, using a name from `GET ?type=checkin_methods` `available`.
	// - `contacts`: `contacts: [...]` in Booking.com's contact shape.
	//
	// If Booking.com refuses the write, the response is `422 booking_rejected` with Booking.com's reason, even when Booking.com answered HTTP 200. Resending the same body will be refused again.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/booking/content (the `UpdateBookingContent` operationId).
	UpdateBookingContentWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateBookingContent Update Booking.com content
	//
	// Write one kind of content to a Booking.com property only. Nothing on the canonical listing or on Airbnb changes. To send the listing's own content to every channel instead, use `PUT /v1/listings/{id}/content` and publish.
	//
	// | `type` | What it is |
	// |---|---|
	// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
	// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
	// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
	// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
	// | `policies` | Cancellation and prepayment policies. |
	// | `licences` | The region's licence rules and the licence on file. |
	// | `checkin_methods` | How guests get in (holiday homes). |
	// | `contacts` | Who Booking.com contacts about the property. |
	//
	// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
	//
	// What each `type` takes:
	//
	// - `description`: `text`, optional `language` (default `en`).
	// - `facilities`: `facilities: [{ facility_id | room_facility_id, state: "PRESENT" | "MISSING", instances? }]`. Facilities you do not send stay as they are.
	// - `photos`: `photos: [{ url }]`, uploaded in the background. With `room_id`, send `photo_ids` instead to add photos that have finished processing to that room.
	// - `settings`: `settings: { <block>: {…} }`, for example `{ "pets": { "pets_allowed": "PETS_ALLOWED" } }`. Each block is written separately and reported in `results`.
	// - `policies`: `policyCode` (152 = free cancellation at any time, 1 = non-refundable, …), optional `prepaymentRequired`; add `policyId` to change an existing policy. A property holds at most 7 policies and none can be deleted.
	// - `licences`: `variantId` and `contentData: [{ name, value }]`, from the rules `GET ?type=licences` returns; optional `room_id`.
	// - `checkin_methods`: `methods: [{ checkin_method }]`, using a name from `GET ?type=checkin_methods` `available`.
	// - `contacts`: `contacts: [...]` in Booking.com's contact shape.
	//
	// If Booking.com refuses the write, the response is `422 booking_rejected` with Booking.com's reason, even when Booking.com answered HTTP 200. Resending the same body will be refused again.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/booking/content (the `UpdateBookingContent` operationId).
	UpdateBookingContent(ctx context.Context, body UpdateBookingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// MapBookingRoomWithBody Map a Booking.com room to a Repull listing
	//
	// Link a Booking.com room to a canonical Repull listing — the API-key equivalent of the room mapping the hosted Connect flow performs, and the counterpart of `POST /v1/channels/airbnb/listings/map`.
	//
	// Discover `roomBookingId` with `GET /v1/channels/booking/properties/{id}/rooms`, which returns every room of a property with the `roomId` this route takes.
	//
	// Booking.com attaches at the ROOM level: a property is a building and its rooms are what a guest books, so each room maps to one listing. Pass `listingId: null` to unmap a room and remove its channel link.
	//
	// The room mapping and its channel link are repointed together in one transaction, so a link can never outlive the mapping it describes — a stale link keeps routing that room's reservations to the previous listing. Re-sending a mapping that is already in place writes nothing (`alreadyMapped: true`).
	//
	// **The property's reservations are pulled as part of the call.** Once the room is mapped, every active reservation Booking.com holds for the property is imported and attached to its listing — `reservationsImported` says how many were processed. One already present is left as it is, so re-sending never duplicates. You do not need a follow-up call: reservations that arrived before the room was mapped are never picked up by the regular sync, so this is the moment they are brought in. It runs on every successful map, including a re-send, so re-sending retries an import that did not run. If the import cannot run, the mapping still stands and `reservationsImported` is `null`. A property with a long booking history can take tens of seconds. Unmapping pulls nothing.
	//
	// Unlike the Airbnb route, there is no conflict when the target listing already carries another Booking.com room: one listing served by several rooms is a normal arrangement and is not refused.
	//
	// Scope is enforced on both sides against your workspace — the room's property and the target listing. A room or listing belonging to another workspace returns the same 404 as one that does not exist.
	//
	// Returns `403 listing_inactive` when the target listing, or the listing the room is mapped to now, is inactive; nothing is changed.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/booking/listings/map (the `MapBookingRoom` operationId).
	MapBookingRoomWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// MapBookingRoom Map a Booking.com room to a Repull listing
	//
	// Link a Booking.com room to a canonical Repull listing — the API-key equivalent of the room mapping the hosted Connect flow performs, and the counterpart of `POST /v1/channels/airbnb/listings/map`.
	//
	// Discover `roomBookingId` with `GET /v1/channels/booking/properties/{id}/rooms`, which returns every room of a property with the `roomId` this route takes.
	//
	// Booking.com attaches at the ROOM level: a property is a building and its rooms are what a guest books, so each room maps to one listing. Pass `listingId: null` to unmap a room and remove its channel link.
	//
	// The room mapping and its channel link are repointed together in one transaction, so a link can never outlive the mapping it describes — a stale link keeps routing that room's reservations to the previous listing. Re-sending a mapping that is already in place writes nothing (`alreadyMapped: true`).
	//
	// **The property's reservations are pulled as part of the call.** Once the room is mapped, every active reservation Booking.com holds for the property is imported and attached to its listing — `reservationsImported` says how many were processed. One already present is left as it is, so re-sending never duplicates. You do not need a follow-up call: reservations that arrived before the room was mapped are never picked up by the regular sync, so this is the moment they are brought in. It runs on every successful map, including a re-send, so re-sending retries an import that did not run. If the import cannot run, the mapping still stands and `reservationsImported` is `null`. A property with a long booking history can take tens of seconds. Unmapping pulls nothing.
	//
	// Unlike the Airbnb route, there is no conflict when the target listing already carries another Booking.com room: one listing served by several rooms is a normal arrangement and is not refused.
	//
	// Scope is enforced on both sides against your workspace — the room's property and the target listing. A room or listing belonging to another workspace returns the same 404 as one that does not exist.
	//
	// Returns `403 listing_inactive` when the target listing, or the listing the room is mapped to now, is inactive; nothing is changed.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/booking/listings/map (the `MapBookingRoom` operationId).
	MapBookingRoom(ctx context.Context, body MapBookingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetBookingListingPricing Get Booking.com pricing for a listing
	//
	// Resolves the Repull listing id to its Booking.com `hotel_id` (via the room mapping the Connect flow records for the authenticated workspace), then proxies Booking's `getRoomRateAvailability` for the requested window. Pricing on Booking is per-room/per-rate-plan, so `room_id` and `room_level` flow through query params unchanged.
	//
	// Mirrors the per-channel `/listings/{id}/pricing` shape used by Airbnb so SDK consumers can carry a Repull listing id across channels. `id` is a Repull listing id, never a Booking.com hotel id — the hotel-id surface is `/v1/channels/booking/availability`.
	//
	// A listing can be published under several Booking.com properties. GET uses the oldest and reports the rest in `otherHotelIds`; PUT refuses with `409 ambiguous_booking_mapping` rather than push rates into a property it guessed at. `?hotel_id=` names the property explicitly for either.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/booking/listings/{id}/pricing (the `GetBookingListingPricing` operationId).
	GetBookingListingPricing(ctx context.Context, id int, params *GetBookingListingPricingParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateBookingListingPricingWithBody Update Booking.com pricing for a listing
	//
	// Writes nightly prices for a listing's Booking.com room + rate plan. Each update needs `roomId` + `rateId` + `dateRange` + `price` + `currency`; `dateRange` is inclusive at both ends, so `start` equal to `end` writes exactly one night.
	//
	// **Occupancy.** Booking.com stores a rate amount against the party size the rate plan prices. Send `occupancy` and that is what is used; omit it and it is resolved from Booking.com's own data for that (room, rate plan) and echoed back in `occupancy[]` with its `source`. When it cannot be resolved the write is refused with `422` naming `updates[N].occupancy` — a price is never sent at a guessed party size, because Booking.com declines such an amount without saying so.
	//
	// **Inventory is a separate write.** `roomsToSell` on a rate update returns `422 inventory_not_in_rate_update`; use `PUT /v1/channels/booking/availability` with `type: "availability"`.
	//
	// **Restrictions ride along, on their own wire.** Send `restrictions` with the price and Booking.com receives two writes; the response reports each separately in `price` and `restrictions`, each with its own state, read-back and — when refused — Booking.com's own reason. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` (Booking.com's notification has no element for them — set those on the rate plan in the Extranet). Nothing you send is silently ignored.
	//
	// **The response says what is known.** Booking.com acknowledges a write without per-date status, so the affected nights are read back — prices and restrictions out of the same read — and `applied` reports `verified`, `mismatch`, `partial`, `rejected` or `unverified`. `partial` means one half landed and the other did not, which is never reported as a total failure. Send `verify: false` to skip the read-back; `applied` is then `unverified`. Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
	//
	// `id` is a Repull listing id. When it is published under several Booking.com properties this returns `409 ambiguous_booking_mapping` and pushes nothing — name the property with `?hotel_id=` instead.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/booking/listings/{id}/pricing (the `UpdateBookingListingPricing` operationId).
	UpdateBookingListingPricingWithBody(ctx context.Context, id int, params *UpdateBookingListingPricingParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateBookingListingPricing Update Booking.com pricing for a listing
	//
	// Writes nightly prices for a listing's Booking.com room + rate plan. Each update needs `roomId` + `rateId` + `dateRange` + `price` + `currency`; `dateRange` is inclusive at both ends, so `start` equal to `end` writes exactly one night.
	//
	// **Occupancy.** Booking.com stores a rate amount against the party size the rate plan prices. Send `occupancy` and that is what is used; omit it and it is resolved from Booking.com's own data for that (room, rate plan) and echoed back in `occupancy[]` with its `source`. When it cannot be resolved the write is refused with `422` naming `updates[N].occupancy` — a price is never sent at a guessed party size, because Booking.com declines such an amount without saying so.
	//
	// **Inventory is a separate write.** `roomsToSell` on a rate update returns `422 inventory_not_in_rate_update`; use `PUT /v1/channels/booking/availability` with `type: "availability"`.
	//
	// **Restrictions ride along, on their own wire.** Send `restrictions` with the price and Booking.com receives two writes; the response reports each separately in `price` and `restrictions`, each with its own state, read-back and — when refused — Booking.com's own reason. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` (Booking.com's notification has no element for them — set those on the rate plan in the Extranet). Nothing you send is silently ignored.
	//
	// **The response says what is known.** Booking.com acknowledges a write without per-date status, so the affected nights are read back — prices and restrictions out of the same read — and `applied` reports `verified`, `mismatch`, `partial`, `rejected` or `unverified`. `partial` means one half landed and the other did not, which is never reported as a total failure. Send `verify: false` to skip the read-back; `applied` is then `unverified`. Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
	//
	// `id` is a Repull listing id. When it is published under several Booking.com properties this returns `409 ambiguous_booking_mapping` and pushes nothing — name the property with `?hotel_id=` instead.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/booking/listings/{id}/pricing (the `UpdateBookingListingPricing` operationId).
	UpdateBookingListingPricing(ctx context.Context, id int, params *UpdateBookingListingPricingParams, body UpdateBookingListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListBookingConversations List Booking.com conversations
	//
	// List Booking.com guest conversations. Cursor-paginated. Use the messaging POST to send a reply.
	//
	// Scoped to this workspace. With `property_id`, the property must be connected to this workspace — any other id returns `404 not_found`. Without it, only messages for this workspace's own Booking.com properties are returned.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/booking/messaging (the `ListBookingConversations` operationId).
	ListBookingConversations(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SendBookingMessageWithBody Send Booking.com message
	//
	// Send a text message in a Booking.com conversation as the host. Booking enforces content rules similar to Airbnb.
	//
	// **Text only.** To send photos, use `POST /v1/conversations/{id}/messages` with `attachments` (JPEG or PNG, up to 10 MB each, with message text) — it uploads the files to Booking.com and records them in the conversation. Passing `attachments`, `attachment_ids` or `mediaUrl` here returns `422 attachments_not_supported` and sends nothing.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/booking/messaging (the `SendBookingMessage` operationId).
	SendBookingMessageWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SendBookingMessage Send Booking.com message
	//
	// Send a text message in a Booking.com conversation as the host. Booking enforces content rules similar to Airbnb.
	//
	// **Text only.** To send photos, use `POST /v1/conversations/{id}/messages` with `attachments` (JPEG or PNG, up to 10 MB each, with message text) — it uploads the files to Booking.com and records them in the conversation. Passing `attachments`, `attachment_ids` or `mediaUrl` here returns `422 attachments_not_supported` and sends nothing.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/booking/messaging (the `SendBookingMessage` operationId).
	SendBookingMessage(ctx context.Context, body SendBookingMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListBookingProperties List Booking.com properties
	//
	// List every Booking.com property this workspace holds. Each property is returned ONCE, with the Repull listings mapped under it.
	//
	// A Booking.com property is a building; its rooms are what guests book, and each room is mapped to one Repull listing — so one property routinely carries many listings. `listings[].roomBookingId` is the Booking.com room id an ARI write takes.
	//
	// A property whose rooms are not mapped yet is still listed, with `mappingStatus: "unmapped"` and an empty `listings` array. That is a real mid-onboarding state, not an error: finish `POST /v1/connect/booking/map-rooms` and the listings appear. Such a property used to be dropped silently, which made a mapped-but-unreadable workspace indistinguishable from one with no Booking connection at all.
	//
	// Inactive listings are left out of `listings`; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
	//
	// Corresponds with GET /v1/channels/booking/properties (the `ListBookingProperties` operationId).
	ListBookingProperties(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetBookingProperty Get Booking.com connection for a listing
	//
	// Return the Booking.com connection record(s) for a Repull listing — the linked Booking hotel id, sync flags, markup, sync category, suspension state, and the Booking room the mapping runs through.
	//
	// `id` is a **Repull listing id**, not a Booking.com hotel id, despite the `properties` segment. (The hotel-id surface is `/v1/channels/booking/availability`.) The mapping is read from wherever the Connect flow recorded it — `listings_booking_rooms` for anything mapped through `POST /v1/connect/booking/map-rooms`, which is essentially every live mapping.
	//
	// An ARRAY, because one listing can be published under several Booking.com properties at once; `mappedVia` says which record carries each mapping. A listing with no Booking.com mapping returns 404, and the message says which id space the path takes.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/booking/properties/{id} (the `GetBookingProperty` operationId).
	GetBookingProperty(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BookingPropertyActionWithBody Take a property off sale / put it back (unlist/relist)
	//
	// Stop this listing's Booking.com property being sold, or start it again. `id` is a **Repull listing id**, not a Booking.com hotel id, as on the GET.
	//
	// **Booking.com has no unlist, so this is an availability write.** Airbnb has a real deactivate; Booking.com does not. `unlist` closes the mapped room across the whole forward window, so the property stops selling. `relist` is not its mirror image: it re-syncs the true calendar, so dates that are genuinely blocked (a reservation, an owner stay) stay blocked and only the closure `unlist` wrote lifts. Re-opening everything would sell dates that are not for sale.
	//
	// **Which property gets closed.** A listing can be mapped to more than one Booking.com property — the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. With exactly one, send nothing. With several, name one with `hotelId` (or `?hotel_id=`); omit it and the request is refused with **`409 ambiguous_booking_mapping`** listing the candidates, and nothing is written. That refusal matters more here than on a publish: writing content into the wrong property is recoverable, closing the wrong property's availability takes real inventory off sale while the property you meant keeps selling. Naming a property this listing is not mapped to is a `404` that names the ones it is.
	//
	// **This does not change the listing in Repull.** `active` — what Repull bills and serves — is untouched by both actions and is deliberately not echoed in the response, so the two ideas can never be read as one field. To take a listing off the market on every channel at once, use `POST /v1/listings/{id}/offline`.
	//
	// Any other action returns a structured `422` naming the ones that are supported. To push content use `POST /v1/listings/{id}/publish/booking`; to map rooms use `POST /v1/connect/booking/map-rooms`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/booking/properties/{id} (the `BookingPropertyAction` operationId).
	BookingPropertyActionWithBody(ctx context.Context, id int, params *BookingPropertyActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BookingPropertyAction Take a property off sale / put it back (unlist/relist)
	//
	// Stop this listing's Booking.com property being sold, or start it again. `id` is a **Repull listing id**, not a Booking.com hotel id, as on the GET.
	//
	// **Booking.com has no unlist, so this is an availability write.** Airbnb has a real deactivate; Booking.com does not. `unlist` closes the mapped room across the whole forward window, so the property stops selling. `relist` is not its mirror image: it re-syncs the true calendar, so dates that are genuinely blocked (a reservation, an owner stay) stay blocked and only the closure `unlist` wrote lifts. Re-opening everything would sell dates that are not for sale.
	//
	// **Which property gets closed.** A listing can be mapped to more than one Booking.com property — the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. With exactly one, send nothing. With several, name one with `hotelId` (or `?hotel_id=`); omit it and the request is refused with **`409 ambiguous_booking_mapping`** listing the candidates, and nothing is written. That refusal matters more here than on a publish: writing content into the wrong property is recoverable, closing the wrong property's availability takes real inventory off sale while the property you meant keeps selling. Naming a property this listing is not mapped to is a `404` that names the ones it is.
	//
	// **This does not change the listing in Repull.** `active` — what Repull bills and serves — is untouched by both actions and is deliberately not echoed in the response, so the two ideas can never be read as one field. To take a listing off the market on every channel at once, use `POST /v1/listings/{id}/offline`.
	//
	// Any other action returns a structured `422` naming the ones that are supported. To push content use `POST /v1/listings/{id}/publish/booking`; to map rooms use `POST /v1/connect/booking/map-rooms`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/booking/properties/{id} (the `BookingPropertyAction` operationId).
	BookingPropertyAction(ctx context.Context, id int, params *BookingPropertyActionParams, body BookingPropertyActionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListBookingPropertyRooms List Booking.com rooms + rate-plan ids for a listing
	//
	// Return every Booking.com room and its rate plans for a listing, each with the `roomId` / `rateId` needed to assemble a restriction write via `PUT /v1/channels/booking/availability`.
	//
	// `id` is a **Repull listing id**, not a Booking.com hotel id, despite the `properties` segment — resolved to the Booking `hotel_id` through the workspace mapping, read from wherever the Connect flow recorded it (`listings_booking_rooms` for anything mapped through `POST /v1/connect/booking/map-rooms`). A listing with no active Booking.com mapping returns 404, and the message says which id space the path takes. When the listing is published under several properties the oldest is used, the rest come back in `otherHotelIds`, and `?hotel_id=` names a different one. Sourced from Booking's B.XML roomrates feed, which returns rooms and rate plans together (the rooms-unit feed alone omits rate-plan ids). This is the API-key surface for the room/rate ids that were previously only reachable inside the hosted Connect room-mapping flow.
	//
	// `source` says where the answer came from. `booking` means it was read live just now. If Booking.com returns nothing usable for the property, the rooms and rate plans recorded at the last import are served instead, `source` is `mirror`, and `mirrorReason` names what went wrong live — the ids are Booking.com's own and can be written against, but they can be stale, and `maxPersons`, `policy`, `policyId`, `pricingType` and `isChildRate` come back `null` because only the live feed states them. `rooms` is empty only when Booking.com and the last import both have nothing; a read that failed is an error, never an empty list.
	//
	// Each rate plan carries `maxPersons` — the party size that rate plan prices, which is the `occupancy` a rate amount must be written at. Each room carries `maxAdults`, Booking.com's capacity for the room, which is what a rate write falls back to when the rate plan states no `maxPersons`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/booking/properties/{id}/rooms (the `ListBookingPropertyRooms` operationId).
	ListBookingPropertyRooms(ctx context.Context, id int, params *ListBookingPropertyRoomsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListBookingReservations List Booking.com reservations
	//
	// Pull reservations from Booking.com. `type=new` (default) returns un-acknowledged bookings; `type=modified` returns changed bookings. Pass both `reservation_id` and `hotel_id` to fetch a single reservation's full details. Acknowledge processed reservations with the POST so Booking stops re-serving them in the `new` queue.
	//
	// Scoped to this workspace. `hotel_id` (or its alias `property_id`) must be a property connected to this workspace; any other id returns `404 not_found`, the same as an id that does not exist. Without a hotel, `new`/`modified` cover every Booking.com property this workspace holds (and return `404 not_found` if it holds none). A `reservation_id` that belongs to another workspace returns `404 not_found`.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/booking/reservations (the `ListBookingReservations` operationId).
	ListBookingReservations(ctx context.Context, params *ListBookingReservationsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AcknowledgeBookingReservationsWithBody Acknowledge Booking.com reservations
	//
	// Acknowledge one or more reservations so Booking.com removes them from the `new` queue. The body carries `reservation_ids` (non-empty array). Acknowledge only after you have durably persisted each reservation.
	//
	// Only reservations that belong to this workspace can be acknowledged. If any id in `reservation_ids` is not one of this workspace's Booking.com reservations (`confirmationCode` on `GET /v1/reservations?platform=booking`), nothing is acknowledged and the response is `404 not_found` naming those ids in `reservation_ids`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/booking/reservations (the `AcknowledgeBookingReservations` operationId).
	AcknowledgeBookingReservationsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AcknowledgeBookingReservations Acknowledge Booking.com reservations
	//
	// Acknowledge one or more reservations so Booking.com removes them from the `new` queue. The body carries `reservation_ids` (non-empty array). Acknowledge only after you have durably persisted each reservation.
	//
	// Only reservations that belong to this workspace can be acknowledged. If any id in `reservation_ids` is not one of this workspace's Booking.com reservations (`confirmationCode` on `GET /v1/reservations?platform=booking`), nothing is acknowledged and the response is `404 not_found` naming those ids in `reservation_ids`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/booking/reservations (the `AcknowledgeBookingReservations` operationId).
	AcknowledgeBookingReservations(ctx context.Context, body AcknowledgeBookingReservationsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListBookingReviews List Booking.com reviews
	//
	// List guest reviews for a Booking.com property. Pass `property_id` (the Booking.com hotel id) as a query param — required.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/booking/reviews (the `ListBookingReviews` operationId).
	ListBookingReviews(ctx context.Context, params *ListBookingReviewsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReplyBookingReviewWithBody Reply to Booking.com review
	//
	// Post a public host reply to a guest review on Booking.com. Booking allows one host reply per review — repeated POSTs are rejected by upstream.
	//
	// Booking.com does NOT support host-authored reviews of guests via the API (platform-level limitation), so this endpoint is reply-only.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/booking/reviews (the `ReplyBookingReview` operationId).
	ReplyBookingReviewWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReplyBookingReview Reply to Booking.com review
	//
	// Post a public host reply to a guest review on Booking.com. Booking allows one host reply per review — repeated POSTs are rejected by upstream.
	//
	// Booking.com does NOT support host-authored reviews of guests via the API (platform-level limitation), so this endpoint is reply-only.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/booking/reviews (the `ReplyBookingReview` operationId).
	ReplyBookingReview(ctx context.Context, body ReplyBookingReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BookingSetupWithBody Booking.com property setup actions
	//
	// Action-router for putting a property onto Booking.com — including building one from nothing. Select the step with `action`.
	//
	// ## Opening a property
	//
	// - `create-property` — create a NEW Booking.com property for a Repull listing (`listing_id`). Creates the property, its first room with the listing's beds, a rate plan and the room-rate product that makes the room sellable (under the listing's cancellation policy), sets the contact and invoice details and the facilities, seeds availability and rates, syncs the calendar, then runs Booking.com's readiness check and reports what still blocks opening in `warnings`. Send `contact` (`name`, `email`, `phone` in international form); without it the workspace owner is used, and a workspace with no usable contact is refused before anything is created. Returns 201.
	// - `add-room` — add another room type (and its sellable product) to a property (`listing_id`, `property_id`). Returns 201.
	// - `add-unit` — raise the number of identical units on an existing room (`listing_id`, `property_id`, `room_id`).
	// - `advance` — run Booking.com's readiness check for a property (`property_id`) and, when it passes, open it. Returns `checked`, `opened`, `sellable` and `blockers` — Booking.com's own reasons it cannot open yet.
	//
	// ## Account and policy steps
	//
	// - `create-legal-entity` — register a legal entity directly (returns 201). Not normally needed: see the legal-entity rules below.
	// - `check-legal-status` — always `404`. A legal entity's details are readable for any id on the connectivity-provider credentials every workspace shares, and nothing records which workspace registered which entity, so no entity can be shown to be yours. `create-property` resolves it for you.
	// - `check-readiness` — whether a property is ready to open (`property_id`): `ready` and `blockers`, without trying to open it.
	// - `open-property` — open the property for sale (`property_id`). Refused with `422 booking_rejected` naming the blockers when it is not ready.
	// - `set-contacts` — set property contacts (`property_id`, `contacts` in Booking.com's Contacts API shape; at most one carries the `general` profile).
	// - `set-policies` — add a cancellation policy (`property_id`, `policyCode`, optional `prepaymentRequired`). House rules, pets, children and the damage deposit are `POST /v1/channels/booking/content` with `type: "settings"`.
	//
	// ## Three things about Booking.com that cost real money
	//
	// **A newly created property is NOT sellable.** Booking.com opens it only when its readiness check passes, and the check names what is missing — a main photo still processing, no availability, a licence the region requires. The response always reports `status: "being_built"` and `sellable: false`, never a guess, with the reasons in `warnings`. Resolve them, then `advance`.
	//
	// **A room with no ACTIVE rate plan is invisible.** Booking only renders rooms that have at least one active product linkage (room × rate plan). A room can be created successfully, return a `roomId`, and never appear on the property page. If `rateId` comes back `null` from `create-property` or `add-room`, that is exactly what happened: activate a rate plan on the property in the Extranet, then add the room again.
	//
	// **Room names are Booking.com's.** Travellers see one of Booking.com's standard names ("Two-Bedroom Apartment"), chosen from the listing's bedrooms. The listing's own name is kept as the operator-side reference, never shown to guests.
	//
	// ## The legal entity is resolved, not asked for
	//
	// A property is created against the legal entity Booking.com contracts with, invoices and pays. You do not normally send one:
	//
	// 1. If this workspace already creates properties under a legal entity, that one is reused. A second is never registered.
	// 2. If it has none and the request carries `legal_entity` (`company_name`, `legal_contact_name`, `legal_contact_email`), one is registered and used. Booking.com emails the legal contact a contract; creation only succeeds once it is signed.
	// 3. If it has none and no `legal_entity`, the request is refused with `422 legal_entity_required` naming the fields — a contracted company is never invented.
	//
	// `legal_entity_id` overrides all of that. An id that already carries another workspace's properties is refused with `403 legal_entity_not_yours` before anything is created.
	//
	// ## What you do not control
	//
	// Properties are created against Booking's **production** target only. A test-target property cannot be sold through and there is no route back from one, so `target` is not a parameter — sending it changes nothing.
	//
	// The property category comes from the listing's property type (Apartment when it has none; Holiday home, Villa or Chalet when it says so). The initial room count is 1. Latitude and longitude come from the listing and are adjusted slightly to clear Booking.com's duplicate detection — send the property's true position on the listing and do not pre-adjust it yourself.
	//
	// The listing's name, check-in/check-out times, currency, capacity and price come from the listing. Its postal code is taken from the listing's own `postalCode`; when the listing has none, it falls back to a connected Airbnb listing. A listing with neither is created without a postal code, so set `postalCode` on the listing first.
	//
	// ## Guards
	//
	// Every action that takes a `property_id` requires a property connected to this workspace; any other id returns `404 not_found`. Every action that takes a `listing_id` requires a listing in this workspace; any other id returns `404 not_found`.
	//
	// `create-property` refuses a listing with no coordinates (`422 missing_coordinates`) before anything is created — creating a Booking.com property cannot be undone.
	//
	// `create-property` is subject to the same published-listing gate as the dashboard: no plan, or the plan's listing limit reached, returns `403 billing_error` with `used` and `limit`, and nothing is created.
	//
	// Returns `403 listing_inactive` when the listing — or any listing mapped to the Booking.com property — is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// If a property is created and a later step fails, the response is `422 booking_create_partial` carrying `property_id`. The property EXISTS. Do not retry `create-property`, which would open a second one — continue with `add-room` and `advance`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/booking/setup (the `BookingSetup` operationId).
	BookingSetupWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BookingSetup Booking.com property setup actions
	//
	// Action-router for putting a property onto Booking.com — including building one from nothing. Select the step with `action`.
	//
	// ## Opening a property
	//
	// - `create-property` — create a NEW Booking.com property for a Repull listing (`listing_id`). Creates the property, its first room with the listing's beds, a rate plan and the room-rate product that makes the room sellable (under the listing's cancellation policy), sets the contact and invoice details and the facilities, seeds availability and rates, syncs the calendar, then runs Booking.com's readiness check and reports what still blocks opening in `warnings`. Send `contact` (`name`, `email`, `phone` in international form); without it the workspace owner is used, and a workspace with no usable contact is refused before anything is created. Returns 201.
	// - `add-room` — add another room type (and its sellable product) to a property (`listing_id`, `property_id`). Returns 201.
	// - `add-unit` — raise the number of identical units on an existing room (`listing_id`, `property_id`, `room_id`).
	// - `advance` — run Booking.com's readiness check for a property (`property_id`) and, when it passes, open it. Returns `checked`, `opened`, `sellable` and `blockers` — Booking.com's own reasons it cannot open yet.
	//
	// ## Account and policy steps
	//
	// - `create-legal-entity` — register a legal entity directly (returns 201). Not normally needed: see the legal-entity rules below.
	// - `check-legal-status` — always `404`. A legal entity's details are readable for any id on the connectivity-provider credentials every workspace shares, and nothing records which workspace registered which entity, so no entity can be shown to be yours. `create-property` resolves it for you.
	// - `check-readiness` — whether a property is ready to open (`property_id`): `ready` and `blockers`, without trying to open it.
	// - `open-property` — open the property for sale (`property_id`). Refused with `422 booking_rejected` naming the blockers when it is not ready.
	// - `set-contacts` — set property contacts (`property_id`, `contacts` in Booking.com's Contacts API shape; at most one carries the `general` profile).
	// - `set-policies` — add a cancellation policy (`property_id`, `policyCode`, optional `prepaymentRequired`). House rules, pets, children and the damage deposit are `POST /v1/channels/booking/content` with `type: "settings"`.
	//
	// ## Three things about Booking.com that cost real money
	//
	// **A newly created property is NOT sellable.** Booking.com opens it only when its readiness check passes, and the check names what is missing — a main photo still processing, no availability, a licence the region requires. The response always reports `status: "being_built"` and `sellable: false`, never a guess, with the reasons in `warnings`. Resolve them, then `advance`.
	//
	// **A room with no ACTIVE rate plan is invisible.** Booking only renders rooms that have at least one active product linkage (room × rate plan). A room can be created successfully, return a `roomId`, and never appear on the property page. If `rateId` comes back `null` from `create-property` or `add-room`, that is exactly what happened: activate a rate plan on the property in the Extranet, then add the room again.
	//
	// **Room names are Booking.com's.** Travellers see one of Booking.com's standard names ("Two-Bedroom Apartment"), chosen from the listing's bedrooms. The listing's own name is kept as the operator-side reference, never shown to guests.
	//
	// ## The legal entity is resolved, not asked for
	//
	// A property is created against the legal entity Booking.com contracts with, invoices and pays. You do not normally send one:
	//
	// 1. If this workspace already creates properties under a legal entity, that one is reused. A second is never registered.
	// 2. If it has none and the request carries `legal_entity` (`company_name`, `legal_contact_name`, `legal_contact_email`), one is registered and used. Booking.com emails the legal contact a contract; creation only succeeds once it is signed.
	// 3. If it has none and no `legal_entity`, the request is refused with `422 legal_entity_required` naming the fields — a contracted company is never invented.
	//
	// `legal_entity_id` overrides all of that. An id that already carries another workspace's properties is refused with `403 legal_entity_not_yours` before anything is created.
	//
	// ## What you do not control
	//
	// Properties are created against Booking's **production** target only. A test-target property cannot be sold through and there is no route back from one, so `target` is not a parameter — sending it changes nothing.
	//
	// The property category comes from the listing's property type (Apartment when it has none; Holiday home, Villa or Chalet when it says so). The initial room count is 1. Latitude and longitude come from the listing and are adjusted slightly to clear Booking.com's duplicate detection — send the property's true position on the listing and do not pre-adjust it yourself.
	//
	// The listing's name, check-in/check-out times, currency, capacity and price come from the listing. Its postal code is taken from the listing's own `postalCode`; when the listing has none, it falls back to a connected Airbnb listing. A listing with neither is created without a postal code, so set `postalCode` on the listing first.
	//
	// ## Guards
	//
	// Every action that takes a `property_id` requires a property connected to this workspace; any other id returns `404 not_found`. Every action that takes a `listing_id` requires a listing in this workspace; any other id returns `404 not_found`.
	//
	// `create-property` refuses a listing with no coordinates (`422 missing_coordinates`) before anything is created — creating a Booking.com property cannot be undone.
	//
	// `create-property` is subject to the same published-listing gate as the dashboard: no plan, or the plan's listing limit reached, returns `403 billing_error` with `used` and `limit`, and nothing is created.
	//
	// Returns `403 listing_inactive` when the listing — or any listing mapped to the Booking.com property — is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// If a property is created and a later step fails, the response is `422 booking_create_partial` carrying `property_id`. The property EXISTS. Do not retry `create-property`, which would open a second one — continue with `add-room` and `advance`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/booking/setup (the `BookingSetup` operationId).
	BookingSetup(ctx context.Context, body BookingSetupJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteBookingWebhook Unsubscribe from a Booking.com notification
	//
	// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
	//
	// Corresponds with DELETE /v1/channels/booking/webhooks (the `DeleteBookingWebhook` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	DeleteBookingWebhook(ctx context.Context, params *DeleteBookingWebhookParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListBookingWebhooks List Booking.com webhook subscriptions
	//
	// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
	//
	// Corresponds with GET /v1/channels/booking/webhooks (the `ListBookingWebhooks` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	ListBookingWebhooks(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateBookingWebhookWithBody Subscribe to a Booking.com notification
	//
	// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/channels/booking/webhooks (the `CreateBookingWebhook` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	CreateBookingWebhookWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateBookingWebhook Subscribe to a Booking.com notification
	//
	// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/channels/booking/webhooks (the `CreateBookingWebhook` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	CreateBookingWebhook(ctx context.Context, body CreateBookingWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetPlumguideAvailability Get Plumguide availability
	//
	// Read the per-day availability calendar for a Plumguide listing. Returns the same row shape as Airbnb availability for SDK convenience.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/plumguide/availability (the `GetPlumguideAvailability` operationId).
	GetPlumguideAvailability(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdatePlumguideAvailability Push availability to Plumguide
	//
	// Push per-day availability changes to Plumguide. Plumguide accepts only the next 24 months — dates beyond that are silently ignored.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with PUT /v1/channels/plumguide/availability (the `UpdatePlumguideAvailability` operationId).
	UpdatePlumguideAvailability(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListPlumguideBookings List Plumguide bookings
	//
	// List Plumguide bookings. Default returns all bookings; pass `listing_id` to filter to one listing, or `booking_code` to fetch a single booking.
	//
	// Returns `403 listing_inactive` when `listing_id` or `booking_code` resolves to an inactive listing. The unfiltered list is read straight from Plum Guide and is not filtered by listing status.
	//
	// Corresponds with GET /v1/channels/plumguide/bookings (the `ListPlumguideBookings` operationId).
	ListPlumguideBookings(ctx context.Context, params *ListPlumguideBookingsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListPlumguideListings List Plumguide listings
	//
	// List Plumguide listings this workspace has access to. Plumguide is approval-based — listings appear once Plumguide has accepted them.
	//
	// Corresponds with GET /v1/channels/plumguide/listings (the `ListPlumguideListings` operationId).
	ListPlumguideListings(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetPlumguidePricing Get Plumguide pricing
	//
	// Read the current pricing for a Plumguide listing (base price, currency, weekend uplift).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/channels/plumguide/pricing (the `GetPlumguidePricing` operationId).
	GetPlumguidePricing(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdatePlumguidePricing Push pricing to Plumguide
	//
	// Push pricing changes to Plumguide. Plumguide rounds all prices to whole units of the listing currency — sub-unit precision is silently truncated.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with PUT /v1/channels/plumguide/pricing (the `UpdatePlumguidePricing` operationId).
	UpdatePlumguidePricing(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeletePlumguideWebhooks Remove Plumguide webhook config
	//
	// Delete the Plumguide webhook configuration for this workspace. Plumguide stops delivering webhooks until a new config is set.
	//
	// Corresponds with DELETE /v1/channels/plumguide/webhooks (the `DeletePlumguideWebhooks` operationId).
	DeletePlumguideWebhooks(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetPlumguideWebhooks Get Plumguide webhook config
	//
	// Read the current Plumguide webhook configuration for this workspace.
	//
	// Corresponds with GET /v1/channels/plumguide/webhooks (the `GetPlumguideWebhooks` operationId).
	GetPlumguideWebhooks(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdatePlumguideWebhooksWithBody Replace Plumguide webhook config
	//
	// Replace the Plumguide webhook configuration. The body carries the full webhook config payload — this is a full replacement, not a patch.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/channels/plumguide/webhooks (the `UpdatePlumguideWebhooks` operationId).
	UpdatePlumguideWebhooksWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdatePlumguideWebhooks Replace Plumguide webhook config
	//
	// Replace the Plumguide webhook configuration. The body carries the full webhook config payload — this is a full replacement, not a patch.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/channels/plumguide/webhooks (the `UpdatePlumguideWebhooks` operationId).
	UpdatePlumguideWebhooks(ctx context.Context, body UpdatePlumguideWebhooksJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListVrboListings List VRBO listings
	//
	// List VRBO listings this workspace owns. VRBO is agency-model — Repull reads listings via the public iCal/HTTP feeds.
	//
	// Inactive listings are left out; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
	//
	// Corresponds with GET /v1/channels/vrbo/listings (the `ListVrboListings` operationId).
	ListVrboListings(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListVrboReservations List VRBO reservations
	//
	// Cursor-paginated list of VRBO reservations sourced from the public booking feed. Lag is typically 5-10 minutes vs. Airbnb / Booking.com. `?offset=` is accepted as a first-class alias for `?cursor=` (mutually exclusive; offset capped at 10000).
	//
	// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated.
	//
	// Corresponds with GET /v1/channels/vrbo/reservations (the `ListVrboReservations` operationId).
	ListVrboReservations(ctx context.Context, params *ListVrboReservationsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListConnections List PMS/OTA connections
	//
	// Returns all active connections to PMS and OTA platforms.
	//
	// Corresponds with GET /v1/connect (the `ListConnections` operationId).
	ListConnections(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateConnectSessionWithBody Create a multi-channel Connect picker session
	//
	// Mints a session that lands the user on the channel picker at `connect.repull.dev/{sessionId}` instead of jumping straight to a single provider. The user picks a channel from the registry, the picker page POSTs `selectConnectProvider` to bind the choice, and the per-provider flow takes over.
	//
	// Use this when you want one entry point for all 13 channels. Use `POST /v1/connect/{provider}` instead when your UI already knows which channel to connect.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect (the `CreateConnectSession` operationId).
	CreateConnectSessionWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateConnectSession Create a multi-channel Connect picker session
	//
	// Mints a session that lands the user on the channel picker at `connect.repull.dev/{sessionId}` instead of jumping straight to a single provider. The user picks a channel from the registry, the picker page POSTs `selectConnectProvider` to bind the choice, and the per-provider flow takes over.
	//
	// Use this when you want one entry point for all 13 channels. Use `POST /v1/connect/{provider}` instead when your UI already knows which channel to connect.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect (the `CreateConnectSession` operationId).
	CreateConnectSession(ctx context.Context, body CreateConnectSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitBeds24CredentialsWithBody Submit Beds24 credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Beds24. API key + prop key from Beds24 → Settings → Apps & Integrations.
	//
	// The credentials are validated against Beds24 before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/beds24/credentials (the `SubmitBeds24Credentials` operationId).
	SubmitBeds24CredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitBeds24Credentials Submit Beds24 credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Beds24. API key + prop key from Beds24 → Settings → Apps & Integrations.
	//
	// The credentials are validated against Beds24 before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/beds24/credentials (the `SubmitBeds24Credentials` operationId).
	SubmitBeds24Credentials(ctx context.Context, body SubmitBeds24CredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BookingConnectCallbackWithBody Booking.com connectivity callback
	//
	// Receives Booking.com's asynchronous confirmation that a property has designated Repull as its connectivity provider, and advances the Connect session. Called by Booking.com, not by integrators.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/booking/callback (the `BookingConnectCallback` operationId).
	BookingConnectCallbackWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BookingConnectCallback Booking.com connectivity callback
	//
	// Receives Booking.com's asynchronous confirmation that a property has designated Repull as its connectivity provider, and advances the Connect session. Called by Booking.com, not by integrators.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/booking/callback (the `BookingConnectCallback` operationId).
	BookingConnectCallback(ctx context.Context, body BookingConnectCallbackJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// MapConnectBookingRoomsWithBody Submit room→listing mappings for a Booking.com Connect session
	//
	// Submits the customer's room→listing mapping choices in one transaction. For each mapping, updates `listings_booking_rooms.listing_id` and replaces the corresponding `listing_platform_links` row. Pass `listingId: null` to explicitly unmap a room.
	//
	// On success the Connect session is marked `completed` and the hosted picker page emits a `repull:connect:completed` postMessage to the embedding window.
	//
	// No API key required — the `sessionId` in the body is the capability token. Each mapping's `roomId` must belong to the customer's claimed hotel; mismatched IDs are rejected with 403.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/booking/map-rooms (the `MapConnectBookingRooms` operationId).
	MapConnectBookingRoomsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// MapConnectBookingRooms Submit room→listing mappings for a Booking.com Connect session
	//
	// Submits the customer's room→listing mapping choices in one transaction. For each mapping, updates `listings_booking_rooms.listing_id` and replaces the corresponding `listing_platform_links` row. Pass `listingId: null` to explicitly unmap a room.
	//
	// On success the Connect session is marked `completed` and the hosted picker page emits a `repull:connect:completed` postMessage to the embedding window.
	//
	// No API key required — the `sessionId` in the body is the capability token. Each mapping's `roomId` must belong to the customer's claimed hotel; mismatched IDs are rejected with 403.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/booking/map-rooms (the `MapConnectBookingRooms` operationId).
	MapConnectBookingRooms(ctx context.Context, body MapConnectBookingRoomsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListConnectBookingRooms List Booking.com rooms imported for a Connect session
	//
	// Returns the rooms imported from the Booking.com hotel claimed in this Connect session, plus the customer's listing options for the mapping dropdowns. Hosted-picker pages poll this endpoint every ~2s after `verifyBookingHotel` succeeds; once rooms appear the page transitions to the mapping UI.
	//
	// No API key required — the `sessionId` query param is the capability token.
	//
	// Corresponds with GET /v1/connect/booking/rooms (the `ListConnectBookingRooms` operationId).
	ListConnectBookingRooms(ctx context.Context, params *ListConnectBookingRoomsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// VerifyBookingHotelWithBody Verify a Booking.com hotel ID for a Connect session
	//
	// Manual-paste fallback that closes the Booking.com claim flow. Call this after the customer completes Stage 1 designation in their Booking Extranet (ticking FantasticStay/Repull as their connectivity provider) and pastes their Hotel ID into the hosted picker.
	//
	// Validates the hotel against Booking's property API, persists the `pms_connections` row, kicks off the room import, and transitions the Connect session to `awaiting_room_mapping`.
	//
	// No API key required — the `sessionId` is the capability token. Sessions in any terminal state are rejected.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/booking/verify (the `VerifyBookingHotel` operationId).
	VerifyBookingHotelWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// VerifyBookingHotel Verify a Booking.com hotel ID for a Connect session
	//
	// Manual-paste fallback that closes the Booking.com claim flow. Call this after the customer completes Stage 1 designation in their Booking Extranet (ticking FantasticStay/Repull as their connectivity provider) and pastes their Hotel ID into the hosted picker.
	//
	// Validates the hotel against Booking's property API, persists the `pms_connections` row, kicks off the room import, and transitions the Connect session to `awaiting_room_mapping`.
	//
	// No API key required — the `sessionId` is the capability token. Sessions in any terminal state are rejected.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/booking/verify (the `VerifyBookingHotel` operationId).
	VerifyBookingHotel(ctx context.Context, body VerifyBookingHotelJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitBookingsyncCredentialsWithBody Submit BookingSync credentials for a Connect session
	//
	// Completes a credentials-pattern connection for BookingSync. OAuth client credentials issued by BookingSync.
	//
	// The credentials are validated against BookingSync before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/bookingsync/credentials (the `SubmitBookingsyncCredentials` operationId).
	SubmitBookingsyncCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitBookingsyncCredentials Submit BookingSync credentials for a Connect session
	//
	// Completes a credentials-pattern connection for BookingSync. OAuth client credentials issued by BookingSync.
	//
	// The credentials are validated against BookingSync before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/bookingsync/credentials (the `SubmitBookingsyncCredentials` operationId).
	SubmitBookingsyncCredentials(ctx context.Context, body SubmitBookingsyncCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitGuestyCredentialsWithBody Submit Guesty credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Guesty. Client ID + secret from Guesty → Integrations → Open API.
	//
	// The credentials are validated against Guesty before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/guesty/credentials (the `SubmitGuestyCredentials` operationId).
	SubmitGuestyCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitGuestyCredentials Submit Guesty credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Guesty. Client ID + secret from Guesty → Integrations → Open API.
	//
	// The credentials are validated against Guesty before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/guesty/credentials (the `SubmitGuestyCredentials` operationId).
	SubmitGuestyCredentials(ctx context.Context, body SubmitGuestyCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitHospitableCredentialsWithBody Submit Hospitable credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Hospitable. Personal access token from Hospitable → Settings → API.
	//
	// The credentials are validated against Hospitable before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/hospitable/credentials (the `SubmitHospitableCredentials` operationId).
	SubmitHospitableCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitHospitableCredentials Submit Hospitable credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Hospitable. Personal access token from Hospitable → Settings → API.
	//
	// The credentials are validated against Hospitable before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/hospitable/credentials (the `SubmitHospitableCredentials` operationId).
	SubmitHospitableCredentials(ctx context.Context, body SubmitHospitableCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitHostawayCredentialsWithBody Submit Hostaway credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Hostaway. Account ID + API key from Hostaway → Settings → Public API.
	//
	// The credentials are validated against Hostaway before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/hostaway/credentials (the `SubmitHostawayCredentials` operationId).
	SubmitHostawayCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitHostawayCredentials Submit Hostaway credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Hostaway. Account ID + API key from Hostaway → Settings → Public API.
	//
	// The credentials are validated against Hostaway before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/hostaway/credentials (the `SubmitHostawayCredentials` operationId).
	SubmitHostawayCredentials(ctx context.Context, body SubmitHostawayCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitIgmsCredentialsWithBody Submit iGMS credentials for a Connect session
	//
	// Completes a credentials-pattern connection for iGMS. API token from iGMS → Settings → Integrations.
	//
	// The credentials are validated against iGMS before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/igms/credentials (the `SubmitIgmsCredentials` operationId).
	SubmitIgmsCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitIgmsCredentials Submit iGMS credentials for a Connect session
	//
	// Completes a credentials-pattern connection for iGMS. API token from iGMS → Settings → Integrations.
	//
	// The credentials are validated against iGMS before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/igms/credentials (the `SubmitIgmsCredentials` operationId).
	SubmitIgmsCredentials(ctx context.Context, body SubmitIgmsCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitLodgifyCredentialsWithBody Submit Lodgify credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Lodgify. API key from Lodgify → Settings → Public API.
	//
	// The credentials are validated against Lodgify before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/lodgify/credentials (the `SubmitLodgifyCredentials` operationId).
	SubmitLodgifyCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitLodgifyCredentials Submit Lodgify credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Lodgify. API key from Lodgify → Settings → Public API.
	//
	// The credentials are validated against Lodgify before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/lodgify/credentials (the `SubmitLodgifyCredentials` operationId).
	SubmitLodgifyCredentials(ctx context.Context, body SubmitLodgifyCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitOwnerrezCredentialsWithBody Submit OwnerRez credentials for a Connect session
	//
	// Completes a credentials-pattern connection for OwnerRez. Username + API token from OwnerRez → Settings → API.
	//
	// The credentials are validated against OwnerRez before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/ownerrez/credentials (the `SubmitOwnerrezCredentials` operationId).
	SubmitOwnerrezCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitOwnerrezCredentials Submit OwnerRez credentials for a Connect session
	//
	// Completes a credentials-pattern connection for OwnerRez. Username + API token from OwnerRez → Settings → API.
	//
	// The credentials are validated against OwnerRez before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/ownerrez/credentials (the `SubmitOwnerrezCredentials` operationId).
	SubmitOwnerrezCredentials(ctx context.Context, body SubmitOwnerrezCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListConnectProviders List Connect channels
	//
	// Returns the public registry of every channel the picker supports. No customer-specific data — display metadata only. Cached for 5 minutes at the edge.
	//
	// Corresponds with GET /v1/connect/providers (the `ListConnectProviders` operationId).
	ListConnectProviders(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SelectConnectProviderWithBody Bind a picker session to a provider
	//
	// Called by the hosted picker page once the user clicks a channel card. Validates the provider exists and is permitted by the session's `allowedProviders` whitelist (if any), then returns the next-step URL the picker should navigate to.
	//
	// No API key required — the session ID is the capability token. The session must still be pending and unexpired.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/sessions/{sessionId}/select-provider (the `SelectConnectProvider` operationId).
	SelectConnectProviderWithBody(ctx context.Context, sessionId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SelectConnectProvider Bind a picker session to a provider
	//
	// Called by the hosted picker page once the user clicks a channel card. Validates the provider exists and is permitted by the session's `allowedProviders` whitelist (if any), then returns the next-step URL the picker should navigate to.
	//
	// No API key required — the session ID is the capability token. The session must still be pending and unexpired.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/sessions/{sessionId}/select-provider (the `SelectConnectProvider` operationId).
	SelectConnectProvider(ctx context.Context, sessionId string, body SelectConnectProviderJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitSmoobuCredentialsWithBody Submit Smoobu credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Smoobu. API key from Smoobu → Settings → For developers.
	//
	// The credentials are validated against Smoobu before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/smoobu/credentials (the `SubmitSmoobuCredentials` operationId).
	SubmitSmoobuCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitSmoobuCredentials Submit Smoobu credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Smoobu. API key from Smoobu → Settings → For developers.
	//
	// The credentials are validated against Smoobu before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/smoobu/credentials (the `SubmitSmoobuCredentials` operationId).
	SubmitSmoobuCredentials(ctx context.Context, body SubmitSmoobuCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitVrboCredentialsWithBody Submit Vrbo credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Vrbo. Activation handshake — Repull mints the Basic-Auth pair the host pastes into Vrbo Partner Central.
	//
	// The credentials are validated against Vrbo before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/vrbo/credentials (the `SubmitVrboCredentials` operationId).
	SubmitVrboCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SubmitVrboCredentials Submit Vrbo credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Vrbo. Activation handshake — Repull mints the Basic-Auth pair the host pastes into Vrbo Partner Central.
	//
	// The credentials are validated against Vrbo before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/vrbo/credentials (the `SubmitVrboCredentials` operationId).
	SubmitVrboCredentials(ctx context.Context, body SubmitVrboCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteConnection Disconnect provider
	//
	// Disconnect ONE connected account of a provider from this workspace. Supported for `airbnb` and `booking`.
	//
	// **Which account.** Pass `accountId` — for Airbnb the host id (`accounts[].externalAccountId` from `GET /v1/connect/airbnb`), for Booking.com the hotel id. It is optional only when the workspace has exactly one account for the provider. With several and no `accountId`, the call returns `422` with the account ids in `valid_values` instead of guessing. An `accountId` that is not connected to this workspace returns `404`. Disconnecting one account leaves the others connected.
	//
	// **What happens.** The account's stored authorization is removed and it stops syncing. Its listings are **deactivated**, not deleted: they stop counting toward your plan's listing limit, their data is kept, and they are returned in `listingsDeactivated`. A listing that is still connected through another account or channel stays active. Reconnect the account, then activate the listings with `POST /v1/listings/status`.
	//
	// The change is all or nothing. For Airbnb, the host can also revoke access on Airbnb's side (Account → Privacy & sharing → Connected apps); that alone does not update this workspace, so call this endpoint as well.
	//
	// Other providers return `501 not_implemented` with instructions for disconnecting on the provider's side. That answer depends only on the provider, not on your workspace: an unsupported provider returns `501` whether or not you have a connection to it. `404 not_found` on a supported provider means this workspace has no connection to it (or, with `accountId`, that the account is not connected here).
	//
	// Corresponds with DELETE /v1/connect/{provider} (the `DeleteConnection` operationId).
	DeleteConnection(ctx context.Context, provider Provider, params *DeleteConnectionParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetConnectStatus Get connection status
	//
	// Returns the current connection status for a provider, including host metadata (display name + avatar) for Airbnb so clients can render an account-level confirmation UI.
	//
	// Corresponds with GET /v1/connect/{provider} (the `GetConnectStatus` operationId).
	GetConnectStatus(ctx context.Context, provider Provider, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateConnectionWithBody Connect to PMS/OTA provider
	//
	// Establish a connection to a PMS or OTA platform. Credentials vary by provider — see docs for each provider.
	//
	// Airbnb-specific: pass `redirectUrl` (where to send the user after consent) and optionally `accessType`. Three tiers: `read_only` grants read-only scopes; `messaging` grants read scopes plus message read/send but NOT property management, so it can coexist with another app (e.g. an existing PMS) that already holds property management on the same Airbnb account; `full_access` — the default — grants full host scopes including the exclusive property management (only one app per Airbnb account can hold it). The response returns a hosted `url` to redirect the user to.
	//
	// Booking.com: pass `redirectUrl` (no `accessType`). The response returns a hosted `url` — send the user there to designate FantasticStay in their Booking.com Extranet and paste their Hotel ID. Same response shape as Airbnb (`url`, `sessionId`, `expiresAt`).
	//
	// PMS providers (api-key based) pass `apiKey` instead; Plumguide passes `clientId`/`clientSecret`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/connect/{provider} (the `CreateConnection` operationId).
	CreateConnectionWithBody(ctx context.Context, provider Provider, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateConnection Connect to PMS/OTA provider
	//
	// Establish a connection to a PMS or OTA platform. Credentials vary by provider — see docs for each provider.
	//
	// Airbnb-specific: pass `redirectUrl` (where to send the user after consent) and optionally `accessType`. Three tiers: `read_only` grants read-only scopes; `messaging` grants read scopes plus message read/send but NOT property management, so it can coexist with another app (e.g. an existing PMS) that already holds property management on the same Airbnb account; `full_access` — the default — grants full host scopes including the exclusive property management (only one app per Airbnb account can hold it). The response returns a hosted `url` to redirect the user to.
	//
	// Booking.com: pass `redirectUrl` (no `accessType`). The response returns a hosted `url` — send the user there to designate FantasticStay in their Booking.com Extranet and paste their Hotel ID. Same response shape as Airbnb (`url`, `sessionId`, `expiresAt`).
	//
	// PMS providers (api-key based) pass `apiKey` instead; Plumguide passes `clientId`/`clientSecret`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/connect/{provider} (the `CreateConnection` operationId).
	CreateConnection(ctx context.Context, provider Provider, body CreateConnectionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListConversations List conversations
	//
	// Cursor-paginated list of message threads owned by the workspace. Backed by main vanio's `/api/threads/list` which keyset-paginates against `(last_message_at, id)` for constant per-page cost. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// Filters: `platform` (`airbnb`|`booking`|`vrbo`|`website`|`email`), `status` (`open`|`archived` — `archived` is a stable no-op until the bit lands on `message_threads`).
	//
	// **Inactive listings:** conversations that belong to an inactive listing (by the thread's listing or its reservation's listing) are left out of the page and of `pagination.total`. Inactive listings keep syncing; activate the listing to use it here.
	//
	// Corresponds with GET /v1/conversations (the `ListConversations` operationId).
	ListConversations(ctx context.Context, params *ListConversationsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetConversation Get conversation detail
	//
	// Returns one thread (the same shape as the list-row `Conversation`) plus expanded `host` (from `airbnb_hosts` for the thread's `host_id`) and `guest` (resolved via the thread's `reservation_id`, with up to 50 contacts) blocks.
	//
	// A conversation that belongs to an inactive listing (by the thread's listing or its reservation's listing) returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
	//
	// Corresponds with GET /v1/conversations/{id} (the `GetConversation` operationId).
	GetConversation(ctx context.Context, id int, params *GetConversationParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListConversationMessages List messages in a conversation
	//
	// Cursor-paginated messages within one thread. Defaults to newest-first (`?order=desc`); pass `?order=asc` for chronological replay. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// A conversation that belongs to an inactive listing returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
	//
	// Corresponds with GET /v1/conversations/{id}/messages (the `ListConversationMessages` operationId).
	ListConversationMessages(ctx context.Context, id int, params *ListConversationMessagesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SendConversationMessageWithBody Send a message to the guest
	//
	// Sends a message to the guest on this conversation and records it in the thread.
	//
	// Omit `channel` and the message goes out on whichever channel the conversation already uses (Airbnb, Booking.com, SMS, email or the direct-booking site) — that is the right default. Pass `channel` only to force a specific one.
	//
	// The message is attributed to the API: it is recorded with `aiGenerated` false so an API send is never counted as an automated reply.
	//
	// ### Airbnb rewrites links — check `contentRewritten`
	//
	// Airbnb rejects guest messages containing a link, an email address or a phone number, and names the offending text. When that happens the offending fragment is stripped and the remainder is re-sent once, which means **the guest receives a message that is not the one you wrote**. Reporting that as a plain success would be a lie, so every response carries `contentRewritten`; when it is `true`, `deliveredContent` is the text that actually reached the guest. Check it before assuming your message went out verbatim.
	//
	// When the text cannot be salvaged (the link is most of the message) nothing is delivered and the call returns `422 message_not_sent` with the channel's verbatim refusal in `statusReason`.
	//
	// Send `Idempotency-Key` — without it, retrying after a network timeout sends the guest the same message twice.
	//
	// ### Attachments
	//
	// Send files with `attachments: [{ url, contentType?, filename? }]` — public `https://` URLs, up to 5 per request, 10 MB each. `message` may be omitted when there are attachments (except on Booking.com). Repull downloads each file, reads its real type from the bytes, keeps a durable copy and delivers it through the channel's own file flow. **Every file is checked before anything is sent**: if one is unreachable, too large or of a type the channel refuses, the call returns 422 naming the file (`index`) and the guest receives nothing.
	//
	// | Channel | Accepted types | Text | How it arrives |
	// |---|---|---|---|
	// | Airbnb | JPEG, PNG, GIF, WebP (converted to JPEG), MP4, QuickTime | optional | each file as its own message, then the text as a separate message |
	// | Booking.com | JPEG, PNG | **required** | one message carrying the text and every file |
	// | SMS, email, direct-booking site chat | — | — | `422 attachments_not_supported`, nothing sent |
	//
	// Airbnb does not allow files in pre-booking (inquiry) conversations; that refusal comes back as `422 message_not_sent`. Because Airbnb delivers files one message at a time, a later file can be refused after earlier ones arrived — that returns `422 message_partially_sent` with `parts` saying exactly which messages reached the guest; resend only the rest.
	//
	// The response's `attachments` lists each file's durable `url`, and `parts` lists every channel message the send produced. Read-back (`GET /v1/conversations/{id}/messages`) shows the same files in each message's `attachments`.
	//
	// **Inactive listings:** a conversation that belongs to an inactive listing returns `403 listing_inactive` and no message is sent. Activate the listing first.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/conversations/{id}/messages (the `SendConversationMessage` operationId).
	SendConversationMessageWithBody(ctx context.Context, id int, params *SendConversationMessageParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SendConversationMessage Send a message to the guest
	//
	// Sends a message to the guest on this conversation and records it in the thread.
	//
	// Omit `channel` and the message goes out on whichever channel the conversation already uses (Airbnb, Booking.com, SMS, email or the direct-booking site) — that is the right default. Pass `channel` only to force a specific one.
	//
	// The message is attributed to the API: it is recorded with `aiGenerated` false so an API send is never counted as an automated reply.
	//
	// ### Airbnb rewrites links — check `contentRewritten`
	//
	// Airbnb rejects guest messages containing a link, an email address or a phone number, and names the offending text. When that happens the offending fragment is stripped and the remainder is re-sent once, which means **the guest receives a message that is not the one you wrote**. Reporting that as a plain success would be a lie, so every response carries `contentRewritten`; when it is `true`, `deliveredContent` is the text that actually reached the guest. Check it before assuming your message went out verbatim.
	//
	// When the text cannot be salvaged (the link is most of the message) nothing is delivered and the call returns `422 message_not_sent` with the channel's verbatim refusal in `statusReason`.
	//
	// Send `Idempotency-Key` — without it, retrying after a network timeout sends the guest the same message twice.
	//
	// ### Attachments
	//
	// Send files with `attachments: [{ url, contentType?, filename? }]` — public `https://` URLs, up to 5 per request, 10 MB each. `message` may be omitted when there are attachments (except on Booking.com). Repull downloads each file, reads its real type from the bytes, keeps a durable copy and delivers it through the channel's own file flow. **Every file is checked before anything is sent**: if one is unreachable, too large or of a type the channel refuses, the call returns 422 naming the file (`index`) and the guest receives nothing.
	//
	// | Channel | Accepted types | Text | How it arrives |
	// |---|---|---|---|
	// | Airbnb | JPEG, PNG, GIF, WebP (converted to JPEG), MP4, QuickTime | optional | each file as its own message, then the text as a separate message |
	// | Booking.com | JPEG, PNG | **required** | one message carrying the text and every file |
	// | SMS, email, direct-booking site chat | — | — | `422 attachments_not_supported`, nothing sent |
	//
	// Airbnb does not allow files in pre-booking (inquiry) conversations; that refusal comes back as `422 message_not_sent`. Because Airbnb delivers files one message at a time, a later file can be refused after earlier ones arrived — that returns `422 message_partially_sent` with `parts` saying exactly which messages reached the guest; resend only the rest.
	//
	// The response's `attachments` lists each file's durable `url`, and `parts` lists every channel message the send produced. Read-back (`GET /v1/conversations/{id}/messages`) shows the same files in each message's `attachments`.
	//
	// **Inactive listings:** a conversation that belongs to an inactive listing returns `403 listing_inactive` and no message is sent. Activate the listing first.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/conversations/{id}/messages (the `SendConversationMessage` operationId).
	SendConversationMessage(ctx context.Context, id int, params *SendConversationMessageParams, body SendConversationMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PreapproveConversationWithBody Pre-approve an inquiry
	//
	// Pre-approve the Airbnb inquiry on this conversation: the guest who asked about dates may now book them at the listed price, without waiting on you. To change the dates, guests or price, send a special offer instead (`POST /v1/conversations/{id}/special-offers`).
	//
	// Find inquiries that need an answer with `GET /v1/inquiries` (default `status=open`); each carries the `conversationId` to use here.
	//
	// **Airbnb only**, and only for listings connected to Airbnb directly. A Booking.com, VRBO or direct-booking conversation, or an Airbnb one relayed through a PMS (Hostaway, Guesty), returns `422 channel_not_supported` and nothing is sent.
	//
	// The inquiry is marked `pre_approved` everywhere, the same as pre-approving in Airbnb.
	//
	// An Airbnb refusal is never reported as a success: an inquiry that already moved on is `409 inquiry_no_longer_open`, an expired one `409 inquiry_expired`, a conversation that already has a booking `409 conversation_already_booked`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/conversations/{id}/pre-approval (the `PreapproveConversation` operationId).
	PreapproveConversationWithBody(ctx context.Context, id int, params *PreapproveConversationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PreapproveConversation Pre-approve an inquiry
	//
	// Pre-approve the Airbnb inquiry on this conversation: the guest who asked about dates may now book them at the listed price, without waiting on you. To change the dates, guests or price, send a special offer instead (`POST /v1/conversations/{id}/special-offers`).
	//
	// Find inquiries that need an answer with `GET /v1/inquiries` (default `status=open`); each carries the `conversationId` to use here.
	//
	// **Airbnb only**, and only for listings connected to Airbnb directly. A Booking.com, VRBO or direct-booking conversation, or an Airbnb one relayed through a PMS (Hostaway, Guesty), returns `422 channel_not_supported` and nothing is sent.
	//
	// The inquiry is marked `pre_approved` everywhere, the same as pre-approving in Airbnb.
	//
	// An Airbnb refusal is never reported as a success: an inquiry that already moved on is `409 inquiry_no_longer_open`, an expired one `409 inquiry_expired`, a conversation that already has a booking `409 conversation_already_booked`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/conversations/{id}/pre-approval (the `PreapproveConversation` operationId).
	PreapproveConversation(ctx context.Context, id int, params *PreapproveConversationParams, body PreapproveConversationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateConversationSpecialOfferWithBody Send a special offer
	//
	// Send the guest on this conversation an Airbnb special offer: your own dates, guest count and total price. The guest has 24 hours to book it. Use it to answer an inquiry with different terms, or to make a returning guest a custom price. To accept the guest’s own dates and price as they asked, pre-approve instead (`POST /v1/conversations/{id}/pre-approval`).
	//
	// `listingId` is optional: omit it to offer the listing the guest asked about. It is a **Repull** listing id; Repull sends Airbnb its own listing id, using the link that belongs to this conversation’s Airbnb account.
	//
	// `totalPrice` is the whole stay, in the listing’s Airbnb currency — Airbnb does not take a currency on an offer.
	//
	// **Airbnb only**, and only for listings connected to Airbnb directly; anything else is `422 channel_not_supported` and nothing is sent. The inquiry is marked `special_offer_sent`.
	//
	// An offer Airbnb refuses is never a `201`: dates that are taken, a price below Airbnb’s minimum, too many guests and the like are `422 airbnb_rejected` with Airbnb’s own reason in `message`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again. Without it, a retry after a timeout can send the guest two offers.
	//
	// Read or withdraw the offer with `GET` / `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/conversations/{id}/special-offers (the `CreateConversationSpecialOffer` operationId).
	CreateConversationSpecialOfferWithBody(ctx context.Context, id int, params *CreateConversationSpecialOfferParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateConversationSpecialOffer Send a special offer
	//
	// Send the guest on this conversation an Airbnb special offer: your own dates, guest count and total price. The guest has 24 hours to book it. Use it to answer an inquiry with different terms, or to make a returning guest a custom price. To accept the guest’s own dates and price as they asked, pre-approve instead (`POST /v1/conversations/{id}/pre-approval`).
	//
	// `listingId` is optional: omit it to offer the listing the guest asked about. It is a **Repull** listing id; Repull sends Airbnb its own listing id, using the link that belongs to this conversation’s Airbnb account.
	//
	// `totalPrice` is the whole stay, in the listing’s Airbnb currency — Airbnb does not take a currency on an offer.
	//
	// **Airbnb only**, and only for listings connected to Airbnb directly; anything else is `422 channel_not_supported` and nothing is sent. The inquiry is marked `special_offer_sent`.
	//
	// An offer Airbnb refuses is never a `201`: dates that are taken, a price below Airbnb’s minimum, too many guests and the like are `422 airbnb_rejected` with Airbnb’s own reason in `message`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again. Without it, a retry after a timeout can send the guest two offers.
	//
	// Read or withdraw the offer with `GET` / `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/conversations/{id}/special-offers (the `CreateConversationSpecialOffer` operationId).
	CreateConversationSpecialOffer(ctx context.Context, id int, params *CreateConversationSpecialOfferParams, body CreateConversationSpecialOfferJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// WithdrawConversationSpecialOffer Withdraw a special offer
	//
	// Withdraw a special offer the guest has not booked yet, so it can no longer be booked. An offer the guest already booked cannot be withdrawn — Airbnb refuses with `409 inquiry_no_longer_open`; cancel the booking instead.
	//
	// Corresponds with DELETE /v1/conversations/{id}/special-offers/{offerId} (the `WithdrawConversationSpecialOffer` operationId).
	WithdrawConversationSpecialOffer(ctx context.Context, id int, offerId string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetConversationSpecialOffer Get a special offer
	//
	// Read a special offer on this conversation back from Airbnb — typically to check its `status` (`active` until the guest books it, it expires, or you withdraw it). Read live from Airbnb with the conversation’s own Airbnb account.
	//
	// Corresponds with GET /v1/conversations/{id}/special-offers/{offerId} (the `GetConversationSpecialOffer` operationId).
	GetConversationSpecialOffer(ctx context.Context, id int, offerId string, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListGuests List guests
	//
	// Cursor-paginated list of guests in the workspace. Walks `guests.id ASC` keyset for constant per-page cost regardless of how many guests the customer has. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// Filters: `q` (substring on name/email/phone), `has_reservation` (`true`|`false`), `listing_id` (restrict to guests with at least one reservation on that listing).
	//
	// **Inactive listings:** a guest whose every reservation is on an inactive listing is left out of the page and of `pagination.total`, and `totalReservations`, `totalRevenue`, `firstStayedAt` / `lastStayedAt`, `has_reservation` and `listing_id` only consider reservations that are not on an inactive listing. Guests with no reservations are tied to no listing and are always listed. Filtering by an inactive `listing_id` returns `403 listing_inactive`.
	//
	// Corresponds with GET /v1/guests (the `ListGuests` operationId).
	ListGuests(ctx context.Context, params *ListGuestsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateGuestWithBody Create a guest
	//
	// Creates a guest in the workspace, with contact normalisation applied (phone digits, email lowercased) and each contact stored as its own record.
	//
	// **This is find-or-create, and the response tells you which happened.** A guest already on file matching on email — then phone — AND name is returned instead of a duplicate being created. Read the `created` flag rather than inferring from the status: `201` with `created: true` means a new record was written, `200` with `created: false` means an existing guest matched. Quietly handing back an existing record as though it were new is exactly the ambiguity this flag removes.
	//
	// Field names are camelCase, and an unrecognised field is rejected by name rather than silently dropped.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/guests (the `CreateGuest` operationId).
	CreateGuestWithBody(ctx context.Context, params *CreateGuestParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateGuest Create a guest
	//
	// Creates a guest in the workspace, with contact normalisation applied (phone digits, email lowercased) and each contact stored as its own record.
	//
	// **This is find-or-create, and the response tells you which happened.** A guest already on file matching on email — then phone — AND name is returned instead of a duplicate being created. Read the `created` flag rather than inferring from the status: `201` with `created: true` means a new record was written, `200` with `created: false` means an existing guest matched. Quietly handing back an existing record as though it were new is exactly the ambiguity this flag removes.
	//
	// Field names are camelCase, and an unrecognised field is rejected by name rather than silently dropped.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/guests (the `CreateGuest` operationId).
	CreateGuest(ctx context.Context, params *CreateGuestParams, body CreateGuestJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetGuest Get guest profile
	//
	// Returns the full guest profile — base list-row fields plus contacts, flags, notes, risk metadata, and reservation aggregates. Aggregates main vanio's `GuestService.getGuestProfile()` into the public Repull shape so SDK consumers don't have to learn the internal schema.
	//
	// **Inactive listings:** a guest whose every reservation is on an inactive listing returns `403 listing_inactive` naming those listings (the guest is kept, so this is not a 404). Otherwise the reservation aggregates exclude reservations on inactive listings. A guest with no reservations is always readable.
	//
	// Corresponds with GET /v1/guests/{id} (the `GetGuest` operationId).
	GetGuest(ctx context.Context, id int, params *GetGuestParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetHealth Health check
	//
	// Liveness probe. Returns `{ status: "ok", version }` when the API process is running. No auth required. Suitable for uptime monitors, load-balancer health checks, and SDK self-tests.
	//
	// Corresponds with GET /v1/health (the `GetHealth` operationId).
	GetHealth(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetAuthHealth API-key authentication path health
	//
	// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
	//
	// Corresponds with GET /v1/health/auth (the `GetAuthHealth` operationId).
	GetAuthHealth(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetChannelHealth Per-channel connectivity health
	//
	// Reports reachability and auth state for one channel (`airbnb`, `booking`, `vrbo`, `plumguide`). Use it to tell "the channel is down" apart from "this workspace's connection expired".
	//
	// Corresponds with GET /v1/health/channels/{channel} (the `GetChannelHealth` operationId).
	GetChannelHealth(ctx context.Context, channel GetChannelHealthParamsChannel, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetMcpHealth MCP server reachability
	//
	// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
	//
	// Corresponds with GET /v1/health/mcp (the `GetMcpHealth` operationId).
	GetMcpHealth(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetWebhooksHealth Webhook delivery pipeline health
	//
	// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
	//
	// Corresponds with GET /v1/health/webhooks (the `GetWebhooksHealth` operationId).
	GetWebhooksHealth(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListInquiries List inquiries
	//
	// Airbnb inquiries — guests asking about dates before booking — newest first. By default only `open` ones: nobody has answered and the stay is still ahead. Answer one with `POST /v1/conversations/{conversationId}/pre-approval` (accept their dates and price) or `POST /v1/conversations/{conversationId}/special-offers` (your own terms).
	//
	// Booking **requests** are not inquiries: they are reservations with status `pending` — list them with `GET /v1/reservations?status=pending` and answer with `POST /v1/reservations/{id}/accept` or `/decline`.
	//
	// **Pagination:** pass `pagination.nextCursor` back as `?cursor=` until `pagination.hasMore` is `false`. `?offset=` also works (0..10000). `limit` defaults to 50, max 100.
	//
	// Inquiries on inactive listings are left out; `?listing_id=` naming an inactive listing returns `403 listing_inactive`. `X-Account-Id` narrows to one connected account.
	//
	// Corresponds with GET /v1/inquiries (the `ListInquiries` operationId).
	ListInquiries(ctx context.Context, params *ListInquiriesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ClearKv Clear KV entries by prefix
	//
	// Bulk-deletes every key in the project whose name starts with `prefix`. The `prefix` parameter is required — there is no "delete every key in this project" shortcut; pass an empty `prefix` is rejected with 422 to prevent accidental wipes. Returns the number of rows removed.
	//
	// Corresponds with DELETE /v1/kv (the `ClearKv` operationId).
	ClearKv(ctx context.Context, params *ClearKvParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListKv List KV entries
	//
	// Returns every non-expired key-value row in the given project, sorted ascending by key. Use `prefix` to scope to a key namespace (e.g. `prefix=user:42:` to fetch all entries for one user). Hard cap of 1,000 rows per response — for projects approaching that, paginate by walking prefix buckets.
	//
	// Corresponds with GET /v1/kv (the `ListKv` operationId).
	ListKv(ctx context.Context, params *ListKvParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteKv Delete a KV entry
	//
	// Removes a single key. Returns `{ deleted: true }` if the row was present, `{ deleted: false }` if it was already absent — both are 200 (idempotent).
	//
	// Corresponds with DELETE /v1/kv/{key} (the `DeleteKv` operationId).
	DeleteKv(ctx context.Context, key string, params *DeleteKvParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetKv Get a KV entry
	//
	// Fetches a single key. Returns 404 when the key does not exist OR has expired (rows past `ttl_at` are filtered from reads). Cross-tenant lookups also return 404 — the API never reveals existence of another customer's keys.
	//
	// Corresponds with GET /v1/kv/{key} (the `GetKv` operationId).
	GetKv(ctx context.Context, key string, params *GetKvParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetKvWithBody Set a KV entry
	//
	// Upserts a key. The full row is replaced — there is no partial update. Pass `ttl_seconds` (positive integer) to auto-expire the row; omit for no expiry. **Caps:** 64 KiB per row (key bytes + value JSON bytes), 1 MiB per customer (sum across ALL projects/keys). Over either cap returns 413.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/kv/{key} (the `SetKv` operationId).
	SetKvWithBody(ctx context.Context, key string, params *SetKvParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetKv Set a KV entry
	//
	// Upserts a key. The full row is replaced — there is no partial update. Pass `ttl_seconds` (positive integer) to auto-expire the row; omit for no expiry. **Caps:** 64 KiB per row (key bytes + value JSON bytes), 1 MiB per customer (sum across ALL projects/keys). Over either cap returns 413.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/kv/{key} (the `SetKv` operationId).
	SetKv(ctx context.Context, key string, params *SetKvParams, body SetKvJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListListings List listings
	//
	// Cursor-paginated list of listings owned by the authenticated workspace. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request to walk the full set. `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. Filters: `q` (substring on name/street/city), `status`, `channel`.
	//
	// **Optional expansions:** Pass `?include=content` to enrich each row with the rich content slab (summary, description, space, house rules, etc. — sourced from `listings_descriptions` for the `en` locale). Pass `?include=details` for the structural slab (bedrooms, bathrooms, person capacity, check-in window, wifi, house manual, etc.). Both default to `null` per row when the underlying `listings_descriptions` / `listings_details` row is missing — distinct from the field being absent (which signals the expansion was not requested). Pass `?include=thumbnail` to guarantee `thumbnailUrl` on every returned row — including the reduced inactive ones. Combine comma-separated, e.g. `?include=content,thumbnail`. The default response stays lean; consumers must opt in.
	//
	// **Inactive listings:** by default only active listings are returned. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated, so when `status` asks for inactive ones they carry only `id`, `name`, `status` and `channels` — enough to choose what to activate with `PATCH /v1/listings/{id}`. The `content` and `details` expansions are not applied to them. `?include=thumbnail` is the one exception: it adds `thumbnailUrl` to an inactive row so a single request can render an active/inactive selection screen with pictures, instead of one follow-up call per listing (which an inactive listing would answer with `403 listing_inactive` anyway).
	//
	// Corresponds with GET /v1/listings (the `ListListings` operationId).
	ListListings(ctx context.Context, params *ListListingsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateListingWithBody Create a Repull listing
	//
	// Create a new vacation-rental listing under the authenticated workspace. The listing is stored in the canonical Vanio listings tables and can be published to multiple channels (Airbnb, Booking.com) via the publish endpoints.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/listings (the `CreateListing` operationId).
	CreateListingWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateListing Create a Repull listing
	//
	// Create a new vacation-rental listing under the authenticated workspace. The listing is stored in the canonical Vanio listings tables and can be published to multiple channels (Airbnb, Booking.com) via the publish endpoints.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/listings (the `CreateListing` operationId).
	CreateListing(ctx context.Context, body CreateListingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BulkApplyPricingWithBody Bulk apply or decline pricing recommendations
	//
	// Apply or decline pending Atlas pricing recommendations across many listings in one call. Built for power users with hundreds of listings who would otherwise need 500 sequential single-listing POSTs.
	//
	// - `items` is capped at 500 entries per request — exceeding returns 422.
	// - Per-item failures (stale listing IDs, no pending recs, channel auth blips) DO NOT fail the whole batch — partial success is the norm at this scale and the granular `failed[]` array lets the SDK retry just the bad entries.
	// - Tier-limit accounting: this endpoint counts as **1 API call** regardless of how many items the body contains.
	//
	// Apply path writes the recommended price to each listing's calendar via the calendar service (which fans out to Airbnb/Booking/VRBO) then marks the Atlas recommendation `applied`. Decline path is Atlas-only — fast.
	//
	// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/listings/pricing/bulk (the `BulkApplyPricing` operationId).
	BulkApplyPricingWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// BulkApplyPricing Bulk apply or decline pricing recommendations
	//
	// Apply or decline pending Atlas pricing recommendations across many listings in one call. Built for power users with hundreds of listings who would otherwise need 500 sequential single-listing POSTs.
	//
	// - `items` is capped at 500 entries per request — exceeding returns 422.
	// - Per-item failures (stale listing IDs, no pending recs, channel auth blips) DO NOT fail the whole batch — partial success is the norm at this scale and the granular `failed[]` array lets the SDK retry just the bad entries.
	// - Tier-limit accounting: this endpoint counts as **1 API call** regardless of how many items the body contains.
	//
	// Apply path writes the recommended price to each listing's calendar via the calendar service (which fans out to Airbnb/Booking/VRBO) then marks the Atlas recommendation `applied`. Decline path is Atlas-only — fast.
	//
	// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/listings/pricing/bulk (the `BulkApplyPricing` operationId).
	BulkApplyPricing(ctx context.Context, body BulkApplyPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetListingsStatusWithBody Activate or deactivate listings in bulk
	//
	// Sets up to 500 listings active or inactive in one call. Send `{ "listingIds": ["4118", "4119"], "active": false }` to deactivate them, or `"active": true` to activate them.
	//
	// An inactive listing is not counted toward your plan's listing limit or billed. It is NOT deleted and the upstream channel (Airbnb / Booking.com / your PMS) is never touched — its data keeps syncing, so it is complete the moment you activate it again. Until then it cannot be read, changed, or receive webhooks.
	//
	// **All or nothing.** Nothing changes unless the whole request can be applied:
	// - If any id is not one of your listings, the call returns `404` naming those ids.
	// - If activating would take you over your plan's listing limit, the call returns `402 listings_limit_exceeded`. Only listings that are currently inactive count toward the new total, so re-sending ids that are already active never trips the limit.
	//
	// Deactivating is always allowed, including when your account is already over its limit — it is how you get back under it.
	//
	// **Idempotent.** Ids already in the requested state are returned in `unchanged`; ids this call changed are returned in `updated`.
	//
	// For a single listing, `PATCH /v1/listings/{id}` does the same.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/listings/status (the `SetListingsStatus` operationId).
	SetListingsStatusWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// SetListingsStatus Activate or deactivate listings in bulk
	//
	// Sets up to 500 listings active or inactive in one call. Send `{ "listingIds": ["4118", "4119"], "active": false }` to deactivate them, or `"active": true` to activate them.
	//
	// An inactive listing is not counted toward your plan's listing limit or billed. It is NOT deleted and the upstream channel (Airbnb / Booking.com / your PMS) is never touched — its data keeps syncing, so it is complete the moment you activate it again. Until then it cannot be read, changed, or receive webhooks.
	//
	// **All or nothing.** Nothing changes unless the whole request can be applied:
	// - If any id is not one of your listings, the call returns `404` naming those ids.
	// - If activating would take you over your plan's listing limit, the call returns `402 listings_limit_exceeded`. Only listings that are currently inactive count toward the new total, so re-sending ids that are already active never trips the limit.
	//
	// Deactivating is always allowed, including when your account is already over its limit — it is how you get back under it.
	//
	// **Idempotent.** Ids already in the requested state are returned in `unchanged`; ids this call changed are returned in `updated`.
	//
	// For a single listing, `PATCH /v1/listings/{id}` does the same.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/listings/status (the `SetListingsStatus` operationId).
	SetListingsStatus(ctx context.Context, body SetListingsStatusJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeactivateListing Deactivate (exclude) a listing
	//
	// Deactivate a listing — sets it inactive and excludes it from Repull. This is a **soft** operation: the listing row is KEPT (never hard-deleted) and the upstream channel (Airbnb / Hospitable / Booking.com) is NEVER touched. Repull only mutates its own copy.
	//
	// Equivalent to `PATCH /v1/listings/{id}` with `{ "active": false }`. This is the primary self-serve way for a free-tier customer to trim back under the plan-listings cap — `DELETE` is served even when the account is over the cap (a 402-locked account can still call it). To bring a listing back, use `PATCH` with `{ "active": true }`.
	//
	// Idempotent: deactivating an already-inactive listing returns 200.
	//
	// To deactivate many listings at once, use `POST /v1/listings/status` with `{ "active": false }`.
	//
	// Corresponds with DELETE /v1/listings/{id} (the `DeactivateListing` operationId).
	DeactivateListing(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetListing Get a listing
	//
	// Fetch a single listing by id. Returns the same shape as one element of the `GET /v1/listings` response, so you can bind the result to the same model. Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403, never reveals the listing's existence.
	//
	// **Optional expansions:** Pass `?include=amenities` to enrich the response with the listing's amenity rows (`[]` when the listing has none). Pass `?include=content` for the rich content slab (summary, description, space, house rules, etc. — sourced from `listings_descriptions` for the `en` locale; `null` when no row is stored). Pass `?include=details` for the structural slab (bedrooms, bathrooms, person capacity, check-in window, wifi, house manual, etc.; `null` when no row is stored). Combine comma-separated, e.g. `?include=amenities,content,details`. The default response stays lean; consumers must opt in.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/listings/{id} (the `GetListing` operationId).
	GetListing(ctx context.Context, id int, params *GetListingParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateListingActiveWithBody Deactivate or reactivate a listing
	//
	// Toggle a listing's active state. Send `{ "active": false }` to **deactivate** (exclude the listing from Repull) or `{ "active": true }` to **reactivate** it.
	//
	// "Deactivate" keeps the listing row — it is NOT a hard delete, and it NEVER touches the upstream channel (Airbnb / Hospitable / Booking.com). Repull only mutates its own copy of the inventory. Deactivating is the self-serve way to get back under the plan-listings cap without paying.
	//
	// Reactivation respects the plan-listings cap: if activating this listing would push you over the cap for your tier, the call returns `402 listings_limit_exceeded` and the listing stays inactive. Deactivate another listing or upgrade first.
	//
	// Idempotent: setting a listing to the state it's already in returns 200.
	//
	// To change many listings at once, all or nothing, use `POST /v1/listings/status`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /v1/listings/{id} (the `UpdateListingActive` operationId).
	UpdateListingActiveWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateListingActive Deactivate or reactivate a listing
	//
	// Toggle a listing's active state. Send `{ "active": false }` to **deactivate** (exclude the listing from Repull) or `{ "active": true }` to **reactivate** it.
	//
	// "Deactivate" keeps the listing row — it is NOT a hard delete, and it NEVER touches the upstream channel (Airbnb / Hospitable / Booking.com). Repull only mutates its own copy of the inventory. Deactivating is the self-serve way to get back under the plan-listings cap without paying.
	//
	// Reactivation respects the plan-listings cap: if activating this listing would push you over the cap for your tier, the call returns `402 listings_limit_exceeded` and the listing stays inactive. Deactivate another listing or upgrade first.
	//
	// Idempotent: setting a listing to the state it's already in returns 200.
	//
	// To change many listings at once, all or nothing, use `POST /v1/listings/status`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /v1/listings/{id} (the `UpdateListingActive` operationId).
	UpdateListingActive(ctx context.Context, id int, body UpdateListingActiveJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListListingComps Comp set for a listing (with daily nightly pricing)
	//
	// Returns the actual comp set for a listing — the underlying competitor listings (with daily nightly pricing), not just the aggregated `compSummary` from `/pricing`. Each comp comes back with distance, bedrooms, ratings, lat/lng, platform link, and a per-day rate/availability series for the requested window.
	//
	// Powered by Atlas. Comps with no coordinates are excluded — there's no way to rank them by distance. Listings without coordinates return `data: []` and a `warning` field.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/listings/{id}/comps (the `ListListingComps` operationId).
	ListListingComps(ctx context.Context, id int, params *ListListingCompsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateListingContentWithBody Update canonical listing content
	//
	// Write your PMS's canonical listing content — title, description, amenities, address, occupancy, and policies — into a Repull listing, making it the source of truth. This is the flagship "the PMS owns listing content, Repull distributes it" enabler.
	//
	// **Partial update:** every field is optional. Only the fields you send are written; absent fields are left untouched. `amenities` is a FULL replacement of the amenity set (omit to leave untouched, send `[]` to clear).
	//
	// **Multilingual:** send `locale` to say which language this copy is in (`it`, `pt-BR`, …). Canonical content is stored per locale, so each language keeps its own row instead of overwriting the English one. Omit it for English.
	//
	// **Local write only — NOT a channel publish.** This mutates Repull's own copy of the content. It does NOT push to Airbnb / Booking.com; it marks the channels dirty so a later publish knows what changed. Distribution stays a separate explicit step.
	//
	// **Photos are deferred:** a provided `photos` array is echoed back in the `deferred` field and NOT persisted (media ingestion is a follow-up).
	//
	// Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403. This endpoint is served even when the account is over the plan-listings cap, since editing content on a listing you already own never grows the portfolio.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/listings/{id}/content (the `UpdateListingContent` operationId).
	UpdateListingContentWithBody(ctx context.Context, id int, params *UpdateListingContentParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateListingContent Update canonical listing content
	//
	// Write your PMS's canonical listing content — title, description, amenities, address, occupancy, and policies — into a Repull listing, making it the source of truth. This is the flagship "the PMS owns listing content, Repull distributes it" enabler.
	//
	// **Partial update:** every field is optional. Only the fields you send are written; absent fields are left untouched. `amenities` is a FULL replacement of the amenity set (omit to leave untouched, send `[]` to clear).
	//
	// **Multilingual:** send `locale` to say which language this copy is in (`it`, `pt-BR`, …). Canonical content is stored per locale, so each language keeps its own row instead of overwriting the English one. Omit it for English.
	//
	// **Local write only — NOT a channel publish.** This mutates Repull's own copy of the content. It does NOT push to Airbnb / Booking.com; it marks the channels dirty so a later publish knows what changed. Distribution stays a separate explicit step.
	//
	// **Photos are deferred:** a provided `photos` array is echoed back in the `deferred` field and NOT persisted (media ingestion is a follow-up).
	//
	// Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403. This endpoint is served even when the account is over the plan-listings cap, since editing content on a listing you already own never grows the portfolio.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/listings/{id}/content (the `UpdateListingContent` operationId).
	UpdateListingContent(ctx context.Context, id int, params *UpdateListingContentParams, body UpdateListingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GenerateListingContentWithBody AI-generate listing content
	//
	// Generate guest-facing copy (title, summary, description, amenities, etc.) for a listing using Repull AI. When `photos` are provided the vision model is used for photo-grounded copy. Persists into the listing by default.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/listings/{id}/generate-content (the `GenerateListingContent` operationId).
	GenerateListingContentWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GenerateListingContent AI-generate listing content
	//
	// Generate guest-facing copy (title, summary, description, amenities, etc.) for a listing using Repull AI. When `photos` are provided the vision model is used for photo-grounded copy. Persists into the listing by default.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/listings/{id}/generate-content (the `GenerateListingContent` operationId).
	GenerateListingContent(ctx context.Context, id int, body GenerateListingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// TakeListingOfflineWithBody Take a listing off the market
	//
	// Stop this listing being sold, on every channel it is connected to, in one call.
	//
	// What that means differs per channel and you do not have to know which is which. On **Airbnb** the live listing is deactivated with a valid deactivation reason and then READ BACK — Airbnb accepts some deactivations and leaves the listing up, so "we sent the request" is never reported as success. On **Booking.com** there is no unlist at all; the equivalent is closing the room's availability across the whole forward window, which is what happens.
	//
	// **This is not the same as deactivating the listing in Repull.** The two get confused because both sound like removal, and they have opposite consequences:
	//
	// | | Take offline (this endpoint) | Deactivate in Repull (`PATCH /v1/listings/{id}` `{"active": false}`) |
	// |---|---|---|
	// | The guest-facing listing | **Stops taking bookings** | Stays live and keeps taking bookings |
	// | Billing and plan limits | Unchanged | No longer billed, no longer counts toward the cap |
	// | API access to the listing | Unchanged — you can still read and write it | `403 listing_inactive` until reactivated |
	// | Reverse it with | `POST /v1/listings/{id}/online` | `PATCH /v1/listings/{id}` `{"active": true}` |
	// | Data kept | Yes | Yes, and it keeps syncing |
	//
	// Neither one deletes anything, on either side.
	//
	// **The answer is per channel item.** A listing can sit on several Airbnb connections and a Booking.com property at once; they fail independently and a partial result is the ordinary outcome, so every item reports its own `state`, `code` and `message` and there is no top-level success flag to mislead you. Nothing is rolled back — re-send the same request to retry the items that did not land.
	//
	// **Booking.com ambiguity is reported, not fanned out.** A listing mapped to more than one active Booking.com property comes back with that item refused (`ambiguous_booking_mapping`) while the Airbnb items still run: closing the wrong property's availability takes real inventory off sale, and taking a listing off Airbnb is not less urgent because its Booking.com mapping is untidy. Name the property with `hotelId` and send it again.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/listings/{id}/offline (the `TakeListingOffline` operationId).
	TakeListingOfflineWithBody(ctx context.Context, id int, params *TakeListingOfflineParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// TakeListingOffline Take a listing off the market
	//
	// Stop this listing being sold, on every channel it is connected to, in one call.
	//
	// What that means differs per channel and you do not have to know which is which. On **Airbnb** the live listing is deactivated with a valid deactivation reason and then READ BACK — Airbnb accepts some deactivations and leaves the listing up, so "we sent the request" is never reported as success. On **Booking.com** there is no unlist at all; the equivalent is closing the room's availability across the whole forward window, which is what happens.
	//
	// **This is not the same as deactivating the listing in Repull.** The two get confused because both sound like removal, and they have opposite consequences:
	//
	// | | Take offline (this endpoint) | Deactivate in Repull (`PATCH /v1/listings/{id}` `{"active": false}`) |
	// |---|---|---|
	// | The guest-facing listing | **Stops taking bookings** | Stays live and keeps taking bookings |
	// | Billing and plan limits | Unchanged | No longer billed, no longer counts toward the cap |
	// | API access to the listing | Unchanged — you can still read and write it | `403 listing_inactive` until reactivated |
	// | Reverse it with | `POST /v1/listings/{id}/online` | `PATCH /v1/listings/{id}` `{"active": true}` |
	// | Data kept | Yes | Yes, and it keeps syncing |
	//
	// Neither one deletes anything, on either side.
	//
	// **The answer is per channel item.** A listing can sit on several Airbnb connections and a Booking.com property at once; they fail independently and a partial result is the ordinary outcome, so every item reports its own `state`, `code` and `message` and there is no top-level success flag to mislead you. Nothing is rolled back — re-send the same request to retry the items that did not land.
	//
	// **Booking.com ambiguity is reported, not fanned out.** A listing mapped to more than one active Booking.com property comes back with that item refused (`ambiguous_booking_mapping`) while the Airbnb items still run: closing the wrong property's availability takes real inventory off sale, and taking a listing off Airbnb is not less urgent because its Booking.com mapping is untidy. Name the property with `hotelId` and send it again.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/listings/{id}/offline (the `TakeListingOffline` operationId).
	TakeListingOffline(ctx context.Context, id int, params *TakeListingOfflineParams, body TakeListingOfflineJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// TakeListingOnlineWithBody Put a listing back on the market
	//
	// Put this listing back on sale, on every channel it is connected to. The counterpart of `POST /v1/listings/{id}/offline`, which documents the per-item response and the difference between this and deactivating a listing in Repull.
	//
	// **It does not push content.** On **Airbnb** it re-enables sync and makes the listing available again; anything that changed while the listing was down is still unpublished, so follow with `POST /v1/listings/{id}/publish/airbnb` if the content moved. On **Booking.com** it re-syncs the true calendar rather than opening everything: dates that are genuinely blocked — a reservation, an owner stay — stay blocked, and only the closure `offline` wrote lifts. The two directions are not mirror images, and that is deliberate.
	//
	// **One asymmetry worth planning for.** Taking a listing down passes no billing gate; putting it back up goes through the channel-publish gate. So on a workspace whose subscription has lapsed, `offline` still works and this endpoint answers `402 payment_required` — a listing can be left off the market until billing is sorted out. That refusal is reported as a billing refusal with the action that fixes it, never as a channel error: retrying, or reconnecting the channel, does nothing for it.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/listings/{id}/online (the `TakeListingOnline` operationId).
	TakeListingOnlineWithBody(ctx context.Context, id int, params *TakeListingOnlineParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// TakeListingOnline Put a listing back on the market
	//
	// Put this listing back on sale, on every channel it is connected to. The counterpart of `POST /v1/listings/{id}/offline`, which documents the per-item response and the difference between this and deactivating a listing in Repull.
	//
	// **It does not push content.** On **Airbnb** it re-enables sync and makes the listing available again; anything that changed while the listing was down is still unpublished, so follow with `POST /v1/listings/{id}/publish/airbnb` if the content moved. On **Booking.com** it re-syncs the true calendar rather than opening everything: dates that are genuinely blocked — a reservation, an owner stay — stay blocked, and only the closure `offline` wrote lifts. The two directions are not mirror images, and that is deliberate.
	//
	// **One asymmetry worth planning for.** Taking a listing down passes no billing gate; putting it back up goes through the channel-publish gate. So on a workspace whose subscription has lapsed, `offline` still works and this endpoint answers `402 payment_required` — a listing can be left off the market until billing is sorted out. That refusal is reported as a billing refusal with the action that fixes it, never as a channel error: retrying, or reconnecting the channel, does nothing for it.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/listings/{id}/online (the `TakeListingOnline` operationId).
	TakeListingOnline(ctx context.Context, id int, params *TakeListingOnlineParams, body TakeListingOnlineJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteListingPhotoWithBody Delete a stored listing photo
	//
	// Deletes a single stored photo by its storage `path` (as returned by `GET /v1/listings/{id}/photos` or `POST /v1/listings/{id}/photos/upload-url`).
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with DELETE /v1/listings/{id}/photos (the `DeleteListingPhoto` operationId).
	DeleteListingPhotoWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteListingPhoto Delete a stored listing photo
	//
	// Deletes a single stored photo by its storage `path` (as returned by `GET /v1/listings/{id}/photos` or `POST /v1/listings/{id}/photos/upload-url`).
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with DELETE /v1/listings/{id}/photos (the `DeleteListingPhoto` operationId).
	DeleteListingPhoto(ctx context.Context, id int, body DeleteListingPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListListingPhotos List a listing's stored photos
	//
	// Returns the photo set currently stored for this listing.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/listings/{id}/photos (the `ListListingPhotos` operationId).
	ListListingPhotos(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateListingPhotoUploadUrlWithBody Mint a direct-to-storage photo upload URL
	//
	// Mints a short-lived signed upload URL + token for a listing photo. **The client PUTs the raw file bytes directly to the returned `uploadUrl` — the file bytes never pass through the Repull API or main vanio.** This endpoint only mints the URL; do not POST the file itself here, it will not be accepted.
	//
	// Flow: (1) POST here with `fileName`/`fileType`/optional `fileSize` to get `{ uploadUrl, token, path, publicUrl, expiresIn }`; (2) PUT the raw file bytes to `uploadUrl` from the client; (3) `publicUrl` is the durable URL for the uploaded photo — attach it to the listing via `PUT /v1/listings/{id}/content` (`photos` field) or list it back via `GET /v1/listings/{id}/photos`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/listings/{id}/photos/upload-url (the `CreateListingPhotoUploadUrl` operationId).
	CreateListingPhotoUploadUrlWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateListingPhotoUploadUrl Mint a direct-to-storage photo upload URL
	//
	// Mints a short-lived signed upload URL + token for a listing photo. **The client PUTs the raw file bytes directly to the returned `uploadUrl` — the file bytes never pass through the Repull API or main vanio.** This endpoint only mints the URL; do not POST the file itself here, it will not be accepted.
	//
	// Flow: (1) POST here with `fileName`/`fileType`/optional `fileSize` to get `{ uploadUrl, token, path, publicUrl, expiresIn }`; (2) PUT the raw file bytes to `uploadUrl` from the client; (3) `publicUrl` is the durable URL for the uploaded photo — attach it to the listing via `PUT /v1/listings/{id}/content` (`photos` field) or list it back via `GET /v1/listings/{id}/photos`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/listings/{id}/photos/upload-url (the `CreateListingPhotoUploadUrl` operationId).
	CreateListingPhotoUploadUrl(ctx context.Context, id int, body CreateListingPhotoUploadUrlJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetListingPricing Get pricing recommendations
	//
	// Returns date-by-date pricing recommendations for a listing's upcoming calendar window, plus the listing's base-price context and a 5km comp summary. Recommendations come from the Atlas pricing model — pre-computed nightly and stored in `pricing_recommendations`. Use POST to apply or decline pending recommendations.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/listings/{id}/pricing (the `GetListingPricing` operationId).
	GetListingPricing(ctx context.Context, id int, params *GetListingPricingParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ApplyListingPricingWithBody Apply or decline pricing recommendations
	//
	// Apply: writes the recommended price to the listing's calendar for the given dates and triggers the platform fan-out (Airbnb / Booking.com / VRBO). Decline: marks the recommendation as `declined` so it stops surfacing — the model can re-recommend on the next training cycle.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/listings/{id}/pricing (the `ApplyListingPricing` operationId).
	ApplyListingPricingWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ApplyListingPricing Apply or decline pricing recommendations
	//
	// Apply: writes the recommended price to the listing's calendar for the given dates and triggers the platform fan-out (Airbnb / Booking.com / VRBO). Decline: marks the recommendation as `declined` so it stops surfacing — the model can re-recommend on the next training cycle.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/listings/{id}/pricing (the `ApplyListingPricing` operationId).
	ApplyListingPricing(ctx context.Context, id int, body ApplyListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetListingPricingHistory Pricing recommendation audit trail
	//
	// Cursor-paginated audit trail of pricing recommendations vs applied prices for a listing across a date window. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
	//
	// Defaults to ±90 days from today. Cursor is a keyset on `date ASC` — stable even if rows are added during a partner's pagination walk. `limit` is capped at 500 — exceeding returns 422.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/listings/{id}/pricing/history (the `GetListingPricingHistory` operationId).
	GetListingPricingHistory(ctx context.Context, id int, params *GetListingPricingHistoryParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetListingPricingStrategy Get pricing strategy
	//
	// Returns the strategy that constrains how the Atlas pricing model behaves for this listing. If no strategy row exists yet, returns sane defaults flagged with `isDefault: true`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/listings/{id}/pricing/strategy (the `GetListingPricingStrategy` operationId).
	GetListingPricingStrategy(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateListingPricingStrategyWithBody Update pricing strategy
	//
	// Upserts the strategy on `(listing_id, customer_id)` — repeated PUTs are idempotent. Send only the fields you want to change; omitted fields take server-side defaults.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PUT /v1/listings/{id}/pricing/strategy (the `UpdateListingPricingStrategy` operationId).
	UpdateListingPricingStrategyWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateListingPricingStrategy Update pricing strategy
	//
	// Upserts the strategy on `(listing_id, customer_id)` — repeated PUTs are idempotent. Send only the fields you want to change; omitted fields take server-side defaults.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PUT /v1/listings/{id}/pricing/strategy (the `UpdateListingPricingStrategy` operationId).
	UpdateListingPricingStrategy(ctx context.Context, id int, body UpdateListingPricingStrategyJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetListingPublishStatus Per-channel publish status
	//
	// Returns connection state and sync activity per channel. `channels` is sync activity (empty until first push). `connections` is connection state (populated as soon as a channel is linked). Recommended polling cadence: at most once per 30s per listing — for bulk views, prefer `GET /v1/listings` and filter client-side.
	//
	// **When a push fails, this endpoint says why.** `channels[].pushError` carries the channel's own reason for the last failed push, verbatim — `"Links and contact info can't be shared"`, `"Check-in start time must be before end time"`, `"property_type_group must be one of […]"`. It is free text written by the channel, so render it next to the retry button rather than parsing it. `null` when the last push succeeded or none has run; pair it with `pushStatus` to tell those two apart.
	//
	// **It also says what you will not be allowed to change.** The `airbnb` entry in `connections` carries `lockedFields` — attributes Airbnb has locked on this listing. Airbnb does not refuse a write to one: it answers 200, reports the field as locked, and applies nothing, so a locked write is indistinguishable from a successful one unless you looked first. Read it before you let someone edit. Airbnb-only; no other channel has the concept, and no other entry carries the field.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/listings/{id}/publish-status (the `GetListingPublishStatus` operationId).
	GetListingPublishStatus(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PublishListingToAirbnbWithBody Publish a listing to Airbnb
	//
	// Push a Repull listing's canonical content to Airbnb. Pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create a brand-new Airbnb listing under that host.
	//
	// **A publish is not one call to Airbnb.** It is up to eight independent ones — details, description, amenities, rooms, policies, photos, pricing, checkout_tasks — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Airbnb's own reason, per section, for the ones that did not. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Publish again once you have fixed the failing sections — a re-publish of an unchanged section is harmless.
	//
	// `result.live` is a different question from `result.published`. `published` is about CONTENT — every attempted section landed. `live` is about whether the listing takes bookings: it is true only when activation was actually performed and succeeded. A create can land all eight sections and still leave the listing inactive, because activation is skipped when instant-booking cannot be confirmed to be off — so `published: true` with `live: false` is a real and common outcome, and `result.warnings` says why. `live` is ABSENT, not `false`, when activation was never part of the operation: publishing to an already-mapped listing updates content and activates nothing. Only treat a listing as not-live when `live` is present and false.
	//
	// `result.warnings[]` lists steps that failed WITHOUT failing the publish — optional work the push carried on past. They were previously swallowed, so the only sign of one was a listing that was somehow not quite right afterwards. A publish can be `published: true` and still carry warnings; read them before concluding nothing needs doing.
	//
	// `result.lockedFields` names the fields Airbnb will not let this listing change at all. They are not retryable by anyone: Airbnb answers 200 and applies nothing. `GET /v1/channels/airbnb/listings/{id}` reports the same list up front.
	//
	// **Which fields this pushes** — title, description sections and house rules (English/primary locale), amenities, rooms and beds, photos, nightly price and fees, cancellation policy and guest controls, check-in/out times, quiet hours, property and room type, checkout tasks. **Not pushed by this endpoint:** non-primary locales (`PUT /v1/channels/airbnb/listings/{id}/descriptions`), guest-safety disclosures (`PUT …/safety-disclosures`), check-in method (`PUT …/details`), permits (`PUT …/permits`), and the calendar (`PUT …/availability`).
	//
	// `force: true` re-pushes every section, ignoring dirty-field tracking. Without it only the sections changed since the last successful publish are sent.
	//
	// Send `Idempotency-Key` to make a retry safe: a timeout on a publish otherwise leaves you unable to tell whether it ran.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/listings/{id}/publish/airbnb (the `PublishListingToAirbnb` operationId).
	PublishListingToAirbnbWithBody(ctx context.Context, id int, params *PublishListingToAirbnbParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PublishListingToAirbnb Publish a listing to Airbnb
	//
	// Push a Repull listing's canonical content to Airbnb. Pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create a brand-new Airbnb listing under that host.
	//
	// **A publish is not one call to Airbnb.** It is up to eight independent ones — details, description, amenities, rooms, policies, photos, pricing, checkout_tasks — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Airbnb's own reason, per section, for the ones that did not. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Publish again once you have fixed the failing sections — a re-publish of an unchanged section is harmless.
	//
	// `result.live` is a different question from `result.published`. `published` is about CONTENT — every attempted section landed. `live` is about whether the listing takes bookings: it is true only when activation was actually performed and succeeded. A create can land all eight sections and still leave the listing inactive, because activation is skipped when instant-booking cannot be confirmed to be off — so `published: true` with `live: false` is a real and common outcome, and `result.warnings` says why. `live` is ABSENT, not `false`, when activation was never part of the operation: publishing to an already-mapped listing updates content and activates nothing. Only treat a listing as not-live when `live` is present and false.
	//
	// `result.warnings[]` lists steps that failed WITHOUT failing the publish — optional work the push carried on past. They were previously swallowed, so the only sign of one was a listing that was somehow not quite right afterwards. A publish can be `published: true` and still carry warnings; read them before concluding nothing needs doing.
	//
	// `result.lockedFields` names the fields Airbnb will not let this listing change at all. They are not retryable by anyone: Airbnb answers 200 and applies nothing. `GET /v1/channels/airbnb/listings/{id}` reports the same list up front.
	//
	// **Which fields this pushes** — title, description sections and house rules (English/primary locale), amenities, rooms and beds, photos, nightly price and fees, cancellation policy and guest controls, check-in/out times, quiet hours, property and room type, checkout tasks. **Not pushed by this endpoint:** non-primary locales (`PUT /v1/channels/airbnb/listings/{id}/descriptions`), guest-safety disclosures (`PUT …/safety-disclosures`), check-in method (`PUT …/details`), permits (`PUT …/permits`), and the calendar (`PUT …/availability`).
	//
	// `force: true` re-pushes every section, ignoring dirty-field tracking. Without it only the sections changed since the last successful publish are sent.
	//
	// Send `Idempotency-Key` to make a retry safe: a timeout on a publish otherwise leaves you unable to tell whether it ran.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/listings/{id}/publish/airbnb (the `PublishListingToAirbnb` operationId).
	PublishListingToAirbnb(ctx context.Context, id int, params *PublishListingToAirbnbParams, body PublishListingToAirbnbJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PublishListingToBookingWithBody Publish a listing to Booking.com
	//
	// Push a Repull listing's content to Booking.com. The listing must already be mapped to a Booking.com property + room — claim the hotel through the Connect Booking flow, then map its rooms with `POST /v1/connect/booking/map-rooms`.
	//
	// **Which property the content lands in.** A listing can be mapped to more than one Booking.com property; the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. When the listing has exactly one property you need send nothing. When it has several, name one with `hotelId` in the body (or `?hotel_id=` — the same value, accepted either way, body wins if you send both). Omit it on such a listing and the push is refused with **`409 ambiguous_booking_mapping`**, listing the candidate ids: content pushed into a property chosen for you lands on the wrong listing and reports success, which is worse than a refusal. `GET /v1/channels/booking/properties` lists every property with the listings mapped under it. Naming a property this listing is not mapped to is a `404` that names the ones it is.
	//
	// The property that actually received the content comes back as `result.hotelId`.
	//
	// **A publish is not one call to Booking.com.** It is several independent Content API calls — details, description, amenities, rooms, photos, pricing — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Booking.com's own reason, per section, for the ones that did not. A property whose Content API credentials do not cover a section answers 403 for that section alone. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Fix the failing sections and publish again — re-publishing an unchanged section is harmless.
	//
	// A listing with no Booking.com property mapped at all is not an error: the call returns `result.published: false` with `result.reason` and `result.hotelId: null`, and nothing is pushed.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/listings/{id}/publish/booking (the `PublishListingToBooking` operationId).
	PublishListingToBookingWithBody(ctx context.Context, id int, params *PublishListingToBookingParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PublishListingToBooking Publish a listing to Booking.com
	//
	// Push a Repull listing's content to Booking.com. The listing must already be mapped to a Booking.com property + room — claim the hotel through the Connect Booking flow, then map its rooms with `POST /v1/connect/booking/map-rooms`.
	//
	// **Which property the content lands in.** A listing can be mapped to more than one Booking.com property; the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. When the listing has exactly one property you need send nothing. When it has several, name one with `hotelId` in the body (or `?hotel_id=` — the same value, accepted either way, body wins if you send both). Omit it on such a listing and the push is refused with **`409 ambiguous_booking_mapping`**, listing the candidate ids: content pushed into a property chosen for you lands on the wrong listing and reports success, which is worse than a refusal. `GET /v1/channels/booking/properties` lists every property with the listings mapped under it. Naming a property this listing is not mapped to is a `404` that names the ones it is.
	//
	// The property that actually received the content comes back as `result.hotelId`.
	//
	// **A publish is not one call to Booking.com.** It is several independent Content API calls — details, description, amenities, rooms, photos, pricing — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Booking.com's own reason, per section, for the ones that did not. A property whose Content API credentials do not cover a section answers 403 for that section alone. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Fix the failing sections and publish again — re-publishing an unchanged section is harmless.
	//
	// A listing with no Booking.com property mapped at all is not an error: the call returns `result.published: false` with `result.reason` and `result.hotelId: null`, and nothing is pushed.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/listings/{id}/publish/booking (the `PublishListingToBooking` operationId).
	PublishListingToBooking(ctx context.Context, id int, params *PublishListingToBookingParams, body PublishListingToBookingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PullListingFromAirbnbWithBody Refresh a listing from Airbnb
	//
	// Re-read this listing from Airbnb and update your stored copy, then report what was refreshed and when. The mirror image of `POST /v1/listings/{id}/publish/airbnb`.
	//
	// Every other Airbnb read on this API is served from our database. This endpoint is the one that goes and asks Airbnb — use it after a push, to see the values Airbnb actually kept, or when a host has changed something in the Airbnb app.
	//
	// **What it refreshes:** basic listing facts (property type, bedrooms, beds, bathrooms, capacity), descriptions, photos, rooms and beds, amenities, booking settings (check-in/check-out windows, guest controls, cancellation policy), stay rules (min/max nights, advance-booking window, turnover buffer), pricing settings and standard fees, permits, checkout tasks and the check-in guide. After it returns, those values are what `GET /v1/listings/{id}?include=content,details` and the `/v1/channels/airbnb/**` routes serve.
	//
	// **What it does NOT refresh:** the calendar (nightly rates and availability — see `GET /v1/channels/airbnb/listings/{id}/availability`), reservations, messages, reviews or payouts. Those arrive continuously through the channel's own sync and never need a manual pull.
	//
	// **Runs synchronously** — the response is the result, not a job id. Expect several seconds.
	//
	// **One pull per listing per 15 minutes.** A pull is roughly a dozen Airbnb calls; a second call inside the window returns `429 rate_limit_exceeded` with `Retry-After` and `nextPullAvailableAt`, and makes no Airbnb calls. Two simultaneous calls cannot both run.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/listings/{id}/pull/airbnb (the `PullListingFromAirbnb` operationId).
	PullListingFromAirbnbWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PullListingFromAirbnb Refresh a listing from Airbnb
	//
	// Re-read this listing from Airbnb and update your stored copy, then report what was refreshed and when. The mirror image of `POST /v1/listings/{id}/publish/airbnb`.
	//
	// Every other Airbnb read on this API is served from our database. This endpoint is the one that goes and asks Airbnb — use it after a push, to see the values Airbnb actually kept, or when a host has changed something in the Airbnb app.
	//
	// **What it refreshes:** basic listing facts (property type, bedrooms, beds, bathrooms, capacity), descriptions, photos, rooms and beds, amenities, booking settings (check-in/check-out windows, guest controls, cancellation policy), stay rules (min/max nights, advance-booking window, turnover buffer), pricing settings and standard fees, permits, checkout tasks and the check-in guide. After it returns, those values are what `GET /v1/listings/{id}?include=content,details` and the `/v1/channels/airbnb/**` routes serve.
	//
	// **What it does NOT refresh:** the calendar (nightly rates and availability — see `GET /v1/channels/airbnb/listings/{id}/availability`), reservations, messages, reviews or payouts. Those arrive continuously through the channel's own sync and never need a manual pull.
	//
	// **Runs synchronously** — the response is the result, not a job id. Expect several seconds.
	//
	// **One pull per listing per 15 minutes.** A pull is roughly a dozen Airbnb calls; a second call inside the window returns `429 rate_limit_exceeded` with `Retry-After` and `nextPullAvailableAt`, and makes no Airbnb calls. Two simultaneous calls cannot both run.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/listings/{id}/pull/airbnb (the `PullListingFromAirbnb` operationId).
	PullListingFromAirbnb(ctx context.Context, id int, body PullListingFromAirbnbJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetListingSegments Atlas DNA segment intelligence for a listing
	//
	// Aggregates Atlas DNA segment signal (quality tier, design style, bedrooms) across the listing's geographic neighborhood (default: 5km radius) or the whole city, so consumers can answer:
	// - What segments dominate my market?
	// - Which segment does my listing match best?
	// - What's the ADR uplift for moving up a tier?
	//
	// DNA coverage is still ramping — segments are scored asynchronously. Cities and radii without scored comps return `totalCompsAnalyzed: 0` plus a `low_dna_coverage` recommendation rather than fabricated data.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/listings/{id}/segments (the `GetListingSegments` operationId).
	GetListingSegments(ctx context.Context, id int, params *GetListingSegmentsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListMarkets List markets the customer operates in
	//
	// Returns per-city KPIs across every market the authenticated customer has listings in (market share, ADR vs market, occupancy, ratings) plus a lightweight `browse` discovery summary (top-50 featured markets, country categories, total catalog size). For the full paginated discovery catalog with search, call `GET /v1/markets/browse`. Each `markets[]` entry is enriched with `subscribed` + `source` from the customer's market subscriptions.
	//
	// Corresponds with GET /v1/markets (the `ListMarkets` operationId).
	ListMarkets(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListMarketBrowse Paginated discovery catalog
	//
	// Cursor-paginated, search-filterable catalog of every Atlas-tracked market the customer could expand into. Backed by the precomputed `market_summaries` table (>=5 active comps per city). Supports fuzzy `q` substring search (trigram-indexed), `country` (ISO 3166-1 alpha-2) filter, and `sort` (`listings_desc` | `name_asc`). Use the `nextCursor` from `pagination` to walk pages — the cursor is an opaque base64 token; do not parse it.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// `pagination.total` is the count of markets matching the current `q`/`country`/`min_listings` filter (across all pages) — same shape as every other list endpoint.
	//
	// Corresponds with GET /v1/markets/browse (the `ListMarketBrowse` operationId).
	ListMarketBrowse(ctx context.Context, params *ListMarketBrowseParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetMarket Deep-dive on a single market
	//
	// Detailed market view for one city — price distribution, bedroom mix, property types, upcoming events, Wheelhouse demand, monthly benchmarks, customer health rollup, top comps (proximity-sorted, paginated), customer's percentile position, capacity-mix gap, and a 6-month supply trend.
	//
	// Corresponds with GET /v1/markets/{city} (the `GetMarket` operationId).
	GetMarket(ctx context.Context, city string, params *GetMarketParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetMarketCalendar Calendar-level market view
	//
	// Date-by-date market view for a city — market avg / min / max nightly rate, occupancy %, Wheelhouse demand, events touching the date, and (when `listingId` is supplied) an overlay of the customer's own pricing + availability.
	//
	// Corresponds with GET /v1/markets/{city}/calendar (the `GetMarketCalendar` operationId).
	GetMarketCalendar(ctx context.Context, city string, params *GetMarketCalendarParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListMigrations List migrations
	//
	// Every property manager your workspace has moved through Repull Migrate, newest first, each with where it stands. Page with `pagination.nextCursor`.
	//
	// Corresponds with GET /v1/migrations (the `ListMigrations` operationId).
	ListMigrations(ctx context.Context, params *ListMigrationsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteMigration End a migration
	//
	// Disconnects the source (it stops syncing) and deactivates the migration's workspace. The imported data is kept and stays readable.
	//
	// Corresponds with DELETE /v1/migrations/{workspaceId} (the `DeleteMigration` operationId).
	DeleteMigration(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetMigration Get a migration
	//
	// Where one migration stands: its state, the source connections with their last import run, and how much data has landed.
	//
	// Corresponds with GET /v1/migrations/{workspaceId} (the `GetMigration` operationId).
	GetMigration(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetMigrationChannelMap Get channel links
	//
	// The Airbnb, Booking.com and VRBO listing each migrated property is linked to in the source PMS, read live. Use it to link each listing to the right property when the channels are reconnected in the destination — channel connections themselves cannot be moved between PMSs.
	//
	// Corresponds with GET /v1/migrations/{workspaceId}/channel-map (the `GetMigrationChannelMap` operationId).
	GetMigrationChannelMap(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CutoverMigration Cut over
	//
	// The property manager has switched: disconnect the source so it stops syncing. The imported data stays readable. Idempotent.
	//
	// Corresponds with POST /v1/migrations/{workspaceId}/cutover (the `CutoverMigration` operationId).
	CutoverMigration(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CheckMigrationCutoverWithBody Check reservations before switching
	//
	// Send every upcoming reservation the destination holds; get back what is missing, extra, or on different dates compared with the source. Read-only — run it as often as you like before cutover.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/migrations/{workspaceId}/cutover-check (the `CheckMigrationCutover` operationId).
	CheckMigrationCutoverWithBody(ctx context.Context, workspaceId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CheckMigrationCutover Check reservations before switching
	//
	// Send every upcoming reservation the destination holds; get back what is missing, extra, or on different dates compared with the source. Read-only — run it as often as you like before cutover.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/migrations/{workspaceId}/cutover-check (the `CheckMigrationCutover` operationId).
	CheckMigrationCutover(ctx context.Context, workspaceId int, body CheckMigrationCutoverJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RunMigrationImportWithBody Run the import again
	//
	// Queue another import from the source PMS — for example messages after the first pass, or only reservations changed since a date. Progress shows on `GET /v1/migrations/{workspaceId}`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/migrations/{workspaceId}/import (the `RunMigrationImport` operationId).
	RunMigrationImportWithBody(ctx context.Context, workspaceId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RunMigrationImport Run the import again
	//
	// Queue another import from the source PMS — for example messages after the first pass, or only reservations changed since a date. Progress shows on `GET /v1/migrations/{workspaceId}`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/migrations/{workspaceId}/import (the `RunMigrationImport` operationId).
	RunMigrationImport(ctx context.Context, workspaceId int, body RunMigrationImportJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetMigrationReport Get a migration report
	//
	// What came across and what needs a decision in the destination: properties without an address, upcoming reservations with no guest contact, channel reservations that must not be re-created, and anything the source PMS cannot carry. Includes the source's capability matrix.
	//
	// Corresponds with GET /v1/migrations/{workspaceId}/report (the `GetMigrationReport` operationId).
	GetMigrationReport(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListProperties List properties
	//
	// Cursor-paginated list of properties for the authenticated workspace. Walk pages with `?cursor=<pagination.nextCursor>`; stop when `pagination.hasMore` is `false`. Cursor is opaque base64 — do not parse it.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// Filters: `q` (substring on name/street/city), `status` (active|inactive|all), `lifecycle_status` (exact match on the listing's lifecycle state). Other unknown params (e.g. `?search=` or `?propertyId=`) are rejected with 422 — no silent unfiltered results.
	//
	// **Incremental sync (only changes since last poll):** pass `?updated_since=<ISO8601>` to receive only properties changed at or after that instant. Each property carries `updatedAt` — the last row of the final page is your next watermark. `updated_since` changes the page ordering to `updatedAt ASC, id ASC` (and the cursor with it); see the parameter description. `GET /v1/listings` does NOT yet accept `updated_since` — use this endpoint for property-side incremental sync.
	//
	// **Inactive properties:** an inactive property keeps syncing, but cannot be read or changed through the API until it is activated. They are only listed when `status` asks for them, and then with `id`, `name`, `status`, `lifecycleStatus`, `channels` and `updatedAt` only — enough to choose what to activate with `PATCH /v1/listings/{id}`.
	//
	// Corresponds with GET /v1/properties (the `ListProperties` operationId).
	ListProperties(ctx context.Context, params *ListPropertiesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetProperty Get property details
	//
	// Fetch a single property by Repull id. Property ids are workspace-scoped — an id from one workspace is not valid in another. 404 means the id does not exist OR belongs to a different workspace.
	//
	// **Optional expansions:** Pass `?include=amenities` to enrich the response with the property's amenities (sourced from the unified `listings_amenities` table). Returns `[]` when the property has no amenity rows. The default response stays lean; consumers must opt in.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/properties/{id} (the `GetProperty` operationId).
	GetProperty(ctx context.Context, id int, params *GetPropertyParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetQuote Price a stay
	//
	// Returns the full price breakdown for a stay — nightly total, length-of-stay discount, cleaning fee, pet and other fees, taxes, and the total.
	//
	// A quote is priced against a booking website, because the markup, custom fees and tax overrides that decide what a guest is actually charged live there. A workspace with no booking site receives `422 quote_unavailable` rather than a number computed from different rules than the ones applied at checkout.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/quotes (the `GetQuote` operationId).
	GetQuote(ctx context.Context, params *GetQuoteParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListReservations List reservations
	//
	// Cursor-paginated list of reservations across all connected PMS platforms. Filter by platform, status, listing, or check-in date range.
	//
	// **Pagination:** Walk pages with `?cursor=` — pass `pagination.nextCursor` from one response back as `?cursor=` on the next request. Stop when `pagination.hasMore` is `false`. `limit` defaults to 50, max 100; requesting more returns 422 (no silent truncation).
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. For deep pagination cursor remains O(1) per page; offset > 10000 returns 422 with a docs link.
	//
	// **Incremental sync (only changes since last poll):** pass `?updated_since=<ISO8601>` to receive only reservations amended, cancelled, or created at or after that instant — no full re-walk. Each row carries `updatedAt`; the last row of the final page is your next watermark. Note that `updated_since` changes the page ordering to `updatedAt ASC, id ASC` (and the cursor with it) so mid-walk amendments cannot be skipped — see the parameter description for the full contract.
	//
	// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
	//
	// Corresponds with GET /v1/reservations (the `ListReservations` operationId).
	ListReservations(ctx context.Context, params *ListReservationsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateReservationWithBody Create a reservation
	//
	// Creates a reservation and everything that hangs off one: the guest, the conversation thread, the dashboard item, the calendar block, and the `reservation.created` fan-out that issues the door code and starts the messaging automations.
	//
	// **Platform is restricted to `direct`, `website` and `owner`.** Reservations on Airbnb, Booking.com and Vrbo are owned by the channel and arrive through sync — creating one here would mint a local booking the channel has never heard of, which then fights the next sync. Create those on the channel.
	//
	// **Dates are validated** (`YYYY-MM-DD`, and `checkOut` must be after `checkIn`), and an unrecognised field is rejected by name rather than silently ignored.
	//
	// **This endpoint does not set the price.** There is no `totalPrice` field: the reservation pipeline derives the price breakdown from the property's own rates and overwrites anything supplied, so accepting a total would be taking a value and discarding it. A reservation created here is priced by that engine (`0` when the property has no rates for the range). `currency` IS honoured. Quote a stay with `GET /v1/quotes` before booking if you need the figure up front.
	//
	// **Availability is NOT checked.** This creates the reservation you asked for even if the dates overlap an existing booking. Call `GET /v1/availability/{propertyId}` first if that matters.
	//
	// Send `Idempotency-Key` — a network timeout here is exactly the case it exists for: without it, a retry books the guest twice.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/reservations (the `CreateReservation` operationId).
	CreateReservationWithBody(ctx context.Context, params *CreateReservationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateReservation Create a reservation
	//
	// Creates a reservation and everything that hangs off one: the guest, the conversation thread, the dashboard item, the calendar block, and the `reservation.created` fan-out that issues the door code and starts the messaging automations.
	//
	// **Platform is restricted to `direct`, `website` and `owner`.** Reservations on Airbnb, Booking.com and Vrbo are owned by the channel and arrive through sync — creating one here would mint a local booking the channel has never heard of, which then fights the next sync. Create those on the channel.
	//
	// **Dates are validated** (`YYYY-MM-DD`, and `checkOut` must be after `checkIn`), and an unrecognised field is rejected by name rather than silently ignored.
	//
	// **This endpoint does not set the price.** There is no `totalPrice` field: the reservation pipeline derives the price breakdown from the property's own rates and overwrites anything supplied, so accepting a total would be taking a value and discarding it. A reservation created here is priced by that engine (`0` when the property has no rates for the range). `currency` IS honoured. Quote a stay with `GET /v1/quotes` before booking if you need the figure up front.
	//
	// **Availability is NOT checked.** This creates the reservation you asked for even if the dates overlap an existing booking. Call `GET /v1/availability/{propertyId}` first if that matters.
	//
	// Send `Idempotency-Key` — a network timeout here is exactly the case it exists for: without it, a retry books the guest twice.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/reservations (the `CreateReservation` operationId).
	CreateReservation(ctx context.Context, params *CreateReservationParams, body CreateReservationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetReservation Get reservation details
	//
	// Returns the full record for a single reservation, scoped to the authenticated workspace. Response shape is identical to a single row in `GET /v1/reservations` so SDK consumers can use the same type for both. Returns **404** if the id does not exist OR belongs to a different workspace — the API never differentiates the two so caller can't enumerate other workspaces' ids.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Corresponds with GET /v1/reservations/{id} (the `GetReservation` operationId).
	GetReservation(ctx context.Context, id int, params *GetReservationParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateReservationWithBody Update a reservation
	//
	// Changes the dates, the occupancy, or the unit. Drives the same command path the dashboard does, so the side effects come with it: the change audit is appended, bound task due dates re-sync, the old calendar dates unblock and the new ones block, the conversation's cached listing is invalidated, and `reservation.updated` fires — which is what revokes and re-issues the door code.
	//
	// Supply at least one field; an empty body returns 422 rather than a 200 that changed nothing.
	//
	// **Moving and re-dating in one call is one operation.** Send `listingId` together with `checkIn`/`checkOut` and it is applied as a single move, so the access code is re-issued once rather than twice.
	//
	// ### Fields this endpoint deliberately does NOT accept
	//
	// Each is rejected by name with the reason, never accepted and ignored:
	//
	// | Field | Why |
	// |---|---|
	// | `guest` / `guestDetails` | Guest name, email and phone live on the guest record. The underlying command has no branch for them, so accepting them would return a success that changed nothing. |
	// | `pricing` / `totalPrice` / `currency` | Repricing writes the price breakdown, the pricing row and a pricing-history entry. It belongs to its own endpoint. |
	// | `status` | Not a field. Cancelling, confirming and checking out are separate operations with materially different side effects — cancellation issues a credit refund and revokes access codes. |
	// | `platform` | Immutable: it records where the booking actually originated. |
	// | `notes` | `internal_notes` is an append-only audit trail the system writes on every change. |
	//
	// **Availability is NOT checked.** A date change that overlaps another booking will be written. Call `GET /v1/availability/{propertyId}` first if that matters.
	//
	// Returns `403 listing_inactive` when the reservation is on an inactive listing, or when a `listingId` move targets one; nothing is changed.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /v1/reservations/{id} (the `UpdateReservation` operationId).
	UpdateReservationWithBody(ctx context.Context, id int, params *UpdateReservationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateReservation Update a reservation
	//
	// Changes the dates, the occupancy, or the unit. Drives the same command path the dashboard does, so the side effects come with it: the change audit is appended, bound task due dates re-sync, the old calendar dates unblock and the new ones block, the conversation's cached listing is invalidated, and `reservation.updated` fires — which is what revokes and re-issues the door code.
	//
	// Supply at least one field; an empty body returns 422 rather than a 200 that changed nothing.
	//
	// **Moving and re-dating in one call is one operation.** Send `listingId` together with `checkIn`/`checkOut` and it is applied as a single move, so the access code is re-issued once rather than twice.
	//
	// ### Fields this endpoint deliberately does NOT accept
	//
	// Each is rejected by name with the reason, never accepted and ignored:
	//
	// | Field | Why |
	// |---|---|
	// | `guest` / `guestDetails` | Guest name, email and phone live on the guest record. The underlying command has no branch for them, so accepting them would return a success that changed nothing. |
	// | `pricing` / `totalPrice` / `currency` | Repricing writes the price breakdown, the pricing row and a pricing-history entry. It belongs to its own endpoint. |
	// | `status` | Not a field. Cancelling, confirming and checking out are separate operations with materially different side effects — cancellation issues a credit refund and revokes access codes. |
	// | `platform` | Immutable: it records where the booking actually originated. |
	// | `notes` | `internal_notes` is an append-only audit trail the system writes on every change. |
	//
	// **Availability is NOT checked.** A date change that overlaps another booking will be written. Call `GET /v1/availability/{propertyId}` first if that matters.
	//
	// Returns `403 listing_inactive` when the reservation is on an inactive listing, or when a `listingId` move targets one; nothing is changed.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /v1/reservations/{id} (the `UpdateReservation` operationId).
	UpdateReservation(ctx context.Context, id int, params *UpdateReservationParams, body UpdateReservationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// AcceptReservationRequest Accept a booking request
	//
	// Accept a pending Airbnb booking request — a reservation with status `pending`, made on a listing without Instant Book. Find them with `GET /v1/reservations?status=pending`. Airbnb expires a request the host has not answered within 24 hours.
	//
	// Airbnb confirms asynchronously: the reservation’s status moves to confirmed, and a `reservation.updated` webhook fires, when Airbnb’s notification lands (usually within seconds). The response reports what Airbnb was asked to do.
	//
	// **Airbnb only**, and only for listings connected to Airbnb directly: other channels have no request step (`422 channel_not_supported`). A reservation that is not pending is refused before Airbnb is contacted (`409 reservation_not_pending`); one Airbnb says already moved on is `409 request_no_longer_pending`. Neither is worth retrying.
	//
	// Takes no body.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Corresponds with POST /v1/reservations/{id}/accept (the `AcceptReservationRequest` operationId).
	AcceptReservationRequest(ctx context.Context, id int, params *AcceptReservationRequestParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeclineReservationRequestWithBody Decline a booking request
	//
	// Decline a pending Airbnb booking request (a reservation with status `pending`; find them with `GET /v1/reservations?status=pending`).
	//
	// `reason` must be one of Airbnb’s own decline reasons. `message` is required: Airbnb sends it to the guest with the decline (at most 500 characters). It is not defaulted — a canned message would put words in your mouth.
	//
	// Airbnb confirms asynchronously; the reservation’s status moves, and `reservation.updated` fires, when its notification lands. Same channel and status rules as `POST /v1/reservations/{id}/accept`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/reservations/{id}/decline (the `DeclineReservationRequest` operationId).
	DeclineReservationRequestWithBody(ctx context.Context, id int, params *DeclineReservationRequestParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeclineReservationRequest Decline a booking request
	//
	// Decline a pending Airbnb booking request (a reservation with status `pending`; find them with `GET /v1/reservations?status=pending`).
	//
	// `reason` must be one of Airbnb’s own decline reasons. `message` is required: Airbnb sends it to the guest with the decline (at most 500 characters). It is not defaulted — a canned message would put words in your mouth.
	//
	// Airbnb confirms asynchronously; the reservation’s status moves, and `reservation.updated` fires, when its notification lands. Same channel and status rules as `POST /v1/reservations/{id}/accept`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/reservations/{id}/decline (the `DeclineReservationRequest` operationId).
	DeclineReservationRequest(ctx context.Context, id int, params *DeclineReservationRequestParams, body DeclineReservationRequestJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListReviews List reviews
	//
	// Cursor-paginated guest + host review stream for the workspace. Backed by main vanio's unified `reviews` table (populated by per-channel backfill crons), so this surface returns the complete cross-channel history — separate from `/v1/channels/airbnb/reviews` which hits Airbnb live.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// Filters: `platform` (`airbnb`|`booking`|`vrbo`), `listing_id` (internal Repull listing id), `rating_min` / `rating_max` (inclusive bounds, 0..5), `status` (`responded`|`unanswered`|`all`), `reviewer_role` (`guest` (default) | `host` | `all`).
	//
	// **Inactive listings:** reviews of inactive listings are left out of the page and of `pagination.total`. Filtering by an inactive `listing_id` returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
	//
	// Corresponds with GET /v1/reviews (the `ListReviews` operationId).
	ListReviews(ctx context.Context, params *ListReviewsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetReview Get review
	//
	// Returns one review (the bare `Review` object — NOT wrapped in `{ data: ... }`). Scoped to the authenticated workspace via the listings join — reviews that don't belong to the workspace return 404 (we don't differentiate to avoid leaking other customers' ids).
	//
	// A review of an inactive listing returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
	//
	// Corresponds with GET /v1/reviews/{id} (the `GetReview` operationId).
	GetReview(ctx context.Context, id int, params *GetReviewParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReplyToReviewWithBody Reply to a review on any channel
	//
	// Resolves the review, reads its channel and dispatches the reply. Channel-neutral: you do not need to know where the review came from.
	//
	// Replies are available on Airbnb today; a review from a channel without a reply API returns `422 unsupported_channel` naming the channels that do work.
	//
	// **Inactive listings:** a review of an inactive listing returns `403 listing_inactive` and no reply reaches the channel. Activate the listing first.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/reviews/{id}/reply (the `ReplyToReview` operationId).
	ReplyToReviewWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReplyToReview Reply to a review on any channel
	//
	// Resolves the review, reads its channel and dispatches the reply. Channel-neutral: you do not need to know where the review came from.
	//
	// Replies are available on Airbnb today; a review from a channel without a reply API returns `422 unsupported_channel` naming the channels that do work.
	//
	// **Inactive listings:** a review of an inactive listing returns `403 listing_inactive` and no reply reaches the channel. Activate the listing first.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/reviews/{id}/reply (the `ReplyToReview` operationId).
	ReplyToReview(ctx context.Context, id int, body ReplyToReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListCustomSchemas List custom schemas
	//
	// Returns every custom schema owned by the workspace, including inactive ones.
	//
	// Corresponds with GET /v1/schema/custom (the `ListCustomSchemas` operationId).
	ListCustomSchemas(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateCustomSchemaWithBody Create a custom schema
	//
	// Create a workspace-scoped field-mapping schema. The schema reshapes the `native` response payload into your app's preferred field names. After creation, set `X-Schema: <name>` on any read endpoint to apply it.
	//
	// **Reserved names:** `calry`, `calry-v1`, `native` are built-in schemas and cannot be used as a custom name.
	//
	// **Mapping safety:** Each mapping value is parsed by an internal expression engine — `eval`, `Function`, `process`, and other unsafe keywords are rejected up front. Field names are capped at 100 chars and expressions at 500 chars.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/schema/custom (the `CreateCustomSchema` operationId).
	CreateCustomSchemaWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateCustomSchema Create a custom schema
	//
	// Create a workspace-scoped field-mapping schema. The schema reshapes the `native` response payload into your app's preferred field names. After creation, set `X-Schema: <name>` on any read endpoint to apply it.
	//
	// **Reserved names:** `calry`, `calry-v1`, `native` are built-in schemas and cannot be used as a custom name.
	//
	// **Mapping safety:** Each mapping value is parsed by an internal expression engine — `eval`, `Function`, `process`, and other unsafe keywords are rejected up front. Field names are capped at 100 chars and expressions at 500 chars.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/schema/custom (the `CreateCustomSchema` operationId).
	CreateCustomSchema(ctx context.Context, body CreateCustomSchemaJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteCustomSchema Delete a custom schema
	//
	// Hard-delete a custom schema. Subsequent requests carrying its name in `X-Schema` fall back to `native`. There is no undelete.
	//
	// Corresponds with DELETE /v1/schema/custom/{id} (the `DeleteCustomSchema` operationId).
	DeleteCustomSchema(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetCustomSchema Get a custom schema
	//
	// Fetch a single custom schema by id. Scoped to the authenticated workspace — schemas that belong to other workspaces return 404.
	//
	// Corresponds with GET /v1/schema/custom/{id} (the `GetCustomSchema` operationId).
	GetCustomSchema(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateCustomSchemaWithBody Update a custom schema
	//
	// Patch the description, mappings, or active flag of a custom schema. The schema `name` is immutable — create a new schema and migrate consumers if you need to rename. Mapping updates are revalidated for safety.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /v1/schema/custom/{id} (the `UpdateCustomSchema` operationId).
	UpdateCustomSchemaWithBody(ctx context.Context, id openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateCustomSchema Update a custom schema
	//
	// Patch the description, mappings, or active flag of a custom schema. The schema `name` is immutable — create a new schema and migrate consumers if you need to rename. Mapping updates are revalidated for safety.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /v1/schema/custom/{id} (the `UpdateCustomSchema` operationId).
	UpdateCustomSchema(ctx context.Context, id openapi_types.UUID, body UpdateCustomSchemaJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetUsageLogs List API request logs
	//
	// Cursor-paginated raw API request log for the authenticated workspace, newest first. Filter by time `range`, `operation` id(s), status class, or free-text `q`. Walk pages with `cursor` from `pagination.nextCursor` until `pagination.hasMore` is `false`; `offset` is accepted as a shallow alias (deep walks must use `cursor`).
	//
	// Corresponds with GET /v1/usage/logs (the `GetUsageLogs` operationId).
	GetUsageLogs(ctx context.Context, params *GetUsageLogsParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetUsageSummary Get usage summary
	//
	// Aggregated usage over the requested `range` — tier + plan limits, quota used/remaining, next reset, a per-operation breakdown (request/error counts, error rate, avg latency), a daily timeline, status-class distribution, and range totals. Two request quotas are reported and they reset at different times: `dailyRequests` is the daily circuit breaker that stops runaway client loops (resets at `dailyResetsAt`, the next UTC midnight) and `monthlyRequests` is the billing quota (resets at `resetsAt`). `null` limits mean unlimited on that dimension.
	//
	// Corresponds with GET /v1/usage/summary (the `GetUsageSummary` operationId).
	GetUsageSummary(ctx context.Context, params *GetUsageSummaryParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetUsageTier Get tier and quota
	//
	// Lightweight current-tier snapshot for status badges and quota meters — plan limits (daily requests, monthly requests, daily AI requests, dynamic-pricing listings), the amount used, the amount remaining, and the next reset. Two request quotas are reported and they reset at different times: `dailyRequests` is the daily circuit breaker that stops runaway client loops (resets at `dailyResetsAt`, the next UTC midnight) and `monthlyRequests` is the billing quota (resets at `resetsAt`). Exceeding the daily cap returns 429 `daily_limit_exceeded`; exceeding the monthly one returns 429 `rate_limit_exceeded`. `null` limits mean unlimited on that dimension.
	//
	// Corresponds with GET /v1/usage/tier (the `GetUsageTier` operationId).
	GetUsageTier(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListWebhooks List webhook subscriptions
	//
	// List every webhook subscription registered for this workspace. Each row includes the destination URL, subscribed event types, and the most recent delivery summary.
	//
	// Corresponds with GET /v1/webhooks (the `ListWebhooks` operationId).
	ListWebhooks(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateWebhookWithBody Create webhook subscription
	//
	// Register a new endpoint. Returns the plaintext signing secret ONCE — capture it from the response and store it securely. After this call the secret is masked everywhere; mint a new one with `POST /v1/webhooks/{id}/rotate-secret` if you lose it. See `GET /v1/webhooks/event-types` for the full list of subscribable events. Events about an inactive listing (reservations, messages, alterations, reviews, payments, calendar and listing events) are not delivered. The data keeps syncing while the listing is inactive, but its events are never sent — including after you reactivate it; webhooks resume for events that happen from reactivation on. Account-level events are always delivered.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/webhooks (the `CreateWebhook` operationId).
	CreateWebhookWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// CreateWebhook Create webhook subscription
	//
	// Register a new endpoint. Returns the plaintext signing secret ONCE — capture it from the response and store it securely. After this call the secret is masked everywhere; mint a new one with `POST /v1/webhooks/{id}/rotate-secret` if you lose it. See `GET /v1/webhooks/event-types` for the full list of subscribable events. Events about an inactive listing (reservations, messages, alterations, reviews, payments, calendar and listing events) are not delivered. The data keeps syncing while the listing is inactive, but its events are never sent — including after you reactivate it; webhooks resume for events that happen from reactivation on. Account-level events are always delivered.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/webhooks (the `CreateWebhook` operationId).
	CreateWebhook(ctx context.Context, body CreateWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListWebhookEventTypes List webhook event types
	//
	// The canonical catalog of every event the API can deliver, grouped by domain, with realistic sample payloads.
	//
	// Corresponds with GET /v1/webhooks/event-types (the `ListWebhookEventTypes` operationId).
	ListWebhookEventTypes(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error)

	// TestWebhookWithBody [Legacy] Send test webhook to a URL
	//
	// Deprecated: prefer creating a subscription then calling `POST /v1/webhooks/{id}/ping`. Kept for back-compat.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/webhooks/test (the `TestWebhook` operationId).
	TestWebhookWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// TestWebhook [Legacy] Send test webhook to a URL
	//
	// Deprecated: prefer creating a subscription then calling `POST /v1/webhooks/{id}/ping`. Kept for back-compat.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/webhooks/test (the `TestWebhook` operationId).
	TestWebhook(ctx context.Context, body TestWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// DeleteWebhook Delete webhook subscription
	//
	// Delete a webhook subscription. In-flight deliveries already on the queue are still attempted; new events stop firing immediately.
	//
	// Corresponds with DELETE /v1/webhooks/{id} (the `DeleteWebhook` operationId).
	DeleteWebhook(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetWebhook Get webhook subscription
	//
	// Fetch a single webhook subscription by id. Use the `deliveries` sub-resource to list recent attempts, and `ping` to send a test event.
	//
	// Corresponds with GET /v1/webhooks/{id} (the `GetWebhook` operationId).
	GetWebhook(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateWebhookWithBody Update webhook subscription
	//
	// Update url, description, events, or status (active|paused). Re-enabling clears `consecutiveFailures` and `disabledAt`.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with PATCH /v1/webhooks/{id} (the `UpdateWebhook` operationId).
	UpdateWebhookWithBody(ctx context.Context, id openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// UpdateWebhook Update webhook subscription
	//
	// Update url, description, events, or status (active|paused). Re-enabling clears `consecutiveFailures` and `disabledAt`.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with PATCH /v1/webhooks/{id} (the `UpdateWebhook` operationId).
	UpdateWebhook(ctx context.Context, id openapi_types.UUID, body UpdateWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ListWebhookDeliveries List webhook deliveries
	//
	// Cursor-paginated history of every delivery attempt for this subscription. Walk pages with `?cursor=<pagination.nextCursor>`; stop when `pagination.hasMore` is `false`. The cursor is opaque base64 — do not parse it. `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — mutually exclusive with `cursor`.
	//
	// Corresponds with GET /v1/webhooks/{id}/deliveries (the `ListWebhookDeliveries` operationId).
	ListWebhookDeliveries(ctx context.Context, id openapi_types.UUID, params *ListWebhookDeliveriesParams, reqEditors ...RequestEditorFn) (*http.Response, error)

	// GetWebhookDelivery Get webhook delivery
	//
	// Full request + response capture for one delivery attempt.
	//
	// Corresponds with GET /v1/webhooks/{id}/deliveries/{delivery_id} (the `GetWebhookDelivery` operationId).
	GetWebhookDelivery(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReplayWebhookDeliveryWithBody Replay webhook delivery
	//
	// Re-sends the original payload (same eventId, fresh deliveryId, attempt + 1).
	//
	// A delivery may be replayed at most **3 times per rolling 60 minutes**; the 4th inside that window answers `409 replay_limit_reached` and names the time the next one is allowed. The limit is charged to the original delivery, so replaying the delivery a replay produced draws on the same budget. It is not a lifetime cap — a delivery that has not been replayed for an hour starts fresh.
	//
	// A delivery your endpoint already accepted is not re-sent (it would be a duplicate) and answers `409 delivery_already_succeeded`; send `{"force": true}` to replay it anyway, which still counts against the limit.
	//
	// A delivery about a listing that is inactive now is not re-sent and answers `403 listing_inactive`; activate the listing first.
	//
	// Takes any type of body and a specified content type.
	//
	// Corresponds with POST /v1/webhooks/{id}/deliveries/{delivery_id}/replay (the `ReplayWebhookDelivery` operationId).
	ReplayWebhookDeliveryWithBody(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	// ReplayWebhookDelivery Replay webhook delivery
	//
	// Re-sends the original payload (same eventId, fresh deliveryId, attempt + 1).
	//
	// A delivery may be replayed at most **3 times per rolling 60 minutes**; the 4th inside that window answers `409 replay_limit_reached` and names the time the next one is allowed. The limit is charged to the original delivery, so replaying the delivery a replay produced draws on the same budget. It is not a lifetime cap — a delivery that has not been replayed for an hour starts fresh.
	//
	// A delivery your endpoint already accepted is not re-sent (it would be a duplicate) and answers `409 delivery_already_succeeded`; send `{"force": true}` to replay it anyway, which still counts against the limit.
	//
	// A delivery about a listing that is inactive now is not re-sent and answers `403 listing_inactive`; activate the listing first.
	//
	// Takes a body of the `application/json` content type.
	//
	// Corresponds with POST /v1/webhooks/{id}/deliveries/{delivery_id}/replay (the `ReplayWebhookDelivery` operationId).
	ReplayWebhookDelivery(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, body ReplayWebhookDeliveryJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)

	// PingWebhook Send ping event
	//
	// Fires a synthetic `repull.ping` at the subscription URL and returns the full delivery cycle inline. Used by dashboards and health-check probes.
	//
	// Corresponds with POST /v1/webhooks/{id}/ping (the `PingWebhook` operationId).
	PingWebhook(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// RotateWebhookSecret Rotate signing secret
	//
	// Mints a fresh signing secret and returns the plaintext ONCE. After this response the secret is masked everywhere — capture and store it now.
	//
	// Corresponds with POST /v1/webhooks/{id}/rotate-secret (the `RotateWebhookSecret` operationId).
	RotateWebhookSecret(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error)

	// TestFireWebhook Send test event of a specific type
	//
	// Delivers a realistic fixture payload of the requested event type to the subscription URL.
	//
	// Corresponds with POST /v1/webhooks/{id}/test/{event_type} (the `TestFireWebhook` operationId).
	TestFireWebhook(ctx context.Context, id openapi_types.UUID, eventType WebhookEventType, reqEditors ...RequestEditorFn) (*http.Response, error)
}

// BatchUpdateAvailabilityWithBody Update availability across many properties
//
// Applies ONE settings object across up to 500 properties and pushes the result to every connected channel.
//
// Ownership is checked before anything is written: a batch containing a property from another workspace is refused as a whole and names the offending ids, rather than being partially applied.
//
// Per-property *different* values are separate calls — presenting them as one request would be a false claim about atomicity.
//
// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /v1/availability/batch (the `BatchUpdateAvailability` operationId).
func (c *Client) BatchUpdateAvailabilityWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBatchUpdateAvailabilityRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BatchUpdateAvailability Update availability across many properties
//
// Applies ONE settings object across up to 500 properties and pushes the result to every connected channel.
//
// Ownership is checked before anything is written: a batch containing a property from another workspace is refused as a whole and names the offending ids, rather than being partially applied.
//
// Per-property *different* values are separate calls — presenting them as one request would be a false claim about atomicity.
//
// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /v1/availability/batch (the `BatchUpdateAvailability` operationId).
func (c *Client) BatchUpdateAvailability(ctx context.Context, body BatchUpdateAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBatchUpdateAvailabilityRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAvailability Get property availability
//
// Channel-agnostic day-by-day availability calendar for a property over a date window. Returns a thin per-date shape — `{ date, available, price, minNights }` — projected from the property calendar.
//
// The `from` and `to` query params are **required** (ISO `YYYY-MM-DD`, inclusive) — omitting or malforming either returns 422. The window is capped at 366 days; longer ranges are truncated to the first 366 days.
//
// **`days` contains only the dates we actually hold calendar data for.** Requested dates with no calendar row are listed in `coverage.missingDates` — their availability is unknown. Never treat a missing date as bookable: this endpoint deliberately does not synthesise availability, because a fabricated open date can be double-booked. A property with no calendar still returns a real 200 (`days: []`, every date in `coverage.missingDates`), never a 404 — 404 means the property id does not exist or belongs to a different workspace.
//
// This endpoint is read-only, and the projected per-date shape carries **availability, price, and min-nights only** — it does NOT expose max-stay, closed-to-arrival (CTA), closed-to-departure (CTD), or the dedicated stop-sell flag. To read or write that full restriction set on Booking.com use the channel routes: `GET`/`PUT /v1/channels/booking/availability` (with the room + rate ids from `GET /v1/channels/booking/properties/{id}/rooms`). To **write** calendar values use `PUT /v1/availability/{propertyId}` (or `PATCH /v1/availability/batch`), which updates the property calendar and pushes to its connected channels; channel-only settings stay on the channel routes.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/availability/{propertyId} (the `GetAvailability` operationId).
func (c *Client) GetAvailability(ctx context.Context, propertyId int, params *GetAvailabilityParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAvailabilityRequest(c.Server, propertyId, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAvailabilityWithBody Set prices, block or unblock dates
//
// Writes the calendar for one property AND pushes to every connected channel in the same step. A write that only changed our copy would leave the OTA calendars stale and eventually double-book a guest.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/availability/{propertyId} (the `UpdateAvailability` operationId).
func (c *Client) UpdateAvailabilityWithBody(ctx context.Context, propertyId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAvailabilityRequestWithBody(c.Server, propertyId, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAvailability Set prices, block or unblock dates
//
// Writes the calendar for one property AND pushes to every connected channel in the same step. A write that only changed our copy would leave the OTA calendars stale and eventually double-book a guest.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/availability/{propertyId} (the `UpdateAvailability` operationId).
func (c *Client) UpdateAvailability(ctx context.Context, propertyId int, body UpdateAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAvailabilityRequest(c.Server, propertyId, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateBillingCheckoutWithBody Create checkout session
//
// Redirect user to Stripe checkout.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/billing (the `CreateBillingCheckout` operationId).
func (c *Client) CreateBillingCheckoutWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateBillingCheckoutRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateBillingCheckout Create checkout session
//
// Redirect user to Stripe checkout.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/billing (the `CreateBillingCheckout` operationId).
func (c *Client) CreateBillingCheckout(ctx context.Context, body CreateBillingCheckoutJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateBillingCheckoutRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbAlterations List Airbnb alterations
//
// List reservation alteration requests for Airbnb reservations in this workspace. **Pure DB read** from the local `reservation_alterations` mirror — never calls Airbnb upstream — scoped to your workspace via the reservations join.
//
// Default returns only pending alterations; pass `?type=all` for the full history. Filter to a single reservation with `?reservation_code=<confirmation code>`. Every response carries the `dataFreshness` envelope.
//
// Each row carries the proposed change in its `new*` fields. A **listing transfer** shows up as `newListingId` (Repull listing id) and `newAirbnbListingId` (Airbnb's own id); both are `null` when the alteration does not move the reservation.
//
// Alterations of reservations on inactive listings are left out. Filtering by a reservation on an inactive listing (`reservation_code`) returns `403 listing_inactive`.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Corresponds with GET /v1/channels/airbnb/alterations (the `ListAirbnbAlterations` operationId).
func (c *Client) ListAirbnbAlterations(ctx context.Context, params *ListAirbnbAlterationsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbAlterationsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateAirbnbAlterationWithBody Create Airbnb alteration
//
// Propose a change to an existing Airbnb reservation: new dates, a new guest count, a new total price, or a move to a different listing. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host for the workspace, else `404 no_connection`.
//
// The body is validated before anything reaches Airbnb. `confirmation_code` is required and **at least one** of `check_in`, `check_out`, `number_of_guests`, `total_price` or `listing_id` must be sent with it — an alteration that changes nothing is `422 invalid_params`, not a request Airbnb is asked to act on. Unknown fields are refused rather than ignored, so a misspelling can never look like a successful write.
//
// **Listing transfer.** `listing_id` moves the reservation to another listing in your workspace. Send the **Repull** listing id (the `id` from `GET /v1/properties`); Repull checks you own it, that it is active and connected to Airbnb, translates it to the Airbnb listing id and sends it upstream. Callers who hold the Airbnb-side id instead may send `airbnb_listing_id`. **Airbnb decides** whether to honour a listing change on an alteration — Repull sends it and reports Airbnb’s answer; a refusal comes back as `422 airbnb_rejected` carrying Airbnb’s own message.
//
// Returns `403 listing_inactive` when the reservation’s listing, or the listing it is being moved to, is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/alterations (the `CreateAirbnbAlteration` operationId).
func (c *Client) CreateAirbnbAlterationWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateAirbnbAlterationRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateAirbnbAlteration Create Airbnb alteration
//
// Propose a change to an existing Airbnb reservation: new dates, a new guest count, a new total price, or a move to a different listing. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host for the workspace, else `404 no_connection`.
//
// The body is validated before anything reaches Airbnb. `confirmation_code` is required and **at least one** of `check_in`, `check_out`, `number_of_guests`, `total_price` or `listing_id` must be sent with it — an alteration that changes nothing is `422 invalid_params`, not a request Airbnb is asked to act on. Unknown fields are refused rather than ignored, so a misspelling can never look like a successful write.
//
// **Listing transfer.** `listing_id` moves the reservation to another listing in your workspace. Send the **Repull** listing id (the `id` from `GET /v1/properties`); Repull checks you own it, that it is active and connected to Airbnb, translates it to the Airbnb listing id and sends it upstream. Callers who hold the Airbnb-side id instead may send `airbnb_listing_id`. **Airbnb decides** whether to honour a listing change on an alteration — Repull sends it and reports Airbnb’s answer; a refusal comes back as `422 airbnb_rejected` carrying Airbnb’s own message.
//
// Returns `403 listing_inactive` when the reservation’s listing, or the listing it is being moved to, is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/alterations (the `CreateAirbnbAlteration` operationId).
func (c *Client) CreateAirbnbAlteration(ctx context.Context, body CreateAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateAirbnbAlterationRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbAlteration Get Airbnb alteration
//
// Fetch a single Airbnb reservation alteration by its Airbnb alteration id. **Pure DB read**, workspace-scoped via the reservations join. Returns `404 not_found` when no alteration matches the id in your workspace.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/alterations/{id} (the `GetAirbnbAlteration` operationId).
func (c *Client) GetAirbnbAlteration(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbAlterationRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AcceptAirbnbAlterationWithBody Accept Airbnb alteration
//
// Accept a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to approve the proposed date / guest-count / price change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/accept (the `AcceptAirbnbAlteration` operationId).
func (c *Client) AcceptAirbnbAlterationWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAcceptAirbnbAlterationRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AcceptAirbnbAlteration Accept Airbnb alteration
//
// Accept a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to approve the proposed date / guest-count / price change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/accept (the `AcceptAirbnbAlteration` operationId).
func (c *Client) AcceptAirbnbAlteration(ctx context.Context, id string, body AcceptAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAcceptAirbnbAlterationRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CancelAirbnbAlterationWithBody Cancel Airbnb alteration
//
// Withdraw an alteration you proposed, before the other side has answered it. **Write-side** — calls Airbnb upstream (`respondToAlteration` with status `canceled`). Use this when you sent the wrong dates, guest count, price or listing: the alteration stops being pending instead of sitting there until the guest acts on it.
//
// This is the third of Airbnb's three answers to a pending alteration, alongside `accept` and `decline`, and behaves identically to them: requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Airbnb decides whether an alteration can still be withdrawn — one that has already been accepted or declined is refused upstream, and Airbnb's own reason comes back in `message`.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/cancel (the `CancelAirbnbAlteration` operationId).
func (c *Client) CancelAirbnbAlterationWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCancelAirbnbAlterationRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CancelAirbnbAlteration Cancel Airbnb alteration
//
// Withdraw an alteration you proposed, before the other side has answered it. **Write-side** — calls Airbnb upstream (`respondToAlteration` with status `canceled`). Use this when you sent the wrong dates, guest count, price or listing: the alteration stops being pending instead of sitting there until the guest acts on it.
//
// This is the third of Airbnb's three answers to a pending alteration, alongside `accept` and `decline`, and behaves identically to them: requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Airbnb decides whether an alteration can still be withdrawn — one that has already been accepted or declined is refused upstream, and Airbnb's own reason comes back in `message`.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/cancel (the `CancelAirbnbAlteration` operationId).
func (c *Client) CancelAirbnbAlteration(ctx context.Context, id string, body CancelAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCancelAirbnbAlterationRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeclineAirbnbAlterationWithBody Decline Airbnb alteration
//
// Decline a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to reject the proposed change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/decline (the `DeclineAirbnbAlteration` operationId).
func (c *Client) DeclineAirbnbAlterationWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeclineAirbnbAlterationRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeclineAirbnbAlteration Decline Airbnb alteration
//
// Decline a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to reject the proposed change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/decline (the `DeclineAirbnbAlteration` operationId).
func (c *Client) DeclineAirbnbAlteration(ctx context.Context, id string, body DeclineAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeclineAirbnbAlterationRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbConnection Get Airbnb connection state
//
// Returns the workspace's Airbnb host connection state in one envelope. Use this instead of inferring connection health from per-listing 401s on `GET /v1/channels/airbnb/listings` — that's noisy (every per-listing call has to fail before you know) and ambiguous (a single 5xx looks identical to a deauth).
//
// Pure DB read — does NOT touch Airbnb's API, so it's cheap to poll from a status-page surface.
//
// The response includes one row per Airbnb host the workspace has linked. Each row carries `isConnected`, `lastSyncedAt`, `deactivatedAt`, and `lastDisconnectReason` (most recent non-backfill row in `airbnb_host_events`).
//
// A self-serve `fixUrl` is included whenever `status` is anything other than `connected` — points at the dashboard where the host re-authorizes (or initiates the first OAuth flow for `never_connected` workspaces).
//
// Corresponds with GET /v1/channels/airbnb/connection (the `GetAirbnbConnection` operationId).
func (c *Client) GetAirbnbConnection(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbConnectionRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbListings List Airbnb listings
//
// List every Airbnb listing this workspace has access to via the connected Airbnb account. **Pure DB read — never calls Airbnb upstream.** The connect flow is what populates the local cache; the API serves what's already there. Customers with a disconnected host still see their last-synced data, with the top-level `dataFreshness` envelope flagging the staleness and pointing at the reconnect URL.
//
// Pass `?include=amenities` to enrich each connection with its locally-cached amenity set. Returns `null` per connection when the cache is empty.
//
// Pass `?include=thumbnail` to add `thumbnailUrl` to each listing — one extra column on the query that already runs, so a selection screen renders from a single request instead of one call per listing. `null` when the listing has no thumbnail stored. Combine comma-separated, e.g. `?include=amenities,thumbnail`.
//
// **Can this listing be written to?** Every connection carries `syncCategory` — Airbnb's own per-listing API sync decision (`sync_all`, `sync_rates_and_availability`, or `none`) — and `writable`, which is `false` exactly when that category is `none`. Airbnb authorises sync one listing at a time, so a connected account can still hold listings Airbnb refuses every write to; a write to one of those returns `403 listing_not_api_connected` before anything is sent, and reconnecting the account does not change it (the host must switch the listing on in Airbnb). Check `writable` here before a portfolio-wide push instead of discovering it one 403 at a time.
//
// Inactive listings are left out; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Corresponds with GET /v1/channels/airbnb/listings (the `ListAirbnbListings` operationId).
func (c *Client) ListAirbnbListings(ctx context.Context, params *ListAirbnbListingsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbListingsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// MapAirbnbListingWithBody Map an Airbnb listing to a Repull listing
//
// Link an existing Airbnb listing to a canonical Repull listing/property. **API-key-scoped** (unlike the Booking room mapping, which is Connect-session-scoped).
//
// Discover the `airbnbId` (+ `hostId`) via `GET /v1/channels/airbnb/listings`, then re-point it at the `listingId` of your choice — the dedup / consolidation case where the Airbnb sync auto-created its own listing but you want the inventory under an existing property.
//
// Repoints both the Airbnb record and its platform link to the target listing in one transaction. Idempotent — re-mapping to the same listing is a 200 no-op (`alreadyMapped: true`). Scope is enforced against your workspace on both the target listing and the existing Airbnb record; a listing that already links a different Airbnb listing returns 409.
//
// Returns `403 listing_inactive` when the target listing, or the listing the Airbnb listing is mapped to now, is inactive; nothing is changed.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/listings/map (the `MapAirbnbListing` operationId).
func (c *Client) MapAirbnbListingWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewMapAirbnbListingRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// MapAirbnbListing Map an Airbnb listing to a Repull listing
//
// Link an existing Airbnb listing to a canonical Repull listing/property. **API-key-scoped** (unlike the Booking room mapping, which is Connect-session-scoped).
//
// Discover the `airbnbId` (+ `hostId`) via `GET /v1/channels/airbnb/listings`, then re-point it at the `listingId` of your choice — the dedup / consolidation case where the Airbnb sync auto-created its own listing but you want the inventory under an existing property.
//
// Repoints both the Airbnb record and its platform link to the target listing in one transaction. Idempotent — re-mapping to the same listing is a 200 no-op (`alreadyMapped: true`). Scope is enforced against your workspace on both the target listing and the existing Airbnb record; a listing that already links a different Airbnb listing returns 409.
//
// Returns `403 listing_inactive` when the target listing, or the listing the Airbnb listing is mapped to now, is inactive; nothing is changed.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/listings/map (the `MapAirbnbListing` operationId).
func (c *Client) MapAirbnbListing(ctx context.Context, body MapAirbnbListingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewMapAirbnbListingRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbListing Get Airbnb listing
//
// Fetch all Airbnb connection rows for a single Vanio listing id. A property may be linked from multiple Airbnb hosts — every match is returned. Pass `?include=amenities` to enrich each row with its current Airbnb amenities.
//
// Each row carries `syncCategory` — Airbnb's own per-listing API sync decision (`sync_all`, `sync_rates_and_availability`, or `none`) — and `writable`, which is `false` exactly when that category is `none`, meaning Airbnb refuses every write to the listing and Repull returns `403 listing_not_api_connected` without sending anything. `GET /v1/channels/airbnb/listings` reports both fields for the whole portfolio in one call.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id} (the `GetAirbnbListing` operationId).
func (c *Client) GetAirbnbListing(ctx context.Context, id string, params *GetAirbnbListingParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbListingRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AirbnbListingActionWithBody Listing action (delete/push/publish/unlist/relist)
//
// Apply a state action to a listing by id. The path `id` is the canonical Repull listing id.
//
// **`delete` here never touches Airbnb. Read this before you call it.**
//
// | | `action: "delete"` (this endpoint) | `action: "unlist"` (this endpoint) |
// |---|---|---|
// | What it changes | The Repull record | The live Airbnb listing |
// | Calls Airbnb | **No. Never.** | Yes |
// | The guest-facing listing | Stays live and keeps taking bookings | **Goes down** and stops taking bookings |
// | Billing and plan limits | No longer billed, no longer counts toward the cap | Unchanged |
// | API access to the listing | `403 listing_inactive` until reactivated | Unchanged — you can still read and write it |
// | Reverse it with | `PATCH /v1/listings/{id}` `{ "active": true }` | `action: "relist"` |
// | Data kept | Yes, and it keeps syncing | Yes |
//
// Neither one deletes anything on Airbnb. **There is no endpoint on this API that deletes an Airbnb listing** — the word `delete` on this route means "deactivate the Repull record" and nothing else. (Main vanio's internal listing-sync layer has a same-named action that DOES hard-delete on Airbnb; it is not exposed here, by any endpoint, deliberately. If you have read that code, note that the two names do not mean the same thing.)
//
// `delete` is idempotent. To take a listing off the market on every channel at once — Airbnb and Booking.com together — use `POST /v1/listings/{id}/offline`.
//
// `unlist` calls Airbnb and **takes the live listing down**: it is deactivated with a valid deactivation reason and then READ BACK, so "Airbnb accepted the call but the listing is still live" is reported as a failure rather than a success. Requires `airbnbConnectionId` — a listing can be connected to more than one Airbnb listing, and taking down the wrong one is not undoable through this API. `relist` puts it back up (re-enables sync and makes the listing available again); it does not push content.
//
// `relist` goes through the channel-publish billing gate and `unlist` does not, so on a workspace whose subscription has lapsed a listing can be taken down and not put back until billing is sorted out. That refusal comes back as `402` with the action that fixes it — never as an Airbnb error, because retrying and reconnecting Airbnb do nothing for it.
//
// `push` / `publish` push the listing's content to Airbnb via the same host-side sync orchestrator as `POST /v1/listings/{id}/publish/airbnb` — pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create + publish a new one under that host. `force` re-pushes every field, ignoring dirty-field tracking. The result is per-section: see `AirbnbPublishResult`.
//
// Any other action (e.g. `pull`) returns a structured 422 naming the supported actions.
//
// Returns `403 listing_inactive` for `push`/`publish`/`unlist`/`relist` when the listing is inactive. `delete` (deactivation) is always accepted.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/listings/{id} (the `AirbnbListingAction` operationId).
func (c *Client) AirbnbListingActionWithBody(ctx context.Context, id string, params *AirbnbListingActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAirbnbListingActionRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AirbnbListingAction Listing action (delete/push/publish/unlist/relist)
//
// Apply a state action to a listing by id. The path `id` is the canonical Repull listing id.
//
// **`delete` here never touches Airbnb. Read this before you call it.**
//
// | | `action: "delete"` (this endpoint) | `action: "unlist"` (this endpoint) |
// |---|---|---|
// | What it changes | The Repull record | The live Airbnb listing |
// | Calls Airbnb | **No. Never.** | Yes |
// | The guest-facing listing | Stays live and keeps taking bookings | **Goes down** and stops taking bookings |
// | Billing and plan limits | No longer billed, no longer counts toward the cap | Unchanged |
// | API access to the listing | `403 listing_inactive` until reactivated | Unchanged — you can still read and write it |
// | Reverse it with | `PATCH /v1/listings/{id}` `{ "active": true }` | `action: "relist"` |
// | Data kept | Yes, and it keeps syncing | Yes |
//
// Neither one deletes anything on Airbnb. **There is no endpoint on this API that deletes an Airbnb listing** — the word `delete` on this route means "deactivate the Repull record" and nothing else. (Main vanio's internal listing-sync layer has a same-named action that DOES hard-delete on Airbnb; it is not exposed here, by any endpoint, deliberately. If you have read that code, note that the two names do not mean the same thing.)
//
// `delete` is idempotent. To take a listing off the market on every channel at once — Airbnb and Booking.com together — use `POST /v1/listings/{id}/offline`.
//
// `unlist` calls Airbnb and **takes the live listing down**: it is deactivated with a valid deactivation reason and then READ BACK, so "Airbnb accepted the call but the listing is still live" is reported as a failure rather than a success. Requires `airbnbConnectionId` — a listing can be connected to more than one Airbnb listing, and taking down the wrong one is not undoable through this API. `relist` puts it back up (re-enables sync and makes the listing available again); it does not push content.
//
// `relist` goes through the channel-publish billing gate and `unlist` does not, so on a workspace whose subscription has lapsed a listing can be taken down and not put back until billing is sorted out. That refusal comes back as `402` with the action that fixes it — never as an Airbnb error, because retrying and reconnecting Airbnb do nothing for it.
//
// `push` / `publish` push the listing's content to Airbnb via the same host-side sync orchestrator as `POST /v1/listings/{id}/publish/airbnb` — pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create + publish a new one under that host. `force` re-pushes every field, ignoring dirty-field tracking. The result is per-section: see `AirbnbPublishResult`.
//
// Any other action (e.g. `pull`) returns a structured 422 naming the supported actions.
//
// Returns `403 listing_inactive` for `push`/`publish`/`unlist`/`relist` when the listing is inactive. `delete` (deactivation) is always accepted.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/listings/{id} (the `AirbnbListingAction` operationId).
func (c *Client) AirbnbListingAction(ctx context.Context, id string, params *AirbnbListingActionParams, body AirbnbListingActionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAirbnbListingActionRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbListingAmenities List Airbnb amenities
//
// List an Airbnb listing's amenities. **Pure DB read** from the local `listings_airbnb_amenities` cache — never calls Airbnb upstream. The response splits amenities into `amenities` (regular) and `accessibility_amenities` (step-free access, wide doorways, grab rails, disabled parking, wheelchair, accessible-height fixtures, hoists, etc). Both are arrays (`[]` when none). Consult `dataFreshness` to disambiguate "never synced" from "fresh and genuinely empty". Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/amenities (the `ListAirbnbListingAmenities` operationId).
func (c *Client) ListAirbnbListingAmenities(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbListingAmenitiesRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingAmenitiesWithBody Update Airbnb amenities
//
// Set amenities on an Airbnb listing. **Write-side** — calls Airbnb upstream.
//
// **Partial by design**: only the amenities you name change, so turning one off is a one-line body and nothing else on the listing moves. Ids are the `id` values `GET /amenities` returns (e.g. `wireless_internet`, `ac`, `kitchen`); case is ignored. Airbnb refuses ids outside its vocabulary — that comes back as `422 airbnb_rejected` carrying Airbnb's own message.
//
// `accessibility_amenities` go to Airbnb's separate accessibility resource, which has **no read side at all** — Airbnb offers no endpoint to fetch them back, and the combined amenities GET 404s on production listings. What you can read back is our own copy: this endpoint updates it on success, and `GET /amenities` returns it under `accessibilityAmenities`. Airbnb may also hold an accessibility claim for review until photo evidence is attached; pass `photo_ids` to supply it.
//
// Our Airbnb copy is updated on success so a read straight after this write returns the new values. The platform-neutral copy behind `GET /v1/listings/{id}?include=amenities` uses a different amenity vocabulary and is refreshed by the next sync, except where an id happens to be identical in both.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/amenities (the `UpdateAirbnbListingAmenities` operationId).
func (c *Client) UpdateAirbnbListingAmenitiesWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingAmenitiesRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingAmenities Update Airbnb amenities
//
// Set amenities on an Airbnb listing. **Write-side** — calls Airbnb upstream.
//
// **Partial by design**: only the amenities you name change, so turning one off is a one-line body and nothing else on the listing moves. Ids are the `id` values `GET /amenities` returns (e.g. `wireless_internet`, `ac`, `kitchen`); case is ignored. Airbnb refuses ids outside its vocabulary — that comes back as `422 airbnb_rejected` carrying Airbnb's own message.
//
// `accessibility_amenities` go to Airbnb's separate accessibility resource, which has **no read side at all** — Airbnb offers no endpoint to fetch them back, and the combined amenities GET 404s on production listings. What you can read back is our own copy: this endpoint updates it on success, and `GET /amenities` returns it under `accessibilityAmenities`. Airbnb may also hold an accessibility claim for review until photo evidence is attached; pass `photo_ids` to supply it.
//
// Our Airbnb copy is updated on success so a read straight after this write returns the new values. The platform-neutral copy behind `GET /v1/listings/{id}?include=amenities` uses a different amenity vocabulary and is refreshed by the next sync, except where an id happens to be identical in both.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/amenities (the `UpdateAirbnbListingAmenities` operationId).
func (c *Client) UpdateAirbnbListingAmenities(ctx context.Context, id string, body UpdateAirbnbListingAmenitiesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingAmenitiesRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbListingAvailability Get Airbnb availability
//
// Read the per-day availability calendar for an Airbnb listing. Returns one row per day including price overrides, min-stay, and blocked status.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/availability (the `GetAirbnbListingAvailability` operationId).
func (c *Client) GetAirbnbListingAvailability(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbListingAvailabilityRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingAvailabilityWithBody Update Airbnb availability
//
// Push availability + restrictions to Airbnb. `type: "calendar"` writes per-date restrictions — min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`) — via a batch of operations that each target either a date range or an explicit date list. `type: "rules"` writes listing-level availability rules (default min/max nights, booking lead time, turnover days, seasonal/day-of-week min nights). Restrictions never leak across channels — this endpoint writes only to Airbnb.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
//
// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
//
// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/availability (the `UpdateAirbnbListingAvailability` operationId).
func (c *Client) UpdateAirbnbListingAvailabilityWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingAvailabilityRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingAvailability Update Airbnb availability
//
// Push availability + restrictions to Airbnb. `type: "calendar"` writes per-date restrictions — min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`) — via a batch of operations that each target either a date range or an explicit date list. `type: "rules"` writes listing-level availability rules (default min/max nights, booking lead time, turnover days, seasonal/day-of-week min nights). Restrictions never leak across channels — this endpoint writes only to Airbnb.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
//
// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
//
// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/availability (the `UpdateAirbnbListingAvailability` operationId).
func (c *Client) UpdateAirbnbListingAvailability(ctx context.Context, id string, body UpdateAirbnbListingAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingAvailabilityRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbBookingSettings Get Airbnb booking settings
//
// Read how an Airbnb listing takes bookings and what happens when a guest cancels: booking mode, Instant Book state, the good-track-record requirement, check-in/check-out times, advance notice, preparation time, booking window, the short-stay and long-stay cancellation policies, and the non-refundable option.
//
// **This is Repull's stored copy, not a live call to Airbnb.** Values come from the local Airbnb mirror that the sync workers fill, so the response always carries `dataFreshness` — check `dataFreshness.stale` (and `dataFreshness.accounts[]` when the workspace has several Airbnb accounts) before treating a value as current.
//
// **Not exposed by Airbnb.** The pre-reservation message and automatic stay extension have no field on Airbnb's `booking_settings` resource, so neither can be read or written here; set the pre-reservation message in the Airbnb host dashboard, and handle extensions through `/v1/channels/airbnb/alterations`. Airbnb also expresses the same-day cutoff only as whole hours of advance notice, so `advanceNotice.hours` is as precise as the cutoff gets.
//
// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive — an inactive listing keeps syncing but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/booking-settings (the `GetAirbnbBookingSettings` operationId).
func (c *Client) GetAirbnbBookingSettings(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbBookingSettingsRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbBookingSettingsWithBody Update Airbnb booking settings
//
// Set any subset of a listing's booking settings on Airbnb. Partial — a field you do not send is left as it is.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id; Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb, so a bad value is a `422` naming the field rather than a failed upstream call. Unknown fields are refused rather than dropped.
//
// **Two groups, applied in order.** Instant Book, check-in/out and the cancellation fields go to Airbnb's booking-settings resource. Advance notice, preparation time and booking window go to Airbnb's availability rules — Airbnb replaces that whole document, so Repull reads the current rules first and merges your change onto them, which is why setting a preparation time does not blank the listing's min/max nights. The response's `applied` array names the groups that were written.
//
// **Non-refundable is a percentage here, a factor on Airbnb.** Airbnb stores `non_refundable_price_factor` between 0.7 and 1.0; send `cancellation.nonRefundable.discountPercent` (0-30) and Repull converts — 10% becomes 0.9. `enabled: false` sets the factor to 1.0.
//
// **Not exposed by Airbnb:** `preReservationMessage` and `automaticStayExtension`. Sending either returns a `422` explaining where to set it instead.
//
// **Errors:** `403 connection_reauth_required` — Airbnb no longer accepts the connection for this listing (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 invalid_params` — the body is wrong; `field` names it. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/booking-settings (the `UpdateAirbnbBookingSettings` operationId).
func (c *Client) UpdateAirbnbBookingSettingsWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbBookingSettingsRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbBookingSettings Update Airbnb booking settings
//
// Set any subset of a listing's booking settings on Airbnb. Partial — a field you do not send is left as it is.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id; Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb, so a bad value is a `422` naming the field rather than a failed upstream call. Unknown fields are refused rather than dropped.
//
// **Two groups, applied in order.** Instant Book, check-in/out and the cancellation fields go to Airbnb's booking-settings resource. Advance notice, preparation time and booking window go to Airbnb's availability rules — Airbnb replaces that whole document, so Repull reads the current rules first and merges your change onto them, which is why setting a preparation time does not blank the listing's min/max nights. The response's `applied` array names the groups that were written.
//
// **Non-refundable is a percentage here, a factor on Airbnb.** Airbnb stores `non_refundable_price_factor` between 0.7 and 1.0; send `cancellation.nonRefundable.discountPercent` (0-30) and Repull converts — 10% becomes 0.9. `enabled: false` sets the factor to 1.0.
//
// **Not exposed by Airbnb:** `preReservationMessage` and `automaticStayExtension`. Sending either returns a `422` explaining where to set it instead.
//
// **Errors:** `403 connection_reauth_required` — Airbnb no longer accepts the connection for this listing (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 invalid_params` — the body is wrong; `field` names it. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/booking-settings (the `UpdateAirbnbBookingSettings` operationId).
func (c *Client) UpdateAirbnbBookingSettings(ctx context.Context, id string, body UpdateAirbnbBookingSettingsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbBookingSettingsRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbCheckinGuide Get Airbnb check-in guide
//
// Return every published locale variant of an Airbnb listing's check-in guide. **Pure DB read** from `listings_airbnb_check_in_guides`. Pass `?locale=en` to filter to one locale (prefix match). Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/checkin-guide (the `GetAirbnbCheckinGuide` operationId).
func (c *Client) GetAirbnbCheckinGuide(ctx context.Context, id string, params *GetAirbnbCheckinGuideParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbCheckinGuideRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbCheckinGuide Upsert Airbnb check-in guide
//
// Upsert the check-in guide for one locale on an Airbnb listing. **Write-side** — calls Airbnb upstream; the DB mirror is reconciled by the sync worker once the upstream call returns. Target the locale with `?locale=en` (defaults to `en`). Requires a connected Airbnb host, else `404 no_connection`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/checkin-guide (the `UpdateAirbnbCheckinGuide` operationId).
func (c *Client) UpdateAirbnbCheckinGuide(ctx context.Context, id string, params *UpdateAirbnbCheckinGuideParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbCheckinGuideRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbCheckoutGuide Get Airbnb checkout guide
//
// Return the checkout tasks an Airbnb listing shows guests at departure. **Pure DB read** from `listings_airbnb_checkout_tasks`. Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/checkout-guide (the `GetAirbnbCheckoutGuide` operationId).
func (c *Client) GetAirbnbCheckoutGuide(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbCheckoutGuideRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbListingDescriptions List Airbnb descriptions
//
// List an Airbnb listing's per-locale content (name, summary, house rules, etc). **Pure DB read** from `listings_airbnb_descriptions`. Filter to one locale with `?locale=en` (the legacy `?country=` param is accepted as a soft alias). Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/descriptions (the `ListAirbnbListingDescriptions` operationId).
func (c *Client) ListAirbnbListingDescriptions(ctx context.Context, id string, params *ListAirbnbListingDescriptionsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbListingDescriptionsRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingDescriptionWithBody Update an Airbnb description for one locale
//
// Write one locale's copy to the live Airbnb listing.
//
// Airbnb keeps a SEPARATE description per locale (`PUT /v2/listing_descriptions/{listingId}/{locale}`), which is why `locale` is part of the request and not a guess: a listing can carry twelve of them, and writing Italian copy into the English row is how a translation gets lost. Only the fields you send are written; Airbnb keeps the rest. `GET /v1/channels/airbnb/listings/{id}/settings?type=locales` lists the locales already synced for the listing.
//
// `description` is not an accepted field: Airbnb composes the public description from the sections (`summary`, `space`, `access`, …) and ignores a directly-supplied one.
//
// **A 200 does not by itself mean the change was applied.** On an established listing Airbnb LOCKS host-managed description fields — the write returns 200, reports them as locked, and applies nothing for them. The response reports `blockedFields`: the fields YOU sent that Airbnb dropped. `blockedFields: []` is what a landed write looks like; a non-empty list is still a 200 (the other fields really were written) with a `message` naming what was not. Reporting that as a clean success is the bug behind "the description does not push to Airbnb".
//
// This writes to AIRBNB. To write Repull's own canonical copy — the content a later publish distributes — use `PUT /v1/listings/{id}/content` with `locale`.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Returns `403 listing_inactive` when the listing is inactive.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/descriptions (the `UpdateAirbnbListingDescription` operationId).
func (c *Client) UpdateAirbnbListingDescriptionWithBody(ctx context.Context, id string, params *UpdateAirbnbListingDescriptionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingDescriptionRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingDescription Update an Airbnb description for one locale
//
// Write one locale's copy to the live Airbnb listing.
//
// Airbnb keeps a SEPARATE description per locale (`PUT /v2/listing_descriptions/{listingId}/{locale}`), which is why `locale` is part of the request and not a guess: a listing can carry twelve of them, and writing Italian copy into the English row is how a translation gets lost. Only the fields you send are written; Airbnb keeps the rest. `GET /v1/channels/airbnb/listings/{id}/settings?type=locales` lists the locales already synced for the listing.
//
// `description` is not an accepted field: Airbnb composes the public description from the sections (`summary`, `space`, `access`, …) and ignores a directly-supplied one.
//
// **A 200 does not by itself mean the change was applied.** On an established listing Airbnb LOCKS host-managed description fields — the write returns 200, reports them as locked, and applies nothing for them. The response reports `blockedFields`: the fields YOU sent that Airbnb dropped. `blockedFields: []` is what a landed write looks like; a non-empty list is still a 200 (the other fields really were written) with a `message` naming what was not. Reporting that as a clean success is the bug behind "the description does not push to Airbnb".
//
// This writes to AIRBNB. To write Repull's own canonical copy — the content a later publish distributes — use `PUT /v1/listings/{id}/content` with `locale`.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Returns `403 listing_inactive` when the listing is inactive.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/descriptions (the `UpdateAirbnbListingDescription` operationId).
func (c *Client) UpdateAirbnbListingDescription(ctx context.Context, id string, params *UpdateAirbnbListingDescriptionParams, body UpdateAirbnbListingDescriptionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingDescriptionRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbListingDetails Get Airbnb listing details
//
// What kind of property Airbnb thinks this is — property type group and category, room type, capacity — plus the check-in method (`checkInOption`), whether the listing is live (`hasAvailability`), and **`lockedFields`: the attributes Airbnb refuses to change on this listing**.
//
// **Pure DB read** from the local mirror, one entry per Airbnb connection.
//
// Read `lockedFields` before a content write. Airbnb does not refuse a write to a locked attribute: it returns 200, reports the attribute as locked, and applies nothing — which is why a write can look successful and change nothing. 1,180 of 5,917 synced listings carry at least one locked attribute.
//
// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/details (the `GetAirbnbListingDetails` operationId).
func (c *Client) GetAirbnbListingDetails(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbListingDetailsRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingDetailsWithBody Update property type, room type, quiet hours or check-in method
//
// Change what kind of property the Airbnb listing is, when its quiet hours are, or how the guest gets in. Partial: only the fields you send are written. At least one required; an unknown field is refused by name rather than dropped.
//
// This is the UPDATE path for fields that previously had none. `POST /v1/listings` accepts a `propertyType` when a listing is CREATED and nothing could change it afterwards, so a listing mis-typed at import stayed mis-typed; the check-in method was mirrored and never exposed at all.
//
// **A 200 does not by itself mean the change was applied.** `property_type_category`, `property_type_group` and `check_in_option` are among the attributes Airbnb locks on established listings: the write returns 200, and Airbnb applies nothing for the locked ones. The response reports `blockedFields` — the fields YOU sent that Airbnb dropped — and `blockedFields: []` is what a landed write looks like. `GET …/details` reports the same list as `lockedFields` so you can check first.
//
// Canonical property type (the value Repull keeps and republishes) is set with `PUT /v1/listings/{id}/content` under `details`; this endpoint writes straight to Airbnb.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/details (the `UpdateAirbnbListingDetails` operationId).
func (c *Client) UpdateAirbnbListingDetailsWithBody(ctx context.Context, id string, params *UpdateAirbnbListingDetailsParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingDetailsRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingDetails Update property type, room type, quiet hours or check-in method
//
// Change what kind of property the Airbnb listing is, when its quiet hours are, or how the guest gets in. Partial: only the fields you send are written. At least one required; an unknown field is refused by name rather than dropped.
//
// This is the UPDATE path for fields that previously had none. `POST /v1/listings` accepts a `propertyType` when a listing is CREATED and nothing could change it afterwards, so a listing mis-typed at import stayed mis-typed; the check-in method was mirrored and never exposed at all.
//
// **A 200 does not by itself mean the change was applied.** `property_type_category`, `property_type_group` and `check_in_option` are among the attributes Airbnb locks on established listings: the write returns 200, and Airbnb applies nothing for the locked ones. The response reports `blockedFields` — the fields YOU sent that Airbnb dropped — and `blockedFields: []` is what a landed write looks like. `GET …/details` reports the same list as `lockedFields` so you can check first.
//
// Canonical property type (the value Repull keeps and republishes) is set with `PUT /v1/listings/{id}/content` under `details`; this endpoint writes straight to Airbnb.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/details (the `UpdateAirbnbListingDetails` operationId).
func (c *Client) UpdateAirbnbListingDetails(ctx context.Context, id string, params *UpdateAirbnbListingDetailsParams, body UpdateAirbnbListingDetailsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingDetailsRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbListingPermits List Airbnb permits and licences
//
// The regulatory permits, licences and registration numbers attached to an Airbnb listing.
//
// **DB-only by default.** `?source=cache` (the default) returns the permits as last mirrored by the sync worker — regulatory body, regulation type, status, permit number — with no upstream call.
//
// **`?source=live` also returns the QUESTIONS.** The mirror stores the RESULT of a permit, not what Airbnb asks for it, so a caller that is about to write needs `?source=live` once: it returns each permit's `flows[]` with the `answer_key`, `type` and `choices` of every question, and the answers already on file. Airbnb refuses an `answer_key` it did not ask for on this listing, so this is not optional guesswork you can skip.
//
// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/permits (the `ListAirbnbListingPermits` operationId).
func (c *Client) ListAirbnbListingPermits(ctx context.Context, id string, params *ListAirbnbListingPermitsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbListingPermitsRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingPermitsWithBody Answer Airbnb permit questions
//
// Answer the regulatory permit questions for a listing — the licence or registration number a city requires to keep the listing up.
//
// Read the questions first with `GET …/permits?source=live`. For each permit, pick one of its `flows[]` and send its `slug` as `flow_slug`; key every answer by the question's `answer_key`, and let the question's `type` decide the value field (`text_value`, `attestation_value`, `radio_value`, `date_value` or `selected_options_value`). Answers are forwarded verbatim — nothing is defaulted or inferred, because a wrong licence number can take a listing down in a regulated city.
//
// Send `Idempotency-Key`: a timeout here leaves you unable to tell "never arrived" from "arrived, response lost", and this is a compliance filing.
//
// Airbnb refusing the answers (an unknown `answer_key`, a malformed licence number) is `422 airbnb_rejected` carrying Airbnb's own reason. An expired or revoked Airbnb connection is `403 connection_reauth_required`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/permits (the `UpdateAirbnbListingPermits` operationId).
func (c *Client) UpdateAirbnbListingPermitsWithBody(ctx context.Context, id string, params *UpdateAirbnbListingPermitsParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingPermitsRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingPermits Answer Airbnb permit questions
//
// Answer the regulatory permit questions for a listing — the licence or registration number a city requires to keep the listing up.
//
// Read the questions first with `GET …/permits?source=live`. For each permit, pick one of its `flows[]` and send its `slug` as `flow_slug`; key every answer by the question's `answer_key`, and let the question's `type` decide the value field (`text_value`, `attestation_value`, `radio_value`, `date_value` or `selected_options_value`). Answers are forwarded verbatim — nothing is defaulted or inferred, because a wrong licence number can take a listing down in a regulated city.
//
// Send `Idempotency-Key`: a timeout here leaves you unable to tell "never arrived" from "arrived, response lost", and this is a compliance filing.
//
// Airbnb refusing the answers (an unknown `answer_key`, a malformed licence number) is `422 airbnb_rejected` carrying Airbnb's own reason. An expired or revoked Airbnb connection is `403 connection_reauth_required`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/permits (the `UpdateAirbnbListingPermits` operationId).
func (c *Client) UpdateAirbnbListingPermits(ctx context.Context, id string, params *UpdateAirbnbListingPermitsParams, body UpdateAirbnbListingPermitsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingPermitsRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteAirbnbListingPhoto Delete an Airbnb photo
//
// Remove a single photo from an Airbnb listing. Pass the Airbnb-side photo id as `?photoId=`. **Write-side** — calls Airbnb upstream.
//
// The photo is proven to belong to the listing named in the path first; a photo from another listing returns `404`. Airbnb refuses to delete a listing's last photo. Both stored copies drop the photo on success — `stored` reports whether that succeeded.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with DELETE /v1/channels/airbnb/listings/{id}/photos (the `DeleteAirbnbListingPhoto` operationId).
func (c *Client) DeleteAirbnbListingPhoto(ctx context.Context, id string, params *DeleteAirbnbListingPhotoParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteAirbnbListingPhotoRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbListingPhotos List Airbnb photos
//
// List photos attached to an Airbnb listing in display order. Returns the public CDN URL plus Airbnb-side metadata (id, caption, room).
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/photos (the `ListAirbnbListingPhotos` operationId).
func (c *Client) ListAirbnbListingPhotos(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbListingPhotosRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingPhotoWithBody Update an Airbnb photo
//
// Change one photo's caption, its position in the tour, the room it is filed under, or its metadata. **Write-side** — calls Airbnb upstream.
//
// Airbnb's photo endpoints are keyed by photo id alone, so the photo is proven to belong to the listing named in the path before anything is sent; a photo from another listing returns `404`, the same answer a photo that does not exist gets.
//
// On success both stored copies are updated — the Airbnb mirror `GET /photos` serves AND the canonical photo tour behind `GET /v1/listings/{id}` — so a read straight after this write returns the new value instead of waiting for the next sync. `stored` says whether that succeeded; `false` means Airbnb accepted the change but our copy will only catch up at the next sync.
//
// To move several photos at once use `PUT /photos/order`: it is one call instead of N, it validates the whole order before writing anything, and it reports exactly what landed if Airbnb refuses part-way.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /v1/channels/airbnb/listings/{id}/photos (the `UpdateAirbnbListingPhoto` operationId).
func (c *Client) UpdateAirbnbListingPhotoWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingPhotoRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingPhoto Update an Airbnb photo
//
// Change one photo's caption, its position in the tour, the room it is filed under, or its metadata. **Write-side** — calls Airbnb upstream.
//
// Airbnb's photo endpoints are keyed by photo id alone, so the photo is proven to belong to the listing named in the path before anything is sent; a photo from another listing returns `404`, the same answer a photo that does not exist gets.
//
// On success both stored copies are updated — the Airbnb mirror `GET /photos` serves AND the canonical photo tour behind `GET /v1/listings/{id}` — so a read straight after this write returns the new value instead of waiting for the next sync. `stored` says whether that succeeded; `false` means Airbnb accepted the change but our copy will only catch up at the next sync.
//
// To move several photos at once use `PUT /photos/order`: it is one call instead of N, it validates the whole order before writing anything, and it reports exactly what landed if Airbnb refuses part-way.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /v1/channels/airbnb/listings/{id}/photos (the `UpdateAirbnbListingPhoto` operationId).
func (c *Client) UpdateAirbnbListingPhoto(ctx context.Context, id string, body UpdateAirbnbListingPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingPhotoRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UploadAirbnbListingPhotosWithBody Upload photos to Airbnb
//
// Upload one or more photos to an Airbnb listing.
//
// `image` is base64 image DATA, not a url — a `data:image/jpeg;base64,…` prefix is accepted and stripped, and the decoded image must be under 25 MB. (This operation previously documented public image urls that Airbnb would fetch. It never did: a url arrived at Airbnb as a ~60-byte image.)
//
// Airbnb assigns the photo id and CDN urls, so the newly uploaded photos appear in `GET /photos` after the next sync. Caption, order and room assignment can be set straight away with `PATCH /photos`, `PUT /photos/order` and `PUT /photos/cover`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/listings/{id}/photos (the `UploadAirbnbListingPhotos` operationId).
func (c *Client) UploadAirbnbListingPhotosWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUploadAirbnbListingPhotosRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UploadAirbnbListingPhotos Upload photos to Airbnb
//
// Upload one or more photos to an Airbnb listing.
//
// `image` is base64 image DATA, not a url — a `data:image/jpeg;base64,…` prefix is accepted and stripped, and the decoded image must be under 25 MB. (This operation previously documented public image urls that Airbnb would fetch. It never did: a url arrived at Airbnb as a ~60-byte image.)
//
// Airbnb assigns the photo id and CDN urls, so the newly uploaded photos appear in `GET /photos` after the next sync. Caption, order and room assignment can be set straight away with `PATCH /photos`, `PUT /photos/order` and `PUT /photos/cover`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/listings/{id}/photos (the `UploadAirbnbListingPhotos` operationId).
func (c *Client) UploadAirbnbListingPhotos(ctx context.Context, id string, body UploadAirbnbListingPhotosJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUploadAirbnbListingPhotosRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetAirbnbListingCoverPhotoWithBody Set the Airbnb cover photo
//
// Choose which photo leads the listing. **Write-side** — calls Airbnb upstream.
//
// Airbnb has no "cover" field: the cover is the first photo of the tour, so this is a position write. Usually it is a single upstream request — the chosen photo takes a position below the current first one and nothing else moves. When the tour already starts at position 1 and there is no room below it, the tour is renumbered instead, one request per photo whose position actually changes, with the same stop-at-first-failure reporting as `PUT /photos/order` (`applied`, `failed_photo_id`, `not_attempted`; re-send the identical body to finish).
//
// The listing thumbnail — what every list view renders — is repointed at the new cover, so the change is not visible only inside the photo tour.
//
// The photo must already be in our cached copy of the tour; one uploaded since the last sync returns `404`, and `PATCH /photos` can set its position in the meantime.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/cover (the `SetAirbnbListingCoverPhoto` operationId).
func (c *Client) SetAirbnbListingCoverPhotoWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetAirbnbListingCoverPhotoRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetAirbnbListingCoverPhoto Set the Airbnb cover photo
//
// Choose which photo leads the listing. **Write-side** — calls Airbnb upstream.
//
// Airbnb has no "cover" field: the cover is the first photo of the tour, so this is a position write. Usually it is a single upstream request — the chosen photo takes a position below the current first one and nothing else moves. When the tour already starts at position 1 and there is no room below it, the tour is renumbered instead, one request per photo whose position actually changes, with the same stop-at-first-failure reporting as `PUT /photos/order` (`applied`, `failed_photo_id`, `not_attempted`; re-send the identical body to finish).
//
// The listing thumbnail — what every list view renders — is repointed at the new cover, so the change is not visible only inside the photo tour.
//
// The photo must already be in our cached copy of the tour; one uploaded since the last sync returns `404`, and `PATCH /photos` can set its position in the meantime.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/cover (the `SetAirbnbListingCoverPhoto` operationId).
func (c *Client) SetAirbnbListingCoverPhoto(ctx context.Context, id string, body SetAirbnbListingCoverPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetAirbnbListingCoverPhotoRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReorderAirbnbListingPhotosWithBody Reorder the Airbnb photo tour
//
// Set the order of a listing's photo tour in one call. **Write-side** — calls Airbnb upstream.
//
// Send the photo ids in the order you want them shown, first photo first. Ids you leave out keep their current relative order behind the ones you list, so moving one photo to the front is `{"photo_ids": ["<id>"]}`. Positions are then written as a dense run starting at 1.
//
// Airbnb has no bulk photo endpoint — order is one `sort_order` per photo — so this saves a loop of up to 200 requests against your rate limit and makes the partial-failure case reportable. How much is atomic:
//
// - Everything is validated before anything is written. A duplicate id, an id that is not on this listing, an inactive listing or a missing connection all fail with **zero** upstream writes.
// - Only photos whose position actually changes are written; re-sending the order you already have writes nothing.
// - On the first upstream failure the run stops — nothing after it is attempted. The error carries `applied`, `failed_photo_id` and `not_attempted`, uses Airbnb's own status (`422` rejected / `403` reauth / `429` / `502`), and the operation is idempotent: re-send the identical body to finish the run.
// - Our stored copy records what actually landed, never the intent.
//
// Validation is against our cached copy of the tour, so a listing whose photos have never synced returns `404` — use `PATCH /photos` (which needs no cache) until the first sync lands.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/order (the `ReorderAirbnbListingPhotos` operationId).
func (c *Client) ReorderAirbnbListingPhotosWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReorderAirbnbListingPhotosRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReorderAirbnbListingPhotos Reorder the Airbnb photo tour
//
// Set the order of a listing's photo tour in one call. **Write-side** — calls Airbnb upstream.
//
// Send the photo ids in the order you want them shown, first photo first. Ids you leave out keep their current relative order behind the ones you list, so moving one photo to the front is `{"photo_ids": ["<id>"]}`. Positions are then written as a dense run starting at 1.
//
// Airbnb has no bulk photo endpoint — order is one `sort_order` per photo — so this saves a loop of up to 200 requests against your rate limit and makes the partial-failure case reportable. How much is atomic:
//
// - Everything is validated before anything is written. A duplicate id, an id that is not on this listing, an inactive listing or a missing connection all fail with **zero** upstream writes.
// - Only photos whose position actually changes are written; re-sending the order you already have writes nothing.
// - On the first upstream failure the run stops — nothing after it is attempted. The error carries `applied`, `failed_photo_id` and `not_attempted`, uses Airbnb's own status (`422` rejected / `403` reauth / `429` / `502`), and the operation is idempotent: re-send the identical body to finish the run.
// - Our stored copy records what actually landed, never the intent.
//
// Validation is against our cached copy of the tour, so a listing whose photos have never synced returns `404` — use `PATCH /photos` (which needs no cache) until the first sync lands.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/order (the `ReorderAirbnbListingPhotos` operationId).
func (c *Client) ReorderAirbnbListingPhotos(ctx context.Context, id string, body ReorderAirbnbListingPhotosJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReorderAirbnbListingPhotosRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbListingPricing Get Airbnb pricing
//
// Read the current pricing config (base price, weekend uplift, length-of-stay discounts, smart-pricing bounds) for an Airbnb listing.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/pricing (the `GetAirbnbListingPricing` operationId).
func (c *Client) GetAirbnbListingPricing(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbListingPricingRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingPricingWithBody Update Airbnb pricing
//
// Push pricing changes to Airbnb. The `type` discriminator selects the sub-resource (model, standard settings, LOS, rate-plan, fees, currency, rule, or per-date `calendar`). `type: "calendar"` carries the full per-date restriction set — nightly price, min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`). For settings sub-resources the full object is replaced — GET first, mutate locally, then PUT the whole object.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
//
// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
//
// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/pricing (the `UpdateAirbnbListingPricing` operationId).
func (c *Client) UpdateAirbnbListingPricingWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingPricingRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingPricing Update Airbnb pricing
//
// Push pricing changes to Airbnb. The `type` discriminator selects the sub-resource (model, standard settings, LOS, rate-plan, fees, currency, rule, or per-date `calendar`). `type: "calendar"` carries the full per-date restriction set — nightly price, min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`). For settings sub-resources the full object is replaced — GET first, mutate locally, then PUT the whole object.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
//
// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
//
// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/pricing (the `UpdateAirbnbListingPricing` operationId).
func (c *Client) UpdateAirbnbListingPricing(ctx context.Context, id string, body UpdateAirbnbListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingPricingRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbListingQuality Get Airbnb listing quality
//
// Return an Airbnb listing's quality signals — standards, reservation issues, and monthly quality stats. **Pure DB read** from the local quality mirrors. Scope the response with `?type=all|standards|issues|stats` (default `all`, which returns `{ standards, issues }`). Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/quality (the `GetAirbnbListingQuality` operationId).
func (c *Client) GetAirbnbListingQuality(ctx context.Context, id string, params *GetAirbnbListingQualityParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbListingQualityRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteAirbnbListingRoom Delete an Airbnb room
//
// Delete a room from an Airbnb listing, and its beds with it. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=`. Requires a connected Airbnb host, else `404 no_connection`.
//
// The room is proven to belong to the listing named in the path first; a room from another listing returns `404`. Both stored copies drop the room on success — `stored` reports whether that succeeded.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with DELETE /v1/channels/airbnb/listings/{id}/rooms (the `DeleteAirbnbListingRoom` operationId).
func (c *Client) DeleteAirbnbListingRoom(ctx context.Context, id string, params *DeleteAirbnbListingRoomParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteAirbnbListingRoomRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbListingRooms List Airbnb rooms
//
// List the rooms configured on an Airbnb listing, ordered by room number, each with its sleeping arrangement in `beds`. **Pure DB read** from `listings_airbnb_rooms` + `listings_airbnb_beds`. Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/rooms (the `ListAirbnbListingRooms` operationId).
func (c *Client) ListAirbnbListingRooms(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbListingRoomsRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateAirbnbListingRoomWithBody Create an Airbnb room
//
// Create a new room on an Airbnb listing, with its sleeping arrangement. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host, else `404 no_connection`.
//
// The response is the room object as Airbnb returned it. The new room is also seeded into our own copy, so the very next `GET /rooms` shows it rather than waiting for the sync.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/listings/{id}/rooms (the `CreateAirbnbListingRoom` operationId).
func (c *Client) CreateAirbnbListingRoomWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateAirbnbListingRoomRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateAirbnbListingRoom Create an Airbnb room
//
// Create a new room on an Airbnb listing, with its sleeping arrangement. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host, else `404 no_connection`.
//
// The response is the room object as Airbnb returned it. The new room is also seeded into our own copy, so the very next `GET /rooms` shows it rather than waiting for the sync.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/listings/{id}/rooms (the `CreateAirbnbListingRoom` operationId).
func (c *Client) CreateAirbnbListingRoom(ctx context.Context, id string, body CreateAirbnbListingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateAirbnbListingRoomRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingRoomWithBody Update an Airbnb room
//
// Change a room's type, number, privacy or sleeping arrangement. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=` and send only the fields you want to change.
//
// `beds` REPLACES the room's whole arrangement — that is Airbnb's semantics for the field — so send every bed the room has, not just the changed one.
//
// Airbnb's room endpoints are keyed by room id alone, so the room is proven to belong to the listing named in the path before anything is sent; a room from another listing returns `404`, the same answer a room that does not exist gets.
//
// On success both stored copies are rebuilt to match, so a read straight after this write returns the new arrangement. `stored` says whether that succeeded.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/rooms (the `UpdateAirbnbListingRoom` operationId).
func (c *Client) UpdateAirbnbListingRoomWithBody(ctx context.Context, id string, params *UpdateAirbnbListingRoomParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingRoomRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingRoom Update an Airbnb room
//
// Change a room's type, number, privacy or sleeping arrangement. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=` and send only the fields you want to change.
//
// `beds` REPLACES the room's whole arrangement — that is Airbnb's semantics for the field — so send every bed the room has, not just the changed one.
//
// Airbnb's room endpoints are keyed by room id alone, so the room is proven to belong to the listing named in the path before anything is sent; a room from another listing returns `404`, the same answer a room that does not exist gets.
//
// On success both stored copies are rebuilt to match, so a read straight after this write returns the new arrangement. `stored` says whether that succeeded.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/rooms (the `UpdateAirbnbListingRoom` operationId).
func (c *Client) UpdateAirbnbListingRoom(ctx context.Context, id string, params *UpdateAirbnbListingRoomParams, body UpdateAirbnbListingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingRoomRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbListingSafetyDisclosures List guest-safety disclosures
//
// What a guest is told about the property before they book — exterior security cameras, a decibel noise monitor, pets on the property, stairs, a pool with no fence, weapons, shared spaces, limited parking. Airbnb calls them `listing_expectations_for_guests` and shows them at booking time.
//
// **Pure DB read** from the local mirror. EVERY supported disclosure type is returned, including the ones this listing has not declared (`value: false`), so "does this property have cameras?" has an answer rather than a missing key — `declared` tells you whether Airbnb holds an explicit answer. Types Airbnb returns that are not in the documented set are passed through rather than dropped.
//
// Where a listing is connected to several Airbnb listings, a disclosure declared on any of them is reported as true of the property.
//
// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/safety-disclosures (the `ListAirbnbListingSafetyDisclosures` operationId).
func (c *Client) ListAirbnbListingSafetyDisclosures(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbListingSafetyDisclosuresRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingSafetyDisclosuresWithBody Update guest-safety disclosures
//
// Declare or retract the guest-safety disclosures on the live Airbnb listing.
//
// **This is a MERGE, not a replacement.** Airbnb keeps one value per disclosure type: a type you leave out keeps the value it has, and to retract one you send it with `value: false`. A full replacement would let a partial request silently un-declare a security camera — a guest-safety statement, not a preference.
//
// Only the disclosures are sent upstream. The same Airbnb endpoint carries the cancellation policy and instant-book settings, and this endpoint never touches them.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Airbnb refusing the change is `422 airbnb_rejected` with Airbnb's own reason; an expired or revoked connection is `403 connection_reauth_required`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/safety-disclosures (the `UpdateAirbnbListingSafetyDisclosures` operationId).
func (c *Client) UpdateAirbnbListingSafetyDisclosuresWithBody(ctx context.Context, id string, params *UpdateAirbnbListingSafetyDisclosuresParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingSafetyDisclosuresRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbListingSafetyDisclosures Update guest-safety disclosures
//
// Declare or retract the guest-safety disclosures on the live Airbnb listing.
//
// **This is a MERGE, not a replacement.** Airbnb keeps one value per disclosure type: a type you leave out keeps the value it has, and to retract one you send it with `value: false`. A full replacement would let a partial request silently un-declare a security camera — a guest-safety statement, not a preference.
//
// Only the disclosures are sent upstream. The same Airbnb endpoint carries the cancellation policy and instant-book settings, and this endpoint never touches them.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Airbnb refusing the change is `422 airbnb_rejected` with Airbnb's own reason; an expired or revoked connection is `403 connection_reauth_required`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/safety-disclosures (the `UpdateAirbnbListingSafetyDisclosures` operationId).
func (c *Client) UpdateAirbnbListingSafetyDisclosures(ctx context.Context, id string, params *UpdateAirbnbListingSafetyDisclosuresParams, body UpdateAirbnbListingSafetyDisclosuresJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbListingSafetyDisclosuresRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbListingSettings Get Airbnb listing settings
//
// Return an Airbnb listing's host roles, published locales, and regulatory permits. **Pure DB read** — host roles from `listings_airbnb_details.host_roles`, locales from distinct `listings_airbnb_descriptions.locale`, permits from `listings_airbnb_permits`. Scope with `?type=all|hosts|permits|locales` (default `all`, which returns `{ hosts, locales }`). Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/settings (the `GetAirbnbListingSettings` operationId).
func (c *Client) GetAirbnbListingSettings(ctx context.Context, id string, params *GetAirbnbListingSettingsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbListingSettingsRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbThreads List Airbnb message threads
//
// List Airbnb message threads (one per guest conversation). Cursor-paginated. Each thread includes a preview of the latest message.
//
// Threads on inactive listings are left out; they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Corresponds with GET /v1/channels/airbnb/messaging (the `ListAirbnbThreads` operationId).
func (c *Client) ListAirbnbThreads(ctx context.Context, params *ListAirbnbThreadsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbThreadsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbThread Get Airbnb thread
//
// Fetch a single Airbnb message thread by its Airbnb thread id. **Pure DB read** from the local `message_threads` mirror, workspace-scoped. Returns `404 not_found` when no thread matches. For the messages within a thread use `GET /v1/channels/airbnb/messaging/{threadId}/messages`.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/messaging/{threadId} (the `GetAirbnbThread` operationId).
func (c *Client) GetAirbnbThread(ctx context.Context, threadId string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbThreadRequest(c.Server, threadId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbThreadMessages Get Airbnb messages
//
// Messages stored for an Airbnb thread, as recorded rows (not the unified `Message` shape — use `GET /v1/conversations/{id}/messages` for that). By default returns 50 per page, newest first; walk older pages with `?cursor=` (the `pagination.nextCursor` of the previous page) until `pagination.hasMore` is `false`. `?all=true` returns up to 1000 rows oldest-first in one response, with no `pagination`.
//
// Each row carries `attachments` — photos and other files on that message, inbound or outbound — in the same shape as the unified endpoint.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/messaging/{threadId}/messages (the `ListAirbnbThreadMessages` operationId).
func (c *Client) ListAirbnbThreadMessages(ctx context.Context, threadId string, params *ListAirbnbThreadMessagesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbThreadMessagesRequest(c.Server, threadId, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SendAirbnbMessageWithBody Send Airbnb message
//
// Send a message in an Airbnb thread as the host. Airbnb enforces content rules (no off-platform contact info, no external URLs) — violating messages are rejected upstream and surface as `422 airbnb_rejected` carrying Airbnb's own reason. Resending the same text is refused again; edit it first. `502 airbnb_error` is the other answer and means something else entirely: Airbnb did not complete the send, so retry it unchanged.
//
// ### Sending a photo or video (`mediaUrl`)
//
// Airbnb only accepts media uploaded to a signed URL it issues, one file per message and no text on the same message. With `mediaUrl`, Repull downloads the file (public `https://` only, 10 MB max), reads its real type from the bytes (JPEG, PNG, GIF, WebP — converted to JPEG — or MP4/QuickTime), uploads it to Airbnb and sends it; `message`, if given, follows as a separate message. This is the same flow as `POST /v1/conversations/{id}/messages` with `attachments` — prefer that endpoint, which also takes several files per request. The response is a `SendMessageResponse`, the send is recorded in the conversation, and failures are the 422 codes documented there (`attachment_type_not_supported`, `attachment_too_large`, `message_not_sent` for a pre-booking thread, …). The thread must already be synced to Repull (`GET /v1/conversations` lists them), otherwise `404`.
//
// Text-only sends (no `mediaUrl`) go straight to Airbnb and return Airbnb's message object.
//
// The `{threadId}` is the Airbnb thread id — the `externalThreadId` field on a unified `Conversation` (`GET /v1/conversations`).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/messaging/{threadId}/messages (the `SendAirbnbMessage` operationId).
func (c *Client) SendAirbnbMessageWithBody(ctx context.Context, threadId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSendAirbnbMessageRequestWithBody(c.Server, threadId, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SendAirbnbMessage Send Airbnb message
//
// Send a message in an Airbnb thread as the host. Airbnb enforces content rules (no off-platform contact info, no external URLs) — violating messages are rejected upstream and surface as `422 airbnb_rejected` carrying Airbnb's own reason. Resending the same text is refused again; edit it first. `502 airbnb_error` is the other answer and means something else entirely: Airbnb did not complete the send, so retry it unchanged.
//
// ### Sending a photo or video (`mediaUrl`)
//
// Airbnb only accepts media uploaded to a signed URL it issues, one file per message and no text on the same message. With `mediaUrl`, Repull downloads the file (public `https://` only, 10 MB max), reads its real type from the bytes (JPEG, PNG, GIF, WebP — converted to JPEG — or MP4/QuickTime), uploads it to Airbnb and sends it; `message`, if given, follows as a separate message. This is the same flow as `POST /v1/conversations/{id}/messages` with `attachments` — prefer that endpoint, which also takes several files per request. The response is a `SendMessageResponse`, the send is recorded in the conversation, and failures are the 422 codes documented there (`attachment_type_not_supported`, `attachment_too_large`, `message_not_sent` for a pre-booking thread, …). The thread must already be synced to Repull (`GET /v1/conversations` lists them), otherwise `404`.
//
// Text-only sends (no `mediaUrl`) go straight to Airbnb and return Airbnb's message object.
//
// The `{threadId}` is the Airbnb thread id — the `externalThreadId` field on a unified `Conversation` (`GET /v1/conversations`).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/messaging/{threadId}/messages (the `SendAirbnbMessage` operationId).
func (c *Client) SendAirbnbMessage(ctx context.Context, threadId string, body SendAirbnbMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSendAirbnbMessageRequest(c.Server, threadId, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbMessageWithBody Edit / react to / mark an Airbnb message
//
// Act on a single message in an Airbnb thread. **Write-side** — calls Airbnb upstream. The `action` discriminator selects the operation:
//
// - `edit` — replace message text (requires `message`).
// - `unsend` — retract the message.
// - `read` — mark the message as read.
// - `react` — add a reaction (requires `reaction`).
//
// Requires a connected Airbnb host, else `404 no_connection`.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /v1/channels/airbnb/messaging/{threadId}/messages/{messageId} (the `UpdateAirbnbMessage` operationId).
func (c *Client) UpdateAirbnbMessageWithBody(ctx context.Context, threadId string, messageId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbMessageRequestWithBody(c.Server, threadId, messageId, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateAirbnbMessage Edit / react to / mark an Airbnb message
//
// Act on a single message in an Airbnb thread. **Write-side** — calls Airbnb upstream. The `action` discriminator selects the operation:
//
// - `edit` — replace message text (requires `message`).
// - `unsend` — retract the message.
// - `read` — mark the message as read.
// - `react` — add a reaction (requires `reaction`).
//
// Requires a connected Airbnb host, else `404 no_connection`.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /v1/channels/airbnb/messaging/{threadId}/messages/{messageId} (the `UpdateAirbnbMessage` operationId).
func (c *Client) UpdateAirbnbMessage(ctx context.Context, threadId string, messageId string, body UpdateAirbnbMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateAirbnbMessageRequest(c.Server, threadId, messageId, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// WithdrawAirbnbOffer Withdraw Airbnb special offer
//
// Withdraw a special offer the guest has not booked. **Write-side** — calls Airbnb upstream. Pass the Airbnb offer id as `?offerId=`. The Repull-id equivalent is `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
//
// Corresponds with DELETE /v1/channels/airbnb/offers (the `WithdrawAirbnbOffer` operationId).
func (c *Client) WithdrawAirbnbOffer(ctx context.Context, params *WithdrawAirbnbOfferParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewWithdrawAirbnbOfferRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbOffer Get Airbnb special offer
//
// Read a pre-approval or special offer from Airbnb by its Airbnb id. **Live read** — calls Airbnb upstream. Pass the id as `?offerId=`. The Repull-id equivalent is `GET /v1/conversations/{id}/special-offers/{offerId}`, which also confirms the offer belongs to that conversation.
//
// Corresponds with GET /v1/channels/airbnb/offers (the `GetAirbnbOffer` operationId).
func (c *Client) GetAirbnbOffer(ctx context.Context, params *GetAirbnbOfferParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbOfferRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateAirbnbOfferWithBody Create Airbnb special offer or pre-approval
//
// Create a pre-approval or a special offer on an Airbnb thread, addressed by **Airbnb** ids. **Write-side** — calls Airbnb upstream. The Repull-id equivalents, which also update the inquiry in Vanio, are `POST /v1/conversations/{id}/pre-approval` and `POST /v1/conversations/{id}/special-offers` — prefer those unless you only hold Airbnb ids.
//
// - `type: "preapproval"` — let the guest book the dates and price they asked about. Requires `thread_id`; optional `block_instant_booking`.
// - `type: "offer"` — your own terms. Requires `thread_id`, `listing_id` (the **Airbnb** listing id, as a string), `start_date`, `nights`, `total_price` (whole stay, listing currency) and `guest_details` with `number_of_guests` (or `number_of_adults`; Airbnb counts adults + children).
//
// The body is validated before anything reaches Airbnb (a `422 invalid_params` names the field), and unknown fields are refused. The legacy spellings `threadId` and `blockInstantBooking` still work. The request is sent as the Airbnb account that owns the thread or listing.
//
// Airbnb refusals are mapped rather than returned as a 500: `409 inquiry_no_longer_open` / `inquiry_expired` when the inquiry moved on, `422 airbnb_rejected` with Airbnb’s reason otherwise, `403 connection_reauth_required` when the grant does not allow it.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/offers (the `CreateAirbnbOffer` operationId).
func (c *Client) CreateAirbnbOfferWithBody(ctx context.Context, params *CreateAirbnbOfferParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateAirbnbOfferRequestWithBody(c.Server, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateAirbnbOffer Create Airbnb special offer or pre-approval
//
// Create a pre-approval or a special offer on an Airbnb thread, addressed by **Airbnb** ids. **Write-side** — calls Airbnb upstream. The Repull-id equivalents, which also update the inquiry in Vanio, are `POST /v1/conversations/{id}/pre-approval` and `POST /v1/conversations/{id}/special-offers` — prefer those unless you only hold Airbnb ids.
//
// - `type: "preapproval"` — let the guest book the dates and price they asked about. Requires `thread_id`; optional `block_instant_booking`.
// - `type: "offer"` — your own terms. Requires `thread_id`, `listing_id` (the **Airbnb** listing id, as a string), `start_date`, `nights`, `total_price` (whole stay, listing currency) and `guest_details` with `number_of_guests` (or `number_of_adults`; Airbnb counts adults + children).
//
// The body is validated before anything reaches Airbnb (a `422 invalid_params` names the field), and unknown fields are refused. The legacy spellings `threadId` and `blockInstantBooking` still work. The request is sent as the Airbnb account that owns the thread or listing.
//
// Airbnb refusals are mapped rather than returned as a 500: `409 inquiry_no_longer_open` / `inquiry_expired` when the inquiry moved on, `422 airbnb_rejected` with Airbnb’s reason otherwise, `403 connection_reauth_required` when the grant does not allow it.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/offers (the `CreateAirbnbOffer` operationId).
func (c *Client) CreateAirbnbOffer(ctx context.Context, params *CreateAirbnbOfferParams, body CreateAirbnbOfferJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateAirbnbOfferRequest(c.Server, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbReservations List Airbnb reservations
//
// Cursor-paginated list of reservations sourced directly from Airbnb. Use this when you need Airbnb-specific fields (guest payout split, cancellation policy snapshot) that the unified `/v1/reservations` endpoint flattens away.
//
// Walk pages with `?cursor=<pagination.nextCursor>` until `pagination.hasMore` is `false`. The cursor is opaque — never construct or parse it client-side.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. Internally this walks upstream Airbnb cursor pages to skip rows, so deep offsets cost N/limit upstream round-trips; cursor remains the better choice for deep pagination.
//
// When `status` is omitted, all statuses are returned (Airbnb defaults to `accepted` only on its own surface, but this endpoint normalises to "all"). Pass `?status=accepted` to scope.
//
// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Corresponds with GET /v1/channels/airbnb/reservations (the `ListAirbnbReservations` operationId).
func (c *Client) ListAirbnbReservations(ctx context.Context, params *ListAirbnbReservationsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbReservationsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAirbnbReservation Get Airbnb reservation
//
// Fetch a single Airbnb reservation by Airbnb confirmation code (e.g. `HMABCDEF12`).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/airbnb/reservations/{code} (the `GetAirbnbReservation` operationId).
func (c *Client) GetAirbnbReservation(ctx context.Context, code string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAirbnbReservationRequest(c.Server, code)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AirbnbReservationActionWithBody Accept, decline or cancel an Airbnb reservation
//
// Act on an Airbnb reservation by its Airbnb confirmation code. **Write-side** — calls Airbnb upstream, as the Airbnb account that owns the booking.
//
// - `accept` — accept a pending booking request.
// - `decline` — decline a pending booking request. Requires `reason` (one of Airbnb's decline reasons) and `message` (sent to the guest, at most 500 characters).
// - `cancel` — cancel a confirmed booking as the host. Requires `reason` (one of Airbnb's host-cancellation reasons). **Host cancellations carry Airbnb penalties.**
//
// The body is validated before anything reaches Airbnb; unknown fields are refused. There is no `pre-approve` action: a pre-approval answers an inquiry, which has no confirmation code — use `POST /v1/conversations/{id}/pre-approval`. For accept/decline, `POST /v1/reservations/{id}/accept` and `/decline` do the same by Repull id and keep the reservation in Repull in sync.
//
// Airbnb refusals are mapped rather than returned as a 500: a request that already moved on is `409 request_no_longer_pending` (do not retry), an expired one `409 request_expired`, any other refusal `422 airbnb_rejected` with Airbnb's reason.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/reservations/{code} (the `AirbnbReservationAction` operationId).
func (c *Client) AirbnbReservationActionWithBody(ctx context.Context, code string, params *AirbnbReservationActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAirbnbReservationActionRequestWithBody(c.Server, code, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AirbnbReservationAction Accept, decline or cancel an Airbnb reservation
//
// Act on an Airbnb reservation by its Airbnb confirmation code. **Write-side** — calls Airbnb upstream, as the Airbnb account that owns the booking.
//
// - `accept` — accept a pending booking request.
// - `decline` — decline a pending booking request. Requires `reason` (one of Airbnb's decline reasons) and `message` (sent to the guest, at most 500 characters).
// - `cancel` — cancel a confirmed booking as the host. Requires `reason` (one of Airbnb's host-cancellation reasons). **Host cancellations carry Airbnb penalties.**
//
// The body is validated before anything reaches Airbnb; unknown fields are refused. There is no `pre-approve` action: a pre-approval answers an inquiry, which has no confirmation code — use `POST /v1/conversations/{id}/pre-approval`. For accept/decline, `POST /v1/reservations/{id}/accept` and `/decline` do the same by Repull id and keep the reservation in Repull in sync.
//
// Airbnb refusals are mapped rather than returned as a 500: a request that already moved on is `409 request_no_longer_pending` (do not retry), an expired one `409 request_expired`, any other refusal `422 airbnb_rejected` with Airbnb's reason.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/reservations/{code} (the `AirbnbReservationAction` operationId).
func (c *Client) AirbnbReservationAction(ctx context.Context, code string, params *AirbnbReservationActionParams, body AirbnbReservationActionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAirbnbReservationActionRequest(c.Server, code, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbReviews List Airbnb reviews
//
// List reviews left by guests on Airbnb listings in this workspace. Includes both reviews of the host and reviews of the guest (where the host has not yet submitted theirs).
//
// Reviews of inactive listings are left out; they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Corresponds with GET /v1/channels/airbnb/reviews (the `ListAirbnbReviews` operationId).
func (c *Client) ListAirbnbReviews(ctx context.Context, params *ListAirbnbReviewsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbReviewsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RespondAirbnbReviewLegacy Respond to / submit Airbnb review (legacy)
//
// Legacy action-based shape. Body `{ action: "respond"|"submit", reviewId, response?, review? }`. Kept for backwards compatibility — prefer `PUT /v1/channels/airbnb/reviews/{id}` (edit) and `POST /v1/channels/airbnb/reviews/{id}/respond` (reply) for new integrations.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with POST /v1/channels/airbnb/reviews (the `RespondAirbnbReviewLegacy` operationId).
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *Client) RespondAirbnbReviewLegacy(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRespondAirbnbReviewLegacyRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// EditAirbnbReviewWithBody Edit Airbnb host review
//
// Edit a host-side review for an Airbnb stay. Airbnb collapses POST + PUT into the same upstream call (`PUT /v2/listing_reviews/{id}`), so this endpoint covers both initial submit and subsequent edits while the review window is open.
//
// Body is a partial `AirbnbReview` — pass the fields you want to change (rating, public review, private feedback, category ratings).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/airbnb/reviews/{id} (the `EditAirbnbReview` operationId).
func (c *Client) EditAirbnbReviewWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewEditAirbnbReviewRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// EditAirbnbReview Edit Airbnb host review
//
// Edit a host-side review for an Airbnb stay. Airbnb collapses POST + PUT into the same upstream call (`PUT /v2/listing_reviews/{id}`), so this endpoint covers both initial submit and subsequent edits while the review window is open.
//
// Body is a partial `AirbnbReview` — pass the fields you want to change (rating, public review, private feedback, category ratings).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/airbnb/reviews/{id} (the `EditAirbnbReview` operationId).
func (c *Client) EditAirbnbReview(ctx context.Context, id string, body EditAirbnbReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewEditAirbnbReviewRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RespondAirbnbReviewWithBody Respond to Airbnb review
//
// Post a public host response to a guest review. Airbnb allows one response per review — repeated POSTs return 409. Response text is capped at 1000 characters.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/reviews/{id}/respond (the `RespondAirbnbReview` operationId).
func (c *Client) RespondAirbnbReviewWithBody(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRespondAirbnbReviewRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RespondAirbnbReview Respond to Airbnb review
//
// Post a public host response to a guest review. Airbnb allows one response per review — repeated POSTs return 409. Response text is capped at 1000 characters.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/reviews/{id}/respond (the `RespondAirbnbReview` operationId).
func (c *Client) RespondAirbnbReview(ctx context.Context, id string, body RespondAirbnbReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRespondAirbnbReviewRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListAirbnbTransactions List Airbnb transactions
//
// List Airbnb host transactions (reservation earnings, payouts, resolution adjustments) for this workspace, newest first. **Pure DB read** — customer-facing reads never call Airbnb upstream; they serve the `airbnb_transactions` mirror. Each row carries the genuine host- and guest-side financial breakdown (accommodation subtotal, cleaning fee, host + guest service fees split base/VAT, tax buckets, expected/actual host payout with settlement status). Trigger a refresh with `POST` on this path. When the mirror is empty or the host disconnected, `dataFreshness.stale = true` with a `reason` (`never_synced`, `host_disconnected_<iso>`, `sync_lag_>_24h`).
//
// Transactions of reservations on inactive listings are left out; payout rows, which belong to no listing, are always included.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Corresponds with GET /v1/channels/airbnb/transactions (the `ListAirbnbTransactions` operationId).
func (c *Client) ListAirbnbTransactions(ctx context.Context, params *ListAirbnbTransactionsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListAirbnbTransactionsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SyncAirbnbTransactionsWithBody Sync Airbnb transactions
//
// Refresh the Airbnb transactions mirror for this workspace by pulling from Airbnb upstream and upserting the breakdown that `GET` serves. Optional JSON body `{ start_date, end_date, transaction_type }` (`transaction_type` is `COMPLETED` or `UPCOMING`; both are synced when omitted). Returns `{ synced, count }`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/airbnb/transactions (the `SyncAirbnbTransactions` operationId).
func (c *Client) SyncAirbnbTransactionsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSyncAirbnbTransactionsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SyncAirbnbTransactions Sync Airbnb transactions
//
// Refresh the Airbnb transactions mirror for this workspace by pulling from Airbnb upstream and upserting the breakdown that `GET` serves. Optional JSON body `{ start_date, end_date, transaction_type }` (`transaction_type` is `COMPLETED` or `UPCOMING`; both are synced when omitted). Returns `{ synced, count }`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/airbnb/transactions (the `SyncAirbnbTransactions` operationId).
func (c *Client) SyncAirbnbTransactions(ctx context.Context, body SyncAirbnbTransactionsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSyncAirbnbTransactionsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetBookingAvailability Read current Booking.com rates/availability/restrictions
//
// Read the current rate, availability, and restriction state for a Booking.com property so you can reconcile before writing with the PUT on this path. Keyed by `property_id` (the Booking hotel id), symmetric with the PUT.
//
// Proxies Booking's `getRoomRateAvailability` — the returned fields (price, rooms-to-sell, min/max stay, closed-to-arrival/departure, stop-sell) are whatever Booking.com emits for the window. A listing-id-keyed equivalent is available at `GET /v1/channels/booking/listings/{id}/pricing`.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/booking/availability (the `GetBookingAvailability` operationId).
func (c *Client) GetBookingAvailability(ctx context.Context, params *GetBookingAvailabilityParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetBookingAvailabilityRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateBookingAvailabilityWithBody Update Booking.com rates/availability
//
// Write rates, availability and restrictions to a Booking.com property. `type` selects the write:
//
// - `rates` — nightly prices, plus any length-of-stay / arrival restrictions sent with them.
// - `availability` — inventory (`availableRooms`), the stop-sell flag (`closed`), and restrictions. Omit `availableRooms` and `closed` for a restriction-only write.
// - `derived-pricing` — occupancy-derived pricing rules.
//
// **Dates are inclusive at both ends.** `{ "start": "2026-11-04", "end": "2026-11-04" }` is exactly one night.
//
// **A rate amount needs an occupancy.** Booking.com stores the amount against the party size the rate plan prices: sent above that number it declines the price in silence, sent below it it answers 400. Send `occupancy`, or omit it and Repull resolves it from Booking.com's own data and echoes the value and its `source` back in `occupancy[]`. If it cannot be resolved the write is refused with `422` naming `updates[N].occupancy`.
//
// **Restrictions are sent in the same call, on their own wire.** A price and a minimum stay are two writes on Booking.com's side. Send them together and the response reports each separately: `price` and `restrictions` carry their own state, their own read-back, and — when refused — Booking.com's own reason. The top-level `applied` is `partial` when they disagree, so a price that landed is never reported as a failure. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` because Booking.com's notification has no element for them — set those on the rate plan in the Extranet. Nothing you send is ever silently ignored.
//
// **Inventory is not part of a rate update.** `roomsToSell` on a `rates` update returns `422 inventory_not_in_rate_update`; send it as `type: "availability"` instead.
//
// **The response says what is known.** Booking.com acknowledges a write with no per-date status, so the nights are read back — prices and restrictions out of the same read: `applied` is `verified`, `mismatch`, `partial`, `rejected` or `unverified` (send `verify: false` to skip the read-back). A bare acknowledgement is never reported as "all updates applied". Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
//
// Restrictions never leak across channels — this endpoint writes only to Booking.com. When Booking.com refuses a write outright, their own reason comes back as `422 booking_rejected` with `booking_ruid`; a genuine outage on their side is `502 booking_error`.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/booking/availability (the `UpdateBookingAvailability` operationId).
func (c *Client) UpdateBookingAvailabilityWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateBookingAvailabilityRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateBookingAvailability Update Booking.com rates/availability
//
// Write rates, availability and restrictions to a Booking.com property. `type` selects the write:
//
// - `rates` — nightly prices, plus any length-of-stay / arrival restrictions sent with them.
// - `availability` — inventory (`availableRooms`), the stop-sell flag (`closed`), and restrictions. Omit `availableRooms` and `closed` for a restriction-only write.
// - `derived-pricing` — occupancy-derived pricing rules.
//
// **Dates are inclusive at both ends.** `{ "start": "2026-11-04", "end": "2026-11-04" }` is exactly one night.
//
// **A rate amount needs an occupancy.** Booking.com stores the amount against the party size the rate plan prices: sent above that number it declines the price in silence, sent below it it answers 400. Send `occupancy`, or omit it and Repull resolves it from Booking.com's own data and echoes the value and its `source` back in `occupancy[]`. If it cannot be resolved the write is refused with `422` naming `updates[N].occupancy`.
//
// **Restrictions are sent in the same call, on their own wire.** A price and a minimum stay are two writes on Booking.com's side. Send them together and the response reports each separately: `price` and `restrictions` carry their own state, their own read-back, and — when refused — Booking.com's own reason. The top-level `applied` is `partial` when they disagree, so a price that landed is never reported as a failure. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` because Booking.com's notification has no element for them — set those on the rate plan in the Extranet. Nothing you send is ever silently ignored.
//
// **Inventory is not part of a rate update.** `roomsToSell` on a `rates` update returns `422 inventory_not_in_rate_update`; send it as `type: "availability"` instead.
//
// **The response says what is known.** Booking.com acknowledges a write with no per-date status, so the nights are read back — prices and restrictions out of the same read: `applied` is `verified`, `mismatch`, `partial`, `rejected` or `unverified` (send `verify: false` to skip the read-back). A bare acknowledgement is never reported as "all updates applied". Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
//
// Restrictions never leak across channels — this endpoint writes only to Booking.com. When Booking.com refuses a write outright, their own reason comes back as `422 booking_rejected` with `booking_ruid`; a genuine outage on their side is `502 booking_error`.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/booking/availability (the `UpdateBookingAvailability` operationId).
func (c *Client) UpdateBookingAvailability(ctx context.Context, body UpdateBookingAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateBookingAvailabilityRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetBookingCharges Get Booking.com charges
//
// Fetch the extra-charge set (cleaning fee, resort fee, city tax, etc.) configured for a Booking.com property. Pass the Booking.com `property_id` as a query param — required.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/booking/charges (the `GetBookingCharges` operationId).
func (c *Client) GetBookingCharges(ctx context.Context, params *GetBookingChargesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetBookingChargesRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateBookingChargesWithBody Set Booking.com charges
//
// Replace the extra-charge set for a Booking.com property. The body carries the target `property_id` and the full `charges` array — Booking treats the write as a full replacement, so include every charge you want to keep.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/booking/charges (the `UpdateBookingCharges` operationId).
func (c *Client) UpdateBookingChargesWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateBookingChargesRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateBookingCharges Set Booking.com charges
//
// Replace the extra-charge set for a Booking.com property. The body carries the target `property_id` and the full `charges` array — Booking treats the write as a full replacement, so include every charge you want to keep.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/booking/charges (the `UpdateBookingCharges` operationId).
func (c *Client) UpdateBookingCharges(ctx context.Context, body UpdateBookingChargesJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateBookingChargesRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetBookingContent Get Booking.com content
//
// Read one kind of content for a Booking.com property, straight from Booking.com.
//
// | `type` | What it is |
// |---|---|
// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
// | `policies` | Cancellation and prepayment policies. |
// | `licences` | The region's licence rules and the licence on file. |
// | `checkin_methods` | How guests get in (holiday homes). |
// | `contacts` | Who Booking.com contacts about the property. |
//
// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/booking/content (the `GetBookingContent` operationId).
func (c *Client) GetBookingContent(ctx context.Context, params *GetBookingContentParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetBookingContentRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateBookingContentWithBody Update Booking.com content
//
// Write one kind of content to a Booking.com property only. Nothing on the canonical listing or on Airbnb changes. To send the listing's own content to every channel instead, use `PUT /v1/listings/{id}/content` and publish.
//
// | `type` | What it is |
// |---|---|
// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
// | `policies` | Cancellation and prepayment policies. |
// | `licences` | The region's licence rules and the licence on file. |
// | `checkin_methods` | How guests get in (holiday homes). |
// | `contacts` | Who Booking.com contacts about the property. |
//
// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
//
// What each `type` takes:
//
// - `description`: `text`, optional `language` (default `en`).
// - `facilities`: `facilities: [{ facility_id | room_facility_id, state: "PRESENT" | "MISSING", instances? }]`. Facilities you do not send stay as they are.
// - `photos`: `photos: [{ url }]`, uploaded in the background. With `room_id`, send `photo_ids` instead to add photos that have finished processing to that room.
// - `settings`: `settings: { <block>: {…} }`, for example `{ "pets": { "pets_allowed": "PETS_ALLOWED" } }`. Each block is written separately and reported in `results`.
// - `policies`: `policyCode` (152 = free cancellation at any time, 1 = non-refundable, …), optional `prepaymentRequired`; add `policyId` to change an existing policy. A property holds at most 7 policies and none can be deleted.
// - `licences`: `variantId` and `contentData: [{ name, value }]`, from the rules `GET ?type=licences` returns; optional `room_id`.
// - `checkin_methods`: `methods: [{ checkin_method }]`, using a name from `GET ?type=checkin_methods` `available`.
// - `contacts`: `contacts: [...]` in Booking.com's contact shape.
//
// If Booking.com refuses the write, the response is `422 booking_rejected` with Booking.com's reason, even when Booking.com answered HTTP 200. Resending the same body will be refused again.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/booking/content (the `UpdateBookingContent` operationId).
func (c *Client) UpdateBookingContentWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateBookingContentRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateBookingContent Update Booking.com content
//
// Write one kind of content to a Booking.com property only. Nothing on the canonical listing or on Airbnb changes. To send the listing's own content to every channel instead, use `PUT /v1/listings/{id}/content` and publish.
//
// | `type` | What it is |
// |---|---|
// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
// | `policies` | Cancellation and prepayment policies. |
// | `licences` | The region's licence rules and the licence on file. |
// | `checkin_methods` | How guests get in (holiday homes). |
// | `contacts` | Who Booking.com contacts about the property. |
//
// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
//
// What each `type` takes:
//
// - `description`: `text`, optional `language` (default `en`).
// - `facilities`: `facilities: [{ facility_id | room_facility_id, state: "PRESENT" | "MISSING", instances? }]`. Facilities you do not send stay as they are.
// - `photos`: `photos: [{ url }]`, uploaded in the background. With `room_id`, send `photo_ids` instead to add photos that have finished processing to that room.
// - `settings`: `settings: { <block>: {…} }`, for example `{ "pets": { "pets_allowed": "PETS_ALLOWED" } }`. Each block is written separately and reported in `results`.
// - `policies`: `policyCode` (152 = free cancellation at any time, 1 = non-refundable, …), optional `prepaymentRequired`; add `policyId` to change an existing policy. A property holds at most 7 policies and none can be deleted.
// - `licences`: `variantId` and `contentData: [{ name, value }]`, from the rules `GET ?type=licences` returns; optional `room_id`.
// - `checkin_methods`: `methods: [{ checkin_method }]`, using a name from `GET ?type=checkin_methods` `available`.
// - `contacts`: `contacts: [...]` in Booking.com's contact shape.
//
// If Booking.com refuses the write, the response is `422 booking_rejected` with Booking.com's reason, even when Booking.com answered HTTP 200. Resending the same body will be refused again.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/booking/content (the `UpdateBookingContent` operationId).
func (c *Client) UpdateBookingContent(ctx context.Context, body UpdateBookingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateBookingContentRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// MapBookingRoomWithBody Map a Booking.com room to a Repull listing
//
// Link a Booking.com room to a canonical Repull listing — the API-key equivalent of the room mapping the hosted Connect flow performs, and the counterpart of `POST /v1/channels/airbnb/listings/map`.
//
// Discover `roomBookingId` with `GET /v1/channels/booking/properties/{id}/rooms`, which returns every room of a property with the `roomId` this route takes.
//
// Booking.com attaches at the ROOM level: a property is a building and its rooms are what a guest books, so each room maps to one listing. Pass `listingId: null` to unmap a room and remove its channel link.
//
// The room mapping and its channel link are repointed together in one transaction, so a link can never outlive the mapping it describes — a stale link keeps routing that room's reservations to the previous listing. Re-sending a mapping that is already in place writes nothing (`alreadyMapped: true`).
//
// **The property's reservations are pulled as part of the call.** Once the room is mapped, every active reservation Booking.com holds for the property is imported and attached to its listing — `reservationsImported` says how many were processed. One already present is left as it is, so re-sending never duplicates. You do not need a follow-up call: reservations that arrived before the room was mapped are never picked up by the regular sync, so this is the moment they are brought in. It runs on every successful map, including a re-send, so re-sending retries an import that did not run. If the import cannot run, the mapping still stands and `reservationsImported` is `null`. A property with a long booking history can take tens of seconds. Unmapping pulls nothing.
//
// Unlike the Airbnb route, there is no conflict when the target listing already carries another Booking.com room: one listing served by several rooms is a normal arrangement and is not refused.
//
// Scope is enforced on both sides against your workspace — the room's property and the target listing. A room or listing belonging to another workspace returns the same 404 as one that does not exist.
//
// Returns `403 listing_inactive` when the target listing, or the listing the room is mapped to now, is inactive; nothing is changed.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/booking/listings/map (the `MapBookingRoom` operationId).
func (c *Client) MapBookingRoomWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewMapBookingRoomRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// MapBookingRoom Map a Booking.com room to a Repull listing
//
// Link a Booking.com room to a canonical Repull listing — the API-key equivalent of the room mapping the hosted Connect flow performs, and the counterpart of `POST /v1/channels/airbnb/listings/map`.
//
// Discover `roomBookingId` with `GET /v1/channels/booking/properties/{id}/rooms`, which returns every room of a property with the `roomId` this route takes.
//
// Booking.com attaches at the ROOM level: a property is a building and its rooms are what a guest books, so each room maps to one listing. Pass `listingId: null` to unmap a room and remove its channel link.
//
// The room mapping and its channel link are repointed together in one transaction, so a link can never outlive the mapping it describes — a stale link keeps routing that room's reservations to the previous listing. Re-sending a mapping that is already in place writes nothing (`alreadyMapped: true`).
//
// **The property's reservations are pulled as part of the call.** Once the room is mapped, every active reservation Booking.com holds for the property is imported and attached to its listing — `reservationsImported` says how many were processed. One already present is left as it is, so re-sending never duplicates. You do not need a follow-up call: reservations that arrived before the room was mapped are never picked up by the regular sync, so this is the moment they are brought in. It runs on every successful map, including a re-send, so re-sending retries an import that did not run. If the import cannot run, the mapping still stands and `reservationsImported` is `null`. A property with a long booking history can take tens of seconds. Unmapping pulls nothing.
//
// Unlike the Airbnb route, there is no conflict when the target listing already carries another Booking.com room: one listing served by several rooms is a normal arrangement and is not refused.
//
// Scope is enforced on both sides against your workspace — the room's property and the target listing. A room or listing belonging to another workspace returns the same 404 as one that does not exist.
//
// Returns `403 listing_inactive` when the target listing, or the listing the room is mapped to now, is inactive; nothing is changed.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/booking/listings/map (the `MapBookingRoom` operationId).
func (c *Client) MapBookingRoom(ctx context.Context, body MapBookingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewMapBookingRoomRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetBookingListingPricing Get Booking.com pricing for a listing
//
// Resolves the Repull listing id to its Booking.com `hotel_id` (via the room mapping the Connect flow records for the authenticated workspace), then proxies Booking's `getRoomRateAvailability` for the requested window. Pricing on Booking is per-room/per-rate-plan, so `room_id` and `room_level` flow through query params unchanged.
//
// Mirrors the per-channel `/listings/{id}/pricing` shape used by Airbnb so SDK consumers can carry a Repull listing id across channels. `id` is a Repull listing id, never a Booking.com hotel id — the hotel-id surface is `/v1/channels/booking/availability`.
//
// A listing can be published under several Booking.com properties. GET uses the oldest and reports the rest in `otherHotelIds`; PUT refuses with `409 ambiguous_booking_mapping` rather than push rates into a property it guessed at. `?hotel_id=` names the property explicitly for either.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/booking/listings/{id}/pricing (the `GetBookingListingPricing` operationId).
func (c *Client) GetBookingListingPricing(ctx context.Context, id int, params *GetBookingListingPricingParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetBookingListingPricingRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateBookingListingPricingWithBody Update Booking.com pricing for a listing
//
// Writes nightly prices for a listing's Booking.com room + rate plan. Each update needs `roomId` + `rateId` + `dateRange` + `price` + `currency`; `dateRange` is inclusive at both ends, so `start` equal to `end` writes exactly one night.
//
// **Occupancy.** Booking.com stores a rate amount against the party size the rate plan prices. Send `occupancy` and that is what is used; omit it and it is resolved from Booking.com's own data for that (room, rate plan) and echoed back in `occupancy[]` with its `source`. When it cannot be resolved the write is refused with `422` naming `updates[N].occupancy` — a price is never sent at a guessed party size, because Booking.com declines such an amount without saying so.
//
// **Inventory is a separate write.** `roomsToSell` on a rate update returns `422 inventory_not_in_rate_update`; use `PUT /v1/channels/booking/availability` with `type: "availability"`.
//
// **Restrictions ride along, on their own wire.** Send `restrictions` with the price and Booking.com receives two writes; the response reports each separately in `price` and `restrictions`, each with its own state, read-back and — when refused — Booking.com's own reason. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` (Booking.com's notification has no element for them — set those on the rate plan in the Extranet). Nothing you send is silently ignored.
//
// **The response says what is known.** Booking.com acknowledges a write without per-date status, so the affected nights are read back — prices and restrictions out of the same read — and `applied` reports `verified`, `mismatch`, `partial`, `rejected` or `unverified`. `partial` means one half landed and the other did not, which is never reported as a total failure. Send `verify: false` to skip the read-back; `applied` is then `unverified`. Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
//
// `id` is a Repull listing id. When it is published under several Booking.com properties this returns `409 ambiguous_booking_mapping` and pushes nothing — name the property with `?hotel_id=` instead.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/booking/listings/{id}/pricing (the `UpdateBookingListingPricing` operationId).
func (c *Client) UpdateBookingListingPricingWithBody(ctx context.Context, id int, params *UpdateBookingListingPricingParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateBookingListingPricingRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateBookingListingPricing Update Booking.com pricing for a listing
//
// Writes nightly prices for a listing's Booking.com room + rate plan. Each update needs `roomId` + `rateId` + `dateRange` + `price` + `currency`; `dateRange` is inclusive at both ends, so `start` equal to `end` writes exactly one night.
//
// **Occupancy.** Booking.com stores a rate amount against the party size the rate plan prices. Send `occupancy` and that is what is used; omit it and it is resolved from Booking.com's own data for that (room, rate plan) and echoed back in `occupancy[]` with its `source`. When it cannot be resolved the write is refused with `422` naming `updates[N].occupancy` — a price is never sent at a guessed party size, because Booking.com declines such an amount without saying so.
//
// **Inventory is a separate write.** `roomsToSell` on a rate update returns `422 inventory_not_in_rate_update`; use `PUT /v1/channels/booking/availability` with `type: "availability"`.
//
// **Restrictions ride along, on their own wire.** Send `restrictions` with the price and Booking.com receives two writes; the response reports each separately in `price` and `restrictions`, each with its own state, read-back and — when refused — Booking.com's own reason. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` (Booking.com's notification has no element for them — set those on the rate plan in the Extranet). Nothing you send is silently ignored.
//
// **The response says what is known.** Booking.com acknowledges a write without per-date status, so the affected nights are read back — prices and restrictions out of the same read — and `applied` reports `verified`, `mismatch`, `partial`, `rejected` or `unverified`. `partial` means one half landed and the other did not, which is never reported as a total failure. Send `verify: false` to skip the read-back; `applied` is then `unverified`. Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
//
// `id` is a Repull listing id. When it is published under several Booking.com properties this returns `409 ambiguous_booking_mapping` and pushes nothing — name the property with `?hotel_id=` instead.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/booking/listings/{id}/pricing (the `UpdateBookingListingPricing` operationId).
func (c *Client) UpdateBookingListingPricing(ctx context.Context, id int, params *UpdateBookingListingPricingParams, body UpdateBookingListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateBookingListingPricingRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListBookingConversations List Booking.com conversations
//
// List Booking.com guest conversations. Cursor-paginated. Use the messaging POST to send a reply.
//
// Scoped to this workspace. With `property_id`, the property must be connected to this workspace — any other id returns `404 not_found`. Without it, only messages for this workspace's own Booking.com properties are returned.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/booking/messaging (the `ListBookingConversations` operationId).
func (c *Client) ListBookingConversations(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListBookingConversationsRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SendBookingMessageWithBody Send Booking.com message
//
// Send a text message in a Booking.com conversation as the host. Booking enforces content rules similar to Airbnb.
//
// **Text only.** To send photos, use `POST /v1/conversations/{id}/messages` with `attachments` (JPEG or PNG, up to 10 MB each, with message text) — it uploads the files to Booking.com and records them in the conversation. Passing `attachments`, `attachment_ids` or `mediaUrl` here returns `422 attachments_not_supported` and sends nothing.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/booking/messaging (the `SendBookingMessage` operationId).
func (c *Client) SendBookingMessageWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSendBookingMessageRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SendBookingMessage Send Booking.com message
//
// Send a text message in a Booking.com conversation as the host. Booking enforces content rules similar to Airbnb.
//
// **Text only.** To send photos, use `POST /v1/conversations/{id}/messages` with `attachments` (JPEG or PNG, up to 10 MB each, with message text) — it uploads the files to Booking.com and records them in the conversation. Passing `attachments`, `attachment_ids` or `mediaUrl` here returns `422 attachments_not_supported` and sends nothing.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/booking/messaging (the `SendBookingMessage` operationId).
func (c *Client) SendBookingMessage(ctx context.Context, body SendBookingMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSendBookingMessageRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListBookingProperties List Booking.com properties
//
// List every Booking.com property this workspace holds. Each property is returned ONCE, with the Repull listings mapped under it.
//
// A Booking.com property is a building; its rooms are what guests book, and each room is mapped to one Repull listing — so one property routinely carries many listings. `listings[].roomBookingId` is the Booking.com room id an ARI write takes.
//
// A property whose rooms are not mapped yet is still listed, with `mappingStatus: "unmapped"` and an empty `listings` array. That is a real mid-onboarding state, not an error: finish `POST /v1/connect/booking/map-rooms` and the listings appear. Such a property used to be dropped silently, which made a mapped-but-unreadable workspace indistinguishable from one with no Booking connection at all.
//
// Inactive listings are left out of `listings`; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
//
// Corresponds with GET /v1/channels/booking/properties (the `ListBookingProperties` operationId).
func (c *Client) ListBookingProperties(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListBookingPropertiesRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetBookingProperty Get Booking.com connection for a listing
//
// Return the Booking.com connection record(s) for a Repull listing — the linked Booking hotel id, sync flags, markup, sync category, suspension state, and the Booking room the mapping runs through.
//
// `id` is a **Repull listing id**, not a Booking.com hotel id, despite the `properties` segment. (The hotel-id surface is `/v1/channels/booking/availability`.) The mapping is read from wherever the Connect flow recorded it — `listings_booking_rooms` for anything mapped through `POST /v1/connect/booking/map-rooms`, which is essentially every live mapping.
//
// An ARRAY, because one listing can be published under several Booking.com properties at once; `mappedVia` says which record carries each mapping. A listing with no Booking.com mapping returns 404, and the message says which id space the path takes.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/booking/properties/{id} (the `GetBookingProperty` operationId).
func (c *Client) GetBookingProperty(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetBookingPropertyRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BookingPropertyActionWithBody Take a property off sale / put it back (unlist/relist)
//
// Stop this listing's Booking.com property being sold, or start it again. `id` is a **Repull listing id**, not a Booking.com hotel id, as on the GET.
//
// **Booking.com has no unlist, so this is an availability write.** Airbnb has a real deactivate; Booking.com does not. `unlist` closes the mapped room across the whole forward window, so the property stops selling. `relist` is not its mirror image: it re-syncs the true calendar, so dates that are genuinely blocked (a reservation, an owner stay) stay blocked and only the closure `unlist` wrote lifts. Re-opening everything would sell dates that are not for sale.
//
// **Which property gets closed.** A listing can be mapped to more than one Booking.com property — the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. With exactly one, send nothing. With several, name one with `hotelId` (or `?hotel_id=`); omit it and the request is refused with **`409 ambiguous_booking_mapping`** listing the candidates, and nothing is written. That refusal matters more here than on a publish: writing content into the wrong property is recoverable, closing the wrong property's availability takes real inventory off sale while the property you meant keeps selling. Naming a property this listing is not mapped to is a `404` that names the ones it is.
//
// **This does not change the listing in Repull.** `active` — what Repull bills and serves — is untouched by both actions and is deliberately not echoed in the response, so the two ideas can never be read as one field. To take a listing off the market on every channel at once, use `POST /v1/listings/{id}/offline`.
//
// Any other action returns a structured `422` naming the ones that are supported. To push content use `POST /v1/listings/{id}/publish/booking`; to map rooms use `POST /v1/connect/booking/map-rooms`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/booking/properties/{id} (the `BookingPropertyAction` operationId).
func (c *Client) BookingPropertyActionWithBody(ctx context.Context, id int, params *BookingPropertyActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBookingPropertyActionRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BookingPropertyAction Take a property off sale / put it back (unlist/relist)
//
// Stop this listing's Booking.com property being sold, or start it again. `id` is a **Repull listing id**, not a Booking.com hotel id, as on the GET.
//
// **Booking.com has no unlist, so this is an availability write.** Airbnb has a real deactivate; Booking.com does not. `unlist` closes the mapped room across the whole forward window, so the property stops selling. `relist` is not its mirror image: it re-syncs the true calendar, so dates that are genuinely blocked (a reservation, an owner stay) stay blocked and only the closure `unlist` wrote lifts. Re-opening everything would sell dates that are not for sale.
//
// **Which property gets closed.** A listing can be mapped to more than one Booking.com property — the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. With exactly one, send nothing. With several, name one with `hotelId` (or `?hotel_id=`); omit it and the request is refused with **`409 ambiguous_booking_mapping`** listing the candidates, and nothing is written. That refusal matters more here than on a publish: writing content into the wrong property is recoverable, closing the wrong property's availability takes real inventory off sale while the property you meant keeps selling. Naming a property this listing is not mapped to is a `404` that names the ones it is.
//
// **This does not change the listing in Repull.** `active` — what Repull bills and serves — is untouched by both actions and is deliberately not echoed in the response, so the two ideas can never be read as one field. To take a listing off the market on every channel at once, use `POST /v1/listings/{id}/offline`.
//
// Any other action returns a structured `422` naming the ones that are supported. To push content use `POST /v1/listings/{id}/publish/booking`; to map rooms use `POST /v1/connect/booking/map-rooms`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/booking/properties/{id} (the `BookingPropertyAction` operationId).
func (c *Client) BookingPropertyAction(ctx context.Context, id int, params *BookingPropertyActionParams, body BookingPropertyActionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBookingPropertyActionRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListBookingPropertyRooms List Booking.com rooms + rate-plan ids for a listing
//
// Return every Booking.com room and its rate plans for a listing, each with the `roomId` / `rateId` needed to assemble a restriction write via `PUT /v1/channels/booking/availability`.
//
// `id` is a **Repull listing id**, not a Booking.com hotel id, despite the `properties` segment — resolved to the Booking `hotel_id` through the workspace mapping, read from wherever the Connect flow recorded it (`listings_booking_rooms` for anything mapped through `POST /v1/connect/booking/map-rooms`). A listing with no active Booking.com mapping returns 404, and the message says which id space the path takes. When the listing is published under several properties the oldest is used, the rest come back in `otherHotelIds`, and `?hotel_id=` names a different one. Sourced from Booking's B.XML roomrates feed, which returns rooms and rate plans together (the rooms-unit feed alone omits rate-plan ids). This is the API-key surface for the room/rate ids that were previously only reachable inside the hosted Connect room-mapping flow.
//
// `source` says where the answer came from. `booking` means it was read live just now. If Booking.com returns nothing usable for the property, the rooms and rate plans recorded at the last import are served instead, `source` is `mirror`, and `mirrorReason` names what went wrong live — the ids are Booking.com's own and can be written against, but they can be stale, and `maxPersons`, `policy`, `policyId`, `pricingType` and `isChildRate` come back `null` because only the live feed states them. `rooms` is empty only when Booking.com and the last import both have nothing; a read that failed is an error, never an empty list.
//
// Each rate plan carries `maxPersons` — the party size that rate plan prices, which is the `occupancy` a rate amount must be written at. Each room carries `maxAdults`, Booking.com's capacity for the room, which is what a rate write falls back to when the rate plan states no `maxPersons`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/booking/properties/{id}/rooms (the `ListBookingPropertyRooms` operationId).
func (c *Client) ListBookingPropertyRooms(ctx context.Context, id int, params *ListBookingPropertyRoomsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListBookingPropertyRoomsRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListBookingReservations List Booking.com reservations
//
// Pull reservations from Booking.com. `type=new` (default) returns un-acknowledged bookings; `type=modified` returns changed bookings. Pass both `reservation_id` and `hotel_id` to fetch a single reservation's full details. Acknowledge processed reservations with the POST so Booking stops re-serving them in the `new` queue.
//
// Scoped to this workspace. `hotel_id` (or its alias `property_id`) must be a property connected to this workspace; any other id returns `404 not_found`, the same as an id that does not exist. Without a hotel, `new`/`modified` cover every Booking.com property this workspace holds (and return `404 not_found` if it holds none). A `reservation_id` that belongs to another workspace returns `404 not_found`.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/booking/reservations (the `ListBookingReservations` operationId).
func (c *Client) ListBookingReservations(ctx context.Context, params *ListBookingReservationsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListBookingReservationsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AcknowledgeBookingReservationsWithBody Acknowledge Booking.com reservations
//
// Acknowledge one or more reservations so Booking.com removes them from the `new` queue. The body carries `reservation_ids` (non-empty array). Acknowledge only after you have durably persisted each reservation.
//
// Only reservations that belong to this workspace can be acknowledged. If any id in `reservation_ids` is not one of this workspace's Booking.com reservations (`confirmationCode` on `GET /v1/reservations?platform=booking`), nothing is acknowledged and the response is `404 not_found` naming those ids in `reservation_ids`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/booking/reservations (the `AcknowledgeBookingReservations` operationId).
func (c *Client) AcknowledgeBookingReservationsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAcknowledgeBookingReservationsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AcknowledgeBookingReservations Acknowledge Booking.com reservations
//
// Acknowledge one or more reservations so Booking.com removes them from the `new` queue. The body carries `reservation_ids` (non-empty array). Acknowledge only after you have durably persisted each reservation.
//
// Only reservations that belong to this workspace can be acknowledged. If any id in `reservation_ids` is not one of this workspace's Booking.com reservations (`confirmationCode` on `GET /v1/reservations?platform=booking`), nothing is acknowledged and the response is `404 not_found` naming those ids in `reservation_ids`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/booking/reservations (the `AcknowledgeBookingReservations` operationId).
func (c *Client) AcknowledgeBookingReservations(ctx context.Context, body AcknowledgeBookingReservationsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAcknowledgeBookingReservationsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListBookingReviews List Booking.com reviews
//
// List guest reviews for a Booking.com property. Pass `property_id` (the Booking.com hotel id) as a query param — required.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/booking/reviews (the `ListBookingReviews` operationId).
func (c *Client) ListBookingReviews(ctx context.Context, params *ListBookingReviewsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListBookingReviewsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReplyBookingReviewWithBody Reply to Booking.com review
//
// Post a public host reply to a guest review on Booking.com. Booking allows one host reply per review — repeated POSTs are rejected by upstream.
//
// Booking.com does NOT support host-authored reviews of guests via the API (platform-level limitation), so this endpoint is reply-only.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/booking/reviews (the `ReplyBookingReview` operationId).
func (c *Client) ReplyBookingReviewWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReplyBookingReviewRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReplyBookingReview Reply to Booking.com review
//
// Post a public host reply to a guest review on Booking.com. Booking allows one host reply per review — repeated POSTs are rejected by upstream.
//
// Booking.com does NOT support host-authored reviews of guests via the API (platform-level limitation), so this endpoint is reply-only.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/booking/reviews (the `ReplyBookingReview` operationId).
func (c *Client) ReplyBookingReview(ctx context.Context, body ReplyBookingReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReplyBookingReviewRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BookingSetupWithBody Booking.com property setup actions
//
// Action-router for putting a property onto Booking.com — including building one from nothing. Select the step with `action`.
//
// ## Opening a property
//
// - `create-property` — create a NEW Booking.com property for a Repull listing (`listing_id`). Creates the property, its first room with the listing's beds, a rate plan and the room-rate product that makes the room sellable (under the listing's cancellation policy), sets the contact and invoice details and the facilities, seeds availability and rates, syncs the calendar, then runs Booking.com's readiness check and reports what still blocks opening in `warnings`. Send `contact` (`name`, `email`, `phone` in international form); without it the workspace owner is used, and a workspace with no usable contact is refused before anything is created. Returns 201.
// - `add-room` — add another room type (and its sellable product) to a property (`listing_id`, `property_id`). Returns 201.
// - `add-unit` — raise the number of identical units on an existing room (`listing_id`, `property_id`, `room_id`).
// - `advance` — run Booking.com's readiness check for a property (`property_id`) and, when it passes, open it. Returns `checked`, `opened`, `sellable` and `blockers` — Booking.com's own reasons it cannot open yet.
//
// ## Account and policy steps
//
// - `create-legal-entity` — register a legal entity directly (returns 201). Not normally needed: see the legal-entity rules below.
// - `check-legal-status` — always `404`. A legal entity's details are readable for any id on the connectivity-provider credentials every workspace shares, and nothing records which workspace registered which entity, so no entity can be shown to be yours. `create-property` resolves it for you.
// - `check-readiness` — whether a property is ready to open (`property_id`): `ready` and `blockers`, without trying to open it.
// - `open-property` — open the property for sale (`property_id`). Refused with `422 booking_rejected` naming the blockers when it is not ready.
// - `set-contacts` — set property contacts (`property_id`, `contacts` in Booking.com's Contacts API shape; at most one carries the `general` profile).
// - `set-policies` — add a cancellation policy (`property_id`, `policyCode`, optional `prepaymentRequired`). House rules, pets, children and the damage deposit are `POST /v1/channels/booking/content` with `type: "settings"`.
//
// ## Three things about Booking.com that cost real money
//
// **A newly created property is NOT sellable.** Booking.com opens it only when its readiness check passes, and the check names what is missing — a main photo still processing, no availability, a licence the region requires. The response always reports `status: "being_built"` and `sellable: false`, never a guess, with the reasons in `warnings`. Resolve them, then `advance`.
//
// **A room with no ACTIVE rate plan is invisible.** Booking only renders rooms that have at least one active product linkage (room × rate plan). A room can be created successfully, return a `roomId`, and never appear on the property page. If `rateId` comes back `null` from `create-property` or `add-room`, that is exactly what happened: activate a rate plan on the property in the Extranet, then add the room again.
//
// **Room names are Booking.com's.** Travellers see one of Booking.com's standard names ("Two-Bedroom Apartment"), chosen from the listing's bedrooms. The listing's own name is kept as the operator-side reference, never shown to guests.
//
// ## The legal entity is resolved, not asked for
//
// A property is created against the legal entity Booking.com contracts with, invoices and pays. You do not normally send one:
//
// 1. If this workspace already creates properties under a legal entity, that one is reused. A second is never registered.
// 2. If it has none and the request carries `legal_entity` (`company_name`, `legal_contact_name`, `legal_contact_email`), one is registered and used. Booking.com emails the legal contact a contract; creation only succeeds once it is signed.
// 3. If it has none and no `legal_entity`, the request is refused with `422 legal_entity_required` naming the fields — a contracted company is never invented.
//
// `legal_entity_id` overrides all of that. An id that already carries another workspace's properties is refused with `403 legal_entity_not_yours` before anything is created.
//
// ## What you do not control
//
// Properties are created against Booking's **production** target only. A test-target property cannot be sold through and there is no route back from one, so `target` is not a parameter — sending it changes nothing.
//
// The property category comes from the listing's property type (Apartment when it has none; Holiday home, Villa or Chalet when it says so). The initial room count is 1. Latitude and longitude come from the listing and are adjusted slightly to clear Booking.com's duplicate detection — send the property's true position on the listing and do not pre-adjust it yourself.
//
// The listing's name, check-in/check-out times, currency, capacity and price come from the listing. Its postal code is taken from the listing's own `postalCode`; when the listing has none, it falls back to a connected Airbnb listing. A listing with neither is created without a postal code, so set `postalCode` on the listing first.
//
// ## Guards
//
// Every action that takes a `property_id` requires a property connected to this workspace; any other id returns `404 not_found`. Every action that takes a `listing_id` requires a listing in this workspace; any other id returns `404 not_found`.
//
// `create-property` refuses a listing with no coordinates (`422 missing_coordinates`) before anything is created — creating a Booking.com property cannot be undone.
//
// `create-property` is subject to the same published-listing gate as the dashboard: no plan, or the plan's listing limit reached, returns `403 billing_error` with `used` and `limit`, and nothing is created.
//
// Returns `403 listing_inactive` when the listing — or any listing mapped to the Booking.com property — is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// If a property is created and a later step fails, the response is `422 booking_create_partial` carrying `property_id`. The property EXISTS. Do not retry `create-property`, which would open a second one — continue with `add-room` and `advance`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/booking/setup (the `BookingSetup` operationId).
func (c *Client) BookingSetupWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBookingSetupRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BookingSetup Booking.com property setup actions
//
// Action-router for putting a property onto Booking.com — including building one from nothing. Select the step with `action`.
//
// ## Opening a property
//
// - `create-property` — create a NEW Booking.com property for a Repull listing (`listing_id`). Creates the property, its first room with the listing's beds, a rate plan and the room-rate product that makes the room sellable (under the listing's cancellation policy), sets the contact and invoice details and the facilities, seeds availability and rates, syncs the calendar, then runs Booking.com's readiness check and reports what still blocks opening in `warnings`. Send `contact` (`name`, `email`, `phone` in international form); without it the workspace owner is used, and a workspace with no usable contact is refused before anything is created. Returns 201.
// - `add-room` — add another room type (and its sellable product) to a property (`listing_id`, `property_id`). Returns 201.
// - `add-unit` — raise the number of identical units on an existing room (`listing_id`, `property_id`, `room_id`).
// - `advance` — run Booking.com's readiness check for a property (`property_id`) and, when it passes, open it. Returns `checked`, `opened`, `sellable` and `blockers` — Booking.com's own reasons it cannot open yet.
//
// ## Account and policy steps
//
// - `create-legal-entity` — register a legal entity directly (returns 201). Not normally needed: see the legal-entity rules below.
// - `check-legal-status` — always `404`. A legal entity's details are readable for any id on the connectivity-provider credentials every workspace shares, and nothing records which workspace registered which entity, so no entity can be shown to be yours. `create-property` resolves it for you.
// - `check-readiness` — whether a property is ready to open (`property_id`): `ready` and `blockers`, without trying to open it.
// - `open-property` — open the property for sale (`property_id`). Refused with `422 booking_rejected` naming the blockers when it is not ready.
// - `set-contacts` — set property contacts (`property_id`, `contacts` in Booking.com's Contacts API shape; at most one carries the `general` profile).
// - `set-policies` — add a cancellation policy (`property_id`, `policyCode`, optional `prepaymentRequired`). House rules, pets, children and the damage deposit are `POST /v1/channels/booking/content` with `type: "settings"`.
//
// ## Three things about Booking.com that cost real money
//
// **A newly created property is NOT sellable.** Booking.com opens it only when its readiness check passes, and the check names what is missing — a main photo still processing, no availability, a licence the region requires. The response always reports `status: "being_built"` and `sellable: false`, never a guess, with the reasons in `warnings`. Resolve them, then `advance`.
//
// **A room with no ACTIVE rate plan is invisible.** Booking only renders rooms that have at least one active product linkage (room × rate plan). A room can be created successfully, return a `roomId`, and never appear on the property page. If `rateId` comes back `null` from `create-property` or `add-room`, that is exactly what happened: activate a rate plan on the property in the Extranet, then add the room again.
//
// **Room names are Booking.com's.** Travellers see one of Booking.com's standard names ("Two-Bedroom Apartment"), chosen from the listing's bedrooms. The listing's own name is kept as the operator-side reference, never shown to guests.
//
// ## The legal entity is resolved, not asked for
//
// A property is created against the legal entity Booking.com contracts with, invoices and pays. You do not normally send one:
//
// 1. If this workspace already creates properties under a legal entity, that one is reused. A second is never registered.
// 2. If it has none and the request carries `legal_entity` (`company_name`, `legal_contact_name`, `legal_contact_email`), one is registered and used. Booking.com emails the legal contact a contract; creation only succeeds once it is signed.
// 3. If it has none and no `legal_entity`, the request is refused with `422 legal_entity_required` naming the fields — a contracted company is never invented.
//
// `legal_entity_id` overrides all of that. An id that already carries another workspace's properties is refused with `403 legal_entity_not_yours` before anything is created.
//
// ## What you do not control
//
// Properties are created against Booking's **production** target only. A test-target property cannot be sold through and there is no route back from one, so `target` is not a parameter — sending it changes nothing.
//
// The property category comes from the listing's property type (Apartment when it has none; Holiday home, Villa or Chalet when it says so). The initial room count is 1. Latitude and longitude come from the listing and are adjusted slightly to clear Booking.com's duplicate detection — send the property's true position on the listing and do not pre-adjust it yourself.
//
// The listing's name, check-in/check-out times, currency, capacity and price come from the listing. Its postal code is taken from the listing's own `postalCode`; when the listing has none, it falls back to a connected Airbnb listing. A listing with neither is created without a postal code, so set `postalCode` on the listing first.
//
// ## Guards
//
// Every action that takes a `property_id` requires a property connected to this workspace; any other id returns `404 not_found`. Every action that takes a `listing_id` requires a listing in this workspace; any other id returns `404 not_found`.
//
// `create-property` refuses a listing with no coordinates (`422 missing_coordinates`) before anything is created — creating a Booking.com property cannot be undone.
//
// `create-property` is subject to the same published-listing gate as the dashboard: no plan, or the plan's listing limit reached, returns `403 billing_error` with `used` and `limit`, and nothing is created.
//
// Returns `403 listing_inactive` when the listing — or any listing mapped to the Booking.com property — is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// If a property is created and a later step fails, the response is `422 booking_create_partial` carrying `property_id`. The property EXISTS. Do not retry `create-property`, which would open a second one — continue with `add-room` and `advance`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/booking/setup (the `BookingSetup` operationId).
func (c *Client) BookingSetup(ctx context.Context, body BookingSetupJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBookingSetupRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteBookingWebhook Unsubscribe from a Booking.com notification
//
// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
//
// Corresponds with DELETE /v1/channels/booking/webhooks (the `DeleteBookingWebhook` operationId).
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *Client) DeleteBookingWebhook(ctx context.Context, params *DeleteBookingWebhookParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteBookingWebhookRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListBookingWebhooks List Booking.com webhook subscriptions
//
// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
//
// Corresponds with GET /v1/channels/booking/webhooks (the `ListBookingWebhooks` operationId).
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *Client) ListBookingWebhooks(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListBookingWebhooksRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateBookingWebhookWithBody Subscribe to a Booking.com notification
//
// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/channels/booking/webhooks (the `CreateBookingWebhook` operationId).
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *Client) CreateBookingWebhookWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateBookingWebhookRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateBookingWebhook Subscribe to a Booking.com notification
//
// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/channels/booking/webhooks (the `CreateBookingWebhook` operationId).
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *Client) CreateBookingWebhook(ctx context.Context, body CreateBookingWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateBookingWebhookRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetPlumguideAvailability Get Plumguide availability
//
// Read the per-day availability calendar for a Plumguide listing. Returns the same row shape as Airbnb availability for SDK convenience.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/plumguide/availability (the `GetPlumguideAvailability` operationId).
func (c *Client) GetPlumguideAvailability(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetPlumguideAvailabilityRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdatePlumguideAvailability Push availability to Plumguide
//
// Push per-day availability changes to Plumguide. Plumguide accepts only the next 24 months — dates beyond that are silently ignored.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with PUT /v1/channels/plumguide/availability (the `UpdatePlumguideAvailability` operationId).
func (c *Client) UpdatePlumguideAvailability(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdatePlumguideAvailabilityRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListPlumguideBookings List Plumguide bookings
//
// List Plumguide bookings. Default returns all bookings; pass `listing_id` to filter to one listing, or `booking_code` to fetch a single booking.
//
// Returns `403 listing_inactive` when `listing_id` or `booking_code` resolves to an inactive listing. The unfiltered list is read straight from Plum Guide and is not filtered by listing status.
//
// Corresponds with GET /v1/channels/plumguide/bookings (the `ListPlumguideBookings` operationId).
func (c *Client) ListPlumguideBookings(ctx context.Context, params *ListPlumguideBookingsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListPlumguideBookingsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListPlumguideListings List Plumguide listings
//
// List Plumguide listings this workspace has access to. Plumguide is approval-based — listings appear once Plumguide has accepted them.
//
// Corresponds with GET /v1/channels/plumguide/listings (the `ListPlumguideListings` operationId).
func (c *Client) ListPlumguideListings(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListPlumguideListingsRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetPlumguidePricing Get Plumguide pricing
//
// Read the current pricing for a Plumguide listing (base price, currency, weekend uplift).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/channels/plumguide/pricing (the `GetPlumguidePricing` operationId).
func (c *Client) GetPlumguidePricing(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetPlumguidePricingRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdatePlumguidePricing Push pricing to Plumguide
//
// Push pricing changes to Plumguide. Plumguide rounds all prices to whole units of the listing currency — sub-unit precision is silently truncated.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with PUT /v1/channels/plumguide/pricing (the `UpdatePlumguidePricing` operationId).
func (c *Client) UpdatePlumguidePricing(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdatePlumguidePricingRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeletePlumguideWebhooks Remove Plumguide webhook config
//
// Delete the Plumguide webhook configuration for this workspace. Plumguide stops delivering webhooks until a new config is set.
//
// Corresponds with DELETE /v1/channels/plumguide/webhooks (the `DeletePlumguideWebhooks` operationId).
func (c *Client) DeletePlumguideWebhooks(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeletePlumguideWebhooksRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetPlumguideWebhooks Get Plumguide webhook config
//
// Read the current Plumguide webhook configuration for this workspace.
//
// Corresponds with GET /v1/channels/plumguide/webhooks (the `GetPlumguideWebhooks` operationId).
func (c *Client) GetPlumguideWebhooks(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetPlumguideWebhooksRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdatePlumguideWebhooksWithBody Replace Plumguide webhook config
//
// Replace the Plumguide webhook configuration. The body carries the full webhook config payload — this is a full replacement, not a patch.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/channels/plumguide/webhooks (the `UpdatePlumguideWebhooks` operationId).
func (c *Client) UpdatePlumguideWebhooksWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdatePlumguideWebhooksRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdatePlumguideWebhooks Replace Plumguide webhook config
//
// Replace the Plumguide webhook configuration. The body carries the full webhook config payload — this is a full replacement, not a patch.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/channels/plumguide/webhooks (the `UpdatePlumguideWebhooks` operationId).
func (c *Client) UpdatePlumguideWebhooks(ctx context.Context, body UpdatePlumguideWebhooksJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdatePlumguideWebhooksRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListVrboListings List VRBO listings
//
// List VRBO listings this workspace owns. VRBO is agency-model — Repull reads listings via the public iCal/HTTP feeds.
//
// Inactive listings are left out; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
//
// Corresponds with GET /v1/channels/vrbo/listings (the `ListVrboListings` operationId).
func (c *Client) ListVrboListings(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListVrboListingsRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListVrboReservations List VRBO reservations
//
// Cursor-paginated list of VRBO reservations sourced from the public booking feed. Lag is typically 5-10 minutes vs. Airbnb / Booking.com. `?offset=` is accepted as a first-class alias for `?cursor=` (mutually exclusive; offset capped at 10000).
//
// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated.
//
// Corresponds with GET /v1/channels/vrbo/reservations (the `ListVrboReservations` operationId).
func (c *Client) ListVrboReservations(ctx context.Context, params *ListVrboReservationsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListVrboReservationsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListConnections List PMS/OTA connections
//
// Returns all active connections to PMS and OTA platforms.
//
// Corresponds with GET /v1/connect (the `ListConnections` operationId).
func (c *Client) ListConnections(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListConnectionsRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateConnectSessionWithBody Create a multi-channel Connect picker session
//
// Mints a session that lands the user on the channel picker at `connect.repull.dev/{sessionId}` instead of jumping straight to a single provider. The user picks a channel from the registry, the picker page POSTs `selectConnectProvider` to bind the choice, and the per-provider flow takes over.
//
// Use this when you want one entry point for all 13 channels. Use `POST /v1/connect/{provider}` instead when your UI already knows which channel to connect.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect (the `CreateConnectSession` operationId).
func (c *Client) CreateConnectSessionWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateConnectSessionRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateConnectSession Create a multi-channel Connect picker session
//
// Mints a session that lands the user on the channel picker at `connect.repull.dev/{sessionId}` instead of jumping straight to a single provider. The user picks a channel from the registry, the picker page POSTs `selectConnectProvider` to bind the choice, and the per-provider flow takes over.
//
// Use this when you want one entry point for all 13 channels. Use `POST /v1/connect/{provider}` instead when your UI already knows which channel to connect.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect (the `CreateConnectSession` operationId).
func (c *Client) CreateConnectSession(ctx context.Context, body CreateConnectSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateConnectSessionRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitBeds24CredentialsWithBody Submit Beds24 credentials for a Connect session
//
// Completes a credentials-pattern connection for Beds24. API key + prop key from Beds24 → Settings → Apps & Integrations.
//
// The credentials are validated against Beds24 before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/beds24/credentials (the `SubmitBeds24Credentials` operationId).
func (c *Client) SubmitBeds24CredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitBeds24CredentialsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitBeds24Credentials Submit Beds24 credentials for a Connect session
//
// Completes a credentials-pattern connection for Beds24. API key + prop key from Beds24 → Settings → Apps & Integrations.
//
// The credentials are validated against Beds24 before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/beds24/credentials (the `SubmitBeds24Credentials` operationId).
func (c *Client) SubmitBeds24Credentials(ctx context.Context, body SubmitBeds24CredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitBeds24CredentialsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BookingConnectCallbackWithBody Booking.com connectivity callback
//
// Receives Booking.com's asynchronous confirmation that a property has designated Repull as its connectivity provider, and advances the Connect session. Called by Booking.com, not by integrators.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/booking/callback (the `BookingConnectCallback` operationId).
func (c *Client) BookingConnectCallbackWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBookingConnectCallbackRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BookingConnectCallback Booking.com connectivity callback
//
// Receives Booking.com's asynchronous confirmation that a property has designated Repull as its connectivity provider, and advances the Connect session. Called by Booking.com, not by integrators.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/booking/callback (the `BookingConnectCallback` operationId).
func (c *Client) BookingConnectCallback(ctx context.Context, body BookingConnectCallbackJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBookingConnectCallbackRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// MapConnectBookingRoomsWithBody Submit room→listing mappings for a Booking.com Connect session
//
// Submits the customer's room→listing mapping choices in one transaction. For each mapping, updates `listings_booking_rooms.listing_id` and replaces the corresponding `listing_platform_links` row. Pass `listingId: null` to explicitly unmap a room.
//
// On success the Connect session is marked `completed` and the hosted picker page emits a `repull:connect:completed` postMessage to the embedding window.
//
// No API key required — the `sessionId` in the body is the capability token. Each mapping's `roomId` must belong to the customer's claimed hotel; mismatched IDs are rejected with 403.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/booking/map-rooms (the `MapConnectBookingRooms` operationId).
func (c *Client) MapConnectBookingRoomsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewMapConnectBookingRoomsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// MapConnectBookingRooms Submit room→listing mappings for a Booking.com Connect session
//
// Submits the customer's room→listing mapping choices in one transaction. For each mapping, updates `listings_booking_rooms.listing_id` and replaces the corresponding `listing_platform_links` row. Pass `listingId: null` to explicitly unmap a room.
//
// On success the Connect session is marked `completed` and the hosted picker page emits a `repull:connect:completed` postMessage to the embedding window.
//
// No API key required — the `sessionId` in the body is the capability token. Each mapping's `roomId` must belong to the customer's claimed hotel; mismatched IDs are rejected with 403.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/booking/map-rooms (the `MapConnectBookingRooms` operationId).
func (c *Client) MapConnectBookingRooms(ctx context.Context, body MapConnectBookingRoomsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewMapConnectBookingRoomsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListConnectBookingRooms List Booking.com rooms imported for a Connect session
//
// Returns the rooms imported from the Booking.com hotel claimed in this Connect session, plus the customer's listing options for the mapping dropdowns. Hosted-picker pages poll this endpoint every ~2s after `verifyBookingHotel` succeeds; once rooms appear the page transitions to the mapping UI.
//
// No API key required — the `sessionId` query param is the capability token.
//
// Corresponds with GET /v1/connect/booking/rooms (the `ListConnectBookingRooms` operationId).
func (c *Client) ListConnectBookingRooms(ctx context.Context, params *ListConnectBookingRoomsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListConnectBookingRoomsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// VerifyBookingHotelWithBody Verify a Booking.com hotel ID for a Connect session
//
// Manual-paste fallback that closes the Booking.com claim flow. Call this after the customer completes Stage 1 designation in their Booking Extranet (ticking FantasticStay/Repull as their connectivity provider) and pastes their Hotel ID into the hosted picker.
//
// Validates the hotel against Booking's property API, persists the `pms_connections` row, kicks off the room import, and transitions the Connect session to `awaiting_room_mapping`.
//
// No API key required — the `sessionId` is the capability token. Sessions in any terminal state are rejected.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/booking/verify (the `VerifyBookingHotel` operationId).
func (c *Client) VerifyBookingHotelWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewVerifyBookingHotelRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// VerifyBookingHotel Verify a Booking.com hotel ID for a Connect session
//
// Manual-paste fallback that closes the Booking.com claim flow. Call this after the customer completes Stage 1 designation in their Booking Extranet (ticking FantasticStay/Repull as their connectivity provider) and pastes their Hotel ID into the hosted picker.
//
// Validates the hotel against Booking's property API, persists the `pms_connections` row, kicks off the room import, and transitions the Connect session to `awaiting_room_mapping`.
//
// No API key required — the `sessionId` is the capability token. Sessions in any terminal state are rejected.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/booking/verify (the `VerifyBookingHotel` operationId).
func (c *Client) VerifyBookingHotel(ctx context.Context, body VerifyBookingHotelJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewVerifyBookingHotelRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitBookingsyncCredentialsWithBody Submit BookingSync credentials for a Connect session
//
// Completes a credentials-pattern connection for BookingSync. OAuth client credentials issued by BookingSync.
//
// The credentials are validated against BookingSync before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/bookingsync/credentials (the `SubmitBookingsyncCredentials` operationId).
func (c *Client) SubmitBookingsyncCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitBookingsyncCredentialsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitBookingsyncCredentials Submit BookingSync credentials for a Connect session
//
// Completes a credentials-pattern connection for BookingSync. OAuth client credentials issued by BookingSync.
//
// The credentials are validated against BookingSync before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/bookingsync/credentials (the `SubmitBookingsyncCredentials` operationId).
func (c *Client) SubmitBookingsyncCredentials(ctx context.Context, body SubmitBookingsyncCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitBookingsyncCredentialsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitGuestyCredentialsWithBody Submit Guesty credentials for a Connect session
//
// Completes a credentials-pattern connection for Guesty. Client ID + secret from Guesty → Integrations → Open API.
//
// The credentials are validated against Guesty before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/guesty/credentials (the `SubmitGuestyCredentials` operationId).
func (c *Client) SubmitGuestyCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitGuestyCredentialsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitGuestyCredentials Submit Guesty credentials for a Connect session
//
// Completes a credentials-pattern connection for Guesty. Client ID + secret from Guesty → Integrations → Open API.
//
// The credentials are validated against Guesty before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/guesty/credentials (the `SubmitGuestyCredentials` operationId).
func (c *Client) SubmitGuestyCredentials(ctx context.Context, body SubmitGuestyCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitGuestyCredentialsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitHospitableCredentialsWithBody Submit Hospitable credentials for a Connect session
//
// Completes a credentials-pattern connection for Hospitable. Personal access token from Hospitable → Settings → API.
//
// The credentials are validated against Hospitable before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/hospitable/credentials (the `SubmitHospitableCredentials` operationId).
func (c *Client) SubmitHospitableCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitHospitableCredentialsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitHospitableCredentials Submit Hospitable credentials for a Connect session
//
// Completes a credentials-pattern connection for Hospitable. Personal access token from Hospitable → Settings → API.
//
// The credentials are validated against Hospitable before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/hospitable/credentials (the `SubmitHospitableCredentials` operationId).
func (c *Client) SubmitHospitableCredentials(ctx context.Context, body SubmitHospitableCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitHospitableCredentialsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitHostawayCredentialsWithBody Submit Hostaway credentials for a Connect session
//
// Completes a credentials-pattern connection for Hostaway. Account ID + API key from Hostaway → Settings → Public API.
//
// The credentials are validated against Hostaway before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/hostaway/credentials (the `SubmitHostawayCredentials` operationId).
func (c *Client) SubmitHostawayCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitHostawayCredentialsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitHostawayCredentials Submit Hostaway credentials for a Connect session
//
// Completes a credentials-pattern connection for Hostaway. Account ID + API key from Hostaway → Settings → Public API.
//
// The credentials are validated against Hostaway before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/hostaway/credentials (the `SubmitHostawayCredentials` operationId).
func (c *Client) SubmitHostawayCredentials(ctx context.Context, body SubmitHostawayCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitHostawayCredentialsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitIgmsCredentialsWithBody Submit iGMS credentials for a Connect session
//
// Completes a credentials-pattern connection for iGMS. API token from iGMS → Settings → Integrations.
//
// The credentials are validated against iGMS before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/igms/credentials (the `SubmitIgmsCredentials` operationId).
func (c *Client) SubmitIgmsCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitIgmsCredentialsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitIgmsCredentials Submit iGMS credentials for a Connect session
//
// Completes a credentials-pattern connection for iGMS. API token from iGMS → Settings → Integrations.
//
// The credentials are validated against iGMS before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/igms/credentials (the `SubmitIgmsCredentials` operationId).
func (c *Client) SubmitIgmsCredentials(ctx context.Context, body SubmitIgmsCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitIgmsCredentialsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitLodgifyCredentialsWithBody Submit Lodgify credentials for a Connect session
//
// Completes a credentials-pattern connection for Lodgify. API key from Lodgify → Settings → Public API.
//
// The credentials are validated against Lodgify before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/lodgify/credentials (the `SubmitLodgifyCredentials` operationId).
func (c *Client) SubmitLodgifyCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitLodgifyCredentialsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitLodgifyCredentials Submit Lodgify credentials for a Connect session
//
// Completes a credentials-pattern connection for Lodgify. API key from Lodgify → Settings → Public API.
//
// The credentials are validated against Lodgify before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/lodgify/credentials (the `SubmitLodgifyCredentials` operationId).
func (c *Client) SubmitLodgifyCredentials(ctx context.Context, body SubmitLodgifyCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitLodgifyCredentialsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitOwnerrezCredentialsWithBody Submit OwnerRez credentials for a Connect session
//
// Completes a credentials-pattern connection for OwnerRez. Username + API token from OwnerRez → Settings → API.
//
// The credentials are validated against OwnerRez before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/ownerrez/credentials (the `SubmitOwnerrezCredentials` operationId).
func (c *Client) SubmitOwnerrezCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitOwnerrezCredentialsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitOwnerrezCredentials Submit OwnerRez credentials for a Connect session
//
// Completes a credentials-pattern connection for OwnerRez. Username + API token from OwnerRez → Settings → API.
//
// The credentials are validated against OwnerRez before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/ownerrez/credentials (the `SubmitOwnerrezCredentials` operationId).
func (c *Client) SubmitOwnerrezCredentials(ctx context.Context, body SubmitOwnerrezCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitOwnerrezCredentialsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListConnectProviders List Connect channels
//
// Returns the public registry of every channel the picker supports. No customer-specific data — display metadata only. Cached for 5 minutes at the edge.
//
// Corresponds with GET /v1/connect/providers (the `ListConnectProviders` operationId).
func (c *Client) ListConnectProviders(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListConnectProvidersRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SelectConnectProviderWithBody Bind a picker session to a provider
//
// Called by the hosted picker page once the user clicks a channel card. Validates the provider exists and is permitted by the session's `allowedProviders` whitelist (if any), then returns the next-step URL the picker should navigate to.
//
// No API key required — the session ID is the capability token. The session must still be pending and unexpired.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/sessions/{sessionId}/select-provider (the `SelectConnectProvider` operationId).
func (c *Client) SelectConnectProviderWithBody(ctx context.Context, sessionId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSelectConnectProviderRequestWithBody(c.Server, sessionId, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SelectConnectProvider Bind a picker session to a provider
//
// Called by the hosted picker page once the user clicks a channel card. Validates the provider exists and is permitted by the session's `allowedProviders` whitelist (if any), then returns the next-step URL the picker should navigate to.
//
// No API key required — the session ID is the capability token. The session must still be pending and unexpired.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/sessions/{sessionId}/select-provider (the `SelectConnectProvider` operationId).
func (c *Client) SelectConnectProvider(ctx context.Context, sessionId string, body SelectConnectProviderJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSelectConnectProviderRequest(c.Server, sessionId, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitSmoobuCredentialsWithBody Submit Smoobu credentials for a Connect session
//
// Completes a credentials-pattern connection for Smoobu. API key from Smoobu → Settings → For developers.
//
// The credentials are validated against Smoobu before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/smoobu/credentials (the `SubmitSmoobuCredentials` operationId).
func (c *Client) SubmitSmoobuCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitSmoobuCredentialsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitSmoobuCredentials Submit Smoobu credentials for a Connect session
//
// Completes a credentials-pattern connection for Smoobu. API key from Smoobu → Settings → For developers.
//
// The credentials are validated against Smoobu before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/smoobu/credentials (the `SubmitSmoobuCredentials` operationId).
func (c *Client) SubmitSmoobuCredentials(ctx context.Context, body SubmitSmoobuCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitSmoobuCredentialsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitVrboCredentialsWithBody Submit Vrbo credentials for a Connect session
//
// Completes a credentials-pattern connection for Vrbo. Activation handshake — Repull mints the Basic-Auth pair the host pastes into Vrbo Partner Central.
//
// The credentials are validated against Vrbo before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/vrbo/credentials (the `SubmitVrboCredentials` operationId).
func (c *Client) SubmitVrboCredentialsWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitVrboCredentialsRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SubmitVrboCredentials Submit Vrbo credentials for a Connect session
//
// Completes a credentials-pattern connection for Vrbo. Activation handshake — Repull mints the Basic-Auth pair the host pastes into Vrbo Partner Central.
//
// The credentials are validated against Vrbo before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/vrbo/credentials (the `SubmitVrboCredentials` operationId).
func (c *Client) SubmitVrboCredentials(ctx context.Context, body SubmitVrboCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSubmitVrboCredentialsRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteConnection Disconnect provider
//
// Disconnect ONE connected account of a provider from this workspace. Supported for `airbnb` and `booking`.
//
// **Which account.** Pass `accountId` — for Airbnb the host id (`accounts[].externalAccountId` from `GET /v1/connect/airbnb`), for Booking.com the hotel id. It is optional only when the workspace has exactly one account for the provider. With several and no `accountId`, the call returns `422` with the account ids in `valid_values` instead of guessing. An `accountId` that is not connected to this workspace returns `404`. Disconnecting one account leaves the others connected.
//
// **What happens.** The account's stored authorization is removed and it stops syncing. Its listings are **deactivated**, not deleted: they stop counting toward your plan's listing limit, their data is kept, and they are returned in `listingsDeactivated`. A listing that is still connected through another account or channel stays active. Reconnect the account, then activate the listings with `POST /v1/listings/status`.
//
// The change is all or nothing. For Airbnb, the host can also revoke access on Airbnb's side (Account → Privacy & sharing → Connected apps); that alone does not update this workspace, so call this endpoint as well.
//
// Other providers return `501 not_implemented` with instructions for disconnecting on the provider's side. That answer depends only on the provider, not on your workspace: an unsupported provider returns `501` whether or not you have a connection to it. `404 not_found` on a supported provider means this workspace has no connection to it (or, with `accountId`, that the account is not connected here).
//
// Corresponds with DELETE /v1/connect/{provider} (the `DeleteConnection` operationId).
func (c *Client) DeleteConnection(ctx context.Context, provider Provider, params *DeleteConnectionParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteConnectionRequest(c.Server, provider, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetConnectStatus Get connection status
//
// Returns the current connection status for a provider, including host metadata (display name + avatar) for Airbnb so clients can render an account-level confirmation UI.
//
// Corresponds with GET /v1/connect/{provider} (the `GetConnectStatus` operationId).
func (c *Client) GetConnectStatus(ctx context.Context, provider Provider, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetConnectStatusRequest(c.Server, provider)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateConnectionWithBody Connect to PMS/OTA provider
//
// Establish a connection to a PMS or OTA platform. Credentials vary by provider — see docs for each provider.
//
// Airbnb-specific: pass `redirectUrl` (where to send the user after consent) and optionally `accessType`. Three tiers: `read_only` grants read-only scopes; `messaging` grants read scopes plus message read/send but NOT property management, so it can coexist with another app (e.g. an existing PMS) that already holds property management on the same Airbnb account; `full_access` — the default — grants full host scopes including the exclusive property management (only one app per Airbnb account can hold it). The response returns a hosted `url` to redirect the user to.
//
// Booking.com: pass `redirectUrl` (no `accessType`). The response returns a hosted `url` — send the user there to designate FantasticStay in their Booking.com Extranet and paste their Hotel ID. Same response shape as Airbnb (`url`, `sessionId`, `expiresAt`).
//
// PMS providers (api-key based) pass `apiKey` instead; Plumguide passes `clientId`/`clientSecret`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/connect/{provider} (the `CreateConnection` operationId).
func (c *Client) CreateConnectionWithBody(ctx context.Context, provider Provider, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateConnectionRequestWithBody(c.Server, provider, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateConnection Connect to PMS/OTA provider
//
// Establish a connection to a PMS or OTA platform. Credentials vary by provider — see docs for each provider.
//
// Airbnb-specific: pass `redirectUrl` (where to send the user after consent) and optionally `accessType`. Three tiers: `read_only` grants read-only scopes; `messaging` grants read scopes plus message read/send but NOT property management, so it can coexist with another app (e.g. an existing PMS) that already holds property management on the same Airbnb account; `full_access` — the default — grants full host scopes including the exclusive property management (only one app per Airbnb account can hold it). The response returns a hosted `url` to redirect the user to.
//
// Booking.com: pass `redirectUrl` (no `accessType`). The response returns a hosted `url` — send the user there to designate FantasticStay in their Booking.com Extranet and paste their Hotel ID. Same response shape as Airbnb (`url`, `sessionId`, `expiresAt`).
//
// PMS providers (api-key based) pass `apiKey` instead; Plumguide passes `clientId`/`clientSecret`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/connect/{provider} (the `CreateConnection` operationId).
func (c *Client) CreateConnection(ctx context.Context, provider Provider, body CreateConnectionJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateConnectionRequest(c.Server, provider, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListConversations List conversations
//
// Cursor-paginated list of message threads owned by the workspace. Backed by main vanio's `/api/threads/list` which keyset-paginates against `(last_message_at, id)` for constant per-page cost. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// Filters: `platform` (`airbnb`|`booking`|`vrbo`|`website`|`email`), `status` (`open`|`archived` — `archived` is a stable no-op until the bit lands on `message_threads`).
//
// **Inactive listings:** conversations that belong to an inactive listing (by the thread's listing or its reservation's listing) are left out of the page and of `pagination.total`. Inactive listings keep syncing; activate the listing to use it here.
//
// Corresponds with GET /v1/conversations (the `ListConversations` operationId).
func (c *Client) ListConversations(ctx context.Context, params *ListConversationsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListConversationsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetConversation Get conversation detail
//
// Returns one thread (the same shape as the list-row `Conversation`) plus expanded `host` (from `airbnb_hosts` for the thread's `host_id`) and `guest` (resolved via the thread's `reservation_id`, with up to 50 contacts) blocks.
//
// A conversation that belongs to an inactive listing (by the thread's listing or its reservation's listing) returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
//
// Corresponds with GET /v1/conversations/{id} (the `GetConversation` operationId).
func (c *Client) GetConversation(ctx context.Context, id int, params *GetConversationParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetConversationRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListConversationMessages List messages in a conversation
//
// Cursor-paginated messages within one thread. Defaults to newest-first (`?order=desc`); pass `?order=asc` for chronological replay. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// A conversation that belongs to an inactive listing returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
//
// Corresponds with GET /v1/conversations/{id}/messages (the `ListConversationMessages` operationId).
func (c *Client) ListConversationMessages(ctx context.Context, id int, params *ListConversationMessagesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListConversationMessagesRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SendConversationMessageWithBody Send a message to the guest
//
// Sends a message to the guest on this conversation and records it in the thread.
//
// Omit `channel` and the message goes out on whichever channel the conversation already uses (Airbnb, Booking.com, SMS, email or the direct-booking site) — that is the right default. Pass `channel` only to force a specific one.
//
// The message is attributed to the API: it is recorded with `aiGenerated` false so an API send is never counted as an automated reply.
//
// ### Airbnb rewrites links — check `contentRewritten`
//
// Airbnb rejects guest messages containing a link, an email address or a phone number, and names the offending text. When that happens the offending fragment is stripped and the remainder is re-sent once, which means **the guest receives a message that is not the one you wrote**. Reporting that as a plain success would be a lie, so every response carries `contentRewritten`; when it is `true`, `deliveredContent` is the text that actually reached the guest. Check it before assuming your message went out verbatim.
//
// When the text cannot be salvaged (the link is most of the message) nothing is delivered and the call returns `422 message_not_sent` with the channel's verbatim refusal in `statusReason`.
//
// Send `Idempotency-Key` — without it, retrying after a network timeout sends the guest the same message twice.
//
// ### Attachments
//
// Send files with `attachments: [{ url, contentType?, filename? }]` — public `https://` URLs, up to 5 per request, 10 MB each. `message` may be omitted when there are attachments (except on Booking.com). Repull downloads each file, reads its real type from the bytes, keeps a durable copy and delivers it through the channel's own file flow. **Every file is checked before anything is sent**: if one is unreachable, too large or of a type the channel refuses, the call returns 422 naming the file (`index`) and the guest receives nothing.
//
// | Channel | Accepted types | Text | How it arrives |
// |---|---|---|---|
// | Airbnb | JPEG, PNG, GIF, WebP (converted to JPEG), MP4, QuickTime | optional | each file as its own message, then the text as a separate message |
// | Booking.com | JPEG, PNG | **required** | one message carrying the text and every file |
// | SMS, email, direct-booking site chat | — | — | `422 attachments_not_supported`, nothing sent |
//
// Airbnb does not allow files in pre-booking (inquiry) conversations; that refusal comes back as `422 message_not_sent`. Because Airbnb delivers files one message at a time, a later file can be refused after earlier ones arrived — that returns `422 message_partially_sent` with `parts` saying exactly which messages reached the guest; resend only the rest.
//
// The response's `attachments` lists each file's durable `url`, and `parts` lists every channel message the send produced. Read-back (`GET /v1/conversations/{id}/messages`) shows the same files in each message's `attachments`.
//
// **Inactive listings:** a conversation that belongs to an inactive listing returns `403 listing_inactive` and no message is sent. Activate the listing first.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/conversations/{id}/messages (the `SendConversationMessage` operationId).
func (c *Client) SendConversationMessageWithBody(ctx context.Context, id int, params *SendConversationMessageParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSendConversationMessageRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SendConversationMessage Send a message to the guest
//
// Sends a message to the guest on this conversation and records it in the thread.
//
// Omit `channel` and the message goes out on whichever channel the conversation already uses (Airbnb, Booking.com, SMS, email or the direct-booking site) — that is the right default. Pass `channel` only to force a specific one.
//
// The message is attributed to the API: it is recorded with `aiGenerated` false so an API send is never counted as an automated reply.
//
// ### Airbnb rewrites links — check `contentRewritten`
//
// Airbnb rejects guest messages containing a link, an email address or a phone number, and names the offending text. When that happens the offending fragment is stripped and the remainder is re-sent once, which means **the guest receives a message that is not the one you wrote**. Reporting that as a plain success would be a lie, so every response carries `contentRewritten`; when it is `true`, `deliveredContent` is the text that actually reached the guest. Check it before assuming your message went out verbatim.
//
// When the text cannot be salvaged (the link is most of the message) nothing is delivered and the call returns `422 message_not_sent` with the channel's verbatim refusal in `statusReason`.
//
// Send `Idempotency-Key` — without it, retrying after a network timeout sends the guest the same message twice.
//
// ### Attachments
//
// Send files with `attachments: [{ url, contentType?, filename? }]` — public `https://` URLs, up to 5 per request, 10 MB each. `message` may be omitted when there are attachments (except on Booking.com). Repull downloads each file, reads its real type from the bytes, keeps a durable copy and delivers it through the channel's own file flow. **Every file is checked before anything is sent**: if one is unreachable, too large or of a type the channel refuses, the call returns 422 naming the file (`index`) and the guest receives nothing.
//
// | Channel | Accepted types | Text | How it arrives |
// |---|---|---|---|
// | Airbnb | JPEG, PNG, GIF, WebP (converted to JPEG), MP4, QuickTime | optional | each file as its own message, then the text as a separate message |
// | Booking.com | JPEG, PNG | **required** | one message carrying the text and every file |
// | SMS, email, direct-booking site chat | — | — | `422 attachments_not_supported`, nothing sent |
//
// Airbnb does not allow files in pre-booking (inquiry) conversations; that refusal comes back as `422 message_not_sent`. Because Airbnb delivers files one message at a time, a later file can be refused after earlier ones arrived — that returns `422 message_partially_sent` with `parts` saying exactly which messages reached the guest; resend only the rest.
//
// The response's `attachments` lists each file's durable `url`, and `parts` lists every channel message the send produced. Read-back (`GET /v1/conversations/{id}/messages`) shows the same files in each message's `attachments`.
//
// **Inactive listings:** a conversation that belongs to an inactive listing returns `403 listing_inactive` and no message is sent. Activate the listing first.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/conversations/{id}/messages (the `SendConversationMessage` operationId).
func (c *Client) SendConversationMessage(ctx context.Context, id int, params *SendConversationMessageParams, body SendConversationMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSendConversationMessageRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PreapproveConversationWithBody Pre-approve an inquiry
//
// Pre-approve the Airbnb inquiry on this conversation: the guest who asked about dates may now book them at the listed price, without waiting on you. To change the dates, guests or price, send a special offer instead (`POST /v1/conversations/{id}/special-offers`).
//
// Find inquiries that need an answer with `GET /v1/inquiries` (default `status=open`); each carries the `conversationId` to use here.
//
// **Airbnb only**, and only for listings connected to Airbnb directly. A Booking.com, VRBO or direct-booking conversation, or an Airbnb one relayed through a PMS (Hostaway, Guesty), returns `422 channel_not_supported` and nothing is sent.
//
// The inquiry is marked `pre_approved` everywhere, the same as pre-approving in Airbnb.
//
// An Airbnb refusal is never reported as a success: an inquiry that already moved on is `409 inquiry_no_longer_open`, an expired one `409 inquiry_expired`, a conversation that already has a booking `409 conversation_already_booked`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/conversations/{id}/pre-approval (the `PreapproveConversation` operationId).
func (c *Client) PreapproveConversationWithBody(ctx context.Context, id int, params *PreapproveConversationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPreapproveConversationRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PreapproveConversation Pre-approve an inquiry
//
// Pre-approve the Airbnb inquiry on this conversation: the guest who asked about dates may now book them at the listed price, without waiting on you. To change the dates, guests or price, send a special offer instead (`POST /v1/conversations/{id}/special-offers`).
//
// Find inquiries that need an answer with `GET /v1/inquiries` (default `status=open`); each carries the `conversationId` to use here.
//
// **Airbnb only**, and only for listings connected to Airbnb directly. A Booking.com, VRBO or direct-booking conversation, or an Airbnb one relayed through a PMS (Hostaway, Guesty), returns `422 channel_not_supported` and nothing is sent.
//
// The inquiry is marked `pre_approved` everywhere, the same as pre-approving in Airbnb.
//
// An Airbnb refusal is never reported as a success: an inquiry that already moved on is `409 inquiry_no_longer_open`, an expired one `409 inquiry_expired`, a conversation that already has a booking `409 conversation_already_booked`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/conversations/{id}/pre-approval (the `PreapproveConversation` operationId).
func (c *Client) PreapproveConversation(ctx context.Context, id int, params *PreapproveConversationParams, body PreapproveConversationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPreapproveConversationRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateConversationSpecialOfferWithBody Send a special offer
//
// Send the guest on this conversation an Airbnb special offer: your own dates, guest count and total price. The guest has 24 hours to book it. Use it to answer an inquiry with different terms, or to make a returning guest a custom price. To accept the guest’s own dates and price as they asked, pre-approve instead (`POST /v1/conversations/{id}/pre-approval`).
//
// `listingId` is optional: omit it to offer the listing the guest asked about. It is a **Repull** listing id; Repull sends Airbnb its own listing id, using the link that belongs to this conversation’s Airbnb account.
//
// `totalPrice` is the whole stay, in the listing’s Airbnb currency — Airbnb does not take a currency on an offer.
//
// **Airbnb only**, and only for listings connected to Airbnb directly; anything else is `422 channel_not_supported` and nothing is sent. The inquiry is marked `special_offer_sent`.
//
// An offer Airbnb refuses is never a `201`: dates that are taken, a price below Airbnb’s minimum, too many guests and the like are `422 airbnb_rejected` with Airbnb’s own reason in `message`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again. Without it, a retry after a timeout can send the guest two offers.
//
// Read or withdraw the offer with `GET` / `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/conversations/{id}/special-offers (the `CreateConversationSpecialOffer` operationId).
func (c *Client) CreateConversationSpecialOfferWithBody(ctx context.Context, id int, params *CreateConversationSpecialOfferParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateConversationSpecialOfferRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateConversationSpecialOffer Send a special offer
//
// Send the guest on this conversation an Airbnb special offer: your own dates, guest count and total price. The guest has 24 hours to book it. Use it to answer an inquiry with different terms, or to make a returning guest a custom price. To accept the guest’s own dates and price as they asked, pre-approve instead (`POST /v1/conversations/{id}/pre-approval`).
//
// `listingId` is optional: omit it to offer the listing the guest asked about. It is a **Repull** listing id; Repull sends Airbnb its own listing id, using the link that belongs to this conversation’s Airbnb account.
//
// `totalPrice` is the whole stay, in the listing’s Airbnb currency — Airbnb does not take a currency on an offer.
//
// **Airbnb only**, and only for listings connected to Airbnb directly; anything else is `422 channel_not_supported` and nothing is sent. The inquiry is marked `special_offer_sent`.
//
// An offer Airbnb refuses is never a `201`: dates that are taken, a price below Airbnb’s minimum, too many guests and the like are `422 airbnb_rejected` with Airbnb’s own reason in `message`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again. Without it, a retry after a timeout can send the guest two offers.
//
// Read or withdraw the offer with `GET` / `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/conversations/{id}/special-offers (the `CreateConversationSpecialOffer` operationId).
func (c *Client) CreateConversationSpecialOffer(ctx context.Context, id int, params *CreateConversationSpecialOfferParams, body CreateConversationSpecialOfferJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateConversationSpecialOfferRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// WithdrawConversationSpecialOffer Withdraw a special offer
//
// Withdraw a special offer the guest has not booked yet, so it can no longer be booked. An offer the guest already booked cannot be withdrawn — Airbnb refuses with `409 inquiry_no_longer_open`; cancel the booking instead.
//
// Corresponds with DELETE /v1/conversations/{id}/special-offers/{offerId} (the `WithdrawConversationSpecialOffer` operationId).
func (c *Client) WithdrawConversationSpecialOffer(ctx context.Context, id int, offerId string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewWithdrawConversationSpecialOfferRequest(c.Server, id, offerId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetConversationSpecialOffer Get a special offer
//
// Read a special offer on this conversation back from Airbnb — typically to check its `status` (`active` until the guest books it, it expires, or you withdraw it). Read live from Airbnb with the conversation’s own Airbnb account.
//
// Corresponds with GET /v1/conversations/{id}/special-offers/{offerId} (the `GetConversationSpecialOffer` operationId).
func (c *Client) GetConversationSpecialOffer(ctx context.Context, id int, offerId string, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetConversationSpecialOfferRequest(c.Server, id, offerId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListGuests List guests
//
// Cursor-paginated list of guests in the workspace. Walks `guests.id ASC` keyset for constant per-page cost regardless of how many guests the customer has. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// Filters: `q` (substring on name/email/phone), `has_reservation` (`true`|`false`), `listing_id` (restrict to guests with at least one reservation on that listing).
//
// **Inactive listings:** a guest whose every reservation is on an inactive listing is left out of the page and of `pagination.total`, and `totalReservations`, `totalRevenue`, `firstStayedAt` / `lastStayedAt`, `has_reservation` and `listing_id` only consider reservations that are not on an inactive listing. Guests with no reservations are tied to no listing and are always listed. Filtering by an inactive `listing_id` returns `403 listing_inactive`.
//
// Corresponds with GET /v1/guests (the `ListGuests` operationId).
func (c *Client) ListGuests(ctx context.Context, params *ListGuestsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListGuestsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateGuestWithBody Create a guest
//
// Creates a guest in the workspace, with contact normalisation applied (phone digits, email lowercased) and each contact stored as its own record.
//
// **This is find-or-create, and the response tells you which happened.** A guest already on file matching on email — then phone — AND name is returned instead of a duplicate being created. Read the `created` flag rather than inferring from the status: `201` with `created: true` means a new record was written, `200` with `created: false` means an existing guest matched. Quietly handing back an existing record as though it were new is exactly the ambiguity this flag removes.
//
// Field names are camelCase, and an unrecognised field is rejected by name rather than silently dropped.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/guests (the `CreateGuest` operationId).
func (c *Client) CreateGuestWithBody(ctx context.Context, params *CreateGuestParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateGuestRequestWithBody(c.Server, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateGuest Create a guest
//
// Creates a guest in the workspace, with contact normalisation applied (phone digits, email lowercased) and each contact stored as its own record.
//
// **This is find-or-create, and the response tells you which happened.** A guest already on file matching on email — then phone — AND name is returned instead of a duplicate being created. Read the `created` flag rather than inferring from the status: `201` with `created: true` means a new record was written, `200` with `created: false` means an existing guest matched. Quietly handing back an existing record as though it were new is exactly the ambiguity this flag removes.
//
// Field names are camelCase, and an unrecognised field is rejected by name rather than silently dropped.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/guests (the `CreateGuest` operationId).
func (c *Client) CreateGuest(ctx context.Context, params *CreateGuestParams, body CreateGuestJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateGuestRequest(c.Server, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetGuest Get guest profile
//
// Returns the full guest profile — base list-row fields plus contacts, flags, notes, risk metadata, and reservation aggregates. Aggregates main vanio's `GuestService.getGuestProfile()` into the public Repull shape so SDK consumers don't have to learn the internal schema.
//
// **Inactive listings:** a guest whose every reservation is on an inactive listing returns `403 listing_inactive` naming those listings (the guest is kept, so this is not a 404). Otherwise the reservation aggregates exclude reservations on inactive listings. A guest with no reservations is always readable.
//
// Corresponds with GET /v1/guests/{id} (the `GetGuest` operationId).
func (c *Client) GetGuest(ctx context.Context, id int, params *GetGuestParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetGuestRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetHealth Health check
//
// Liveness probe. Returns `{ status: "ok", version }` when the API process is running. No auth required. Suitable for uptime monitors, load-balancer health checks, and SDK self-tests.
//
// Corresponds with GET /v1/health (the `GetHealth` operationId).
func (c *Client) GetHealth(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetHealthRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetAuthHealth API-key authentication path health
//
// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
//
// Corresponds with GET /v1/health/auth (the `GetAuthHealth` operationId).
func (c *Client) GetAuthHealth(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetAuthHealthRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetChannelHealth Per-channel connectivity health
//
// Reports reachability and auth state for one channel (`airbnb`, `booking`, `vrbo`, `plumguide`). Use it to tell "the channel is down" apart from "this workspace's connection expired".
//
// Corresponds with GET /v1/health/channels/{channel} (the `GetChannelHealth` operationId).
func (c *Client) GetChannelHealth(ctx context.Context, channel GetChannelHealthParamsChannel, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetChannelHealthRequest(c.Server, channel)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetMcpHealth MCP server reachability
//
// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
//
// Corresponds with GET /v1/health/mcp (the `GetMcpHealth` operationId).
func (c *Client) GetMcpHealth(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetMcpHealthRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetWebhooksHealth Webhook delivery pipeline health
//
// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
//
// Corresponds with GET /v1/health/webhooks (the `GetWebhooksHealth` operationId).
func (c *Client) GetWebhooksHealth(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetWebhooksHealthRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListInquiries List inquiries
//
// Airbnb inquiries — guests asking about dates before booking — newest first. By default only `open` ones: nobody has answered and the stay is still ahead. Answer one with `POST /v1/conversations/{conversationId}/pre-approval` (accept their dates and price) or `POST /v1/conversations/{conversationId}/special-offers` (your own terms).
//
// Booking **requests** are not inquiries: they are reservations with status `pending` — list them with `GET /v1/reservations?status=pending` and answer with `POST /v1/reservations/{id}/accept` or `/decline`.
//
// **Pagination:** pass `pagination.nextCursor` back as `?cursor=` until `pagination.hasMore` is `false`. `?offset=` also works (0..10000). `limit` defaults to 50, max 100.
//
// Inquiries on inactive listings are left out; `?listing_id=` naming an inactive listing returns `403 listing_inactive`. `X-Account-Id` narrows to one connected account.
//
// Corresponds with GET /v1/inquiries (the `ListInquiries` operationId).
func (c *Client) ListInquiries(ctx context.Context, params *ListInquiriesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListInquiriesRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ClearKv Clear KV entries by prefix
//
// Bulk-deletes every key in the project whose name starts with `prefix`. The `prefix` parameter is required — there is no "delete every key in this project" shortcut; pass an empty `prefix` is rejected with 422 to prevent accidental wipes. Returns the number of rows removed.
//
// Corresponds with DELETE /v1/kv (the `ClearKv` operationId).
func (c *Client) ClearKv(ctx context.Context, params *ClearKvParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewClearKvRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListKv List KV entries
//
// Returns every non-expired key-value row in the given project, sorted ascending by key. Use `prefix` to scope to a key namespace (e.g. `prefix=user:42:` to fetch all entries for one user). Hard cap of 1,000 rows per response — for projects approaching that, paginate by walking prefix buckets.
//
// Corresponds with GET /v1/kv (the `ListKv` operationId).
func (c *Client) ListKv(ctx context.Context, params *ListKvParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListKvRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteKv Delete a KV entry
//
// Removes a single key. Returns `{ deleted: true }` if the row was present, `{ deleted: false }` if it was already absent — both are 200 (idempotent).
//
// Corresponds with DELETE /v1/kv/{key} (the `DeleteKv` operationId).
func (c *Client) DeleteKv(ctx context.Context, key string, params *DeleteKvParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteKvRequest(c.Server, key, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetKv Get a KV entry
//
// Fetches a single key. Returns 404 when the key does not exist OR has expired (rows past `ttl_at` are filtered from reads). Cross-tenant lookups also return 404 — the API never reveals existence of another customer's keys.
//
// Corresponds with GET /v1/kv/{key} (the `GetKv` operationId).
func (c *Client) GetKv(ctx context.Context, key string, params *GetKvParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetKvRequest(c.Server, key, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetKvWithBody Set a KV entry
//
// Upserts a key. The full row is replaced — there is no partial update. Pass `ttl_seconds` (positive integer) to auto-expire the row; omit for no expiry. **Caps:** 64 KiB per row (key bytes + value JSON bytes), 1 MiB per customer (sum across ALL projects/keys). Over either cap returns 413.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/kv/{key} (the `SetKv` operationId).
func (c *Client) SetKvWithBody(ctx context.Context, key string, params *SetKvParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetKvRequestWithBody(c.Server, key, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetKv Set a KV entry
//
// Upserts a key. The full row is replaced — there is no partial update. Pass `ttl_seconds` (positive integer) to auto-expire the row; omit for no expiry. **Caps:** 64 KiB per row (key bytes + value JSON bytes), 1 MiB per customer (sum across ALL projects/keys). Over either cap returns 413.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/kv/{key} (the `SetKv` operationId).
func (c *Client) SetKv(ctx context.Context, key string, params *SetKvParams, body SetKvJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetKvRequest(c.Server, key, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListListings List listings
//
// Cursor-paginated list of listings owned by the authenticated workspace. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request to walk the full set. `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. Filters: `q` (substring on name/street/city), `status`, `channel`.
//
// **Optional expansions:** Pass `?include=content` to enrich each row with the rich content slab (summary, description, space, house rules, etc. — sourced from `listings_descriptions` for the `en` locale). Pass `?include=details` for the structural slab (bedrooms, bathrooms, person capacity, check-in window, wifi, house manual, etc.). Both default to `null` per row when the underlying `listings_descriptions` / `listings_details` row is missing — distinct from the field being absent (which signals the expansion was not requested). Pass `?include=thumbnail` to guarantee `thumbnailUrl` on every returned row — including the reduced inactive ones. Combine comma-separated, e.g. `?include=content,thumbnail`. The default response stays lean; consumers must opt in.
//
// **Inactive listings:** by default only active listings are returned. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated, so when `status` asks for inactive ones they carry only `id`, `name`, `status` and `channels` — enough to choose what to activate with `PATCH /v1/listings/{id}`. The `content` and `details` expansions are not applied to them. `?include=thumbnail` is the one exception: it adds `thumbnailUrl` to an inactive row so a single request can render an active/inactive selection screen with pictures, instead of one follow-up call per listing (which an inactive listing would answer with `403 listing_inactive` anyway).
//
// Corresponds with GET /v1/listings (the `ListListings` operationId).
func (c *Client) ListListings(ctx context.Context, params *ListListingsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListListingsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateListingWithBody Create a Repull listing
//
// Create a new vacation-rental listing under the authenticated workspace. The listing is stored in the canonical Vanio listings tables and can be published to multiple channels (Airbnb, Booking.com) via the publish endpoints.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/listings (the `CreateListing` operationId).
func (c *Client) CreateListingWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateListingRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateListing Create a Repull listing
//
// Create a new vacation-rental listing under the authenticated workspace. The listing is stored in the canonical Vanio listings tables and can be published to multiple channels (Airbnb, Booking.com) via the publish endpoints.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/listings (the `CreateListing` operationId).
func (c *Client) CreateListing(ctx context.Context, body CreateListingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateListingRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BulkApplyPricingWithBody Bulk apply or decline pricing recommendations
//
// Apply or decline pending Atlas pricing recommendations across many listings in one call. Built for power users with hundreds of listings who would otherwise need 500 sequential single-listing POSTs.
//
// - `items` is capped at 500 entries per request — exceeding returns 422.
// - Per-item failures (stale listing IDs, no pending recs, channel auth blips) DO NOT fail the whole batch — partial success is the norm at this scale and the granular `failed[]` array lets the SDK retry just the bad entries.
// - Tier-limit accounting: this endpoint counts as **1 API call** regardless of how many items the body contains.
//
// Apply path writes the recommended price to each listing's calendar via the calendar service (which fans out to Airbnb/Booking/VRBO) then marks the Atlas recommendation `applied`. Decline path is Atlas-only — fast.
//
// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/listings/pricing/bulk (the `BulkApplyPricing` operationId).
func (c *Client) BulkApplyPricingWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBulkApplyPricingRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// BulkApplyPricing Bulk apply or decline pricing recommendations
//
// Apply or decline pending Atlas pricing recommendations across many listings in one call. Built for power users with hundreds of listings who would otherwise need 500 sequential single-listing POSTs.
//
// - `items` is capped at 500 entries per request — exceeding returns 422.
// - Per-item failures (stale listing IDs, no pending recs, channel auth blips) DO NOT fail the whole batch — partial success is the norm at this scale and the granular `failed[]` array lets the SDK retry just the bad entries.
// - Tier-limit accounting: this endpoint counts as **1 API call** regardless of how many items the body contains.
//
// Apply path writes the recommended price to each listing's calendar via the calendar service (which fans out to Airbnb/Booking/VRBO) then marks the Atlas recommendation `applied`. Decline path is Atlas-only — fast.
//
// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/listings/pricing/bulk (the `BulkApplyPricing` operationId).
func (c *Client) BulkApplyPricing(ctx context.Context, body BulkApplyPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewBulkApplyPricingRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetListingsStatusWithBody Activate or deactivate listings in bulk
//
// Sets up to 500 listings active or inactive in one call. Send `{ "listingIds": ["4118", "4119"], "active": false }` to deactivate them, or `"active": true` to activate them.
//
// An inactive listing is not counted toward your plan's listing limit or billed. It is NOT deleted and the upstream channel (Airbnb / Booking.com / your PMS) is never touched — its data keeps syncing, so it is complete the moment you activate it again. Until then it cannot be read, changed, or receive webhooks.
//
// **All or nothing.** Nothing changes unless the whole request can be applied:
// - If any id is not one of your listings, the call returns `404` naming those ids.
// - If activating would take you over your plan's listing limit, the call returns `402 listings_limit_exceeded`. Only listings that are currently inactive count toward the new total, so re-sending ids that are already active never trips the limit.
//
// Deactivating is always allowed, including when your account is already over its limit — it is how you get back under it.
//
// **Idempotent.** Ids already in the requested state are returned in `unchanged`; ids this call changed are returned in `updated`.
//
// For a single listing, `PATCH /v1/listings/{id}` does the same.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/listings/status (the `SetListingsStatus` operationId).
func (c *Client) SetListingsStatusWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetListingsStatusRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// SetListingsStatus Activate or deactivate listings in bulk
//
// Sets up to 500 listings active or inactive in one call. Send `{ "listingIds": ["4118", "4119"], "active": false }` to deactivate them, or `"active": true` to activate them.
//
// An inactive listing is not counted toward your plan's listing limit or billed. It is NOT deleted and the upstream channel (Airbnb / Booking.com / your PMS) is never touched — its data keeps syncing, so it is complete the moment you activate it again. Until then it cannot be read, changed, or receive webhooks.
//
// **All or nothing.** Nothing changes unless the whole request can be applied:
// - If any id is not one of your listings, the call returns `404` naming those ids.
// - If activating would take you over your plan's listing limit, the call returns `402 listings_limit_exceeded`. Only listings that are currently inactive count toward the new total, so re-sending ids that are already active never trips the limit.
//
// Deactivating is always allowed, including when your account is already over its limit — it is how you get back under it.
//
// **Idempotent.** Ids already in the requested state are returned in `unchanged`; ids this call changed are returned in `updated`.
//
// For a single listing, `PATCH /v1/listings/{id}` does the same.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/listings/status (the `SetListingsStatus` operationId).
func (c *Client) SetListingsStatus(ctx context.Context, body SetListingsStatusJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewSetListingsStatusRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeactivateListing Deactivate (exclude) a listing
//
// Deactivate a listing — sets it inactive and excludes it from Repull. This is a **soft** operation: the listing row is KEPT (never hard-deleted) and the upstream channel (Airbnb / Hospitable / Booking.com) is NEVER touched. Repull only mutates its own copy.
//
// Equivalent to `PATCH /v1/listings/{id}` with `{ "active": false }`. This is the primary self-serve way for a free-tier customer to trim back under the plan-listings cap — `DELETE` is served even when the account is over the cap (a 402-locked account can still call it). To bring a listing back, use `PATCH` with `{ "active": true }`.
//
// Idempotent: deactivating an already-inactive listing returns 200.
//
// To deactivate many listings at once, use `POST /v1/listings/status` with `{ "active": false }`.
//
// Corresponds with DELETE /v1/listings/{id} (the `DeactivateListing` operationId).
func (c *Client) DeactivateListing(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeactivateListingRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetListing Get a listing
//
// Fetch a single listing by id. Returns the same shape as one element of the `GET /v1/listings` response, so you can bind the result to the same model. Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403, never reveals the listing's existence.
//
// **Optional expansions:** Pass `?include=amenities` to enrich the response with the listing's amenity rows (`[]` when the listing has none). Pass `?include=content` for the rich content slab (summary, description, space, house rules, etc. — sourced from `listings_descriptions` for the `en` locale; `null` when no row is stored). Pass `?include=details` for the structural slab (bedrooms, bathrooms, person capacity, check-in window, wifi, house manual, etc.; `null` when no row is stored). Combine comma-separated, e.g. `?include=amenities,content,details`. The default response stays lean; consumers must opt in.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/listings/{id} (the `GetListing` operationId).
func (c *Client) GetListing(ctx context.Context, id int, params *GetListingParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetListingRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateListingActiveWithBody Deactivate or reactivate a listing
//
// Toggle a listing's active state. Send `{ "active": false }` to **deactivate** (exclude the listing from Repull) or `{ "active": true }` to **reactivate** it.
//
// "Deactivate" keeps the listing row — it is NOT a hard delete, and it NEVER touches the upstream channel (Airbnb / Hospitable / Booking.com). Repull only mutates its own copy of the inventory. Deactivating is the self-serve way to get back under the plan-listings cap without paying.
//
// Reactivation respects the plan-listings cap: if activating this listing would push you over the cap for your tier, the call returns `402 listings_limit_exceeded` and the listing stays inactive. Deactivate another listing or upgrade first.
//
// Idempotent: setting a listing to the state it's already in returns 200.
//
// To change many listings at once, all or nothing, use `POST /v1/listings/status`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /v1/listings/{id} (the `UpdateListingActive` operationId).
func (c *Client) UpdateListingActiveWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateListingActiveRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateListingActive Deactivate or reactivate a listing
//
// Toggle a listing's active state. Send `{ "active": false }` to **deactivate** (exclude the listing from Repull) or `{ "active": true }` to **reactivate** it.
//
// "Deactivate" keeps the listing row — it is NOT a hard delete, and it NEVER touches the upstream channel (Airbnb / Hospitable / Booking.com). Repull only mutates its own copy of the inventory. Deactivating is the self-serve way to get back under the plan-listings cap without paying.
//
// Reactivation respects the plan-listings cap: if activating this listing would push you over the cap for your tier, the call returns `402 listings_limit_exceeded` and the listing stays inactive. Deactivate another listing or upgrade first.
//
// Idempotent: setting a listing to the state it's already in returns 200.
//
// To change many listings at once, all or nothing, use `POST /v1/listings/status`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /v1/listings/{id} (the `UpdateListingActive` operationId).
func (c *Client) UpdateListingActive(ctx context.Context, id int, body UpdateListingActiveJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateListingActiveRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListListingComps Comp set for a listing (with daily nightly pricing)
//
// Returns the actual comp set for a listing — the underlying competitor listings (with daily nightly pricing), not just the aggregated `compSummary` from `/pricing`. Each comp comes back with distance, bedrooms, ratings, lat/lng, platform link, and a per-day rate/availability series for the requested window.
//
// Powered by Atlas. Comps with no coordinates are excluded — there's no way to rank them by distance. Listings without coordinates return `data: []` and a `warning` field.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/listings/{id}/comps (the `ListListingComps` operationId).
func (c *Client) ListListingComps(ctx context.Context, id int, params *ListListingCompsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListListingCompsRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateListingContentWithBody Update canonical listing content
//
// Write your PMS's canonical listing content — title, description, amenities, address, occupancy, and policies — into a Repull listing, making it the source of truth. This is the flagship "the PMS owns listing content, Repull distributes it" enabler.
//
// **Partial update:** every field is optional. Only the fields you send are written; absent fields are left untouched. `amenities` is a FULL replacement of the amenity set (omit to leave untouched, send `[]` to clear).
//
// **Multilingual:** send `locale` to say which language this copy is in (`it`, `pt-BR`, …). Canonical content is stored per locale, so each language keeps its own row instead of overwriting the English one. Omit it for English.
//
// **Local write only — NOT a channel publish.** This mutates Repull's own copy of the content. It does NOT push to Airbnb / Booking.com; it marks the channels dirty so a later publish knows what changed. Distribution stays a separate explicit step.
//
// **Photos are deferred:** a provided `photos` array is echoed back in the `deferred` field and NOT persisted (media ingestion is a follow-up).
//
// Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403. This endpoint is served even when the account is over the plan-listings cap, since editing content on a listing you already own never grows the portfolio.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/listings/{id}/content (the `UpdateListingContent` operationId).
func (c *Client) UpdateListingContentWithBody(ctx context.Context, id int, params *UpdateListingContentParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateListingContentRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateListingContent Update canonical listing content
//
// Write your PMS's canonical listing content — title, description, amenities, address, occupancy, and policies — into a Repull listing, making it the source of truth. This is the flagship "the PMS owns listing content, Repull distributes it" enabler.
//
// **Partial update:** every field is optional. Only the fields you send are written; absent fields are left untouched. `amenities` is a FULL replacement of the amenity set (omit to leave untouched, send `[]` to clear).
//
// **Multilingual:** send `locale` to say which language this copy is in (`it`, `pt-BR`, …). Canonical content is stored per locale, so each language keeps its own row instead of overwriting the English one. Omit it for English.
//
// **Local write only — NOT a channel publish.** This mutates Repull's own copy of the content. It does NOT push to Airbnb / Booking.com; it marks the channels dirty so a later publish knows what changed. Distribution stays a separate explicit step.
//
// **Photos are deferred:** a provided `photos` array is echoed back in the `deferred` field and NOT persisted (media ingestion is a follow-up).
//
// Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403. This endpoint is served even when the account is over the plan-listings cap, since editing content on a listing you already own never grows the portfolio.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/listings/{id}/content (the `UpdateListingContent` operationId).
func (c *Client) UpdateListingContent(ctx context.Context, id int, params *UpdateListingContentParams, body UpdateListingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateListingContentRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GenerateListingContentWithBody AI-generate listing content
//
// Generate guest-facing copy (title, summary, description, amenities, etc.) for a listing using Repull AI. When `photos` are provided the vision model is used for photo-grounded copy. Persists into the listing by default.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/listings/{id}/generate-content (the `GenerateListingContent` operationId).
func (c *Client) GenerateListingContentWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGenerateListingContentRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GenerateListingContent AI-generate listing content
//
// Generate guest-facing copy (title, summary, description, amenities, etc.) for a listing using Repull AI. When `photos` are provided the vision model is used for photo-grounded copy. Persists into the listing by default.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/listings/{id}/generate-content (the `GenerateListingContent` operationId).
func (c *Client) GenerateListingContent(ctx context.Context, id int, body GenerateListingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGenerateListingContentRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// TakeListingOfflineWithBody Take a listing off the market
//
// Stop this listing being sold, on every channel it is connected to, in one call.
//
// What that means differs per channel and you do not have to know which is which. On **Airbnb** the live listing is deactivated with a valid deactivation reason and then READ BACK — Airbnb accepts some deactivations and leaves the listing up, so "we sent the request" is never reported as success. On **Booking.com** there is no unlist at all; the equivalent is closing the room's availability across the whole forward window, which is what happens.
//
// **This is not the same as deactivating the listing in Repull.** The two get confused because both sound like removal, and they have opposite consequences:
//
// | | Take offline (this endpoint) | Deactivate in Repull (`PATCH /v1/listings/{id}` `{"active": false}`) |
// |---|---|---|
// | The guest-facing listing | **Stops taking bookings** | Stays live and keeps taking bookings |
// | Billing and plan limits | Unchanged | No longer billed, no longer counts toward the cap |
// | API access to the listing | Unchanged — you can still read and write it | `403 listing_inactive` until reactivated |
// | Reverse it with | `POST /v1/listings/{id}/online` | `PATCH /v1/listings/{id}` `{"active": true}` |
// | Data kept | Yes | Yes, and it keeps syncing |
//
// Neither one deletes anything, on either side.
//
// **The answer is per channel item.** A listing can sit on several Airbnb connections and a Booking.com property at once; they fail independently and a partial result is the ordinary outcome, so every item reports its own `state`, `code` and `message` and there is no top-level success flag to mislead you. Nothing is rolled back — re-send the same request to retry the items that did not land.
//
// **Booking.com ambiguity is reported, not fanned out.** A listing mapped to more than one active Booking.com property comes back with that item refused (`ambiguous_booking_mapping`) while the Airbnb items still run: closing the wrong property's availability takes real inventory off sale, and taking a listing off Airbnb is not less urgent because its Booking.com mapping is untidy. Name the property with `hotelId` and send it again.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/listings/{id}/offline (the `TakeListingOffline` operationId).
func (c *Client) TakeListingOfflineWithBody(ctx context.Context, id int, params *TakeListingOfflineParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewTakeListingOfflineRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// TakeListingOffline Take a listing off the market
//
// Stop this listing being sold, on every channel it is connected to, in one call.
//
// What that means differs per channel and you do not have to know which is which. On **Airbnb** the live listing is deactivated with a valid deactivation reason and then READ BACK — Airbnb accepts some deactivations and leaves the listing up, so "we sent the request" is never reported as success. On **Booking.com** there is no unlist at all; the equivalent is closing the room's availability across the whole forward window, which is what happens.
//
// **This is not the same as deactivating the listing in Repull.** The two get confused because both sound like removal, and they have opposite consequences:
//
// | | Take offline (this endpoint) | Deactivate in Repull (`PATCH /v1/listings/{id}` `{"active": false}`) |
// |---|---|---|
// | The guest-facing listing | **Stops taking bookings** | Stays live and keeps taking bookings |
// | Billing and plan limits | Unchanged | No longer billed, no longer counts toward the cap |
// | API access to the listing | Unchanged — you can still read and write it | `403 listing_inactive` until reactivated |
// | Reverse it with | `POST /v1/listings/{id}/online` | `PATCH /v1/listings/{id}` `{"active": true}` |
// | Data kept | Yes | Yes, and it keeps syncing |
//
// Neither one deletes anything, on either side.
//
// **The answer is per channel item.** A listing can sit on several Airbnb connections and a Booking.com property at once; they fail independently and a partial result is the ordinary outcome, so every item reports its own `state`, `code` and `message` and there is no top-level success flag to mislead you. Nothing is rolled back — re-send the same request to retry the items that did not land.
//
// **Booking.com ambiguity is reported, not fanned out.** A listing mapped to more than one active Booking.com property comes back with that item refused (`ambiguous_booking_mapping`) while the Airbnb items still run: closing the wrong property's availability takes real inventory off sale, and taking a listing off Airbnb is not less urgent because its Booking.com mapping is untidy. Name the property with `hotelId` and send it again.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/listings/{id}/offline (the `TakeListingOffline` operationId).
func (c *Client) TakeListingOffline(ctx context.Context, id int, params *TakeListingOfflineParams, body TakeListingOfflineJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewTakeListingOfflineRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// TakeListingOnlineWithBody Put a listing back on the market
//
// Put this listing back on sale, on every channel it is connected to. The counterpart of `POST /v1/listings/{id}/offline`, which documents the per-item response and the difference between this and deactivating a listing in Repull.
//
// **It does not push content.** On **Airbnb** it re-enables sync and makes the listing available again; anything that changed while the listing was down is still unpublished, so follow with `POST /v1/listings/{id}/publish/airbnb` if the content moved. On **Booking.com** it re-syncs the true calendar rather than opening everything: dates that are genuinely blocked — a reservation, an owner stay — stay blocked, and only the closure `offline` wrote lifts. The two directions are not mirror images, and that is deliberate.
//
// **One asymmetry worth planning for.** Taking a listing down passes no billing gate; putting it back up goes through the channel-publish gate. So on a workspace whose subscription has lapsed, `offline` still works and this endpoint answers `402 payment_required` — a listing can be left off the market until billing is sorted out. That refusal is reported as a billing refusal with the action that fixes it, never as a channel error: retrying, or reconnecting the channel, does nothing for it.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/listings/{id}/online (the `TakeListingOnline` operationId).
func (c *Client) TakeListingOnlineWithBody(ctx context.Context, id int, params *TakeListingOnlineParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewTakeListingOnlineRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// TakeListingOnline Put a listing back on the market
//
// Put this listing back on sale, on every channel it is connected to. The counterpart of `POST /v1/listings/{id}/offline`, which documents the per-item response and the difference between this and deactivating a listing in Repull.
//
// **It does not push content.** On **Airbnb** it re-enables sync and makes the listing available again; anything that changed while the listing was down is still unpublished, so follow with `POST /v1/listings/{id}/publish/airbnb` if the content moved. On **Booking.com** it re-syncs the true calendar rather than opening everything: dates that are genuinely blocked — a reservation, an owner stay — stay blocked, and only the closure `offline` wrote lifts. The two directions are not mirror images, and that is deliberate.
//
// **One asymmetry worth planning for.** Taking a listing down passes no billing gate; putting it back up goes through the channel-publish gate. So on a workspace whose subscription has lapsed, `offline` still works and this endpoint answers `402 payment_required` — a listing can be left off the market until billing is sorted out. That refusal is reported as a billing refusal with the action that fixes it, never as a channel error: retrying, or reconnecting the channel, does nothing for it.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/listings/{id}/online (the `TakeListingOnline` operationId).
func (c *Client) TakeListingOnline(ctx context.Context, id int, params *TakeListingOnlineParams, body TakeListingOnlineJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewTakeListingOnlineRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteListingPhotoWithBody Delete a stored listing photo
//
// Deletes a single stored photo by its storage `path` (as returned by `GET /v1/listings/{id}/photos` or `POST /v1/listings/{id}/photos/upload-url`).
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with DELETE /v1/listings/{id}/photos (the `DeleteListingPhoto` operationId).
func (c *Client) DeleteListingPhotoWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteListingPhotoRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteListingPhoto Delete a stored listing photo
//
// Deletes a single stored photo by its storage `path` (as returned by `GET /v1/listings/{id}/photos` or `POST /v1/listings/{id}/photos/upload-url`).
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with DELETE /v1/listings/{id}/photos (the `DeleteListingPhoto` operationId).
func (c *Client) DeleteListingPhoto(ctx context.Context, id int, body DeleteListingPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteListingPhotoRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListListingPhotos List a listing's stored photos
//
// Returns the photo set currently stored for this listing.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/listings/{id}/photos (the `ListListingPhotos` operationId).
func (c *Client) ListListingPhotos(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListListingPhotosRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateListingPhotoUploadUrlWithBody Mint a direct-to-storage photo upload URL
//
// Mints a short-lived signed upload URL + token for a listing photo. **The client PUTs the raw file bytes directly to the returned `uploadUrl` — the file bytes never pass through the Repull API or main vanio.** This endpoint only mints the URL; do not POST the file itself here, it will not be accepted.
//
// Flow: (1) POST here with `fileName`/`fileType`/optional `fileSize` to get `{ uploadUrl, token, path, publicUrl, expiresIn }`; (2) PUT the raw file bytes to `uploadUrl` from the client; (3) `publicUrl` is the durable URL for the uploaded photo — attach it to the listing via `PUT /v1/listings/{id}/content` (`photos` field) or list it back via `GET /v1/listings/{id}/photos`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/listings/{id}/photos/upload-url (the `CreateListingPhotoUploadUrl` operationId).
func (c *Client) CreateListingPhotoUploadUrlWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateListingPhotoUploadUrlRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateListingPhotoUploadUrl Mint a direct-to-storage photo upload URL
//
// Mints a short-lived signed upload URL + token for a listing photo. **The client PUTs the raw file bytes directly to the returned `uploadUrl` — the file bytes never pass through the Repull API or main vanio.** This endpoint only mints the URL; do not POST the file itself here, it will not be accepted.
//
// Flow: (1) POST here with `fileName`/`fileType`/optional `fileSize` to get `{ uploadUrl, token, path, publicUrl, expiresIn }`; (2) PUT the raw file bytes to `uploadUrl` from the client; (3) `publicUrl` is the durable URL for the uploaded photo — attach it to the listing via `PUT /v1/listings/{id}/content` (`photos` field) or list it back via `GET /v1/listings/{id}/photos`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/listings/{id}/photos/upload-url (the `CreateListingPhotoUploadUrl` operationId).
func (c *Client) CreateListingPhotoUploadUrl(ctx context.Context, id int, body CreateListingPhotoUploadUrlJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateListingPhotoUploadUrlRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetListingPricing Get pricing recommendations
//
// Returns date-by-date pricing recommendations for a listing's upcoming calendar window, plus the listing's base-price context and a 5km comp summary. Recommendations come from the Atlas pricing model — pre-computed nightly and stored in `pricing_recommendations`. Use POST to apply or decline pending recommendations.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/listings/{id}/pricing (the `GetListingPricing` operationId).
func (c *Client) GetListingPricing(ctx context.Context, id int, params *GetListingPricingParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetListingPricingRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ApplyListingPricingWithBody Apply or decline pricing recommendations
//
// Apply: writes the recommended price to the listing's calendar for the given dates and triggers the platform fan-out (Airbnb / Booking.com / VRBO). Decline: marks the recommendation as `declined` so it stops surfacing — the model can re-recommend on the next training cycle.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/listings/{id}/pricing (the `ApplyListingPricing` operationId).
func (c *Client) ApplyListingPricingWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewApplyListingPricingRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ApplyListingPricing Apply or decline pricing recommendations
//
// Apply: writes the recommended price to the listing's calendar for the given dates and triggers the platform fan-out (Airbnb / Booking.com / VRBO). Decline: marks the recommendation as `declined` so it stops surfacing — the model can re-recommend on the next training cycle.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/listings/{id}/pricing (the `ApplyListingPricing` operationId).
func (c *Client) ApplyListingPricing(ctx context.Context, id int, body ApplyListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewApplyListingPricingRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetListingPricingHistory Pricing recommendation audit trail
//
// Cursor-paginated audit trail of pricing recommendations vs applied prices for a listing across a date window. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
//
// Defaults to ±90 days from today. Cursor is a keyset on `date ASC` — stable even if rows are added during a partner's pagination walk. `limit` is capped at 500 — exceeding returns 422.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/listings/{id}/pricing/history (the `GetListingPricingHistory` operationId).
func (c *Client) GetListingPricingHistory(ctx context.Context, id int, params *GetListingPricingHistoryParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetListingPricingHistoryRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetListingPricingStrategy Get pricing strategy
//
// Returns the strategy that constrains how the Atlas pricing model behaves for this listing. If no strategy row exists yet, returns sane defaults flagged with `isDefault: true`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/listings/{id}/pricing/strategy (the `GetListingPricingStrategy` operationId).
func (c *Client) GetListingPricingStrategy(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetListingPricingStrategyRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateListingPricingStrategyWithBody Update pricing strategy
//
// Upserts the strategy on `(listing_id, customer_id)` — repeated PUTs are idempotent. Send only the fields you want to change; omitted fields take server-side defaults.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PUT /v1/listings/{id}/pricing/strategy (the `UpdateListingPricingStrategy` operationId).
func (c *Client) UpdateListingPricingStrategyWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateListingPricingStrategyRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateListingPricingStrategy Update pricing strategy
//
// Upserts the strategy on `(listing_id, customer_id)` — repeated PUTs are idempotent. Send only the fields you want to change; omitted fields take server-side defaults.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PUT /v1/listings/{id}/pricing/strategy (the `UpdateListingPricingStrategy` operationId).
func (c *Client) UpdateListingPricingStrategy(ctx context.Context, id int, body UpdateListingPricingStrategyJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateListingPricingStrategyRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetListingPublishStatus Per-channel publish status
//
// Returns connection state and sync activity per channel. `channels` is sync activity (empty until first push). `connections` is connection state (populated as soon as a channel is linked). Recommended polling cadence: at most once per 30s per listing — for bulk views, prefer `GET /v1/listings` and filter client-side.
//
// **When a push fails, this endpoint says why.** `channels[].pushError` carries the channel's own reason for the last failed push, verbatim — `"Links and contact info can't be shared"`, `"Check-in start time must be before end time"`, `"property_type_group must be one of […]"`. It is free text written by the channel, so render it next to the retry button rather than parsing it. `null` when the last push succeeded or none has run; pair it with `pushStatus` to tell those two apart.
//
// **It also says what you will not be allowed to change.** The `airbnb` entry in `connections` carries `lockedFields` — attributes Airbnb has locked on this listing. Airbnb does not refuse a write to one: it answers 200, reports the field as locked, and applies nothing, so a locked write is indistinguishable from a successful one unless you looked first. Read it before you let someone edit. Airbnb-only; no other channel has the concept, and no other entry carries the field.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/listings/{id}/publish-status (the `GetListingPublishStatus` operationId).
func (c *Client) GetListingPublishStatus(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetListingPublishStatusRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PublishListingToAirbnbWithBody Publish a listing to Airbnb
//
// Push a Repull listing's canonical content to Airbnb. Pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create a brand-new Airbnb listing under that host.
//
// **A publish is not one call to Airbnb.** It is up to eight independent ones — details, description, amenities, rooms, policies, photos, pricing, checkout_tasks — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Airbnb's own reason, per section, for the ones that did not. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Publish again once you have fixed the failing sections — a re-publish of an unchanged section is harmless.
//
// `result.live` is a different question from `result.published`. `published` is about CONTENT — every attempted section landed. `live` is about whether the listing takes bookings: it is true only when activation was actually performed and succeeded. A create can land all eight sections and still leave the listing inactive, because activation is skipped when instant-booking cannot be confirmed to be off — so `published: true` with `live: false` is a real and common outcome, and `result.warnings` says why. `live` is ABSENT, not `false`, when activation was never part of the operation: publishing to an already-mapped listing updates content and activates nothing. Only treat a listing as not-live when `live` is present and false.
//
// `result.warnings[]` lists steps that failed WITHOUT failing the publish — optional work the push carried on past. They were previously swallowed, so the only sign of one was a listing that was somehow not quite right afterwards. A publish can be `published: true` and still carry warnings; read them before concluding nothing needs doing.
//
// `result.lockedFields` names the fields Airbnb will not let this listing change at all. They are not retryable by anyone: Airbnb answers 200 and applies nothing. `GET /v1/channels/airbnb/listings/{id}` reports the same list up front.
//
// **Which fields this pushes** — title, description sections and house rules (English/primary locale), amenities, rooms and beds, photos, nightly price and fees, cancellation policy and guest controls, check-in/out times, quiet hours, property and room type, checkout tasks. **Not pushed by this endpoint:** non-primary locales (`PUT /v1/channels/airbnb/listings/{id}/descriptions`), guest-safety disclosures (`PUT …/safety-disclosures`), check-in method (`PUT …/details`), permits (`PUT …/permits`), and the calendar (`PUT …/availability`).
//
// `force: true` re-pushes every section, ignoring dirty-field tracking. Without it only the sections changed since the last successful publish are sent.
//
// Send `Idempotency-Key` to make a retry safe: a timeout on a publish otherwise leaves you unable to tell whether it ran.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/listings/{id}/publish/airbnb (the `PublishListingToAirbnb` operationId).
func (c *Client) PublishListingToAirbnbWithBody(ctx context.Context, id int, params *PublishListingToAirbnbParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPublishListingToAirbnbRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PublishListingToAirbnb Publish a listing to Airbnb
//
// Push a Repull listing's canonical content to Airbnb. Pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create a brand-new Airbnb listing under that host.
//
// **A publish is not one call to Airbnb.** It is up to eight independent ones — details, description, amenities, rooms, policies, photos, pricing, checkout_tasks — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Airbnb's own reason, per section, for the ones that did not. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Publish again once you have fixed the failing sections — a re-publish of an unchanged section is harmless.
//
// `result.live` is a different question from `result.published`. `published` is about CONTENT — every attempted section landed. `live` is about whether the listing takes bookings: it is true only when activation was actually performed and succeeded. A create can land all eight sections and still leave the listing inactive, because activation is skipped when instant-booking cannot be confirmed to be off — so `published: true` with `live: false` is a real and common outcome, and `result.warnings` says why. `live` is ABSENT, not `false`, when activation was never part of the operation: publishing to an already-mapped listing updates content and activates nothing. Only treat a listing as not-live when `live` is present and false.
//
// `result.warnings[]` lists steps that failed WITHOUT failing the publish — optional work the push carried on past. They were previously swallowed, so the only sign of one was a listing that was somehow not quite right afterwards. A publish can be `published: true` and still carry warnings; read them before concluding nothing needs doing.
//
// `result.lockedFields` names the fields Airbnb will not let this listing change at all. They are not retryable by anyone: Airbnb answers 200 and applies nothing. `GET /v1/channels/airbnb/listings/{id}` reports the same list up front.
//
// **Which fields this pushes** — title, description sections and house rules (English/primary locale), amenities, rooms and beds, photos, nightly price and fees, cancellation policy and guest controls, check-in/out times, quiet hours, property and room type, checkout tasks. **Not pushed by this endpoint:** non-primary locales (`PUT /v1/channels/airbnb/listings/{id}/descriptions`), guest-safety disclosures (`PUT …/safety-disclosures`), check-in method (`PUT …/details`), permits (`PUT …/permits`), and the calendar (`PUT …/availability`).
//
// `force: true` re-pushes every section, ignoring dirty-field tracking. Without it only the sections changed since the last successful publish are sent.
//
// Send `Idempotency-Key` to make a retry safe: a timeout on a publish otherwise leaves you unable to tell whether it ran.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/listings/{id}/publish/airbnb (the `PublishListingToAirbnb` operationId).
func (c *Client) PublishListingToAirbnb(ctx context.Context, id int, params *PublishListingToAirbnbParams, body PublishListingToAirbnbJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPublishListingToAirbnbRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PublishListingToBookingWithBody Publish a listing to Booking.com
//
// Push a Repull listing's content to Booking.com. The listing must already be mapped to a Booking.com property + room — claim the hotel through the Connect Booking flow, then map its rooms with `POST /v1/connect/booking/map-rooms`.
//
// **Which property the content lands in.** A listing can be mapped to more than one Booking.com property; the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. When the listing has exactly one property you need send nothing. When it has several, name one with `hotelId` in the body (or `?hotel_id=` — the same value, accepted either way, body wins if you send both). Omit it on such a listing and the push is refused with **`409 ambiguous_booking_mapping`**, listing the candidate ids: content pushed into a property chosen for you lands on the wrong listing and reports success, which is worse than a refusal. `GET /v1/channels/booking/properties` lists every property with the listings mapped under it. Naming a property this listing is not mapped to is a `404` that names the ones it is.
//
// The property that actually received the content comes back as `result.hotelId`.
//
// **A publish is not one call to Booking.com.** It is several independent Content API calls — details, description, amenities, rooms, photos, pricing — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Booking.com's own reason, per section, for the ones that did not. A property whose Content API credentials do not cover a section answers 403 for that section alone. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Fix the failing sections and publish again — re-publishing an unchanged section is harmless.
//
// A listing with no Booking.com property mapped at all is not an error: the call returns `result.published: false` with `result.reason` and `result.hotelId: null`, and nothing is pushed.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/listings/{id}/publish/booking (the `PublishListingToBooking` operationId).
func (c *Client) PublishListingToBookingWithBody(ctx context.Context, id int, params *PublishListingToBookingParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPublishListingToBookingRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PublishListingToBooking Publish a listing to Booking.com
//
// Push a Repull listing's content to Booking.com. The listing must already be mapped to a Booking.com property + room — claim the hotel through the Connect Booking flow, then map its rooms with `POST /v1/connect/booking/map-rooms`.
//
// **Which property the content lands in.** A listing can be mapped to more than one Booking.com property; the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. When the listing has exactly one property you need send nothing. When it has several, name one with `hotelId` in the body (or `?hotel_id=` — the same value, accepted either way, body wins if you send both). Omit it on such a listing and the push is refused with **`409 ambiguous_booking_mapping`**, listing the candidate ids: content pushed into a property chosen for you lands on the wrong listing and reports success, which is worse than a refusal. `GET /v1/channels/booking/properties` lists every property with the listings mapped under it. Naming a property this listing is not mapped to is a `404` that names the ones it is.
//
// The property that actually received the content comes back as `result.hotelId`.
//
// **A publish is not one call to Booking.com.** It is several independent Content API calls — details, description, amenities, rooms, photos, pricing — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Booking.com's own reason, per section, for the ones that did not. A property whose Content API credentials do not cover a section answers 403 for that section alone. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Fix the failing sections and publish again — re-publishing an unchanged section is harmless.
//
// A listing with no Booking.com property mapped at all is not an error: the call returns `result.published: false` with `result.reason` and `result.hotelId: null`, and nothing is pushed.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/listings/{id}/publish/booking (the `PublishListingToBooking` operationId).
func (c *Client) PublishListingToBooking(ctx context.Context, id int, params *PublishListingToBookingParams, body PublishListingToBookingJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPublishListingToBookingRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PullListingFromAirbnbWithBody Refresh a listing from Airbnb
//
// Re-read this listing from Airbnb and update your stored copy, then report what was refreshed and when. The mirror image of `POST /v1/listings/{id}/publish/airbnb`.
//
// Every other Airbnb read on this API is served from our database. This endpoint is the one that goes and asks Airbnb — use it after a push, to see the values Airbnb actually kept, or when a host has changed something in the Airbnb app.
//
// **What it refreshes:** basic listing facts (property type, bedrooms, beds, bathrooms, capacity), descriptions, photos, rooms and beds, amenities, booking settings (check-in/check-out windows, guest controls, cancellation policy), stay rules (min/max nights, advance-booking window, turnover buffer), pricing settings and standard fees, permits, checkout tasks and the check-in guide. After it returns, those values are what `GET /v1/listings/{id}?include=content,details` and the `/v1/channels/airbnb/**` routes serve.
//
// **What it does NOT refresh:** the calendar (nightly rates and availability — see `GET /v1/channels/airbnb/listings/{id}/availability`), reservations, messages, reviews or payouts. Those arrive continuously through the channel's own sync and never need a manual pull.
//
// **Runs synchronously** — the response is the result, not a job id. Expect several seconds.
//
// **One pull per listing per 15 minutes.** A pull is roughly a dozen Airbnb calls; a second call inside the window returns `429 rate_limit_exceeded` with `Retry-After` and `nextPullAvailableAt`, and makes no Airbnb calls. Two simultaneous calls cannot both run.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/listings/{id}/pull/airbnb (the `PullListingFromAirbnb` operationId).
func (c *Client) PullListingFromAirbnbWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPullListingFromAirbnbRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PullListingFromAirbnb Refresh a listing from Airbnb
//
// Re-read this listing from Airbnb and update your stored copy, then report what was refreshed and when. The mirror image of `POST /v1/listings/{id}/publish/airbnb`.
//
// Every other Airbnb read on this API is served from our database. This endpoint is the one that goes and asks Airbnb — use it after a push, to see the values Airbnb actually kept, or when a host has changed something in the Airbnb app.
//
// **What it refreshes:** basic listing facts (property type, bedrooms, beds, bathrooms, capacity), descriptions, photos, rooms and beds, amenities, booking settings (check-in/check-out windows, guest controls, cancellation policy), stay rules (min/max nights, advance-booking window, turnover buffer), pricing settings and standard fees, permits, checkout tasks and the check-in guide. After it returns, those values are what `GET /v1/listings/{id}?include=content,details` and the `/v1/channels/airbnb/**` routes serve.
//
// **What it does NOT refresh:** the calendar (nightly rates and availability — see `GET /v1/channels/airbnb/listings/{id}/availability`), reservations, messages, reviews or payouts. Those arrive continuously through the channel's own sync and never need a manual pull.
//
// **Runs synchronously** — the response is the result, not a job id. Expect several seconds.
//
// **One pull per listing per 15 minutes.** A pull is roughly a dozen Airbnb calls; a second call inside the window returns `429 rate_limit_exceeded` with `Retry-After` and `nextPullAvailableAt`, and makes no Airbnb calls. Two simultaneous calls cannot both run.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/listings/{id}/pull/airbnb (the `PullListingFromAirbnb` operationId).
func (c *Client) PullListingFromAirbnb(ctx context.Context, id int, body PullListingFromAirbnbJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPullListingFromAirbnbRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetListingSegments Atlas DNA segment intelligence for a listing
//
// Aggregates Atlas DNA segment signal (quality tier, design style, bedrooms) across the listing's geographic neighborhood (default: 5km radius) or the whole city, so consumers can answer:
// - What segments dominate my market?
// - Which segment does my listing match best?
// - What's the ADR uplift for moving up a tier?
//
// DNA coverage is still ramping — segments are scored asynchronously. Cities and radii without scored comps return `totalCompsAnalyzed: 0` plus a `low_dna_coverage` recommendation rather than fabricated data.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/listings/{id}/segments (the `GetListingSegments` operationId).
func (c *Client) GetListingSegments(ctx context.Context, id int, params *GetListingSegmentsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetListingSegmentsRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListMarkets List markets the customer operates in
//
// Returns per-city KPIs across every market the authenticated customer has listings in (market share, ADR vs market, occupancy, ratings) plus a lightweight `browse` discovery summary (top-50 featured markets, country categories, total catalog size). For the full paginated discovery catalog with search, call `GET /v1/markets/browse`. Each `markets[]` entry is enriched with `subscribed` + `source` from the customer's market subscriptions.
//
// Corresponds with GET /v1/markets (the `ListMarkets` operationId).
func (c *Client) ListMarkets(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListMarketsRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListMarketBrowse Paginated discovery catalog
//
// Cursor-paginated, search-filterable catalog of every Atlas-tracked market the customer could expand into. Backed by the precomputed `market_summaries` table (>=5 active comps per city). Supports fuzzy `q` substring search (trigram-indexed), `country` (ISO 3166-1 alpha-2) filter, and `sort` (`listings_desc` | `name_asc`). Use the `nextCursor` from `pagination` to walk pages — the cursor is an opaque base64 token; do not parse it.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// `pagination.total` is the count of markets matching the current `q`/`country`/`min_listings` filter (across all pages) — same shape as every other list endpoint.
//
// Corresponds with GET /v1/markets/browse (the `ListMarketBrowse` operationId).
func (c *Client) ListMarketBrowse(ctx context.Context, params *ListMarketBrowseParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListMarketBrowseRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetMarket Deep-dive on a single market
//
// Detailed market view for one city — price distribution, bedroom mix, property types, upcoming events, Wheelhouse demand, monthly benchmarks, customer health rollup, top comps (proximity-sorted, paginated), customer's percentile position, capacity-mix gap, and a 6-month supply trend.
//
// Corresponds with GET /v1/markets/{city} (the `GetMarket` operationId).
func (c *Client) GetMarket(ctx context.Context, city string, params *GetMarketParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetMarketRequest(c.Server, city, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetMarketCalendar Calendar-level market view
//
// Date-by-date market view for a city — market avg / min / max nightly rate, occupancy %, Wheelhouse demand, events touching the date, and (when `listingId` is supplied) an overlay of the customer's own pricing + availability.
//
// Corresponds with GET /v1/markets/{city}/calendar (the `GetMarketCalendar` operationId).
func (c *Client) GetMarketCalendar(ctx context.Context, city string, params *GetMarketCalendarParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetMarketCalendarRequest(c.Server, city, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListMigrations List migrations
//
// Every property manager your workspace has moved through Repull Migrate, newest first, each with where it stands. Page with `pagination.nextCursor`.
//
// Corresponds with GET /v1/migrations (the `ListMigrations` operationId).
func (c *Client) ListMigrations(ctx context.Context, params *ListMigrationsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListMigrationsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteMigration End a migration
//
// Disconnects the source (it stops syncing) and deactivates the migration's workspace. The imported data is kept and stays readable.
//
// Corresponds with DELETE /v1/migrations/{workspaceId} (the `DeleteMigration` operationId).
func (c *Client) DeleteMigration(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteMigrationRequest(c.Server, workspaceId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetMigration Get a migration
//
// Where one migration stands: its state, the source connections with their last import run, and how much data has landed.
//
// Corresponds with GET /v1/migrations/{workspaceId} (the `GetMigration` operationId).
func (c *Client) GetMigration(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetMigrationRequest(c.Server, workspaceId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetMigrationChannelMap Get channel links
//
// The Airbnb, Booking.com and VRBO listing each migrated property is linked to in the source PMS, read live. Use it to link each listing to the right property when the channels are reconnected in the destination — channel connections themselves cannot be moved between PMSs.
//
// Corresponds with GET /v1/migrations/{workspaceId}/channel-map (the `GetMigrationChannelMap` operationId).
func (c *Client) GetMigrationChannelMap(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetMigrationChannelMapRequest(c.Server, workspaceId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CutoverMigration Cut over
//
// The property manager has switched: disconnect the source so it stops syncing. The imported data stays readable. Idempotent.
//
// Corresponds with POST /v1/migrations/{workspaceId}/cutover (the `CutoverMigration` operationId).
func (c *Client) CutoverMigration(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCutoverMigrationRequest(c.Server, workspaceId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CheckMigrationCutoverWithBody Check reservations before switching
//
// Send every upcoming reservation the destination holds; get back what is missing, extra, or on different dates compared with the source. Read-only — run it as often as you like before cutover.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/migrations/{workspaceId}/cutover-check (the `CheckMigrationCutover` operationId).
func (c *Client) CheckMigrationCutoverWithBody(ctx context.Context, workspaceId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCheckMigrationCutoverRequestWithBody(c.Server, workspaceId, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CheckMigrationCutover Check reservations before switching
//
// Send every upcoming reservation the destination holds; get back what is missing, extra, or on different dates compared with the source. Read-only — run it as often as you like before cutover.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/migrations/{workspaceId}/cutover-check (the `CheckMigrationCutover` operationId).
func (c *Client) CheckMigrationCutover(ctx context.Context, workspaceId int, body CheckMigrationCutoverJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCheckMigrationCutoverRequest(c.Server, workspaceId, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RunMigrationImportWithBody Run the import again
//
// Queue another import from the source PMS — for example messages after the first pass, or only reservations changed since a date. Progress shows on `GET /v1/migrations/{workspaceId}`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/migrations/{workspaceId}/import (the `RunMigrationImport` operationId).
func (c *Client) RunMigrationImportWithBody(ctx context.Context, workspaceId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRunMigrationImportRequestWithBody(c.Server, workspaceId, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RunMigrationImport Run the import again
//
// Queue another import from the source PMS — for example messages after the first pass, or only reservations changed since a date. Progress shows on `GET /v1/migrations/{workspaceId}`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/migrations/{workspaceId}/import (the `RunMigrationImport` operationId).
func (c *Client) RunMigrationImport(ctx context.Context, workspaceId int, body RunMigrationImportJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRunMigrationImportRequest(c.Server, workspaceId, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetMigrationReport Get a migration report
//
// What came across and what needs a decision in the destination: properties without an address, upcoming reservations with no guest contact, channel reservations that must not be re-created, and anything the source PMS cannot carry. Includes the source's capability matrix.
//
// Corresponds with GET /v1/migrations/{workspaceId}/report (the `GetMigrationReport` operationId).
func (c *Client) GetMigrationReport(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetMigrationReportRequest(c.Server, workspaceId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListProperties List properties
//
// Cursor-paginated list of properties for the authenticated workspace. Walk pages with `?cursor=<pagination.nextCursor>`; stop when `pagination.hasMore` is `false`. Cursor is opaque base64 — do not parse it.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// Filters: `q` (substring on name/street/city), `status` (active|inactive|all), `lifecycle_status` (exact match on the listing's lifecycle state). Other unknown params (e.g. `?search=` or `?propertyId=`) are rejected with 422 — no silent unfiltered results.
//
// **Incremental sync (only changes since last poll):** pass `?updated_since=<ISO8601>` to receive only properties changed at or after that instant. Each property carries `updatedAt` — the last row of the final page is your next watermark. `updated_since` changes the page ordering to `updatedAt ASC, id ASC` (and the cursor with it); see the parameter description. `GET /v1/listings` does NOT yet accept `updated_since` — use this endpoint for property-side incremental sync.
//
// **Inactive properties:** an inactive property keeps syncing, but cannot be read or changed through the API until it is activated. They are only listed when `status` asks for them, and then with `id`, `name`, `status`, `lifecycleStatus`, `channels` and `updatedAt` only — enough to choose what to activate with `PATCH /v1/listings/{id}`.
//
// Corresponds with GET /v1/properties (the `ListProperties` operationId).
func (c *Client) ListProperties(ctx context.Context, params *ListPropertiesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListPropertiesRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetProperty Get property details
//
// Fetch a single property by Repull id. Property ids are workspace-scoped — an id from one workspace is not valid in another. 404 means the id does not exist OR belongs to a different workspace.
//
// **Optional expansions:** Pass `?include=amenities` to enrich the response with the property's amenities (sourced from the unified `listings_amenities` table). Returns `[]` when the property has no amenity rows. The default response stays lean; consumers must opt in.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/properties/{id} (the `GetProperty` operationId).
func (c *Client) GetProperty(ctx context.Context, id int, params *GetPropertyParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetPropertyRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetQuote Price a stay
//
// Returns the full price breakdown for a stay — nightly total, length-of-stay discount, cleaning fee, pet and other fees, taxes, and the total.
//
// A quote is priced against a booking website, because the markup, custom fees and tax overrides that decide what a guest is actually charged live there. A workspace with no booking site receives `422 quote_unavailable` rather than a number computed from different rules than the ones applied at checkout.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/quotes (the `GetQuote` operationId).
func (c *Client) GetQuote(ctx context.Context, params *GetQuoteParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetQuoteRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListReservations List reservations
//
// Cursor-paginated list of reservations across all connected PMS platforms. Filter by platform, status, listing, or check-in date range.
//
// **Pagination:** Walk pages with `?cursor=` — pass `pagination.nextCursor` from one response back as `?cursor=` on the next request. Stop when `pagination.hasMore` is `false`. `limit` defaults to 50, max 100; requesting more returns 422 (no silent truncation).
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. For deep pagination cursor remains O(1) per page; offset > 10000 returns 422 with a docs link.
//
// **Incremental sync (only changes since last poll):** pass `?updated_since=<ISO8601>` to receive only reservations amended, cancelled, or created at or after that instant — no full re-walk. Each row carries `updatedAt`; the last row of the final page is your next watermark. Note that `updated_since` changes the page ordering to `updatedAt ASC, id ASC` (and the cursor with it) so mid-walk amendments cannot be skipped — see the parameter description for the full contract.
//
// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
//
// Corresponds with GET /v1/reservations (the `ListReservations` operationId).
func (c *Client) ListReservations(ctx context.Context, params *ListReservationsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListReservationsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateReservationWithBody Create a reservation
//
// Creates a reservation and everything that hangs off one: the guest, the conversation thread, the dashboard item, the calendar block, and the `reservation.created` fan-out that issues the door code and starts the messaging automations.
//
// **Platform is restricted to `direct`, `website` and `owner`.** Reservations on Airbnb, Booking.com and Vrbo are owned by the channel and arrive through sync — creating one here would mint a local booking the channel has never heard of, which then fights the next sync. Create those on the channel.
//
// **Dates are validated** (`YYYY-MM-DD`, and `checkOut` must be after `checkIn`), and an unrecognised field is rejected by name rather than silently ignored.
//
// **This endpoint does not set the price.** There is no `totalPrice` field: the reservation pipeline derives the price breakdown from the property's own rates and overwrites anything supplied, so accepting a total would be taking a value and discarding it. A reservation created here is priced by that engine (`0` when the property has no rates for the range). `currency` IS honoured. Quote a stay with `GET /v1/quotes` before booking if you need the figure up front.
//
// **Availability is NOT checked.** This creates the reservation you asked for even if the dates overlap an existing booking. Call `GET /v1/availability/{propertyId}` first if that matters.
//
// Send `Idempotency-Key` — a network timeout here is exactly the case it exists for: without it, a retry books the guest twice.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/reservations (the `CreateReservation` operationId).
func (c *Client) CreateReservationWithBody(ctx context.Context, params *CreateReservationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateReservationRequestWithBody(c.Server, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateReservation Create a reservation
//
// Creates a reservation and everything that hangs off one: the guest, the conversation thread, the dashboard item, the calendar block, and the `reservation.created` fan-out that issues the door code and starts the messaging automations.
//
// **Platform is restricted to `direct`, `website` and `owner`.** Reservations on Airbnb, Booking.com and Vrbo are owned by the channel and arrive through sync — creating one here would mint a local booking the channel has never heard of, which then fights the next sync. Create those on the channel.
//
// **Dates are validated** (`YYYY-MM-DD`, and `checkOut` must be after `checkIn`), and an unrecognised field is rejected by name rather than silently ignored.
//
// **This endpoint does not set the price.** There is no `totalPrice` field: the reservation pipeline derives the price breakdown from the property's own rates and overwrites anything supplied, so accepting a total would be taking a value and discarding it. A reservation created here is priced by that engine (`0` when the property has no rates for the range). `currency` IS honoured. Quote a stay with `GET /v1/quotes` before booking if you need the figure up front.
//
// **Availability is NOT checked.** This creates the reservation you asked for even if the dates overlap an existing booking. Call `GET /v1/availability/{propertyId}` first if that matters.
//
// Send `Idempotency-Key` — a network timeout here is exactly the case it exists for: without it, a retry books the guest twice.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/reservations (the `CreateReservation` operationId).
func (c *Client) CreateReservation(ctx context.Context, params *CreateReservationParams, body CreateReservationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateReservationRequest(c.Server, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetReservation Get reservation details
//
// Returns the full record for a single reservation, scoped to the authenticated workspace. Response shape is identical to a single row in `GET /v1/reservations` so SDK consumers can use the same type for both. Returns **404** if the id does not exist OR belongs to a different workspace — the API never differentiates the two so caller can't enumerate other workspaces' ids.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Corresponds with GET /v1/reservations/{id} (the `GetReservation` operationId).
func (c *Client) GetReservation(ctx context.Context, id int, params *GetReservationParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetReservationRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateReservationWithBody Update a reservation
//
// Changes the dates, the occupancy, or the unit. Drives the same command path the dashboard does, so the side effects come with it: the change audit is appended, bound task due dates re-sync, the old calendar dates unblock and the new ones block, the conversation's cached listing is invalidated, and `reservation.updated` fires — which is what revokes and re-issues the door code.
//
// Supply at least one field; an empty body returns 422 rather than a 200 that changed nothing.
//
// **Moving and re-dating in one call is one operation.** Send `listingId` together with `checkIn`/`checkOut` and it is applied as a single move, so the access code is re-issued once rather than twice.
//
// ### Fields this endpoint deliberately does NOT accept
//
// Each is rejected by name with the reason, never accepted and ignored:
//
// | Field | Why |
// |---|---|
// | `guest` / `guestDetails` | Guest name, email and phone live on the guest record. The underlying command has no branch for them, so accepting them would return a success that changed nothing. |
// | `pricing` / `totalPrice` / `currency` | Repricing writes the price breakdown, the pricing row and a pricing-history entry. It belongs to its own endpoint. |
// | `status` | Not a field. Cancelling, confirming and checking out are separate operations with materially different side effects — cancellation issues a credit refund and revokes access codes. |
// | `platform` | Immutable: it records where the booking actually originated. |
// | `notes` | `internal_notes` is an append-only audit trail the system writes on every change. |
//
// **Availability is NOT checked.** A date change that overlaps another booking will be written. Call `GET /v1/availability/{propertyId}` first if that matters.
//
// Returns `403 listing_inactive` when the reservation is on an inactive listing, or when a `listingId` move targets one; nothing is changed.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /v1/reservations/{id} (the `UpdateReservation` operationId).
func (c *Client) UpdateReservationWithBody(ctx context.Context, id int, params *UpdateReservationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateReservationRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateReservation Update a reservation
//
// Changes the dates, the occupancy, or the unit. Drives the same command path the dashboard does, so the side effects come with it: the change audit is appended, bound task due dates re-sync, the old calendar dates unblock and the new ones block, the conversation's cached listing is invalidated, and `reservation.updated` fires — which is what revokes and re-issues the door code.
//
// Supply at least one field; an empty body returns 422 rather than a 200 that changed nothing.
//
// **Moving and re-dating in one call is one operation.** Send `listingId` together with `checkIn`/`checkOut` and it is applied as a single move, so the access code is re-issued once rather than twice.
//
// ### Fields this endpoint deliberately does NOT accept
//
// Each is rejected by name with the reason, never accepted and ignored:
//
// | Field | Why |
// |---|---|
// | `guest` / `guestDetails` | Guest name, email and phone live on the guest record. The underlying command has no branch for them, so accepting them would return a success that changed nothing. |
// | `pricing` / `totalPrice` / `currency` | Repricing writes the price breakdown, the pricing row and a pricing-history entry. It belongs to its own endpoint. |
// | `status` | Not a field. Cancelling, confirming and checking out are separate operations with materially different side effects — cancellation issues a credit refund and revokes access codes. |
// | `platform` | Immutable: it records where the booking actually originated. |
// | `notes` | `internal_notes` is an append-only audit trail the system writes on every change. |
//
// **Availability is NOT checked.** A date change that overlaps another booking will be written. Call `GET /v1/availability/{propertyId}` first if that matters.
//
// Returns `403 listing_inactive` when the reservation is on an inactive listing, or when a `listingId` move targets one; nothing is changed.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /v1/reservations/{id} (the `UpdateReservation` operationId).
func (c *Client) UpdateReservation(ctx context.Context, id int, params *UpdateReservationParams, body UpdateReservationJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateReservationRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// AcceptReservationRequest Accept a booking request
//
// Accept a pending Airbnb booking request — a reservation with status `pending`, made on a listing without Instant Book. Find them with `GET /v1/reservations?status=pending`. Airbnb expires a request the host has not answered within 24 hours.
//
// Airbnb confirms asynchronously: the reservation’s status moves to confirmed, and a `reservation.updated` webhook fires, when Airbnb’s notification lands (usually within seconds). The response reports what Airbnb was asked to do.
//
// **Airbnb only**, and only for listings connected to Airbnb directly: other channels have no request step (`422 channel_not_supported`). A reservation that is not pending is refused before Airbnb is contacted (`409 reservation_not_pending`); one Airbnb says already moved on is `409 request_no_longer_pending`. Neither is worth retrying.
//
// Takes no body.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Corresponds with POST /v1/reservations/{id}/accept (the `AcceptReservationRequest` operationId).
func (c *Client) AcceptReservationRequest(ctx context.Context, id int, params *AcceptReservationRequestParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewAcceptReservationRequestRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeclineReservationRequestWithBody Decline a booking request
//
// Decline a pending Airbnb booking request (a reservation with status `pending`; find them with `GET /v1/reservations?status=pending`).
//
// `reason` must be one of Airbnb’s own decline reasons. `message` is required: Airbnb sends it to the guest with the decline (at most 500 characters). It is not defaulted — a canned message would put words in your mouth.
//
// Airbnb confirms asynchronously; the reservation’s status moves, and `reservation.updated` fires, when its notification lands. Same channel and status rules as `POST /v1/reservations/{id}/accept`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/reservations/{id}/decline (the `DeclineReservationRequest` operationId).
func (c *Client) DeclineReservationRequestWithBody(ctx context.Context, id int, params *DeclineReservationRequestParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeclineReservationRequestRequestWithBody(c.Server, id, params, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeclineReservationRequest Decline a booking request
//
// Decline a pending Airbnb booking request (a reservation with status `pending`; find them with `GET /v1/reservations?status=pending`).
//
// `reason` must be one of Airbnb’s own decline reasons. `message` is required: Airbnb sends it to the guest with the decline (at most 500 characters). It is not defaulted — a canned message would put words in your mouth.
//
// Airbnb confirms asynchronously; the reservation’s status moves, and `reservation.updated` fires, when its notification lands. Same channel and status rules as `POST /v1/reservations/{id}/accept`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/reservations/{id}/decline (the `DeclineReservationRequest` operationId).
func (c *Client) DeclineReservationRequest(ctx context.Context, id int, params *DeclineReservationRequestParams, body DeclineReservationRequestJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeclineReservationRequestRequest(c.Server, id, params, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListReviews List reviews
//
// Cursor-paginated guest + host review stream for the workspace. Backed by main vanio's unified `reviews` table (populated by per-channel backfill crons), so this surface returns the complete cross-channel history — separate from `/v1/channels/airbnb/reviews` which hits Airbnb live.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// Filters: `platform` (`airbnb`|`booking`|`vrbo`), `listing_id` (internal Repull listing id), `rating_min` / `rating_max` (inclusive bounds, 0..5), `status` (`responded`|`unanswered`|`all`), `reviewer_role` (`guest` (default) | `host` | `all`).
//
// **Inactive listings:** reviews of inactive listings are left out of the page and of `pagination.total`. Filtering by an inactive `listing_id` returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
//
// Corresponds with GET /v1/reviews (the `ListReviews` operationId).
func (c *Client) ListReviews(ctx context.Context, params *ListReviewsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListReviewsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetReview Get review
//
// Returns one review (the bare `Review` object — NOT wrapped in `{ data: ... }`). Scoped to the authenticated workspace via the listings join — reviews that don't belong to the workspace return 404 (we don't differentiate to avoid leaking other customers' ids).
//
// A review of an inactive listing returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
//
// Corresponds with GET /v1/reviews/{id} (the `GetReview` operationId).
func (c *Client) GetReview(ctx context.Context, id int, params *GetReviewParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetReviewRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReplyToReviewWithBody Reply to a review on any channel
//
// Resolves the review, reads its channel and dispatches the reply. Channel-neutral: you do not need to know where the review came from.
//
// Replies are available on Airbnb today; a review from a channel without a reply API returns `422 unsupported_channel` naming the channels that do work.
//
// **Inactive listings:** a review of an inactive listing returns `403 listing_inactive` and no reply reaches the channel. Activate the listing first.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/reviews/{id}/reply (the `ReplyToReview` operationId).
func (c *Client) ReplyToReviewWithBody(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReplyToReviewRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReplyToReview Reply to a review on any channel
//
// Resolves the review, reads its channel and dispatches the reply. Channel-neutral: you do not need to know where the review came from.
//
// Replies are available on Airbnb today; a review from a channel without a reply API returns `422 unsupported_channel` naming the channels that do work.
//
// **Inactive listings:** a review of an inactive listing returns `403 listing_inactive` and no reply reaches the channel. Activate the listing first.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/reviews/{id}/reply (the `ReplyToReview` operationId).
func (c *Client) ReplyToReview(ctx context.Context, id int, body ReplyToReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReplyToReviewRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListCustomSchemas List custom schemas
//
// Returns every custom schema owned by the workspace, including inactive ones.
//
// Corresponds with GET /v1/schema/custom (the `ListCustomSchemas` operationId).
func (c *Client) ListCustomSchemas(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListCustomSchemasRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateCustomSchemaWithBody Create a custom schema
//
// Create a workspace-scoped field-mapping schema. The schema reshapes the `native` response payload into your app's preferred field names. After creation, set `X-Schema: <name>` on any read endpoint to apply it.
//
// **Reserved names:** `calry`, `calry-v1`, `native` are built-in schemas and cannot be used as a custom name.
//
// **Mapping safety:** Each mapping value is parsed by an internal expression engine — `eval`, `Function`, `process`, and other unsafe keywords are rejected up front. Field names are capped at 100 chars and expressions at 500 chars.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/schema/custom (the `CreateCustomSchema` operationId).
func (c *Client) CreateCustomSchemaWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateCustomSchemaRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateCustomSchema Create a custom schema
//
// Create a workspace-scoped field-mapping schema. The schema reshapes the `native` response payload into your app's preferred field names. After creation, set `X-Schema: <name>` on any read endpoint to apply it.
//
// **Reserved names:** `calry`, `calry-v1`, `native` are built-in schemas and cannot be used as a custom name.
//
// **Mapping safety:** Each mapping value is parsed by an internal expression engine — `eval`, `Function`, `process`, and other unsafe keywords are rejected up front. Field names are capped at 100 chars and expressions at 500 chars.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/schema/custom (the `CreateCustomSchema` operationId).
func (c *Client) CreateCustomSchema(ctx context.Context, body CreateCustomSchemaJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateCustomSchemaRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteCustomSchema Delete a custom schema
//
// Hard-delete a custom schema. Subsequent requests carrying its name in `X-Schema` fall back to `native`. There is no undelete.
//
// Corresponds with DELETE /v1/schema/custom/{id} (the `DeleteCustomSchema` operationId).
func (c *Client) DeleteCustomSchema(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteCustomSchemaRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetCustomSchema Get a custom schema
//
// Fetch a single custom schema by id. Scoped to the authenticated workspace — schemas that belong to other workspaces return 404.
//
// Corresponds with GET /v1/schema/custom/{id} (the `GetCustomSchema` operationId).
func (c *Client) GetCustomSchema(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetCustomSchemaRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateCustomSchemaWithBody Update a custom schema
//
// Patch the description, mappings, or active flag of a custom schema. The schema `name` is immutable — create a new schema and migrate consumers if you need to rename. Mapping updates are revalidated for safety.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /v1/schema/custom/{id} (the `UpdateCustomSchema` operationId).
func (c *Client) UpdateCustomSchemaWithBody(ctx context.Context, id openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateCustomSchemaRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateCustomSchema Update a custom schema
//
// Patch the description, mappings, or active flag of a custom schema. The schema `name` is immutable — create a new schema and migrate consumers if you need to rename. Mapping updates are revalidated for safety.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /v1/schema/custom/{id} (the `UpdateCustomSchema` operationId).
func (c *Client) UpdateCustomSchema(ctx context.Context, id openapi_types.UUID, body UpdateCustomSchemaJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateCustomSchemaRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetUsageLogs List API request logs
//
// Cursor-paginated raw API request log for the authenticated workspace, newest first. Filter by time `range`, `operation` id(s), status class, or free-text `q`. Walk pages with `cursor` from `pagination.nextCursor` until `pagination.hasMore` is `false`; `offset` is accepted as a shallow alias (deep walks must use `cursor`).
//
// Corresponds with GET /v1/usage/logs (the `GetUsageLogs` operationId).
func (c *Client) GetUsageLogs(ctx context.Context, params *GetUsageLogsParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetUsageLogsRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetUsageSummary Get usage summary
//
// Aggregated usage over the requested `range` — tier + plan limits, quota used/remaining, next reset, a per-operation breakdown (request/error counts, error rate, avg latency), a daily timeline, status-class distribution, and range totals. Two request quotas are reported and they reset at different times: `dailyRequests` is the daily circuit breaker that stops runaway client loops (resets at `dailyResetsAt`, the next UTC midnight) and `monthlyRequests` is the billing quota (resets at `resetsAt`). `null` limits mean unlimited on that dimension.
//
// Corresponds with GET /v1/usage/summary (the `GetUsageSummary` operationId).
func (c *Client) GetUsageSummary(ctx context.Context, params *GetUsageSummaryParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetUsageSummaryRequest(c.Server, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetUsageTier Get tier and quota
//
// Lightweight current-tier snapshot for status badges and quota meters — plan limits (daily requests, monthly requests, daily AI requests, dynamic-pricing listings), the amount used, the amount remaining, and the next reset. Two request quotas are reported and they reset at different times: `dailyRequests` is the daily circuit breaker that stops runaway client loops (resets at `dailyResetsAt`, the next UTC midnight) and `monthlyRequests` is the billing quota (resets at `resetsAt`). Exceeding the daily cap returns 429 `daily_limit_exceeded`; exceeding the monthly one returns 429 `rate_limit_exceeded`. `null` limits mean unlimited on that dimension.
//
// Corresponds with GET /v1/usage/tier (the `GetUsageTier` operationId).
func (c *Client) GetUsageTier(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetUsageTierRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListWebhooks List webhook subscriptions
//
// List every webhook subscription registered for this workspace. Each row includes the destination URL, subscribed event types, and the most recent delivery summary.
//
// Corresponds with GET /v1/webhooks (the `ListWebhooks` operationId).
func (c *Client) ListWebhooks(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListWebhooksRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateWebhookWithBody Create webhook subscription
//
// Register a new endpoint. Returns the plaintext signing secret ONCE — capture it from the response and store it securely. After this call the secret is masked everywhere; mint a new one with `POST /v1/webhooks/{id}/rotate-secret` if you lose it. See `GET /v1/webhooks/event-types` for the full list of subscribable events. Events about an inactive listing (reservations, messages, alterations, reviews, payments, calendar and listing events) are not delivered. The data keeps syncing while the listing is inactive, but its events are never sent — including after you reactivate it; webhooks resume for events that happen from reactivation on. Account-level events are always delivered.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/webhooks (the `CreateWebhook` operationId).
func (c *Client) CreateWebhookWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateWebhookRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// CreateWebhook Create webhook subscription
//
// Register a new endpoint. Returns the plaintext signing secret ONCE — capture it from the response and store it securely. After this call the secret is masked everywhere; mint a new one with `POST /v1/webhooks/{id}/rotate-secret` if you lose it. See `GET /v1/webhooks/event-types` for the full list of subscribable events. Events about an inactive listing (reservations, messages, alterations, reviews, payments, calendar and listing events) are not delivered. The data keeps syncing while the listing is inactive, but its events are never sent — including after you reactivate it; webhooks resume for events that happen from reactivation on. Account-level events are always delivered.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/webhooks (the `CreateWebhook` operationId).
func (c *Client) CreateWebhook(ctx context.Context, body CreateWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewCreateWebhookRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListWebhookEventTypes List webhook event types
//
// The canonical catalog of every event the API can deliver, grouped by domain, with realistic sample payloads.
//
// Corresponds with GET /v1/webhooks/event-types (the `ListWebhookEventTypes` operationId).
func (c *Client) ListWebhookEventTypes(ctx context.Context, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListWebhookEventTypesRequest(c.Server)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// TestWebhookWithBody [Legacy] Send test webhook to a URL
//
// Deprecated: prefer creating a subscription then calling `POST /v1/webhooks/{id}/ping`. Kept for back-compat.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/webhooks/test (the `TestWebhook` operationId).
func (c *Client) TestWebhookWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewTestWebhookRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// TestWebhook [Legacy] Send test webhook to a URL
//
// Deprecated: prefer creating a subscription then calling `POST /v1/webhooks/{id}/ping`. Kept for back-compat.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/webhooks/test (the `TestWebhook` operationId).
func (c *Client) TestWebhook(ctx context.Context, body TestWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewTestWebhookRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// DeleteWebhook Delete webhook subscription
//
// Delete a webhook subscription. In-flight deliveries already on the queue are still attempted; new events stop firing immediately.
//
// Corresponds with DELETE /v1/webhooks/{id} (the `DeleteWebhook` operationId).
func (c *Client) DeleteWebhook(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewDeleteWebhookRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetWebhook Get webhook subscription
//
// Fetch a single webhook subscription by id. Use the `deliveries` sub-resource to list recent attempts, and `ping` to send a test event.
//
// Corresponds with GET /v1/webhooks/{id} (the `GetWebhook` operationId).
func (c *Client) GetWebhook(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetWebhookRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateWebhookWithBody Update webhook subscription
//
// Update url, description, events, or status (active|paused). Re-enabling clears `consecutiveFailures` and `disabledAt`.
//
// Takes any type of body and a specified content type.
//
// Corresponds with PATCH /v1/webhooks/{id} (the `UpdateWebhook` operationId).
func (c *Client) UpdateWebhookWithBody(ctx context.Context, id openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateWebhookRequestWithBody(c.Server, id, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// UpdateWebhook Update webhook subscription
//
// Update url, description, events, or status (active|paused). Re-enabling clears `consecutiveFailures` and `disabledAt`.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with PATCH /v1/webhooks/{id} (the `UpdateWebhook` operationId).
func (c *Client) UpdateWebhook(ctx context.Context, id openapi_types.UUID, body UpdateWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewUpdateWebhookRequest(c.Server, id, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ListWebhookDeliveries List webhook deliveries
//
// Cursor-paginated history of every delivery attempt for this subscription. Walk pages with `?cursor=<pagination.nextCursor>`; stop when `pagination.hasMore` is `false`. The cursor is opaque base64 — do not parse it. `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — mutually exclusive with `cursor`.
//
// Corresponds with GET /v1/webhooks/{id}/deliveries (the `ListWebhookDeliveries` operationId).
func (c *Client) ListWebhookDeliveries(ctx context.Context, id openapi_types.UUID, params *ListWebhookDeliveriesParams, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewListWebhookDeliveriesRequest(c.Server, id, params)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// GetWebhookDelivery Get webhook delivery
//
// Full request + response capture for one delivery attempt.
//
// Corresponds with GET /v1/webhooks/{id}/deliveries/{delivery_id} (the `GetWebhookDelivery` operationId).
func (c *Client) GetWebhookDelivery(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewGetWebhookDeliveryRequest(c.Server, id, deliveryId)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReplayWebhookDeliveryWithBody Replay webhook delivery
//
// Re-sends the original payload (same eventId, fresh deliveryId, attempt + 1).
//
// A delivery may be replayed at most **3 times per rolling 60 minutes**; the 4th inside that window answers `409 replay_limit_reached` and names the time the next one is allowed. The limit is charged to the original delivery, so replaying the delivery a replay produced draws on the same budget. It is not a lifetime cap — a delivery that has not been replayed for an hour starts fresh.
//
// A delivery your endpoint already accepted is not re-sent (it would be a duplicate) and answers `409 delivery_already_succeeded`; send `{"force": true}` to replay it anyway, which still counts against the limit.
//
// A delivery about a listing that is inactive now is not re-sent and answers `403 listing_inactive`; activate the listing first.
//
// Takes any type of body and a specified content type.
//
// Corresponds with POST /v1/webhooks/{id}/deliveries/{delivery_id}/replay (the `ReplayWebhookDelivery` operationId).
func (c *Client) ReplayWebhookDeliveryWithBody(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReplayWebhookDeliveryRequestWithBody(c.Server, id, deliveryId, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// ReplayWebhookDelivery Replay webhook delivery
//
// Re-sends the original payload (same eventId, fresh deliveryId, attempt + 1).
//
// A delivery may be replayed at most **3 times per rolling 60 minutes**; the 4th inside that window answers `409 replay_limit_reached` and names the time the next one is allowed. The limit is charged to the original delivery, so replaying the delivery a replay produced draws on the same budget. It is not a lifetime cap — a delivery that has not been replayed for an hour starts fresh.
//
// A delivery your endpoint already accepted is not re-sent (it would be a duplicate) and answers `409 delivery_already_succeeded`; send `{"force": true}` to replay it anyway, which still counts against the limit.
//
// A delivery about a listing that is inactive now is not re-sent and answers `403 listing_inactive`; activate the listing first.
//
// Takes a body of the `application/json` content type.
//
// Corresponds with POST /v1/webhooks/{id}/deliveries/{delivery_id}/replay (the `ReplayWebhookDelivery` operationId).
func (c *Client) ReplayWebhookDelivery(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, body ReplayWebhookDeliveryJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewReplayWebhookDeliveryRequest(c.Server, id, deliveryId, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// PingWebhook Send ping event
//
// Fires a synthetic `repull.ping` at the subscription URL and returns the full delivery cycle inline. Used by dashboards and health-check probes.
//
// Corresponds with POST /v1/webhooks/{id}/ping (the `PingWebhook` operationId).
func (c *Client) PingWebhook(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPingWebhookRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// RotateWebhookSecret Rotate signing secret
//
// Mints a fresh signing secret and returns the plaintext ONCE. After this response the secret is masked everywhere — capture and store it now.
//
// Corresponds with POST /v1/webhooks/{id}/rotate-secret (the `RotateWebhookSecret` operationId).
func (c *Client) RotateWebhookSecret(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewRotateWebhookSecretRequest(c.Server, id)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// TestFireWebhook Send test event of a specific type
//
// Delivers a realistic fixture payload of the requested event type to the subscription URL.
//
// Corresponds with POST /v1/webhooks/{id}/test/{event_type} (the `TestFireWebhook` operationId).
func (c *Client) TestFireWebhook(ctx context.Context, id openapi_types.UUID, eventType WebhookEventType, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewTestFireWebhookRequest(c.Server, id, eventType)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// NewBatchUpdateAvailabilityRequest calls the generic BatchUpdateAvailability builder with application/json body
func NewBatchUpdateAvailabilityRequest(server string, body BatchUpdateAvailabilityJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewBatchUpdateAvailabilityRequestWithBody(server, "application/json", bodyReader)
}

// NewBatchUpdateAvailabilityRequestWithBody constructs an http.Request for the BatchUpdateAvailability method, with any body, and a specified content type
func NewBatchUpdateAvailabilityRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/availability/batch")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetAvailabilityRequest constructs an http.Request for the GetAvailability method
func NewGetAvailabilityRequest(server string, propertyId int, params *GetAvailabilityParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "propertyId", propertyId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/availability/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "from", params.From, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "to", params.To, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAvailabilityRequest calls the generic UpdateAvailability builder with application/json body
func NewUpdateAvailabilityRequest(server string, propertyId int, body UpdateAvailabilityJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAvailabilityRequestWithBody(server, propertyId, "application/json", bodyReader)
}

// NewUpdateAvailabilityRequestWithBody constructs an http.Request for the UpdateAvailability method, with any body, and a specified content type
func NewUpdateAvailabilityRequestWithBody(server string, propertyId int, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "propertyId", propertyId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/availability/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewCreateBillingCheckoutRequest calls the generic CreateBillingCheckout builder with application/json body
func NewCreateBillingCheckoutRequest(server string, body CreateBillingCheckoutJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateBillingCheckoutRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateBillingCheckoutRequestWithBody constructs an http.Request for the CreateBillingCheckout method, with any body, and a specified content type
func NewCreateBillingCheckoutRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/billing")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListAirbnbAlterationsRequest constructs an http.Request for the ListAirbnbAlterations method
func NewListAirbnbAlterationsRequest(server string, params *ListAirbnbAlterationsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/alterations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.AccountId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "account_id", *params.AccountId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Type != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "type", *params.Type, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.ReservationCode != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "reservation_code", *params.ReservationCode, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateAirbnbAlterationRequest calls the generic CreateAirbnbAlteration builder with application/json body
func NewCreateAirbnbAlterationRequest(server string, body CreateAirbnbAlterationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateAirbnbAlterationRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateAirbnbAlterationRequestWithBody constructs an http.Request for the CreateAirbnbAlteration method, with any body, and a specified content type
func NewCreateAirbnbAlterationRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/alterations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetAirbnbAlterationRequest constructs an http.Request for the GetAirbnbAlteration method
func NewGetAirbnbAlterationRequest(server string, id string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/alterations/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewAcceptAirbnbAlterationRequest calls the generic AcceptAirbnbAlteration builder with application/json body
func NewAcceptAirbnbAlterationRequest(server string, id string, body AcceptAirbnbAlterationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewAcceptAirbnbAlterationRequestWithBody(server, id, "application/json", bodyReader)
}

// NewAcceptAirbnbAlterationRequestWithBody constructs an http.Request for the AcceptAirbnbAlteration method, with any body, and a specified content type
func NewAcceptAirbnbAlterationRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/alterations/%s/accept", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewCancelAirbnbAlterationRequest calls the generic CancelAirbnbAlteration builder with application/json body
func NewCancelAirbnbAlterationRequest(server string, id string, body CancelAirbnbAlterationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCancelAirbnbAlterationRequestWithBody(server, id, "application/json", bodyReader)
}

// NewCancelAirbnbAlterationRequestWithBody constructs an http.Request for the CancelAirbnbAlteration method, with any body, and a specified content type
func NewCancelAirbnbAlterationRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/alterations/%s/cancel", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeclineAirbnbAlterationRequest calls the generic DeclineAirbnbAlteration builder with application/json body
func NewDeclineAirbnbAlterationRequest(server string, id string, body DeclineAirbnbAlterationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewDeclineAirbnbAlterationRequestWithBody(server, id, "application/json", bodyReader)
}

// NewDeclineAirbnbAlterationRequestWithBody constructs an http.Request for the DeclineAirbnbAlteration method, with any body, and a specified content type
func NewDeclineAirbnbAlterationRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/alterations/%s/decline", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetAirbnbConnectionRequest constructs an http.Request for the GetAirbnbConnection method
func NewGetAirbnbConnectionRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/connection")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListAirbnbListingsRequest constructs an http.Request for the ListAirbnbListings method
func NewListAirbnbListingsRequest(server string, params *ListAirbnbListingsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.AccountId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "account_id", *params.AccountId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Include != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include", *params.Include, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewMapAirbnbListingRequest calls the generic MapAirbnbListing builder with application/json body
func NewMapAirbnbListingRequest(server string, body MapAirbnbListingJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewMapAirbnbListingRequestWithBody(server, "application/json", bodyReader)
}

// NewMapAirbnbListingRequestWithBody constructs an http.Request for the MapAirbnbListing method, with any body, and a specified content type
func NewMapAirbnbListingRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/map")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetAirbnbListingRequest constructs an http.Request for the GetAirbnbListing method
func NewGetAirbnbListingRequest(server string, id string, params *GetAirbnbListingParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Include != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include", *params.Include, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewAirbnbListingActionRequest calls the generic AirbnbListingAction builder with application/json body
func NewAirbnbListingActionRequest(server string, id string, params *AirbnbListingActionParams, body AirbnbListingActionJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewAirbnbListingActionRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewAirbnbListingActionRequestWithBody constructs an http.Request for the AirbnbListingAction method, with any body, and a specified content type
func NewAirbnbListingActionRequestWithBody(server string, id string, params *AirbnbListingActionParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewListAirbnbListingAmenitiesRequest constructs an http.Request for the ListAirbnbListingAmenities method
func NewListAirbnbListingAmenitiesRequest(server string, id string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/amenities", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAirbnbListingAmenitiesRequest calls the generic UpdateAirbnbListingAmenities builder with application/json body
func NewUpdateAirbnbListingAmenitiesRequest(server string, id string, body UpdateAirbnbListingAmenitiesJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAirbnbListingAmenitiesRequestWithBody(server, id, "application/json", bodyReader)
}

// NewUpdateAirbnbListingAmenitiesRequestWithBody constructs an http.Request for the UpdateAirbnbListingAmenities method, with any body, and a specified content type
func NewUpdateAirbnbListingAmenitiesRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/amenities", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetAirbnbListingAvailabilityRequest constructs an http.Request for the GetAirbnbListingAvailability method
func NewGetAirbnbListingAvailabilityRequest(server string, id string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/availability", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAirbnbListingAvailabilityRequest calls the generic UpdateAirbnbListingAvailability builder with application/json body
func NewUpdateAirbnbListingAvailabilityRequest(server string, id string, body UpdateAirbnbListingAvailabilityJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAirbnbListingAvailabilityRequestWithBody(server, id, "application/json", bodyReader)
}

// NewUpdateAirbnbListingAvailabilityRequestWithBody constructs an http.Request for the UpdateAirbnbListingAvailability method, with any body, and a specified content type
func NewUpdateAirbnbListingAvailabilityRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/availability", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetAirbnbBookingSettingsRequest constructs an http.Request for the GetAirbnbBookingSettings method
func NewGetAirbnbBookingSettingsRequest(server string, id string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/booking-settings", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAirbnbBookingSettingsRequest calls the generic UpdateAirbnbBookingSettings builder with application/json body
func NewUpdateAirbnbBookingSettingsRequest(server string, id string, body UpdateAirbnbBookingSettingsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAirbnbBookingSettingsRequestWithBody(server, id, "application/json", bodyReader)
}

// NewUpdateAirbnbBookingSettingsRequestWithBody constructs an http.Request for the UpdateAirbnbBookingSettings method, with any body, and a specified content type
func NewUpdateAirbnbBookingSettingsRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/booking-settings", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetAirbnbCheckinGuideRequest constructs an http.Request for the GetAirbnbCheckinGuide method
func NewGetAirbnbCheckinGuideRequest(server string, id string, params *GetAirbnbCheckinGuideParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/checkin-guide", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Locale != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "locale", *params.Locale, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAirbnbCheckinGuideRequest constructs an http.Request for the UpdateAirbnbCheckinGuide method
func NewUpdateAirbnbCheckinGuideRequest(server string, id string, params *UpdateAirbnbCheckinGuideParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/checkin-guide", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Locale != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "locale", *params.Locale, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetAirbnbCheckoutGuideRequest constructs an http.Request for the GetAirbnbCheckoutGuide method
func NewGetAirbnbCheckoutGuideRequest(server string, id string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/checkout-guide", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListAirbnbListingDescriptionsRequest constructs an http.Request for the ListAirbnbListingDescriptions method
func NewListAirbnbListingDescriptionsRequest(server string, id string, params *ListAirbnbListingDescriptionsParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/descriptions", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Locale != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "locale", *params.Locale, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Country != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "country", *params.Country, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAirbnbListingDescriptionRequest calls the generic UpdateAirbnbListingDescription builder with application/json body
func NewUpdateAirbnbListingDescriptionRequest(server string, id string, params *UpdateAirbnbListingDescriptionParams, body UpdateAirbnbListingDescriptionJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAirbnbListingDescriptionRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewUpdateAirbnbListingDescriptionRequestWithBody constructs an http.Request for the UpdateAirbnbListingDescription method, with any body, and a specified content type
func NewUpdateAirbnbListingDescriptionRequestWithBody(server string, id string, params *UpdateAirbnbListingDescriptionParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/descriptions", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewGetAirbnbListingDetailsRequest constructs an http.Request for the GetAirbnbListingDetails method
func NewGetAirbnbListingDetailsRequest(server string, id string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/details", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAirbnbListingDetailsRequest calls the generic UpdateAirbnbListingDetails builder with application/json body
func NewUpdateAirbnbListingDetailsRequest(server string, id string, params *UpdateAirbnbListingDetailsParams, body UpdateAirbnbListingDetailsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAirbnbListingDetailsRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewUpdateAirbnbListingDetailsRequestWithBody constructs an http.Request for the UpdateAirbnbListingDetails method, with any body, and a specified content type
func NewUpdateAirbnbListingDetailsRequestWithBody(server string, id string, params *UpdateAirbnbListingDetailsParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/details", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewListAirbnbListingPermitsRequest constructs an http.Request for the ListAirbnbListingPermits method
func NewListAirbnbListingPermitsRequest(server string, id string, params *ListAirbnbListingPermitsParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/permits", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Source != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "source", *params.Source, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAirbnbListingPermitsRequest calls the generic UpdateAirbnbListingPermits builder with application/json body
func NewUpdateAirbnbListingPermitsRequest(server string, id string, params *UpdateAirbnbListingPermitsParams, body UpdateAirbnbListingPermitsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAirbnbListingPermitsRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewUpdateAirbnbListingPermitsRequestWithBody constructs an http.Request for the UpdateAirbnbListingPermits method, with any body, and a specified content type
func NewUpdateAirbnbListingPermitsRequestWithBody(server string, id string, params *UpdateAirbnbListingPermitsParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/permits", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewDeleteAirbnbListingPhotoRequest constructs an http.Request for the DeleteAirbnbListingPhoto method
func NewDeleteAirbnbListingPhotoRequest(server string, id string, params *DeleteAirbnbListingPhotoParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/photos", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "photoId", params.PhotoId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListAirbnbListingPhotosRequest constructs an http.Request for the ListAirbnbListingPhotos method
func NewListAirbnbListingPhotosRequest(server string, id string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/photos", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAirbnbListingPhotoRequest calls the generic UpdateAirbnbListingPhoto builder with application/json body
func NewUpdateAirbnbListingPhotoRequest(server string, id string, body UpdateAirbnbListingPhotoJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAirbnbListingPhotoRequestWithBody(server, id, "application/json", bodyReader)
}

// NewUpdateAirbnbListingPhotoRequestWithBody constructs an http.Request for the UpdateAirbnbListingPhoto method, with any body, and a specified content type
func NewUpdateAirbnbListingPhotoRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/photos", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewUploadAirbnbListingPhotosRequest calls the generic UploadAirbnbListingPhotos builder with application/json body
func NewUploadAirbnbListingPhotosRequest(server string, id string, body UploadAirbnbListingPhotosJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUploadAirbnbListingPhotosRequestWithBody(server, id, "application/json", bodyReader)
}

// NewUploadAirbnbListingPhotosRequestWithBody constructs an http.Request for the UploadAirbnbListingPhotos method, with any body, and a specified content type
func NewUploadAirbnbListingPhotosRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/photos", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSetAirbnbListingCoverPhotoRequest calls the generic SetAirbnbListingCoverPhoto builder with application/json body
func NewSetAirbnbListingCoverPhotoRequest(server string, id string, body SetAirbnbListingCoverPhotoJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSetAirbnbListingCoverPhotoRequestWithBody(server, id, "application/json", bodyReader)
}

// NewSetAirbnbListingCoverPhotoRequestWithBody constructs an http.Request for the SetAirbnbListingCoverPhoto method, with any body, and a specified content type
func NewSetAirbnbListingCoverPhotoRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/photos/cover", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewReorderAirbnbListingPhotosRequest calls the generic ReorderAirbnbListingPhotos builder with application/json body
func NewReorderAirbnbListingPhotosRequest(server string, id string, body ReorderAirbnbListingPhotosJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewReorderAirbnbListingPhotosRequestWithBody(server, id, "application/json", bodyReader)
}

// NewReorderAirbnbListingPhotosRequestWithBody constructs an http.Request for the ReorderAirbnbListingPhotos method, with any body, and a specified content type
func NewReorderAirbnbListingPhotosRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/photos/order", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetAirbnbListingPricingRequest constructs an http.Request for the GetAirbnbListingPricing method
func NewGetAirbnbListingPricingRequest(server string, id string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/pricing", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAirbnbListingPricingRequest calls the generic UpdateAirbnbListingPricing builder with application/json body
func NewUpdateAirbnbListingPricingRequest(server string, id string, body UpdateAirbnbListingPricingJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAirbnbListingPricingRequestWithBody(server, id, "application/json", bodyReader)
}

// NewUpdateAirbnbListingPricingRequestWithBody constructs an http.Request for the UpdateAirbnbListingPricing method, with any body, and a specified content type
func NewUpdateAirbnbListingPricingRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/pricing", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetAirbnbListingQualityRequest constructs an http.Request for the GetAirbnbListingQuality method
func NewGetAirbnbListingQualityRequest(server string, id string, params *GetAirbnbListingQualityParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/quality", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Type != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "type", *params.Type, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewDeleteAirbnbListingRoomRequest constructs an http.Request for the DeleteAirbnbListingRoom method
func NewDeleteAirbnbListingRoomRequest(server string, id string, params *DeleteAirbnbListingRoomParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/rooms", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "roomId", params.RoomId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListAirbnbListingRoomsRequest constructs an http.Request for the ListAirbnbListingRooms method
func NewListAirbnbListingRoomsRequest(server string, id string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/rooms", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateAirbnbListingRoomRequest calls the generic CreateAirbnbListingRoom builder with application/json body
func NewCreateAirbnbListingRoomRequest(server string, id string, body CreateAirbnbListingRoomJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateAirbnbListingRoomRequestWithBody(server, id, "application/json", bodyReader)
}

// NewCreateAirbnbListingRoomRequestWithBody constructs an http.Request for the CreateAirbnbListingRoom method, with any body, and a specified content type
func NewCreateAirbnbListingRoomRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/rooms", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewUpdateAirbnbListingRoomRequest calls the generic UpdateAirbnbListingRoom builder with application/json body
func NewUpdateAirbnbListingRoomRequest(server string, id string, params *UpdateAirbnbListingRoomParams, body UpdateAirbnbListingRoomJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAirbnbListingRoomRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewUpdateAirbnbListingRoomRequestWithBody constructs an http.Request for the UpdateAirbnbListingRoom method, with any body, and a specified content type
func NewUpdateAirbnbListingRoomRequestWithBody(server string, id string, params *UpdateAirbnbListingRoomParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/rooms", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "roomId", params.RoomId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListAirbnbListingSafetyDisclosuresRequest constructs an http.Request for the ListAirbnbListingSafetyDisclosures method
func NewListAirbnbListingSafetyDisclosuresRequest(server string, id string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/safety-disclosures", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateAirbnbListingSafetyDisclosuresRequest calls the generic UpdateAirbnbListingSafetyDisclosures builder with application/json body
func NewUpdateAirbnbListingSafetyDisclosuresRequest(server string, id string, params *UpdateAirbnbListingSafetyDisclosuresParams, body UpdateAirbnbListingSafetyDisclosuresJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAirbnbListingSafetyDisclosuresRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewUpdateAirbnbListingSafetyDisclosuresRequestWithBody constructs an http.Request for the UpdateAirbnbListingSafetyDisclosures method, with any body, and a specified content type
func NewUpdateAirbnbListingSafetyDisclosuresRequestWithBody(server string, id string, params *UpdateAirbnbListingSafetyDisclosuresParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/safety-disclosures", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewGetAirbnbListingSettingsRequest constructs an http.Request for the GetAirbnbListingSettings method
func NewGetAirbnbListingSettingsRequest(server string, id string, params *GetAirbnbListingSettingsParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/listings/%s/settings", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Type != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "type", *params.Type, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListAirbnbThreadsRequest constructs an http.Request for the ListAirbnbThreads method
func NewListAirbnbThreadsRequest(server string, params *ListAirbnbThreadsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/messaging")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.AccountId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "account_id", *params.AccountId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetAirbnbThreadRequest constructs an http.Request for the GetAirbnbThread method
func NewGetAirbnbThreadRequest(server string, threadId string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "threadId", threadId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/messaging/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListAirbnbThreadMessagesRequest constructs an http.Request for the ListAirbnbThreadMessages method
func NewListAirbnbThreadMessagesRequest(server string, threadId string, params *ListAirbnbThreadMessagesParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "threadId", threadId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/messaging/%s/messages", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.All != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "all", *params.All, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewSendAirbnbMessageRequest calls the generic SendAirbnbMessage builder with application/json body
func NewSendAirbnbMessageRequest(server string, threadId string, body SendAirbnbMessageJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSendAirbnbMessageRequestWithBody(server, threadId, "application/json", bodyReader)
}

// NewSendAirbnbMessageRequestWithBody constructs an http.Request for the SendAirbnbMessage method, with any body, and a specified content type
func NewSendAirbnbMessageRequestWithBody(server string, threadId string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "threadId", threadId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/messaging/%s/messages", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewUpdateAirbnbMessageRequest calls the generic UpdateAirbnbMessage builder with application/json body
func NewUpdateAirbnbMessageRequest(server string, threadId string, messageId string, body UpdateAirbnbMessageJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateAirbnbMessageRequestWithBody(server, threadId, messageId, "application/json", bodyReader)
}

// NewUpdateAirbnbMessageRequestWithBody constructs an http.Request for the UpdateAirbnbMessage method, with any body, and a specified content type
func NewUpdateAirbnbMessageRequestWithBody(server string, threadId string, messageId string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "threadId", threadId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "messageId", messageId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/messaging/%s/messages/%s", pathParam0, pathParam1)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewWithdrawAirbnbOfferRequest constructs an http.Request for the WithdrawAirbnbOffer method
func NewWithdrawAirbnbOfferRequest(server string, params *WithdrawAirbnbOfferParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/offers")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offerId", params.OfferId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetAirbnbOfferRequest constructs an http.Request for the GetAirbnbOffer method
func NewGetAirbnbOfferRequest(server string, params *GetAirbnbOfferParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/offers")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offerId", params.OfferId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateAirbnbOfferRequest calls the generic CreateAirbnbOffer builder with application/json body
func NewCreateAirbnbOfferRequest(server string, params *CreateAirbnbOfferParams, body CreateAirbnbOfferJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateAirbnbOfferRequestWithBody(server, params, "application/json", bodyReader)
}

// NewCreateAirbnbOfferRequestWithBody constructs an http.Request for the CreateAirbnbOffer method, with any body, and a specified content type
func NewCreateAirbnbOfferRequestWithBody(server string, params *CreateAirbnbOfferParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/offers")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewListAirbnbReservationsRequest constructs an http.Request for the ListAirbnbReservations method
func NewListAirbnbReservationsRequest(server string, params *ListAirbnbReservationsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/reservations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.AccountId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "account_id", *params.AccountId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.ListingId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "listing_id", *params.ListingId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.StartDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "start_date", *params.StartDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.EndDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "end_date", *params.EndDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeTotal != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include_total", *params.IncludeTotal, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetAirbnbReservationRequest constructs an http.Request for the GetAirbnbReservation method
func NewGetAirbnbReservationRequest(server string, code string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "code", code, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/reservations/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewAirbnbReservationActionRequest calls the generic AirbnbReservationAction builder with application/json body
func NewAirbnbReservationActionRequest(server string, code string, params *AirbnbReservationActionParams, body AirbnbReservationActionJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewAirbnbReservationActionRequestWithBody(server, code, params, "application/json", bodyReader)
}

// NewAirbnbReservationActionRequestWithBody constructs an http.Request for the AirbnbReservationAction method, with any body, and a specified content type
func NewAirbnbReservationActionRequestWithBody(server string, code string, params *AirbnbReservationActionParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "code", code, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/reservations/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewListAirbnbReviewsRequest constructs an http.Request for the ListAirbnbReviews method
func NewListAirbnbReviewsRequest(server string, params *ListAirbnbReviewsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/reviews")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.AccountId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "account_id", *params.AccountId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewRespondAirbnbReviewLegacyRequest constructs an http.Request for the RespondAirbnbReviewLegacy method
func NewRespondAirbnbReviewLegacyRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/reviews")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewEditAirbnbReviewRequest calls the generic EditAirbnbReview builder with application/json body
func NewEditAirbnbReviewRequest(server string, id string, body EditAirbnbReviewJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewEditAirbnbReviewRequestWithBody(server, id, "application/json", bodyReader)
}

// NewEditAirbnbReviewRequestWithBody constructs an http.Request for the EditAirbnbReview method, with any body, and a specified content type
func NewEditAirbnbReviewRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/reviews/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewRespondAirbnbReviewRequest calls the generic RespondAirbnbReview builder with application/json body
func NewRespondAirbnbReviewRequest(server string, id string, body RespondAirbnbReviewJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewRespondAirbnbReviewRequestWithBody(server, id, "application/json", bodyReader)
}

// NewRespondAirbnbReviewRequestWithBody constructs an http.Request for the RespondAirbnbReview method, with any body, and a specified content type
func NewRespondAirbnbReviewRequestWithBody(server string, id string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/reviews/%s/respond", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListAirbnbTransactionsRequest constructs an http.Request for the ListAirbnbTransactions method
func NewListAirbnbTransactionsRequest(server string, params *ListAirbnbTransactionsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/transactions")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.AccountId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "account_id", *params.AccountId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewSyncAirbnbTransactionsRequest calls the generic SyncAirbnbTransactions builder with application/json body
func NewSyncAirbnbTransactionsRequest(server string, body SyncAirbnbTransactionsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSyncAirbnbTransactionsRequestWithBody(server, "application/json", bodyReader)
}

// NewSyncAirbnbTransactionsRequestWithBody constructs an http.Request for the SyncAirbnbTransactions method, with any body, and a specified content type
func NewSyncAirbnbTransactionsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/airbnb/transactions")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetBookingAvailabilityRequest constructs an http.Request for the GetBookingAvailability method
func NewGetBookingAvailabilityRequest(server string, params *GetBookingAvailabilityParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/availability")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "property_id", params.PropertyId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.StartDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "start_date", *params.StartDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.NumberOfDays != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "number_of_days", *params.NumberOfDays, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.RoomId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "room_id", *params.RoomId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.RoomLevel != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "room_level", *params.RoomLevel, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateBookingAvailabilityRequest calls the generic UpdateBookingAvailability builder with application/json body
func NewUpdateBookingAvailabilityRequest(server string, body UpdateBookingAvailabilityJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateBookingAvailabilityRequestWithBody(server, "application/json", bodyReader)
}

// NewUpdateBookingAvailabilityRequestWithBody constructs an http.Request for the UpdateBookingAvailability method, with any body, and a specified content type
func NewUpdateBookingAvailabilityRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/availability")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetBookingChargesRequest constructs an http.Request for the GetBookingCharges method
func NewGetBookingChargesRequest(server string, params *GetBookingChargesParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/charges")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "property_id", params.PropertyId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateBookingChargesRequest calls the generic UpdateBookingCharges builder with application/json body
func NewUpdateBookingChargesRequest(server string, body UpdateBookingChargesJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateBookingChargesRequestWithBody(server, "application/json", bodyReader)
}

// NewUpdateBookingChargesRequestWithBody constructs an http.Request for the UpdateBookingCharges method, with any body, and a specified content type
func NewUpdateBookingChargesRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/charges")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetBookingContentRequest constructs an http.Request for the GetBookingContent method
func NewGetBookingContentRequest(server string, params *GetBookingContentParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/content")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "property_id", params.PropertyId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.Type != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "type", *params.Type, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.RoomId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "room_id", *params.RoomId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateBookingContentRequest calls the generic UpdateBookingContent builder with application/json body
func NewUpdateBookingContentRequest(server string, body UpdateBookingContentJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateBookingContentRequestWithBody(server, "application/json", bodyReader)
}

// NewUpdateBookingContentRequestWithBody constructs an http.Request for the UpdateBookingContent method, with any body, and a specified content type
func NewUpdateBookingContentRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/content")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewMapBookingRoomRequest calls the generic MapBookingRoom builder with application/json body
func NewMapBookingRoomRequest(server string, body MapBookingRoomJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewMapBookingRoomRequestWithBody(server, "application/json", bodyReader)
}

// NewMapBookingRoomRequestWithBody constructs an http.Request for the MapBookingRoom method, with any body, and a specified content type
func NewMapBookingRoomRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/listings/map")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetBookingListingPricingRequest constructs an http.Request for the GetBookingListingPricing method
func NewGetBookingListingPricingRequest(server string, id int, params *GetBookingListingPricingParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/listings/%s/pricing", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.StartDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "startDate", *params.StartDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.NumberOfDays != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "number_of_days", *params.NumberOfDays, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.RoomId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "room_id", *params.RoomId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.RoomLevel != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "room_level", *params.RoomLevel, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.HotelId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "hotel_id", *params.HotelId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateBookingListingPricingRequest calls the generic UpdateBookingListingPricing builder with application/json body
func NewUpdateBookingListingPricingRequest(server string, id int, params *UpdateBookingListingPricingParams, body UpdateBookingListingPricingJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateBookingListingPricingRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewUpdateBookingListingPricingRequestWithBody constructs an http.Request for the UpdateBookingListingPricing method, with any body, and a specified content type
func NewUpdateBookingListingPricingRequestWithBody(server string, id int, params *UpdateBookingListingPricingParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/listings/%s/pricing", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.HotelId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "hotel_id", *params.HotelId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListBookingConversationsRequest constructs an http.Request for the ListBookingConversations method
func NewListBookingConversationsRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/messaging")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewSendBookingMessageRequest calls the generic SendBookingMessage builder with application/json body
func NewSendBookingMessageRequest(server string, body SendBookingMessageJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSendBookingMessageRequestWithBody(server, "application/json", bodyReader)
}

// NewSendBookingMessageRequestWithBody constructs an http.Request for the SendBookingMessage method, with any body, and a specified content type
func NewSendBookingMessageRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/messaging")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListBookingPropertiesRequest constructs an http.Request for the ListBookingProperties method
func NewListBookingPropertiesRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/properties")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetBookingPropertyRequest constructs an http.Request for the GetBookingProperty method
func NewGetBookingPropertyRequest(server string, id int) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/properties/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewBookingPropertyActionRequest calls the generic BookingPropertyAction builder with application/json body
func NewBookingPropertyActionRequest(server string, id int, params *BookingPropertyActionParams, body BookingPropertyActionJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewBookingPropertyActionRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewBookingPropertyActionRequestWithBody constructs an http.Request for the BookingPropertyAction method, with any body, and a specified content type
func NewBookingPropertyActionRequestWithBody(server string, id int, params *BookingPropertyActionParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/properties/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.HotelId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "hotel_id", *params.HotelId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewListBookingPropertyRoomsRequest constructs an http.Request for the ListBookingPropertyRooms method
func NewListBookingPropertyRoomsRequest(server string, id int, params *ListBookingPropertyRoomsParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/properties/%s/rooms", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.HotelId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "hotel_id", *params.HotelId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListBookingReservationsRequest constructs an http.Request for the ListBookingReservations method
func NewListBookingReservationsRequest(server string, params *ListBookingReservationsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/reservations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Type != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "type", *params.Type, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.HotelId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "hotel_id", *params.HotelId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.ReservationId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "reservation_id", *params.ReservationId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewAcknowledgeBookingReservationsRequest calls the generic AcknowledgeBookingReservations builder with application/json body
func NewAcknowledgeBookingReservationsRequest(server string, body AcknowledgeBookingReservationsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewAcknowledgeBookingReservationsRequestWithBody(server, "application/json", bodyReader)
}

// NewAcknowledgeBookingReservationsRequestWithBody constructs an http.Request for the AcknowledgeBookingReservations method, with any body, and a specified content type
func NewAcknowledgeBookingReservationsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/reservations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListBookingReviewsRequest constructs an http.Request for the ListBookingReviews method
func NewListBookingReviewsRequest(server string, params *ListBookingReviewsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/reviews")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "property_id", params.PropertyId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewReplyBookingReviewRequest calls the generic ReplyBookingReview builder with application/json body
func NewReplyBookingReviewRequest(server string, body ReplyBookingReviewJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewReplyBookingReviewRequestWithBody(server, "application/json", bodyReader)
}

// NewReplyBookingReviewRequestWithBody constructs an http.Request for the ReplyBookingReview method, with any body, and a specified content type
func NewReplyBookingReviewRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/reviews")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewBookingSetupRequest calls the generic BookingSetup builder with application/json body
func NewBookingSetupRequest(server string, body BookingSetupJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewBookingSetupRequestWithBody(server, "application/json", bodyReader)
}

// NewBookingSetupRequestWithBody constructs an http.Request for the BookingSetup method, with any body, and a specified content type
func NewBookingSetupRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/setup")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteBookingWebhookRequest constructs an http.Request for the DeleteBookingWebhook method
func NewDeleteBookingWebhookRequest(server string, params *DeleteBookingWebhookParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/webhooks")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "notification_type", params.NotificationType, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListBookingWebhooksRequest constructs an http.Request for the ListBookingWebhooks method
func NewListBookingWebhooksRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/webhooks")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateBookingWebhookRequest calls the generic CreateBookingWebhook builder with application/json body
func NewCreateBookingWebhookRequest(server string, body CreateBookingWebhookJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateBookingWebhookRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateBookingWebhookRequestWithBody constructs an http.Request for the CreateBookingWebhook method, with any body, and a specified content type
func NewCreateBookingWebhookRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/booking/webhooks")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetPlumguideAvailabilityRequest constructs an http.Request for the GetPlumguideAvailability method
func NewGetPlumguideAvailabilityRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/plumguide/availability")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdatePlumguideAvailabilityRequest constructs an http.Request for the UpdatePlumguideAvailability method
func NewUpdatePlumguideAvailabilityRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/plumguide/availability")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListPlumguideBookingsRequest constructs an http.Request for the ListPlumguideBookings method
func NewListPlumguideBookingsRequest(server string, params *ListPlumguideBookingsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/plumguide/bookings")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.ListingId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "listing_id", *params.ListingId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.BookingCode != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "booking_code", *params.BookingCode, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListPlumguideListingsRequest constructs an http.Request for the ListPlumguideListings method
func NewListPlumguideListingsRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/plumguide/listings")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetPlumguidePricingRequest constructs an http.Request for the GetPlumguidePricing method
func NewGetPlumguidePricingRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/plumguide/pricing")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdatePlumguidePricingRequest constructs an http.Request for the UpdatePlumguidePricing method
func NewUpdatePlumguidePricingRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/plumguide/pricing")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewDeletePlumguideWebhooksRequest constructs an http.Request for the DeletePlumguideWebhooks method
func NewDeletePlumguideWebhooksRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/plumguide/webhooks")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetPlumguideWebhooksRequest constructs an http.Request for the GetPlumguideWebhooks method
func NewGetPlumguideWebhooksRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/plumguide/webhooks")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdatePlumguideWebhooksRequest calls the generic UpdatePlumguideWebhooks builder with application/json body
func NewUpdatePlumguideWebhooksRequest(server string, body UpdatePlumguideWebhooksJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdatePlumguideWebhooksRequestWithBody(server, "application/json", bodyReader)
}

// NewUpdatePlumguideWebhooksRequestWithBody constructs an http.Request for the UpdatePlumguideWebhooks method, with any body, and a specified content type
func NewUpdatePlumguideWebhooksRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/plumguide/webhooks")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListVrboListingsRequest constructs an http.Request for the ListVrboListings method
func NewListVrboListingsRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/vrbo/listings")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListVrboReservationsRequest constructs an http.Request for the ListVrboReservations method
func NewListVrboReservationsRequest(server string, params *ListVrboReservationsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/channels/vrbo/reservations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeTotal != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include_total", *params.IncludeTotal, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListConnectionsRequest constructs an http.Request for the ListConnections method
func NewListConnectionsRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateConnectSessionRequest calls the generic CreateConnectSession builder with application/json body
func NewCreateConnectSessionRequest(server string, body CreateConnectSessionJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateConnectSessionRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateConnectSessionRequestWithBody constructs an http.Request for the CreateConnectSession method, with any body, and a specified content type
func NewCreateConnectSessionRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSubmitBeds24CredentialsRequest calls the generic SubmitBeds24Credentials builder with application/json body
func NewSubmitBeds24CredentialsRequest(server string, body SubmitBeds24CredentialsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSubmitBeds24CredentialsRequestWithBody(server, "application/json", bodyReader)
}

// NewSubmitBeds24CredentialsRequestWithBody constructs an http.Request for the SubmitBeds24Credentials method, with any body, and a specified content type
func NewSubmitBeds24CredentialsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/beds24/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewBookingConnectCallbackRequest calls the generic BookingConnectCallback builder with application/json body
func NewBookingConnectCallbackRequest(server string, body BookingConnectCallbackJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewBookingConnectCallbackRequestWithBody(server, "application/json", bodyReader)
}

// NewBookingConnectCallbackRequestWithBody constructs an http.Request for the BookingConnectCallback method, with any body, and a specified content type
func NewBookingConnectCallbackRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/booking/callback")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewMapConnectBookingRoomsRequest calls the generic MapConnectBookingRooms builder with application/json body
func NewMapConnectBookingRoomsRequest(server string, body MapConnectBookingRoomsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewMapConnectBookingRoomsRequestWithBody(server, "application/json", bodyReader)
}

// NewMapConnectBookingRoomsRequestWithBody constructs an http.Request for the MapConnectBookingRooms method, with any body, and a specified content type
func NewMapConnectBookingRoomsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/booking/map-rooms")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListConnectBookingRoomsRequest constructs an http.Request for the ListConnectBookingRooms method
func NewListConnectBookingRoomsRequest(server string, params *ListConnectBookingRoomsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/booking/rooms")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "sessionId", params.SessionId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewVerifyBookingHotelRequest calls the generic VerifyBookingHotel builder with application/json body
func NewVerifyBookingHotelRequest(server string, body VerifyBookingHotelJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewVerifyBookingHotelRequestWithBody(server, "application/json", bodyReader)
}

// NewVerifyBookingHotelRequestWithBody constructs an http.Request for the VerifyBookingHotel method, with any body, and a specified content type
func NewVerifyBookingHotelRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/booking/verify")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSubmitBookingsyncCredentialsRequest calls the generic SubmitBookingsyncCredentials builder with application/json body
func NewSubmitBookingsyncCredentialsRequest(server string, body SubmitBookingsyncCredentialsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSubmitBookingsyncCredentialsRequestWithBody(server, "application/json", bodyReader)
}

// NewSubmitBookingsyncCredentialsRequestWithBody constructs an http.Request for the SubmitBookingsyncCredentials method, with any body, and a specified content type
func NewSubmitBookingsyncCredentialsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/bookingsync/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSubmitGuestyCredentialsRequest calls the generic SubmitGuestyCredentials builder with application/json body
func NewSubmitGuestyCredentialsRequest(server string, body SubmitGuestyCredentialsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSubmitGuestyCredentialsRequestWithBody(server, "application/json", bodyReader)
}

// NewSubmitGuestyCredentialsRequestWithBody constructs an http.Request for the SubmitGuestyCredentials method, with any body, and a specified content type
func NewSubmitGuestyCredentialsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/guesty/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSubmitHospitableCredentialsRequest calls the generic SubmitHospitableCredentials builder with application/json body
func NewSubmitHospitableCredentialsRequest(server string, body SubmitHospitableCredentialsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSubmitHospitableCredentialsRequestWithBody(server, "application/json", bodyReader)
}

// NewSubmitHospitableCredentialsRequestWithBody constructs an http.Request for the SubmitHospitableCredentials method, with any body, and a specified content type
func NewSubmitHospitableCredentialsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/hospitable/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSubmitHostawayCredentialsRequest calls the generic SubmitHostawayCredentials builder with application/json body
func NewSubmitHostawayCredentialsRequest(server string, body SubmitHostawayCredentialsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSubmitHostawayCredentialsRequestWithBody(server, "application/json", bodyReader)
}

// NewSubmitHostawayCredentialsRequestWithBody constructs an http.Request for the SubmitHostawayCredentials method, with any body, and a specified content type
func NewSubmitHostawayCredentialsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/hostaway/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSubmitIgmsCredentialsRequest calls the generic SubmitIgmsCredentials builder with application/json body
func NewSubmitIgmsCredentialsRequest(server string, body SubmitIgmsCredentialsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSubmitIgmsCredentialsRequestWithBody(server, "application/json", bodyReader)
}

// NewSubmitIgmsCredentialsRequestWithBody constructs an http.Request for the SubmitIgmsCredentials method, with any body, and a specified content type
func NewSubmitIgmsCredentialsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/igms/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSubmitLodgifyCredentialsRequest calls the generic SubmitLodgifyCredentials builder with application/json body
func NewSubmitLodgifyCredentialsRequest(server string, body SubmitLodgifyCredentialsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSubmitLodgifyCredentialsRequestWithBody(server, "application/json", bodyReader)
}

// NewSubmitLodgifyCredentialsRequestWithBody constructs an http.Request for the SubmitLodgifyCredentials method, with any body, and a specified content type
func NewSubmitLodgifyCredentialsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/lodgify/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSubmitOwnerrezCredentialsRequest calls the generic SubmitOwnerrezCredentials builder with application/json body
func NewSubmitOwnerrezCredentialsRequest(server string, body SubmitOwnerrezCredentialsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSubmitOwnerrezCredentialsRequestWithBody(server, "application/json", bodyReader)
}

// NewSubmitOwnerrezCredentialsRequestWithBody constructs an http.Request for the SubmitOwnerrezCredentials method, with any body, and a specified content type
func NewSubmitOwnerrezCredentialsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/ownerrez/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListConnectProvidersRequest constructs an http.Request for the ListConnectProviders method
func NewListConnectProvidersRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/providers")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewSelectConnectProviderRequest calls the generic SelectConnectProvider builder with application/json body
func NewSelectConnectProviderRequest(server string, sessionId string, body SelectConnectProviderJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSelectConnectProviderRequestWithBody(server, sessionId, "application/json", bodyReader)
}

// NewSelectConnectProviderRequestWithBody constructs an http.Request for the SelectConnectProvider method, with any body, and a specified content type
func NewSelectConnectProviderRequestWithBody(server string, sessionId string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "sessionId", sessionId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/sessions/%s/select-provider", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSubmitSmoobuCredentialsRequest calls the generic SubmitSmoobuCredentials builder with application/json body
func NewSubmitSmoobuCredentialsRequest(server string, body SubmitSmoobuCredentialsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSubmitSmoobuCredentialsRequestWithBody(server, "application/json", bodyReader)
}

// NewSubmitSmoobuCredentialsRequestWithBody constructs an http.Request for the SubmitSmoobuCredentials method, with any body, and a specified content type
func NewSubmitSmoobuCredentialsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/smoobu/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSubmitVrboCredentialsRequest calls the generic SubmitVrboCredentials builder with application/json body
func NewSubmitVrboCredentialsRequest(server string, body SubmitVrboCredentialsJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSubmitVrboCredentialsRequestWithBody(server, "application/json", bodyReader)
}

// NewSubmitVrboCredentialsRequestWithBody constructs an http.Request for the SubmitVrboCredentials method, with any body, and a specified content type
func NewSubmitVrboCredentialsRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/vrbo/credentials")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteConnectionRequest constructs an http.Request for the DeleteConnection method
func NewDeleteConnectionRequest(server string, provider Provider, params *DeleteConnectionParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "provider", provider, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.AccountId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "accountId", *params.AccountId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetConnectStatusRequest constructs an http.Request for the GetConnectStatus method
func NewGetConnectStatusRequest(server string, provider Provider) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "provider", provider, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateConnectionRequest calls the generic CreateConnection builder with application/json body
func NewCreateConnectionRequest(server string, provider Provider, body CreateConnectionJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateConnectionRequestWithBody(server, provider, "application/json", bodyReader)
}

// NewCreateConnectionRequestWithBody constructs an http.Request for the CreateConnection method, with any body, and a specified content type
func NewCreateConnectionRequestWithBody(server string, provider Provider, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "provider", provider, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/connect/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListConversationsRequest constructs an http.Request for the ListConversations method
func NewListConversationsRequest(server string, params *ListConversationsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/conversations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Platform != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "platform", *params.Platform, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.XSchema != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Schema", *params.XSchema, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Schema", headerParam0)
		}

	}

	return req, nil
}

// NewGetConversationRequest constructs an http.Request for the GetConversation method
func NewGetConversationRequest(server string, id int, params *GetConversationParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/conversations/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.XSchema != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Schema", *params.XSchema, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Schema", headerParam0)
		}

	}

	return req, nil
}

// NewListConversationMessagesRequest constructs an http.Request for the ListConversationMessages method
func NewListConversationMessagesRequest(server string, id int, params *ListConversationMessagesParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/conversations/%s/messages", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Order != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "order", *params.Order, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.XSchema != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Schema", *params.XSchema, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Schema", headerParam0)
		}

	}

	return req, nil
}

// NewSendConversationMessageRequest calls the generic SendConversationMessage builder with application/json body
func NewSendConversationMessageRequest(server string, id int, params *SendConversationMessageParams, body SendConversationMessageJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSendConversationMessageRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewSendConversationMessageRequestWithBody constructs an http.Request for the SendConversationMessage method, with any body, and a specified content type
func NewSendConversationMessageRequestWithBody(server string, id int, params *SendConversationMessageParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/conversations/%s/messages", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewPreapproveConversationRequest calls the generic PreapproveConversation builder with application/json body
func NewPreapproveConversationRequest(server string, id int, params *PreapproveConversationParams, body PreapproveConversationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPreapproveConversationRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewPreapproveConversationRequestWithBody constructs an http.Request for the PreapproveConversation method, with any body, and a specified content type
func NewPreapproveConversationRequestWithBody(server string, id int, params *PreapproveConversationParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/conversations/%s/pre-approval", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewCreateConversationSpecialOfferRequest calls the generic CreateConversationSpecialOffer builder with application/json body
func NewCreateConversationSpecialOfferRequest(server string, id int, params *CreateConversationSpecialOfferParams, body CreateConversationSpecialOfferJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateConversationSpecialOfferRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewCreateConversationSpecialOfferRequestWithBody constructs an http.Request for the CreateConversationSpecialOffer method, with any body, and a specified content type
func NewCreateConversationSpecialOfferRequestWithBody(server string, id int, params *CreateConversationSpecialOfferParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/conversations/%s/special-offers", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewWithdrawConversationSpecialOfferRequest constructs an http.Request for the WithdrawConversationSpecialOffer method
func NewWithdrawConversationSpecialOfferRequest(server string, id int, offerId string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "offerId", offerId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/conversations/%s/special-offers/%s", pathParam0, pathParam1)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetConversationSpecialOfferRequest constructs an http.Request for the GetConversationSpecialOffer method
func NewGetConversationSpecialOfferRequest(server string, id int, offerId string) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "offerId", offerId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/conversations/%s/special-offers/%s", pathParam0, pathParam1)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListGuestsRequest constructs an http.Request for the ListGuests method
func NewListGuestsRequest(server string, params *ListGuestsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/guests")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Q != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "q", *params.Q, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.HasReservation != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "has_reservation", *params.HasReservation, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.ListingId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "listingId", *params.ListingId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.XSchema != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Schema", *params.XSchema, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Schema", headerParam0)
		}

	}

	return req, nil
}

// NewCreateGuestRequest calls the generic CreateGuest builder with application/json body
func NewCreateGuestRequest(server string, params *CreateGuestParams, body CreateGuestJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateGuestRequestWithBody(server, params, "application/json", bodyReader)
}

// NewCreateGuestRequestWithBody constructs an http.Request for the CreateGuest method, with any body, and a specified content type
func NewCreateGuestRequestWithBody(server string, params *CreateGuestParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/guests")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewGetGuestRequest constructs an http.Request for the GetGuest method
func NewGetGuestRequest(server string, id int, params *GetGuestParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/guests/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.XSchema != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Schema", *params.XSchema, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Schema", headerParam0)
		}

	}

	return req, nil
}

// NewGetHealthRequest constructs an http.Request for the GetHealth method
func NewGetHealthRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/health")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetAuthHealthRequest constructs an http.Request for the GetAuthHealth method
func NewGetAuthHealthRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/health/auth")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetChannelHealthRequest constructs an http.Request for the GetChannelHealth method
func NewGetChannelHealthRequest(server string, channel GetChannelHealthParamsChannel) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "channel", channel, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/health/channels/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetMcpHealthRequest constructs an http.Request for the GetMcpHealth method
func NewGetMcpHealthRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/health/mcp")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetWebhooksHealthRequest constructs an http.Request for the GetWebhooksHealth method
func NewGetWebhooksHealthRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/health/webhooks")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListInquiriesRequest constructs an http.Request for the ListInquiries method
func NewListInquiriesRequest(server string, params *ListInquiriesParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/inquiries")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.ListingId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "listing_id", *params.ListingId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.ConversationId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "conversation_id", *params.ConversationId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeTotal != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include_total", *params.IncludeTotal, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewClearKvRequest constructs an http.Request for the ClearKv method
func NewClearKvRequest(server string, params *ClearKvParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/kv")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.ProjectId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "project_id", *params.ProjectId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "prefix", params.Prefix, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListKvRequest constructs an http.Request for the ListKv method
func NewListKvRequest(server string, params *ListKvParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/kv")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.ProjectId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "project_id", *params.ProjectId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Prefix != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "prefix", *params.Prefix, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewDeleteKvRequest constructs an http.Request for the DeleteKv method
func NewDeleteKvRequest(server string, key string, params *DeleteKvParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "key", key, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/kv/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.ProjectId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "project_id", *params.ProjectId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetKvRequest constructs an http.Request for the GetKv method
func NewGetKvRequest(server string, key string, params *GetKvParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "key", key, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/kv/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.ProjectId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "project_id", *params.ProjectId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewSetKvRequest calls the generic SetKv builder with application/json body
func NewSetKvRequest(server string, key string, params *SetKvParams, body SetKvJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSetKvRequestWithBody(server, key, params, "application/json", bodyReader)
}

// NewSetKvRequestWithBody constructs an http.Request for the SetKv method, with any body, and a specified content type
func NewSetKvRequestWithBody(server string, key string, params *SetKvParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "key", key, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/kv/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.ProjectId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "project_id", *params.ProjectId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListListingsRequest constructs an http.Request for the ListListings method
func NewListListingsRequest(server string, params *ListListingsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Q != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "q", *params.Q, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Channel != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "channel", *params.Channel, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Include != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include", *params.Include, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.XSchema != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Schema", *params.XSchema, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Schema", headerParam0)
		}

	}

	return req, nil
}

// NewCreateListingRequest calls the generic CreateListing builder with application/json body
func NewCreateListingRequest(server string, body CreateListingJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateListingRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateListingRequestWithBody constructs an http.Request for the CreateListing method, with any body, and a specified content type
func NewCreateListingRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewBulkApplyPricingRequest calls the generic BulkApplyPricing builder with application/json body
func NewBulkApplyPricingRequest(server string, body BulkApplyPricingJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewBulkApplyPricingRequestWithBody(server, "application/json", bodyReader)
}

// NewBulkApplyPricingRequestWithBody constructs an http.Request for the BulkApplyPricing method, with any body, and a specified content type
func NewBulkApplyPricingRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/pricing/bulk")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewSetListingsStatusRequest calls the generic SetListingsStatus builder with application/json body
func NewSetListingsStatusRequest(server string, body SetListingsStatusJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewSetListingsStatusRequestWithBody(server, "application/json", bodyReader)
}

// NewSetListingsStatusRequestWithBody constructs an http.Request for the SetListingsStatus method, with any body, and a specified content type
func NewSetListingsStatusRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/status")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeactivateListingRequest constructs an http.Request for the DeactivateListing method
func NewDeactivateListingRequest(server string, id int) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetListingRequest constructs an http.Request for the GetListing method
func NewGetListingRequest(server string, id int, params *GetListingParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Include != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include", *params.Include, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.XSchema != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Schema", *params.XSchema, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Schema", headerParam0)
		}

	}

	return req, nil
}

// NewUpdateListingActiveRequest calls the generic UpdateListingActive builder with application/json body
func NewUpdateListingActiveRequest(server string, id int, body UpdateListingActiveJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateListingActiveRequestWithBody(server, id, "application/json", bodyReader)
}

// NewUpdateListingActiveRequestWithBody constructs an http.Request for the UpdateListingActive method, with any body, and a specified content type
func NewUpdateListingActiveRequestWithBody(server string, id int, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListListingCompsRequest constructs an http.Request for the ListListingComps method
func NewListListingCompsRequest(server string, id int, params *ListListingCompsParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/comps", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.RadiusKm != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "radius_km", *params.RadiusKm, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "number", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.StartDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "startDate", *params.StartDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.EndDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "endDate", *params.EndDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateListingContentRequest calls the generic UpdateListingContent builder with application/json body
func NewUpdateListingContentRequest(server string, id int, params *UpdateListingContentParams, body UpdateListingContentJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateListingContentRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewUpdateListingContentRequestWithBody constructs an http.Request for the UpdateListingContent method, with any body, and a specified content type
func NewUpdateListingContentRequestWithBody(server string, id int, params *UpdateListingContentParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/content", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewGenerateListingContentRequest calls the generic GenerateListingContent builder with application/json body
func NewGenerateListingContentRequest(server string, id int, body GenerateListingContentJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewGenerateListingContentRequestWithBody(server, id, "application/json", bodyReader)
}

// NewGenerateListingContentRequestWithBody constructs an http.Request for the GenerateListingContent method, with any body, and a specified content type
func NewGenerateListingContentRequestWithBody(server string, id int, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/generate-content", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewTakeListingOfflineRequest calls the generic TakeListingOffline builder with application/json body
func NewTakeListingOfflineRequest(server string, id int, params *TakeListingOfflineParams, body TakeListingOfflineJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewTakeListingOfflineRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewTakeListingOfflineRequestWithBody constructs an http.Request for the TakeListingOffline method, with any body, and a specified content type
func NewTakeListingOfflineRequestWithBody(server string, id int, params *TakeListingOfflineParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/offline", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.HotelId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "hotel_id", *params.HotelId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewTakeListingOnlineRequest calls the generic TakeListingOnline builder with application/json body
func NewTakeListingOnlineRequest(server string, id int, params *TakeListingOnlineParams, body TakeListingOnlineJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewTakeListingOnlineRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewTakeListingOnlineRequestWithBody constructs an http.Request for the TakeListingOnline method, with any body, and a specified content type
func NewTakeListingOnlineRequestWithBody(server string, id int, params *TakeListingOnlineParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/online", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.HotelId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "hotel_id", *params.HotelId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewDeleteListingPhotoRequest calls the generic DeleteListingPhoto builder with application/json body
func NewDeleteListingPhotoRequest(server string, id int, body DeleteListingPhotoJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewDeleteListingPhotoRequestWithBody(server, id, "application/json", bodyReader)
}

// NewDeleteListingPhotoRequestWithBody constructs an http.Request for the DeleteListingPhoto method, with any body, and a specified content type
func NewDeleteListingPhotoRequestWithBody(server string, id int, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/photos", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListListingPhotosRequest constructs an http.Request for the ListListingPhotos method
func NewListListingPhotosRequest(server string, id int) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/photos", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateListingPhotoUploadUrlRequest calls the generic CreateListingPhotoUploadUrl builder with application/json body
func NewCreateListingPhotoUploadUrlRequest(server string, id int, body CreateListingPhotoUploadUrlJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateListingPhotoUploadUrlRequestWithBody(server, id, "application/json", bodyReader)
}

// NewCreateListingPhotoUploadUrlRequestWithBody constructs an http.Request for the CreateListingPhotoUploadUrl method, with any body, and a specified content type
func NewCreateListingPhotoUploadUrlRequestWithBody(server string, id int, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/photos/upload-url", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetListingPricingRequest constructs an http.Request for the GetListingPricing method
func NewGetListingPricingRequest(server string, id int, params *GetListingPricingParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/pricing", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.StartDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "startDate", *params.StartDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.EndDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "endDate", *params.EndDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewApplyListingPricingRequest calls the generic ApplyListingPricing builder with application/json body
func NewApplyListingPricingRequest(server string, id int, body ApplyListingPricingJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewApplyListingPricingRequestWithBody(server, id, "application/json", bodyReader)
}

// NewApplyListingPricingRequestWithBody constructs an http.Request for the ApplyListingPricing method, with any body, and a specified content type
func NewApplyListingPricingRequestWithBody(server string, id int, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/pricing", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetListingPricingHistoryRequest constructs an http.Request for the GetListingPricingHistory method
func NewGetListingPricingHistoryRequest(server string, id int, params *GetListingPricingHistoryParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/pricing/history", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.StartDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "startDate", *params.StartDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.EndDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "endDate", *params.EndDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetListingPricingStrategyRequest constructs an http.Request for the GetListingPricingStrategy method
func NewGetListingPricingStrategyRequest(server string, id int) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/pricing/strategy", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateListingPricingStrategyRequest calls the generic UpdateListingPricingStrategy builder with application/json body
func NewUpdateListingPricingStrategyRequest(server string, id int, body UpdateListingPricingStrategyJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateListingPricingStrategyRequestWithBody(server, id, "application/json", bodyReader)
}

// NewUpdateListingPricingStrategyRequestWithBody constructs an http.Request for the UpdateListingPricingStrategy method, with any body, and a specified content type
func NewUpdateListingPricingStrategyRequestWithBody(server string, id int, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/pricing/strategy", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPut, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetListingPublishStatusRequest constructs an http.Request for the GetListingPublishStatus method
func NewGetListingPublishStatusRequest(server string, id int) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/publish-status", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewPublishListingToAirbnbRequest calls the generic PublishListingToAirbnb builder with application/json body
func NewPublishListingToAirbnbRequest(server string, id int, params *PublishListingToAirbnbParams, body PublishListingToAirbnbJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPublishListingToAirbnbRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewPublishListingToAirbnbRequestWithBody constructs an http.Request for the PublishListingToAirbnb method, with any body, and a specified content type
func NewPublishListingToAirbnbRequestWithBody(server string, id int, params *PublishListingToAirbnbParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/publish/airbnb", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewPublishListingToBookingRequest calls the generic PublishListingToBooking builder with application/json body
func NewPublishListingToBookingRequest(server string, id int, params *PublishListingToBookingParams, body PublishListingToBookingJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPublishListingToBookingRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewPublishListingToBookingRequestWithBody constructs an http.Request for the PublishListingToBooking method, with any body, and a specified content type
func NewPublishListingToBookingRequestWithBody(server string, id int, params *PublishListingToBookingParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/publish/booking", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.HotelId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "hotel_id", *params.HotelId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewPullListingFromAirbnbRequest calls the generic PullListingFromAirbnb builder with application/json body
func NewPullListingFromAirbnbRequest(server string, id int, body PullListingFromAirbnbJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPullListingFromAirbnbRequestWithBody(server, id, "application/json", bodyReader)
}

// NewPullListingFromAirbnbRequestWithBody constructs an http.Request for the PullListingFromAirbnb method, with any body, and a specified content type
func NewPullListingFromAirbnbRequestWithBody(server string, id int, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/pull/airbnb", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetListingSegmentsRequest constructs an http.Request for the GetListingSegments method
func NewGetListingSegmentsRequest(server string, id int, params *GetListingSegmentsParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/listings/%s/segments", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Level != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "level", *params.Level, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.RadiusKm != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "radius_km", *params.RadiusKm, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "number", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListMarketsRequest constructs an http.Request for the ListMarkets method
func NewListMarketsRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/markets")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListMarketBrowseRequest constructs an http.Request for the ListMarketBrowse method
func NewListMarketBrowseRequest(server string, params *ListMarketBrowseParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/markets/browse")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Q != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "q", *params.Q, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Country != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "country", *params.Country, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.MinListings != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "min_listings", *params.MinListings, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Sort != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "sort", *params.Sort, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetMarketRequest constructs an http.Request for the GetMarket method
func NewGetMarketRequest(server string, city string, params *GetMarketParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "city", city, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/markets/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.CompsPage != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "compsPage", *params.CompsPage, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetMarketCalendarRequest constructs an http.Request for the GetMarketCalendar method
func NewGetMarketCalendarRequest(server string, city string, params *GetMarketCalendarParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "city", city, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/markets/%s/calendar", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.StartDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "startDate", *params.StartDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.EndDate != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "endDate", *params.EndDate, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.ListingId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "listingId", *params.ListingId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListMigrationsRequest constructs an http.Request for the ListMigrations method
func NewListMigrationsRequest(server string, params *ListMigrationsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/migrations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewDeleteMigrationRequest constructs an http.Request for the DeleteMigration method
func NewDeleteMigrationRequest(server string, workspaceId int) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "workspaceId", workspaceId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/migrations/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetMigrationRequest constructs an http.Request for the GetMigration method
func NewGetMigrationRequest(server string, workspaceId int) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "workspaceId", workspaceId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/migrations/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetMigrationChannelMapRequest constructs an http.Request for the GetMigrationChannelMap method
func NewGetMigrationChannelMapRequest(server string, workspaceId int) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "workspaceId", workspaceId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/migrations/%s/channel-map", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCutoverMigrationRequest constructs an http.Request for the CutoverMigration method
func NewCutoverMigrationRequest(server string, workspaceId int) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "workspaceId", workspaceId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/migrations/%s/cutover", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCheckMigrationCutoverRequest calls the generic CheckMigrationCutover builder with application/json body
func NewCheckMigrationCutoverRequest(server string, workspaceId int, body CheckMigrationCutoverJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCheckMigrationCutoverRequestWithBody(server, workspaceId, "application/json", bodyReader)
}

// NewCheckMigrationCutoverRequestWithBody constructs an http.Request for the CheckMigrationCutover method, with any body, and a specified content type
func NewCheckMigrationCutoverRequestWithBody(server string, workspaceId int, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "workspaceId", workspaceId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/migrations/%s/cutover-check", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewRunMigrationImportRequest calls the generic RunMigrationImport builder with application/json body
func NewRunMigrationImportRequest(server string, workspaceId int, body RunMigrationImportJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewRunMigrationImportRequestWithBody(server, workspaceId, "application/json", bodyReader)
}

// NewRunMigrationImportRequestWithBody constructs an http.Request for the RunMigrationImport method, with any body, and a specified content type
func NewRunMigrationImportRequestWithBody(server string, workspaceId int, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "workspaceId", workspaceId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/migrations/%s/import", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetMigrationReportRequest constructs an http.Request for the GetMigrationReport method
func NewGetMigrationReportRequest(server string, workspaceId int) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "workspaceId", workspaceId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/migrations/%s/report", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListPropertiesRequest constructs an http.Request for the ListProperties method
func NewListPropertiesRequest(server string, params *ListPropertiesParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/properties")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Q != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "q", *params.Q, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.LifecycleStatus != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "lifecycle_status", *params.LifecycleStatus, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Channel != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "channel", *params.Channel, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.UpdatedSince != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "updated_since", *params.UpdatedSince, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date-time"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeTotal != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include_total", *params.IncludeTotal, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetPropertyRequest constructs an http.Request for the GetProperty method
func NewGetPropertyRequest(server string, id int, params *GetPropertyParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/properties/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Include != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include", *params.Include, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetQuoteRequest constructs an http.Request for the GetQuote method
func NewGetQuoteRequest(server string, params *GetQuoteParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/quotes")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "property_id", params.PropertyId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "check_in", params.CheckIn, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if queryFrag, err := runtime.StyleParamWithOptions("form", true, "check_out", params.CheckOut, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
			return nil, err
		} else {
			for _, qp := range strings.Split(queryFrag, "&") {
				rawQueryFragments = append(rawQueryFragments, qp)
			}
		}

		if params.Guests != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "guests", *params.Guests, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Pets != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "pets", *params.Pets, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.WebsiteId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "website_id", *params.WebsiteId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListReservationsRequest constructs an http.Request for the ListReservations method
func NewListReservationsRequest(server string, params *ListReservationsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/reservations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Platform != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "platform", *params.Platform, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.ListingId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "listingId", *params.ListingId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.CheckInAfter != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "check_in_after", *params.CheckInAfter, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.CheckInBefore != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "check_in_before", *params.CheckInBefore, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.CheckOutAfter != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "check_out_after", *params.CheckOutAfter, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.CheckOutBefore != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "check_out_before", *params.CheckOutBefore, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.CheckInFrom != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "checkInFrom", *params.CheckInFrom, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.CheckInTo != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "checkInTo", *params.CheckInTo, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.CheckInAfterCamel != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "checkInAfter", *params.CheckInAfterCamel, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.CheckInBeforeCamel != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "checkInBefore", *params.CheckInBeforeCamel, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.CheckOutAfterCamel != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "checkOutAfter", *params.CheckOutAfterCamel, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.CheckOutBeforeCamel != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "checkOutBefore", *params.CheckOutBeforeCamel, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.UpdatedSince != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "updated_since", *params.UpdatedSince, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: "date-time"}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeTotal != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include_total", *params.IncludeTotal, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.XSchema != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Schema", *params.XSchema, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Schema", headerParam0)
		}

	}

	return req, nil
}

// NewCreateReservationRequest calls the generic CreateReservation builder with application/json body
func NewCreateReservationRequest(server string, params *CreateReservationParams, body CreateReservationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateReservationRequestWithBody(server, params, "application/json", bodyReader)
}

// NewCreateReservationRequestWithBody constructs an http.Request for the CreateReservation method, with any body, and a specified content type
func NewCreateReservationRequestWithBody(server string, params *CreateReservationParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/reservations")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewGetReservationRequest constructs an http.Request for the GetReservation method
func NewGetReservationRequest(server string, id int, params *GetReservationParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/reservations/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.XSchema != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Schema", *params.XSchema, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Schema", headerParam0)
		}

	}

	return req, nil
}

// NewUpdateReservationRequest calls the generic UpdateReservation builder with application/json body
func NewUpdateReservationRequest(server string, id int, params *UpdateReservationParams, body UpdateReservationJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateReservationRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewUpdateReservationRequestWithBody constructs an http.Request for the UpdateReservation method, with any body, and a specified content type
func NewUpdateReservationRequestWithBody(server string, id int, params *UpdateReservationParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/reservations/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewAcceptReservationRequestRequest constructs an http.Request for the AcceptReservationRequest method
func NewAcceptReservationRequestRequest(server string, id int, params *AcceptReservationRequestParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/reservations/%s/accept", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewDeclineReservationRequestRequest calls the generic DeclineReservationRequest builder with application/json body
func NewDeclineReservationRequestRequest(server string, id int, params *DeclineReservationRequestParams, body DeclineReservationRequestJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewDeclineReservationRequestRequestWithBody(server, id, params, "application/json", bodyReader)
}

// NewDeclineReservationRequestRequestWithBody constructs an http.Request for the DeclineReservationRequest method, with any body, and a specified content type
func NewDeclineReservationRequestRequestWithBody(server string, id int, params *DeclineReservationRequestParams, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/reservations/%s/decline", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	if params != nil {

		if params.IdempotencyKey != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "Idempotency-Key", *params.IdempotencyKey, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("Idempotency-Key", headerParam0)
		}

	}

	return req, nil
}

// NewListReviewsRequest constructs an http.Request for the ListReviews method
func NewListReviewsRequest(server string, params *ListReviewsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/reviews")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Platform != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "platform", *params.Platform, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.ListingId != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "listingId", *params.ListingId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.RatingMin != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "rating_min", *params.RatingMin, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "number", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.RatingMax != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "rating_max", *params.RatingMax, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "number", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.ReviewerRole != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "reviewerRole", *params.ReviewerRole, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.XSchema != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Schema", *params.XSchema, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Schema", headerParam0)
		}

	}

	return req, nil
}

// NewGetReviewRequest constructs an http.Request for the GetReview method
func NewGetReviewRequest(server string, id int, params *GetReviewParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/reviews/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	if params != nil {

		if params.XSchema != nil {
			var headerParam0 string

			headerParam0, err = runtime.StyleParamWithOptions("simple", false, "X-Schema", *params.XSchema, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationHeader, Type: "string", Format: ""})
			if err != nil {
				return nil, err
			}

			req.Header.Set("X-Schema", headerParam0)
		}

	}

	return req, nil
}

// NewReplyToReviewRequest calls the generic ReplyToReview builder with application/json body
func NewReplyToReviewRequest(server string, id int, body ReplyToReviewJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewReplyToReviewRequestWithBody(server, id, "application/json", bodyReader)
}

// NewReplyToReviewRequestWithBody constructs an http.Request for the ReplyToReview method, with any body, and a specified content type
func NewReplyToReviewRequestWithBody(server string, id int, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "integer", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/reviews/%s/reply", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListCustomSchemasRequest constructs an http.Request for the ListCustomSchemas method
func NewListCustomSchemasRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/schema/custom")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateCustomSchemaRequest calls the generic CreateCustomSchema builder with application/json body
func NewCreateCustomSchemaRequest(server string, body CreateCustomSchemaJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateCustomSchemaRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateCustomSchemaRequestWithBody constructs an http.Request for the CreateCustomSchema method, with any body, and a specified content type
func NewCreateCustomSchemaRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/schema/custom")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteCustomSchemaRequest constructs an http.Request for the DeleteCustomSchema method
func NewDeleteCustomSchemaRequest(server string, id openapi_types.UUID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/schema/custom/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetCustomSchemaRequest constructs an http.Request for the GetCustomSchema method
func NewGetCustomSchemaRequest(server string, id openapi_types.UUID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/schema/custom/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateCustomSchemaRequest calls the generic UpdateCustomSchema builder with application/json body
func NewUpdateCustomSchemaRequest(server string, id openapi_types.UUID, body UpdateCustomSchemaJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateCustomSchemaRequestWithBody(server, id, "application/json", bodyReader)
}

// NewUpdateCustomSchemaRequestWithBody constructs an http.Request for the UpdateCustomSchema method, with any body, and a specified content type
func NewUpdateCustomSchemaRequestWithBody(server string, id openapi_types.UUID, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/schema/custom/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewGetUsageLogsRequest constructs an http.Request for the GetUsageLogs method
func NewGetUsageLogsRequest(server string, params *GetUsageLogsParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/usage/logs")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Range != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "range", *params.Range, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Operation != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "operation", *params.Operation, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Q != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "q", *params.Q, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.IncludeTotal != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "include_total", *params.IncludeTotal, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "boolean", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetUsageSummaryRequest constructs an http.Request for the GetUsageSummary method
func NewGetUsageSummaryRequest(server string, params *GetUsageSummaryParams) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/usage/summary")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Range != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "range", *params.Range, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetUsageTierRequest constructs an http.Request for the GetUsageTier method
func NewGetUsageTierRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/usage/tier")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewListWebhooksRequest constructs an http.Request for the ListWebhooks method
func NewListWebhooksRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewCreateWebhookRequest calls the generic CreateWebhook builder with application/json body
func NewCreateWebhookRequest(server string, body CreateWebhookJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewCreateWebhookRequestWithBody(server, "application/json", bodyReader)
}

// NewCreateWebhookRequestWithBody constructs an http.Request for the CreateWebhook method, with any body, and a specified content type
func NewCreateWebhookRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListWebhookEventTypesRequest constructs an http.Request for the ListWebhookEventTypes method
func NewListWebhookEventTypesRequest(server string) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks/event-types")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewTestWebhookRequest calls the generic TestWebhook builder with application/json body
func NewTestWebhookRequest(server string, body TestWebhookJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewTestWebhookRequestWithBody(server, "application/json", bodyReader)
}

// NewTestWebhookRequestWithBody constructs an http.Request for the TestWebhook method, with any body, and a specified content type
func NewTestWebhookRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks/test")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewDeleteWebhookRequest constructs an http.Request for the DeleteWebhook method
func NewDeleteWebhookRequest(server string, id openapi_types.UUID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodDelete, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetWebhookRequest constructs an http.Request for the GetWebhook method
func NewGetWebhookRequest(server string, id openapi_types.UUID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewUpdateWebhookRequest calls the generic UpdateWebhook builder with application/json body
func NewUpdateWebhookRequest(server string, id openapi_types.UUID, body UpdateWebhookJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewUpdateWebhookRequestWithBody(server, id, "application/json", bodyReader)
}

// NewUpdateWebhookRequestWithBody constructs an http.Request for the UpdateWebhook method, with any body, and a specified content type
func NewUpdateWebhookRequestWithBody(server string, id openapi_types.UUID, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks/%s", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPatch, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewListWebhookDeliveriesRequest constructs an http.Request for the ListWebhookDeliveries method
func NewListWebhookDeliveriesRequest(server string, id openapi_types.UUID, params *ListWebhookDeliveriesParams) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks/%s/deliveries", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	if params != nil {
		// queryValues collects non-styled parameters (passthrough, JSON)
		// that are safe to round-trip through url.Values.Encode().
		queryValues := queryURL.Query()
		// rawQueryFragments collects pre-encoded query fragments from
		// styled parameters, preserving literal commas as delimiters
		// per the OpenAPI spec (e.g. "color=blue,black,brown").
		var rawQueryFragments []string

		if params.Limit != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "limit", *params.Limit, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Cursor != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "cursor", *params.Cursor, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Offset != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "offset", *params.Offset, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "integer", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if params.Status != nil {

			if queryFrag, err := runtime.StyleParamWithOptions("form", true, "status", *params.Status, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationQuery, Type: "string", Format: ""}); err != nil {
				return nil, err
			} else {
				for _, qp := range strings.Split(queryFrag, "&") {
					rawQueryFragments = append(rawQueryFragments, qp)
				}
			}

		}

		if encoded := queryValues.Encode(); encoded != "" {
			rawQueryFragments = append(rawQueryFragments, encoded)
		}
		queryURL.RawQuery = strings.Join(rawQueryFragments, "&")
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewGetWebhookDeliveryRequest constructs an http.Request for the GetWebhookDelivery method
func NewGetWebhookDeliveryRequest(server string, id openapi_types.UUID, deliveryId openapi_types.UUID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "delivery_id", deliveryId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks/%s/deliveries/%s", pathParam0, pathParam1)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodGet, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewReplayWebhookDeliveryRequest calls the generic ReplayWebhookDelivery builder with application/json body
func NewReplayWebhookDeliveryRequest(server string, id openapi_types.UUID, deliveryId openapi_types.UUID, body ReplayWebhookDeliveryJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewReplayWebhookDeliveryRequestWithBody(server, id, deliveryId, "application/json", bodyReader)
}

// NewReplayWebhookDeliveryRequestWithBody constructs an http.Request for the ReplayWebhookDelivery method, with any body, and a specified content type
func NewReplayWebhookDeliveryRequestWithBody(server string, id openapi_types.UUID, deliveryId openapi_types.UUID, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "delivery_id", deliveryId, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks/%s/deliveries/%s/replay", pathParam0, pathParam1)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

// NewPingWebhookRequest constructs an http.Request for the PingWebhook method
func NewPingWebhookRequest(server string, id openapi_types.UUID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks/%s/ping", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewRotateWebhookSecretRequest constructs an http.Request for the RotateWebhookSecret method
func NewRotateWebhookSecretRequest(server string, id openapi_types.UUID) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks/%s/rotate-secret", pathParam0)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

// NewTestFireWebhookRequest constructs an http.Request for the TestFireWebhook method
func NewTestFireWebhookRequest(server string, id openapi_types.UUID, eventType WebhookEventType) (*http.Request, error) {
	var err error

	var pathParam0 string

	pathParam0, err = runtime.StyleParamWithOptions("simple", false, "id", id, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: "uuid"})
	if err != nil {
		return nil, err
	}

	var pathParam1 string

	pathParam1, err = runtime.StyleParamWithOptions("simple", false, "event_type", eventType, runtime.StyleParamOptions{ParamLocation: runtime.ParamLocationPath, Type: "string", Format: ""})
	if err != nil {
		return nil, err
	}

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/webhooks/%s/test/%s", pathParam0, pathParam1)
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest(http.MethodPost, queryURL.String(), nil)
	if err != nil {
		return nil, err
	}

	return req, nil
}

func (c *Client) applyEditors(ctx context.Context, req *http.Request, additionalEditors []RequestEditorFn) error {
	for _, r := range c.RequestEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	for _, r := range additionalEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	return nil
}

// ClientWithResponses builds on ClientInterface to offer response payloads
type ClientWithResponses struct {
	ClientInterface
}

// NewClientWithResponses creates a new ClientWithResponses, which wraps
// Client with return type handling
func NewClientWithResponses(server string, opts ...ClientOption) (*ClientWithResponses, error) {
	client, err := NewClient(server, opts...)
	if err != nil {
		return nil, err
	}
	return &ClientWithResponses{client}, nil
}

// WithBaseURL overrides the baseURL.
func WithBaseURL(baseURL string) ClientOption {
	return func(c *Client) error {
		newBaseURL, err := url.Parse(baseURL)
		if err != nil {
			return err
		}
		c.Server = newBaseURL.String()
		return nil
	}
}

// ClientWithResponsesInterface is the interface specification for the client with responses above.
type ClientWithResponsesInterface interface {

	// BatchUpdateAvailabilityWithBodyWithResponse Update availability across many properties
	//
	// Applies ONE settings object across up to 500 properties and pushes the result to every connected channel.
	//
	// Ownership is checked before anything is written: a batch containing a property from another workspace is refused as a whole and names the offending ids, rather than being partially applied.
	//
	// Per-property *different* values are separate calls — presenting them as one request would be a false claim about atomicity.
	//
	// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/availability/batch (the `BatchUpdateAvailability` operationId).
	BatchUpdateAvailabilityWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BatchUpdateAvailabilityClientResponse, error)

	// BatchUpdateAvailabilityWithResponse Update availability across many properties
	//
	// Applies ONE settings object across up to 500 properties and pushes the result to every connected channel.
	//
	// Ownership is checked before anything is written: a batch containing a property from another workspace is refused as a whole and names the offending ids, rather than being partially applied.
	//
	// Per-property *different* values are separate calls — presenting them as one request would be a false claim about atomicity.
	//
	// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/availability/batch (the `BatchUpdateAvailability` operationId).
	BatchUpdateAvailabilityWithResponse(ctx context.Context, body BatchUpdateAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*BatchUpdateAvailabilityClientResponse, error)

	// GetAvailabilityWithResponse Get property availability
	//
	// Channel-agnostic day-by-day availability calendar for a property over a date window. Returns a thin per-date shape — `{ date, available, price, minNights }` — projected from the property calendar.
	//
	// The `from` and `to` query params are **required** (ISO `YYYY-MM-DD`, inclusive) — omitting or malforming either returns 422. The window is capped at 366 days; longer ranges are truncated to the first 366 days.
	//
	// **`days` contains only the dates we actually hold calendar data for.** Requested dates with no calendar row are listed in `coverage.missingDates` — their availability is unknown. Never treat a missing date as bookable: this endpoint deliberately does not synthesise availability, because a fabricated open date can be double-booked. A property with no calendar still returns a real 200 (`days: []`, every date in `coverage.missingDates`), never a 404 — 404 means the property id does not exist or belongs to a different workspace.
	//
	// This endpoint is read-only, and the projected per-date shape carries **availability, price, and min-nights only** — it does NOT expose max-stay, closed-to-arrival (CTA), closed-to-departure (CTD), or the dedicated stop-sell flag. To read or write that full restriction set on Booking.com use the channel routes: `GET`/`PUT /v1/channels/booking/availability` (with the room + rate ids from `GET /v1/channels/booking/properties/{id}/rooms`). To **write** calendar values use `PUT /v1/availability/{propertyId}` (or `PATCH /v1/availability/batch`), which updates the property calendar and pushes to its connected channels; channel-only settings stay on the channel routes.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/availability/{propertyId} (the `GetAvailability` operationId).
	GetAvailabilityWithResponse(ctx context.Context, propertyId int, params *GetAvailabilityParams, reqEditors ...RequestEditorFn) (*GetAvailabilityClientResponse, error)

	// UpdateAvailabilityWithBodyWithResponse Set prices, block or unblock dates
	//
	// Writes the calendar for one property AND pushes to every connected channel in the same step. A write that only changed our copy would leave the OTA calendars stale and eventually double-book a guest.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/availability/{propertyId} (the `UpdateAvailability` operationId).
	UpdateAvailabilityWithBodyWithResponse(ctx context.Context, propertyId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAvailabilityClientResponse, error)

	// UpdateAvailabilityWithResponse Set prices, block or unblock dates
	//
	// Writes the calendar for one property AND pushes to every connected channel in the same step. A write that only changed our copy would leave the OTA calendars stale and eventually double-book a guest.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/availability/{propertyId} (the `UpdateAvailability` operationId).
	UpdateAvailabilityWithResponse(ctx context.Context, propertyId int, body UpdateAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAvailabilityClientResponse, error)

	// CreateBillingCheckoutWithBodyWithResponse Create checkout session
	//
	// Redirect user to Stripe checkout.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/billing (the `CreateBillingCheckout` operationId).
	CreateBillingCheckoutWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateBillingCheckoutClientResponse, error)

	// CreateBillingCheckoutWithResponse Create checkout session
	//
	// Redirect user to Stripe checkout.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/billing (the `CreateBillingCheckout` operationId).
	CreateBillingCheckoutWithResponse(ctx context.Context, body CreateBillingCheckoutJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateBillingCheckoutClientResponse, error)

	// ListAirbnbAlterationsWithResponse List Airbnb alterations
	//
	// List reservation alteration requests for Airbnb reservations in this workspace. **Pure DB read** from the local `reservation_alterations` mirror — never calls Airbnb upstream — scoped to your workspace via the reservations join.
	//
	// Default returns only pending alterations; pass `?type=all` for the full history. Filter to a single reservation with `?reservation_code=<confirmation code>`. Every response carries the `dataFreshness` envelope.
	//
	// Each row carries the proposed change in its `new*` fields. A **listing transfer** shows up as `newListingId` (Repull listing id) and `newAirbnbListingId` (Airbnb's own id); both are `null` when the alteration does not move the reservation.
	//
	// Alterations of reservations on inactive listings are left out. Filtering by a reservation on an inactive listing (`reservation_code`) returns `403 listing_inactive`.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/alterations (the `ListAirbnbAlterations` operationId).
	ListAirbnbAlterationsWithResponse(ctx context.Context, params *ListAirbnbAlterationsParams, reqEditors ...RequestEditorFn) (*ListAirbnbAlterationsClientResponse, error)

	// CreateAirbnbAlterationWithBodyWithResponse Create Airbnb alteration
	//
	// Propose a change to an existing Airbnb reservation: new dates, a new guest count, a new total price, or a move to a different listing. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host for the workspace, else `404 no_connection`.
	//
	// The body is validated before anything reaches Airbnb. `confirmation_code` is required and **at least one** of `check_in`, `check_out`, `number_of_guests`, `total_price` or `listing_id` must be sent with it — an alteration that changes nothing is `422 invalid_params`, not a request Airbnb is asked to act on. Unknown fields are refused rather than ignored, so a misspelling can never look like a successful write.
	//
	// **Listing transfer.** `listing_id` moves the reservation to another listing in your workspace. Send the **Repull** listing id (the `id` from `GET /v1/properties`); Repull checks you own it, that it is active and connected to Airbnb, translates it to the Airbnb listing id and sends it upstream. Callers who hold the Airbnb-side id instead may send `airbnb_listing_id`. **Airbnb decides** whether to honour a listing change on an alteration — Repull sends it and reports Airbnb’s answer; a refusal comes back as `422 airbnb_rejected` carrying Airbnb’s own message.
	//
	// Returns `403 listing_inactive` when the reservation’s listing, or the listing it is being moved to, is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/alterations (the `CreateAirbnbAlteration` operationId).
	CreateAirbnbAlterationWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateAirbnbAlterationClientResponse, error)

	// CreateAirbnbAlterationWithResponse Create Airbnb alteration
	//
	// Propose a change to an existing Airbnb reservation: new dates, a new guest count, a new total price, or a move to a different listing. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host for the workspace, else `404 no_connection`.
	//
	// The body is validated before anything reaches Airbnb. `confirmation_code` is required and **at least one** of `check_in`, `check_out`, `number_of_guests`, `total_price` or `listing_id` must be sent with it — an alteration that changes nothing is `422 invalid_params`, not a request Airbnb is asked to act on. Unknown fields are refused rather than ignored, so a misspelling can never look like a successful write.
	//
	// **Listing transfer.** `listing_id` moves the reservation to another listing in your workspace. Send the **Repull** listing id (the `id` from `GET /v1/properties`); Repull checks you own it, that it is active and connected to Airbnb, translates it to the Airbnb listing id and sends it upstream. Callers who hold the Airbnb-side id instead may send `airbnb_listing_id`. **Airbnb decides** whether to honour a listing change on an alteration — Repull sends it and reports Airbnb’s answer; a refusal comes back as `422 airbnb_rejected` carrying Airbnb’s own message.
	//
	// Returns `403 listing_inactive` when the reservation’s listing, or the listing it is being moved to, is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/alterations (the `CreateAirbnbAlteration` operationId).
	CreateAirbnbAlterationWithResponse(ctx context.Context, body CreateAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateAirbnbAlterationClientResponse, error)

	// GetAirbnbAlterationWithResponse Get Airbnb alteration
	//
	// Fetch a single Airbnb reservation alteration by its Airbnb alteration id. **Pure DB read**, workspace-scoped via the reservations join. Returns `404 not_found` when no alteration matches the id in your workspace.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/alterations/{id} (the `GetAirbnbAlteration` operationId).
	GetAirbnbAlterationWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbAlterationClientResponse, error)

	// AcceptAirbnbAlterationWithBodyWithResponse Accept Airbnb alteration
	//
	// Accept a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to approve the proposed date / guest-count / price change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/accept (the `AcceptAirbnbAlteration` operationId).
	AcceptAirbnbAlterationWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AcceptAirbnbAlterationClientResponse, error)

	// AcceptAirbnbAlterationWithResponse Accept Airbnb alteration
	//
	// Accept a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to approve the proposed date / guest-count / price change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/accept (the `AcceptAirbnbAlteration` operationId).
	AcceptAirbnbAlterationWithResponse(ctx context.Context, id string, body AcceptAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*AcceptAirbnbAlterationClientResponse, error)

	// CancelAirbnbAlterationWithBodyWithResponse Cancel Airbnb alteration
	//
	// Withdraw an alteration you proposed, before the other side has answered it. **Write-side** — calls Airbnb upstream (`respondToAlteration` with status `canceled`). Use this when you sent the wrong dates, guest count, price or listing: the alteration stops being pending instead of sitting there until the guest acts on it.
	//
	// This is the third of Airbnb's three answers to a pending alteration, alongside `accept` and `decline`, and behaves identically to them: requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Airbnb decides whether an alteration can still be withdrawn — one that has already been accepted or declined is refused upstream, and Airbnb's own reason comes back in `message`.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/cancel (the `CancelAirbnbAlteration` operationId).
	CancelAirbnbAlterationWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CancelAirbnbAlterationClientResponse, error)

	// CancelAirbnbAlterationWithResponse Cancel Airbnb alteration
	//
	// Withdraw an alteration you proposed, before the other side has answered it. **Write-side** — calls Airbnb upstream (`respondToAlteration` with status `canceled`). Use this when you sent the wrong dates, guest count, price or listing: the alteration stops being pending instead of sitting there until the guest acts on it.
	//
	// This is the third of Airbnb's three answers to a pending alteration, alongside `accept` and `decline`, and behaves identically to them: requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Airbnb decides whether an alteration can still be withdrawn — one that has already been accepted or declined is refused upstream, and Airbnb's own reason comes back in `message`.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/cancel (the `CancelAirbnbAlteration` operationId).
	CancelAirbnbAlterationWithResponse(ctx context.Context, id string, body CancelAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*CancelAirbnbAlterationClientResponse, error)

	// DeclineAirbnbAlterationWithBodyWithResponse Decline Airbnb alteration
	//
	// Decline a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to reject the proposed change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/decline (the `DeclineAirbnbAlteration` operationId).
	DeclineAirbnbAlterationWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*DeclineAirbnbAlterationClientResponse, error)

	// DeclineAirbnbAlterationWithResponse Decline Airbnb alteration
	//
	// Decline a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to reject the proposed change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/alterations/{id}/decline (the `DeclineAirbnbAlteration` operationId).
	DeclineAirbnbAlterationWithResponse(ctx context.Context, id string, body DeclineAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*DeclineAirbnbAlterationClientResponse, error)

	// GetAirbnbConnectionWithResponse Get Airbnb connection state
	//
	// Returns the workspace's Airbnb host connection state in one envelope. Use this instead of inferring connection health from per-listing 401s on `GET /v1/channels/airbnb/listings` — that's noisy (every per-listing call has to fail before you know) and ambiguous (a single 5xx looks identical to a deauth).
	//
	// Pure DB read — does NOT touch Airbnb's API, so it's cheap to poll from a status-page surface.
	//
	// The response includes one row per Airbnb host the workspace has linked. Each row carries `isConnected`, `lastSyncedAt`, `deactivatedAt`, and `lastDisconnectReason` (most recent non-backfill row in `airbnb_host_events`).
	//
	// A self-serve `fixUrl` is included whenever `status` is anything other than `connected` — points at the dashboard where the host re-authorizes (or initiates the first OAuth flow for `never_connected` workspaces).
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/connection (the `GetAirbnbConnection` operationId).
	GetAirbnbConnectionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetAirbnbConnectionClientResponse, error)

	// ListAirbnbListingsWithResponse List Airbnb listings
	//
	// List every Airbnb listing this workspace has access to via the connected Airbnb account. **Pure DB read — never calls Airbnb upstream.** The connect flow is what populates the local cache; the API serves what's already there. Customers with a disconnected host still see their last-synced data, with the top-level `dataFreshness` envelope flagging the staleness and pointing at the reconnect URL.
	//
	// Pass `?include=amenities` to enrich each connection with its locally-cached amenity set. Returns `null` per connection when the cache is empty.
	//
	// Pass `?include=thumbnail` to add `thumbnailUrl` to each listing — one extra column on the query that already runs, so a selection screen renders from a single request instead of one call per listing. `null` when the listing has no thumbnail stored. Combine comma-separated, e.g. `?include=amenities,thumbnail`.
	//
	// **Can this listing be written to?** Every connection carries `syncCategory` — Airbnb's own per-listing API sync decision (`sync_all`, `sync_rates_and_availability`, or `none`) — and `writable`, which is `false` exactly when that category is `none`. Airbnb authorises sync one listing at a time, so a connected account can still hold listings Airbnb refuses every write to; a write to one of those returns `403 listing_not_api_connected` before anything is sent, and reconnecting the account does not change it (the host must switch the listing on in Airbnb). Check `writable` here before a portfolio-wide push instead of discovering it one 403 at a time.
	//
	// Inactive listings are left out; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings (the `ListAirbnbListings` operationId).
	ListAirbnbListingsWithResponse(ctx context.Context, params *ListAirbnbListingsParams, reqEditors ...RequestEditorFn) (*ListAirbnbListingsClientResponse, error)

	// MapAirbnbListingWithBodyWithResponse Map an Airbnb listing to a Repull listing
	//
	// Link an existing Airbnb listing to a canonical Repull listing/property. **API-key-scoped** (unlike the Booking room mapping, which is Connect-session-scoped).
	//
	// Discover the `airbnbId` (+ `hostId`) via `GET /v1/channels/airbnb/listings`, then re-point it at the `listingId` of your choice — the dedup / consolidation case where the Airbnb sync auto-created its own listing but you want the inventory under an existing property.
	//
	// Repoints both the Airbnb record and its platform link to the target listing in one transaction. Idempotent — re-mapping to the same listing is a 200 no-op (`alreadyMapped: true`). Scope is enforced against your workspace on both the target listing and the existing Airbnb record; a listing that already links a different Airbnb listing returns 409.
	//
	// Returns `403 listing_inactive` when the target listing, or the listing the Airbnb listing is mapped to now, is inactive; nothing is changed.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/listings/map (the `MapAirbnbListing` operationId).
	MapAirbnbListingWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*MapAirbnbListingClientResponse, error)

	// MapAirbnbListingWithResponse Map an Airbnb listing to a Repull listing
	//
	// Link an existing Airbnb listing to a canonical Repull listing/property. **API-key-scoped** (unlike the Booking room mapping, which is Connect-session-scoped).
	//
	// Discover the `airbnbId` (+ `hostId`) via `GET /v1/channels/airbnb/listings`, then re-point it at the `listingId` of your choice — the dedup / consolidation case where the Airbnb sync auto-created its own listing but you want the inventory under an existing property.
	//
	// Repoints both the Airbnb record and its platform link to the target listing in one transaction. Idempotent — re-mapping to the same listing is a 200 no-op (`alreadyMapped: true`). Scope is enforced against your workspace on both the target listing and the existing Airbnb record; a listing that already links a different Airbnb listing returns 409.
	//
	// Returns `403 listing_inactive` when the target listing, or the listing the Airbnb listing is mapped to now, is inactive; nothing is changed.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/listings/map (the `MapAirbnbListing` operationId).
	MapAirbnbListingWithResponse(ctx context.Context, body MapAirbnbListingJSONRequestBody, reqEditors ...RequestEditorFn) (*MapAirbnbListingClientResponse, error)

	// GetAirbnbListingWithResponse Get Airbnb listing
	//
	// Fetch all Airbnb connection rows for a single Vanio listing id. A property may be linked from multiple Airbnb hosts — every match is returned. Pass `?include=amenities` to enrich each row with its current Airbnb amenities.
	//
	// Each row carries `syncCategory` — Airbnb's own per-listing API sync decision (`sync_all`, `sync_rates_and_availability`, or `none`) — and `writable`, which is `false` exactly when that category is `none`, meaning Airbnb refuses every write to the listing and Repull returns `403 listing_not_api_connected` without sending anything. `GET /v1/channels/airbnb/listings` reports both fields for the whole portfolio in one call.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id} (the `GetAirbnbListing` operationId).
	GetAirbnbListingWithResponse(ctx context.Context, id string, params *GetAirbnbListingParams, reqEditors ...RequestEditorFn) (*GetAirbnbListingClientResponse, error)

	// AirbnbListingActionWithBodyWithResponse Listing action (delete/push/publish/unlist/relist)
	//
	// Apply a state action to a listing by id. The path `id` is the canonical Repull listing id.
	//
	// **`delete` here never touches Airbnb. Read this before you call it.**
	//
	// | | `action: "delete"` (this endpoint) | `action: "unlist"` (this endpoint) |
	// |---|---|---|
	// | What it changes | The Repull record | The live Airbnb listing |
	// | Calls Airbnb | **No. Never.** | Yes |
	// | The guest-facing listing | Stays live and keeps taking bookings | **Goes down** and stops taking bookings |
	// | Billing and plan limits | No longer billed, no longer counts toward the cap | Unchanged |
	// | API access to the listing | `403 listing_inactive` until reactivated | Unchanged — you can still read and write it |
	// | Reverse it with | `PATCH /v1/listings/{id}` `{ "active": true }` | `action: "relist"` |
	// | Data kept | Yes, and it keeps syncing | Yes |
	//
	// Neither one deletes anything on Airbnb. **There is no endpoint on this API that deletes an Airbnb listing** — the word `delete` on this route means "deactivate the Repull record" and nothing else. (Main vanio's internal listing-sync layer has a same-named action that DOES hard-delete on Airbnb; it is not exposed here, by any endpoint, deliberately. If you have read that code, note that the two names do not mean the same thing.)
	//
	// `delete` is idempotent. To take a listing off the market on every channel at once — Airbnb and Booking.com together — use `POST /v1/listings/{id}/offline`.
	//
	// `unlist` calls Airbnb and **takes the live listing down**: it is deactivated with a valid deactivation reason and then READ BACK, so "Airbnb accepted the call but the listing is still live" is reported as a failure rather than a success. Requires `airbnbConnectionId` — a listing can be connected to more than one Airbnb listing, and taking down the wrong one is not undoable through this API. `relist` puts it back up (re-enables sync and makes the listing available again); it does not push content.
	//
	// `relist` goes through the channel-publish billing gate and `unlist` does not, so on a workspace whose subscription has lapsed a listing can be taken down and not put back until billing is sorted out. That refusal comes back as `402` with the action that fixes it — never as an Airbnb error, because retrying and reconnecting Airbnb do nothing for it.
	//
	// `push` / `publish` push the listing's content to Airbnb via the same host-side sync orchestrator as `POST /v1/listings/{id}/publish/airbnb` — pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create + publish a new one under that host. `force` re-pushes every field, ignoring dirty-field tracking. The result is per-section: see `AirbnbPublishResult`.
	//
	// Any other action (e.g. `pull`) returns a structured 422 naming the supported actions.
	//
	// Returns `403 listing_inactive` for `push`/`publish`/`unlist`/`relist` when the listing is inactive. `delete` (deactivation) is always accepted.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id} (the `AirbnbListingAction` operationId).
	AirbnbListingActionWithBodyWithResponse(ctx context.Context, id string, params *AirbnbListingActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AirbnbListingActionClientResponse, error)

	// AirbnbListingActionWithResponse Listing action (delete/push/publish/unlist/relist)
	//
	// Apply a state action to a listing by id. The path `id` is the canonical Repull listing id.
	//
	// **`delete` here never touches Airbnb. Read this before you call it.**
	//
	// | | `action: "delete"` (this endpoint) | `action: "unlist"` (this endpoint) |
	// |---|---|---|
	// | What it changes | The Repull record | The live Airbnb listing |
	// | Calls Airbnb | **No. Never.** | Yes |
	// | The guest-facing listing | Stays live and keeps taking bookings | **Goes down** and stops taking bookings |
	// | Billing and plan limits | No longer billed, no longer counts toward the cap | Unchanged |
	// | API access to the listing | `403 listing_inactive` until reactivated | Unchanged — you can still read and write it |
	// | Reverse it with | `PATCH /v1/listings/{id}` `{ "active": true }` | `action: "relist"` |
	// | Data kept | Yes, and it keeps syncing | Yes |
	//
	// Neither one deletes anything on Airbnb. **There is no endpoint on this API that deletes an Airbnb listing** — the word `delete` on this route means "deactivate the Repull record" and nothing else. (Main vanio's internal listing-sync layer has a same-named action that DOES hard-delete on Airbnb; it is not exposed here, by any endpoint, deliberately. If you have read that code, note that the two names do not mean the same thing.)
	//
	// `delete` is idempotent. To take a listing off the market on every channel at once — Airbnb and Booking.com together — use `POST /v1/listings/{id}/offline`.
	//
	// `unlist` calls Airbnb and **takes the live listing down**: it is deactivated with a valid deactivation reason and then READ BACK, so "Airbnb accepted the call but the listing is still live" is reported as a failure rather than a success. Requires `airbnbConnectionId` — a listing can be connected to more than one Airbnb listing, and taking down the wrong one is not undoable through this API. `relist` puts it back up (re-enables sync and makes the listing available again); it does not push content.
	//
	// `relist` goes through the channel-publish billing gate and `unlist` does not, so on a workspace whose subscription has lapsed a listing can be taken down and not put back until billing is sorted out. That refusal comes back as `402` with the action that fixes it — never as an Airbnb error, because retrying and reconnecting Airbnb do nothing for it.
	//
	// `push` / `publish` push the listing's content to Airbnb via the same host-side sync orchestrator as `POST /v1/listings/{id}/publish/airbnb` — pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create + publish a new one under that host. `force` re-pushes every field, ignoring dirty-field tracking. The result is per-section: see `AirbnbPublishResult`.
	//
	// Any other action (e.g. `pull`) returns a structured 422 naming the supported actions.
	//
	// Returns `403 listing_inactive` for `push`/`publish`/`unlist`/`relist` when the listing is inactive. `delete` (deactivation) is always accepted.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id} (the `AirbnbListingAction` operationId).
	AirbnbListingActionWithResponse(ctx context.Context, id string, params *AirbnbListingActionParams, body AirbnbListingActionJSONRequestBody, reqEditors ...RequestEditorFn) (*AirbnbListingActionClientResponse, error)

	// ListAirbnbListingAmenitiesWithResponse List Airbnb amenities
	//
	// List an Airbnb listing's amenities. **Pure DB read** from the local `listings_airbnb_amenities` cache — never calls Airbnb upstream. The response splits amenities into `amenities` (regular) and `accessibility_amenities` (step-free access, wide doorways, grab rails, disabled parking, wheelchair, accessible-height fixtures, hoists, etc). Both are arrays (`[]` when none). Consult `dataFreshness` to disambiguate "never synced" from "fresh and genuinely empty". Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/amenities (the `ListAirbnbListingAmenities` operationId).
	ListAirbnbListingAmenitiesWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*ListAirbnbListingAmenitiesClientResponse, error)

	// UpdateAirbnbListingAmenitiesWithBodyWithResponse Update Airbnb amenities
	//
	// Set amenities on an Airbnb listing. **Write-side** — calls Airbnb upstream.
	//
	// **Partial by design**: only the amenities you name change, so turning one off is a one-line body and nothing else on the listing moves. Ids are the `id` values `GET /amenities` returns (e.g. `wireless_internet`, `ac`, `kitchen`); case is ignored. Airbnb refuses ids outside its vocabulary — that comes back as `422 airbnb_rejected` carrying Airbnb's own message.
	//
	// `accessibility_amenities` go to Airbnb's separate accessibility resource, which has **no read side at all** — Airbnb offers no endpoint to fetch them back, and the combined amenities GET 404s on production listings. What you can read back is our own copy: this endpoint updates it on success, and `GET /amenities` returns it under `accessibilityAmenities`. Airbnb may also hold an accessibility claim for review until photo evidence is attached; pass `photo_ids` to supply it.
	//
	// Our Airbnb copy is updated on success so a read straight after this write returns the new values. The platform-neutral copy behind `GET /v1/listings/{id}?include=amenities` uses a different amenity vocabulary and is refreshed by the next sync, except where an id happens to be identical in both.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/amenities (the `UpdateAirbnbListingAmenities` operationId).
	UpdateAirbnbListingAmenitiesWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingAmenitiesClientResponse, error)

	// UpdateAirbnbListingAmenitiesWithResponse Update Airbnb amenities
	//
	// Set amenities on an Airbnb listing. **Write-side** — calls Airbnb upstream.
	//
	// **Partial by design**: only the amenities you name change, so turning one off is a one-line body and nothing else on the listing moves. Ids are the `id` values `GET /amenities` returns (e.g. `wireless_internet`, `ac`, `kitchen`); case is ignored. Airbnb refuses ids outside its vocabulary — that comes back as `422 airbnb_rejected` carrying Airbnb's own message.
	//
	// `accessibility_amenities` go to Airbnb's separate accessibility resource, which has **no read side at all** — Airbnb offers no endpoint to fetch them back, and the combined amenities GET 404s on production listings. What you can read back is our own copy: this endpoint updates it on success, and `GET /amenities` returns it under `accessibilityAmenities`. Airbnb may also hold an accessibility claim for review until photo evidence is attached; pass `photo_ids` to supply it.
	//
	// Our Airbnb copy is updated on success so a read straight after this write returns the new values. The platform-neutral copy behind `GET /v1/listings/{id}?include=amenities` uses a different amenity vocabulary and is refreshed by the next sync, except where an id happens to be identical in both.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/amenities (the `UpdateAirbnbListingAmenities` operationId).
	UpdateAirbnbListingAmenitiesWithResponse(ctx context.Context, id string, body UpdateAirbnbListingAmenitiesJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingAmenitiesClientResponse, error)

	// GetAirbnbListingAvailabilityWithResponse Get Airbnb availability
	//
	// Read the per-day availability calendar for an Airbnb listing. Returns one row per day including price overrides, min-stay, and blocked status.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/availability (the `GetAirbnbListingAvailability` operationId).
	GetAirbnbListingAvailabilityWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbListingAvailabilityClientResponse, error)

	// UpdateAirbnbListingAvailabilityWithBodyWithResponse Update Airbnb availability
	//
	// Push availability + restrictions to Airbnb. `type: "calendar"` writes per-date restrictions — min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`) — via a batch of operations that each target either a date range or an explicit date list. `type: "rules"` writes listing-level availability rules (default min/max nights, booking lead time, turnover days, seasonal/day-of-week min nights). Restrictions never leak across channels — this endpoint writes only to Airbnb.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
	//
	// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
	//
	// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/availability (the `UpdateAirbnbListingAvailability` operationId).
	UpdateAirbnbListingAvailabilityWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingAvailabilityClientResponse, error)

	// UpdateAirbnbListingAvailabilityWithResponse Update Airbnb availability
	//
	// Push availability + restrictions to Airbnb. `type: "calendar"` writes per-date restrictions — min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`) — via a batch of operations that each target either a date range or an explicit date list. `type: "rules"` writes listing-level availability rules (default min/max nights, booking lead time, turnover days, seasonal/day-of-week min nights). Restrictions never leak across channels — this endpoint writes only to Airbnb.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
	//
	// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
	//
	// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/availability (the `UpdateAirbnbListingAvailability` operationId).
	UpdateAirbnbListingAvailabilityWithResponse(ctx context.Context, id string, body UpdateAirbnbListingAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingAvailabilityClientResponse, error)

	// GetAirbnbBookingSettingsWithResponse Get Airbnb booking settings
	//
	// Read how an Airbnb listing takes bookings and what happens when a guest cancels: booking mode, Instant Book state, the good-track-record requirement, check-in/check-out times, advance notice, preparation time, booking window, the short-stay and long-stay cancellation policies, and the non-refundable option.
	//
	// **This is Repull's stored copy, not a live call to Airbnb.** Values come from the local Airbnb mirror that the sync workers fill, so the response always carries `dataFreshness` — check `dataFreshness.stale` (and `dataFreshness.accounts[]` when the workspace has several Airbnb accounts) before treating a value as current.
	//
	// **Not exposed by Airbnb.** The pre-reservation message and automatic stay extension have no field on Airbnb's `booking_settings` resource, so neither can be read or written here; set the pre-reservation message in the Airbnb host dashboard, and handle extensions through `/v1/channels/airbnb/alterations`. Airbnb also expresses the same-day cutoff only as whole hours of advance notice, so `advanceNotice.hours` is as precise as the cutoff gets.
	//
	// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive — an inactive listing keeps syncing but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/booking-settings (the `GetAirbnbBookingSettings` operationId).
	GetAirbnbBookingSettingsWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbBookingSettingsClientResponse, error)

	// UpdateAirbnbBookingSettingsWithBodyWithResponse Update Airbnb booking settings
	//
	// Set any subset of a listing's booking settings on Airbnb. Partial — a field you do not send is left as it is.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id; Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb, so a bad value is a `422` naming the field rather than a failed upstream call. Unknown fields are refused rather than dropped.
	//
	// **Two groups, applied in order.** Instant Book, check-in/out and the cancellation fields go to Airbnb's booking-settings resource. Advance notice, preparation time and booking window go to Airbnb's availability rules — Airbnb replaces that whole document, so Repull reads the current rules first and merges your change onto them, which is why setting a preparation time does not blank the listing's min/max nights. The response's `applied` array names the groups that were written.
	//
	// **Non-refundable is a percentage here, a factor on Airbnb.** Airbnb stores `non_refundable_price_factor` between 0.7 and 1.0; send `cancellation.nonRefundable.discountPercent` (0-30) and Repull converts — 10% becomes 0.9. `enabled: false` sets the factor to 1.0.
	//
	// **Not exposed by Airbnb:** `preReservationMessage` and `automaticStayExtension`. Sending either returns a `422` explaining where to set it instead.
	//
	// **Errors:** `403 connection_reauth_required` — Airbnb no longer accepts the connection for this listing (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 invalid_params` — the body is wrong; `field` names it. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/booking-settings (the `UpdateAirbnbBookingSettings` operationId).
	UpdateAirbnbBookingSettingsWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbBookingSettingsClientResponse, error)

	// UpdateAirbnbBookingSettingsWithResponse Update Airbnb booking settings
	//
	// Set any subset of a listing's booking settings on Airbnb. Partial — a field you do not send is left as it is.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id; Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb, so a bad value is a `422` naming the field rather than a failed upstream call. Unknown fields are refused rather than dropped.
	//
	// **Two groups, applied in order.** Instant Book, check-in/out and the cancellation fields go to Airbnb's booking-settings resource. Advance notice, preparation time and booking window go to Airbnb's availability rules — Airbnb replaces that whole document, so Repull reads the current rules first and merges your change onto them, which is why setting a preparation time does not blank the listing's min/max nights. The response's `applied` array names the groups that were written.
	//
	// **Non-refundable is a percentage here, a factor on Airbnb.** Airbnb stores `non_refundable_price_factor` between 0.7 and 1.0; send `cancellation.nonRefundable.discountPercent` (0-30) and Repull converts — 10% becomes 0.9. `enabled: false` sets the factor to 1.0.
	//
	// **Not exposed by Airbnb:** `preReservationMessage` and `automaticStayExtension`. Sending either returns a `422` explaining where to set it instead.
	//
	// **Errors:** `403 connection_reauth_required` — Airbnb no longer accepts the connection for this listing (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 invalid_params` — the body is wrong; `field` names it. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/booking-settings (the `UpdateAirbnbBookingSettings` operationId).
	UpdateAirbnbBookingSettingsWithResponse(ctx context.Context, id string, body UpdateAirbnbBookingSettingsJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbBookingSettingsClientResponse, error)

	// GetAirbnbCheckinGuideWithResponse Get Airbnb check-in guide
	//
	// Return every published locale variant of an Airbnb listing's check-in guide. **Pure DB read** from `listings_airbnb_check_in_guides`. Pass `?locale=en` to filter to one locale (prefix match). Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/checkin-guide (the `GetAirbnbCheckinGuide` operationId).
	GetAirbnbCheckinGuideWithResponse(ctx context.Context, id string, params *GetAirbnbCheckinGuideParams, reqEditors ...RequestEditorFn) (*GetAirbnbCheckinGuideClientResponse, error)

	// UpdateAirbnbCheckinGuideWithResponse Upsert Airbnb check-in guide
	//
	// Upsert the check-in guide for one locale on an Airbnb listing. **Write-side** — calls Airbnb upstream; the DB mirror is reconciled by the sync worker once the upstream call returns. Target the locale with `?locale=en` (defaults to `en`). Requires a connected Airbnb host, else `404 no_connection`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/checkin-guide (the `UpdateAirbnbCheckinGuide` operationId).
	UpdateAirbnbCheckinGuideWithResponse(ctx context.Context, id string, params *UpdateAirbnbCheckinGuideParams, reqEditors ...RequestEditorFn) (*UpdateAirbnbCheckinGuideClientResponse, error)

	// GetAirbnbCheckoutGuideWithResponse Get Airbnb checkout guide
	//
	// Return the checkout tasks an Airbnb listing shows guests at departure. **Pure DB read** from `listings_airbnb_checkout_tasks`. Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/checkout-guide (the `GetAirbnbCheckoutGuide` operationId).
	GetAirbnbCheckoutGuideWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbCheckoutGuideClientResponse, error)

	// ListAirbnbListingDescriptionsWithResponse List Airbnb descriptions
	//
	// List an Airbnb listing's per-locale content (name, summary, house rules, etc). **Pure DB read** from `listings_airbnb_descriptions`. Filter to one locale with `?locale=en` (the legacy `?country=` param is accepted as a soft alias). Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/descriptions (the `ListAirbnbListingDescriptions` operationId).
	ListAirbnbListingDescriptionsWithResponse(ctx context.Context, id string, params *ListAirbnbListingDescriptionsParams, reqEditors ...RequestEditorFn) (*ListAirbnbListingDescriptionsClientResponse, error)

	// UpdateAirbnbListingDescriptionWithBodyWithResponse Update an Airbnb description for one locale
	//
	// Write one locale's copy to the live Airbnb listing.
	//
	// Airbnb keeps a SEPARATE description per locale (`PUT /v2/listing_descriptions/{listingId}/{locale}`), which is why `locale` is part of the request and not a guess: a listing can carry twelve of them, and writing Italian copy into the English row is how a translation gets lost. Only the fields you send are written; Airbnb keeps the rest. `GET /v1/channels/airbnb/listings/{id}/settings?type=locales` lists the locales already synced for the listing.
	//
	// `description` is not an accepted field: Airbnb composes the public description from the sections (`summary`, `space`, `access`, …) and ignores a directly-supplied one.
	//
	// **A 200 does not by itself mean the change was applied.** On an established listing Airbnb LOCKS host-managed description fields — the write returns 200, reports them as locked, and applies nothing for them. The response reports `blockedFields`: the fields YOU sent that Airbnb dropped. `blockedFields: []` is what a landed write looks like; a non-empty list is still a 200 (the other fields really were written) with a `message` naming what was not. Reporting that as a clean success is the bug behind "the description does not push to Airbnb".
	//
	// This writes to AIRBNB. To write Repull's own canonical copy — the content a later publish distributes — use `PUT /v1/listings/{id}/content` with `locale`.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Returns `403 listing_inactive` when the listing is inactive.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/descriptions (the `UpdateAirbnbListingDescription` operationId).
	UpdateAirbnbListingDescriptionWithBodyWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingDescriptionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingDescriptionClientResponse, error)

	// UpdateAirbnbListingDescriptionWithResponse Update an Airbnb description for one locale
	//
	// Write one locale's copy to the live Airbnb listing.
	//
	// Airbnb keeps a SEPARATE description per locale (`PUT /v2/listing_descriptions/{listingId}/{locale}`), which is why `locale` is part of the request and not a guess: a listing can carry twelve of them, and writing Italian copy into the English row is how a translation gets lost. Only the fields you send are written; Airbnb keeps the rest. `GET /v1/channels/airbnb/listings/{id}/settings?type=locales` lists the locales already synced for the listing.
	//
	// `description` is not an accepted field: Airbnb composes the public description from the sections (`summary`, `space`, `access`, …) and ignores a directly-supplied one.
	//
	// **A 200 does not by itself mean the change was applied.** On an established listing Airbnb LOCKS host-managed description fields — the write returns 200, reports them as locked, and applies nothing for them. The response reports `blockedFields`: the fields YOU sent that Airbnb dropped. `blockedFields: []` is what a landed write looks like; a non-empty list is still a 200 (the other fields really were written) with a `message` naming what was not. Reporting that as a clean success is the bug behind "the description does not push to Airbnb".
	//
	// This writes to AIRBNB. To write Repull's own canonical copy — the content a later publish distributes — use `PUT /v1/listings/{id}/content` with `locale`.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Returns `403 listing_inactive` when the listing is inactive.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/descriptions (the `UpdateAirbnbListingDescription` operationId).
	UpdateAirbnbListingDescriptionWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingDescriptionParams, body UpdateAirbnbListingDescriptionJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingDescriptionClientResponse, error)

	// GetAirbnbListingDetailsWithResponse Get Airbnb listing details
	//
	// What kind of property Airbnb thinks this is — property type group and category, room type, capacity — plus the check-in method (`checkInOption`), whether the listing is live (`hasAvailability`), and **`lockedFields`: the attributes Airbnb refuses to change on this listing**.
	//
	// **Pure DB read** from the local mirror, one entry per Airbnb connection.
	//
	// Read `lockedFields` before a content write. Airbnb does not refuse a write to a locked attribute: it returns 200, reports the attribute as locked, and applies nothing — which is why a write can look successful and change nothing. 1,180 of 5,917 synced listings carry at least one locked attribute.
	//
	// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/details (the `GetAirbnbListingDetails` operationId).
	GetAirbnbListingDetailsWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbListingDetailsClientResponse, error)

	// UpdateAirbnbListingDetailsWithBodyWithResponse Update property type, room type, quiet hours or check-in method
	//
	// Change what kind of property the Airbnb listing is, when its quiet hours are, or how the guest gets in. Partial: only the fields you send are written. At least one required; an unknown field is refused by name rather than dropped.
	//
	// This is the UPDATE path for fields that previously had none. `POST /v1/listings` accepts a `propertyType` when a listing is CREATED and nothing could change it afterwards, so a listing mis-typed at import stayed mis-typed; the check-in method was mirrored and never exposed at all.
	//
	// **A 200 does not by itself mean the change was applied.** `property_type_category`, `property_type_group` and `check_in_option` are among the attributes Airbnb locks on established listings: the write returns 200, and Airbnb applies nothing for the locked ones. The response reports `blockedFields` — the fields YOU sent that Airbnb dropped — and `blockedFields: []` is what a landed write looks like. `GET …/details` reports the same list as `lockedFields` so you can check first.
	//
	// Canonical property type (the value Repull keeps and republishes) is set with `PUT /v1/listings/{id}/content` under `details`; this endpoint writes straight to Airbnb.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/details (the `UpdateAirbnbListingDetails` operationId).
	UpdateAirbnbListingDetailsWithBodyWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingDetailsParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingDetailsClientResponse, error)

	// UpdateAirbnbListingDetailsWithResponse Update property type, room type, quiet hours or check-in method
	//
	// Change what kind of property the Airbnb listing is, when its quiet hours are, or how the guest gets in. Partial: only the fields you send are written. At least one required; an unknown field is refused by name rather than dropped.
	//
	// This is the UPDATE path for fields that previously had none. `POST /v1/listings` accepts a `propertyType` when a listing is CREATED and nothing could change it afterwards, so a listing mis-typed at import stayed mis-typed; the check-in method was mirrored and never exposed at all.
	//
	// **A 200 does not by itself mean the change was applied.** `property_type_category`, `property_type_group` and `check_in_option` are among the attributes Airbnb locks on established listings: the write returns 200, and Airbnb applies nothing for the locked ones. The response reports `blockedFields` — the fields YOU sent that Airbnb dropped — and `blockedFields: []` is what a landed write looks like. `GET …/details` reports the same list as `lockedFields` so you can check first.
	//
	// Canonical property type (the value Repull keeps and republishes) is set with `PUT /v1/listings/{id}/content` under `details`; this endpoint writes straight to Airbnb.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/details (the `UpdateAirbnbListingDetails` operationId).
	UpdateAirbnbListingDetailsWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingDetailsParams, body UpdateAirbnbListingDetailsJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingDetailsClientResponse, error)

	// ListAirbnbListingPermitsWithResponse List Airbnb permits and licences
	//
	// The regulatory permits, licences and registration numbers attached to an Airbnb listing.
	//
	// **DB-only by default.** `?source=cache` (the default) returns the permits as last mirrored by the sync worker — regulatory body, regulation type, status, permit number — with no upstream call.
	//
	// **`?source=live` also returns the QUESTIONS.** The mirror stores the RESULT of a permit, not what Airbnb asks for it, so a caller that is about to write needs `?source=live` once: it returns each permit's `flows[]` with the `answer_key`, `type` and `choices` of every question, and the answers already on file. Airbnb refuses an `answer_key` it did not ask for on this listing, so this is not optional guesswork you can skip.
	//
	// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/permits (the `ListAirbnbListingPermits` operationId).
	ListAirbnbListingPermitsWithResponse(ctx context.Context, id string, params *ListAirbnbListingPermitsParams, reqEditors ...RequestEditorFn) (*ListAirbnbListingPermitsClientResponse, error)

	// UpdateAirbnbListingPermitsWithBodyWithResponse Answer Airbnb permit questions
	//
	// Answer the regulatory permit questions for a listing — the licence or registration number a city requires to keep the listing up.
	//
	// Read the questions first with `GET …/permits?source=live`. For each permit, pick one of its `flows[]` and send its `slug` as `flow_slug`; key every answer by the question's `answer_key`, and let the question's `type` decide the value field (`text_value`, `attestation_value`, `radio_value`, `date_value` or `selected_options_value`). Answers are forwarded verbatim — nothing is defaulted or inferred, because a wrong licence number can take a listing down in a regulated city.
	//
	// Send `Idempotency-Key`: a timeout here leaves you unable to tell "never arrived" from "arrived, response lost", and this is a compliance filing.
	//
	// Airbnb refusing the answers (an unknown `answer_key`, a malformed licence number) is `422 airbnb_rejected` carrying Airbnb's own reason. An expired or revoked Airbnb connection is `403 connection_reauth_required`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/permits (the `UpdateAirbnbListingPermits` operationId).
	UpdateAirbnbListingPermitsWithBodyWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingPermitsParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPermitsClientResponse, error)

	// UpdateAirbnbListingPermitsWithResponse Answer Airbnb permit questions
	//
	// Answer the regulatory permit questions for a listing — the licence or registration number a city requires to keep the listing up.
	//
	// Read the questions first with `GET …/permits?source=live`. For each permit, pick one of its `flows[]` and send its `slug` as `flow_slug`; key every answer by the question's `answer_key`, and let the question's `type` decide the value field (`text_value`, `attestation_value`, `radio_value`, `date_value` or `selected_options_value`). Answers are forwarded verbatim — nothing is defaulted or inferred, because a wrong licence number can take a listing down in a regulated city.
	//
	// Send `Idempotency-Key`: a timeout here leaves you unable to tell "never arrived" from "arrived, response lost", and this is a compliance filing.
	//
	// Airbnb refusing the answers (an unknown `answer_key`, a malformed licence number) is `422 airbnb_rejected` carrying Airbnb's own reason. An expired or revoked Airbnb connection is `403 connection_reauth_required`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/permits (the `UpdateAirbnbListingPermits` operationId).
	UpdateAirbnbListingPermitsWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingPermitsParams, body UpdateAirbnbListingPermitsJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPermitsClientResponse, error)

	// DeleteAirbnbListingPhotoWithResponse Delete an Airbnb photo
	//
	// Remove a single photo from an Airbnb listing. Pass the Airbnb-side photo id as `?photoId=`. **Write-side** — calls Airbnb upstream.
	//
	// The photo is proven to belong to the listing named in the path first; a photo from another listing returns `404`. Airbnb refuses to delete a listing's last photo. Both stored copies drop the photo on success — `stored` reports whether that succeeded.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/channels/airbnb/listings/{id}/photos (the `DeleteAirbnbListingPhoto` operationId).
	DeleteAirbnbListingPhotoWithResponse(ctx context.Context, id string, params *DeleteAirbnbListingPhotoParams, reqEditors ...RequestEditorFn) (*DeleteAirbnbListingPhotoClientResponse, error)

	// ListAirbnbListingPhotosWithResponse List Airbnb photos
	//
	// List photos attached to an Airbnb listing in display order. Returns the public CDN URL plus Airbnb-side metadata (id, caption, room).
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/photos (the `ListAirbnbListingPhotos` operationId).
	ListAirbnbListingPhotosWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*ListAirbnbListingPhotosClientResponse, error)

	// UpdateAirbnbListingPhotoWithBodyWithResponse Update an Airbnb photo
	//
	// Change one photo's caption, its position in the tour, the room it is filed under, or its metadata. **Write-side** — calls Airbnb upstream.
	//
	// Airbnb's photo endpoints are keyed by photo id alone, so the photo is proven to belong to the listing named in the path before anything is sent; a photo from another listing returns `404`, the same answer a photo that does not exist gets.
	//
	// On success both stored copies are updated — the Airbnb mirror `GET /photos` serves AND the canonical photo tour behind `GET /v1/listings/{id}` — so a read straight after this write returns the new value instead of waiting for the next sync. `stored` says whether that succeeded; `false` means Airbnb accepted the change but our copy will only catch up at the next sync.
	//
	// To move several photos at once use `PUT /photos/order`: it is one call instead of N, it validates the whole order before writing anything, and it reports exactly what landed if Airbnb refuses part-way.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/channels/airbnb/listings/{id}/photos (the `UpdateAirbnbListingPhoto` operationId).
	UpdateAirbnbListingPhotoWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPhotoClientResponse, error)

	// UpdateAirbnbListingPhotoWithResponse Update an Airbnb photo
	//
	// Change one photo's caption, its position in the tour, the room it is filed under, or its metadata. **Write-side** — calls Airbnb upstream.
	//
	// Airbnb's photo endpoints are keyed by photo id alone, so the photo is proven to belong to the listing named in the path before anything is sent; a photo from another listing returns `404`, the same answer a photo that does not exist gets.
	//
	// On success both stored copies are updated — the Airbnb mirror `GET /photos` serves AND the canonical photo tour behind `GET /v1/listings/{id}` — so a read straight after this write returns the new value instead of waiting for the next sync. `stored` says whether that succeeded; `false` means Airbnb accepted the change but our copy will only catch up at the next sync.
	//
	// To move several photos at once use `PUT /photos/order`: it is one call instead of N, it validates the whole order before writing anything, and it reports exactly what landed if Airbnb refuses part-way.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/channels/airbnb/listings/{id}/photos (the `UpdateAirbnbListingPhoto` operationId).
	UpdateAirbnbListingPhotoWithResponse(ctx context.Context, id string, body UpdateAirbnbListingPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPhotoClientResponse, error)

	// UploadAirbnbListingPhotosWithBodyWithResponse Upload photos to Airbnb
	//
	// Upload one or more photos to an Airbnb listing.
	//
	// `image` is base64 image DATA, not a url — a `data:image/jpeg;base64,…` prefix is accepted and stripped, and the decoded image must be under 25 MB. (This operation previously documented public image urls that Airbnb would fetch. It never did: a url arrived at Airbnb as a ~60-byte image.)
	//
	// Airbnb assigns the photo id and CDN urls, so the newly uploaded photos appear in `GET /photos` after the next sync. Caption, order and room assignment can be set straight away with `PATCH /photos`, `PUT /photos/order` and `PUT /photos/cover`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id}/photos (the `UploadAirbnbListingPhotos` operationId).
	UploadAirbnbListingPhotosWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UploadAirbnbListingPhotosClientResponse, error)

	// UploadAirbnbListingPhotosWithResponse Upload photos to Airbnb
	//
	// Upload one or more photos to an Airbnb listing.
	//
	// `image` is base64 image DATA, not a url — a `data:image/jpeg;base64,…` prefix is accepted and stripped, and the decoded image must be under 25 MB. (This operation previously documented public image urls that Airbnb would fetch. It never did: a url arrived at Airbnb as a ~60-byte image.)
	//
	// Airbnb assigns the photo id and CDN urls, so the newly uploaded photos appear in `GET /photos` after the next sync. Caption, order and room assignment can be set straight away with `PATCH /photos`, `PUT /photos/order` and `PUT /photos/cover`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id}/photos (the `UploadAirbnbListingPhotos` operationId).
	UploadAirbnbListingPhotosWithResponse(ctx context.Context, id string, body UploadAirbnbListingPhotosJSONRequestBody, reqEditors ...RequestEditorFn) (*UploadAirbnbListingPhotosClientResponse, error)

	// SetAirbnbListingCoverPhotoWithBodyWithResponse Set the Airbnb cover photo
	//
	// Choose which photo leads the listing. **Write-side** — calls Airbnb upstream.
	//
	// Airbnb has no "cover" field: the cover is the first photo of the tour, so this is a position write. Usually it is a single upstream request — the chosen photo takes a position below the current first one and nothing else moves. When the tour already starts at position 1 and there is no room below it, the tour is renumbered instead, one request per photo whose position actually changes, with the same stop-at-first-failure reporting as `PUT /photos/order` (`applied`, `failed_photo_id`, `not_attempted`; re-send the identical body to finish).
	//
	// The listing thumbnail — what every list view renders — is repointed at the new cover, so the change is not visible only inside the photo tour.
	//
	// The photo must already be in our cached copy of the tour; one uploaded since the last sync returns `404`, and `PATCH /photos` can set its position in the meantime.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/cover (the `SetAirbnbListingCoverPhoto` operationId).
	SetAirbnbListingCoverPhotoWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetAirbnbListingCoverPhotoClientResponse, error)

	// SetAirbnbListingCoverPhotoWithResponse Set the Airbnb cover photo
	//
	// Choose which photo leads the listing. **Write-side** — calls Airbnb upstream.
	//
	// Airbnb has no "cover" field: the cover is the first photo of the tour, so this is a position write. Usually it is a single upstream request — the chosen photo takes a position below the current first one and nothing else moves. When the tour already starts at position 1 and there is no room below it, the tour is renumbered instead, one request per photo whose position actually changes, with the same stop-at-first-failure reporting as `PUT /photos/order` (`applied`, `failed_photo_id`, `not_attempted`; re-send the identical body to finish).
	//
	// The listing thumbnail — what every list view renders — is repointed at the new cover, so the change is not visible only inside the photo tour.
	//
	// The photo must already be in our cached copy of the tour; one uploaded since the last sync returns `404`, and `PATCH /photos` can set its position in the meantime.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/cover (the `SetAirbnbListingCoverPhoto` operationId).
	SetAirbnbListingCoverPhotoWithResponse(ctx context.Context, id string, body SetAirbnbListingCoverPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*SetAirbnbListingCoverPhotoClientResponse, error)

	// ReorderAirbnbListingPhotosWithBodyWithResponse Reorder the Airbnb photo tour
	//
	// Set the order of a listing's photo tour in one call. **Write-side** — calls Airbnb upstream.
	//
	// Send the photo ids in the order you want them shown, first photo first. Ids you leave out keep their current relative order behind the ones you list, so moving one photo to the front is `{"photo_ids": ["<id>"]}`. Positions are then written as a dense run starting at 1.
	//
	// Airbnb has no bulk photo endpoint — order is one `sort_order` per photo — so this saves a loop of up to 200 requests against your rate limit and makes the partial-failure case reportable. How much is atomic:
	//
	// - Everything is validated before anything is written. A duplicate id, an id that is not on this listing, an inactive listing or a missing connection all fail with **zero** upstream writes.
	// - Only photos whose position actually changes are written; re-sending the order you already have writes nothing.
	// - On the first upstream failure the run stops — nothing after it is attempted. The error carries `applied`, `failed_photo_id` and `not_attempted`, uses Airbnb's own status (`422` rejected / `403` reauth / `429` / `502`), and the operation is idempotent: re-send the identical body to finish the run.
	// - Our stored copy records what actually landed, never the intent.
	//
	// Validation is against our cached copy of the tour, so a listing whose photos have never synced returns `404` — use `PATCH /photos` (which needs no cache) until the first sync lands.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/order (the `ReorderAirbnbListingPhotos` operationId).
	ReorderAirbnbListingPhotosWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReorderAirbnbListingPhotosClientResponse, error)

	// ReorderAirbnbListingPhotosWithResponse Reorder the Airbnb photo tour
	//
	// Set the order of a listing's photo tour in one call. **Write-side** — calls Airbnb upstream.
	//
	// Send the photo ids in the order you want them shown, first photo first. Ids you leave out keep their current relative order behind the ones you list, so moving one photo to the front is `{"photo_ids": ["<id>"]}`. Positions are then written as a dense run starting at 1.
	//
	// Airbnb has no bulk photo endpoint — order is one `sort_order` per photo — so this saves a loop of up to 200 requests against your rate limit and makes the partial-failure case reportable. How much is atomic:
	//
	// - Everything is validated before anything is written. A duplicate id, an id that is not on this listing, an inactive listing or a missing connection all fail with **zero** upstream writes.
	// - Only photos whose position actually changes are written; re-sending the order you already have writes nothing.
	// - On the first upstream failure the run stops — nothing after it is attempted. The error carries `applied`, `failed_photo_id` and `not_attempted`, uses Airbnb's own status (`422` rejected / `403` reauth / `429` / `502`), and the operation is idempotent: re-send the identical body to finish the run.
	// - Our stored copy records what actually landed, never the intent.
	//
	// Validation is against our cached copy of the tour, so a listing whose photos have never synced returns `404` — use `PATCH /photos` (which needs no cache) until the first sync lands.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/order (the `ReorderAirbnbListingPhotos` operationId).
	ReorderAirbnbListingPhotosWithResponse(ctx context.Context, id string, body ReorderAirbnbListingPhotosJSONRequestBody, reqEditors ...RequestEditorFn) (*ReorderAirbnbListingPhotosClientResponse, error)

	// GetAirbnbListingPricingWithResponse Get Airbnb pricing
	//
	// Read the current pricing config (base price, weekend uplift, length-of-stay discounts, smart-pricing bounds) for an Airbnb listing.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/pricing (the `GetAirbnbListingPricing` operationId).
	GetAirbnbListingPricingWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbListingPricingClientResponse, error)

	// UpdateAirbnbListingPricingWithBodyWithResponse Update Airbnb pricing
	//
	// Push pricing changes to Airbnb. The `type` discriminator selects the sub-resource (model, standard settings, LOS, rate-plan, fees, currency, rule, or per-date `calendar`). `type: "calendar"` carries the full per-date restriction set — nightly price, min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`). For settings sub-resources the full object is replaced — GET first, mutate locally, then PUT the whole object.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
	//
	// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
	//
	// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/pricing (the `UpdateAirbnbListingPricing` operationId).
	UpdateAirbnbListingPricingWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPricingClientResponse, error)

	// UpdateAirbnbListingPricingWithResponse Update Airbnb pricing
	//
	// Push pricing changes to Airbnb. The `type` discriminator selects the sub-resource (model, standard settings, LOS, rate-plan, fees, currency, rule, or per-date `calendar`). `type: "calendar"` carries the full per-date restriction set — nightly price, min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`). For settings sub-resources the full object is replaced — GET first, mutate locally, then PUT the whole object.
	//
	// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
	//
	// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
	//
	// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
	//
	// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/pricing (the `UpdateAirbnbListingPricing` operationId).
	UpdateAirbnbListingPricingWithResponse(ctx context.Context, id string, body UpdateAirbnbListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPricingClientResponse, error)

	// GetAirbnbListingQualityWithResponse Get Airbnb listing quality
	//
	// Return an Airbnb listing's quality signals — standards, reservation issues, and monthly quality stats. **Pure DB read** from the local quality mirrors. Scope the response with `?type=all|standards|issues|stats` (default `all`, which returns `{ standards, issues }`). Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/quality (the `GetAirbnbListingQuality` operationId).
	GetAirbnbListingQualityWithResponse(ctx context.Context, id string, params *GetAirbnbListingQualityParams, reqEditors ...RequestEditorFn) (*GetAirbnbListingQualityClientResponse, error)

	// DeleteAirbnbListingRoomWithResponse Delete an Airbnb room
	//
	// Delete a room from an Airbnb listing, and its beds with it. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=`. Requires a connected Airbnb host, else `404 no_connection`.
	//
	// The room is proven to belong to the listing named in the path first; a room from another listing returns `404`. Both stored copies drop the room on success — `stored` reports whether that succeeded.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/channels/airbnb/listings/{id}/rooms (the `DeleteAirbnbListingRoom` operationId).
	DeleteAirbnbListingRoomWithResponse(ctx context.Context, id string, params *DeleteAirbnbListingRoomParams, reqEditors ...RequestEditorFn) (*DeleteAirbnbListingRoomClientResponse, error)

	// ListAirbnbListingRoomsWithResponse List Airbnb rooms
	//
	// List the rooms configured on an Airbnb listing, ordered by room number, each with its sleeping arrangement in `beds`. **Pure DB read** from `listings_airbnb_rooms` + `listings_airbnb_beds`. Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/rooms (the `ListAirbnbListingRooms` operationId).
	ListAirbnbListingRoomsWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*ListAirbnbListingRoomsClientResponse, error)

	// CreateAirbnbListingRoomWithBodyWithResponse Create an Airbnb room
	//
	// Create a new room on an Airbnb listing, with its sleeping arrangement. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host, else `404 no_connection`.
	//
	// The response is the room object as Airbnb returned it. The new room is also seeded into our own copy, so the very next `GET /rooms` shows it rather than waiting for the sync.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id}/rooms (the `CreateAirbnbListingRoom` operationId).
	CreateAirbnbListingRoomWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateAirbnbListingRoomClientResponse, error)

	// CreateAirbnbListingRoomWithResponse Create an Airbnb room
	//
	// Create a new room on an Airbnb listing, with its sleeping arrangement. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host, else `404 no_connection`.
	//
	// The response is the room object as Airbnb returned it. The new room is also seeded into our own copy, so the very next `GET /rooms` shows it rather than waiting for the sync.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/listings/{id}/rooms (the `CreateAirbnbListingRoom` operationId).
	CreateAirbnbListingRoomWithResponse(ctx context.Context, id string, body CreateAirbnbListingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateAirbnbListingRoomClientResponse, error)

	// UpdateAirbnbListingRoomWithBodyWithResponse Update an Airbnb room
	//
	// Change a room's type, number, privacy or sleeping arrangement. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=` and send only the fields you want to change.
	//
	// `beds` REPLACES the room's whole arrangement — that is Airbnb's semantics for the field — so send every bed the room has, not just the changed one.
	//
	// Airbnb's room endpoints are keyed by room id alone, so the room is proven to belong to the listing named in the path before anything is sent; a room from another listing returns `404`, the same answer a room that does not exist gets.
	//
	// On success both stored copies are rebuilt to match, so a read straight after this write returns the new arrangement. `stored` says whether that succeeded.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/rooms (the `UpdateAirbnbListingRoom` operationId).
	UpdateAirbnbListingRoomWithBodyWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingRoomParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingRoomClientResponse, error)

	// UpdateAirbnbListingRoomWithResponse Update an Airbnb room
	//
	// Change a room's type, number, privacy or sleeping arrangement. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=` and send only the fields you want to change.
	//
	// `beds` REPLACES the room's whole arrangement — that is Airbnb's semantics for the field — so send every bed the room has, not just the changed one.
	//
	// Airbnb's room endpoints are keyed by room id alone, so the room is proven to belong to the listing named in the path before anything is sent; a room from another listing returns `404`, the same answer a room that does not exist gets.
	//
	// On success both stored copies are rebuilt to match, so a read straight after this write returns the new arrangement. `stored` says whether that succeeded.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/rooms (the `UpdateAirbnbListingRoom` operationId).
	UpdateAirbnbListingRoomWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingRoomParams, body UpdateAirbnbListingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingRoomClientResponse, error)

	// ListAirbnbListingSafetyDisclosuresWithResponse List guest-safety disclosures
	//
	// What a guest is told about the property before they book — exterior security cameras, a decibel noise monitor, pets on the property, stairs, a pool with no fence, weapons, shared spaces, limited parking. Airbnb calls them `listing_expectations_for_guests` and shows them at booking time.
	//
	// **Pure DB read** from the local mirror. EVERY supported disclosure type is returned, including the ones this listing has not declared (`value: false`), so "does this property have cameras?" has an answer rather than a missing key — `declared` tells you whether Airbnb holds an explicit answer. Types Airbnb returns that are not in the documented set are passed through rather than dropped.
	//
	// Where a listing is connected to several Airbnb listings, a disclosure declared on any of them is reported as true of the property.
	//
	// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/safety-disclosures (the `ListAirbnbListingSafetyDisclosures` operationId).
	ListAirbnbListingSafetyDisclosuresWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*ListAirbnbListingSafetyDisclosuresClientResponse, error)

	// UpdateAirbnbListingSafetyDisclosuresWithBodyWithResponse Update guest-safety disclosures
	//
	// Declare or retract the guest-safety disclosures on the live Airbnb listing.
	//
	// **This is a MERGE, not a replacement.** Airbnb keeps one value per disclosure type: a type you leave out keeps the value it has, and to retract one you send it with `value: false`. A full replacement would let a partial request silently un-declare a security camera — a guest-safety statement, not a preference.
	//
	// Only the disclosures are sent upstream. The same Airbnb endpoint carries the cancellation policy and instant-book settings, and this endpoint never touches them.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Airbnb refusing the change is `422 airbnb_rejected` with Airbnb's own reason; an expired or revoked connection is `403 connection_reauth_required`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/safety-disclosures (the `UpdateAirbnbListingSafetyDisclosures` operationId).
	UpdateAirbnbListingSafetyDisclosuresWithBodyWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingSafetyDisclosuresParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingSafetyDisclosuresClientResponse, error)

	// UpdateAirbnbListingSafetyDisclosuresWithResponse Update guest-safety disclosures
	//
	// Declare or retract the guest-safety disclosures on the live Airbnb listing.
	//
	// **This is a MERGE, not a replacement.** Airbnb keeps one value per disclosure type: a type you leave out keeps the value it has, and to retract one you send it with `value: false`. A full replacement would let a partial request silently un-declare a security camera — a guest-safety statement, not a preference.
	//
	// Only the disclosures are sent upstream. The same Airbnb endpoint carries the cancellation policy and instant-book settings, and this endpoint never touches them.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Airbnb refusing the change is `422 airbnb_rejected` with Airbnb's own reason; an expired or revoked connection is `403 connection_reauth_required`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/listings/{id}/safety-disclosures (the `UpdateAirbnbListingSafetyDisclosures` operationId).
	UpdateAirbnbListingSafetyDisclosuresWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingSafetyDisclosuresParams, body UpdateAirbnbListingSafetyDisclosuresJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingSafetyDisclosuresClientResponse, error)

	// GetAirbnbListingSettingsWithResponse Get Airbnb listing settings
	//
	// Return an Airbnb listing's host roles, published locales, and regulatory permits. **Pure DB read** — host roles from `listings_airbnb_details.host_roles`, locales from distinct `listings_airbnb_descriptions.locale`, permits from `listings_airbnb_permits`. Scope with `?type=all|hosts|permits|locales` (default `all`, which returns `{ hosts, locales }`). Returns `404` when the listing has no Airbnb connection in this workspace.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/listings/{id}/settings (the `GetAirbnbListingSettings` operationId).
	GetAirbnbListingSettingsWithResponse(ctx context.Context, id string, params *GetAirbnbListingSettingsParams, reqEditors ...RequestEditorFn) (*GetAirbnbListingSettingsClientResponse, error)

	// ListAirbnbThreadsWithResponse List Airbnb message threads
	//
	// List Airbnb message threads (one per guest conversation). Cursor-paginated. Each thread includes a preview of the latest message.
	//
	// Threads on inactive listings are left out; they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/messaging (the `ListAirbnbThreads` operationId).
	ListAirbnbThreadsWithResponse(ctx context.Context, params *ListAirbnbThreadsParams, reqEditors ...RequestEditorFn) (*ListAirbnbThreadsClientResponse, error)

	// GetAirbnbThreadWithResponse Get Airbnb thread
	//
	// Fetch a single Airbnb message thread by its Airbnb thread id. **Pure DB read** from the local `message_threads` mirror, workspace-scoped. Returns `404 not_found` when no thread matches. For the messages within a thread use `GET /v1/channels/airbnb/messaging/{threadId}/messages`.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/messaging/{threadId} (the `GetAirbnbThread` operationId).
	GetAirbnbThreadWithResponse(ctx context.Context, threadId string, reqEditors ...RequestEditorFn) (*GetAirbnbThreadClientResponse, error)

	// ListAirbnbThreadMessagesWithResponse Get Airbnb messages
	//
	// Messages stored for an Airbnb thread, as recorded rows (not the unified `Message` shape — use `GET /v1/conversations/{id}/messages` for that). By default returns 50 per page, newest first; walk older pages with `?cursor=` (the `pagination.nextCursor` of the previous page) until `pagination.hasMore` is `false`. `?all=true` returns up to 1000 rows oldest-first in one response, with no `pagination`.
	//
	// Each row carries `attachments` — photos and other files on that message, inbound or outbound — in the same shape as the unified endpoint.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/messaging/{threadId}/messages (the `ListAirbnbThreadMessages` operationId).
	ListAirbnbThreadMessagesWithResponse(ctx context.Context, threadId string, params *ListAirbnbThreadMessagesParams, reqEditors ...RequestEditorFn) (*ListAirbnbThreadMessagesClientResponse, error)

	// SendAirbnbMessageWithBodyWithResponse Send Airbnb message
	//
	// Send a message in an Airbnb thread as the host. Airbnb enforces content rules (no off-platform contact info, no external URLs) — violating messages are rejected upstream and surface as `422 airbnb_rejected` carrying Airbnb's own reason. Resending the same text is refused again; edit it first. `502 airbnb_error` is the other answer and means something else entirely: Airbnb did not complete the send, so retry it unchanged.
	//
	// ### Sending a photo or video (`mediaUrl`)
	//
	// Airbnb only accepts media uploaded to a signed URL it issues, one file per message and no text on the same message. With `mediaUrl`, Repull downloads the file (public `https://` only, 10 MB max), reads its real type from the bytes (JPEG, PNG, GIF, WebP — converted to JPEG — or MP4/QuickTime), uploads it to Airbnb and sends it; `message`, if given, follows as a separate message. This is the same flow as `POST /v1/conversations/{id}/messages` with `attachments` — prefer that endpoint, which also takes several files per request. The response is a `SendMessageResponse`, the send is recorded in the conversation, and failures are the 422 codes documented there (`attachment_type_not_supported`, `attachment_too_large`, `message_not_sent` for a pre-booking thread, …). The thread must already be synced to Repull (`GET /v1/conversations` lists them), otherwise `404`.
	//
	// Text-only sends (no `mediaUrl`) go straight to Airbnb and return Airbnb's message object.
	//
	// The `{threadId}` is the Airbnb thread id — the `externalThreadId` field on a unified `Conversation` (`GET /v1/conversations`).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/messaging/{threadId}/messages (the `SendAirbnbMessage` operationId).
	SendAirbnbMessageWithBodyWithResponse(ctx context.Context, threadId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SendAirbnbMessageClientResponse, error)

	// SendAirbnbMessageWithResponse Send Airbnb message
	//
	// Send a message in an Airbnb thread as the host. Airbnb enforces content rules (no off-platform contact info, no external URLs) — violating messages are rejected upstream and surface as `422 airbnb_rejected` carrying Airbnb's own reason. Resending the same text is refused again; edit it first. `502 airbnb_error` is the other answer and means something else entirely: Airbnb did not complete the send, so retry it unchanged.
	//
	// ### Sending a photo or video (`mediaUrl`)
	//
	// Airbnb only accepts media uploaded to a signed URL it issues, one file per message and no text on the same message. With `mediaUrl`, Repull downloads the file (public `https://` only, 10 MB max), reads its real type from the bytes (JPEG, PNG, GIF, WebP — converted to JPEG — or MP4/QuickTime), uploads it to Airbnb and sends it; `message`, if given, follows as a separate message. This is the same flow as `POST /v1/conversations/{id}/messages` with `attachments` — prefer that endpoint, which also takes several files per request. The response is a `SendMessageResponse`, the send is recorded in the conversation, and failures are the 422 codes documented there (`attachment_type_not_supported`, `attachment_too_large`, `message_not_sent` for a pre-booking thread, …). The thread must already be synced to Repull (`GET /v1/conversations` lists them), otherwise `404`.
	//
	// Text-only sends (no `mediaUrl`) go straight to Airbnb and return Airbnb's message object.
	//
	// The `{threadId}` is the Airbnb thread id — the `externalThreadId` field on a unified `Conversation` (`GET /v1/conversations`).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/messaging/{threadId}/messages (the `SendAirbnbMessage` operationId).
	SendAirbnbMessageWithResponse(ctx context.Context, threadId string, body SendAirbnbMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*SendAirbnbMessageClientResponse, error)

	// UpdateAirbnbMessageWithBodyWithResponse Edit / react to / mark an Airbnb message
	//
	// Act on a single message in an Airbnb thread. **Write-side** — calls Airbnb upstream. The `action` discriminator selects the operation:
	//
	// - `edit` — replace message text (requires `message`).
	// - `unsend` — retract the message.
	// - `read` — mark the message as read.
	// - `react` — add a reaction (requires `reaction`).
	//
	// Requires a connected Airbnb host, else `404 no_connection`.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/channels/airbnb/messaging/{threadId}/messages/{messageId} (the `UpdateAirbnbMessage` operationId).
	UpdateAirbnbMessageWithBodyWithResponse(ctx context.Context, threadId string, messageId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbMessageClientResponse, error)

	// UpdateAirbnbMessageWithResponse Edit / react to / mark an Airbnb message
	//
	// Act on a single message in an Airbnb thread. **Write-side** — calls Airbnb upstream. The `action` discriminator selects the operation:
	//
	// - `edit` — replace message text (requires `message`).
	// - `unsend` — retract the message.
	// - `read` — mark the message as read.
	// - `react` — add a reaction (requires `reaction`).
	//
	// Requires a connected Airbnb host, else `404 no_connection`.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/channels/airbnb/messaging/{threadId}/messages/{messageId} (the `UpdateAirbnbMessage` operationId).
	UpdateAirbnbMessageWithResponse(ctx context.Context, threadId string, messageId string, body UpdateAirbnbMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbMessageClientResponse, error)

	// WithdrawAirbnbOfferWithResponse Withdraw Airbnb special offer
	//
	// Withdraw a special offer the guest has not booked. **Write-side** — calls Airbnb upstream. Pass the Airbnb offer id as `?offerId=`. The Repull-id equivalent is `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/channels/airbnb/offers (the `WithdrawAirbnbOffer` operationId).
	WithdrawAirbnbOfferWithResponse(ctx context.Context, params *WithdrawAirbnbOfferParams, reqEditors ...RequestEditorFn) (*WithdrawAirbnbOfferClientResponse, error)

	// GetAirbnbOfferWithResponse Get Airbnb special offer
	//
	// Read a pre-approval or special offer from Airbnb by its Airbnb id. **Live read** — calls Airbnb upstream. Pass the id as `?offerId=`. The Repull-id equivalent is `GET /v1/conversations/{id}/special-offers/{offerId}`, which also confirms the offer belongs to that conversation.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/offers (the `GetAirbnbOffer` operationId).
	GetAirbnbOfferWithResponse(ctx context.Context, params *GetAirbnbOfferParams, reqEditors ...RequestEditorFn) (*GetAirbnbOfferClientResponse, error)

	// CreateAirbnbOfferWithBodyWithResponse Create Airbnb special offer or pre-approval
	//
	// Create a pre-approval or a special offer on an Airbnb thread, addressed by **Airbnb** ids. **Write-side** — calls Airbnb upstream. The Repull-id equivalents, which also update the inquiry in Vanio, are `POST /v1/conversations/{id}/pre-approval` and `POST /v1/conversations/{id}/special-offers` — prefer those unless you only hold Airbnb ids.
	//
	// - `type: "preapproval"` — let the guest book the dates and price they asked about. Requires `thread_id`; optional `block_instant_booking`.
	// - `type: "offer"` — your own terms. Requires `thread_id`, `listing_id` (the **Airbnb** listing id, as a string), `start_date`, `nights`, `total_price` (whole stay, listing currency) and `guest_details` with `number_of_guests` (or `number_of_adults`; Airbnb counts adults + children).
	//
	// The body is validated before anything reaches Airbnb (a `422 invalid_params` names the field), and unknown fields are refused. The legacy spellings `threadId` and `blockInstantBooking` still work. The request is sent as the Airbnb account that owns the thread or listing.
	//
	// Airbnb refusals are mapped rather than returned as a 500: `409 inquiry_no_longer_open` / `inquiry_expired` when the inquiry moved on, `422 airbnb_rejected` with Airbnb’s reason otherwise, `403 connection_reauth_required` when the grant does not allow it.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/offers (the `CreateAirbnbOffer` operationId).
	CreateAirbnbOfferWithBodyWithResponse(ctx context.Context, params *CreateAirbnbOfferParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateAirbnbOfferClientResponse, error)

	// CreateAirbnbOfferWithResponse Create Airbnb special offer or pre-approval
	//
	// Create a pre-approval or a special offer on an Airbnb thread, addressed by **Airbnb** ids. **Write-side** — calls Airbnb upstream. The Repull-id equivalents, which also update the inquiry in Vanio, are `POST /v1/conversations/{id}/pre-approval` and `POST /v1/conversations/{id}/special-offers` — prefer those unless you only hold Airbnb ids.
	//
	// - `type: "preapproval"` — let the guest book the dates and price they asked about. Requires `thread_id`; optional `block_instant_booking`.
	// - `type: "offer"` — your own terms. Requires `thread_id`, `listing_id` (the **Airbnb** listing id, as a string), `start_date`, `nights`, `total_price` (whole stay, listing currency) and `guest_details` with `number_of_guests` (or `number_of_adults`; Airbnb counts adults + children).
	//
	// The body is validated before anything reaches Airbnb (a `422 invalid_params` names the field), and unknown fields are refused. The legacy spellings `threadId` and `blockInstantBooking` still work. The request is sent as the Airbnb account that owns the thread or listing.
	//
	// Airbnb refusals are mapped rather than returned as a 500: `409 inquiry_no_longer_open` / `inquiry_expired` when the inquiry moved on, `422 airbnb_rejected` with Airbnb’s reason otherwise, `403 connection_reauth_required` when the grant does not allow it.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/offers (the `CreateAirbnbOffer` operationId).
	CreateAirbnbOfferWithResponse(ctx context.Context, params *CreateAirbnbOfferParams, body CreateAirbnbOfferJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateAirbnbOfferClientResponse, error)

	// ListAirbnbReservationsWithResponse List Airbnb reservations
	//
	// Cursor-paginated list of reservations sourced directly from Airbnb. Use this when you need Airbnb-specific fields (guest payout split, cancellation policy snapshot) that the unified `/v1/reservations` endpoint flattens away.
	//
	// Walk pages with `?cursor=<pagination.nextCursor>` until `pagination.hasMore` is `false`. The cursor is opaque — never construct or parse it client-side.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. Internally this walks upstream Airbnb cursor pages to skip rows, so deep offsets cost N/limit upstream round-trips; cursor remains the better choice for deep pagination.
	//
	// When `status` is omitted, all statuses are returned (Airbnb defaults to `accepted` only on its own surface, but this endpoint normalises to "all"). Pass `?status=accepted` to scope.
	//
	// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/reservations (the `ListAirbnbReservations` operationId).
	ListAirbnbReservationsWithResponse(ctx context.Context, params *ListAirbnbReservationsParams, reqEditors ...RequestEditorFn) (*ListAirbnbReservationsClientResponse, error)

	// GetAirbnbReservationWithResponse Get Airbnb reservation
	//
	// Fetch a single Airbnb reservation by Airbnb confirmation code (e.g. `HMABCDEF12`).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/reservations/{code} (the `GetAirbnbReservation` operationId).
	GetAirbnbReservationWithResponse(ctx context.Context, code string, reqEditors ...RequestEditorFn) (*GetAirbnbReservationClientResponse, error)

	// AirbnbReservationActionWithBodyWithResponse Accept, decline or cancel an Airbnb reservation
	//
	// Act on an Airbnb reservation by its Airbnb confirmation code. **Write-side** — calls Airbnb upstream, as the Airbnb account that owns the booking.
	//
	// - `accept` — accept a pending booking request.
	// - `decline` — decline a pending booking request. Requires `reason` (one of Airbnb's decline reasons) and `message` (sent to the guest, at most 500 characters).
	// - `cancel` — cancel a confirmed booking as the host. Requires `reason` (one of Airbnb's host-cancellation reasons). **Host cancellations carry Airbnb penalties.**
	//
	// The body is validated before anything reaches Airbnb; unknown fields are refused. There is no `pre-approve` action: a pre-approval answers an inquiry, which has no confirmation code — use `POST /v1/conversations/{id}/pre-approval`. For accept/decline, `POST /v1/reservations/{id}/accept` and `/decline` do the same by Repull id and keep the reservation in Repull in sync.
	//
	// Airbnb refusals are mapped rather than returned as a 500: a request that already moved on is `409 request_no_longer_pending` (do not retry), an expired one `409 request_expired`, any other refusal `422 airbnb_rejected` with Airbnb's reason.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/reservations/{code} (the `AirbnbReservationAction` operationId).
	AirbnbReservationActionWithBodyWithResponse(ctx context.Context, code string, params *AirbnbReservationActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AirbnbReservationActionClientResponse, error)

	// AirbnbReservationActionWithResponse Accept, decline or cancel an Airbnb reservation
	//
	// Act on an Airbnb reservation by its Airbnb confirmation code. **Write-side** — calls Airbnb upstream, as the Airbnb account that owns the booking.
	//
	// - `accept` — accept a pending booking request.
	// - `decline` — decline a pending booking request. Requires `reason` (one of Airbnb's decline reasons) and `message` (sent to the guest, at most 500 characters).
	// - `cancel` — cancel a confirmed booking as the host. Requires `reason` (one of Airbnb's host-cancellation reasons). **Host cancellations carry Airbnb penalties.**
	//
	// The body is validated before anything reaches Airbnb; unknown fields are refused. There is no `pre-approve` action: a pre-approval answers an inquiry, which has no confirmation code — use `POST /v1/conversations/{id}/pre-approval`. For accept/decline, `POST /v1/reservations/{id}/accept` and `/decline` do the same by Repull id and keep the reservation in Repull in sync.
	//
	// Airbnb refusals are mapped rather than returned as a 500: a request that already moved on is `409 request_no_longer_pending` (do not retry), an expired one `409 request_expired`, any other refusal `422 airbnb_rejected` with Airbnb's reason.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/reservations/{code} (the `AirbnbReservationAction` operationId).
	AirbnbReservationActionWithResponse(ctx context.Context, code string, params *AirbnbReservationActionParams, body AirbnbReservationActionJSONRequestBody, reqEditors ...RequestEditorFn) (*AirbnbReservationActionClientResponse, error)

	// ListAirbnbReviewsWithResponse List Airbnb reviews
	//
	// List reviews left by guests on Airbnb listings in this workspace. Includes both reviews of the host and reviews of the guest (where the host has not yet submitted theirs).
	//
	// Reviews of inactive listings are left out; they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/reviews (the `ListAirbnbReviews` operationId).
	ListAirbnbReviewsWithResponse(ctx context.Context, params *ListAirbnbReviewsParams, reqEditors ...RequestEditorFn) (*ListAirbnbReviewsClientResponse, error)

	// RespondAirbnbReviewLegacyWithResponse Respond to / submit Airbnb review (legacy)
	//
	// Legacy action-based shape. Body `{ action: "respond"|"submit", reviewId, response?, review? }`. Kept for backwards compatibility — prefer `PUT /v1/channels/airbnb/reviews/{id}` (edit) and `POST /v1/channels/airbnb/reviews/{id}/respond` (reply) for new integrations.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/reviews (the `RespondAirbnbReviewLegacy` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	RespondAirbnbReviewLegacyWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*RespondAirbnbReviewLegacyClientResponse, error)

	// EditAirbnbReviewWithBodyWithResponse Edit Airbnb host review
	//
	// Edit a host-side review for an Airbnb stay. Airbnb collapses POST + PUT into the same upstream call (`PUT /v2/listing_reviews/{id}`), so this endpoint covers both initial submit and subsequent edits while the review window is open.
	//
	// Body is a partial `AirbnbReview` — pass the fields you want to change (rating, public review, private feedback, category ratings).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/reviews/{id} (the `EditAirbnbReview` operationId).
	EditAirbnbReviewWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*EditAirbnbReviewClientResponse, error)

	// EditAirbnbReviewWithResponse Edit Airbnb host review
	//
	// Edit a host-side review for an Airbnb stay. Airbnb collapses POST + PUT into the same upstream call (`PUT /v2/listing_reviews/{id}`), so this endpoint covers both initial submit and subsequent edits while the review window is open.
	//
	// Body is a partial `AirbnbReview` — pass the fields you want to change (rating, public review, private feedback, category ratings).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/airbnb/reviews/{id} (the `EditAirbnbReview` operationId).
	EditAirbnbReviewWithResponse(ctx context.Context, id string, body EditAirbnbReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*EditAirbnbReviewClientResponse, error)

	// RespondAirbnbReviewWithBodyWithResponse Respond to Airbnb review
	//
	// Post a public host response to a guest review. Airbnb allows one response per review — repeated POSTs return 409. Response text is capped at 1000 characters.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/reviews/{id}/respond (the `RespondAirbnbReview` operationId).
	RespondAirbnbReviewWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*RespondAirbnbReviewClientResponse, error)

	// RespondAirbnbReviewWithResponse Respond to Airbnb review
	//
	// Post a public host response to a guest review. Airbnb allows one response per review — repeated POSTs return 409. Response text is capped at 1000 characters.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/reviews/{id}/respond (the `RespondAirbnbReview` operationId).
	RespondAirbnbReviewWithResponse(ctx context.Context, id string, body RespondAirbnbReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*RespondAirbnbReviewClientResponse, error)

	// ListAirbnbTransactionsWithResponse List Airbnb transactions
	//
	// List Airbnb host transactions (reservation earnings, payouts, resolution adjustments) for this workspace, newest first. **Pure DB read** — customer-facing reads never call Airbnb upstream; they serve the `airbnb_transactions` mirror. Each row carries the genuine host- and guest-side financial breakdown (accommodation subtotal, cleaning fee, host + guest service fees split base/VAT, tax buckets, expected/actual host payout with settlement status). Trigger a refresh with `POST` on this path. When the mirror is empty or the host disconnected, `dataFreshness.stale = true` with a `reason` (`never_synced`, `host_disconnected_<iso>`, `sync_lag_>_24h`).
	//
	// Transactions of reservations on inactive listings are left out; payout rows, which belong to no listing, are always included.
	//
	// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/airbnb/transactions (the `ListAirbnbTransactions` operationId).
	ListAirbnbTransactionsWithResponse(ctx context.Context, params *ListAirbnbTransactionsParams, reqEditors ...RequestEditorFn) (*ListAirbnbTransactionsClientResponse, error)

	// SyncAirbnbTransactionsWithBodyWithResponse Sync Airbnb transactions
	//
	// Refresh the Airbnb transactions mirror for this workspace by pulling from Airbnb upstream and upserting the breakdown that `GET` serves. Optional JSON body `{ start_date, end_date, transaction_type }` (`transaction_type` is `COMPLETED` or `UPCOMING`; both are synced when omitted). Returns `{ synced, count }`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/transactions (the `SyncAirbnbTransactions` operationId).
	SyncAirbnbTransactionsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SyncAirbnbTransactionsClientResponse, error)

	// SyncAirbnbTransactionsWithResponse Sync Airbnb transactions
	//
	// Refresh the Airbnb transactions mirror for this workspace by pulling from Airbnb upstream and upserting the breakdown that `GET` serves. Optional JSON body `{ start_date, end_date, transaction_type }` (`transaction_type` is `COMPLETED` or `UPCOMING`; both are synced when omitted). Returns `{ synced, count }`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/airbnb/transactions (the `SyncAirbnbTransactions` operationId).
	SyncAirbnbTransactionsWithResponse(ctx context.Context, body SyncAirbnbTransactionsJSONRequestBody, reqEditors ...RequestEditorFn) (*SyncAirbnbTransactionsClientResponse, error)

	// GetBookingAvailabilityWithResponse Read current Booking.com rates/availability/restrictions
	//
	// Read the current rate, availability, and restriction state for a Booking.com property so you can reconcile before writing with the PUT on this path. Keyed by `property_id` (the Booking hotel id), symmetric with the PUT.
	//
	// Proxies Booking's `getRoomRateAvailability` — the returned fields (price, rooms-to-sell, min/max stay, closed-to-arrival/departure, stop-sell) are whatever Booking.com emits for the window. A listing-id-keyed equivalent is available at `GET /v1/channels/booking/listings/{id}/pricing`.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/booking/availability (the `GetBookingAvailability` operationId).
	GetBookingAvailabilityWithResponse(ctx context.Context, params *GetBookingAvailabilityParams, reqEditors ...RequestEditorFn) (*GetBookingAvailabilityClientResponse, error)

	// UpdateBookingAvailabilityWithBodyWithResponse Update Booking.com rates/availability
	//
	// Write rates, availability and restrictions to a Booking.com property. `type` selects the write:
	//
	// - `rates` — nightly prices, plus any length-of-stay / arrival restrictions sent with them.
	// - `availability` — inventory (`availableRooms`), the stop-sell flag (`closed`), and restrictions. Omit `availableRooms` and `closed` for a restriction-only write.
	// - `derived-pricing` — occupancy-derived pricing rules.
	//
	// **Dates are inclusive at both ends.** `{ "start": "2026-11-04", "end": "2026-11-04" }` is exactly one night.
	//
	// **A rate amount needs an occupancy.** Booking.com stores the amount against the party size the rate plan prices: sent above that number it declines the price in silence, sent below it it answers 400. Send `occupancy`, or omit it and Repull resolves it from Booking.com's own data and echoes the value and its `source` back in `occupancy[]`. If it cannot be resolved the write is refused with `422` naming `updates[N].occupancy`.
	//
	// **Restrictions are sent in the same call, on their own wire.** A price and a minimum stay are two writes on Booking.com's side. Send them together and the response reports each separately: `price` and `restrictions` carry their own state, their own read-back, and — when refused — Booking.com's own reason. The top-level `applied` is `partial` when they disagree, so a price that landed is never reported as a failure. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` because Booking.com's notification has no element for them — set those on the rate plan in the Extranet. Nothing you send is ever silently ignored.
	//
	// **Inventory is not part of a rate update.** `roomsToSell` on a `rates` update returns `422 inventory_not_in_rate_update`; send it as `type: "availability"` instead.
	//
	// **The response says what is known.** Booking.com acknowledges a write with no per-date status, so the nights are read back — prices and restrictions out of the same read: `applied` is `verified`, `mismatch`, `partial`, `rejected` or `unverified` (send `verify: false` to skip the read-back). A bare acknowledgement is never reported as "all updates applied". Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
	//
	// Restrictions never leak across channels — this endpoint writes only to Booking.com. When Booking.com refuses a write outright, their own reason comes back as `422 booking_rejected` with `booking_ruid`; a genuine outage on their side is `502 booking_error`.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/booking/availability (the `UpdateBookingAvailability` operationId).
	UpdateBookingAvailabilityWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateBookingAvailabilityClientResponse, error)

	// UpdateBookingAvailabilityWithResponse Update Booking.com rates/availability
	//
	// Write rates, availability and restrictions to a Booking.com property. `type` selects the write:
	//
	// - `rates` — nightly prices, plus any length-of-stay / arrival restrictions sent with them.
	// - `availability` — inventory (`availableRooms`), the stop-sell flag (`closed`), and restrictions. Omit `availableRooms` and `closed` for a restriction-only write.
	// - `derived-pricing` — occupancy-derived pricing rules.
	//
	// **Dates are inclusive at both ends.** `{ "start": "2026-11-04", "end": "2026-11-04" }` is exactly one night.
	//
	// **A rate amount needs an occupancy.** Booking.com stores the amount against the party size the rate plan prices: sent above that number it declines the price in silence, sent below it it answers 400. Send `occupancy`, or omit it and Repull resolves it from Booking.com's own data and echoes the value and its `source` back in `occupancy[]`. If it cannot be resolved the write is refused with `422` naming `updates[N].occupancy`.
	//
	// **Restrictions are sent in the same call, on their own wire.** A price and a minimum stay are two writes on Booking.com's side. Send them together and the response reports each separately: `price` and `restrictions` carry their own state, their own read-back, and — when refused — Booking.com's own reason. The top-level `applied` is `partial` when they disagree, so a price that landed is never reported as a failure. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` because Booking.com's notification has no element for them — set those on the rate plan in the Extranet. Nothing you send is ever silently ignored.
	//
	// **Inventory is not part of a rate update.** `roomsToSell` on a `rates` update returns `422 inventory_not_in_rate_update`; send it as `type: "availability"` instead.
	//
	// **The response says what is known.** Booking.com acknowledges a write with no per-date status, so the nights are read back — prices and restrictions out of the same read: `applied` is `verified`, `mismatch`, `partial`, `rejected` or `unverified` (send `verify: false` to skip the read-back). A bare acknowledgement is never reported as "all updates applied". Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
	//
	// Restrictions never leak across channels — this endpoint writes only to Booking.com. When Booking.com refuses a write outright, their own reason comes back as `422 booking_rejected` with `booking_ruid`; a genuine outage on their side is `502 booking_error`.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/booking/availability (the `UpdateBookingAvailability` operationId).
	UpdateBookingAvailabilityWithResponse(ctx context.Context, body UpdateBookingAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateBookingAvailabilityClientResponse, error)

	// GetBookingChargesWithResponse Get Booking.com charges
	//
	// Fetch the extra-charge set (cleaning fee, resort fee, city tax, etc.) configured for a Booking.com property. Pass the Booking.com `property_id` as a query param — required.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/booking/charges (the `GetBookingCharges` operationId).
	GetBookingChargesWithResponse(ctx context.Context, params *GetBookingChargesParams, reqEditors ...RequestEditorFn) (*GetBookingChargesClientResponse, error)

	// UpdateBookingChargesWithBodyWithResponse Set Booking.com charges
	//
	// Replace the extra-charge set for a Booking.com property. The body carries the target `property_id` and the full `charges` array — Booking treats the write as a full replacement, so include every charge you want to keep.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/booking/charges (the `UpdateBookingCharges` operationId).
	UpdateBookingChargesWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateBookingChargesClientResponse, error)

	// UpdateBookingChargesWithResponse Set Booking.com charges
	//
	// Replace the extra-charge set for a Booking.com property. The body carries the target `property_id` and the full `charges` array — Booking treats the write as a full replacement, so include every charge you want to keep.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/booking/charges (the `UpdateBookingCharges` operationId).
	UpdateBookingChargesWithResponse(ctx context.Context, body UpdateBookingChargesJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateBookingChargesClientResponse, error)

	// GetBookingContentWithResponse Get Booking.com content
	//
	// Read one kind of content for a Booking.com property, straight from Booking.com.
	//
	// | `type` | What it is |
	// |---|---|
	// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
	// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
	// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
	// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
	// | `policies` | Cancellation and prepayment policies. |
	// | `licences` | The region's licence rules and the licence on file. |
	// | `checkin_methods` | How guests get in (holiday homes). |
	// | `contacts` | Who Booking.com contacts about the property. |
	//
	// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/booking/content (the `GetBookingContent` operationId).
	GetBookingContentWithResponse(ctx context.Context, params *GetBookingContentParams, reqEditors ...RequestEditorFn) (*GetBookingContentClientResponse, error)

	// UpdateBookingContentWithBodyWithResponse Update Booking.com content
	//
	// Write one kind of content to a Booking.com property only. Nothing on the canonical listing or on Airbnb changes. To send the listing's own content to every channel instead, use `PUT /v1/listings/{id}/content` and publish.
	//
	// | `type` | What it is |
	// |---|---|
	// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
	// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
	// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
	// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
	// | `policies` | Cancellation and prepayment policies. |
	// | `licences` | The region's licence rules and the licence on file. |
	// | `checkin_methods` | How guests get in (holiday homes). |
	// | `contacts` | Who Booking.com contacts about the property. |
	//
	// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
	//
	// What each `type` takes:
	//
	// - `description`: `text`, optional `language` (default `en`).
	// - `facilities`: `facilities: [{ facility_id | room_facility_id, state: "PRESENT" | "MISSING", instances? }]`. Facilities you do not send stay as they are.
	// - `photos`: `photos: [{ url }]`, uploaded in the background. With `room_id`, send `photo_ids` instead to add photos that have finished processing to that room.
	// - `settings`: `settings: { <block>: {…} }`, for example `{ "pets": { "pets_allowed": "PETS_ALLOWED" } }`. Each block is written separately and reported in `results`.
	// - `policies`: `policyCode` (152 = free cancellation at any time, 1 = non-refundable, …), optional `prepaymentRequired`; add `policyId` to change an existing policy. A property holds at most 7 policies and none can be deleted.
	// - `licences`: `variantId` and `contentData: [{ name, value }]`, from the rules `GET ?type=licences` returns; optional `room_id`.
	// - `checkin_methods`: `methods: [{ checkin_method }]`, using a name from `GET ?type=checkin_methods` `available`.
	// - `contacts`: `contacts: [...]` in Booking.com's contact shape.
	//
	// If Booking.com refuses the write, the response is `422 booking_rejected` with Booking.com's reason, even when Booking.com answered HTTP 200. Resending the same body will be refused again.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/content (the `UpdateBookingContent` operationId).
	UpdateBookingContentWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateBookingContentClientResponse, error)

	// UpdateBookingContentWithResponse Update Booking.com content
	//
	// Write one kind of content to a Booking.com property only. Nothing on the canonical listing or on Airbnb changes. To send the listing's own content to every channel instead, use `PUT /v1/listings/{id}/content` and publish.
	//
	// | `type` | What it is |
	// |---|---|
	// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
	// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
	// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
	// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
	// | `policies` | Cancellation and prepayment policies. |
	// | `licences` | The region's licence rules and the licence on file. |
	// | `checkin_methods` | How guests get in (holiday homes). |
	// | `contacts` | Who Booking.com contacts about the property. |
	//
	// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
	//
	// What each `type` takes:
	//
	// - `description`: `text`, optional `language` (default `en`).
	// - `facilities`: `facilities: [{ facility_id | room_facility_id, state: "PRESENT" | "MISSING", instances? }]`. Facilities you do not send stay as they are.
	// - `photos`: `photos: [{ url }]`, uploaded in the background. With `room_id`, send `photo_ids` instead to add photos that have finished processing to that room.
	// - `settings`: `settings: { <block>: {…} }`, for example `{ "pets": { "pets_allowed": "PETS_ALLOWED" } }`. Each block is written separately and reported in `results`.
	// - `policies`: `policyCode` (152 = free cancellation at any time, 1 = non-refundable, …), optional `prepaymentRequired`; add `policyId` to change an existing policy. A property holds at most 7 policies and none can be deleted.
	// - `licences`: `variantId` and `contentData: [{ name, value }]`, from the rules `GET ?type=licences` returns; optional `room_id`.
	// - `checkin_methods`: `methods: [{ checkin_method }]`, using a name from `GET ?type=checkin_methods` `available`.
	// - `contacts`: `contacts: [...]` in Booking.com's contact shape.
	//
	// If Booking.com refuses the write, the response is `422 booking_rejected` with Booking.com's reason, even when Booking.com answered HTTP 200. Resending the same body will be refused again.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/content (the `UpdateBookingContent` operationId).
	UpdateBookingContentWithResponse(ctx context.Context, body UpdateBookingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateBookingContentClientResponse, error)

	// MapBookingRoomWithBodyWithResponse Map a Booking.com room to a Repull listing
	//
	// Link a Booking.com room to a canonical Repull listing — the API-key equivalent of the room mapping the hosted Connect flow performs, and the counterpart of `POST /v1/channels/airbnb/listings/map`.
	//
	// Discover `roomBookingId` with `GET /v1/channels/booking/properties/{id}/rooms`, which returns every room of a property with the `roomId` this route takes.
	//
	// Booking.com attaches at the ROOM level: a property is a building and its rooms are what a guest books, so each room maps to one listing. Pass `listingId: null` to unmap a room and remove its channel link.
	//
	// The room mapping and its channel link are repointed together in one transaction, so a link can never outlive the mapping it describes — a stale link keeps routing that room's reservations to the previous listing. Re-sending a mapping that is already in place writes nothing (`alreadyMapped: true`).
	//
	// **The property's reservations are pulled as part of the call.** Once the room is mapped, every active reservation Booking.com holds for the property is imported and attached to its listing — `reservationsImported` says how many were processed. One already present is left as it is, so re-sending never duplicates. You do not need a follow-up call: reservations that arrived before the room was mapped are never picked up by the regular sync, so this is the moment they are brought in. It runs on every successful map, including a re-send, so re-sending retries an import that did not run. If the import cannot run, the mapping still stands and `reservationsImported` is `null`. A property with a long booking history can take tens of seconds. Unmapping pulls nothing.
	//
	// Unlike the Airbnb route, there is no conflict when the target listing already carries another Booking.com room: one listing served by several rooms is a normal arrangement and is not refused.
	//
	// Scope is enforced on both sides against your workspace — the room's property and the target listing. A room or listing belonging to another workspace returns the same 404 as one that does not exist.
	//
	// Returns `403 listing_inactive` when the target listing, or the listing the room is mapped to now, is inactive; nothing is changed.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/listings/map (the `MapBookingRoom` operationId).
	MapBookingRoomWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*MapBookingRoomClientResponse, error)

	// MapBookingRoomWithResponse Map a Booking.com room to a Repull listing
	//
	// Link a Booking.com room to a canonical Repull listing — the API-key equivalent of the room mapping the hosted Connect flow performs, and the counterpart of `POST /v1/channels/airbnb/listings/map`.
	//
	// Discover `roomBookingId` with `GET /v1/channels/booking/properties/{id}/rooms`, which returns every room of a property with the `roomId` this route takes.
	//
	// Booking.com attaches at the ROOM level: a property is a building and its rooms are what a guest books, so each room maps to one listing. Pass `listingId: null` to unmap a room and remove its channel link.
	//
	// The room mapping and its channel link are repointed together in one transaction, so a link can never outlive the mapping it describes — a stale link keeps routing that room's reservations to the previous listing. Re-sending a mapping that is already in place writes nothing (`alreadyMapped: true`).
	//
	// **The property's reservations are pulled as part of the call.** Once the room is mapped, every active reservation Booking.com holds for the property is imported and attached to its listing — `reservationsImported` says how many were processed. One already present is left as it is, so re-sending never duplicates. You do not need a follow-up call: reservations that arrived before the room was mapped are never picked up by the regular sync, so this is the moment they are brought in. It runs on every successful map, including a re-send, so re-sending retries an import that did not run. If the import cannot run, the mapping still stands and `reservationsImported` is `null`. A property with a long booking history can take tens of seconds. Unmapping pulls nothing.
	//
	// Unlike the Airbnb route, there is no conflict when the target listing already carries another Booking.com room: one listing served by several rooms is a normal arrangement and is not refused.
	//
	// Scope is enforced on both sides against your workspace — the room's property and the target listing. A room or listing belonging to another workspace returns the same 404 as one that does not exist.
	//
	// Returns `403 listing_inactive` when the target listing, or the listing the room is mapped to now, is inactive; nothing is changed.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/listings/map (the `MapBookingRoom` operationId).
	MapBookingRoomWithResponse(ctx context.Context, body MapBookingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*MapBookingRoomClientResponse, error)

	// GetBookingListingPricingWithResponse Get Booking.com pricing for a listing
	//
	// Resolves the Repull listing id to its Booking.com `hotel_id` (via the room mapping the Connect flow records for the authenticated workspace), then proxies Booking's `getRoomRateAvailability` for the requested window. Pricing on Booking is per-room/per-rate-plan, so `room_id` and `room_level` flow through query params unchanged.
	//
	// Mirrors the per-channel `/listings/{id}/pricing` shape used by Airbnb so SDK consumers can carry a Repull listing id across channels. `id` is a Repull listing id, never a Booking.com hotel id — the hotel-id surface is `/v1/channels/booking/availability`.
	//
	// A listing can be published under several Booking.com properties. GET uses the oldest and reports the rest in `otherHotelIds`; PUT refuses with `409 ambiguous_booking_mapping` rather than push rates into a property it guessed at. `?hotel_id=` names the property explicitly for either.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/booking/listings/{id}/pricing (the `GetBookingListingPricing` operationId).
	GetBookingListingPricingWithResponse(ctx context.Context, id int, params *GetBookingListingPricingParams, reqEditors ...RequestEditorFn) (*GetBookingListingPricingClientResponse, error)

	// UpdateBookingListingPricingWithBodyWithResponse Update Booking.com pricing for a listing
	//
	// Writes nightly prices for a listing's Booking.com room + rate plan. Each update needs `roomId` + `rateId` + `dateRange` + `price` + `currency`; `dateRange` is inclusive at both ends, so `start` equal to `end` writes exactly one night.
	//
	// **Occupancy.** Booking.com stores a rate amount against the party size the rate plan prices. Send `occupancy` and that is what is used; omit it and it is resolved from Booking.com's own data for that (room, rate plan) and echoed back in `occupancy[]` with its `source`. When it cannot be resolved the write is refused with `422` naming `updates[N].occupancy` — a price is never sent at a guessed party size, because Booking.com declines such an amount without saying so.
	//
	// **Inventory is a separate write.** `roomsToSell` on a rate update returns `422 inventory_not_in_rate_update`; use `PUT /v1/channels/booking/availability` with `type: "availability"`.
	//
	// **Restrictions ride along, on their own wire.** Send `restrictions` with the price and Booking.com receives two writes; the response reports each separately in `price` and `restrictions`, each with its own state, read-back and — when refused — Booking.com's own reason. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` (Booking.com's notification has no element for them — set those on the rate plan in the Extranet). Nothing you send is silently ignored.
	//
	// **The response says what is known.** Booking.com acknowledges a write without per-date status, so the affected nights are read back — prices and restrictions out of the same read — and `applied` reports `verified`, `mismatch`, `partial`, `rejected` or `unverified`. `partial` means one half landed and the other did not, which is never reported as a total failure. Send `verify: false` to skip the read-back; `applied` is then `unverified`. Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
	//
	// `id` is a Repull listing id. When it is published under several Booking.com properties this returns `409 ambiguous_booking_mapping` and pushes nothing — name the property with `?hotel_id=` instead.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/booking/listings/{id}/pricing (the `UpdateBookingListingPricing` operationId).
	UpdateBookingListingPricingWithBodyWithResponse(ctx context.Context, id int, params *UpdateBookingListingPricingParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateBookingListingPricingClientResponse, error)

	// UpdateBookingListingPricingWithResponse Update Booking.com pricing for a listing
	//
	// Writes nightly prices for a listing's Booking.com room + rate plan. Each update needs `roomId` + `rateId` + `dateRange` + `price` + `currency`; `dateRange` is inclusive at both ends, so `start` equal to `end` writes exactly one night.
	//
	// **Occupancy.** Booking.com stores a rate amount against the party size the rate plan prices. Send `occupancy` and that is what is used; omit it and it is resolved from Booking.com's own data for that (room, rate plan) and echoed back in `occupancy[]` with its `source`. When it cannot be resolved the write is refused with `422` naming `updates[N].occupancy` — a price is never sent at a guessed party size, because Booking.com declines such an amount without saying so.
	//
	// **Inventory is a separate write.** `roomsToSell` on a rate update returns `422 inventory_not_in_rate_update`; use `PUT /v1/channels/booking/availability` with `type: "availability"`.
	//
	// **Restrictions ride along, on their own wire.** Send `restrictions` with the price and Booking.com receives two writes; the response reports each separately in `price` and `restrictions`, each with its own state, read-back and — when refused — Booking.com's own reason. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` (Booking.com's notification has no element for them — set those on the rate plan in the Extranet). Nothing you send is silently ignored.
	//
	// **The response says what is known.** Booking.com acknowledges a write without per-date status, so the affected nights are read back — prices and restrictions out of the same read — and `applied` reports `verified`, `mismatch`, `partial`, `rejected` or `unverified`. `partial` means one half landed and the other did not, which is never reported as a total failure. Send `verify: false` to skip the read-back; `applied` is then `unverified`. Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
	//
	// `id` is a Repull listing id. When it is published under several Booking.com properties this returns `409 ambiguous_booking_mapping` and pushes nothing — name the property with `?hotel_id=` instead.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/booking/listings/{id}/pricing (the `UpdateBookingListingPricing` operationId).
	UpdateBookingListingPricingWithResponse(ctx context.Context, id int, params *UpdateBookingListingPricingParams, body UpdateBookingListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateBookingListingPricingClientResponse, error)

	// ListBookingConversationsWithResponse List Booking.com conversations
	//
	// List Booking.com guest conversations. Cursor-paginated. Use the messaging POST to send a reply.
	//
	// Scoped to this workspace. With `property_id`, the property must be connected to this workspace — any other id returns `404 not_found`. Without it, only messages for this workspace's own Booking.com properties are returned.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/booking/messaging (the `ListBookingConversations` operationId).
	ListBookingConversationsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListBookingConversationsClientResponse, error)

	// SendBookingMessageWithBodyWithResponse Send Booking.com message
	//
	// Send a text message in a Booking.com conversation as the host. Booking enforces content rules similar to Airbnb.
	//
	// **Text only.** To send photos, use `POST /v1/conversations/{id}/messages` with `attachments` (JPEG or PNG, up to 10 MB each, with message text) — it uploads the files to Booking.com and records them in the conversation. Passing `attachments`, `attachment_ids` or `mediaUrl` here returns `422 attachments_not_supported` and sends nothing.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/messaging (the `SendBookingMessage` operationId).
	SendBookingMessageWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SendBookingMessageClientResponse, error)

	// SendBookingMessageWithResponse Send Booking.com message
	//
	// Send a text message in a Booking.com conversation as the host. Booking enforces content rules similar to Airbnb.
	//
	// **Text only.** To send photos, use `POST /v1/conversations/{id}/messages` with `attachments` (JPEG or PNG, up to 10 MB each, with message text) — it uploads the files to Booking.com and records them in the conversation. Passing `attachments`, `attachment_ids` or `mediaUrl` here returns `422 attachments_not_supported` and sends nothing.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/messaging (the `SendBookingMessage` operationId).
	SendBookingMessageWithResponse(ctx context.Context, body SendBookingMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*SendBookingMessageClientResponse, error)

	// ListBookingPropertiesWithResponse List Booking.com properties
	//
	// List every Booking.com property this workspace holds. Each property is returned ONCE, with the Repull listings mapped under it.
	//
	// A Booking.com property is a building; its rooms are what guests book, and each room is mapped to one Repull listing — so one property routinely carries many listings. `listings[].roomBookingId` is the Booking.com room id an ARI write takes.
	//
	// A property whose rooms are not mapped yet is still listed, with `mappingStatus: "unmapped"` and an empty `listings` array. That is a real mid-onboarding state, not an error: finish `POST /v1/connect/booking/map-rooms` and the listings appear. Such a property used to be dropped silently, which made a mapped-but-unreadable workspace indistinguishable from one with no Booking connection at all.
	//
	// Inactive listings are left out of `listings`; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/booking/properties (the `ListBookingProperties` operationId).
	ListBookingPropertiesWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListBookingPropertiesClientResponse, error)

	// GetBookingPropertyWithResponse Get Booking.com connection for a listing
	//
	// Return the Booking.com connection record(s) for a Repull listing — the linked Booking hotel id, sync flags, markup, sync category, suspension state, and the Booking room the mapping runs through.
	//
	// `id` is a **Repull listing id**, not a Booking.com hotel id, despite the `properties` segment. (The hotel-id surface is `/v1/channels/booking/availability`.) The mapping is read from wherever the Connect flow recorded it — `listings_booking_rooms` for anything mapped through `POST /v1/connect/booking/map-rooms`, which is essentially every live mapping.
	//
	// An ARRAY, because one listing can be published under several Booking.com properties at once; `mappedVia` says which record carries each mapping. A listing with no Booking.com mapping returns 404, and the message says which id space the path takes.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/booking/properties/{id} (the `GetBookingProperty` operationId).
	GetBookingPropertyWithResponse(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*GetBookingPropertyClientResponse, error)

	// BookingPropertyActionWithBodyWithResponse Take a property off sale / put it back (unlist/relist)
	//
	// Stop this listing's Booking.com property being sold, or start it again. `id` is a **Repull listing id**, not a Booking.com hotel id, as on the GET.
	//
	// **Booking.com has no unlist, so this is an availability write.** Airbnb has a real deactivate; Booking.com does not. `unlist` closes the mapped room across the whole forward window, so the property stops selling. `relist` is not its mirror image: it re-syncs the true calendar, so dates that are genuinely blocked (a reservation, an owner stay) stay blocked and only the closure `unlist` wrote lifts. Re-opening everything would sell dates that are not for sale.
	//
	// **Which property gets closed.** A listing can be mapped to more than one Booking.com property — the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. With exactly one, send nothing. With several, name one with `hotelId` (or `?hotel_id=`); omit it and the request is refused with **`409 ambiguous_booking_mapping`** listing the candidates, and nothing is written. That refusal matters more here than on a publish: writing content into the wrong property is recoverable, closing the wrong property's availability takes real inventory off sale while the property you meant keeps selling. Naming a property this listing is not mapped to is a `404` that names the ones it is.
	//
	// **This does not change the listing in Repull.** `active` — what Repull bills and serves — is untouched by both actions and is deliberately not echoed in the response, so the two ideas can never be read as one field. To take a listing off the market on every channel at once, use `POST /v1/listings/{id}/offline`.
	//
	// Any other action returns a structured `422` naming the ones that are supported. To push content use `POST /v1/listings/{id}/publish/booking`; to map rooms use `POST /v1/connect/booking/map-rooms`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/properties/{id} (the `BookingPropertyAction` operationId).
	BookingPropertyActionWithBodyWithResponse(ctx context.Context, id int, params *BookingPropertyActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BookingPropertyActionClientResponse, error)

	// BookingPropertyActionWithResponse Take a property off sale / put it back (unlist/relist)
	//
	// Stop this listing's Booking.com property being sold, or start it again. `id` is a **Repull listing id**, not a Booking.com hotel id, as on the GET.
	//
	// **Booking.com has no unlist, so this is an availability write.** Airbnb has a real deactivate; Booking.com does not. `unlist` closes the mapped room across the whole forward window, so the property stops selling. `relist` is not its mirror image: it re-syncs the true calendar, so dates that are genuinely blocked (a reservation, an owner stay) stay blocked and only the closure `unlist` wrote lifts. Re-opening everything would sell dates that are not for sale.
	//
	// **Which property gets closed.** A listing can be mapped to more than one Booking.com property — the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. With exactly one, send nothing. With several, name one with `hotelId` (or `?hotel_id=`); omit it and the request is refused with **`409 ambiguous_booking_mapping`** listing the candidates, and nothing is written. That refusal matters more here than on a publish: writing content into the wrong property is recoverable, closing the wrong property's availability takes real inventory off sale while the property you meant keeps selling. Naming a property this listing is not mapped to is a `404` that names the ones it is.
	//
	// **This does not change the listing in Repull.** `active` — what Repull bills and serves — is untouched by both actions and is deliberately not echoed in the response, so the two ideas can never be read as one field. To take a listing off the market on every channel at once, use `POST /v1/listings/{id}/offline`.
	//
	// Any other action returns a structured `422` naming the ones that are supported. To push content use `POST /v1/listings/{id}/publish/booking`; to map rooms use `POST /v1/connect/booking/map-rooms`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/properties/{id} (the `BookingPropertyAction` operationId).
	BookingPropertyActionWithResponse(ctx context.Context, id int, params *BookingPropertyActionParams, body BookingPropertyActionJSONRequestBody, reqEditors ...RequestEditorFn) (*BookingPropertyActionClientResponse, error)

	// ListBookingPropertyRoomsWithResponse List Booking.com rooms + rate-plan ids for a listing
	//
	// Return every Booking.com room and its rate plans for a listing, each with the `roomId` / `rateId` needed to assemble a restriction write via `PUT /v1/channels/booking/availability`.
	//
	// `id` is a **Repull listing id**, not a Booking.com hotel id, despite the `properties` segment — resolved to the Booking `hotel_id` through the workspace mapping, read from wherever the Connect flow recorded it (`listings_booking_rooms` for anything mapped through `POST /v1/connect/booking/map-rooms`). A listing with no active Booking.com mapping returns 404, and the message says which id space the path takes. When the listing is published under several properties the oldest is used, the rest come back in `otherHotelIds`, and `?hotel_id=` names a different one. Sourced from Booking's B.XML roomrates feed, which returns rooms and rate plans together (the rooms-unit feed alone omits rate-plan ids). This is the API-key surface for the room/rate ids that were previously only reachable inside the hosted Connect room-mapping flow.
	//
	// `source` says where the answer came from. `booking` means it was read live just now. If Booking.com returns nothing usable for the property, the rooms and rate plans recorded at the last import are served instead, `source` is `mirror`, and `mirrorReason` names what went wrong live — the ids are Booking.com's own and can be written against, but they can be stale, and `maxPersons`, `policy`, `policyId`, `pricingType` and `isChildRate` come back `null` because only the live feed states them. `rooms` is empty only when Booking.com and the last import both have nothing; a read that failed is an error, never an empty list.
	//
	// Each rate plan carries `maxPersons` — the party size that rate plan prices, which is the `occupancy` a rate amount must be written at. Each room carries `maxAdults`, Booking.com's capacity for the room, which is what a rate write falls back to when the rate plan states no `maxPersons`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/booking/properties/{id}/rooms (the `ListBookingPropertyRooms` operationId).
	ListBookingPropertyRoomsWithResponse(ctx context.Context, id int, params *ListBookingPropertyRoomsParams, reqEditors ...RequestEditorFn) (*ListBookingPropertyRoomsClientResponse, error)

	// ListBookingReservationsWithResponse List Booking.com reservations
	//
	// Pull reservations from Booking.com. `type=new` (default) returns un-acknowledged bookings; `type=modified` returns changed bookings. Pass both `reservation_id` and `hotel_id` to fetch a single reservation's full details. Acknowledge processed reservations with the POST so Booking stops re-serving them in the `new` queue.
	//
	// Scoped to this workspace. `hotel_id` (or its alias `property_id`) must be a property connected to this workspace; any other id returns `404 not_found`, the same as an id that does not exist. Without a hotel, `new`/`modified` cover every Booking.com property this workspace holds (and return `404 not_found` if it holds none). A `reservation_id` that belongs to another workspace returns `404 not_found`.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/booking/reservations (the `ListBookingReservations` operationId).
	ListBookingReservationsWithResponse(ctx context.Context, params *ListBookingReservationsParams, reqEditors ...RequestEditorFn) (*ListBookingReservationsClientResponse, error)

	// AcknowledgeBookingReservationsWithBodyWithResponse Acknowledge Booking.com reservations
	//
	// Acknowledge one or more reservations so Booking.com removes them from the `new` queue. The body carries `reservation_ids` (non-empty array). Acknowledge only after you have durably persisted each reservation.
	//
	// Only reservations that belong to this workspace can be acknowledged. If any id in `reservation_ids` is not one of this workspace's Booking.com reservations (`confirmationCode` on `GET /v1/reservations?platform=booking`), nothing is acknowledged and the response is `404 not_found` naming those ids in `reservation_ids`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/reservations (the `AcknowledgeBookingReservations` operationId).
	AcknowledgeBookingReservationsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AcknowledgeBookingReservationsClientResponse, error)

	// AcknowledgeBookingReservationsWithResponse Acknowledge Booking.com reservations
	//
	// Acknowledge one or more reservations so Booking.com removes them from the `new` queue. The body carries `reservation_ids` (non-empty array). Acknowledge only after you have durably persisted each reservation.
	//
	// Only reservations that belong to this workspace can be acknowledged. If any id in `reservation_ids` is not one of this workspace's Booking.com reservations (`confirmationCode` on `GET /v1/reservations?platform=booking`), nothing is acknowledged and the response is `404 not_found` naming those ids in `reservation_ids`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/reservations (the `AcknowledgeBookingReservations` operationId).
	AcknowledgeBookingReservationsWithResponse(ctx context.Context, body AcknowledgeBookingReservationsJSONRequestBody, reqEditors ...RequestEditorFn) (*AcknowledgeBookingReservationsClientResponse, error)

	// ListBookingReviewsWithResponse List Booking.com reviews
	//
	// List guest reviews for a Booking.com property. Pass `property_id` (the Booking.com hotel id) as a query param — required.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/booking/reviews (the `ListBookingReviews` operationId).
	ListBookingReviewsWithResponse(ctx context.Context, params *ListBookingReviewsParams, reqEditors ...RequestEditorFn) (*ListBookingReviewsClientResponse, error)

	// ReplyBookingReviewWithBodyWithResponse Reply to Booking.com review
	//
	// Post a public host reply to a guest review on Booking.com. Booking allows one host reply per review — repeated POSTs are rejected by upstream.
	//
	// Booking.com does NOT support host-authored reviews of guests via the API (platform-level limitation), so this endpoint is reply-only.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/reviews (the `ReplyBookingReview` operationId).
	ReplyBookingReviewWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReplyBookingReviewClientResponse, error)

	// ReplyBookingReviewWithResponse Reply to Booking.com review
	//
	// Post a public host reply to a guest review on Booking.com. Booking allows one host reply per review — repeated POSTs are rejected by upstream.
	//
	// Booking.com does NOT support host-authored reviews of guests via the API (platform-level limitation), so this endpoint is reply-only.
	//
	// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
	//
	// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/reviews (the `ReplyBookingReview` operationId).
	ReplyBookingReviewWithResponse(ctx context.Context, body ReplyBookingReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*ReplyBookingReviewClientResponse, error)

	// BookingSetupWithBodyWithResponse Booking.com property setup actions
	//
	// Action-router for putting a property onto Booking.com — including building one from nothing. Select the step with `action`.
	//
	// ## Opening a property
	//
	// - `create-property` — create a NEW Booking.com property for a Repull listing (`listing_id`). Creates the property, its first room with the listing's beds, a rate plan and the room-rate product that makes the room sellable (under the listing's cancellation policy), sets the contact and invoice details and the facilities, seeds availability and rates, syncs the calendar, then runs Booking.com's readiness check and reports what still blocks opening in `warnings`. Send `contact` (`name`, `email`, `phone` in international form); without it the workspace owner is used, and a workspace with no usable contact is refused before anything is created. Returns 201.
	// - `add-room` — add another room type (and its sellable product) to a property (`listing_id`, `property_id`). Returns 201.
	// - `add-unit` — raise the number of identical units on an existing room (`listing_id`, `property_id`, `room_id`).
	// - `advance` — run Booking.com's readiness check for a property (`property_id`) and, when it passes, open it. Returns `checked`, `opened`, `sellable` and `blockers` — Booking.com's own reasons it cannot open yet.
	//
	// ## Account and policy steps
	//
	// - `create-legal-entity` — register a legal entity directly (returns 201). Not normally needed: see the legal-entity rules below.
	// - `check-legal-status` — always `404`. A legal entity's details are readable for any id on the connectivity-provider credentials every workspace shares, and nothing records which workspace registered which entity, so no entity can be shown to be yours. `create-property` resolves it for you.
	// - `check-readiness` — whether a property is ready to open (`property_id`): `ready` and `blockers`, without trying to open it.
	// - `open-property` — open the property for sale (`property_id`). Refused with `422 booking_rejected` naming the blockers when it is not ready.
	// - `set-contacts` — set property contacts (`property_id`, `contacts` in Booking.com's Contacts API shape; at most one carries the `general` profile).
	// - `set-policies` — add a cancellation policy (`property_id`, `policyCode`, optional `prepaymentRequired`). House rules, pets, children and the damage deposit are `POST /v1/channels/booking/content` with `type: "settings"`.
	//
	// ## Three things about Booking.com that cost real money
	//
	// **A newly created property is NOT sellable.** Booking.com opens it only when its readiness check passes, and the check names what is missing — a main photo still processing, no availability, a licence the region requires. The response always reports `status: "being_built"` and `sellable: false`, never a guess, with the reasons in `warnings`. Resolve them, then `advance`.
	//
	// **A room with no ACTIVE rate plan is invisible.** Booking only renders rooms that have at least one active product linkage (room × rate plan). A room can be created successfully, return a `roomId`, and never appear on the property page. If `rateId` comes back `null` from `create-property` or `add-room`, that is exactly what happened: activate a rate plan on the property in the Extranet, then add the room again.
	//
	// **Room names are Booking.com's.** Travellers see one of Booking.com's standard names ("Two-Bedroom Apartment"), chosen from the listing's bedrooms. The listing's own name is kept as the operator-side reference, never shown to guests.
	//
	// ## The legal entity is resolved, not asked for
	//
	// A property is created against the legal entity Booking.com contracts with, invoices and pays. You do not normally send one:
	//
	// 1. If this workspace already creates properties under a legal entity, that one is reused. A second is never registered.
	// 2. If it has none and the request carries `legal_entity` (`company_name`, `legal_contact_name`, `legal_contact_email`), one is registered and used. Booking.com emails the legal contact a contract; creation only succeeds once it is signed.
	// 3. If it has none and no `legal_entity`, the request is refused with `422 legal_entity_required` naming the fields — a contracted company is never invented.
	//
	// `legal_entity_id` overrides all of that. An id that already carries another workspace's properties is refused with `403 legal_entity_not_yours` before anything is created.
	//
	// ## What you do not control
	//
	// Properties are created against Booking's **production** target only. A test-target property cannot be sold through and there is no route back from one, so `target` is not a parameter — sending it changes nothing.
	//
	// The property category comes from the listing's property type (Apartment when it has none; Holiday home, Villa or Chalet when it says so). The initial room count is 1. Latitude and longitude come from the listing and are adjusted slightly to clear Booking.com's duplicate detection — send the property's true position on the listing and do not pre-adjust it yourself.
	//
	// The listing's name, check-in/check-out times, currency, capacity and price come from the listing. Its postal code is taken from the listing's own `postalCode`; when the listing has none, it falls back to a connected Airbnb listing. A listing with neither is created without a postal code, so set `postalCode` on the listing first.
	//
	// ## Guards
	//
	// Every action that takes a `property_id` requires a property connected to this workspace; any other id returns `404 not_found`. Every action that takes a `listing_id` requires a listing in this workspace; any other id returns `404 not_found`.
	//
	// `create-property` refuses a listing with no coordinates (`422 missing_coordinates`) before anything is created — creating a Booking.com property cannot be undone.
	//
	// `create-property` is subject to the same published-listing gate as the dashboard: no plan, or the plan's listing limit reached, returns `403 billing_error` with `used` and `limit`, and nothing is created.
	//
	// Returns `403 listing_inactive` when the listing — or any listing mapped to the Booking.com property — is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// If a property is created and a later step fails, the response is `422 booking_create_partial` carrying `property_id`. The property EXISTS. Do not retry `create-property`, which would open a second one — continue with `add-room` and `advance`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/setup (the `BookingSetup` operationId).
	BookingSetupWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BookingSetupClientResponse, error)

	// BookingSetupWithResponse Booking.com property setup actions
	//
	// Action-router for putting a property onto Booking.com — including building one from nothing. Select the step with `action`.
	//
	// ## Opening a property
	//
	// - `create-property` — create a NEW Booking.com property for a Repull listing (`listing_id`). Creates the property, its first room with the listing's beds, a rate plan and the room-rate product that makes the room sellable (under the listing's cancellation policy), sets the contact and invoice details and the facilities, seeds availability and rates, syncs the calendar, then runs Booking.com's readiness check and reports what still blocks opening in `warnings`. Send `contact` (`name`, `email`, `phone` in international form); without it the workspace owner is used, and a workspace with no usable contact is refused before anything is created. Returns 201.
	// - `add-room` — add another room type (and its sellable product) to a property (`listing_id`, `property_id`). Returns 201.
	// - `add-unit` — raise the number of identical units on an existing room (`listing_id`, `property_id`, `room_id`).
	// - `advance` — run Booking.com's readiness check for a property (`property_id`) and, when it passes, open it. Returns `checked`, `opened`, `sellable` and `blockers` — Booking.com's own reasons it cannot open yet.
	//
	// ## Account and policy steps
	//
	// - `create-legal-entity` — register a legal entity directly (returns 201). Not normally needed: see the legal-entity rules below.
	// - `check-legal-status` — always `404`. A legal entity's details are readable for any id on the connectivity-provider credentials every workspace shares, and nothing records which workspace registered which entity, so no entity can be shown to be yours. `create-property` resolves it for you.
	// - `check-readiness` — whether a property is ready to open (`property_id`): `ready` and `blockers`, without trying to open it.
	// - `open-property` — open the property for sale (`property_id`). Refused with `422 booking_rejected` naming the blockers when it is not ready.
	// - `set-contacts` — set property contacts (`property_id`, `contacts` in Booking.com's Contacts API shape; at most one carries the `general` profile).
	// - `set-policies` — add a cancellation policy (`property_id`, `policyCode`, optional `prepaymentRequired`). House rules, pets, children and the damage deposit are `POST /v1/channels/booking/content` with `type: "settings"`.
	//
	// ## Three things about Booking.com that cost real money
	//
	// **A newly created property is NOT sellable.** Booking.com opens it only when its readiness check passes, and the check names what is missing — a main photo still processing, no availability, a licence the region requires. The response always reports `status: "being_built"` and `sellable: false`, never a guess, with the reasons in `warnings`. Resolve them, then `advance`.
	//
	// **A room with no ACTIVE rate plan is invisible.** Booking only renders rooms that have at least one active product linkage (room × rate plan). A room can be created successfully, return a `roomId`, and never appear on the property page. If `rateId` comes back `null` from `create-property` or `add-room`, that is exactly what happened: activate a rate plan on the property in the Extranet, then add the room again.
	//
	// **Room names are Booking.com's.** Travellers see one of Booking.com's standard names ("Two-Bedroom Apartment"), chosen from the listing's bedrooms. The listing's own name is kept as the operator-side reference, never shown to guests.
	//
	// ## The legal entity is resolved, not asked for
	//
	// A property is created against the legal entity Booking.com contracts with, invoices and pays. You do not normally send one:
	//
	// 1. If this workspace already creates properties under a legal entity, that one is reused. A second is never registered.
	// 2. If it has none and the request carries `legal_entity` (`company_name`, `legal_contact_name`, `legal_contact_email`), one is registered and used. Booking.com emails the legal contact a contract; creation only succeeds once it is signed.
	// 3. If it has none and no `legal_entity`, the request is refused with `422 legal_entity_required` naming the fields — a contracted company is never invented.
	//
	// `legal_entity_id` overrides all of that. An id that already carries another workspace's properties is refused with `403 legal_entity_not_yours` before anything is created.
	//
	// ## What you do not control
	//
	// Properties are created against Booking's **production** target only. A test-target property cannot be sold through and there is no route back from one, so `target` is not a parameter — sending it changes nothing.
	//
	// The property category comes from the listing's property type (Apartment when it has none; Holiday home, Villa or Chalet when it says so). The initial room count is 1. Latitude and longitude come from the listing and are adjusted slightly to clear Booking.com's duplicate detection — send the property's true position on the listing and do not pre-adjust it yourself.
	//
	// The listing's name, check-in/check-out times, currency, capacity and price come from the listing. Its postal code is taken from the listing's own `postalCode`; when the listing has none, it falls back to a connected Airbnb listing. A listing with neither is created without a postal code, so set `postalCode` on the listing first.
	//
	// ## Guards
	//
	// Every action that takes a `property_id` requires a property connected to this workspace; any other id returns `404 not_found`. Every action that takes a `listing_id` requires a listing in this workspace; any other id returns `404 not_found`.
	//
	// `create-property` refuses a listing with no coordinates (`422 missing_coordinates`) before anything is created — creating a Booking.com property cannot be undone.
	//
	// `create-property` is subject to the same published-listing gate as the dashboard: no plan, or the plan's listing limit reached, returns `403 billing_error` with `used` and `limit`, and nothing is created.
	//
	// Returns `403 listing_inactive` when the listing — or any listing mapped to the Booking.com property — is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// If a property is created and a later step fails, the response is `422 booking_create_partial` carrying `property_id`. The property EXISTS. Do not retry `create-property`, which would open a second one — continue with `add-room` and `advance`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/setup (the `BookingSetup` operationId).
	BookingSetupWithResponse(ctx context.Context, body BookingSetupJSONRequestBody, reqEditors ...RequestEditorFn) (*BookingSetupClientResponse, error)

	// DeleteBookingWebhookWithResponse Unsubscribe from a Booking.com notification
	//
	// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/channels/booking/webhooks (the `DeleteBookingWebhook` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	DeleteBookingWebhookWithResponse(ctx context.Context, params *DeleteBookingWebhookParams, reqEditors ...RequestEditorFn) (*DeleteBookingWebhookClientResponse, error)

	// ListBookingWebhooksWithResponse List Booking.com webhook subscriptions
	//
	// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/booking/webhooks (the `ListBookingWebhooks` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	ListBookingWebhooksWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListBookingWebhooksClientResponse, error)

	// CreateBookingWebhookWithBodyWithResponse Subscribe to a Booking.com notification
	//
	// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/webhooks (the `CreateBookingWebhook` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	CreateBookingWebhookWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateBookingWebhookClientResponse, error)

	// CreateBookingWebhookWithResponse Subscribe to a Booking.com notification
	//
	// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/channels/booking/webhooks (the `CreateBookingWebhook` operationId).
	//
	// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
	CreateBookingWebhookWithResponse(ctx context.Context, body CreateBookingWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateBookingWebhookClientResponse, error)

	// GetPlumguideAvailabilityWithResponse Get Plumguide availability
	//
	// Read the per-day availability calendar for a Plumguide listing. Returns the same row shape as Airbnb availability for SDK convenience.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/plumguide/availability (the `GetPlumguideAvailability` operationId).
	GetPlumguideAvailabilityWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetPlumguideAvailabilityClientResponse, error)

	// UpdatePlumguideAvailabilityWithResponse Push availability to Plumguide
	//
	// Push per-day availability changes to Plumguide. Plumguide accepts only the next 24 months — dates beyond that are silently ignored.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/plumguide/availability (the `UpdatePlumguideAvailability` operationId).
	UpdatePlumguideAvailabilityWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*UpdatePlumguideAvailabilityClientResponse, error)

	// ListPlumguideBookingsWithResponse List Plumguide bookings
	//
	// List Plumguide bookings. Default returns all bookings; pass `listing_id` to filter to one listing, or `booking_code` to fetch a single booking.
	//
	// Returns `403 listing_inactive` when `listing_id` or `booking_code` resolves to an inactive listing. The unfiltered list is read straight from Plum Guide and is not filtered by listing status.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/plumguide/bookings (the `ListPlumguideBookings` operationId).
	ListPlumguideBookingsWithResponse(ctx context.Context, params *ListPlumguideBookingsParams, reqEditors ...RequestEditorFn) (*ListPlumguideBookingsClientResponse, error)

	// ListPlumguideListingsWithResponse List Plumguide listings
	//
	// List Plumguide listings this workspace has access to. Plumguide is approval-based — listings appear once Plumguide has accepted them.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/plumguide/listings (the `ListPlumguideListings` operationId).
	ListPlumguideListingsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListPlumguideListingsClientResponse, error)

	// GetPlumguidePricingWithResponse Get Plumguide pricing
	//
	// Read the current pricing for a Plumguide listing (base price, currency, weekend uplift).
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/plumguide/pricing (the `GetPlumguidePricing` operationId).
	GetPlumguidePricingWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetPlumguidePricingClientResponse, error)

	// UpdatePlumguidePricingWithResponse Push pricing to Plumguide
	//
	// Push pricing changes to Plumguide. Plumguide rounds all prices to whole units of the listing currency — sub-unit precision is silently truncated.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/plumguide/pricing (the `UpdatePlumguidePricing` operationId).
	UpdatePlumguidePricingWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*UpdatePlumguidePricingClientResponse, error)

	// DeletePlumguideWebhooksWithResponse Remove Plumguide webhook config
	//
	// Delete the Plumguide webhook configuration for this workspace. Plumguide stops delivering webhooks until a new config is set.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/channels/plumguide/webhooks (the `DeletePlumguideWebhooks` operationId).
	DeletePlumguideWebhooksWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*DeletePlumguideWebhooksClientResponse, error)

	// GetPlumguideWebhooksWithResponse Get Plumguide webhook config
	//
	// Read the current Plumguide webhook configuration for this workspace.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/plumguide/webhooks (the `GetPlumguideWebhooks` operationId).
	GetPlumguideWebhooksWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetPlumguideWebhooksClientResponse, error)

	// UpdatePlumguideWebhooksWithBodyWithResponse Replace Plumguide webhook config
	//
	// Replace the Plumguide webhook configuration. The body carries the full webhook config payload — this is a full replacement, not a patch.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/plumguide/webhooks (the `UpdatePlumguideWebhooks` operationId).
	UpdatePlumguideWebhooksWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdatePlumguideWebhooksClientResponse, error)

	// UpdatePlumguideWebhooksWithResponse Replace Plumguide webhook config
	//
	// Replace the Plumguide webhook configuration. The body carries the full webhook config payload — this is a full replacement, not a patch.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/channels/plumguide/webhooks (the `UpdatePlumguideWebhooks` operationId).
	UpdatePlumguideWebhooksWithResponse(ctx context.Context, body UpdatePlumguideWebhooksJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdatePlumguideWebhooksClientResponse, error)

	// ListVrboListingsWithResponse List VRBO listings
	//
	// List VRBO listings this workspace owns. VRBO is agency-model — Repull reads listings via the public iCal/HTTP feeds.
	//
	// Inactive listings are left out; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/vrbo/listings (the `ListVrboListings` operationId).
	ListVrboListingsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListVrboListingsClientResponse, error)

	// ListVrboReservationsWithResponse List VRBO reservations
	//
	// Cursor-paginated list of VRBO reservations sourced from the public booking feed. Lag is typically 5-10 minutes vs. Airbnb / Booking.com. `?offset=` is accepted as a first-class alias for `?cursor=` (mutually exclusive; offset capped at 10000).
	//
	// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/channels/vrbo/reservations (the `ListVrboReservations` operationId).
	ListVrboReservationsWithResponse(ctx context.Context, params *ListVrboReservationsParams, reqEditors ...RequestEditorFn) (*ListVrboReservationsClientResponse, error)

	// ListConnectionsWithResponse List PMS/OTA connections
	//
	// Returns all active connections to PMS and OTA platforms.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/connect (the `ListConnections` operationId).
	ListConnectionsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListConnectionsClientResponse, error)

	// CreateConnectSessionWithBodyWithResponse Create a multi-channel Connect picker session
	//
	// Mints a session that lands the user on the channel picker at `connect.repull.dev/{sessionId}` instead of jumping straight to a single provider. The user picks a channel from the registry, the picker page POSTs `selectConnectProvider` to bind the choice, and the per-provider flow takes over.
	//
	// Use this when you want one entry point for all 13 channels. Use `POST /v1/connect/{provider}` instead when your UI already knows which channel to connect.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect (the `CreateConnectSession` operationId).
	CreateConnectSessionWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateConnectSessionClientResponse, error)

	// CreateConnectSessionWithResponse Create a multi-channel Connect picker session
	//
	// Mints a session that lands the user on the channel picker at `connect.repull.dev/{sessionId}` instead of jumping straight to a single provider. The user picks a channel from the registry, the picker page POSTs `selectConnectProvider` to bind the choice, and the per-provider flow takes over.
	//
	// Use this when you want one entry point for all 13 channels. Use `POST /v1/connect/{provider}` instead when your UI already knows which channel to connect.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect (the `CreateConnectSession` operationId).
	CreateConnectSessionWithResponse(ctx context.Context, body CreateConnectSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateConnectSessionClientResponse, error)

	// SubmitBeds24CredentialsWithBodyWithResponse Submit Beds24 credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Beds24. API key + prop key from Beds24 → Settings → Apps & Integrations.
	//
	// The credentials are validated against Beds24 before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/beds24/credentials (the `SubmitBeds24Credentials` operationId).
	SubmitBeds24CredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitBeds24CredentialsClientResponse, error)

	// SubmitBeds24CredentialsWithResponse Submit Beds24 credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Beds24. API key + prop key from Beds24 → Settings → Apps & Integrations.
	//
	// The credentials are validated against Beds24 before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/beds24/credentials (the `SubmitBeds24Credentials` operationId).
	SubmitBeds24CredentialsWithResponse(ctx context.Context, body SubmitBeds24CredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitBeds24CredentialsClientResponse, error)

	// BookingConnectCallbackWithBodyWithResponse Booking.com connectivity callback
	//
	// Receives Booking.com's asynchronous confirmation that a property has designated Repull as its connectivity provider, and advances the Connect session. Called by Booking.com, not by integrators.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/booking/callback (the `BookingConnectCallback` operationId).
	BookingConnectCallbackWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BookingConnectCallbackClientResponse, error)

	// BookingConnectCallbackWithResponse Booking.com connectivity callback
	//
	// Receives Booking.com's asynchronous confirmation that a property has designated Repull as its connectivity provider, and advances the Connect session. Called by Booking.com, not by integrators.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/booking/callback (the `BookingConnectCallback` operationId).
	BookingConnectCallbackWithResponse(ctx context.Context, body BookingConnectCallbackJSONRequestBody, reqEditors ...RequestEditorFn) (*BookingConnectCallbackClientResponse, error)

	// MapConnectBookingRoomsWithBodyWithResponse Submit room→listing mappings for a Booking.com Connect session
	//
	// Submits the customer's room→listing mapping choices in one transaction. For each mapping, updates `listings_booking_rooms.listing_id` and replaces the corresponding `listing_platform_links` row. Pass `listingId: null` to explicitly unmap a room.
	//
	// On success the Connect session is marked `completed` and the hosted picker page emits a `repull:connect:completed` postMessage to the embedding window.
	//
	// No API key required — the `sessionId` in the body is the capability token. Each mapping's `roomId` must belong to the customer's claimed hotel; mismatched IDs are rejected with 403.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/booking/map-rooms (the `MapConnectBookingRooms` operationId).
	MapConnectBookingRoomsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*MapConnectBookingRoomsClientResponse, error)

	// MapConnectBookingRoomsWithResponse Submit room→listing mappings for a Booking.com Connect session
	//
	// Submits the customer's room→listing mapping choices in one transaction. For each mapping, updates `listings_booking_rooms.listing_id` and replaces the corresponding `listing_platform_links` row. Pass `listingId: null` to explicitly unmap a room.
	//
	// On success the Connect session is marked `completed` and the hosted picker page emits a `repull:connect:completed` postMessage to the embedding window.
	//
	// No API key required — the `sessionId` in the body is the capability token. Each mapping's `roomId` must belong to the customer's claimed hotel; mismatched IDs are rejected with 403.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/booking/map-rooms (the `MapConnectBookingRooms` operationId).
	MapConnectBookingRoomsWithResponse(ctx context.Context, body MapConnectBookingRoomsJSONRequestBody, reqEditors ...RequestEditorFn) (*MapConnectBookingRoomsClientResponse, error)

	// ListConnectBookingRoomsWithResponse List Booking.com rooms imported for a Connect session
	//
	// Returns the rooms imported from the Booking.com hotel claimed in this Connect session, plus the customer's listing options for the mapping dropdowns. Hosted-picker pages poll this endpoint every ~2s after `verifyBookingHotel` succeeds; once rooms appear the page transitions to the mapping UI.
	//
	// No API key required — the `sessionId` query param is the capability token.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/connect/booking/rooms (the `ListConnectBookingRooms` operationId).
	ListConnectBookingRoomsWithResponse(ctx context.Context, params *ListConnectBookingRoomsParams, reqEditors ...RequestEditorFn) (*ListConnectBookingRoomsClientResponse, error)

	// VerifyBookingHotelWithBodyWithResponse Verify a Booking.com hotel ID for a Connect session
	//
	// Manual-paste fallback that closes the Booking.com claim flow. Call this after the customer completes Stage 1 designation in their Booking Extranet (ticking FantasticStay/Repull as their connectivity provider) and pastes their Hotel ID into the hosted picker.
	//
	// Validates the hotel against Booking's property API, persists the `pms_connections` row, kicks off the room import, and transitions the Connect session to `awaiting_room_mapping`.
	//
	// No API key required — the `sessionId` is the capability token. Sessions in any terminal state are rejected.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/booking/verify (the `VerifyBookingHotel` operationId).
	VerifyBookingHotelWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*VerifyBookingHotelClientResponse, error)

	// VerifyBookingHotelWithResponse Verify a Booking.com hotel ID for a Connect session
	//
	// Manual-paste fallback that closes the Booking.com claim flow. Call this after the customer completes Stage 1 designation in their Booking Extranet (ticking FantasticStay/Repull as their connectivity provider) and pastes their Hotel ID into the hosted picker.
	//
	// Validates the hotel against Booking's property API, persists the `pms_connections` row, kicks off the room import, and transitions the Connect session to `awaiting_room_mapping`.
	//
	// No API key required — the `sessionId` is the capability token. Sessions in any terminal state are rejected.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/booking/verify (the `VerifyBookingHotel` operationId).
	VerifyBookingHotelWithResponse(ctx context.Context, body VerifyBookingHotelJSONRequestBody, reqEditors ...RequestEditorFn) (*VerifyBookingHotelClientResponse, error)

	// SubmitBookingsyncCredentialsWithBodyWithResponse Submit BookingSync credentials for a Connect session
	//
	// Completes a credentials-pattern connection for BookingSync. OAuth client credentials issued by BookingSync.
	//
	// The credentials are validated against BookingSync before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/bookingsync/credentials (the `SubmitBookingsyncCredentials` operationId).
	SubmitBookingsyncCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitBookingsyncCredentialsClientResponse, error)

	// SubmitBookingsyncCredentialsWithResponse Submit BookingSync credentials for a Connect session
	//
	// Completes a credentials-pattern connection for BookingSync. OAuth client credentials issued by BookingSync.
	//
	// The credentials are validated against BookingSync before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/bookingsync/credentials (the `SubmitBookingsyncCredentials` operationId).
	SubmitBookingsyncCredentialsWithResponse(ctx context.Context, body SubmitBookingsyncCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitBookingsyncCredentialsClientResponse, error)

	// SubmitGuestyCredentialsWithBodyWithResponse Submit Guesty credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Guesty. Client ID + secret from Guesty → Integrations → Open API.
	//
	// The credentials are validated against Guesty before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/guesty/credentials (the `SubmitGuestyCredentials` operationId).
	SubmitGuestyCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitGuestyCredentialsClientResponse, error)

	// SubmitGuestyCredentialsWithResponse Submit Guesty credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Guesty. Client ID + secret from Guesty → Integrations → Open API.
	//
	// The credentials are validated against Guesty before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/guesty/credentials (the `SubmitGuestyCredentials` operationId).
	SubmitGuestyCredentialsWithResponse(ctx context.Context, body SubmitGuestyCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitGuestyCredentialsClientResponse, error)

	// SubmitHospitableCredentialsWithBodyWithResponse Submit Hospitable credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Hospitable. Personal access token from Hospitable → Settings → API.
	//
	// The credentials are validated against Hospitable before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/hospitable/credentials (the `SubmitHospitableCredentials` operationId).
	SubmitHospitableCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitHospitableCredentialsClientResponse, error)

	// SubmitHospitableCredentialsWithResponse Submit Hospitable credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Hospitable. Personal access token from Hospitable → Settings → API.
	//
	// The credentials are validated against Hospitable before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/hospitable/credentials (the `SubmitHospitableCredentials` operationId).
	SubmitHospitableCredentialsWithResponse(ctx context.Context, body SubmitHospitableCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitHospitableCredentialsClientResponse, error)

	// SubmitHostawayCredentialsWithBodyWithResponse Submit Hostaway credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Hostaway. Account ID + API key from Hostaway → Settings → Public API.
	//
	// The credentials are validated against Hostaway before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/hostaway/credentials (the `SubmitHostawayCredentials` operationId).
	SubmitHostawayCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitHostawayCredentialsClientResponse, error)

	// SubmitHostawayCredentialsWithResponse Submit Hostaway credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Hostaway. Account ID + API key from Hostaway → Settings → Public API.
	//
	// The credentials are validated against Hostaway before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/hostaway/credentials (the `SubmitHostawayCredentials` operationId).
	SubmitHostawayCredentialsWithResponse(ctx context.Context, body SubmitHostawayCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitHostawayCredentialsClientResponse, error)

	// SubmitIgmsCredentialsWithBodyWithResponse Submit iGMS credentials for a Connect session
	//
	// Completes a credentials-pattern connection for iGMS. API token from iGMS → Settings → Integrations.
	//
	// The credentials are validated against iGMS before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/igms/credentials (the `SubmitIgmsCredentials` operationId).
	SubmitIgmsCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitIgmsCredentialsClientResponse, error)

	// SubmitIgmsCredentialsWithResponse Submit iGMS credentials for a Connect session
	//
	// Completes a credentials-pattern connection for iGMS. API token from iGMS → Settings → Integrations.
	//
	// The credentials are validated against iGMS before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/igms/credentials (the `SubmitIgmsCredentials` operationId).
	SubmitIgmsCredentialsWithResponse(ctx context.Context, body SubmitIgmsCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitIgmsCredentialsClientResponse, error)

	// SubmitLodgifyCredentialsWithBodyWithResponse Submit Lodgify credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Lodgify. API key from Lodgify → Settings → Public API.
	//
	// The credentials are validated against Lodgify before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/lodgify/credentials (the `SubmitLodgifyCredentials` operationId).
	SubmitLodgifyCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitLodgifyCredentialsClientResponse, error)

	// SubmitLodgifyCredentialsWithResponse Submit Lodgify credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Lodgify. API key from Lodgify → Settings → Public API.
	//
	// The credentials are validated against Lodgify before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/lodgify/credentials (the `SubmitLodgifyCredentials` operationId).
	SubmitLodgifyCredentialsWithResponse(ctx context.Context, body SubmitLodgifyCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitLodgifyCredentialsClientResponse, error)

	// SubmitOwnerrezCredentialsWithBodyWithResponse Submit OwnerRez credentials for a Connect session
	//
	// Completes a credentials-pattern connection for OwnerRez. Username + API token from OwnerRez → Settings → API.
	//
	// The credentials are validated against OwnerRez before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/ownerrez/credentials (the `SubmitOwnerrezCredentials` operationId).
	SubmitOwnerrezCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitOwnerrezCredentialsClientResponse, error)

	// SubmitOwnerrezCredentialsWithResponse Submit OwnerRez credentials for a Connect session
	//
	// Completes a credentials-pattern connection for OwnerRez. Username + API token from OwnerRez → Settings → API.
	//
	// The credentials are validated against OwnerRez before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/ownerrez/credentials (the `SubmitOwnerrezCredentials` operationId).
	SubmitOwnerrezCredentialsWithResponse(ctx context.Context, body SubmitOwnerrezCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitOwnerrezCredentialsClientResponse, error)

	// ListConnectProvidersWithResponse List Connect channels
	//
	// Returns the public registry of every channel the picker supports. No customer-specific data — display metadata only. Cached for 5 minutes at the edge.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/connect/providers (the `ListConnectProviders` operationId).
	ListConnectProvidersWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListConnectProvidersClientResponse, error)

	// SelectConnectProviderWithBodyWithResponse Bind a picker session to a provider
	//
	// Called by the hosted picker page once the user clicks a channel card. Validates the provider exists and is permitted by the session's `allowedProviders` whitelist (if any), then returns the next-step URL the picker should navigate to.
	//
	// No API key required — the session ID is the capability token. The session must still be pending and unexpired.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/sessions/{sessionId}/select-provider (the `SelectConnectProvider` operationId).
	SelectConnectProviderWithBodyWithResponse(ctx context.Context, sessionId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SelectConnectProviderClientResponse, error)

	// SelectConnectProviderWithResponse Bind a picker session to a provider
	//
	// Called by the hosted picker page once the user clicks a channel card. Validates the provider exists and is permitted by the session's `allowedProviders` whitelist (if any), then returns the next-step URL the picker should navigate to.
	//
	// No API key required — the session ID is the capability token. The session must still be pending and unexpired.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/sessions/{sessionId}/select-provider (the `SelectConnectProvider` operationId).
	SelectConnectProviderWithResponse(ctx context.Context, sessionId string, body SelectConnectProviderJSONRequestBody, reqEditors ...RequestEditorFn) (*SelectConnectProviderClientResponse, error)

	// SubmitSmoobuCredentialsWithBodyWithResponse Submit Smoobu credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Smoobu. API key from Smoobu → Settings → For developers.
	//
	// The credentials are validated against Smoobu before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/smoobu/credentials (the `SubmitSmoobuCredentials` operationId).
	SubmitSmoobuCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitSmoobuCredentialsClientResponse, error)

	// SubmitSmoobuCredentialsWithResponse Submit Smoobu credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Smoobu. API key from Smoobu → Settings → For developers.
	//
	// The credentials are validated against Smoobu before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/smoobu/credentials (the `SubmitSmoobuCredentials` operationId).
	SubmitSmoobuCredentialsWithResponse(ctx context.Context, body SubmitSmoobuCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitSmoobuCredentialsClientResponse, error)

	// SubmitVrboCredentialsWithBodyWithResponse Submit Vrbo credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Vrbo. Activation handshake — Repull mints the Basic-Auth pair the host pastes into Vrbo Partner Central.
	//
	// The credentials are validated against Vrbo before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/vrbo/credentials (the `SubmitVrboCredentials` operationId).
	SubmitVrboCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitVrboCredentialsClientResponse, error)

	// SubmitVrboCredentialsWithResponse Submit Vrbo credentials for a Connect session
	//
	// Completes a credentials-pattern connection for Vrbo. Activation handshake — Repull mints the Basic-Auth pair the host pastes into Vrbo Partner Central.
	//
	// The credentials are validated against Vrbo before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
	//
	// No API key required when called with a `sessionId` — the session is the capability token.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/vrbo/credentials (the `SubmitVrboCredentials` operationId).
	SubmitVrboCredentialsWithResponse(ctx context.Context, body SubmitVrboCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitVrboCredentialsClientResponse, error)

	// DeleteConnectionWithResponse Disconnect provider
	//
	// Disconnect ONE connected account of a provider from this workspace. Supported for `airbnb` and `booking`.
	//
	// **Which account.** Pass `accountId` — for Airbnb the host id (`accounts[].externalAccountId` from `GET /v1/connect/airbnb`), for Booking.com the hotel id. It is optional only when the workspace has exactly one account for the provider. With several and no `accountId`, the call returns `422` with the account ids in `valid_values` instead of guessing. An `accountId` that is not connected to this workspace returns `404`. Disconnecting one account leaves the others connected.
	//
	// **What happens.** The account's stored authorization is removed and it stops syncing. Its listings are **deactivated**, not deleted: they stop counting toward your plan's listing limit, their data is kept, and they are returned in `listingsDeactivated`. A listing that is still connected through another account or channel stays active. Reconnect the account, then activate the listings with `POST /v1/listings/status`.
	//
	// The change is all or nothing. For Airbnb, the host can also revoke access on Airbnb's side (Account → Privacy & sharing → Connected apps); that alone does not update this workspace, so call this endpoint as well.
	//
	// Other providers return `501 not_implemented` with instructions for disconnecting on the provider's side. That answer depends only on the provider, not on your workspace: an unsupported provider returns `501` whether or not you have a connection to it. `404 not_found` on a supported provider means this workspace has no connection to it (or, with `accountId`, that the account is not connected here).
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/connect/{provider} (the `DeleteConnection` operationId).
	DeleteConnectionWithResponse(ctx context.Context, provider Provider, params *DeleteConnectionParams, reqEditors ...RequestEditorFn) (*DeleteConnectionClientResponse, error)

	// GetConnectStatusWithResponse Get connection status
	//
	// Returns the current connection status for a provider, including host metadata (display name + avatar) for Airbnb so clients can render an account-level confirmation UI.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/connect/{provider} (the `GetConnectStatus` operationId).
	GetConnectStatusWithResponse(ctx context.Context, provider Provider, reqEditors ...RequestEditorFn) (*GetConnectStatusClientResponse, error)

	// CreateConnectionWithBodyWithResponse Connect to PMS/OTA provider
	//
	// Establish a connection to a PMS or OTA platform. Credentials vary by provider — see docs for each provider.
	//
	// Airbnb-specific: pass `redirectUrl` (where to send the user after consent) and optionally `accessType`. Three tiers: `read_only` grants read-only scopes; `messaging` grants read scopes plus message read/send but NOT property management, so it can coexist with another app (e.g. an existing PMS) that already holds property management on the same Airbnb account; `full_access` — the default — grants full host scopes including the exclusive property management (only one app per Airbnb account can hold it). The response returns a hosted `url` to redirect the user to.
	//
	// Booking.com: pass `redirectUrl` (no `accessType`). The response returns a hosted `url` — send the user there to designate FantasticStay in their Booking.com Extranet and paste their Hotel ID. Same response shape as Airbnb (`url`, `sessionId`, `expiresAt`).
	//
	// PMS providers (api-key based) pass `apiKey` instead; Plumguide passes `clientId`/`clientSecret`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/{provider} (the `CreateConnection` operationId).
	CreateConnectionWithBodyWithResponse(ctx context.Context, provider Provider, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateConnectionClientResponse, error)

	// CreateConnectionWithResponse Connect to PMS/OTA provider
	//
	// Establish a connection to a PMS or OTA platform. Credentials vary by provider — see docs for each provider.
	//
	// Airbnb-specific: pass `redirectUrl` (where to send the user after consent) and optionally `accessType`. Three tiers: `read_only` grants read-only scopes; `messaging` grants read scopes plus message read/send but NOT property management, so it can coexist with another app (e.g. an existing PMS) that already holds property management on the same Airbnb account; `full_access` — the default — grants full host scopes including the exclusive property management (only one app per Airbnb account can hold it). The response returns a hosted `url` to redirect the user to.
	//
	// Booking.com: pass `redirectUrl` (no `accessType`). The response returns a hosted `url` — send the user there to designate FantasticStay in their Booking.com Extranet and paste their Hotel ID. Same response shape as Airbnb (`url`, `sessionId`, `expiresAt`).
	//
	// PMS providers (api-key based) pass `apiKey` instead; Plumguide passes `clientId`/`clientSecret`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/connect/{provider} (the `CreateConnection` operationId).
	CreateConnectionWithResponse(ctx context.Context, provider Provider, body CreateConnectionJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateConnectionClientResponse, error)

	// ListConversationsWithResponse List conversations
	//
	// Cursor-paginated list of message threads owned by the workspace. Backed by main vanio's `/api/threads/list` which keyset-paginates against `(last_message_at, id)` for constant per-page cost. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// Filters: `platform` (`airbnb`|`booking`|`vrbo`|`website`|`email`), `status` (`open`|`archived` — `archived` is a stable no-op until the bit lands on `message_threads`).
	//
	// **Inactive listings:** conversations that belong to an inactive listing (by the thread's listing or its reservation's listing) are left out of the page and of `pagination.total`. Inactive listings keep syncing; activate the listing to use it here.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/conversations (the `ListConversations` operationId).
	ListConversationsWithResponse(ctx context.Context, params *ListConversationsParams, reqEditors ...RequestEditorFn) (*ListConversationsClientResponse, error)

	// GetConversationWithResponse Get conversation detail
	//
	// Returns one thread (the same shape as the list-row `Conversation`) plus expanded `host` (from `airbnb_hosts` for the thread's `host_id`) and `guest` (resolved via the thread's `reservation_id`, with up to 50 contacts) blocks.
	//
	// A conversation that belongs to an inactive listing (by the thread's listing or its reservation's listing) returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/conversations/{id} (the `GetConversation` operationId).
	GetConversationWithResponse(ctx context.Context, id int, params *GetConversationParams, reqEditors ...RequestEditorFn) (*GetConversationClientResponse, error)

	// ListConversationMessagesWithResponse List messages in a conversation
	//
	// Cursor-paginated messages within one thread. Defaults to newest-first (`?order=desc`); pass `?order=asc` for chronological replay. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// A conversation that belongs to an inactive listing returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/conversations/{id}/messages (the `ListConversationMessages` operationId).
	ListConversationMessagesWithResponse(ctx context.Context, id int, params *ListConversationMessagesParams, reqEditors ...RequestEditorFn) (*ListConversationMessagesClientResponse, error)

	// SendConversationMessageWithBodyWithResponse Send a message to the guest
	//
	// Sends a message to the guest on this conversation and records it in the thread.
	//
	// Omit `channel` and the message goes out on whichever channel the conversation already uses (Airbnb, Booking.com, SMS, email or the direct-booking site) — that is the right default. Pass `channel` only to force a specific one.
	//
	// The message is attributed to the API: it is recorded with `aiGenerated` false so an API send is never counted as an automated reply.
	//
	// ### Airbnb rewrites links — check `contentRewritten`
	//
	// Airbnb rejects guest messages containing a link, an email address or a phone number, and names the offending text. When that happens the offending fragment is stripped and the remainder is re-sent once, which means **the guest receives a message that is not the one you wrote**. Reporting that as a plain success would be a lie, so every response carries `contentRewritten`; when it is `true`, `deliveredContent` is the text that actually reached the guest. Check it before assuming your message went out verbatim.
	//
	// When the text cannot be salvaged (the link is most of the message) nothing is delivered and the call returns `422 message_not_sent` with the channel's verbatim refusal in `statusReason`.
	//
	// Send `Idempotency-Key` — without it, retrying after a network timeout sends the guest the same message twice.
	//
	// ### Attachments
	//
	// Send files with `attachments: [{ url, contentType?, filename? }]` — public `https://` URLs, up to 5 per request, 10 MB each. `message` may be omitted when there are attachments (except on Booking.com). Repull downloads each file, reads its real type from the bytes, keeps a durable copy and delivers it through the channel's own file flow. **Every file is checked before anything is sent**: if one is unreachable, too large or of a type the channel refuses, the call returns 422 naming the file (`index`) and the guest receives nothing.
	//
	// | Channel | Accepted types | Text | How it arrives |
	// |---|---|---|---|
	// | Airbnb | JPEG, PNG, GIF, WebP (converted to JPEG), MP4, QuickTime | optional | each file as its own message, then the text as a separate message |
	// | Booking.com | JPEG, PNG | **required** | one message carrying the text and every file |
	// | SMS, email, direct-booking site chat | — | — | `422 attachments_not_supported`, nothing sent |
	//
	// Airbnb does not allow files in pre-booking (inquiry) conversations; that refusal comes back as `422 message_not_sent`. Because Airbnb delivers files one message at a time, a later file can be refused after earlier ones arrived — that returns `422 message_partially_sent` with `parts` saying exactly which messages reached the guest; resend only the rest.
	//
	// The response's `attachments` lists each file's durable `url`, and `parts` lists every channel message the send produced. Read-back (`GET /v1/conversations/{id}/messages`) shows the same files in each message's `attachments`.
	//
	// **Inactive listings:** a conversation that belongs to an inactive listing returns `403 listing_inactive` and no message is sent. Activate the listing first.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/conversations/{id}/messages (the `SendConversationMessage` operationId).
	SendConversationMessageWithBodyWithResponse(ctx context.Context, id int, params *SendConversationMessageParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SendConversationMessageClientResponse, error)

	// SendConversationMessageWithResponse Send a message to the guest
	//
	// Sends a message to the guest on this conversation and records it in the thread.
	//
	// Omit `channel` and the message goes out on whichever channel the conversation already uses (Airbnb, Booking.com, SMS, email or the direct-booking site) — that is the right default. Pass `channel` only to force a specific one.
	//
	// The message is attributed to the API: it is recorded with `aiGenerated` false so an API send is never counted as an automated reply.
	//
	// ### Airbnb rewrites links — check `contentRewritten`
	//
	// Airbnb rejects guest messages containing a link, an email address or a phone number, and names the offending text. When that happens the offending fragment is stripped and the remainder is re-sent once, which means **the guest receives a message that is not the one you wrote**. Reporting that as a plain success would be a lie, so every response carries `contentRewritten`; when it is `true`, `deliveredContent` is the text that actually reached the guest. Check it before assuming your message went out verbatim.
	//
	// When the text cannot be salvaged (the link is most of the message) nothing is delivered and the call returns `422 message_not_sent` with the channel's verbatim refusal in `statusReason`.
	//
	// Send `Idempotency-Key` — without it, retrying after a network timeout sends the guest the same message twice.
	//
	// ### Attachments
	//
	// Send files with `attachments: [{ url, contentType?, filename? }]` — public `https://` URLs, up to 5 per request, 10 MB each. `message` may be omitted when there are attachments (except on Booking.com). Repull downloads each file, reads its real type from the bytes, keeps a durable copy and delivers it through the channel's own file flow. **Every file is checked before anything is sent**: if one is unreachable, too large or of a type the channel refuses, the call returns 422 naming the file (`index`) and the guest receives nothing.
	//
	// | Channel | Accepted types | Text | How it arrives |
	// |---|---|---|---|
	// | Airbnb | JPEG, PNG, GIF, WebP (converted to JPEG), MP4, QuickTime | optional | each file as its own message, then the text as a separate message |
	// | Booking.com | JPEG, PNG | **required** | one message carrying the text and every file |
	// | SMS, email, direct-booking site chat | — | — | `422 attachments_not_supported`, nothing sent |
	//
	// Airbnb does not allow files in pre-booking (inquiry) conversations; that refusal comes back as `422 message_not_sent`. Because Airbnb delivers files one message at a time, a later file can be refused after earlier ones arrived — that returns `422 message_partially_sent` with `parts` saying exactly which messages reached the guest; resend only the rest.
	//
	// The response's `attachments` lists each file's durable `url`, and `parts` lists every channel message the send produced. Read-back (`GET /v1/conversations/{id}/messages`) shows the same files in each message's `attachments`.
	//
	// **Inactive listings:** a conversation that belongs to an inactive listing returns `403 listing_inactive` and no message is sent. Activate the listing first.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/conversations/{id}/messages (the `SendConversationMessage` operationId).
	SendConversationMessageWithResponse(ctx context.Context, id int, params *SendConversationMessageParams, body SendConversationMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*SendConversationMessageClientResponse, error)

	// PreapproveConversationWithBodyWithResponse Pre-approve an inquiry
	//
	// Pre-approve the Airbnb inquiry on this conversation: the guest who asked about dates may now book them at the listed price, without waiting on you. To change the dates, guests or price, send a special offer instead (`POST /v1/conversations/{id}/special-offers`).
	//
	// Find inquiries that need an answer with `GET /v1/inquiries` (default `status=open`); each carries the `conversationId` to use here.
	//
	// **Airbnb only**, and only for listings connected to Airbnb directly. A Booking.com, VRBO or direct-booking conversation, or an Airbnb one relayed through a PMS (Hostaway, Guesty), returns `422 channel_not_supported` and nothing is sent.
	//
	// The inquiry is marked `pre_approved` everywhere, the same as pre-approving in Airbnb.
	//
	// An Airbnb refusal is never reported as a success: an inquiry that already moved on is `409 inquiry_no_longer_open`, an expired one `409 inquiry_expired`, a conversation that already has a booking `409 conversation_already_booked`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/conversations/{id}/pre-approval (the `PreapproveConversation` operationId).
	PreapproveConversationWithBodyWithResponse(ctx context.Context, id int, params *PreapproveConversationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PreapproveConversationClientResponse, error)

	// PreapproveConversationWithResponse Pre-approve an inquiry
	//
	// Pre-approve the Airbnb inquiry on this conversation: the guest who asked about dates may now book them at the listed price, without waiting on you. To change the dates, guests or price, send a special offer instead (`POST /v1/conversations/{id}/special-offers`).
	//
	// Find inquiries that need an answer with `GET /v1/inquiries` (default `status=open`); each carries the `conversationId` to use here.
	//
	// **Airbnb only**, and only for listings connected to Airbnb directly. A Booking.com, VRBO or direct-booking conversation, or an Airbnb one relayed through a PMS (Hostaway, Guesty), returns `422 channel_not_supported` and nothing is sent.
	//
	// The inquiry is marked `pre_approved` everywhere, the same as pre-approving in Airbnb.
	//
	// An Airbnb refusal is never reported as a success: an inquiry that already moved on is `409 inquiry_no_longer_open`, an expired one `409 inquiry_expired`, a conversation that already has a booking `409 conversation_already_booked`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/conversations/{id}/pre-approval (the `PreapproveConversation` operationId).
	PreapproveConversationWithResponse(ctx context.Context, id int, params *PreapproveConversationParams, body PreapproveConversationJSONRequestBody, reqEditors ...RequestEditorFn) (*PreapproveConversationClientResponse, error)

	// CreateConversationSpecialOfferWithBodyWithResponse Send a special offer
	//
	// Send the guest on this conversation an Airbnb special offer: your own dates, guest count and total price. The guest has 24 hours to book it. Use it to answer an inquiry with different terms, or to make a returning guest a custom price. To accept the guest’s own dates and price as they asked, pre-approve instead (`POST /v1/conversations/{id}/pre-approval`).
	//
	// `listingId` is optional: omit it to offer the listing the guest asked about. It is a **Repull** listing id; Repull sends Airbnb its own listing id, using the link that belongs to this conversation’s Airbnb account.
	//
	// `totalPrice` is the whole stay, in the listing’s Airbnb currency — Airbnb does not take a currency on an offer.
	//
	// **Airbnb only**, and only for listings connected to Airbnb directly; anything else is `422 channel_not_supported` and nothing is sent. The inquiry is marked `special_offer_sent`.
	//
	// An offer Airbnb refuses is never a `201`: dates that are taken, a price below Airbnb’s minimum, too many guests and the like are `422 airbnb_rejected` with Airbnb’s own reason in `message`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again. Without it, a retry after a timeout can send the guest two offers.
	//
	// Read or withdraw the offer with `GET` / `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/conversations/{id}/special-offers (the `CreateConversationSpecialOffer` operationId).
	CreateConversationSpecialOfferWithBodyWithResponse(ctx context.Context, id int, params *CreateConversationSpecialOfferParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateConversationSpecialOfferClientResponse, error)

	// CreateConversationSpecialOfferWithResponse Send a special offer
	//
	// Send the guest on this conversation an Airbnb special offer: your own dates, guest count and total price. The guest has 24 hours to book it. Use it to answer an inquiry with different terms, or to make a returning guest a custom price. To accept the guest’s own dates and price as they asked, pre-approve instead (`POST /v1/conversations/{id}/pre-approval`).
	//
	// `listingId` is optional: omit it to offer the listing the guest asked about. It is a **Repull** listing id; Repull sends Airbnb its own listing id, using the link that belongs to this conversation’s Airbnb account.
	//
	// `totalPrice` is the whole stay, in the listing’s Airbnb currency — Airbnb does not take a currency on an offer.
	//
	// **Airbnb only**, and only for listings connected to Airbnb directly; anything else is `422 channel_not_supported` and nothing is sent. The inquiry is marked `special_offer_sent`.
	//
	// An offer Airbnb refuses is never a `201`: dates that are taken, a price below Airbnb’s minimum, too many guests and the like are `422 airbnb_rejected` with Airbnb’s own reason in `message`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again. Without it, a retry after a timeout can send the guest two offers.
	//
	// Read or withdraw the offer with `GET` / `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/conversations/{id}/special-offers (the `CreateConversationSpecialOffer` operationId).
	CreateConversationSpecialOfferWithResponse(ctx context.Context, id int, params *CreateConversationSpecialOfferParams, body CreateConversationSpecialOfferJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateConversationSpecialOfferClientResponse, error)

	// WithdrawConversationSpecialOfferWithResponse Withdraw a special offer
	//
	// Withdraw a special offer the guest has not booked yet, so it can no longer be booked. An offer the guest already booked cannot be withdrawn — Airbnb refuses with `409 inquiry_no_longer_open`; cancel the booking instead.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/conversations/{id}/special-offers/{offerId} (the `WithdrawConversationSpecialOffer` operationId).
	WithdrawConversationSpecialOfferWithResponse(ctx context.Context, id int, offerId string, reqEditors ...RequestEditorFn) (*WithdrawConversationSpecialOfferClientResponse, error)

	// GetConversationSpecialOfferWithResponse Get a special offer
	//
	// Read a special offer on this conversation back from Airbnb — typically to check its `status` (`active` until the guest books it, it expires, or you withdraw it). Read live from Airbnb with the conversation’s own Airbnb account.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/conversations/{id}/special-offers/{offerId} (the `GetConversationSpecialOffer` operationId).
	GetConversationSpecialOfferWithResponse(ctx context.Context, id int, offerId string, reqEditors ...RequestEditorFn) (*GetConversationSpecialOfferClientResponse, error)

	// ListGuestsWithResponse List guests
	//
	// Cursor-paginated list of guests in the workspace. Walks `guests.id ASC` keyset for constant per-page cost regardless of how many guests the customer has. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// Filters: `q` (substring on name/email/phone), `has_reservation` (`true`|`false`), `listing_id` (restrict to guests with at least one reservation on that listing).
	//
	// **Inactive listings:** a guest whose every reservation is on an inactive listing is left out of the page and of `pagination.total`, and `totalReservations`, `totalRevenue`, `firstStayedAt` / `lastStayedAt`, `has_reservation` and `listing_id` only consider reservations that are not on an inactive listing. Guests with no reservations are tied to no listing and are always listed. Filtering by an inactive `listing_id` returns `403 listing_inactive`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/guests (the `ListGuests` operationId).
	ListGuestsWithResponse(ctx context.Context, params *ListGuestsParams, reqEditors ...RequestEditorFn) (*ListGuestsClientResponse, error)

	// CreateGuestWithBodyWithResponse Create a guest
	//
	// Creates a guest in the workspace, with contact normalisation applied (phone digits, email lowercased) and each contact stored as its own record.
	//
	// **This is find-or-create, and the response tells you which happened.** A guest already on file matching on email — then phone — AND name is returned instead of a duplicate being created. Read the `created` flag rather than inferring from the status: `201` with `created: true` means a new record was written, `200` with `created: false` means an existing guest matched. Quietly handing back an existing record as though it were new is exactly the ambiguity this flag removes.
	//
	// Field names are camelCase, and an unrecognised field is rejected by name rather than silently dropped.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/guests (the `CreateGuest` operationId).
	CreateGuestWithBodyWithResponse(ctx context.Context, params *CreateGuestParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateGuestClientResponse, error)

	// CreateGuestWithResponse Create a guest
	//
	// Creates a guest in the workspace, with contact normalisation applied (phone digits, email lowercased) and each contact stored as its own record.
	//
	// **This is find-or-create, and the response tells you which happened.** A guest already on file matching on email — then phone — AND name is returned instead of a duplicate being created. Read the `created` flag rather than inferring from the status: `201` with `created: true` means a new record was written, `200` with `created: false` means an existing guest matched. Quietly handing back an existing record as though it were new is exactly the ambiguity this flag removes.
	//
	// Field names are camelCase, and an unrecognised field is rejected by name rather than silently dropped.
	//
	// Send `Idempotency-Key` to make a retry safe.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/guests (the `CreateGuest` operationId).
	CreateGuestWithResponse(ctx context.Context, params *CreateGuestParams, body CreateGuestJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateGuestClientResponse, error)

	// GetGuestWithResponse Get guest profile
	//
	// Returns the full guest profile — base list-row fields plus contacts, flags, notes, risk metadata, and reservation aggregates. Aggregates main vanio's `GuestService.getGuestProfile()` into the public Repull shape so SDK consumers don't have to learn the internal schema.
	//
	// **Inactive listings:** a guest whose every reservation is on an inactive listing returns `403 listing_inactive` naming those listings (the guest is kept, so this is not a 404). Otherwise the reservation aggregates exclude reservations on inactive listings. A guest with no reservations is always readable.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/guests/{id} (the `GetGuest` operationId).
	GetGuestWithResponse(ctx context.Context, id int, params *GetGuestParams, reqEditors ...RequestEditorFn) (*GetGuestClientResponse, error)

	// GetHealthWithResponse Health check
	//
	// Liveness probe. Returns `{ status: "ok", version }` when the API process is running. No auth required. Suitable for uptime monitors, load-balancer health checks, and SDK self-tests.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/health (the `GetHealth` operationId).
	GetHealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetHealthClientResponse, error)

	// GetAuthHealthWithResponse API-key authentication path health
	//
	// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/health/auth (the `GetAuthHealth` operationId).
	GetAuthHealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetAuthHealthClientResponse, error)

	// GetChannelHealthWithResponse Per-channel connectivity health
	//
	// Reports reachability and auth state for one channel (`airbnb`, `booking`, `vrbo`, `plumguide`). Use it to tell "the channel is down" apart from "this workspace's connection expired".
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/health/channels/{channel} (the `GetChannelHealth` operationId).
	GetChannelHealthWithResponse(ctx context.Context, channel GetChannelHealthParamsChannel, reqEditors ...RequestEditorFn) (*GetChannelHealthClientResponse, error)

	// GetMcpHealthWithResponse MCP server reachability
	//
	// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/health/mcp (the `GetMcpHealth` operationId).
	GetMcpHealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetMcpHealthClientResponse, error)

	// GetWebhooksHealthWithResponse Webhook delivery pipeline health
	//
	// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/health/webhooks (the `GetWebhooksHealth` operationId).
	GetWebhooksHealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetWebhooksHealthClientResponse, error)

	// ListInquiriesWithResponse List inquiries
	//
	// Airbnb inquiries — guests asking about dates before booking — newest first. By default only `open` ones: nobody has answered and the stay is still ahead. Answer one with `POST /v1/conversations/{conversationId}/pre-approval` (accept their dates and price) or `POST /v1/conversations/{conversationId}/special-offers` (your own terms).
	//
	// Booking **requests** are not inquiries: they are reservations with status `pending` — list them with `GET /v1/reservations?status=pending` and answer with `POST /v1/reservations/{id}/accept` or `/decline`.
	//
	// **Pagination:** pass `pagination.nextCursor` back as `?cursor=` until `pagination.hasMore` is `false`. `?offset=` also works (0..10000). `limit` defaults to 50, max 100.
	//
	// Inquiries on inactive listings are left out; `?listing_id=` naming an inactive listing returns `403 listing_inactive`. `X-Account-Id` narrows to one connected account.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/inquiries (the `ListInquiries` operationId).
	ListInquiriesWithResponse(ctx context.Context, params *ListInquiriesParams, reqEditors ...RequestEditorFn) (*ListInquiriesClientResponse, error)

	// ClearKvWithResponse Clear KV entries by prefix
	//
	// Bulk-deletes every key in the project whose name starts with `prefix`. The `prefix` parameter is required — there is no "delete every key in this project" shortcut; pass an empty `prefix` is rejected with 422 to prevent accidental wipes. Returns the number of rows removed.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/kv (the `ClearKv` operationId).
	ClearKvWithResponse(ctx context.Context, params *ClearKvParams, reqEditors ...RequestEditorFn) (*ClearKvClientResponse, error)

	// ListKvWithResponse List KV entries
	//
	// Returns every non-expired key-value row in the given project, sorted ascending by key. Use `prefix` to scope to a key namespace (e.g. `prefix=user:42:` to fetch all entries for one user). Hard cap of 1,000 rows per response — for projects approaching that, paginate by walking prefix buckets.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/kv (the `ListKv` operationId).
	ListKvWithResponse(ctx context.Context, params *ListKvParams, reqEditors ...RequestEditorFn) (*ListKvClientResponse, error)

	// DeleteKvWithResponse Delete a KV entry
	//
	// Removes a single key. Returns `{ deleted: true }` if the row was present, `{ deleted: false }` if it was already absent — both are 200 (idempotent).
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/kv/{key} (the `DeleteKv` operationId).
	DeleteKvWithResponse(ctx context.Context, key string, params *DeleteKvParams, reqEditors ...RequestEditorFn) (*DeleteKvClientResponse, error)

	// GetKvWithResponse Get a KV entry
	//
	// Fetches a single key. Returns 404 when the key does not exist OR has expired (rows past `ttl_at` are filtered from reads). Cross-tenant lookups also return 404 — the API never reveals existence of another customer's keys.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/kv/{key} (the `GetKv` operationId).
	GetKvWithResponse(ctx context.Context, key string, params *GetKvParams, reqEditors ...RequestEditorFn) (*GetKvClientResponse, error)

	// SetKvWithBodyWithResponse Set a KV entry
	//
	// Upserts a key. The full row is replaced — there is no partial update. Pass `ttl_seconds` (positive integer) to auto-expire the row; omit for no expiry. **Caps:** 64 KiB per row (key bytes + value JSON bytes), 1 MiB per customer (sum across ALL projects/keys). Over either cap returns 413.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/kv/{key} (the `SetKv` operationId).
	SetKvWithBodyWithResponse(ctx context.Context, key string, params *SetKvParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetKvClientResponse, error)

	// SetKvWithResponse Set a KV entry
	//
	// Upserts a key. The full row is replaced — there is no partial update. Pass `ttl_seconds` (positive integer) to auto-expire the row; omit for no expiry. **Caps:** 64 KiB per row (key bytes + value JSON bytes), 1 MiB per customer (sum across ALL projects/keys). Over either cap returns 413.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/kv/{key} (the `SetKv` operationId).
	SetKvWithResponse(ctx context.Context, key string, params *SetKvParams, body SetKvJSONRequestBody, reqEditors ...RequestEditorFn) (*SetKvClientResponse, error)

	// ListListingsWithResponse List listings
	//
	// Cursor-paginated list of listings owned by the authenticated workspace. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request to walk the full set. `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. Filters: `q` (substring on name/street/city), `status`, `channel`.
	//
	// **Optional expansions:** Pass `?include=content` to enrich each row with the rich content slab (summary, description, space, house rules, etc. — sourced from `listings_descriptions` for the `en` locale). Pass `?include=details` for the structural slab (bedrooms, bathrooms, person capacity, check-in window, wifi, house manual, etc.). Both default to `null` per row when the underlying `listings_descriptions` / `listings_details` row is missing — distinct from the field being absent (which signals the expansion was not requested). Pass `?include=thumbnail` to guarantee `thumbnailUrl` on every returned row — including the reduced inactive ones. Combine comma-separated, e.g. `?include=content,thumbnail`. The default response stays lean; consumers must opt in.
	//
	// **Inactive listings:** by default only active listings are returned. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated, so when `status` asks for inactive ones they carry only `id`, `name`, `status` and `channels` — enough to choose what to activate with `PATCH /v1/listings/{id}`. The `content` and `details` expansions are not applied to them. `?include=thumbnail` is the one exception: it adds `thumbnailUrl` to an inactive row so a single request can render an active/inactive selection screen with pictures, instead of one follow-up call per listing (which an inactive listing would answer with `403 listing_inactive` anyway).
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/listings (the `ListListings` operationId).
	ListListingsWithResponse(ctx context.Context, params *ListListingsParams, reqEditors ...RequestEditorFn) (*ListListingsClientResponse, error)

	// CreateListingWithBodyWithResponse Create a Repull listing
	//
	// Create a new vacation-rental listing under the authenticated workspace. The listing is stored in the canonical Vanio listings tables and can be published to multiple channels (Airbnb, Booking.com) via the publish endpoints.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings (the `CreateListing` operationId).
	CreateListingWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateListingClientResponse, error)

	// CreateListingWithResponse Create a Repull listing
	//
	// Create a new vacation-rental listing under the authenticated workspace. The listing is stored in the canonical Vanio listings tables and can be published to multiple channels (Airbnb, Booking.com) via the publish endpoints.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings (the `CreateListing` operationId).
	CreateListingWithResponse(ctx context.Context, body CreateListingJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateListingClientResponse, error)

	// BulkApplyPricingWithBodyWithResponse Bulk apply or decline pricing recommendations
	//
	// Apply or decline pending Atlas pricing recommendations across many listings in one call. Built for power users with hundreds of listings who would otherwise need 500 sequential single-listing POSTs.
	//
	// - `items` is capped at 500 entries per request — exceeding returns 422.
	// - Per-item failures (stale listing IDs, no pending recs, channel auth blips) DO NOT fail the whole batch — partial success is the norm at this scale and the granular `failed[]` array lets the SDK retry just the bad entries.
	// - Tier-limit accounting: this endpoint counts as **1 API call** regardless of how many items the body contains.
	//
	// Apply path writes the recommended price to each listing's calendar via the calendar service (which fans out to Airbnb/Booking/VRBO) then marks the Atlas recommendation `applied`. Decline path is Atlas-only — fast.
	//
	// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/pricing/bulk (the `BulkApplyPricing` operationId).
	BulkApplyPricingWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BulkApplyPricingClientResponse, error)

	// BulkApplyPricingWithResponse Bulk apply or decline pricing recommendations
	//
	// Apply or decline pending Atlas pricing recommendations across many listings in one call. Built for power users with hundreds of listings who would otherwise need 500 sequential single-listing POSTs.
	//
	// - `items` is capped at 500 entries per request — exceeding returns 422.
	// - Per-item failures (stale listing IDs, no pending recs, channel auth blips) DO NOT fail the whole batch — partial success is the norm at this scale and the granular `failed[]` array lets the SDK retry just the bad entries.
	// - Tier-limit accounting: this endpoint counts as **1 API call** regardless of how many items the body contains.
	//
	// Apply path writes the recommended price to each listing's calendar via the calendar service (which fans out to Airbnb/Booking/VRBO) then marks the Atlas recommendation `applied`. Decline path is Atlas-only — fast.
	//
	// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/pricing/bulk (the `BulkApplyPricing` operationId).
	BulkApplyPricingWithResponse(ctx context.Context, body BulkApplyPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*BulkApplyPricingClientResponse, error)

	// SetListingsStatusWithBodyWithResponse Activate or deactivate listings in bulk
	//
	// Sets up to 500 listings active or inactive in one call. Send `{ "listingIds": ["4118", "4119"], "active": false }` to deactivate them, or `"active": true` to activate them.
	//
	// An inactive listing is not counted toward your plan's listing limit or billed. It is NOT deleted and the upstream channel (Airbnb / Booking.com / your PMS) is never touched — its data keeps syncing, so it is complete the moment you activate it again. Until then it cannot be read, changed, or receive webhooks.
	//
	// **All or nothing.** Nothing changes unless the whole request can be applied:
	// - If any id is not one of your listings, the call returns `404` naming those ids.
	// - If activating would take you over your plan's listing limit, the call returns `402 listings_limit_exceeded`. Only listings that are currently inactive count toward the new total, so re-sending ids that are already active never trips the limit.
	//
	// Deactivating is always allowed, including when your account is already over its limit — it is how you get back under it.
	//
	// **Idempotent.** Ids already in the requested state are returned in `unchanged`; ids this call changed are returned in `updated`.
	//
	// For a single listing, `PATCH /v1/listings/{id}` does the same.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/status (the `SetListingsStatus` operationId).
	SetListingsStatusWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetListingsStatusClientResponse, error)

	// SetListingsStatusWithResponse Activate or deactivate listings in bulk
	//
	// Sets up to 500 listings active or inactive in one call. Send `{ "listingIds": ["4118", "4119"], "active": false }` to deactivate them, or `"active": true` to activate them.
	//
	// An inactive listing is not counted toward your plan's listing limit or billed. It is NOT deleted and the upstream channel (Airbnb / Booking.com / your PMS) is never touched — its data keeps syncing, so it is complete the moment you activate it again. Until then it cannot be read, changed, or receive webhooks.
	//
	// **All or nothing.** Nothing changes unless the whole request can be applied:
	// - If any id is not one of your listings, the call returns `404` naming those ids.
	// - If activating would take you over your plan's listing limit, the call returns `402 listings_limit_exceeded`. Only listings that are currently inactive count toward the new total, so re-sending ids that are already active never trips the limit.
	//
	// Deactivating is always allowed, including when your account is already over its limit — it is how you get back under it.
	//
	// **Idempotent.** Ids already in the requested state are returned in `unchanged`; ids this call changed are returned in `updated`.
	//
	// For a single listing, `PATCH /v1/listings/{id}` does the same.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/status (the `SetListingsStatus` operationId).
	SetListingsStatusWithResponse(ctx context.Context, body SetListingsStatusJSONRequestBody, reqEditors ...RequestEditorFn) (*SetListingsStatusClientResponse, error)

	// DeactivateListingWithResponse Deactivate (exclude) a listing
	//
	// Deactivate a listing — sets it inactive and excludes it from Repull. This is a **soft** operation: the listing row is KEPT (never hard-deleted) and the upstream channel (Airbnb / Hospitable / Booking.com) is NEVER touched. Repull only mutates its own copy.
	//
	// Equivalent to `PATCH /v1/listings/{id}` with `{ "active": false }`. This is the primary self-serve way for a free-tier customer to trim back under the plan-listings cap — `DELETE` is served even when the account is over the cap (a 402-locked account can still call it). To bring a listing back, use `PATCH` with `{ "active": true }`.
	//
	// Idempotent: deactivating an already-inactive listing returns 200.
	//
	// To deactivate many listings at once, use `POST /v1/listings/status` with `{ "active": false }`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/listings/{id} (the `DeactivateListing` operationId).
	DeactivateListingWithResponse(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*DeactivateListingClientResponse, error)

	// GetListingWithResponse Get a listing
	//
	// Fetch a single listing by id. Returns the same shape as one element of the `GET /v1/listings` response, so you can bind the result to the same model. Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403, never reveals the listing's existence.
	//
	// **Optional expansions:** Pass `?include=amenities` to enrich the response with the listing's amenity rows (`[]` when the listing has none). Pass `?include=content` for the rich content slab (summary, description, space, house rules, etc. — sourced from `listings_descriptions` for the `en` locale; `null` when no row is stored). Pass `?include=details` for the structural slab (bedrooms, bathrooms, person capacity, check-in window, wifi, house manual, etc.; `null` when no row is stored). Combine comma-separated, e.g. `?include=amenities,content,details`. The default response stays lean; consumers must opt in.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/listings/{id} (the `GetListing` operationId).
	GetListingWithResponse(ctx context.Context, id int, params *GetListingParams, reqEditors ...RequestEditorFn) (*GetListingClientResponse, error)

	// UpdateListingActiveWithBodyWithResponse Deactivate or reactivate a listing
	//
	// Toggle a listing's active state. Send `{ "active": false }` to **deactivate** (exclude the listing from Repull) or `{ "active": true }` to **reactivate** it.
	//
	// "Deactivate" keeps the listing row — it is NOT a hard delete, and it NEVER touches the upstream channel (Airbnb / Hospitable / Booking.com). Repull only mutates its own copy of the inventory. Deactivating is the self-serve way to get back under the plan-listings cap without paying.
	//
	// Reactivation respects the plan-listings cap: if activating this listing would push you over the cap for your tier, the call returns `402 listings_limit_exceeded` and the listing stays inactive. Deactivate another listing or upgrade first.
	//
	// Idempotent: setting a listing to the state it's already in returns 200.
	//
	// To change many listings at once, all or nothing, use `POST /v1/listings/status`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/listings/{id} (the `UpdateListingActive` operationId).
	UpdateListingActiveWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateListingActiveClientResponse, error)

	// UpdateListingActiveWithResponse Deactivate or reactivate a listing
	//
	// Toggle a listing's active state. Send `{ "active": false }` to **deactivate** (exclude the listing from Repull) or `{ "active": true }` to **reactivate** it.
	//
	// "Deactivate" keeps the listing row — it is NOT a hard delete, and it NEVER touches the upstream channel (Airbnb / Hospitable / Booking.com). Repull only mutates its own copy of the inventory. Deactivating is the self-serve way to get back under the plan-listings cap without paying.
	//
	// Reactivation respects the plan-listings cap: if activating this listing would push you over the cap for your tier, the call returns `402 listings_limit_exceeded` and the listing stays inactive. Deactivate another listing or upgrade first.
	//
	// Idempotent: setting a listing to the state it's already in returns 200.
	//
	// To change many listings at once, all or nothing, use `POST /v1/listings/status`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/listings/{id} (the `UpdateListingActive` operationId).
	UpdateListingActiveWithResponse(ctx context.Context, id int, body UpdateListingActiveJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateListingActiveClientResponse, error)

	// ListListingCompsWithResponse Comp set for a listing (with daily nightly pricing)
	//
	// Returns the actual comp set for a listing — the underlying competitor listings (with daily nightly pricing), not just the aggregated `compSummary` from `/pricing`. Each comp comes back with distance, bedrooms, ratings, lat/lng, platform link, and a per-day rate/availability series for the requested window.
	//
	// Powered by Atlas. Comps with no coordinates are excluded — there's no way to rank them by distance. Listings without coordinates return `data: []` and a `warning` field.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/listings/{id}/comps (the `ListListingComps` operationId).
	ListListingCompsWithResponse(ctx context.Context, id int, params *ListListingCompsParams, reqEditors ...RequestEditorFn) (*ListListingCompsClientResponse, error)

	// UpdateListingContentWithBodyWithResponse Update canonical listing content
	//
	// Write your PMS's canonical listing content — title, description, amenities, address, occupancy, and policies — into a Repull listing, making it the source of truth. This is the flagship "the PMS owns listing content, Repull distributes it" enabler.
	//
	// **Partial update:** every field is optional. Only the fields you send are written; absent fields are left untouched. `amenities` is a FULL replacement of the amenity set (omit to leave untouched, send `[]` to clear).
	//
	// **Multilingual:** send `locale` to say which language this copy is in (`it`, `pt-BR`, …). Canonical content is stored per locale, so each language keeps its own row instead of overwriting the English one. Omit it for English.
	//
	// **Local write only — NOT a channel publish.** This mutates Repull's own copy of the content. It does NOT push to Airbnb / Booking.com; it marks the channels dirty so a later publish knows what changed. Distribution stays a separate explicit step.
	//
	// **Photos are deferred:** a provided `photos` array is echoed back in the `deferred` field and NOT persisted (media ingestion is a follow-up).
	//
	// Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403. This endpoint is served even when the account is over the plan-listings cap, since editing content on a listing you already own never grows the portfolio.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/listings/{id}/content (the `UpdateListingContent` operationId).
	UpdateListingContentWithBodyWithResponse(ctx context.Context, id int, params *UpdateListingContentParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateListingContentClientResponse, error)

	// UpdateListingContentWithResponse Update canonical listing content
	//
	// Write your PMS's canonical listing content — title, description, amenities, address, occupancy, and policies — into a Repull listing, making it the source of truth. This is the flagship "the PMS owns listing content, Repull distributes it" enabler.
	//
	// **Partial update:** every field is optional. Only the fields you send are written; absent fields are left untouched. `amenities` is a FULL replacement of the amenity set (omit to leave untouched, send `[]` to clear).
	//
	// **Multilingual:** send `locale` to say which language this copy is in (`it`, `pt-BR`, …). Canonical content is stored per locale, so each language keeps its own row instead of overwriting the English one. Omit it for English.
	//
	// **Local write only — NOT a channel publish.** This mutates Repull's own copy of the content. It does NOT push to Airbnb / Booking.com; it marks the channels dirty so a later publish knows what changed. Distribution stays a separate explicit step.
	//
	// **Photos are deferred:** a provided `photos` array is echoed back in the `deferred` field and NOT persisted (media ingestion is a follow-up).
	//
	// Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403. This endpoint is served even when the account is over the plan-listings cap, since editing content on a listing you already own never grows the portfolio.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/listings/{id}/content (the `UpdateListingContent` operationId).
	UpdateListingContentWithResponse(ctx context.Context, id int, params *UpdateListingContentParams, body UpdateListingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateListingContentClientResponse, error)

	// GenerateListingContentWithBodyWithResponse AI-generate listing content
	//
	// Generate guest-facing copy (title, summary, description, amenities, etc.) for a listing using Repull AI. When `photos` are provided the vision model is used for photo-grounded copy. Persists into the listing by default.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/generate-content (the `GenerateListingContent` operationId).
	GenerateListingContentWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*GenerateListingContentClientResponse, error)

	// GenerateListingContentWithResponse AI-generate listing content
	//
	// Generate guest-facing copy (title, summary, description, amenities, etc.) for a listing using Repull AI. When `photos` are provided the vision model is used for photo-grounded copy. Persists into the listing by default.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/generate-content (the `GenerateListingContent` operationId).
	GenerateListingContentWithResponse(ctx context.Context, id int, body GenerateListingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*GenerateListingContentClientResponse, error)

	// TakeListingOfflineWithBodyWithResponse Take a listing off the market
	//
	// Stop this listing being sold, on every channel it is connected to, in one call.
	//
	// What that means differs per channel and you do not have to know which is which. On **Airbnb** the live listing is deactivated with a valid deactivation reason and then READ BACK — Airbnb accepts some deactivations and leaves the listing up, so "we sent the request" is never reported as success. On **Booking.com** there is no unlist at all; the equivalent is closing the room's availability across the whole forward window, which is what happens.
	//
	// **This is not the same as deactivating the listing in Repull.** The two get confused because both sound like removal, and they have opposite consequences:
	//
	// | | Take offline (this endpoint) | Deactivate in Repull (`PATCH /v1/listings/{id}` `{"active": false}`) |
	// |---|---|---|
	// | The guest-facing listing | **Stops taking bookings** | Stays live and keeps taking bookings |
	// | Billing and plan limits | Unchanged | No longer billed, no longer counts toward the cap |
	// | API access to the listing | Unchanged — you can still read and write it | `403 listing_inactive` until reactivated |
	// | Reverse it with | `POST /v1/listings/{id}/online` | `PATCH /v1/listings/{id}` `{"active": true}` |
	// | Data kept | Yes | Yes, and it keeps syncing |
	//
	// Neither one deletes anything, on either side.
	//
	// **The answer is per channel item.** A listing can sit on several Airbnb connections and a Booking.com property at once; they fail independently and a partial result is the ordinary outcome, so every item reports its own `state`, `code` and `message` and there is no top-level success flag to mislead you. Nothing is rolled back — re-send the same request to retry the items that did not land.
	//
	// **Booking.com ambiguity is reported, not fanned out.** A listing mapped to more than one active Booking.com property comes back with that item refused (`ambiguous_booking_mapping`) while the Airbnb items still run: closing the wrong property's availability takes real inventory off sale, and taking a listing off Airbnb is not less urgent because its Booking.com mapping is untidy. Name the property with `hotelId` and send it again.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/offline (the `TakeListingOffline` operationId).
	TakeListingOfflineWithBodyWithResponse(ctx context.Context, id int, params *TakeListingOfflineParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*TakeListingOfflineClientResponse, error)

	// TakeListingOfflineWithResponse Take a listing off the market
	//
	// Stop this listing being sold, on every channel it is connected to, in one call.
	//
	// What that means differs per channel and you do not have to know which is which. On **Airbnb** the live listing is deactivated with a valid deactivation reason and then READ BACK — Airbnb accepts some deactivations and leaves the listing up, so "we sent the request" is never reported as success. On **Booking.com** there is no unlist at all; the equivalent is closing the room's availability across the whole forward window, which is what happens.
	//
	// **This is not the same as deactivating the listing in Repull.** The two get confused because both sound like removal, and they have opposite consequences:
	//
	// | | Take offline (this endpoint) | Deactivate in Repull (`PATCH /v1/listings/{id}` `{"active": false}`) |
	// |---|---|---|
	// | The guest-facing listing | **Stops taking bookings** | Stays live and keeps taking bookings |
	// | Billing and plan limits | Unchanged | No longer billed, no longer counts toward the cap |
	// | API access to the listing | Unchanged — you can still read and write it | `403 listing_inactive` until reactivated |
	// | Reverse it with | `POST /v1/listings/{id}/online` | `PATCH /v1/listings/{id}` `{"active": true}` |
	// | Data kept | Yes | Yes, and it keeps syncing |
	//
	// Neither one deletes anything, on either side.
	//
	// **The answer is per channel item.** A listing can sit on several Airbnb connections and a Booking.com property at once; they fail independently and a partial result is the ordinary outcome, so every item reports its own `state`, `code` and `message` and there is no top-level success flag to mislead you. Nothing is rolled back — re-send the same request to retry the items that did not land.
	//
	// **Booking.com ambiguity is reported, not fanned out.** A listing mapped to more than one active Booking.com property comes back with that item refused (`ambiguous_booking_mapping`) while the Airbnb items still run: closing the wrong property's availability takes real inventory off sale, and taking a listing off Airbnb is not less urgent because its Booking.com mapping is untidy. Name the property with `hotelId` and send it again.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/offline (the `TakeListingOffline` operationId).
	TakeListingOfflineWithResponse(ctx context.Context, id int, params *TakeListingOfflineParams, body TakeListingOfflineJSONRequestBody, reqEditors ...RequestEditorFn) (*TakeListingOfflineClientResponse, error)

	// TakeListingOnlineWithBodyWithResponse Put a listing back on the market
	//
	// Put this listing back on sale, on every channel it is connected to. The counterpart of `POST /v1/listings/{id}/offline`, which documents the per-item response and the difference between this and deactivating a listing in Repull.
	//
	// **It does not push content.** On **Airbnb** it re-enables sync and makes the listing available again; anything that changed while the listing was down is still unpublished, so follow with `POST /v1/listings/{id}/publish/airbnb` if the content moved. On **Booking.com** it re-syncs the true calendar rather than opening everything: dates that are genuinely blocked — a reservation, an owner stay — stay blocked, and only the closure `offline` wrote lifts. The two directions are not mirror images, and that is deliberate.
	//
	// **One asymmetry worth planning for.** Taking a listing down passes no billing gate; putting it back up goes through the channel-publish gate. So on a workspace whose subscription has lapsed, `offline` still works and this endpoint answers `402 payment_required` — a listing can be left off the market until billing is sorted out. That refusal is reported as a billing refusal with the action that fixes it, never as a channel error: retrying, or reconnecting the channel, does nothing for it.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/online (the `TakeListingOnline` operationId).
	TakeListingOnlineWithBodyWithResponse(ctx context.Context, id int, params *TakeListingOnlineParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*TakeListingOnlineClientResponse, error)

	// TakeListingOnlineWithResponse Put a listing back on the market
	//
	// Put this listing back on sale, on every channel it is connected to. The counterpart of `POST /v1/listings/{id}/offline`, which documents the per-item response and the difference between this and deactivating a listing in Repull.
	//
	// **It does not push content.** On **Airbnb** it re-enables sync and makes the listing available again; anything that changed while the listing was down is still unpublished, so follow with `POST /v1/listings/{id}/publish/airbnb` if the content moved. On **Booking.com** it re-syncs the true calendar rather than opening everything: dates that are genuinely blocked — a reservation, an owner stay — stay blocked, and only the closure `offline` wrote lifts. The two directions are not mirror images, and that is deliberate.
	//
	// **One asymmetry worth planning for.** Taking a listing down passes no billing gate; putting it back up goes through the channel-publish gate. So on a workspace whose subscription has lapsed, `offline` still works and this endpoint answers `402 payment_required` — a listing can be left off the market until billing is sorted out. That refusal is reported as a billing refusal with the action that fixes it, never as a channel error: retrying, or reconnecting the channel, does nothing for it.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/online (the `TakeListingOnline` operationId).
	TakeListingOnlineWithResponse(ctx context.Context, id int, params *TakeListingOnlineParams, body TakeListingOnlineJSONRequestBody, reqEditors ...RequestEditorFn) (*TakeListingOnlineClientResponse, error)

	// DeleteListingPhotoWithBodyWithResponse Delete a stored listing photo
	//
	// Deletes a single stored photo by its storage `path` (as returned by `GET /v1/listings/{id}/photos` or `POST /v1/listings/{id}/photos/upload-url`).
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/listings/{id}/photos (the `DeleteListingPhoto` operationId).
	DeleteListingPhotoWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*DeleteListingPhotoClientResponse, error)

	// DeleteListingPhotoWithResponse Delete a stored listing photo
	//
	// Deletes a single stored photo by its storage `path` (as returned by `GET /v1/listings/{id}/photos` or `POST /v1/listings/{id}/photos/upload-url`).
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/listings/{id}/photos (the `DeleteListingPhoto` operationId).
	DeleteListingPhotoWithResponse(ctx context.Context, id int, body DeleteListingPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*DeleteListingPhotoClientResponse, error)

	// ListListingPhotosWithResponse List a listing's stored photos
	//
	// Returns the photo set currently stored for this listing.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/listings/{id}/photos (the `ListListingPhotos` operationId).
	ListListingPhotosWithResponse(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*ListListingPhotosClientResponse, error)

	// CreateListingPhotoUploadUrlWithBodyWithResponse Mint a direct-to-storage photo upload URL
	//
	// Mints a short-lived signed upload URL + token for a listing photo. **The client PUTs the raw file bytes directly to the returned `uploadUrl` — the file bytes never pass through the Repull API or main vanio.** This endpoint only mints the URL; do not POST the file itself here, it will not be accepted.
	//
	// Flow: (1) POST here with `fileName`/`fileType`/optional `fileSize` to get `{ uploadUrl, token, path, publicUrl, expiresIn }`; (2) PUT the raw file bytes to `uploadUrl` from the client; (3) `publicUrl` is the durable URL for the uploaded photo — attach it to the listing via `PUT /v1/listings/{id}/content` (`photos` field) or list it back via `GET /v1/listings/{id}/photos`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/photos/upload-url (the `CreateListingPhotoUploadUrl` operationId).
	CreateListingPhotoUploadUrlWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateListingPhotoUploadUrlClientResponse, error)

	// CreateListingPhotoUploadUrlWithResponse Mint a direct-to-storage photo upload URL
	//
	// Mints a short-lived signed upload URL + token for a listing photo. **The client PUTs the raw file bytes directly to the returned `uploadUrl` — the file bytes never pass through the Repull API or main vanio.** This endpoint only mints the URL; do not POST the file itself here, it will not be accepted.
	//
	// Flow: (1) POST here with `fileName`/`fileType`/optional `fileSize` to get `{ uploadUrl, token, path, publicUrl, expiresIn }`; (2) PUT the raw file bytes to `uploadUrl` from the client; (3) `publicUrl` is the durable URL for the uploaded photo — attach it to the listing via `PUT /v1/listings/{id}/content` (`photos` field) or list it back via `GET /v1/listings/{id}/photos`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/photos/upload-url (the `CreateListingPhotoUploadUrl` operationId).
	CreateListingPhotoUploadUrlWithResponse(ctx context.Context, id int, body CreateListingPhotoUploadUrlJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateListingPhotoUploadUrlClientResponse, error)

	// GetListingPricingWithResponse Get pricing recommendations
	//
	// Returns date-by-date pricing recommendations for a listing's upcoming calendar window, plus the listing's base-price context and a 5km comp summary. Recommendations come from the Atlas pricing model — pre-computed nightly and stored in `pricing_recommendations`. Use POST to apply or decline pending recommendations.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/listings/{id}/pricing (the `GetListingPricing` operationId).
	GetListingPricingWithResponse(ctx context.Context, id int, params *GetListingPricingParams, reqEditors ...RequestEditorFn) (*GetListingPricingClientResponse, error)

	// ApplyListingPricingWithBodyWithResponse Apply or decline pricing recommendations
	//
	// Apply: writes the recommended price to the listing's calendar for the given dates and triggers the platform fan-out (Airbnb / Booking.com / VRBO). Decline: marks the recommendation as `declined` so it stops surfacing — the model can re-recommend on the next training cycle.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/pricing (the `ApplyListingPricing` operationId).
	ApplyListingPricingWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ApplyListingPricingClientResponse, error)

	// ApplyListingPricingWithResponse Apply or decline pricing recommendations
	//
	// Apply: writes the recommended price to the listing's calendar for the given dates and triggers the platform fan-out (Airbnb / Booking.com / VRBO). Decline: marks the recommendation as `declined` so it stops surfacing — the model can re-recommend on the next training cycle.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/pricing (the `ApplyListingPricing` operationId).
	ApplyListingPricingWithResponse(ctx context.Context, id int, body ApplyListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*ApplyListingPricingClientResponse, error)

	// GetListingPricingHistoryWithResponse Pricing recommendation audit trail
	//
	// Cursor-paginated audit trail of pricing recommendations vs applied prices for a listing across a date window. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
	//
	// Defaults to ±90 days from today. Cursor is a keyset on `date ASC` — stable even if rows are added during a partner's pagination walk. `limit` is capped at 500 — exceeding returns 422.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/listings/{id}/pricing/history (the `GetListingPricingHistory` operationId).
	GetListingPricingHistoryWithResponse(ctx context.Context, id int, params *GetListingPricingHistoryParams, reqEditors ...RequestEditorFn) (*GetListingPricingHistoryClientResponse, error)

	// GetListingPricingStrategyWithResponse Get pricing strategy
	//
	// Returns the strategy that constrains how the Atlas pricing model behaves for this listing. If no strategy row exists yet, returns sane defaults flagged with `isDefault: true`.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/listings/{id}/pricing/strategy (the `GetListingPricingStrategy` operationId).
	GetListingPricingStrategyWithResponse(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*GetListingPricingStrategyClientResponse, error)

	// UpdateListingPricingStrategyWithBodyWithResponse Update pricing strategy
	//
	// Upserts the strategy on `(listing_id, customer_id)` — repeated PUTs are idempotent. Send only the fields you want to change; omitted fields take server-side defaults.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/listings/{id}/pricing/strategy (the `UpdateListingPricingStrategy` operationId).
	UpdateListingPricingStrategyWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateListingPricingStrategyClientResponse, error)

	// UpdateListingPricingStrategyWithResponse Update pricing strategy
	//
	// Upserts the strategy on `(listing_id, customer_id)` — repeated PUTs are idempotent. Send only the fields you want to change; omitted fields take server-side defaults.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PUT /v1/listings/{id}/pricing/strategy (the `UpdateListingPricingStrategy` operationId).
	UpdateListingPricingStrategyWithResponse(ctx context.Context, id int, body UpdateListingPricingStrategyJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateListingPricingStrategyClientResponse, error)

	// GetListingPublishStatusWithResponse Per-channel publish status
	//
	// Returns connection state and sync activity per channel. `channels` is sync activity (empty until first push). `connections` is connection state (populated as soon as a channel is linked). Recommended polling cadence: at most once per 30s per listing — for bulk views, prefer `GET /v1/listings` and filter client-side.
	//
	// **When a push fails, this endpoint says why.** `channels[].pushError` carries the channel's own reason for the last failed push, verbatim — `"Links and contact info can't be shared"`, `"Check-in start time must be before end time"`, `"property_type_group must be one of […]"`. It is free text written by the channel, so render it next to the retry button rather than parsing it. `null` when the last push succeeded or none has run; pair it with `pushStatus` to tell those two apart.
	//
	// **It also says what you will not be allowed to change.** The `airbnb` entry in `connections` carries `lockedFields` — attributes Airbnb has locked on this listing. Airbnb does not refuse a write to one: it answers 200, reports the field as locked, and applies nothing, so a locked write is indistinguishable from a successful one unless you looked first. Read it before you let someone edit. Airbnb-only; no other channel has the concept, and no other entry carries the field.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/listings/{id}/publish-status (the `GetListingPublishStatus` operationId).
	GetListingPublishStatusWithResponse(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*GetListingPublishStatusClientResponse, error)

	// PublishListingToAirbnbWithBodyWithResponse Publish a listing to Airbnb
	//
	// Push a Repull listing's canonical content to Airbnb. Pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create a brand-new Airbnb listing under that host.
	//
	// **A publish is not one call to Airbnb.** It is up to eight independent ones — details, description, amenities, rooms, policies, photos, pricing, checkout_tasks — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Airbnb's own reason, per section, for the ones that did not. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Publish again once you have fixed the failing sections — a re-publish of an unchanged section is harmless.
	//
	// `result.live` is a different question from `result.published`. `published` is about CONTENT — every attempted section landed. `live` is about whether the listing takes bookings: it is true only when activation was actually performed and succeeded. A create can land all eight sections and still leave the listing inactive, because activation is skipped when instant-booking cannot be confirmed to be off — so `published: true` with `live: false` is a real and common outcome, and `result.warnings` says why. `live` is ABSENT, not `false`, when activation was never part of the operation: publishing to an already-mapped listing updates content and activates nothing. Only treat a listing as not-live when `live` is present and false.
	//
	// `result.warnings[]` lists steps that failed WITHOUT failing the publish — optional work the push carried on past. They were previously swallowed, so the only sign of one was a listing that was somehow not quite right afterwards. A publish can be `published: true` and still carry warnings; read them before concluding nothing needs doing.
	//
	// `result.lockedFields` names the fields Airbnb will not let this listing change at all. They are not retryable by anyone: Airbnb answers 200 and applies nothing. `GET /v1/channels/airbnb/listings/{id}` reports the same list up front.
	//
	// **Which fields this pushes** — title, description sections and house rules (English/primary locale), amenities, rooms and beds, photos, nightly price and fees, cancellation policy and guest controls, check-in/out times, quiet hours, property and room type, checkout tasks. **Not pushed by this endpoint:** non-primary locales (`PUT /v1/channels/airbnb/listings/{id}/descriptions`), guest-safety disclosures (`PUT …/safety-disclosures`), check-in method (`PUT …/details`), permits (`PUT …/permits`), and the calendar (`PUT …/availability`).
	//
	// `force: true` re-pushes every section, ignoring dirty-field tracking. Without it only the sections changed since the last successful publish are sent.
	//
	// Send `Idempotency-Key` to make a retry safe: a timeout on a publish otherwise leaves you unable to tell whether it ran.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/publish/airbnb (the `PublishListingToAirbnb` operationId).
	PublishListingToAirbnbWithBodyWithResponse(ctx context.Context, id int, params *PublishListingToAirbnbParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PublishListingToAirbnbClientResponse, error)

	// PublishListingToAirbnbWithResponse Publish a listing to Airbnb
	//
	// Push a Repull listing's canonical content to Airbnb. Pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create a brand-new Airbnb listing under that host.
	//
	// **A publish is not one call to Airbnb.** It is up to eight independent ones — details, description, amenities, rooms, policies, photos, pricing, checkout_tasks — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Airbnb's own reason, per section, for the ones that did not. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Publish again once you have fixed the failing sections — a re-publish of an unchanged section is harmless.
	//
	// `result.live` is a different question from `result.published`. `published` is about CONTENT — every attempted section landed. `live` is about whether the listing takes bookings: it is true only when activation was actually performed and succeeded. A create can land all eight sections and still leave the listing inactive, because activation is skipped when instant-booking cannot be confirmed to be off — so `published: true` with `live: false` is a real and common outcome, and `result.warnings` says why. `live` is ABSENT, not `false`, when activation was never part of the operation: publishing to an already-mapped listing updates content and activates nothing. Only treat a listing as not-live when `live` is present and false.
	//
	// `result.warnings[]` lists steps that failed WITHOUT failing the publish — optional work the push carried on past. They were previously swallowed, so the only sign of one was a listing that was somehow not quite right afterwards. A publish can be `published: true` and still carry warnings; read them before concluding nothing needs doing.
	//
	// `result.lockedFields` names the fields Airbnb will not let this listing change at all. They are not retryable by anyone: Airbnb answers 200 and applies nothing. `GET /v1/channels/airbnb/listings/{id}` reports the same list up front.
	//
	// **Which fields this pushes** — title, description sections and house rules (English/primary locale), amenities, rooms and beds, photos, nightly price and fees, cancellation policy and guest controls, check-in/out times, quiet hours, property and room type, checkout tasks. **Not pushed by this endpoint:** non-primary locales (`PUT /v1/channels/airbnb/listings/{id}/descriptions`), guest-safety disclosures (`PUT …/safety-disclosures`), check-in method (`PUT …/details`), permits (`PUT …/permits`), and the calendar (`PUT …/availability`).
	//
	// `force: true` re-pushes every section, ignoring dirty-field tracking. Without it only the sections changed since the last successful publish are sent.
	//
	// Send `Idempotency-Key` to make a retry safe: a timeout on a publish otherwise leaves you unable to tell whether it ran.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/publish/airbnb (the `PublishListingToAirbnb` operationId).
	PublishListingToAirbnbWithResponse(ctx context.Context, id int, params *PublishListingToAirbnbParams, body PublishListingToAirbnbJSONRequestBody, reqEditors ...RequestEditorFn) (*PublishListingToAirbnbClientResponse, error)

	// PublishListingToBookingWithBodyWithResponse Publish a listing to Booking.com
	//
	// Push a Repull listing's content to Booking.com. The listing must already be mapped to a Booking.com property + room — claim the hotel through the Connect Booking flow, then map its rooms with `POST /v1/connect/booking/map-rooms`.
	//
	// **Which property the content lands in.** A listing can be mapped to more than one Booking.com property; the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. When the listing has exactly one property you need send nothing. When it has several, name one with `hotelId` in the body (or `?hotel_id=` — the same value, accepted either way, body wins if you send both). Omit it on such a listing and the push is refused with **`409 ambiguous_booking_mapping`**, listing the candidate ids: content pushed into a property chosen for you lands on the wrong listing and reports success, which is worse than a refusal. `GET /v1/channels/booking/properties` lists every property with the listings mapped under it. Naming a property this listing is not mapped to is a `404` that names the ones it is.
	//
	// The property that actually received the content comes back as `result.hotelId`.
	//
	// **A publish is not one call to Booking.com.** It is several independent Content API calls — details, description, amenities, rooms, photos, pricing — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Booking.com's own reason, per section, for the ones that did not. A property whose Content API credentials do not cover a section answers 403 for that section alone. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Fix the failing sections and publish again — re-publishing an unchanged section is harmless.
	//
	// A listing with no Booking.com property mapped at all is not an error: the call returns `result.published: false` with `result.reason` and `result.hotelId: null`, and nothing is pushed.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/publish/booking (the `PublishListingToBooking` operationId).
	PublishListingToBookingWithBodyWithResponse(ctx context.Context, id int, params *PublishListingToBookingParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PublishListingToBookingClientResponse, error)

	// PublishListingToBookingWithResponse Publish a listing to Booking.com
	//
	// Push a Repull listing's content to Booking.com. The listing must already be mapped to a Booking.com property + room — claim the hotel through the Connect Booking flow, then map its rooms with `POST /v1/connect/booking/map-rooms`.
	//
	// **Which property the content lands in.** A listing can be mapped to more than one Booking.com property; the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. When the listing has exactly one property you need send nothing. When it has several, name one with `hotelId` in the body (or `?hotel_id=` — the same value, accepted either way, body wins if you send both). Omit it on such a listing and the push is refused with **`409 ambiguous_booking_mapping`**, listing the candidate ids: content pushed into a property chosen for you lands on the wrong listing and reports success, which is worse than a refusal. `GET /v1/channels/booking/properties` lists every property with the listings mapped under it. Naming a property this listing is not mapped to is a `404` that names the ones it is.
	//
	// The property that actually received the content comes back as `result.hotelId`.
	//
	// **A publish is not one call to Booking.com.** It is several independent Content API calls — details, description, amenities, rooms, photos, pricing — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Booking.com's own reason, per section, for the ones that did not. A property whose Content API credentials do not cover a section answers 403 for that section alone. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Fix the failing sections and publish again — re-publishing an unchanged section is harmless.
	//
	// A listing with no Booking.com property mapped at all is not an error: the call returns `result.published: false` with `result.reason` and `result.hotelId: null`, and nothing is pushed.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/publish/booking (the `PublishListingToBooking` operationId).
	PublishListingToBookingWithResponse(ctx context.Context, id int, params *PublishListingToBookingParams, body PublishListingToBookingJSONRequestBody, reqEditors ...RequestEditorFn) (*PublishListingToBookingClientResponse, error)

	// PullListingFromAirbnbWithBodyWithResponse Refresh a listing from Airbnb
	//
	// Re-read this listing from Airbnb and update your stored copy, then report what was refreshed and when. The mirror image of `POST /v1/listings/{id}/publish/airbnb`.
	//
	// Every other Airbnb read on this API is served from our database. This endpoint is the one that goes and asks Airbnb — use it after a push, to see the values Airbnb actually kept, or when a host has changed something in the Airbnb app.
	//
	// **What it refreshes:** basic listing facts (property type, bedrooms, beds, bathrooms, capacity), descriptions, photos, rooms and beds, amenities, booking settings (check-in/check-out windows, guest controls, cancellation policy), stay rules (min/max nights, advance-booking window, turnover buffer), pricing settings and standard fees, permits, checkout tasks and the check-in guide. After it returns, those values are what `GET /v1/listings/{id}?include=content,details` and the `/v1/channels/airbnb/**` routes serve.
	//
	// **What it does NOT refresh:** the calendar (nightly rates and availability — see `GET /v1/channels/airbnb/listings/{id}/availability`), reservations, messages, reviews or payouts. Those arrive continuously through the channel's own sync and never need a manual pull.
	//
	// **Runs synchronously** — the response is the result, not a job id. Expect several seconds.
	//
	// **One pull per listing per 15 minutes.** A pull is roughly a dozen Airbnb calls; a second call inside the window returns `429 rate_limit_exceeded` with `Retry-After` and `nextPullAvailableAt`, and makes no Airbnb calls. Two simultaneous calls cannot both run.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/pull/airbnb (the `PullListingFromAirbnb` operationId).
	PullListingFromAirbnbWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PullListingFromAirbnbClientResponse, error)

	// PullListingFromAirbnbWithResponse Refresh a listing from Airbnb
	//
	// Re-read this listing from Airbnb and update your stored copy, then report what was refreshed and when. The mirror image of `POST /v1/listings/{id}/publish/airbnb`.
	//
	// Every other Airbnb read on this API is served from our database. This endpoint is the one that goes and asks Airbnb — use it after a push, to see the values Airbnb actually kept, or when a host has changed something in the Airbnb app.
	//
	// **What it refreshes:** basic listing facts (property type, bedrooms, beds, bathrooms, capacity), descriptions, photos, rooms and beds, amenities, booking settings (check-in/check-out windows, guest controls, cancellation policy), stay rules (min/max nights, advance-booking window, turnover buffer), pricing settings and standard fees, permits, checkout tasks and the check-in guide. After it returns, those values are what `GET /v1/listings/{id}?include=content,details` and the `/v1/channels/airbnb/**` routes serve.
	//
	// **What it does NOT refresh:** the calendar (nightly rates and availability — see `GET /v1/channels/airbnb/listings/{id}/availability`), reservations, messages, reviews or payouts. Those arrive continuously through the channel's own sync and never need a manual pull.
	//
	// **Runs synchronously** — the response is the result, not a job id. Expect several seconds.
	//
	// **One pull per listing per 15 minutes.** A pull is roughly a dozen Airbnb calls; a second call inside the window returns `429 rate_limit_exceeded` with `Retry-After` and `nextPullAvailableAt`, and makes no Airbnb calls. Two simultaneous calls cannot both run.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/listings/{id}/pull/airbnb (the `PullListingFromAirbnb` operationId).
	PullListingFromAirbnbWithResponse(ctx context.Context, id int, body PullListingFromAirbnbJSONRequestBody, reqEditors ...RequestEditorFn) (*PullListingFromAirbnbClientResponse, error)

	// GetListingSegmentsWithResponse Atlas DNA segment intelligence for a listing
	//
	// Aggregates Atlas DNA segment signal (quality tier, design style, bedrooms) across the listing's geographic neighborhood (default: 5km radius) or the whole city, so consumers can answer:
	// - What segments dominate my market?
	// - Which segment does my listing match best?
	// - What's the ADR uplift for moving up a tier?
	//
	// DNA coverage is still ramping — segments are scored asynchronously. Cities and radii without scored comps return `totalCompsAnalyzed: 0` plus a `low_dna_coverage` recommendation rather than fabricated data.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/listings/{id}/segments (the `GetListingSegments` operationId).
	GetListingSegmentsWithResponse(ctx context.Context, id int, params *GetListingSegmentsParams, reqEditors ...RequestEditorFn) (*GetListingSegmentsClientResponse, error)

	// ListMarketsWithResponse List markets the customer operates in
	//
	// Returns per-city KPIs across every market the authenticated customer has listings in (market share, ADR vs market, occupancy, ratings) plus a lightweight `browse` discovery summary (top-50 featured markets, country categories, total catalog size). For the full paginated discovery catalog with search, call `GET /v1/markets/browse`. Each `markets[]` entry is enriched with `subscribed` + `source` from the customer's market subscriptions.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/markets (the `ListMarkets` operationId).
	ListMarketsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListMarketsClientResponse, error)

	// ListMarketBrowseWithResponse Paginated discovery catalog
	//
	// Cursor-paginated, search-filterable catalog of every Atlas-tracked market the customer could expand into. Backed by the precomputed `market_summaries` table (>=5 active comps per city). Supports fuzzy `q` substring search (trigram-indexed), `country` (ISO 3166-1 alpha-2) filter, and `sort` (`listings_desc` | `name_asc`). Use the `nextCursor` from `pagination` to walk pages — the cursor is an opaque base64 token; do not parse it.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// `pagination.total` is the count of markets matching the current `q`/`country`/`min_listings` filter (across all pages) — same shape as every other list endpoint.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/markets/browse (the `ListMarketBrowse` operationId).
	ListMarketBrowseWithResponse(ctx context.Context, params *ListMarketBrowseParams, reqEditors ...RequestEditorFn) (*ListMarketBrowseClientResponse, error)

	// GetMarketWithResponse Deep-dive on a single market
	//
	// Detailed market view for one city — price distribution, bedroom mix, property types, upcoming events, Wheelhouse demand, monthly benchmarks, customer health rollup, top comps (proximity-sorted, paginated), customer's percentile position, capacity-mix gap, and a 6-month supply trend.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/markets/{city} (the `GetMarket` operationId).
	GetMarketWithResponse(ctx context.Context, city string, params *GetMarketParams, reqEditors ...RequestEditorFn) (*GetMarketClientResponse, error)

	// GetMarketCalendarWithResponse Calendar-level market view
	//
	// Date-by-date market view for a city — market avg / min / max nightly rate, occupancy %, Wheelhouse demand, events touching the date, and (when `listingId` is supplied) an overlay of the customer's own pricing + availability.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/markets/{city}/calendar (the `GetMarketCalendar` operationId).
	GetMarketCalendarWithResponse(ctx context.Context, city string, params *GetMarketCalendarParams, reqEditors ...RequestEditorFn) (*GetMarketCalendarClientResponse, error)

	// ListMigrationsWithResponse List migrations
	//
	// Every property manager your workspace has moved through Repull Migrate, newest first, each with where it stands. Page with `pagination.nextCursor`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/migrations (the `ListMigrations` operationId).
	ListMigrationsWithResponse(ctx context.Context, params *ListMigrationsParams, reqEditors ...RequestEditorFn) (*ListMigrationsClientResponse, error)

	// DeleteMigrationWithResponse End a migration
	//
	// Disconnects the source (it stops syncing) and deactivates the migration's workspace. The imported data is kept and stays readable.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/migrations/{workspaceId} (the `DeleteMigration` operationId).
	DeleteMigrationWithResponse(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*DeleteMigrationClientResponse, error)

	// GetMigrationWithResponse Get a migration
	//
	// Where one migration stands: its state, the source connections with their last import run, and how much data has landed.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/migrations/{workspaceId} (the `GetMigration` operationId).
	GetMigrationWithResponse(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*GetMigrationClientResponse, error)

	// GetMigrationChannelMapWithResponse Get channel links
	//
	// The Airbnb, Booking.com and VRBO listing each migrated property is linked to in the source PMS, read live. Use it to link each listing to the right property when the channels are reconnected in the destination — channel connections themselves cannot be moved between PMSs.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/migrations/{workspaceId}/channel-map (the `GetMigrationChannelMap` operationId).
	GetMigrationChannelMapWithResponse(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*GetMigrationChannelMapClientResponse, error)

	// CutoverMigrationWithResponse Cut over
	//
	// The property manager has switched: disconnect the source so it stops syncing. The imported data stays readable. Idempotent.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/migrations/{workspaceId}/cutover (the `CutoverMigration` operationId).
	CutoverMigrationWithResponse(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*CutoverMigrationClientResponse, error)

	// CheckMigrationCutoverWithBodyWithResponse Check reservations before switching
	//
	// Send every upcoming reservation the destination holds; get back what is missing, extra, or on different dates compared with the source. Read-only — run it as often as you like before cutover.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/migrations/{workspaceId}/cutover-check (the `CheckMigrationCutover` operationId).
	CheckMigrationCutoverWithBodyWithResponse(ctx context.Context, workspaceId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CheckMigrationCutoverClientResponse, error)

	// CheckMigrationCutoverWithResponse Check reservations before switching
	//
	// Send every upcoming reservation the destination holds; get back what is missing, extra, or on different dates compared with the source. Read-only — run it as often as you like before cutover.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/migrations/{workspaceId}/cutover-check (the `CheckMigrationCutover` operationId).
	CheckMigrationCutoverWithResponse(ctx context.Context, workspaceId int, body CheckMigrationCutoverJSONRequestBody, reqEditors ...RequestEditorFn) (*CheckMigrationCutoverClientResponse, error)

	// RunMigrationImportWithBodyWithResponse Run the import again
	//
	// Queue another import from the source PMS — for example messages after the first pass, or only reservations changed since a date. Progress shows on `GET /v1/migrations/{workspaceId}`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/migrations/{workspaceId}/import (the `RunMigrationImport` operationId).
	RunMigrationImportWithBodyWithResponse(ctx context.Context, workspaceId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*RunMigrationImportClientResponse, error)

	// RunMigrationImportWithResponse Run the import again
	//
	// Queue another import from the source PMS — for example messages after the first pass, or only reservations changed since a date. Progress shows on `GET /v1/migrations/{workspaceId}`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/migrations/{workspaceId}/import (the `RunMigrationImport` operationId).
	RunMigrationImportWithResponse(ctx context.Context, workspaceId int, body RunMigrationImportJSONRequestBody, reqEditors ...RequestEditorFn) (*RunMigrationImportClientResponse, error)

	// GetMigrationReportWithResponse Get a migration report
	//
	// What came across and what needs a decision in the destination: properties without an address, upcoming reservations with no guest contact, channel reservations that must not be re-created, and anything the source PMS cannot carry. Includes the source's capability matrix.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/migrations/{workspaceId}/report (the `GetMigrationReport` operationId).
	GetMigrationReportWithResponse(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*GetMigrationReportClientResponse, error)

	// ListPropertiesWithResponse List properties
	//
	// Cursor-paginated list of properties for the authenticated workspace. Walk pages with `?cursor=<pagination.nextCursor>`; stop when `pagination.hasMore` is `false`. Cursor is opaque base64 — do not parse it.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// Filters: `q` (substring on name/street/city), `status` (active|inactive|all), `lifecycle_status` (exact match on the listing's lifecycle state). Other unknown params (e.g. `?search=` or `?propertyId=`) are rejected with 422 — no silent unfiltered results.
	//
	// **Incremental sync (only changes since last poll):** pass `?updated_since=<ISO8601>` to receive only properties changed at or after that instant. Each property carries `updatedAt` — the last row of the final page is your next watermark. `updated_since` changes the page ordering to `updatedAt ASC, id ASC` (and the cursor with it); see the parameter description. `GET /v1/listings` does NOT yet accept `updated_since` — use this endpoint for property-side incremental sync.
	//
	// **Inactive properties:** an inactive property keeps syncing, but cannot be read or changed through the API until it is activated. They are only listed when `status` asks for them, and then with `id`, `name`, `status`, `lifecycleStatus`, `channels` and `updatedAt` only — enough to choose what to activate with `PATCH /v1/listings/{id}`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/properties (the `ListProperties` operationId).
	ListPropertiesWithResponse(ctx context.Context, params *ListPropertiesParams, reqEditors ...RequestEditorFn) (*ListPropertiesClientResponse, error)

	// GetPropertyWithResponse Get property details
	//
	// Fetch a single property by Repull id. Property ids are workspace-scoped — an id from one workspace is not valid in another. 404 means the id does not exist OR belongs to a different workspace.
	//
	// **Optional expansions:** Pass `?include=amenities` to enrich the response with the property's amenities (sourced from the unified `listings_amenities` table). Returns `[]` when the property has no amenity rows. The default response stays lean; consumers must opt in.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/properties/{id} (the `GetProperty` operationId).
	GetPropertyWithResponse(ctx context.Context, id int, params *GetPropertyParams, reqEditors ...RequestEditorFn) (*GetPropertyClientResponse, error)

	// GetQuoteWithResponse Price a stay
	//
	// Returns the full price breakdown for a stay — nightly total, length-of-stay discount, cleaning fee, pet and other fees, taxes, and the total.
	//
	// A quote is priced against a booking website, because the markup, custom fees and tax overrides that decide what a guest is actually charged live there. A workspace with no booking site receives `422 quote_unavailable` rather than a number computed from different rules than the ones applied at checkout.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/quotes (the `GetQuote` operationId).
	GetQuoteWithResponse(ctx context.Context, params *GetQuoteParams, reqEditors ...RequestEditorFn) (*GetQuoteClientResponse, error)

	// ListReservationsWithResponse List reservations
	//
	// Cursor-paginated list of reservations across all connected PMS platforms. Filter by platform, status, listing, or check-in date range.
	//
	// **Pagination:** Walk pages with `?cursor=` — pass `pagination.nextCursor` from one response back as `?cursor=` on the next request. Stop when `pagination.hasMore` is `false`. `limit` defaults to 50, max 100; requesting more returns 422 (no silent truncation).
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. For deep pagination cursor remains O(1) per page; offset > 10000 returns 422 with a docs link.
	//
	// **Incremental sync (only changes since last poll):** pass `?updated_since=<ISO8601>` to receive only reservations amended, cancelled, or created at or after that instant — no full re-walk. Each row carries `updatedAt`; the last row of the final page is your next watermark. Note that `updated_since` changes the page ordering to `updatedAt ASC, id ASC` (and the cursor with it) so mid-walk amendments cannot be skipped — see the parameter description for the full contract.
	//
	// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/reservations (the `ListReservations` operationId).
	ListReservationsWithResponse(ctx context.Context, params *ListReservationsParams, reqEditors ...RequestEditorFn) (*ListReservationsClientResponse, error)

	// CreateReservationWithBodyWithResponse Create a reservation
	//
	// Creates a reservation and everything that hangs off one: the guest, the conversation thread, the dashboard item, the calendar block, and the `reservation.created` fan-out that issues the door code and starts the messaging automations.
	//
	// **Platform is restricted to `direct`, `website` and `owner`.** Reservations on Airbnb, Booking.com and Vrbo are owned by the channel and arrive through sync — creating one here would mint a local booking the channel has never heard of, which then fights the next sync. Create those on the channel.
	//
	// **Dates are validated** (`YYYY-MM-DD`, and `checkOut` must be after `checkIn`), and an unrecognised field is rejected by name rather than silently ignored.
	//
	// **This endpoint does not set the price.** There is no `totalPrice` field: the reservation pipeline derives the price breakdown from the property's own rates and overwrites anything supplied, so accepting a total would be taking a value and discarding it. A reservation created here is priced by that engine (`0` when the property has no rates for the range). `currency` IS honoured. Quote a stay with `GET /v1/quotes` before booking if you need the figure up front.
	//
	// **Availability is NOT checked.** This creates the reservation you asked for even if the dates overlap an existing booking. Call `GET /v1/availability/{propertyId}` first if that matters.
	//
	// Send `Idempotency-Key` — a network timeout here is exactly the case it exists for: without it, a retry books the guest twice.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/reservations (the `CreateReservation` operationId).
	CreateReservationWithBodyWithResponse(ctx context.Context, params *CreateReservationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateReservationClientResponse, error)

	// CreateReservationWithResponse Create a reservation
	//
	// Creates a reservation and everything that hangs off one: the guest, the conversation thread, the dashboard item, the calendar block, and the `reservation.created` fan-out that issues the door code and starts the messaging automations.
	//
	// **Platform is restricted to `direct`, `website` and `owner`.** Reservations on Airbnb, Booking.com and Vrbo are owned by the channel and arrive through sync — creating one here would mint a local booking the channel has never heard of, which then fights the next sync. Create those on the channel.
	//
	// **Dates are validated** (`YYYY-MM-DD`, and `checkOut` must be after `checkIn`), and an unrecognised field is rejected by name rather than silently ignored.
	//
	// **This endpoint does not set the price.** There is no `totalPrice` field: the reservation pipeline derives the price breakdown from the property's own rates and overwrites anything supplied, so accepting a total would be taking a value and discarding it. A reservation created here is priced by that engine (`0` when the property has no rates for the range). `currency` IS honoured. Quote a stay with `GET /v1/quotes` before booking if you need the figure up front.
	//
	// **Availability is NOT checked.** This creates the reservation you asked for even if the dates overlap an existing booking. Call `GET /v1/availability/{propertyId}` first if that matters.
	//
	// Send `Idempotency-Key` — a network timeout here is exactly the case it exists for: without it, a retry books the guest twice.
	//
	// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/reservations (the `CreateReservation` operationId).
	CreateReservationWithResponse(ctx context.Context, params *CreateReservationParams, body CreateReservationJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateReservationClientResponse, error)

	// GetReservationWithResponse Get reservation details
	//
	// Returns the full record for a single reservation, scoped to the authenticated workspace. Response shape is identical to a single row in `GET /v1/reservations` so SDK consumers can use the same type for both. Returns **404** if the id does not exist OR belongs to a different workspace — the API never differentiates the two so caller can't enumerate other workspaces' ids.
	//
	// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/reservations/{id} (the `GetReservation` operationId).
	GetReservationWithResponse(ctx context.Context, id int, params *GetReservationParams, reqEditors ...RequestEditorFn) (*GetReservationClientResponse, error)

	// UpdateReservationWithBodyWithResponse Update a reservation
	//
	// Changes the dates, the occupancy, or the unit. Drives the same command path the dashboard does, so the side effects come with it: the change audit is appended, bound task due dates re-sync, the old calendar dates unblock and the new ones block, the conversation's cached listing is invalidated, and `reservation.updated` fires — which is what revokes and re-issues the door code.
	//
	// Supply at least one field; an empty body returns 422 rather than a 200 that changed nothing.
	//
	// **Moving and re-dating in one call is one operation.** Send `listingId` together with `checkIn`/`checkOut` and it is applied as a single move, so the access code is re-issued once rather than twice.
	//
	// ### Fields this endpoint deliberately does NOT accept
	//
	// Each is rejected by name with the reason, never accepted and ignored:
	//
	// | Field | Why |
	// |---|---|
	// | `guest` / `guestDetails` | Guest name, email and phone live on the guest record. The underlying command has no branch for them, so accepting them would return a success that changed nothing. |
	// | `pricing` / `totalPrice` / `currency` | Repricing writes the price breakdown, the pricing row and a pricing-history entry. It belongs to its own endpoint. |
	// | `status` | Not a field. Cancelling, confirming and checking out are separate operations with materially different side effects — cancellation issues a credit refund and revokes access codes. |
	// | `platform` | Immutable: it records where the booking actually originated. |
	// | `notes` | `internal_notes` is an append-only audit trail the system writes on every change. |
	//
	// **Availability is NOT checked.** A date change that overlaps another booking will be written. Call `GET /v1/availability/{propertyId}` first if that matters.
	//
	// Returns `403 listing_inactive` when the reservation is on an inactive listing, or when a `listingId` move targets one; nothing is changed.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/reservations/{id} (the `UpdateReservation` operationId).
	UpdateReservationWithBodyWithResponse(ctx context.Context, id int, params *UpdateReservationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateReservationClientResponse, error)

	// UpdateReservationWithResponse Update a reservation
	//
	// Changes the dates, the occupancy, or the unit. Drives the same command path the dashboard does, so the side effects come with it: the change audit is appended, bound task due dates re-sync, the old calendar dates unblock and the new ones block, the conversation's cached listing is invalidated, and `reservation.updated` fires — which is what revokes and re-issues the door code.
	//
	// Supply at least one field; an empty body returns 422 rather than a 200 that changed nothing.
	//
	// **Moving and re-dating in one call is one operation.** Send `listingId` together with `checkIn`/`checkOut` and it is applied as a single move, so the access code is re-issued once rather than twice.
	//
	// ### Fields this endpoint deliberately does NOT accept
	//
	// Each is rejected by name with the reason, never accepted and ignored:
	//
	// | Field | Why |
	// |---|---|
	// | `guest` / `guestDetails` | Guest name, email and phone live on the guest record. The underlying command has no branch for them, so accepting them would return a success that changed nothing. |
	// | `pricing` / `totalPrice` / `currency` | Repricing writes the price breakdown, the pricing row and a pricing-history entry. It belongs to its own endpoint. |
	// | `status` | Not a field. Cancelling, confirming and checking out are separate operations with materially different side effects — cancellation issues a credit refund and revokes access codes. |
	// | `platform` | Immutable: it records where the booking actually originated. |
	// | `notes` | `internal_notes` is an append-only audit trail the system writes on every change. |
	//
	// **Availability is NOT checked.** A date change that overlaps another booking will be written. Call `GET /v1/availability/{propertyId}` first if that matters.
	//
	// Returns `403 listing_inactive` when the reservation is on an inactive listing, or when a `listingId` move targets one; nothing is changed.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/reservations/{id} (the `UpdateReservation` operationId).
	UpdateReservationWithResponse(ctx context.Context, id int, params *UpdateReservationParams, body UpdateReservationJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateReservationClientResponse, error)

	// AcceptReservationRequestWithResponse Accept a booking request
	//
	// Accept a pending Airbnb booking request — a reservation with status `pending`, made on a listing without Instant Book. Find them with `GET /v1/reservations?status=pending`. Airbnb expires a request the host has not answered within 24 hours.
	//
	// Airbnb confirms asynchronously: the reservation’s status moves to confirmed, and a `reservation.updated` webhook fires, when Airbnb’s notification lands (usually within seconds). The response reports what Airbnb was asked to do.
	//
	// **Airbnb only**, and only for listings connected to Airbnb directly: other channels have no request step (`422 channel_not_supported`). A reservation that is not pending is refused before Airbnb is contacted (`409 reservation_not_pending`); one Airbnb says already moved on is `409 request_no_longer_pending`. Neither is worth retrying.
	//
	// Takes no body.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/reservations/{id}/accept (the `AcceptReservationRequest` operationId).
	AcceptReservationRequestWithResponse(ctx context.Context, id int, params *AcceptReservationRequestParams, reqEditors ...RequestEditorFn) (*AcceptReservationRequestClientResponse, error)

	// DeclineReservationRequestWithBodyWithResponse Decline a booking request
	//
	// Decline a pending Airbnb booking request (a reservation with status `pending`; find them with `GET /v1/reservations?status=pending`).
	//
	// `reason` must be one of Airbnb’s own decline reasons. `message` is required: Airbnb sends it to the guest with the decline (at most 500 characters). It is not defaulted — a canned message would put words in your mouth.
	//
	// Airbnb confirms asynchronously; the reservation’s status moves, and `reservation.updated` fires, when its notification lands. Same channel and status rules as `POST /v1/reservations/{id}/accept`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/reservations/{id}/decline (the `DeclineReservationRequest` operationId).
	DeclineReservationRequestWithBodyWithResponse(ctx context.Context, id int, params *DeclineReservationRequestParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*DeclineReservationRequestClientResponse, error)

	// DeclineReservationRequestWithResponse Decline a booking request
	//
	// Decline a pending Airbnb booking request (a reservation with status `pending`; find them with `GET /v1/reservations?status=pending`).
	//
	// `reason` must be one of Airbnb’s own decline reasons. `message` is required: Airbnb sends it to the guest with the decline (at most 500 characters). It is not defaulted — a canned message would put words in your mouth.
	//
	// Airbnb confirms asynchronously; the reservation’s status moves, and `reservation.updated` fires, when its notification lands. Same channel and status rules as `POST /v1/reservations/{id}/accept`.
	//
	// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/reservations/{id}/decline (the `DeclineReservationRequest` operationId).
	DeclineReservationRequestWithResponse(ctx context.Context, id int, params *DeclineReservationRequestParams, body DeclineReservationRequestJSONRequestBody, reqEditors ...RequestEditorFn) (*DeclineReservationRequestClientResponse, error)

	// ListReviewsWithResponse List reviews
	//
	// Cursor-paginated guest + host review stream for the workspace. Backed by main vanio's unified `reviews` table (populated by per-channel backfill crons), so this surface returns the complete cross-channel history — separate from `/v1/channels/airbnb/reviews` which hits Airbnb live.
	//
	// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
	//
	// Filters: `platform` (`airbnb`|`booking`|`vrbo`), `listing_id` (internal Repull listing id), `rating_min` / `rating_max` (inclusive bounds, 0..5), `status` (`responded`|`unanswered`|`all`), `reviewer_role` (`guest` (default) | `host` | `all`).
	//
	// **Inactive listings:** reviews of inactive listings are left out of the page and of `pagination.total`. Filtering by an inactive `listing_id` returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/reviews (the `ListReviews` operationId).
	ListReviewsWithResponse(ctx context.Context, params *ListReviewsParams, reqEditors ...RequestEditorFn) (*ListReviewsClientResponse, error)

	// GetReviewWithResponse Get review
	//
	// Returns one review (the bare `Review` object — NOT wrapped in `{ data: ... }`). Scoped to the authenticated workspace via the listings join — reviews that don't belong to the workspace return 404 (we don't differentiate to avoid leaking other customers' ids).
	//
	// A review of an inactive listing returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/reviews/{id} (the `GetReview` operationId).
	GetReviewWithResponse(ctx context.Context, id int, params *GetReviewParams, reqEditors ...RequestEditorFn) (*GetReviewClientResponse, error)

	// ReplyToReviewWithBodyWithResponse Reply to a review on any channel
	//
	// Resolves the review, reads its channel and dispatches the reply. Channel-neutral: you do not need to know where the review came from.
	//
	// Replies are available on Airbnb today; a review from a channel without a reply API returns `422 unsupported_channel` naming the channels that do work.
	//
	// **Inactive listings:** a review of an inactive listing returns `403 listing_inactive` and no reply reaches the channel. Activate the listing first.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/reviews/{id}/reply (the `ReplyToReview` operationId).
	ReplyToReviewWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReplyToReviewClientResponse, error)

	// ReplyToReviewWithResponse Reply to a review on any channel
	//
	// Resolves the review, reads its channel and dispatches the reply. Channel-neutral: you do not need to know where the review came from.
	//
	// Replies are available on Airbnb today; a review from a channel without a reply API returns `422 unsupported_channel` naming the channels that do work.
	//
	// **Inactive listings:** a review of an inactive listing returns `403 listing_inactive` and no reply reaches the channel. Activate the listing first.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/reviews/{id}/reply (the `ReplyToReview` operationId).
	ReplyToReviewWithResponse(ctx context.Context, id int, body ReplyToReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*ReplyToReviewClientResponse, error)

	// ListCustomSchemasWithResponse List custom schemas
	//
	// Returns every custom schema owned by the workspace, including inactive ones.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/schema/custom (the `ListCustomSchemas` operationId).
	ListCustomSchemasWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListCustomSchemasClientResponse, error)

	// CreateCustomSchemaWithBodyWithResponse Create a custom schema
	//
	// Create a workspace-scoped field-mapping schema. The schema reshapes the `native` response payload into your app's preferred field names. After creation, set `X-Schema: <name>` on any read endpoint to apply it.
	//
	// **Reserved names:** `calry`, `calry-v1`, `native` are built-in schemas and cannot be used as a custom name.
	//
	// **Mapping safety:** Each mapping value is parsed by an internal expression engine — `eval`, `Function`, `process`, and other unsafe keywords are rejected up front. Field names are capped at 100 chars and expressions at 500 chars.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/schema/custom (the `CreateCustomSchema` operationId).
	CreateCustomSchemaWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateCustomSchemaClientResponse, error)

	// CreateCustomSchemaWithResponse Create a custom schema
	//
	// Create a workspace-scoped field-mapping schema. The schema reshapes the `native` response payload into your app's preferred field names. After creation, set `X-Schema: <name>` on any read endpoint to apply it.
	//
	// **Reserved names:** `calry`, `calry-v1`, `native` are built-in schemas and cannot be used as a custom name.
	//
	// **Mapping safety:** Each mapping value is parsed by an internal expression engine — `eval`, `Function`, `process`, and other unsafe keywords are rejected up front. Field names are capped at 100 chars and expressions at 500 chars.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/schema/custom (the `CreateCustomSchema` operationId).
	CreateCustomSchemaWithResponse(ctx context.Context, body CreateCustomSchemaJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateCustomSchemaClientResponse, error)

	// DeleteCustomSchemaWithResponse Delete a custom schema
	//
	// Hard-delete a custom schema. Subsequent requests carrying its name in `X-Schema` fall back to `native`. There is no undelete.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/schema/custom/{id} (the `DeleteCustomSchema` operationId).
	DeleteCustomSchemaWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*DeleteCustomSchemaClientResponse, error)

	// GetCustomSchemaWithResponse Get a custom schema
	//
	// Fetch a single custom schema by id. Scoped to the authenticated workspace — schemas that belong to other workspaces return 404.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/schema/custom/{id} (the `GetCustomSchema` operationId).
	GetCustomSchemaWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*GetCustomSchemaClientResponse, error)

	// UpdateCustomSchemaWithBodyWithResponse Update a custom schema
	//
	// Patch the description, mappings, or active flag of a custom schema. The schema `name` is immutable — create a new schema and migrate consumers if you need to rename. Mapping updates are revalidated for safety.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/schema/custom/{id} (the `UpdateCustomSchema` operationId).
	UpdateCustomSchemaWithBodyWithResponse(ctx context.Context, id openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateCustomSchemaClientResponse, error)

	// UpdateCustomSchemaWithResponse Update a custom schema
	//
	// Patch the description, mappings, or active flag of a custom schema. The schema `name` is immutable — create a new schema and migrate consumers if you need to rename. Mapping updates are revalidated for safety.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/schema/custom/{id} (the `UpdateCustomSchema` operationId).
	UpdateCustomSchemaWithResponse(ctx context.Context, id openapi_types.UUID, body UpdateCustomSchemaJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateCustomSchemaClientResponse, error)

	// GetUsageLogsWithResponse List API request logs
	//
	// Cursor-paginated raw API request log for the authenticated workspace, newest first. Filter by time `range`, `operation` id(s), status class, or free-text `q`. Walk pages with `cursor` from `pagination.nextCursor` until `pagination.hasMore` is `false`; `offset` is accepted as a shallow alias (deep walks must use `cursor`).
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/usage/logs (the `GetUsageLogs` operationId).
	GetUsageLogsWithResponse(ctx context.Context, params *GetUsageLogsParams, reqEditors ...RequestEditorFn) (*GetUsageLogsClientResponse, error)

	// GetUsageSummaryWithResponse Get usage summary
	//
	// Aggregated usage over the requested `range` — tier + plan limits, quota used/remaining, next reset, a per-operation breakdown (request/error counts, error rate, avg latency), a daily timeline, status-class distribution, and range totals. Two request quotas are reported and they reset at different times: `dailyRequests` is the daily circuit breaker that stops runaway client loops (resets at `dailyResetsAt`, the next UTC midnight) and `monthlyRequests` is the billing quota (resets at `resetsAt`). `null` limits mean unlimited on that dimension.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/usage/summary (the `GetUsageSummary` operationId).
	GetUsageSummaryWithResponse(ctx context.Context, params *GetUsageSummaryParams, reqEditors ...RequestEditorFn) (*GetUsageSummaryClientResponse, error)

	// GetUsageTierWithResponse Get tier and quota
	//
	// Lightweight current-tier snapshot for status badges and quota meters — plan limits (daily requests, monthly requests, daily AI requests, dynamic-pricing listings), the amount used, the amount remaining, and the next reset. Two request quotas are reported and they reset at different times: `dailyRequests` is the daily circuit breaker that stops runaway client loops (resets at `dailyResetsAt`, the next UTC midnight) and `monthlyRequests` is the billing quota (resets at `resetsAt`). Exceeding the daily cap returns 429 `daily_limit_exceeded`; exceeding the monthly one returns 429 `rate_limit_exceeded`. `null` limits mean unlimited on that dimension.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/usage/tier (the `GetUsageTier` operationId).
	GetUsageTierWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetUsageTierClientResponse, error)

	// ListWebhooksWithResponse List webhook subscriptions
	//
	// List every webhook subscription registered for this workspace. Each row includes the destination URL, subscribed event types, and the most recent delivery summary.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/webhooks (the `ListWebhooks` operationId).
	ListWebhooksWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListWebhooksClientResponse, error)

	// CreateWebhookWithBodyWithResponse Create webhook subscription
	//
	// Register a new endpoint. Returns the plaintext signing secret ONCE — capture it from the response and store it securely. After this call the secret is masked everywhere; mint a new one with `POST /v1/webhooks/{id}/rotate-secret` if you lose it. See `GET /v1/webhooks/event-types` for the full list of subscribable events. Events about an inactive listing (reservations, messages, alterations, reviews, payments, calendar and listing events) are not delivered. The data keeps syncing while the listing is inactive, but its events are never sent — including after you reactivate it; webhooks resume for events that happen from reactivation on. Account-level events are always delivered.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/webhooks (the `CreateWebhook` operationId).
	CreateWebhookWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateWebhookClientResponse, error)

	// CreateWebhookWithResponse Create webhook subscription
	//
	// Register a new endpoint. Returns the plaintext signing secret ONCE — capture it from the response and store it securely. After this call the secret is masked everywhere; mint a new one with `POST /v1/webhooks/{id}/rotate-secret` if you lose it. See `GET /v1/webhooks/event-types` for the full list of subscribable events. Events about an inactive listing (reservations, messages, alterations, reviews, payments, calendar and listing events) are not delivered. The data keeps syncing while the listing is inactive, but its events are never sent — including after you reactivate it; webhooks resume for events that happen from reactivation on. Account-level events are always delivered.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/webhooks (the `CreateWebhook` operationId).
	CreateWebhookWithResponse(ctx context.Context, body CreateWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateWebhookClientResponse, error)

	// ListWebhookEventTypesWithResponse List webhook event types
	//
	// The canonical catalog of every event the API can deliver, grouped by domain, with realistic sample payloads.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/webhooks/event-types (the `ListWebhookEventTypes` operationId).
	ListWebhookEventTypesWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListWebhookEventTypesClientResponse, error)

	// TestWebhookWithBodyWithResponse [Legacy] Send test webhook to a URL
	//
	// Deprecated: prefer creating a subscription then calling `POST /v1/webhooks/{id}/ping`. Kept for back-compat.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/webhooks/test (the `TestWebhook` operationId).
	TestWebhookWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*TestWebhookClientResponse, error)

	// TestWebhookWithResponse [Legacy] Send test webhook to a URL
	//
	// Deprecated: prefer creating a subscription then calling `POST /v1/webhooks/{id}/ping`. Kept for back-compat.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/webhooks/test (the `TestWebhook` operationId).
	TestWebhookWithResponse(ctx context.Context, body TestWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*TestWebhookClientResponse, error)

	// DeleteWebhookWithResponse Delete webhook subscription
	//
	// Delete a webhook subscription. In-flight deliveries already on the queue are still attempted; new events stop firing immediately.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with DELETE /v1/webhooks/{id} (the `DeleteWebhook` operationId).
	DeleteWebhookWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*DeleteWebhookClientResponse, error)

	// GetWebhookWithResponse Get webhook subscription
	//
	// Fetch a single webhook subscription by id. Use the `deliveries` sub-resource to list recent attempts, and `ping` to send a test event.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/webhooks/{id} (the `GetWebhook` operationId).
	GetWebhookWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*GetWebhookClientResponse, error)

	// UpdateWebhookWithBodyWithResponse Update webhook subscription
	//
	// Update url, description, events, or status (active|paused). Re-enabling clears `consecutiveFailures` and `disabledAt`.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/webhooks/{id} (the `UpdateWebhook` operationId).
	UpdateWebhookWithBodyWithResponse(ctx context.Context, id openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateWebhookClientResponse, error)

	// UpdateWebhookWithResponse Update webhook subscription
	//
	// Update url, description, events, or status (active|paused). Re-enabling clears `consecutiveFailures` and `disabledAt`.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with PATCH /v1/webhooks/{id} (the `UpdateWebhook` operationId).
	UpdateWebhookWithResponse(ctx context.Context, id openapi_types.UUID, body UpdateWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateWebhookClientResponse, error)

	// ListWebhookDeliveriesWithResponse List webhook deliveries
	//
	// Cursor-paginated history of every delivery attempt for this subscription. Walk pages with `?cursor=<pagination.nextCursor>`; stop when `pagination.hasMore` is `false`. The cursor is opaque base64 — do not parse it. `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — mutually exclusive with `cursor`.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/webhooks/{id}/deliveries (the `ListWebhookDeliveries` operationId).
	ListWebhookDeliveriesWithResponse(ctx context.Context, id openapi_types.UUID, params *ListWebhookDeliveriesParams, reqEditors ...RequestEditorFn) (*ListWebhookDeliveriesClientResponse, error)

	// GetWebhookDeliveryWithResponse Get webhook delivery
	//
	// Full request + response capture for one delivery attempt.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with GET /v1/webhooks/{id}/deliveries/{delivery_id} (the `GetWebhookDelivery` operationId).
	GetWebhookDeliveryWithResponse(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, reqEditors ...RequestEditorFn) (*GetWebhookDeliveryClientResponse, error)

	// ReplayWebhookDeliveryWithBodyWithResponse Replay webhook delivery
	//
	// Re-sends the original payload (same eventId, fresh deliveryId, attempt + 1).
	//
	// A delivery may be replayed at most **3 times per rolling 60 minutes**; the 4th inside that window answers `409 replay_limit_reached` and names the time the next one is allowed. The limit is charged to the original delivery, so replaying the delivery a replay produced draws on the same budget. It is not a lifetime cap — a delivery that has not been replayed for an hour starts fresh.
	//
	// A delivery your endpoint already accepted is not re-sent (it would be a duplicate) and answers `409 delivery_already_succeeded`; send `{"force": true}` to replay it anyway, which still counts against the limit.
	//
	// A delivery about a listing that is inactive now is not re-sent and answers `403 listing_inactive`; activate the listing first.
	//
	// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/webhooks/{id}/deliveries/{delivery_id}/replay (the `ReplayWebhookDelivery` operationId).
	ReplayWebhookDeliveryWithBodyWithResponse(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReplayWebhookDeliveryClientResponse, error)

	// ReplayWebhookDeliveryWithResponse Replay webhook delivery
	//
	// Re-sends the original payload (same eventId, fresh deliveryId, attempt + 1).
	//
	// A delivery may be replayed at most **3 times per rolling 60 minutes**; the 4th inside that window answers `409 replay_limit_reached` and names the time the next one is allowed. The limit is charged to the original delivery, so replaying the delivery a replay produced draws on the same budget. It is not a lifetime cap — a delivery that has not been replayed for an hour starts fresh.
	//
	// A delivery your endpoint already accepted is not re-sent (it would be a duplicate) and answers `409 delivery_already_succeeded`; send `{"force": true}` to replay it anyway, which still counts against the limit.
	//
	// A delivery about a listing that is inactive now is not re-sent and answers `403 listing_inactive`; activate the listing first.
	//
	// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/webhooks/{id}/deliveries/{delivery_id}/replay (the `ReplayWebhookDelivery` operationId).
	ReplayWebhookDeliveryWithResponse(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, body ReplayWebhookDeliveryJSONRequestBody, reqEditors ...RequestEditorFn) (*ReplayWebhookDeliveryClientResponse, error)

	// PingWebhookWithResponse Send ping event
	//
	// Fires a synthetic `repull.ping` at the subscription URL and returns the full delivery cycle inline. Used by dashboards and health-check probes.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/webhooks/{id}/ping (the `PingWebhook` operationId).
	PingWebhookWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*PingWebhookClientResponse, error)

	// RotateWebhookSecretWithResponse Rotate signing secret
	//
	// Mints a fresh signing secret and returns the plaintext ONCE. After this response the secret is masked everywhere — capture and store it now.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/webhooks/{id}/rotate-secret (the `RotateWebhookSecret` operationId).
	RotateWebhookSecretWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*RotateWebhookSecretClientResponse, error)

	// TestFireWebhookWithResponse Send test event of a specific type
	//
	// Delivers a realistic fixture payload of the requested event type to the subscription URL.
	//
	// Returns a wrapper object for the known response body format(s).
	//
	// Corresponds with POST /v1/webhooks/{id}/test/{event_type} (the `TestFireWebhook` operationId).
	TestFireWebhookWithResponse(ctx context.Context, id openapi_types.UUID, eventType WebhookEventType, reqEditors ...RequestEditorFn) (*TestFireWebhookClientResponse, error)
}

type BatchUpdateAvailabilityClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AvailabilityWriteResult
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r BatchUpdateAvailabilityClientResponse) GetJSON200() *AvailabilityWriteResult {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r BatchUpdateAvailabilityClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r BatchUpdateAvailabilityClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r BatchUpdateAvailabilityClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r BatchUpdateAvailabilityClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r BatchUpdateAvailabilityClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r BatchUpdateAvailabilityClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r BatchUpdateAvailabilityClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r BatchUpdateAvailabilityClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAvailabilityClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *PropertyAvailability
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAvailabilityClientResponse) GetJSON200() *PropertyAvailability {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAvailabilityClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAvailabilityClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetAvailabilityClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r GetAvailabilityClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAvailabilityClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAvailabilityClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAvailabilityClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAvailabilityClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AvailabilityWriteResult
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateAvailabilityClientResponse) GetJSON200() *AvailabilityWriteResult {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAvailabilityClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAvailabilityClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAvailabilityClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAvailabilityClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r UpdateAvailabilityClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAvailabilityClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAvailabilityClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAvailabilityClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateBillingCheckoutClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
}

// GetBody returns the raw response body bytes
func (r CreateBillingCheckoutClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateBillingCheckoutClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateBillingCheckoutClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateBillingCheckoutClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbAlterationsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data []AirbnbAlteration `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *AirbnbAccountNotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbAlterationsClientResponse) GetJSON200() *struct {
	Data []AirbnbAlteration `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListAirbnbAlterationsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAirbnbAlterationsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListAirbnbAlterationsClientResponse) GetJSON404() *AirbnbAccountNotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListAirbnbAlterationsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListAirbnbAlterationsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbAlterationsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbAlterationsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbAlterationsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateAirbnbAlterationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *AirbnbAlteration
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateAirbnbAlterationClientResponse) GetJSON201() *AirbnbAlteration {
	return r.JSON201
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateAirbnbAlterationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateAirbnbAlterationClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CreateAirbnbAlterationClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r CreateAirbnbAlterationClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r CreateAirbnbAlterationClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CreateAirbnbAlterationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r CreateAirbnbAlterationClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r CreateAirbnbAlterationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateAirbnbAlterationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateAirbnbAlterationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateAirbnbAlterationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbAlterationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Data An Airbnb reservation alteration request (date change, guest-count change, price change, or a move to another listing), mirrored locally in `reservation_alterations`. Fields prefixed `original*` describe the reservation as it stands today; `new*` fields describe the proposed change. Compare them to render a diff and decide whether to accept (`POST .../{id}/accept`) or decline (`POST .../{id}/decline`) — or, for one you proposed yourself, to withdraw it (`POST .../{id}/cancel`).
		Data AirbnbAlteration `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbAlterationClientResponse) GetJSON200() *struct {
	// Data An Airbnb reservation alteration request (date change, guest-count change, price change, or a move to another listing), mirrored locally in `reservation_alterations`. Fields prefixed `original*` describe the reservation as it stands today; `new*` fields describe the proposed change. Compare them to render a diff and decide whether to accept (`POST .../{id}/accept`) or decline (`POST .../{id}/decline`) — or, for one you proposed yourself, to withdraw it (`POST .../{id}/cancel`).
	Data AirbnbAlteration `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAirbnbAlterationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbAlterationClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAirbnbAlterationClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetAirbnbAlterationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetAirbnbAlterationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbAlterationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbAlterationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbAlterationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type AcceptAirbnbAlterationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r AcceptAirbnbAlterationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r AcceptAirbnbAlterationClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r AcceptAirbnbAlterationClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r AcceptAirbnbAlterationClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r AcceptAirbnbAlterationClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r AcceptAirbnbAlterationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r AcceptAirbnbAlterationClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r AcceptAirbnbAlterationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r AcceptAirbnbAlterationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r AcceptAirbnbAlterationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r AcceptAirbnbAlterationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CancelAirbnbAlterationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CancelAirbnbAlterationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CancelAirbnbAlterationClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CancelAirbnbAlterationClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r CancelAirbnbAlterationClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r CancelAirbnbAlterationClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CancelAirbnbAlterationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r CancelAirbnbAlterationClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r CancelAirbnbAlterationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CancelAirbnbAlterationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CancelAirbnbAlterationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CancelAirbnbAlterationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeclineAirbnbAlterationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeclineAirbnbAlterationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r DeclineAirbnbAlterationClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeclineAirbnbAlterationClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r DeclineAirbnbAlterationClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r DeclineAirbnbAlterationClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r DeclineAirbnbAlterationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r DeclineAirbnbAlterationClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r DeclineAirbnbAlterationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeclineAirbnbAlterationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeclineAirbnbAlterationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeclineAirbnbAlterationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbConnectionClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbConnectionResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbConnectionClientResponse) GetJSON200() *AirbnbConnectionResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAirbnbConnectionClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetAirbnbConnectionClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetAirbnbConnectionClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbConnectionClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbConnectionClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbConnectionClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbListingsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbListingListResponse
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *AirbnbAccountNotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbListingsClientResponse) GetJSON200() *AirbnbListingListResponse {
	return r.JSON200
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListAirbnbListingsClientResponse) GetJSON404() *AirbnbAccountNotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListAirbnbListingsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r ListAirbnbListingsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbListingsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbListingsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbListingsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type MapAirbnbListingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *MapAirbnbListingResponse
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r MapAirbnbListingClientResponse) GetJSON200() *MapAirbnbListingResponse {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r MapAirbnbListingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r MapAirbnbListingClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r MapAirbnbListingClientResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r MapAirbnbListingClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r MapAirbnbListingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r MapAirbnbListingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r MapAirbnbListingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r MapAirbnbListingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbListingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbListing
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbListingClientResponse) GetJSON200() *AirbnbListing {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbListingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r GetAirbnbListingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbListingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbListingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbListingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type AirbnbListingActionClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbListingAction200JSONResponseBody
	// JSON402 the response for an HTTP 402 `application/json` response
	JSON402 *PublishBillingRefused
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r AirbnbListingActionClientResponse) GetJSON200() *AirbnbListingAction200JSONResponseBody {
	return r.JSON200
}

// GetJSON402 returns the response for an HTTP 402 `application/json` response
func (r AirbnbListingActionClientResponse) GetJSON402() *PublishBillingRefused {
	return r.JSON402
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r AirbnbListingActionClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r AirbnbListingActionClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r AirbnbListingActionClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r AirbnbListingActionClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r AirbnbListingActionClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r AirbnbListingActionClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r AirbnbListingActionClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r AirbnbListingActionClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r AirbnbListingActionClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbListingAmenitiesClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data struct {
			AccessibilityAmenities *[]AirbnbAmenity `json:"accessibility_amenities,omitempty"`
			Amenities              *[]AirbnbAmenity `json:"amenities,omitempty"`
		} `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbListingAmenitiesClientResponse) GetJSON200() *struct {
	Data struct {
		AccessibilityAmenities *[]AirbnbAmenity `json:"accessibility_amenities,omitempty"`
		Amenities              *[]AirbnbAmenity `json:"amenities,omitempty"`
	} `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListAirbnbListingAmenitiesClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAirbnbListingAmenitiesClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListAirbnbListingAmenitiesClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListAirbnbListingAmenitiesClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListAirbnbListingAmenitiesClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListAirbnbListingAmenitiesClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbListingAmenitiesClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbListingAmenitiesClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbListingAmenitiesClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbListingAmenitiesClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data struct {
			// AccessibilityAmenities How many accessibility amenities were written.
			AccessibilityAmenities *int `json:"accessibilityAmenities,omitempty"`

			// Amenities How many regular amenities were written.
			Amenities *int `json:"amenities,omitempty"`
		} `json:"data"`

		// Stored Whether our own copy was brought in line with the change.
		Stored bool `json:"stored"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateAirbnbListingAmenitiesClientResponse) GetJSON200() *struct {
	Data struct {
		// AccessibilityAmenities How many accessibility amenities were written.
		AccessibilityAmenities *int `json:"accessibilityAmenities,omitempty"`

		// Amenities How many regular amenities were written.
		Amenities *int `json:"amenities,omitempty"`
	} `json:"data"`

	// Stored Whether our own copy was brought in line with the change.
	Stored bool `json:"stored"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAirbnbListingAmenitiesClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbListingAmenitiesClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbListingAmenitiesClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbListingAmenitiesClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAirbnbListingAmenitiesClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateAirbnbListingAmenitiesClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbListingAmenitiesClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbListingAmenitiesClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbListingAmenitiesClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbListingAmenitiesClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbListingAvailabilityClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbListingAvailabilityClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r GetAirbnbListingAvailabilityClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbListingAvailabilityClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbListingAvailabilityClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbListingAvailabilityClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbListingAvailabilityClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbListingAvailabilityClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbListingAvailabilityClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbListingAvailabilityClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAirbnbListingAvailabilityClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdateAirbnbListingAvailabilityClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbListingAvailabilityClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbListingAvailabilityClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbListingAvailabilityClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbListingAvailabilityClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbBookingSettingsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data struct {
			// AdvanceNotice How much notice a booking needs.
			AdvanceNotice *struct {
				// AllowRequestToBook Whether a guest may still REQUEST to book inside the notice window.
				AllowRequestToBook *bool `json:"allowRequestToBook,omitempty"`

				// Hours Whole hours of notice. `0` allows same-day bookings.
				Hours *int `json:"hours,omitempty"`

				// SameDayBookingsAllowed Derived: `hours === 0`.
				SameDayBookingsAllowed *bool `json:"sameDayBookingsAllowed,omitempty"`
			} `json:"advanceNotice,omitempty"`

			// BookingMode Derived from `instantBook`. `request_to_book` means every booking needs the host to approve it.
			BookingMode *GetAirbnbBookingSettings200JSONResponseBodyDataBookingMode `json:"bookingMode,omitempty"`

			// BookingWindow How far ahead guests may book.
			BookingWindow *struct {
				// Days `null` when unlimited.
				Days      *int  `json:"days,omitempty"`
				Unlimited *bool `json:"unlimited,omitempty"`
			} `json:"bookingWindow,omitempty"`
			Cancellation *struct {
				// LongStayPolicy Airbnb's long-term-stay policy id, for stays of 28+ nights. Opaque.
				LongStayPolicy *string `json:"longStayPolicy,omitempty"`
				NonRefundable  *struct {
					// DiscountPercent Whole-percent discount a guest gets for giving up refundability.
					DiscountPercent *int  `json:"discountPercent,omitempty"`
					Enabled         *bool `json:"enabled,omitempty"`

					// PriceFactor Airbnb's own representation: `1 - discountPercent/100`.
					PriceFactor *float32 `json:"priceFactor,omitempty"`
				} `json:"nonRefundable,omitempty"`

				// ShortStayPolicy Policy for stays under 28 nights.
				ShortStayPolicy *GetAirbnbBookingSettings200JSONResponseBodyDataCancellationShortStayPolicy `json:"shortStayPolicy,omitempty"`
			} `json:"cancellation,omitempty"`

			// CheckIn Check-in window as hours of the day, or `FLEXIBLE`.
			CheckIn *struct {
				End   *GetAirbnbBookingSettings200JSONResponseBody_Data_CheckIn_End   `json:"end,omitempty"`
				Start *GetAirbnbBookingSettings200JSONResponseBody_Data_CheckIn_Start `json:"start,omitempty"`
			} `json:"checkIn,omitempty"`
			CheckOut *struct {
				Time *int `json:"time,omitempty"`
			} `json:"checkOut,omitempty"`

			// InstantBook Instant Book state. All three fields describe ONE Airbnb value, `instant_booking_allowed_category`.
			InstantBook *struct {
				// Enabled `false` when the category is `off` — the listing is request-to-book.
				Enabled *bool `json:"enabled,omitempty"`

				// GuestCategory Which guests may Instant Book.
				GuestCategory *GetAirbnbBookingSettings200JSONResponseBodyDataInstantBookGuestCategory `json:"guestCategory,omitempty"`

				// RequiresGoodTrackRecord `true` for `experienced_guests_only` / `recommended_guests_only` — Airbnb calls this a good track record.
				RequiresGoodTrackRecord *bool `json:"requiresGoodTrackRecord,omitempty"`
			} `json:"instantBook,omitempty"`

			// PreparationTime Airbnb's `turnover_days` — nights blocked between two stays.
			PreparationTime *struct {
				Nights *int `json:"nights,omitempty"`
			} `json:"preparationTime,omitempty"`
		} `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbBookingSettingsClientResponse) GetJSON200() *struct {
	Data struct {
		// AdvanceNotice How much notice a booking needs.
		AdvanceNotice *struct {
			// AllowRequestToBook Whether a guest may still REQUEST to book inside the notice window.
			AllowRequestToBook *bool `json:"allowRequestToBook,omitempty"`

			// Hours Whole hours of notice. `0` allows same-day bookings.
			Hours *int `json:"hours,omitempty"`

			// SameDayBookingsAllowed Derived: `hours === 0`.
			SameDayBookingsAllowed *bool `json:"sameDayBookingsAllowed,omitempty"`
		} `json:"advanceNotice,omitempty"`

		// BookingMode Derived from `instantBook`. `request_to_book` means every booking needs the host to approve it.
		BookingMode *GetAirbnbBookingSettings200JSONResponseBodyDataBookingMode `json:"bookingMode,omitempty"`

		// BookingWindow How far ahead guests may book.
		BookingWindow *struct {
			// Days `null` when unlimited.
			Days      *int  `json:"days,omitempty"`
			Unlimited *bool `json:"unlimited,omitempty"`
		} `json:"bookingWindow,omitempty"`
		Cancellation *struct {
			// LongStayPolicy Airbnb's long-term-stay policy id, for stays of 28+ nights. Opaque.
			LongStayPolicy *string `json:"longStayPolicy,omitempty"`
			NonRefundable  *struct {
				// DiscountPercent Whole-percent discount a guest gets for giving up refundability.
				DiscountPercent *int  `json:"discountPercent,omitempty"`
				Enabled         *bool `json:"enabled,omitempty"`

				// PriceFactor Airbnb's own representation: `1 - discountPercent/100`.
				PriceFactor *float32 `json:"priceFactor,omitempty"`
			} `json:"nonRefundable,omitempty"`

			// ShortStayPolicy Policy for stays under 28 nights.
			ShortStayPolicy *GetAirbnbBookingSettings200JSONResponseBodyDataCancellationShortStayPolicy `json:"shortStayPolicy,omitempty"`
		} `json:"cancellation,omitempty"`

		// CheckIn Check-in window as hours of the day, or `FLEXIBLE`.
		CheckIn *struct {
			End   *GetAirbnbBookingSettings200JSONResponseBody_Data_CheckIn_End   `json:"end,omitempty"`
			Start *GetAirbnbBookingSettings200JSONResponseBody_Data_CheckIn_Start `json:"start,omitempty"`
		} `json:"checkIn,omitempty"`
		CheckOut *struct {
			Time *int `json:"time,omitempty"`
		} `json:"checkOut,omitempty"`

		// InstantBook Instant Book state. All three fields describe ONE Airbnb value, `instant_booking_allowed_category`.
		InstantBook *struct {
			// Enabled `false` when the category is `off` — the listing is request-to-book.
			Enabled *bool `json:"enabled,omitempty"`

			// GuestCategory Which guests may Instant Book.
			GuestCategory *GetAirbnbBookingSettings200JSONResponseBodyDataInstantBookGuestCategory `json:"guestCategory,omitempty"`

			// RequiresGoodTrackRecord `true` for `experienced_guests_only` / `recommended_guests_only` — Airbnb calls this a good track record.
			RequiresGoodTrackRecord *bool `json:"requiresGoodTrackRecord,omitempty"`
		} `json:"instantBook,omitempty"`

		// PreparationTime Airbnb's `turnover_days` — nights blocked between two stays.
		PreparationTime *struct {
			Nights *int `json:"nights,omitempty"`
		} `json:"preparationTime,omitempty"`
	} `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAirbnbBookingSettingsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbBookingSettingsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAirbnbBookingSettingsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetAirbnbBookingSettingsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetAirbnbBookingSettingsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetAirbnbBookingSettingsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbBookingSettingsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbBookingSettingsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbBookingSettingsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbBookingSettingsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data struct {
			// Applied Which upstream groups this request wrote.
			Applied  []UpdateAirbnbBookingSettings200JSONResponseBodyDataApplied `json:"applied"`
			Settings struct {
				// AdvanceNotice How much notice a booking needs.
				AdvanceNotice *struct {
					// AllowRequestToBook Whether a guest may still REQUEST to book inside the notice window.
					AllowRequestToBook *bool `json:"allowRequestToBook,omitempty"`

					// Hours Whole hours of notice. `0` allows same-day bookings.
					Hours *int `json:"hours,omitempty"`

					// SameDayBookingsAllowed Derived: `hours === 0`.
					SameDayBookingsAllowed *bool `json:"sameDayBookingsAllowed,omitempty"`
				} `json:"advanceNotice,omitempty"`

				// BookingMode Derived from `instantBook`. `request_to_book` means every booking needs the host to approve it.
				BookingMode *UpdateAirbnbBookingSettings200JSONResponseBodyDataSettingsBookingMode `json:"bookingMode,omitempty"`

				// BookingWindow How far ahead guests may book.
				BookingWindow *struct {
					// Days `null` when unlimited.
					Days      *int  `json:"days,omitempty"`
					Unlimited *bool `json:"unlimited,omitempty"`
				} `json:"bookingWindow,omitempty"`
				Cancellation *struct {
					// LongStayPolicy Airbnb's long-term-stay policy id, for stays of 28+ nights. Opaque.
					LongStayPolicy *string `json:"longStayPolicy,omitempty"`
					NonRefundable  *struct {
						// DiscountPercent Whole-percent discount a guest gets for giving up refundability.
						DiscountPercent *int  `json:"discountPercent,omitempty"`
						Enabled         *bool `json:"enabled,omitempty"`

						// PriceFactor Airbnb's own representation: `1 - discountPercent/100`.
						PriceFactor *float32 `json:"priceFactor,omitempty"`
					} `json:"nonRefundable,omitempty"`

					// ShortStayPolicy Policy for stays under 28 nights.
					ShortStayPolicy *UpdateAirbnbBookingSettings200JSONResponseBodyDataSettingsCancellationShortStayPolicy `json:"shortStayPolicy,omitempty"`
				} `json:"cancellation,omitempty"`

				// CheckIn Check-in window as hours of the day, or `FLEXIBLE`.
				CheckIn *struct {
					End   *UpdateAirbnbBookingSettings200JSONResponseBody_Data_Settings_CheckIn_End   `json:"end,omitempty"`
					Start *UpdateAirbnbBookingSettings200JSONResponseBody_Data_Settings_CheckIn_Start `json:"start,omitempty"`
				} `json:"checkIn,omitempty"`
				CheckOut *struct {
					Time *int `json:"time,omitempty"`
				} `json:"checkOut,omitempty"`

				// InstantBook Instant Book state. All three fields describe ONE Airbnb value, `instant_booking_allowed_category`.
				InstantBook *struct {
					// Enabled `false` when the category is `off` — the listing is request-to-book.
					Enabled *bool `json:"enabled,omitempty"`

					// GuestCategory Which guests may Instant Book.
					GuestCategory *UpdateAirbnbBookingSettings200JSONResponseBodyDataSettingsInstantBookGuestCategory `json:"guestCategory,omitempty"`

					// RequiresGoodTrackRecord `true` for `experienced_guests_only` / `recommended_guests_only` — Airbnb calls this a good track record.
					RequiresGoodTrackRecord *bool `json:"requiresGoodTrackRecord,omitempty"`
				} `json:"instantBook,omitempty"`

				// PreparationTime Airbnb's `turnover_days` — nights blocked between two stays.
				PreparationTime *struct {
					Nights *int `json:"nights,omitempty"`
				} `json:"preparationTime,omitempty"`
			} `json:"settings"`
		} `json:"data"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateAirbnbBookingSettingsClientResponse) GetJSON200() *struct {
	Data struct {
		// Applied Which upstream groups this request wrote.
		Applied  []UpdateAirbnbBookingSettings200JSONResponseBodyDataApplied `json:"applied"`
		Settings struct {
			// AdvanceNotice How much notice a booking needs.
			AdvanceNotice *struct {
				// AllowRequestToBook Whether a guest may still REQUEST to book inside the notice window.
				AllowRequestToBook *bool `json:"allowRequestToBook,omitempty"`

				// Hours Whole hours of notice. `0` allows same-day bookings.
				Hours *int `json:"hours,omitempty"`

				// SameDayBookingsAllowed Derived: `hours === 0`.
				SameDayBookingsAllowed *bool `json:"sameDayBookingsAllowed,omitempty"`
			} `json:"advanceNotice,omitempty"`

			// BookingMode Derived from `instantBook`. `request_to_book` means every booking needs the host to approve it.
			BookingMode *UpdateAirbnbBookingSettings200JSONResponseBodyDataSettingsBookingMode `json:"bookingMode,omitempty"`

			// BookingWindow How far ahead guests may book.
			BookingWindow *struct {
				// Days `null` when unlimited.
				Days      *int  `json:"days,omitempty"`
				Unlimited *bool `json:"unlimited,omitempty"`
			} `json:"bookingWindow,omitempty"`
			Cancellation *struct {
				// LongStayPolicy Airbnb's long-term-stay policy id, for stays of 28+ nights. Opaque.
				LongStayPolicy *string `json:"longStayPolicy,omitempty"`
				NonRefundable  *struct {
					// DiscountPercent Whole-percent discount a guest gets for giving up refundability.
					DiscountPercent *int  `json:"discountPercent,omitempty"`
					Enabled         *bool `json:"enabled,omitempty"`

					// PriceFactor Airbnb's own representation: `1 - discountPercent/100`.
					PriceFactor *float32 `json:"priceFactor,omitempty"`
				} `json:"nonRefundable,omitempty"`

				// ShortStayPolicy Policy for stays under 28 nights.
				ShortStayPolicy *UpdateAirbnbBookingSettings200JSONResponseBodyDataSettingsCancellationShortStayPolicy `json:"shortStayPolicy,omitempty"`
			} `json:"cancellation,omitempty"`

			// CheckIn Check-in window as hours of the day, or `FLEXIBLE`.
			CheckIn *struct {
				End   *UpdateAirbnbBookingSettings200JSONResponseBody_Data_Settings_CheckIn_End   `json:"end,omitempty"`
				Start *UpdateAirbnbBookingSettings200JSONResponseBody_Data_Settings_CheckIn_Start `json:"start,omitempty"`
			} `json:"checkIn,omitempty"`
			CheckOut *struct {
				Time *int `json:"time,omitempty"`
			} `json:"checkOut,omitempty"`

			// InstantBook Instant Book state. All three fields describe ONE Airbnb value, `instant_booking_allowed_category`.
			InstantBook *struct {
				// Enabled `false` when the category is `off` — the listing is request-to-book.
				Enabled *bool `json:"enabled,omitempty"`

				// GuestCategory Which guests may Instant Book.
				GuestCategory *UpdateAirbnbBookingSettings200JSONResponseBodyDataSettingsInstantBookGuestCategory `json:"guestCategory,omitempty"`

				// RequiresGoodTrackRecord `true` for `experienced_guests_only` / `recommended_guests_only` — Airbnb calls this a good track record.
				RequiresGoodTrackRecord *bool `json:"requiresGoodTrackRecord,omitempty"`
			} `json:"instantBook,omitempty"`

			// PreparationTime Airbnb's `turnover_days` — nights blocked between two stays.
			PreparationTime *struct {
				Nights *int `json:"nights,omitempty"`
			} `json:"preparationTime,omitempty"`
		} `json:"settings"`
	} `json:"data"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAirbnbBookingSettingsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbBookingSettingsClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbBookingSettingsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbBookingSettingsClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAirbnbBookingSettingsClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdateAirbnbBookingSettingsClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbBookingSettingsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbBookingSettingsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbBookingSettingsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbBookingSettingsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbCheckinGuideClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data []map[string]interface{} `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbCheckinGuideClientResponse) GetJSON200() *struct {
	Data []map[string]interface{} `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAirbnbCheckinGuideClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbCheckinGuideClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAirbnbCheckinGuideClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetAirbnbCheckinGuideClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetAirbnbCheckinGuideClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetAirbnbCheckinGuideClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbCheckinGuideClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbCheckinGuideClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbCheckinGuideClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbCheckinGuideClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAirbnbCheckinGuideClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbCheckinGuideClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbCheckinGuideClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbCheckinGuideClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAirbnbCheckinGuideClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateAirbnbCheckinGuideClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdateAirbnbCheckinGuideClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbCheckinGuideClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbCheckinGuideClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbCheckinGuideClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbCheckinGuideClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbCheckoutGuideClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data []map[string]interface{} `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbCheckoutGuideClientResponse) GetJSON200() *struct {
	Data []map[string]interface{} `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAirbnbCheckoutGuideClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbCheckoutGuideClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAirbnbCheckoutGuideClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetAirbnbCheckoutGuideClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetAirbnbCheckoutGuideClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetAirbnbCheckoutGuideClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbCheckoutGuideClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbCheckoutGuideClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbCheckoutGuideClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbListingDescriptionsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data []map[string]interface{} `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbListingDescriptionsClientResponse) GetJSON200() *struct {
	Data []map[string]interface{} `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListAirbnbListingDescriptionsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAirbnbListingDescriptionsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListAirbnbListingDescriptionsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListAirbnbListingDescriptionsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListAirbnbListingDescriptionsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListAirbnbListingDescriptionsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbListingDescriptionsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbListingDescriptionsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbListingDescriptionsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbListingDescriptionClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbContentWriteResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateAirbnbListingDescriptionClientResponse) GetJSON200() *AirbnbContentWriteResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAirbnbListingDescriptionClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbListingDescriptionClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbListingDescriptionClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbListingDescriptionClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAirbnbListingDescriptionClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateAirbnbListingDescriptionClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdateAirbnbListingDescriptionClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbListingDescriptionClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbListingDescriptionClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbListingDescriptionClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbListingDescriptionClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbListingDetailsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data []AirbnbListingDetailsResponse `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbListingDetailsClientResponse) GetJSON200() *struct {
	Data []AirbnbListingDetailsResponse `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAirbnbListingDetailsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbListingDetailsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAirbnbListingDetailsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetAirbnbListingDetailsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetAirbnbListingDetailsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetAirbnbListingDetailsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbListingDetailsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbListingDetailsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbListingDetailsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbListingDetailsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbContentWriteResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateAirbnbListingDetailsClientResponse) GetJSON200() *AirbnbContentWriteResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAirbnbListingDetailsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbListingDetailsClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbListingDetailsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbListingDetailsClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAirbnbListingDetailsClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateAirbnbListingDetailsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdateAirbnbListingDetailsClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbListingDetailsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbListingDetailsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbListingDetailsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbListingDetailsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbListingPermitsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data AirbnbPermitsResponse `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbListingPermitsClientResponse) GetJSON200() *struct {
	Data AirbnbPermitsResponse `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListAirbnbListingPermitsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAirbnbListingPermitsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListAirbnbListingPermitsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListAirbnbListingPermitsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListAirbnbListingPermitsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r ListAirbnbListingPermitsClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r ListAirbnbListingPermitsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbListingPermitsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbListingPermitsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbListingPermitsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbListingPermitsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AirbnbListingId *string `json:"airbnbListingId,omitempty"`
		ListingId       *string `json:"listingId,omitempty"`

		// Permits The permit flows as Airbnb reports them after the write, including each flow's new `status`.
		Permits *[]map[string]interface{} `json:"permits,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateAirbnbListingPermitsClientResponse) GetJSON200() *struct {
	AirbnbListingId *string `json:"airbnbListingId,omitempty"`
	ListingId       *string `json:"listingId,omitempty"`

	// Permits The permit flows as Airbnb reports them after the write, including each flow's new `status`.
	Permits *[]map[string]interface{} `json:"permits,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAirbnbListingPermitsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbListingPermitsClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbListingPermitsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbListingPermitsClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAirbnbListingPermitsClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateAirbnbListingPermitsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdateAirbnbListingPermitsClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbListingPermitsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbListingPermitsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbListingPermitsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbListingPermitsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteAirbnbListingPhotoClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Deleted Example: true
		Deleted *bool `json:"deleted,omitempty"`

		// Stored Whether our own copy dropped the photo too.
		Stored *bool `json:"stored,omitempty"`
	}
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DeleteAirbnbListingPhotoClientResponse) GetJSON200() *struct {
	// Deleted Example: true
	Deleted *bool `json:"deleted,omitempty"`

	// Stored Whether our own copy dropped the photo too.
	Stored *bool `json:"stored,omitempty"`
} {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r DeleteAirbnbListingPhotoClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeleteAirbnbListingPhotoClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r DeleteAirbnbListingPhotoClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r DeleteAirbnbListingPhotoClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r DeleteAirbnbListingPhotoClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteAirbnbListingPhotoClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteAirbnbListingPhotoClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteAirbnbListingPhotoClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbListingPhotosClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAirbnbListingPhotosClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r ListAirbnbListingPhotosClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbListingPhotosClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbListingPhotosClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbListingPhotosClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbListingPhotoClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Data The photo as Airbnb returned it.
		Data map[string]interface{} `json:"data"`

		// Stored Whether our own copy was brought in line with the change.
		Stored bool `json:"stored"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateAirbnbListingPhotoClientResponse) GetJSON200() *struct {
	// Data The photo as Airbnb returned it.
	Data map[string]interface{} `json:"data"`

	// Stored Whether our own copy was brought in line with the change.
	Stored bool `json:"stored"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAirbnbListingPhotoClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbListingPhotoClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbListingPhotoClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbListingPhotoClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateAirbnbListingPhotoClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbListingPhotoClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbListingPhotoClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbListingPhotoClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbListingPhotoClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UploadAirbnbListingPhotosClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UploadAirbnbListingPhotosClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UploadAirbnbListingPhotosClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UploadAirbnbListingPhotosClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UploadAirbnbListingPhotosClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UploadAirbnbListingPhotosClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r UploadAirbnbListingPhotosClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UploadAirbnbListingPhotosClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UploadAirbnbListingPhotosClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UploadAirbnbListingPhotosClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SetAirbnbListingCoverPhotoClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data struct {
			// Applied The photos this call actually moved. Empty when the photo was already the cover.
			Applied      *[]AirbnbPhotoPosition `json:"applied,omitempty"`
			CoverPhotoId *string                `json:"coverPhotoId,omitempty"`

			// Order Present only when the whole tour had to be renumbered.
			Order *[]AirbnbPhotoPosition `json:"order,omitempty"`
		} `json:"data"`

		// Stored Whether our own copy (tour order and listing thumbnail) was brought in line.
		Stored bool `json:"stored"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SetAirbnbListingCoverPhotoClientResponse) GetJSON200() *struct {
	Data struct {
		// Applied The photos this call actually moved. Empty when the photo was already the cover.
		Applied      *[]AirbnbPhotoPosition `json:"applied,omitempty"`
		CoverPhotoId *string                `json:"coverPhotoId,omitempty"`

		// Order Present only when the whole tour had to be renumbered.
		Order *[]AirbnbPhotoPosition `json:"order,omitempty"`
	} `json:"data"`

	// Stored Whether our own copy (tour order and listing thumbnail) was brought in line.
	Stored bool `json:"stored"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SetAirbnbListingCoverPhotoClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r SetAirbnbListingCoverPhotoClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SetAirbnbListingCoverPhotoClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SetAirbnbListingCoverPhotoClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r SetAirbnbListingCoverPhotoClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r SetAirbnbListingCoverPhotoClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r SetAirbnbListingCoverPhotoClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SetAirbnbListingCoverPhotoClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SetAirbnbListingCoverPhotoClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SetAirbnbListingCoverPhotoClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ReorderAirbnbListingPhotosClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data struct {
			// Applied The photos this call actually moved.
			Applied *[]AirbnbPhotoPosition `json:"applied,omitempty"`

			// Order The resulting tour order, including photos you did not name.
			Order *[]AirbnbPhotoPosition `json:"order,omitempty"`

			// Unchanged Photos that were already in the right position.
			Unchanged *int `json:"unchanged,omitempty"`
		} `json:"data"`

		// Stored Whether our own copy was brought in line with what landed.
		Stored bool `json:"stored"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ReorderAirbnbListingPhotosClientResponse) GetJSON200() *struct {
	Data struct {
		// Applied The photos this call actually moved.
		Applied *[]AirbnbPhotoPosition `json:"applied,omitempty"`

		// Order The resulting tour order, including photos you did not name.
		Order *[]AirbnbPhotoPosition `json:"order,omitempty"`

		// Unchanged Photos that were already in the right position.
		Unchanged *int `json:"unchanged,omitempty"`
	} `json:"data"`

	// Stored Whether our own copy was brought in line with what landed.
	Stored bool `json:"stored"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ReorderAirbnbListingPhotosClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ReorderAirbnbListingPhotosClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ReorderAirbnbListingPhotosClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ReorderAirbnbListingPhotosClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ReorderAirbnbListingPhotosClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ReorderAirbnbListingPhotosClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ReorderAirbnbListingPhotosClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ReorderAirbnbListingPhotosClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ReorderAirbnbListingPhotosClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ReorderAirbnbListingPhotosClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbListingPricingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbListingPricingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r GetAirbnbListingPricingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbListingPricingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbListingPricingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbListingPricingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbListingPricingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbListingPricingClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbListingPricingClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbListingPricingClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAirbnbListingPricingClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdateAirbnbListingPricingClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbListingPricingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbListingPricingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbListingPricingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbListingPricingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbListingQualityClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Data Shape depends on `type`: an object `{ standards, issues }` for `all`/`standards`, or an array for `issues`/`stats`.
		Data interface{} `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbListingQualityClientResponse) GetJSON200() *struct {
	// Data Shape depends on `type`: an object `{ standards, issues }` for `all`/`standards`, or an array for `issues`/`stats`.
	Data interface{} `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAirbnbListingQualityClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbListingQualityClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAirbnbListingQualityClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetAirbnbListingQualityClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetAirbnbListingQualityClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetAirbnbListingQualityClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbListingQualityClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbListingQualityClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbListingQualityClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteAirbnbListingRoomClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Deleted Example: true
		Deleted *bool `json:"deleted,omitempty"`

		// Stored Whether our own copy dropped the room too.
		Stored *bool `json:"stored,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DeleteAirbnbListingRoomClientResponse) GetJSON200() *struct {
	// Deleted Example: true
	Deleted *bool `json:"deleted,omitempty"`

	// Stored Whether our own copy dropped the room too.
	Stored *bool `json:"stored,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteAirbnbListingRoomClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r DeleteAirbnbListingRoomClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeleteAirbnbListingRoomClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r DeleteAirbnbListingRoomClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r DeleteAirbnbListingRoomClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r DeleteAirbnbListingRoomClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteAirbnbListingRoomClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteAirbnbListingRoomClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteAirbnbListingRoomClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbListingRoomsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data []map[string]interface{} `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbListingRoomsClientResponse) GetJSON200() *struct {
	Data []map[string]interface{} `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListAirbnbListingRoomsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAirbnbListingRoomsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListAirbnbListingRoomsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListAirbnbListingRoomsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListAirbnbListingRoomsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListAirbnbListingRoomsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbListingRoomsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbListingRoomsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbListingRoomsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateAirbnbListingRoomClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateAirbnbListingRoomClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateAirbnbListingRoomClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CreateAirbnbListingRoomClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CreateAirbnbListingRoomClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r CreateAirbnbListingRoomClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateAirbnbListingRoomClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateAirbnbListingRoomClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateAirbnbListingRoomClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbListingRoomClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Data The room as Airbnb returned it.
		Data map[string]interface{} `json:"data"`

		// Stored Whether our own copy was brought in line with the change.
		Stored bool `json:"stored"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateAirbnbListingRoomClientResponse) GetJSON200() *struct {
	// Data The room as Airbnb returned it.
	Data map[string]interface{} `json:"data"`

	// Stored Whether our own copy was brought in line with the change.
	Stored bool `json:"stored"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAirbnbListingRoomClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbListingRoomClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbListingRoomClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbListingRoomClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAirbnbListingRoomClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateAirbnbListingRoomClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbListingRoomClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbListingRoomClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbListingRoomClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbListingRoomClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbListingSafetyDisclosuresClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data AirbnbSafetyDisclosuresResponse `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbListingSafetyDisclosuresClientResponse) GetJSON200() *struct {
	Data AirbnbSafetyDisclosuresResponse `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListAirbnbListingSafetyDisclosuresClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAirbnbListingSafetyDisclosuresClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListAirbnbListingSafetyDisclosuresClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListAirbnbListingSafetyDisclosuresClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListAirbnbListingSafetyDisclosuresClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListAirbnbListingSafetyDisclosuresClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbListingSafetyDisclosuresClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbListingSafetyDisclosuresClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbListingSafetyDisclosuresClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbListingSafetyDisclosuresClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AirbnbListingId *string `json:"airbnbListingId,omitempty"`

		// Disclosures The set as Airbnb reports it after the write.
		Disclosures *[]AirbnbSafetyDisclosure `json:"disclosures,omitempty"`
		ListingId   *string                   `json:"listingId,omitempty"`

		// Result Airbnb's raw booking-settings response.
		Result *map[string]interface{} `json:"result,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *AirbnbWriteForbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) GetJSON200() *struct {
	AirbnbListingId *string `json:"airbnbListingId,omitempty"`

	// Disclosures The set as Airbnb reports it after the write.
	Disclosures *[]AirbnbSafetyDisclosure `json:"disclosures,omitempty"`
	ListingId   *string                   `json:"listingId,omitempty"`

	// Result Airbnb's raw booking-settings response.
	Result *map[string]interface{} `json:"result,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) GetJSON403() *AirbnbWriteForbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbListingSafetyDisclosuresClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbListingSettingsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Data Shape depends on `type`: `{ hosts, locales }` for `all`, or an array for `hosts`/`permits`/`locales`.
		Data interface{} `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbListingSettingsClientResponse) GetJSON200() *struct {
	// Data Shape depends on `type`: `{ hosts, locales }` for `all`, or an array for `hosts`/`permits`/`locales`.
	Data interface{} `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAirbnbListingSettingsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbListingSettingsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAirbnbListingSettingsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetAirbnbListingSettingsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetAirbnbListingSettingsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetAirbnbListingSettingsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbListingSettingsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbListingSettingsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbListingSettingsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbThreadsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbThreadListResponse
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *AirbnbAccountNotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbThreadsClientResponse) GetJSON200() *AirbnbThreadListResponse {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAirbnbThreadsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListAirbnbThreadsClientResponse) GetJSON404() *AirbnbAccountNotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ListAirbnbThreadsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbThreadsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbThreadsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbThreadsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbThreadClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Data An Airbnb message thread.
		Data AirbnbThread `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbThreadClientResponse) GetJSON200() *struct {
	// Data An Airbnb message thread.
	Data AirbnbThread `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAirbnbThreadClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbThreadClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAirbnbThreadClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetAirbnbThreadClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetAirbnbThreadClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbThreadClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbThreadClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbThreadClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbThreadMessagesClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data *[]struct {
			Attachments *[]ConversationMessageAttachment `json:"attachments,omitempty"`

			// Channel Example: airbnb
			Channel   *string    `json:"channel,omitempty"`
			CreatedAt *time.Time `json:"createdAt,omitempty"`

			// ExternalCreatedAt When Airbnb recorded the message.
			ExternalCreatedAt *time.Time `json:"externalCreatedAt,omitempty"`

			// ExternalMessageId Airbnb's message id.
			ExternalMessageId *string `json:"externalMessageId,omitempty"`

			// Id Repull message id.
			Id *string `json:"id,omitempty"`

			// Message Message text. Empty for a file-only message.
			Message       *string `json:"message,omitempty"`
			ReservationId *string `json:"reservationId,omitempty"`

			// SenderType `guest`, `host`, `user`, `system`, …
			SenderType *string `json:"senderType,omitempty"`

			// ThreadId The Airbnb thread id.
			ThreadId          *string    `json:"threadId,omitempty"`
			TranslatedMessage *string    `json:"translatedMessage,omitempty"`
			UpdatedAt         *time.Time `json:"updatedAt,omitempty"`

			// UserId Airbnb user id of the sender.
			UserId *string `json:"userId,omitempty"`
		} `json:"data,omitempty"`

		// DataFreshness When this workspace's Airbnb data was last synced.
		DataFreshness *map[string]interface{} `json:"dataFreshness,omitempty"`

		// Pagination Absent when `?all=true`.
		Pagination *struct {
			HasMore    *bool   `json:"hasMore,omitempty"`
			NextCursor *string `json:"nextCursor,omitempty"`
		} `json:"pagination,omitempty"`
	}
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbThreadMessagesClientResponse) GetJSON200() *struct {
	Data *[]struct {
		Attachments *[]ConversationMessageAttachment `json:"attachments,omitempty"`

		// Channel Example: airbnb
		Channel   *string    `json:"channel,omitempty"`
		CreatedAt *time.Time `json:"createdAt,omitempty"`

		// ExternalCreatedAt When Airbnb recorded the message.
		ExternalCreatedAt *time.Time `json:"externalCreatedAt,omitempty"`

		// ExternalMessageId Airbnb's message id.
		ExternalMessageId *string `json:"externalMessageId,omitempty"`

		// Id Repull message id.
		Id *string `json:"id,omitempty"`

		// Message Message text. Empty for a file-only message.
		Message       *string `json:"message,omitempty"`
		ReservationId *string `json:"reservationId,omitempty"`

		// SenderType `guest`, `host`, `user`, `system`, …
		SenderType *string `json:"senderType,omitempty"`

		// ThreadId The Airbnb thread id.
		ThreadId          *string    `json:"threadId,omitempty"`
		TranslatedMessage *string    `json:"translatedMessage,omitempty"`
		UpdatedAt         *time.Time `json:"updatedAt,omitempty"`

		// UserId Airbnb user id of the sender.
		UserId *string `json:"userId,omitempty"`
	} `json:"data,omitempty"`

	// DataFreshness When this workspace's Airbnb data was last synced.
	DataFreshness *map[string]interface{} `json:"dataFreshness,omitempty"`

	// Pagination Absent when `?all=true`.
	Pagination *struct {
		HasMore    *bool   `json:"hasMore,omitempty"`
		NextCursor *string `json:"nextCursor,omitempty"`
	} `json:"pagination,omitempty"`
} {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAirbnbThreadMessagesClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r ListAirbnbThreadMessagesClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbThreadMessagesClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbThreadMessagesClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbThreadMessagesClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SendAirbnbMessageClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *SendAirbnbMessage201JSONResponseBody
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r SendAirbnbMessageClientResponse) GetJSON201() *SendAirbnbMessage201JSONResponseBody {
	return r.JSON201
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SendAirbnbMessageClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r SendAirbnbMessageClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SendAirbnbMessageClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SendAirbnbMessageClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r SendAirbnbMessageClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r SendAirbnbMessageClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r SendAirbnbMessageClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r SendAirbnbMessageClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SendAirbnbMessageClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SendAirbnbMessageClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SendAirbnbMessageClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateAirbnbMessageClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateAirbnbMessageClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateAirbnbMessageClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateAirbnbMessageClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateAirbnbMessageClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateAirbnbMessageClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateAirbnbMessageClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdateAirbnbMessageClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdateAirbnbMessageClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateAirbnbMessageClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateAirbnbMessageClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateAirbnbMessageClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type WithdrawAirbnbOfferClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		CreatedAt    *time.Time              `json:"createdAt,omitempty"`
		ExpiresAt    *time.Time              `json:"expiresAt,omitempty"`
		GuestDetails *map[string]interface{} `json:"guestDetails,omitempty"`

		// Id Airbnb special-offer id.
		Id *string `json:"id,omitempty"`

		// ListingId Airbnb listing id (special offers only).
		ListingId *string                                          `json:"listingId,omitempty"`
		Nights    *int                                             `json:"nights,omitempty"`
		OfferType *WithdrawAirbnbOffer200JSONResponseBodyOfferType `json:"offerType,omitempty"`
		StartDate *openapi_types.Date                              `json:"startDate,omitempty"`
		Status    *WithdrawAirbnbOffer200JSONResponseBodyStatus    `json:"status,omitempty"`

		// ThreadId Airbnb thread id the offer was sent on.
		ThreadId   *string  `json:"threadId,omitempty"`
		TotalPrice *float32 `json:"totalPrice,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r WithdrawAirbnbOfferClientResponse) GetJSON200() *struct {
	CreatedAt    *time.Time              `json:"createdAt,omitempty"`
	ExpiresAt    *time.Time              `json:"expiresAt,omitempty"`
	GuestDetails *map[string]interface{} `json:"guestDetails,omitempty"`

	// Id Airbnb special-offer id.
	Id *string `json:"id,omitempty"`

	// ListingId Airbnb listing id (special offers only).
	ListingId *string                                          `json:"listingId,omitempty"`
	Nights    *int                                             `json:"nights,omitempty"`
	OfferType *WithdrawAirbnbOffer200JSONResponseBodyOfferType `json:"offerType,omitempty"`
	StartDate *openapi_types.Date                              `json:"startDate,omitempty"`
	Status    *WithdrawAirbnbOffer200JSONResponseBodyStatus    `json:"status,omitempty"`

	// ThreadId Airbnb thread id the offer was sent on.
	ThreadId   *string  `json:"threadId,omitempty"`
	TotalPrice *float32 `json:"totalPrice,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r WithdrawAirbnbOfferClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r WithdrawAirbnbOfferClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r WithdrawAirbnbOfferClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r WithdrawAirbnbOfferClientResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r WithdrawAirbnbOfferClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r WithdrawAirbnbOfferClientResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r WithdrawAirbnbOfferClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r WithdrawAirbnbOfferClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r WithdrawAirbnbOfferClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r WithdrawAirbnbOfferClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r WithdrawAirbnbOfferClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbOfferClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		CreatedAt    *time.Time              `json:"createdAt,omitempty"`
		ExpiresAt    *time.Time              `json:"expiresAt,omitempty"`
		GuestDetails *map[string]interface{} `json:"guestDetails,omitempty"`

		// Id Airbnb special-offer id.
		Id *string `json:"id,omitempty"`

		// ListingId Airbnb listing id (special offers only).
		ListingId *string                                     `json:"listingId,omitempty"`
		Nights    *int                                        `json:"nights,omitempty"`
		OfferType *GetAirbnbOffer200JSONResponseBodyOfferType `json:"offerType,omitempty"`
		StartDate *openapi_types.Date                         `json:"startDate,omitempty"`
		Status    *GetAirbnbOffer200JSONResponseBodyStatus    `json:"status,omitempty"`

		// ThreadId Airbnb thread id the offer was sent on.
		ThreadId   *string  `json:"threadId,omitempty"`
		TotalPrice *float32 `json:"totalPrice,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbOfferClientResponse) GetJSON200() *struct {
	CreatedAt    *time.Time              `json:"createdAt,omitempty"`
	ExpiresAt    *time.Time              `json:"expiresAt,omitempty"`
	GuestDetails *map[string]interface{} `json:"guestDetails,omitempty"`

	// Id Airbnb special-offer id.
	Id *string `json:"id,omitempty"`

	// ListingId Airbnb listing id (special offers only).
	ListingId *string                                     `json:"listingId,omitempty"`
	Nights    *int                                        `json:"nights,omitempty"`
	OfferType *GetAirbnbOffer200JSONResponseBodyOfferType `json:"offerType,omitempty"`
	StartDate *openapi_types.Date                         `json:"startDate,omitempty"`
	Status    *GetAirbnbOffer200JSONResponseBodyStatus    `json:"status,omitempty"`

	// ThreadId Airbnb thread id the offer was sent on.
	ThreadId   *string  `json:"threadId,omitempty"`
	TotalPrice *float32 `json:"totalPrice,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetAirbnbOfferClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbOfferClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetAirbnbOfferClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetAirbnbOfferClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r GetAirbnbOfferClientResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r GetAirbnbOfferClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r GetAirbnbOfferClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbOfferClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbOfferClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbOfferClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateAirbnbOfferClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *struct {
		CreatedAt    *time.Time              `json:"createdAt,omitempty"`
		ExpiresAt    *time.Time              `json:"expiresAt,omitempty"`
		GuestDetails *map[string]interface{} `json:"guestDetails,omitempty"`

		// Id Airbnb special-offer id.
		Id *string `json:"id,omitempty"`

		// ListingId Airbnb listing id (special offers only).
		ListingId *string                                        `json:"listingId,omitempty"`
		Nights    *int                                           `json:"nights,omitempty"`
		OfferType *CreateAirbnbOffer201JSONResponseBodyOfferType `json:"offerType,omitempty"`
		StartDate *openapi_types.Date                            `json:"startDate,omitempty"`
		Status    *CreateAirbnbOffer201JSONResponseBodyStatus    `json:"status,omitempty"`

		// ThreadId Airbnb thread id the offer was sent on.
		ThreadId   *string  `json:"threadId,omitempty"`
		TotalPrice *float32 `json:"totalPrice,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateAirbnbOfferClientResponse) GetJSON201() *struct {
	CreatedAt    *time.Time              `json:"createdAt,omitempty"`
	ExpiresAt    *time.Time              `json:"expiresAt,omitempty"`
	GuestDetails *map[string]interface{} `json:"guestDetails,omitempty"`

	// Id Airbnb special-offer id.
	Id *string `json:"id,omitempty"`

	// ListingId Airbnb listing id (special offers only).
	ListingId *string                                        `json:"listingId,omitempty"`
	Nights    *int                                           `json:"nights,omitempty"`
	OfferType *CreateAirbnbOffer201JSONResponseBodyOfferType `json:"offerType,omitempty"`
	StartDate *openapi_types.Date                            `json:"startDate,omitempty"`
	Status    *CreateAirbnbOffer201JSONResponseBodyStatus    `json:"status,omitempty"`

	// ThreadId Airbnb thread id the offer was sent on.
	ThreadId   *string  `json:"threadId,omitempty"`
	TotalPrice *float32 `json:"totalPrice,omitempty"`
} {
	return r.JSON201
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateAirbnbOfferClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateAirbnbOfferClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CreateAirbnbOfferClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r CreateAirbnbOfferClientResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r CreateAirbnbOfferClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r CreateAirbnbOfferClientResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r CreateAirbnbOfferClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r CreateAirbnbOfferClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateAirbnbOfferClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateAirbnbOfferClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateAirbnbOfferClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbReservationsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbReservationListResponse
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *AirbnbAccountNotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbReservationsClientResponse) GetJSON200() *AirbnbReservationListResponse {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAirbnbReservationsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListAirbnbReservationsClientResponse) GetJSON404() *AirbnbAccountNotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ListAirbnbReservationsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbReservationsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbReservationsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbReservationsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAirbnbReservationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbReservation
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAirbnbReservationClientResponse) GetJSON200() *AirbnbReservation {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetAirbnbReservationClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r GetAirbnbReservationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAirbnbReservationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAirbnbReservationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAirbnbReservationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type AirbnbReservationActionClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		ConfirmationCode *string `json:"confirmationCode,omitempty"`
		StatusType       *string `json:"statusType,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r AirbnbReservationActionClientResponse) GetJSON200() *struct {
	ConfirmationCode *string `json:"confirmationCode,omitempty"`
	StatusType       *string `json:"statusType,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r AirbnbReservationActionClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r AirbnbReservationActionClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r AirbnbReservationActionClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r AirbnbReservationActionClientResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r AirbnbReservationActionClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r AirbnbReservationActionClientResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r AirbnbReservationActionClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r AirbnbReservationActionClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r AirbnbReservationActionClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r AirbnbReservationActionClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r AirbnbReservationActionClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbReviewsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbReviewListResponse
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *AirbnbAccountNotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbReviewsClientResponse) GetJSON200() *AirbnbReviewListResponse {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListAirbnbReviewsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListAirbnbReviewsClientResponse) GetJSON404() *AirbnbAccountNotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ListAirbnbReviewsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbReviewsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbReviewsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbReviewsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type RespondAirbnbReviewLegacyClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r RespondAirbnbReviewLegacyClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r RespondAirbnbReviewLegacyClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r RespondAirbnbReviewLegacyClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r RespondAirbnbReviewLegacyClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r RespondAirbnbReviewLegacyClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r RespondAirbnbReviewLegacyClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r RespondAirbnbReviewLegacyClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r RespondAirbnbReviewLegacyClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type EditAirbnbReviewClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbReview
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r EditAirbnbReviewClientResponse) GetJSON200() *AirbnbReview {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r EditAirbnbReviewClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r EditAirbnbReviewClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r EditAirbnbReviewClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r EditAirbnbReviewClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r EditAirbnbReviewClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r EditAirbnbReviewClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r EditAirbnbReviewClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r EditAirbnbReviewClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r EditAirbnbReviewClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r EditAirbnbReviewClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r EditAirbnbReviewClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type RespondAirbnbReviewClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *AirbnbReview
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r RespondAirbnbReviewClientResponse) GetJSON200() *AirbnbReview {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r RespondAirbnbReviewClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r RespondAirbnbReviewClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r RespondAirbnbReviewClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r RespondAirbnbReviewClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r RespondAirbnbReviewClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r RespondAirbnbReviewClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r RespondAirbnbReviewClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r RespondAirbnbReviewClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r RespondAirbnbReviewClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r RespondAirbnbReviewClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r RespondAirbnbReviewClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListAirbnbTransactionsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data []AirbnbTransaction `json:"data"`

		// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
		//
		// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
		DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *AirbnbAccountNotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListAirbnbTransactionsClientResponse) GetJSON200() *struct {
	Data []AirbnbTransaction `json:"data"`

	// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
	//
	// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
	DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListAirbnbTransactionsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListAirbnbTransactionsClientResponse) GetJSON404() *AirbnbAccountNotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListAirbnbTransactionsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListAirbnbTransactionsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListAirbnbTransactionsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListAirbnbTransactionsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListAirbnbTransactionsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SyncAirbnbTransactionsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Count Number of transactions upserted.
		Count  int  `json:"count"`
		Synced bool `json:"synced"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SyncAirbnbTransactionsClientResponse) GetJSON200() *struct {
	// Count Number of transactions upserted.
	Count  int  `json:"count"`
	Synced bool `json:"synced"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SyncAirbnbTransactionsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r SyncAirbnbTransactionsClientResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r SyncAirbnbTransactionsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r SyncAirbnbTransactionsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SyncAirbnbTransactionsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SyncAirbnbTransactionsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SyncAirbnbTransactionsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetBookingAvailabilityClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BookingAvailabilityStateResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *BookingUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetBookingAvailabilityClientResponse) GetJSON200() *BookingAvailabilityStateResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetBookingAvailabilityClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetBookingAvailabilityClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetBookingAvailabilityClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetBookingAvailabilityClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetBookingAvailabilityClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r GetBookingAvailabilityClientResponse) GetJSON502() *BookingUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r GetBookingAvailabilityClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetBookingAvailabilityClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetBookingAvailabilityClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetBookingAvailabilityClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateBookingAvailabilityClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BookingPricingUpdateResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *BookingWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *BookingRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *BookingUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateBookingAvailabilityClientResponse) GetJSON200() *BookingPricingUpdateResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r UpdateBookingAvailabilityClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateBookingAvailabilityClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateBookingAvailabilityClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateBookingAvailabilityClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateBookingAvailabilityClientResponse) GetJSON422() *BookingWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateBookingAvailabilityClientResponse) GetJSON429() *BookingRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateBookingAvailabilityClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdateBookingAvailabilityClientResponse) GetJSON502() *BookingUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdateBookingAvailabilityClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateBookingAvailabilityClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateBookingAvailabilityClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateBookingAvailabilityClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetBookingChargesClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetBookingChargesClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetBookingChargesClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetBookingChargesClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetBookingChargesClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetBookingChargesClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetBookingChargesClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetBookingChargesClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetBookingChargesClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetBookingChargesClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateBookingChargesClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r UpdateBookingChargesClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateBookingChargesClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateBookingChargesClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateBookingChargesClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateBookingChargesClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r UpdateBookingChargesClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateBookingChargesClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateBookingChargesClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateBookingChargesClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetBookingContentClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetBookingContentClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetBookingContentClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetBookingContentClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetBookingContentClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r GetBookingContentClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetBookingContentClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetBookingContentClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetBookingContentClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateBookingContentClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateBookingContentClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateBookingContentClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateBookingContentClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateBookingContentClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r UpdateBookingContentClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateBookingContentClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateBookingContentClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateBookingContentClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type MapBookingRoomClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *MapBookingRoomResponse
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r MapBookingRoomClientResponse) GetJSON200() *MapBookingRoomResponse {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r MapBookingRoomClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r MapBookingRoomClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r MapBookingRoomClientResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r MapBookingRoomClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r MapBookingRoomClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r MapBookingRoomClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r MapBookingRoomClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r MapBookingRoomClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetBookingListingPricingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BookingPricingResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *BookingUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetBookingListingPricingClientResponse) GetJSON200() *BookingPricingResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetBookingListingPricingClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetBookingListingPricingClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetBookingListingPricingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetBookingListingPricingClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetBookingListingPricingClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r GetBookingListingPricingClientResponse) GetJSON502() *BookingUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r GetBookingListingPricingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetBookingListingPricingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetBookingListingPricingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetBookingListingPricingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateBookingListingPricingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BookingPricingUpdateResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *BookingWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *BookingRateLimited
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *BookingUpstreamError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateBookingListingPricingClientResponse) GetJSON200() *BookingPricingUpdateResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r UpdateBookingListingPricingClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateBookingListingPricingClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateBookingListingPricingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateBookingListingPricingClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r UpdateBookingListingPricingClientResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateBookingListingPricingClientResponse) GetJSON422() *BookingWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r UpdateBookingListingPricingClientResponse) GetJSON429() *BookingRateLimited {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateBookingListingPricingClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r UpdateBookingListingPricingClientResponse) GetJSON502() *BookingUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r UpdateBookingListingPricingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateBookingListingPricingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateBookingListingPricingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateBookingListingPricingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListBookingConversationsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BookingConversationListResponse
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListBookingConversationsClientResponse) GetJSON200() *BookingConversationListResponse {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListBookingConversationsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListBookingConversationsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ListBookingConversationsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListBookingConversationsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListBookingConversationsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListBookingConversationsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SendBookingMessageClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SendBookingMessageClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SendBookingMessageClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r SendBookingMessageClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SendBookingMessageClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SendBookingMessageClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r SendBookingMessageClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r SendBookingMessageClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SendBookingMessageClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SendBookingMessageClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SendBookingMessageClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListBookingPropertiesClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BookingPropertyListResponse
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListBookingPropertiesClientResponse) GetJSON200() *BookingPropertyListResponse {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r ListBookingPropertiesClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListBookingPropertiesClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListBookingPropertiesClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListBookingPropertiesClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetBookingPropertyClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetBookingPropertyClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetBookingPropertyClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetBookingPropertyClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetBookingPropertyClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetBookingPropertyClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetBookingPropertyClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetBookingPropertyClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetBookingPropertyClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetBookingPropertyClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type BookingPropertyActionClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BookingPropertyActionResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON402 the response for an HTTP 402 `application/json` response
	JSON402 *PublishBillingRefused
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *BookingWriteRejected
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *BookingUpstreamError
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *ChannelActionUnavailable
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r BookingPropertyActionClientResponse) GetJSON200() *BookingPropertyActionResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r BookingPropertyActionClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r BookingPropertyActionClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON402 returns the response for an HTTP 402 `application/json` response
func (r BookingPropertyActionClientResponse) GetJSON402() *PublishBillingRefused {
	return r.JSON402
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r BookingPropertyActionClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r BookingPropertyActionClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r BookingPropertyActionClientResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r BookingPropertyActionClientResponse) GetJSON422() *BookingWriteRejected {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r BookingPropertyActionClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r BookingPropertyActionClientResponse) GetJSON502() *BookingUpstreamError {
	return r.JSON502
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r BookingPropertyActionClientResponse) GetJSON503() *ChannelActionUnavailable {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r BookingPropertyActionClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r BookingPropertyActionClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r BookingPropertyActionClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r BookingPropertyActionClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListBookingPropertyRoomsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BookingRoomsRatesResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListBookingPropertyRoomsClientResponse) GetJSON200() *BookingRoomsRatesResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListBookingPropertyRoomsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListBookingPropertyRoomsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListBookingPropertyRoomsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListBookingPropertyRoomsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListBookingPropertyRoomsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListBookingPropertyRoomsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListBookingPropertyRoomsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListBookingPropertyRoomsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListBookingPropertyRoomsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListBookingReservationsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListBookingReservations200JSONResponseBody
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListBookingReservationsClientResponse) GetJSON200() *ListBookingReservations200JSONResponseBody {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListBookingReservationsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListBookingReservationsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListBookingReservationsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListBookingReservationsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListBookingReservationsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListBookingReservationsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListBookingReservationsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListBookingReservationsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListBookingReservationsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type AcknowledgeBookingReservationsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r AcknowledgeBookingReservationsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r AcknowledgeBookingReservationsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r AcknowledgeBookingReservationsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r AcknowledgeBookingReservationsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r AcknowledgeBookingReservationsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r AcknowledgeBookingReservationsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r AcknowledgeBookingReservationsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r AcknowledgeBookingReservationsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListBookingReviewsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListBookingReviewsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListBookingReviewsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListBookingReviewsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListBookingReviewsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListBookingReviewsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListBookingReviewsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListBookingReviewsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListBookingReviewsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ReplyBookingReviewClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Success *bool `json:"success,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ReplyBookingReviewClientResponse) GetJSON200() *struct {
	Success *bool `json:"success,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ReplyBookingReviewClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ReplyBookingReviewClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ReplyBookingReviewClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ReplyBookingReviewClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r ReplyBookingReviewClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r ReplyBookingReviewClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ReplyBookingReviewClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ReplyBookingReviewClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ReplyBookingReviewClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type BookingSetupClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r BookingSetupClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r BookingSetupClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r BookingSetupClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r BookingSetupClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r BookingSetupClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r BookingSetupClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r BookingSetupClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r BookingSetupClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r BookingSetupClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r BookingSetupClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r BookingSetupClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteBookingWebhookClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteBookingWebhookClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r DeleteBookingWebhookClientResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r DeleteBookingWebhookClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteBookingWebhookClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteBookingWebhookClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteBookingWebhookClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListBookingWebhooksClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListBookingWebhooksClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListBookingWebhooksClientResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r ListBookingWebhooksClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListBookingWebhooksClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListBookingWebhooksClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListBookingWebhooksClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateBookingWebhookClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateBookingWebhookClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateBookingWebhookClientResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r CreateBookingWebhookClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateBookingWebhookClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateBookingWebhookClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateBookingWebhookClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetPlumguideAvailabilityClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetPlumguideAvailabilityClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r GetPlumguideAvailabilityClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetPlumguideAvailabilityClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetPlumguideAvailabilityClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetPlumguideAvailabilityClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdatePlumguideAvailabilityClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdatePlumguideAvailabilityClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r UpdatePlumguideAvailabilityClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdatePlumguideAvailabilityClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdatePlumguideAvailabilityClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdatePlumguideAvailabilityClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListPlumguideBookingsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListPlumguideBookingsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListPlumguideBookingsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListPlumguideBookingsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListPlumguideBookingsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListPlumguideBookingsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListPlumguideBookingsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListPlumguideBookingsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListPlumguideBookingsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListPlumguideListingsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *PlumguideListingListResponse
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListPlumguideListingsClientResponse) GetJSON200() *PlumguideListingListResponse {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r ListPlumguideListingsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListPlumguideListingsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListPlumguideListingsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListPlumguideListingsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetPlumguidePricingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetPlumguidePricingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r GetPlumguidePricingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetPlumguidePricingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetPlumguidePricingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetPlumguidePricingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdatePlumguidePricingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdatePlumguidePricingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r UpdatePlumguidePricingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdatePlumguidePricingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdatePlumguidePricingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdatePlumguidePricingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeletePlumguideWebhooksClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeletePlumguideWebhooksClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeletePlumguideWebhooksClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r DeletePlumguideWebhooksClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r DeletePlumguideWebhooksClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeletePlumguideWebhooksClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeletePlumguideWebhooksClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeletePlumguideWebhooksClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetPlumguideWebhooksClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetPlumguideWebhooksClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetPlumguideWebhooksClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetPlumguideWebhooksClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetPlumguideWebhooksClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetPlumguideWebhooksClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetPlumguideWebhooksClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetPlumguideWebhooksClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdatePlumguideWebhooksClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r UpdatePlumguideWebhooksClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdatePlumguideWebhooksClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdatePlumguideWebhooksClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdatePlumguideWebhooksClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r UpdatePlumguideWebhooksClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdatePlumguideWebhooksClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdatePlumguideWebhooksClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdatePlumguideWebhooksClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListVrboListingsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *VrboListingListResponse
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListVrboListingsClientResponse) GetJSON200() *VrboListingListResponse {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r ListVrboListingsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListVrboListingsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListVrboListingsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListVrboListingsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListVrboReservationsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *VrboReservationListResponse
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListVrboReservationsClientResponse) GetJSON200() *VrboReservationListResponse {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r ListVrboReservationsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListVrboReservationsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListVrboReservationsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListVrboReservationsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListConnectionsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ConnectionListResponse
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListConnectionsClientResponse) GetJSON200() *ConnectionListResponse {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r ListConnectionsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListConnectionsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListConnectionsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListConnectionsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateConnectSessionClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *ConnectSession
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateConnectSessionClientResponse) GetJSON201() *ConnectSession {
	return r.JSON201
}

// GetBody returns the raw response body bytes
func (r CreateConnectSessionClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateConnectSessionClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateConnectSessionClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateConnectSessionClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SubmitBeds24CredentialsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
		Ok          *bool                   `json:"ok,omitempty"`
	}
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SubmitBeds24CredentialsClientResponse) GetJSON200() *struct {
	AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
	Ok          *bool                   `json:"ok,omitempty"`
} {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SubmitBeds24CredentialsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SubmitBeds24CredentialsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SubmitBeds24CredentialsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SubmitBeds24CredentialsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SubmitBeds24CredentialsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SubmitBeds24CredentialsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SubmitBeds24CredentialsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SubmitBeds24CredentialsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type BookingConnectCallbackClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r BookingConnectCallbackClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetBody returns the raw response body bytes
func (r BookingConnectCallbackClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r BookingConnectCallbackClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r BookingConnectCallbackClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r BookingConnectCallbackClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type MapConnectBookingRoomsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *MapConnectBookingRoomsResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
	// JSON410 the response for an HTTP 410 `application/json` response
	JSON410 *Error
	// JSON425 the response for an HTTP 425 `application/json` response
	JSON425 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r MapConnectBookingRoomsClientResponse) GetJSON200() *MapConnectBookingRoomsResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r MapConnectBookingRoomsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r MapConnectBookingRoomsClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r MapConnectBookingRoomsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r MapConnectBookingRoomsClientResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetJSON410 returns the response for an HTTP 410 `application/json` response
func (r MapConnectBookingRoomsClientResponse) GetJSON410() *Error {
	return r.JSON410
}

// GetJSON425 returns the response for an HTTP 425 `application/json` response
func (r MapConnectBookingRoomsClientResponse) GetJSON425() *Error {
	return r.JSON425
}

// GetBody returns the raw response body bytes
func (r MapConnectBookingRoomsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r MapConnectBookingRoomsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r MapConnectBookingRoomsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r MapConnectBookingRoomsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListConnectBookingRoomsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BookingConnectRoomsResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
	// JSON410 the response for an HTTP 410 `application/json` response
	JSON410 *Error
	// JSON425 the response for an HTTP 425 `application/json` response
	JSON425 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListConnectBookingRoomsClientResponse) GetJSON200() *BookingConnectRoomsResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListConnectBookingRoomsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListConnectBookingRoomsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r ListConnectBookingRoomsClientResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetJSON410 returns the response for an HTTP 410 `application/json` response
func (r ListConnectBookingRoomsClientResponse) GetJSON410() *Error {
	return r.JSON410
}

// GetJSON425 returns the response for an HTTP 425 `application/json` response
func (r ListConnectBookingRoomsClientResponse) GetJSON425() *Error {
	return r.JSON425
}

// GetBody returns the raw response body bytes
func (r ListConnectBookingRoomsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListConnectBookingRoomsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListConnectBookingRoomsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListConnectBookingRoomsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type VerifyBookingHotelClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BookingVerifyHotelResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
	// JSON410 the response for an HTTP 410 `application/json` response
	JSON410 *Error
	// JSON412 the response for an HTTP 412 `application/json` response
	JSON412 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r VerifyBookingHotelClientResponse) GetJSON200() *BookingVerifyHotelResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r VerifyBookingHotelClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r VerifyBookingHotelClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r VerifyBookingHotelClientResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetJSON410 returns the response for an HTTP 410 `application/json` response
func (r VerifyBookingHotelClientResponse) GetJSON410() *Error {
	return r.JSON410
}

// GetJSON412 returns the response for an HTTP 412 `application/json` response
func (r VerifyBookingHotelClientResponse) GetJSON412() *Error {
	return r.JSON412
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r VerifyBookingHotelClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r VerifyBookingHotelClientResponse) GetJSON503() *Error {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r VerifyBookingHotelClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r VerifyBookingHotelClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r VerifyBookingHotelClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r VerifyBookingHotelClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SubmitBookingsyncCredentialsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
		Ok          *bool                   `json:"ok,omitempty"`
	}
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SubmitBookingsyncCredentialsClientResponse) GetJSON200() *struct {
	AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
	Ok          *bool                   `json:"ok,omitempty"`
} {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SubmitBookingsyncCredentialsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SubmitBookingsyncCredentialsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SubmitBookingsyncCredentialsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SubmitBookingsyncCredentialsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SubmitBookingsyncCredentialsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SubmitBookingsyncCredentialsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SubmitBookingsyncCredentialsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SubmitBookingsyncCredentialsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SubmitGuestyCredentialsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
		Ok          *bool                   `json:"ok,omitempty"`
	}
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SubmitGuestyCredentialsClientResponse) GetJSON200() *struct {
	AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
	Ok          *bool                   `json:"ok,omitempty"`
} {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SubmitGuestyCredentialsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SubmitGuestyCredentialsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SubmitGuestyCredentialsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SubmitGuestyCredentialsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SubmitGuestyCredentialsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SubmitGuestyCredentialsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SubmitGuestyCredentialsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SubmitGuestyCredentialsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SubmitHospitableCredentialsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
		Ok          *bool                   `json:"ok,omitempty"`
	}
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SubmitHospitableCredentialsClientResponse) GetJSON200() *struct {
	AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
	Ok          *bool                   `json:"ok,omitempty"`
} {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SubmitHospitableCredentialsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SubmitHospitableCredentialsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SubmitHospitableCredentialsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SubmitHospitableCredentialsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SubmitHospitableCredentialsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SubmitHospitableCredentialsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SubmitHospitableCredentialsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SubmitHospitableCredentialsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SubmitHostawayCredentialsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
		Ok          *bool                   `json:"ok,omitempty"`
	}
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SubmitHostawayCredentialsClientResponse) GetJSON200() *struct {
	AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
	Ok          *bool                   `json:"ok,omitempty"`
} {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SubmitHostawayCredentialsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SubmitHostawayCredentialsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SubmitHostawayCredentialsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SubmitHostawayCredentialsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SubmitHostawayCredentialsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SubmitHostawayCredentialsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SubmitHostawayCredentialsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SubmitHostawayCredentialsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SubmitIgmsCredentialsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
		Ok          *bool                   `json:"ok,omitempty"`
	}
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SubmitIgmsCredentialsClientResponse) GetJSON200() *struct {
	AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
	Ok          *bool                   `json:"ok,omitempty"`
} {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SubmitIgmsCredentialsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SubmitIgmsCredentialsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SubmitIgmsCredentialsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SubmitIgmsCredentialsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SubmitIgmsCredentialsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SubmitIgmsCredentialsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SubmitIgmsCredentialsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SubmitIgmsCredentialsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SubmitLodgifyCredentialsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
		Ok          *bool                   `json:"ok,omitempty"`
	}
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SubmitLodgifyCredentialsClientResponse) GetJSON200() *struct {
	AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
	Ok          *bool                   `json:"ok,omitempty"`
} {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SubmitLodgifyCredentialsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SubmitLodgifyCredentialsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SubmitLodgifyCredentialsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SubmitLodgifyCredentialsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SubmitLodgifyCredentialsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SubmitLodgifyCredentialsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SubmitLodgifyCredentialsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SubmitLodgifyCredentialsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SubmitOwnerrezCredentialsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
		Ok          *bool                   `json:"ok,omitempty"`
	}
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SubmitOwnerrezCredentialsClientResponse) GetJSON200() *struct {
	AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
	Ok          *bool                   `json:"ok,omitempty"`
} {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SubmitOwnerrezCredentialsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SubmitOwnerrezCredentialsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SubmitOwnerrezCredentialsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SubmitOwnerrezCredentialsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SubmitOwnerrezCredentialsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SubmitOwnerrezCredentialsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SubmitOwnerrezCredentialsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SubmitOwnerrezCredentialsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListConnectProvidersClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ConnectProviderListResponse
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListConnectProvidersClientResponse) GetJSON200() *ConnectProviderListResponse {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r ListConnectProvidersClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListConnectProvidersClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListConnectProvidersClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListConnectProvidersClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SelectConnectProviderClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SelectProviderResponse
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SelectConnectProviderClientResponse) GetJSON200() *SelectProviderResponse {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r SelectConnectProviderClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SelectConnectProviderClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SelectConnectProviderClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SelectConnectProviderClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SubmitSmoobuCredentialsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
		Ok          *bool                   `json:"ok,omitempty"`
	}
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SubmitSmoobuCredentialsClientResponse) GetJSON200() *struct {
	AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
	Ok          *bool                   `json:"ok,omitempty"`
} {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SubmitSmoobuCredentialsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SubmitSmoobuCredentialsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SubmitSmoobuCredentialsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SubmitSmoobuCredentialsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SubmitSmoobuCredentialsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SubmitSmoobuCredentialsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SubmitSmoobuCredentialsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SubmitSmoobuCredentialsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SubmitVrboCredentialsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
		Ok          *bool                   `json:"ok,omitempty"`
	}
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SubmitVrboCredentialsClientResponse) GetJSON200() *struct {
	AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
	Ok          *bool                   `json:"ok,omitempty"`
} {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SubmitVrboCredentialsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SubmitVrboCredentialsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SubmitVrboCredentialsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SubmitVrboCredentialsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SubmitVrboCredentialsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SubmitVrboCredentialsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SubmitVrboCredentialsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SubmitVrboCredentialsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteConnectionClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// AccountId The account that was disconnected. `null` only when the workspace had a stale connection record with no account to name, which was cleared.
		AccountId *string `json:"accountId"`

		// Disconnected Always `true` on success.
		Disconnected bool `json:"disconnected"`

		// ListingsDeactivated Ids of the listings this call deactivated. Listings still connected through another account or channel are not included and stay active.
		ListingsDeactivated []string `json:"listingsDeactivated"`

		// Provider The provider the account belonged to.
		Provider string `json:"provider"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON501 the response for an HTTP 501 `application/json` response
	JSON501 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DeleteConnectionClientResponse) GetJSON200() *struct {
	// AccountId The account that was disconnected. `null` only when the workspace had a stale connection record with no account to name, which was cleared.
	AccountId *string `json:"accountId"`

	// Disconnected Always `true` on success.
	Disconnected bool `json:"disconnected"`

	// ListingsDeactivated Ids of the listings this call deactivated. Listings still connected through another account or channel are not included and stay active.
	ListingsDeactivated []string `json:"listingsDeactivated"`

	// Provider The provider the account belonged to.
	Provider string `json:"provider"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteConnectionClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeleteConnectionClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r DeleteConnectionClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON501 returns the response for an HTTP 501 `application/json` response
func (r DeleteConnectionClientResponse) GetJSON501() *Error {
	return r.JSON501
}

// GetBody returns the raw response body bytes
func (r DeleteConnectionClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteConnectionClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteConnectionClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteConnectionClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetConnectStatusClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ConnectStatus
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetConnectStatusClientResponse) GetJSON200() *ConnectStatus {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r GetConnectStatusClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetConnectStatusClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetConnectStatusClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetConnectStatusClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateConnectionClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Connection
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CreateConnectionClientResponse) GetJSON200() *Connection {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r CreateConnectionClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateConnectionClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateConnectionClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateConnectionClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListConversationsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ConversationListResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListConversationsClientResponse) GetJSON200() *ConversationListResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListConversationsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListConversationsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListConversationsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListConversationsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListConversationsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListConversationsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListConversationsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListConversationsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetConversationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ConversationDetail
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetConversationClientResponse) GetJSON200() *ConversationDetail {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetConversationClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetConversationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetConversationClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetConversationClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetConversationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetConversationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetConversationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetConversationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetConversationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListConversationMessagesClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *MessageListResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListConversationMessagesClientResponse) GetJSON200() *MessageListResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListConversationMessagesClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListConversationMessagesClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListConversationMessagesClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListConversationMessagesClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListConversationMessagesClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListConversationMessagesClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListConversationMessagesClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListConversationMessagesClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListConversationMessagesClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListConversationMessagesClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SendConversationMessageClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *SendMessageResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Error
	// JSON503 the response for an HTTP 503 `application/json` response
	JSON503 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SendConversationMessageClientResponse) GetJSON200() *SendMessageResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SendConversationMessageClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r SendConversationMessageClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SendConversationMessageClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SendConversationMessageClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r SendConversationMessageClientResponse) GetJSON500() *Error {
	return r.JSON500
}

// GetJSON503 returns the response for an HTTP 503 `application/json` response
func (r SendConversationMessageClientResponse) GetJSON503() *Error {
	return r.JSON503
}

// GetBody returns the raw response body bytes
func (r SendConversationMessageClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SendConversationMessageClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SendConversationMessageClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SendConversationMessageClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type PreapproveConversationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *struct {
		// BlockInstantBooking Example: false
		BlockInstantBooking bool `json:"blockInstantBooking"`

		// ConversationId Example: 164743
		ConversationId string `json:"conversationId"`

		// ExpiresAt When the guest can no longer book on the pre-approval, if Airbnb reported it.
		ExpiresAt *time.Time                                      `json:"expiresAt"`
		Status    PreapproveConversation201JSONResponseBodyStatus `json:"status"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r PreapproveConversationClientResponse) GetJSON201() *struct {
	// BlockInstantBooking Example: false
	BlockInstantBooking bool `json:"blockInstantBooking"`

	// ConversationId Example: 164743
	ConversationId string `json:"conversationId"`

	// ExpiresAt When the guest can no longer book on the pre-approval, if Airbnb reported it.
	ExpiresAt *time.Time                                      `json:"expiresAt"`
	Status    PreapproveConversation201JSONResponseBodyStatus `json:"status"`
} {
	return r.JSON201
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r PreapproveConversationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r PreapproveConversationClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r PreapproveConversationClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r PreapproveConversationClientResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r PreapproveConversationClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r PreapproveConversationClientResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r PreapproveConversationClientResponse) GetJSON500() *Error {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r PreapproveConversationClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r PreapproveConversationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PreapproveConversationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PreapproveConversationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PreapproveConversationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateConversationSpecialOfferClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *struct {
		// AirbnbListingId Airbnb listing id the offer is for (a string — it exceeds 2^53).
		//
		// Example: 955656266214757921
		AirbnbListingId *string `json:"airbnbListingId,omitempty"`

		// CheckIn Example: 2026-10-01
		CheckIn *openapi_types.Date `json:"checkIn"`

		// CheckOut Example: 2026-10-05
		CheckOut *openapi_types.Date `json:"checkOut"`

		// ConversationId Repull conversation id the offer was sent on.
		//
		// Example: 164743
		ConversationId string     `json:"conversationId"`
		CreatedAt      *time.Time `json:"createdAt,omitempty"`

		// ExpiresAt When the guest can no longer book the offer (Airbnb gives them 24 hours).
		ExpiresAt *time.Time `json:"expiresAt,omitempty"`

		// Guests Guests on the offer. Airbnb counts adults + children as guests; infants and pets are extra.
		Guests *struct {
			// Adults Example: 2
			Adults *int `json:"adults,omitempty"`

			// Children Example: 1
			Children *int `json:"children,omitempty"`

			// Infants Example: 0
			Infants *int `json:"infants,omitempty"`

			// Pets Example: 0
			Pets *int `json:"pets,omitempty"`

			// Total Example: 3
			Total *int `json:"total,omitempty"`
		} `json:"guests,omitempty"`

		// Id Airbnb special-offer id. Use it to read or withdraw the offer.
		//
		// Example: 1459920384
		Id *string `json:"id"`

		// ListingId Repull listing id, when known.
		//
		// Example: 23892
		ListingId *string `json:"listingId,omitempty"`

		// Nights Example: 4
		Nights *int `json:"nights"`

		// Status Airbnb’s status for the offer: `active` (the guest can book it), `accepted`, `declined`, `expired` or `voided` (withdrawn).
		//
		// Example: active
		Status *string `json:"status"`

		// TotalPrice Total for the stay, in the listing’s Airbnb currency.
		//
		// Example: 880
		TotalPrice *float32 `json:"totalPrice"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateConversationSpecialOfferClientResponse) GetJSON201() *struct {
	// AirbnbListingId Airbnb listing id the offer is for (a string — it exceeds 2^53).
	//
	// Example: 955656266214757921
	AirbnbListingId *string `json:"airbnbListingId,omitempty"`

	// CheckIn Example: 2026-10-01
	CheckIn *openapi_types.Date `json:"checkIn"`

	// CheckOut Example: 2026-10-05
	CheckOut *openapi_types.Date `json:"checkOut"`

	// ConversationId Repull conversation id the offer was sent on.
	//
	// Example: 164743
	ConversationId string     `json:"conversationId"`
	CreatedAt      *time.Time `json:"createdAt,omitempty"`

	// ExpiresAt When the guest can no longer book the offer (Airbnb gives them 24 hours).
	ExpiresAt *time.Time `json:"expiresAt,omitempty"`

	// Guests Guests on the offer. Airbnb counts adults + children as guests; infants and pets are extra.
	Guests *struct {
		// Adults Example: 2
		Adults *int `json:"adults,omitempty"`

		// Children Example: 1
		Children *int `json:"children,omitempty"`

		// Infants Example: 0
		Infants *int `json:"infants,omitempty"`

		// Pets Example: 0
		Pets *int `json:"pets,omitempty"`

		// Total Example: 3
		Total *int `json:"total,omitempty"`
	} `json:"guests,omitempty"`

	// Id Airbnb special-offer id. Use it to read or withdraw the offer.
	//
	// Example: 1459920384
	Id *string `json:"id"`

	// ListingId Repull listing id, when known.
	//
	// Example: 23892
	ListingId *string `json:"listingId,omitempty"`

	// Nights Example: 4
	Nights *int `json:"nights"`

	// Status Airbnb’s status for the offer: `active` (the guest can book it), `accepted`, `declined`, `expired` or `voided` (withdrawn).
	//
	// Example: active
	Status *string `json:"status"`

	// TotalPrice Total for the stay, in the listing’s Airbnb currency.
	//
	// Example: 880
	TotalPrice *float32 `json:"totalPrice"`
} {
	return r.JSON201
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateConversationSpecialOfferClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateConversationSpecialOfferClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CreateConversationSpecialOfferClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r CreateConversationSpecialOfferClientResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r CreateConversationSpecialOfferClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r CreateConversationSpecialOfferClientResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CreateConversationSpecialOfferClientResponse) GetJSON500() *Error {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r CreateConversationSpecialOfferClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r CreateConversationSpecialOfferClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateConversationSpecialOfferClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateConversationSpecialOfferClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateConversationSpecialOfferClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type WithdrawConversationSpecialOfferClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// ConversationId Example: 164743
		ConversationId string `json:"conversationId"`

		// Id Example: 1459920384
		Id     string                                                    `json:"id"`
		Status WithdrawConversationSpecialOffer200JSONResponseBodyStatus `json:"status"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r WithdrawConversationSpecialOfferClientResponse) GetJSON200() *struct {
	// ConversationId Example: 164743
	ConversationId string `json:"conversationId"`

	// Id Example: 1459920384
	Id     string                                                    `json:"id"`
	Status WithdrawConversationSpecialOffer200JSONResponseBodyStatus `json:"status"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r WithdrawConversationSpecialOfferClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r WithdrawConversationSpecialOfferClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r WithdrawConversationSpecialOfferClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r WithdrawConversationSpecialOfferClientResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r WithdrawConversationSpecialOfferClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r WithdrawConversationSpecialOfferClientResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r WithdrawConversationSpecialOfferClientResponse) GetJSON500() *Error {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r WithdrawConversationSpecialOfferClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r WithdrawConversationSpecialOfferClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r WithdrawConversationSpecialOfferClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r WithdrawConversationSpecialOfferClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r WithdrawConversationSpecialOfferClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetConversationSpecialOfferClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// AirbnbListingId Airbnb listing id the offer is for (a string — it exceeds 2^53).
		//
		// Example: 955656266214757921
		AirbnbListingId *string `json:"airbnbListingId,omitempty"`

		// CheckIn Example: 2026-10-01
		CheckIn *openapi_types.Date `json:"checkIn"`

		// CheckOut Example: 2026-10-05
		CheckOut *openapi_types.Date `json:"checkOut"`

		// ConversationId Repull conversation id the offer was sent on.
		//
		// Example: 164743
		ConversationId string     `json:"conversationId"`
		CreatedAt      *time.Time `json:"createdAt,omitempty"`

		// ExpiresAt When the guest can no longer book the offer (Airbnb gives them 24 hours).
		ExpiresAt *time.Time `json:"expiresAt,omitempty"`

		// Guests Guests on the offer. Airbnb counts adults + children as guests; infants and pets are extra.
		Guests *struct {
			// Adults Example: 2
			Adults *int `json:"adults,omitempty"`

			// Children Example: 1
			Children *int `json:"children,omitempty"`

			// Infants Example: 0
			Infants *int `json:"infants,omitempty"`

			// Pets Example: 0
			Pets *int `json:"pets,omitempty"`

			// Total Example: 3
			Total *int `json:"total,omitempty"`
		} `json:"guests,omitempty"`

		// Id Airbnb special-offer id. Use it to read or withdraw the offer.
		//
		// Example: 1459920384
		Id *string `json:"id"`

		// ListingId Repull listing id, when known.
		//
		// Example: 23892
		ListingId *string `json:"listingId,omitempty"`

		// Nights Example: 4
		Nights *int `json:"nights"`

		// Status Airbnb’s status for the offer: `active` (the guest can book it), `accepted`, `declined`, `expired` or `voided` (withdrawn).
		//
		// Example: active
		Status *string `json:"status"`

		// TotalPrice Total for the stay, in the listing’s Airbnb currency.
		//
		// Example: 880
		TotalPrice *float32 `json:"totalPrice"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetConversationSpecialOfferClientResponse) GetJSON200() *struct {
	// AirbnbListingId Airbnb listing id the offer is for (a string — it exceeds 2^53).
	//
	// Example: 955656266214757921
	AirbnbListingId *string `json:"airbnbListingId,omitempty"`

	// CheckIn Example: 2026-10-01
	CheckIn *openapi_types.Date `json:"checkIn"`

	// CheckOut Example: 2026-10-05
	CheckOut *openapi_types.Date `json:"checkOut"`

	// ConversationId Repull conversation id the offer was sent on.
	//
	// Example: 164743
	ConversationId string     `json:"conversationId"`
	CreatedAt      *time.Time `json:"createdAt,omitempty"`

	// ExpiresAt When the guest can no longer book the offer (Airbnb gives them 24 hours).
	ExpiresAt *time.Time `json:"expiresAt,omitempty"`

	// Guests Guests on the offer. Airbnb counts adults + children as guests; infants and pets are extra.
	Guests *struct {
		// Adults Example: 2
		Adults *int `json:"adults,omitempty"`

		// Children Example: 1
		Children *int `json:"children,omitempty"`

		// Infants Example: 0
		Infants *int `json:"infants,omitempty"`

		// Pets Example: 0
		Pets *int `json:"pets,omitempty"`

		// Total Example: 3
		Total *int `json:"total,omitempty"`
	} `json:"guests,omitempty"`

	// Id Airbnb special-offer id. Use it to read or withdraw the offer.
	//
	// Example: 1459920384
	Id *string `json:"id"`

	// ListingId Repull listing id, when known.
	//
	// Example: 23892
	ListingId *string `json:"listingId,omitempty"`

	// Nights Example: 4
	Nights *int `json:"nights"`

	// Status Airbnb’s status for the offer: `active` (the guest can book it), `accepted`, `declined`, `expired` or `voided` (withdrawn).
	//
	// Example: active
	Status *string `json:"status"`

	// TotalPrice Total for the stay, in the listing’s Airbnb currency.
	//
	// Example: 880
	TotalPrice *float32 `json:"totalPrice"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetConversationSpecialOfferClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetConversationSpecialOfferClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetConversationSpecialOfferClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetConversationSpecialOfferClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r GetConversationSpecialOfferClientResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetConversationSpecialOfferClientResponse) GetJSON500() *Error {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r GetConversationSpecialOfferClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r GetConversationSpecialOfferClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetConversationSpecialOfferClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetConversationSpecialOfferClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetConversationSpecialOfferClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListGuestsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *GuestListResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListGuestsClientResponse) GetJSON200() *GuestListResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListGuestsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListGuestsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListGuestsClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListGuestsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListGuestsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListGuestsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListGuestsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListGuestsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListGuestsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateGuestClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *GuestCreateResponse
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *GuestCreateResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CreateGuestClientResponse) GetJSON200() *GuestCreateResponse {
	return r.JSON200
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateGuestClientResponse) GetJSON201() *GuestCreateResponse {
	return r.JSON201
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateGuestClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r CreateGuestClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CreateGuestClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r CreateGuestClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateGuestClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateGuestClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateGuestClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetGuestClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *GuestProfile
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetGuestClientResponse) GetJSON200() *GuestProfile {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetGuestClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetGuestClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetGuestClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetGuestClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetGuestClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetGuestClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetGuestClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetGuestClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetGuestClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetHealthClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Status Example: ok
		Status *string `json:"status,omitempty"`

		// Version Example: 1.0.0
		Version *string `json:"version,omitempty"`
	}
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetHealthClientResponse) GetJSON200() *struct {
	// Status Example: ok
	Status *string `json:"status,omitempty"`

	// Version Example: 1.0.0
	Version *string `json:"version,omitempty"`
} {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r GetHealthClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetHealthClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetHealthClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetHealthClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetAuthHealthClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *map[string]interface{}
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetAuthHealthClientResponse) GetJSON200() *map[string]interface{} {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r GetAuthHealthClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetAuthHealthClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetAuthHealthClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetAuthHealthClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetChannelHealthClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *map[string]interface{}
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetChannelHealthClientResponse) GetJSON200() *map[string]interface{} {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r GetChannelHealthClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetChannelHealthClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetChannelHealthClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetChannelHealthClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetMcpHealthClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *map[string]interface{}
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetMcpHealthClientResponse) GetJSON200() *map[string]interface{} {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r GetMcpHealthClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetMcpHealthClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetMcpHealthClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetMcpHealthClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetWebhooksHealthClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *map[string]interface{}
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetWebhooksHealthClientResponse) GetJSON200() *map[string]interface{} {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r GetWebhooksHealthClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetWebhooksHealthClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetWebhooksHealthClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetWebhooksHealthClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListInquiriesClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data []struct {
			// Channel Example: airbnb
			Channel string `json:"channel"`

			// CheckIn Example: 2026-09-23
			CheckIn *openapi_types.Date `json:"checkIn"`

			// CheckOut Example: 2026-10-11
			CheckOut *openapi_types.Date `json:"checkOut"`

			// ConversationId Repull conversation id — pass it to `POST /v1/conversations/{id}/pre-approval` or `/special-offers`.
			//
			// Example: 164743
			ConversationId *string    `json:"conversationId"`
			CreatedAt      *time.Time `json:"createdAt"`

			// ExpectedPayout What Airbnb quoted the host for the stay the guest asked about.
			ExpectedPayout struct {
				// Amount Example: 2152.6
				Amount *float32 `json:"amount,omitempty"`

				// Currency Example: USD
				Currency *string `json:"currency,omitempty"`
			} `json:"expectedPayout"`
			Guests struct {
				// Adults Example: 2
				Adults *int `json:"adults,omitempty"`

				// Children Example: 0
				Children *int `json:"children,omitempty"`

				// Infants Example: 0
				Infants *int `json:"infants,omitempty"`

				// Pets Example: 0
				Pets *int `json:"pets,omitempty"`

				// Total Example: 2
				Total *int `json:"total,omitempty"`
			} `json:"guests"`

			// Id Repull inquiry id.
			//
			// Example: 25173
			Id string `json:"id"`

			// ListingId Example: 23892
			ListingId *string `json:"listingId"`

			// RelayedBy A PMS (e.g. `hostaway`, `guesty`) this inquiry arrives through. When set, it cannot be pre-approved or offered from Repull — act on it in that PMS.
			RelayedBy *string `json:"relayedBy"`

			// ReservationId The reservation the inquiry became, once booked.
			ReservationId *string `json:"reservationId"`

			// RespondBy Airbnb’s response deadline for the host (it counts toward response rate).
			RespondBy   *time.Time `json:"respondBy"`
			RespondedAt *time.Time `json:"respondedAt"`

			// Status `open` — nobody has answered and the stay is still ahead; `pre_approved`; `special_offer_sent` (from the API, a connected app, or Airbnb’s own app); `booked` — the guest booked (`reservationId`); `expired` — the stay has started or Airbnb expired it; `declined`; `not_possible` — Airbnb says the dates cannot be booked.
			Status    ListInquiries200JSONResponseBodyDataStatus `json:"status"`
			UpdatedAt *time.Time                                 `json:"updatedAt"`
		} `json:"data"`

		// Pagination Canonical cursor-based pagination envelope. Pass `nextCursor` back as `?cursor=` to fetch the next page; stop when `hasMore` is `false`. The cursor is opaque base64 — do not parse or construct it by hand.
		Pagination Pagination `json:"pagination"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListInquiriesClientResponse) GetJSON200() *struct {
	Data []struct {
		// Channel Example: airbnb
		Channel string `json:"channel"`

		// CheckIn Example: 2026-09-23
		CheckIn *openapi_types.Date `json:"checkIn"`

		// CheckOut Example: 2026-10-11
		CheckOut *openapi_types.Date `json:"checkOut"`

		// ConversationId Repull conversation id — pass it to `POST /v1/conversations/{id}/pre-approval` or `/special-offers`.
		//
		// Example: 164743
		ConversationId *string    `json:"conversationId"`
		CreatedAt      *time.Time `json:"createdAt"`

		// ExpectedPayout What Airbnb quoted the host for the stay the guest asked about.
		ExpectedPayout struct {
			// Amount Example: 2152.6
			Amount *float32 `json:"amount,omitempty"`

			// Currency Example: USD
			Currency *string `json:"currency,omitempty"`
		} `json:"expectedPayout"`
		Guests struct {
			// Adults Example: 2
			Adults *int `json:"adults,omitempty"`

			// Children Example: 0
			Children *int `json:"children,omitempty"`

			// Infants Example: 0
			Infants *int `json:"infants,omitempty"`

			// Pets Example: 0
			Pets *int `json:"pets,omitempty"`

			// Total Example: 2
			Total *int `json:"total,omitempty"`
		} `json:"guests"`

		// Id Repull inquiry id.
		//
		// Example: 25173
		Id string `json:"id"`

		// ListingId Example: 23892
		ListingId *string `json:"listingId"`

		// RelayedBy A PMS (e.g. `hostaway`, `guesty`) this inquiry arrives through. When set, it cannot be pre-approved or offered from Repull — act on it in that PMS.
		RelayedBy *string `json:"relayedBy"`

		// ReservationId The reservation the inquiry became, once booked.
		ReservationId *string `json:"reservationId"`

		// RespondBy Airbnb’s response deadline for the host (it counts toward response rate).
		RespondBy   *time.Time `json:"respondBy"`
		RespondedAt *time.Time `json:"respondedAt"`

		// Status `open` — nobody has answered and the stay is still ahead; `pre_approved`; `special_offer_sent` (from the API, a connected app, or Airbnb’s own app); `booked` — the guest booked (`reservationId`); `expired` — the stay has started or Airbnb expired it; `declined`; `not_possible` — Airbnb says the dates cannot be booked.
		Status    ListInquiries200JSONResponseBodyDataStatus `json:"status"`
		UpdatedAt *time.Time                                 `json:"updatedAt"`
	} `json:"data"`

	// Pagination Canonical cursor-based pagination envelope. Pass `nextCursor` back as `?cursor=` to fetch the next page; stop when `hasMore` is `false`. The cursor is opaque base64 — do not parse or construct it by hand.
	Pagination Pagination `json:"pagination"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListInquiriesClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListInquiriesClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListInquiriesClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r ListInquiriesClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListInquiriesClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListInquiriesClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListInquiriesClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ClearKvClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Deleted *int `json:"deleted,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ClearKvClientResponse) GetJSON200() *struct {
	Deleted *int `json:"deleted,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ClearKvClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ClearKvClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r ClearKvClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ClearKvClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ClearKvClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ClearKvClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListKvClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data *[]struct {
			Key       *string     `json:"key,omitempty"`
			TtlAt     *time.Time  `json:"ttl_at,omitempty"`
			UpdatedAt *time.Time  `json:"updated_at,omitempty"`
			Value     interface{} `json:"value,omitempty"`
		} `json:"data,omitempty"`
		Pagination *struct {
			HasMore *bool `json:"hasMore,omitempty"`
			Total   *int  `json:"total,omitempty"`
		} `json:"pagination,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListKvClientResponse) GetJSON200() *struct {
	Data *[]struct {
		Key       *string     `json:"key,omitempty"`
		TtlAt     *time.Time  `json:"ttl_at,omitempty"`
		UpdatedAt *time.Time  `json:"updated_at,omitempty"`
		Value     interface{} `json:"value,omitempty"`
	} `json:"data,omitempty"`
	Pagination *struct {
		HasMore *bool `json:"hasMore,omitempty"`
		Total   *int  `json:"total,omitempty"`
	} `json:"pagination,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListKvClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r ListKvClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListKvClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListKvClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListKvClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteKvClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Deleted *bool `json:"deleted,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DeleteKvClientResponse) GetJSON200() *struct {
	Deleted *bool `json:"deleted,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteKvClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r DeleteKvClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteKvClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteKvClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteKvClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetKvClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Key       *string     `json:"key,omitempty"`
		TtlAt     *time.Time  `json:"ttl_at,omitempty"`
		UpdatedAt *time.Time  `json:"updated_at,omitempty"`
		Value     interface{} `json:"value,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetKvClientResponse) GetJSON200() *struct {
	Key       *string     `json:"key,omitempty"`
	TtlAt     *time.Time  `json:"ttl_at,omitempty"`
	UpdatedAt *time.Time  `json:"updated_at,omitempty"`
	Value     interface{} `json:"value,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetKvClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetKvClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetKvClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetKvClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetKvClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetKvClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SetKvClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Key       *string     `json:"key,omitempty"`
		TtlAt     *time.Time  `json:"ttl_at,omitempty"`
		UpdatedAt *time.Time  `json:"updated_at,omitempty"`
		Value     interface{} `json:"value,omitempty"`
	}
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON413 the response for an HTTP 413 `application/json` response
	JSON413 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SetKvClientResponse) GetJSON200() *struct {
	Key       *string     `json:"key,omitempty"`
	TtlAt     *time.Time  `json:"ttl_at,omitempty"`
	UpdatedAt *time.Time  `json:"updated_at,omitempty"`
	Value     interface{} `json:"value,omitempty"`
} {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SetKvClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SetKvClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON413 returns the response for an HTTP 413 `application/json` response
func (r SetKvClientResponse) GetJSON413() *Error {
	return r.JSON413
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SetKvClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SetKvClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SetKvClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SetKvClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SetKvClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListListingsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingListResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListListingsClientResponse) GetJSON200() *ListingListResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListListingsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListListingsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r ListListingsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListListingsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListListingsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListListingsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateListingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *ListingCreateResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON402 the response for an HTTP 402 `application/json` response
	JSON402 *PaymentRequired
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateListingClientResponse) GetJSON201() *ListingCreateResponse {
	return r.JSON201
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CreateListingClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON402 returns the response for an HTTP 402 `application/json` response
func (r CreateListingClientResponse) GetJSON402() *PaymentRequired {
	return r.JSON402
}

// GetBody returns the raw response body bytes
func (r CreateListingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateListingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateListingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateListingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type BulkApplyPricingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *BulkPricingResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r BulkApplyPricingClientResponse) GetJSON200() *BulkPricingResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r BulkApplyPricingClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r BulkApplyPricingClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r BulkApplyPricingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r BulkApplyPricingClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r BulkApplyPricingClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r BulkApplyPricingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r BulkApplyPricingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r BulkApplyPricingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r BulkApplyPricingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type SetListingsStatusClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingStatusBatchResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON402 the response for an HTTP 402 `application/json` response
	JSON402 *PaymentRequired
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r SetListingsStatusClientResponse) GetJSON200() *ListingStatusBatchResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r SetListingsStatusClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r SetListingsStatusClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON402 returns the response for an HTTP 402 `application/json` response
func (r SetListingsStatusClientResponse) GetJSON402() *PaymentRequired {
	return r.JSON402
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r SetListingsStatusClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r SetListingsStatusClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r SetListingsStatusClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r SetListingsStatusClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r SetListingsStatusClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r SetListingsStatusClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeactivateListingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingActiveResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DeactivateListingClientResponse) GetJSON200() *ListingActiveResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeactivateListingClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeactivateListingClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r DeactivateListingClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r DeactivateListingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeactivateListingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeactivateListingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeactivateListingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetListingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Listing
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetListingClientResponse) GetJSON200() *Listing {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetListingClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetListingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetListingClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetListingClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r GetListingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetListingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetListingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetListingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateListingActiveClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingActiveResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON402 the response for an HTTP 402 `application/json` response
	JSON402 *PaymentRequired
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateListingActiveClientResponse) GetJSON200() *ListingActiveResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateListingActiveClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON402 returns the response for an HTTP 402 `application/json` response
func (r UpdateListingActiveClientResponse) GetJSON402() *PaymentRequired {
	return r.JSON402
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateListingActiveClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateListingActiveClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r UpdateListingActiveClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateListingActiveClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateListingActiveClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateListingActiveClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListListingCompsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingCompsResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListListingCompsClientResponse) GetJSON200() *ListingCompsResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListListingCompsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListListingCompsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListListingCompsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListListingCompsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r ListListingCompsClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r ListListingCompsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListListingCompsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListListingCompsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListListingCompsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateListingContentClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingContentUpdateResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateListingContentClientResponse) GetJSON200() *ListingContentUpdateResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateListingContentClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateListingContentClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateListingContentClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateListingContentClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r UpdateListingContentClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateListingContentClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateListingContentClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateListingContentClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GenerateListingContentClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingGenerateContentResponse
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GenerateListingContentClientResponse) GetJSON200() *ListingGenerateContentResponse {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GenerateListingContentClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GenerateListingContentClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GenerateListingContentClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GenerateListingContentClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GenerateListingContentClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GenerateListingContentClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// TakeListingOfflineClientResponse429Headers the declared response headers of an HTTP 429 response for TakeListingOffline
type TakeListingOfflineClientResponse429Headers struct {
	RetryAfter          *int
	XRateLimitLimit     *int
	XRateLimitRemaining *int
	XRateLimitReset     *time.Time
}

type TakeListingOfflineClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingMarketStateResponse
	// JSON402 the response for an HTTP 402 `application/json` response
	JSON402 *PublishBillingRefused
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *TooManyRequests
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *TakeListingOfflineClientResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r TakeListingOfflineClientResponse) GetJSON200() *ListingMarketStateResponse {
	return r.JSON200
}

// GetJSON402 returns the response for an HTTP 402 `application/json` response
func (r TakeListingOfflineClientResponse) GetJSON402() *PublishBillingRefused {
	return r.JSON402
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r TakeListingOfflineClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r TakeListingOfflineClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r TakeListingOfflineClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r TakeListingOfflineClientResponse) GetJSON429() *TooManyRequests {
	return r.JSON429
}

// GetBody returns the raw response body bytes
func (r TakeListingOfflineClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r TakeListingOfflineClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r TakeListingOfflineClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r TakeListingOfflineClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// TakeListingOnlineClientResponse429Headers the declared response headers of an HTTP 429 response for TakeListingOnline
type TakeListingOnlineClientResponse429Headers struct {
	RetryAfter          *int
	XRateLimitLimit     *int
	XRateLimitRemaining *int
	XRateLimitReset     *time.Time
}

type TakeListingOnlineClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingMarketStateResponse
	// JSON402 the response for an HTTP 402 `application/json` response
	JSON402 *PublishBillingRefused
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *TooManyRequests
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *TakeListingOnlineClientResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r TakeListingOnlineClientResponse) GetJSON200() *ListingMarketStateResponse {
	return r.JSON200
}

// GetJSON402 returns the response for an HTTP 402 `application/json` response
func (r TakeListingOnlineClientResponse) GetJSON402() *PublishBillingRefused {
	return r.JSON402
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r TakeListingOnlineClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r TakeListingOnlineClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r TakeListingOnlineClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r TakeListingOnlineClientResponse) GetJSON429() *TooManyRequests {
	return r.JSON429
}

// GetBody returns the raw response body bytes
func (r TakeListingOnlineClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r TakeListingOnlineClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r TakeListingOnlineClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r TakeListingOnlineClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteListingPhotoClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingPhotoDeleteResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DeleteListingPhotoClientResponse) GetJSON200() *ListingPhotoDeleteResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteListingPhotoClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r DeleteListingPhotoClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeleteListingPhotoClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r DeleteListingPhotoClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r DeleteListingPhotoClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteListingPhotoClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteListingPhotoClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteListingPhotoClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListListingPhotosClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingPhotosResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListListingPhotosClientResponse) GetJSON200() *ListingPhotosResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListListingPhotosClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListListingPhotosClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ListListingPhotosClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r ListListingPhotosClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListListingPhotosClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListListingPhotosClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListListingPhotosClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateListingPhotoUploadUrlClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingPhotoUploadUrlResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CreateListingPhotoUploadUrlClientResponse) GetJSON200() *ListingPhotoUploadUrlResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateListingPhotoUploadUrlClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateListingPhotoUploadUrlClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CreateListingPhotoUploadUrlClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r CreateListingPhotoUploadUrlClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r CreateListingPhotoUploadUrlClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateListingPhotoUploadUrlClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateListingPhotoUploadUrlClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateListingPhotoUploadUrlClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetListingPricingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingPricingResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetListingPricingClientResponse) GetJSON200() *ListingPricingResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetListingPricingClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetListingPricingClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetListingPricingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r GetListingPricingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetListingPricingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetListingPricingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetListingPricingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ApplyListingPricingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingPricingApplyResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ApplyListingPricingClientResponse) GetJSON200() *ListingPricingApplyResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ApplyListingPricingClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ApplyListingPricingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r ApplyListingPricingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ApplyListingPricingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ApplyListingPricingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ApplyListingPricingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetListingPricingHistoryClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingPricingHistoryResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetListingPricingHistoryClientResponse) GetJSON200() *ListingPricingHistoryResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetListingPricingHistoryClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetListingPricingHistoryClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetListingPricingHistoryClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetListingPricingHistoryClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetListingPricingHistoryClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r GetListingPricingHistoryClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r GetListingPricingHistoryClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetListingPricingHistoryClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetListingPricingHistoryClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetListingPricingHistoryClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetListingPricingStrategyClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingPricingStrategy
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetListingPricingStrategyClientResponse) GetJSON200() *ListingPricingStrategy {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetListingPricingStrategyClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetListingPricingStrategyClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r GetListingPricingStrategyClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetListingPricingStrategyClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetListingPricingStrategyClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetListingPricingStrategyClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateListingPricingStrategyClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Ok *bool `json:"ok,omitempty"`
	}
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateListingPricingStrategyClientResponse) GetJSON200() *struct {
	Ok *bool `json:"ok,omitempty"`
} {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateListingPricingStrategyClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r UpdateListingPricingStrategyClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateListingPricingStrategyClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateListingPricingStrategyClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateListingPricingStrategyClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// GetListingPublishStatusClientResponse429Headers the declared response headers of an HTTP 429 response for GetListingPublishStatus
type GetListingPublishStatusClientResponse429Headers struct {
	RetryAfter          *int
	XRateLimitLimit     *int
	XRateLimitRemaining *int
	XRateLimitReset     *time.Time
}

type GetListingPublishStatusClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingPublishStatusResponse
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *TooManyRequests
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *GetListingPublishStatusClientResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetListingPublishStatusClientResponse) GetJSON200() *ListingPublishStatusResponse {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetListingPublishStatusClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetListingPublishStatusClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r GetListingPublishStatusClientResponse) GetJSON429() *TooManyRequests {
	return r.JSON429
}

// GetBody returns the raw response body bytes
func (r GetListingPublishStatusClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetListingPublishStatusClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetListingPublishStatusClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetListingPublishStatusClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type PublishListingToAirbnbClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingPublishAirbnbResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON402 the response for an HTTP 402 `application/json` response
	JSON402 *PublishBillingRefused
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r PublishListingToAirbnbClientResponse) GetJSON200() *ListingPublishAirbnbResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r PublishListingToAirbnbClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON402 returns the response for an HTTP 402 `application/json` response
func (r PublishListingToAirbnbClientResponse) GetJSON402() *PublishBillingRefused {
	return r.JSON402
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r PublishListingToAirbnbClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r PublishListingToAirbnbClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r PublishListingToAirbnbClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PublishListingToAirbnbClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PublishListingToAirbnbClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PublishListingToAirbnbClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type PublishListingToBookingClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingPublishBookingResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON402 the response for an HTTP 402 `application/json` response
	JSON402 *PublishBillingRefused
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r PublishListingToBookingClientResponse) GetJSON200() *ListingPublishBookingResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r PublishListingToBookingClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON402 returns the response for an HTTP 402 `application/json` response
func (r PublishListingToBookingClientResponse) GetJSON402() *PublishBillingRefused {
	return r.JSON402
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r PublishListingToBookingClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r PublishListingToBookingClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r PublishListingToBookingClientResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetBody returns the raw response body bytes
func (r PublishListingToBookingClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PublishListingToBookingClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PublishListingToBookingClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PublishListingToBookingClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// PullListingFromAirbnbClientResponse429Headers the declared response headers of an HTTP 429 response for PullListingFromAirbnb
type PullListingFromAirbnbClientResponse429Headers struct {
	RetryAfter          *int
	XRateLimitLimit     *int
	XRateLimitRemaining *int
	XRateLimitReset     *time.Time
}

type PullListingFromAirbnbClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingPullResponse
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *TooManyRequests
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
	// Headers429 the parsed response headers for an HTTP 429 response
	Headers429 *PullListingFromAirbnbClientResponse429Headers
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r PullListingFromAirbnbClientResponse) GetJSON200() *ListingPullResponse {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r PullListingFromAirbnbClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r PullListingFromAirbnbClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r PullListingFromAirbnbClientResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r PullListingFromAirbnbClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r PullListingFromAirbnbClientResponse) GetJSON429() *TooManyRequests {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r PullListingFromAirbnbClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r PullListingFromAirbnbClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PullListingFromAirbnbClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PullListingFromAirbnbClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PullListingFromAirbnbClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetListingSegmentsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ListingSegmentsResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetListingSegmentsClientResponse) GetJSON200() *ListingSegmentsResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetListingSegmentsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetListingSegmentsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetListingSegmentsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetListingSegmentsClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r GetListingSegmentsClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r GetListingSegmentsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetListingSegmentsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetListingSegmentsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetListingSegmentsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListMarketsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *MarketsOverviewResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListMarketsClientResponse) GetJSON200() *MarketsOverviewResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListMarketsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r ListMarketsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListMarketsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListMarketsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListMarketsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListMarketBrowseClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *MarketBrowseResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListMarketBrowseClientResponse) GetJSON200() *MarketBrowseResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListMarketBrowseClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r ListMarketBrowseClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r ListMarketBrowseClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListMarketBrowseClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListMarketBrowseClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListMarketBrowseClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetMarketClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *MarketDetailResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetMarketClientResponse) GetJSON200() *MarketDetailResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetMarketClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r GetMarketClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetMarketClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetMarketClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetMarketClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetMarketCalendarClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *MarketCalendarResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetMarketCalendarClientResponse) GetJSON200() *MarketCalendarResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetMarketCalendarClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetBody returns the raw response body bytes
func (r GetMarketCalendarClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetMarketCalendarClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetMarketCalendarClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetMarketCalendarClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListMigrationsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data       *[]Migration `json:"data,omitempty"`
		Pagination *struct {
			HasMore    *bool   `json:"hasMore,omitempty"`
			NextCursor *string `json:"nextCursor,omitempty"`
			Total      *int    `json:"total,omitempty"`
		} `json:"pagination,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListMigrationsClientResponse) GetJSON200() *struct {
	Data       *[]Migration `json:"data,omitempty"`
	Pagination *struct {
		HasMore    *bool   `json:"hasMore,omitempty"`
		NextCursor *string `json:"nextCursor,omitempty"`
		Total      *int    `json:"total,omitempty"`
	} `json:"pagination,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListMigrationsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListMigrationsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListMigrationsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListMigrationsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListMigrationsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListMigrationsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListMigrationsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteMigrationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data *struct {
			Deactivated   *bool      `json:"deactivated,omitempty"`
			DeactivatedAt *time.Time `json:"deactivatedAt,omitempty"`
			WorkspaceId   *string    `json:"workspaceId,omitempty"`
		} `json:"data,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DeleteMigrationClientResponse) GetJSON200() *struct {
	Data *struct {
		Deactivated   *bool      `json:"deactivated,omitempty"`
		DeactivatedAt *time.Time `json:"deactivatedAt,omitempty"`
		WorkspaceId   *string    `json:"workspaceId,omitempty"`
	} `json:"data,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteMigrationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeleteMigrationClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r DeleteMigrationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r DeleteMigrationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteMigrationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteMigrationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteMigrationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetMigrationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Data One migration: a property manager moved through Repull Migrate, living in its own workspace.
		Data *Migration `json:"data,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetMigrationClientResponse) GetJSON200() *struct {
	// Data One migration: a property manager moved through Repull Migrate, living in its own workspace.
	Data *Migration `json:"data,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetMigrationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetMigrationClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetMigrationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetMigrationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetMigrationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetMigrationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetMigrationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetMigrationChannelMapClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data *MigrationChannelMap `json:"data,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetMigrationChannelMapClientResponse) GetJSON200() *struct {
	Data *MigrationChannelMap `json:"data,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetMigrationChannelMapClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetMigrationChannelMapClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetMigrationChannelMapClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetMigrationChannelMapClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetMigrationChannelMapClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetMigrationChannelMapClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetMigrationChannelMapClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CutoverMigrationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		// Data One migration: a property manager moved through Repull Migrate, living in its own workspace.
		Data *Migration `json:"data,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CutoverMigrationClientResponse) GetJSON200() *struct {
	// Data One migration: a property manager moved through Repull Migrate, living in its own workspace.
	Data *Migration `json:"data,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CutoverMigrationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CutoverMigrationClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CutoverMigrationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r CutoverMigrationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CutoverMigrationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CutoverMigrationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CutoverMigrationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CheckMigrationCutoverClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data *MigrationCutoverCheck `json:"data,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r CheckMigrationCutoverClientResponse) GetJSON200() *struct {
	Data *MigrationCutoverCheck `json:"data,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CheckMigrationCutoverClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CheckMigrationCutoverClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r CheckMigrationCutoverClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CheckMigrationCutoverClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r CheckMigrationCutoverClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CheckMigrationCutoverClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CheckMigrationCutoverClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CheckMigrationCutoverClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type RunMigrationImportClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON202 the response for an HTTP 202 `application/json` response
	JSON202 *struct {
		Data *struct {
			Queued *[]struct {
				ConnectionId *string `json:"connectionId,omitempty"`
				Error        *string `json:"error,omitempty"`
				Provider     *string `json:"provider,omitempty"`
				Queued       *bool   `json:"queued,omitempty"`
			} `json:"queued,omitempty"`
			WorkspaceId *string `json:"workspaceId,omitempty"`
		} `json:"data,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Conflict
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON202 returns the response for an HTTP 202 `application/json` response
func (r RunMigrationImportClientResponse) GetJSON202() *struct {
	Data *struct {
		Queued *[]struct {
			ConnectionId *string `json:"connectionId,omitempty"`
			Error        *string `json:"error,omitempty"`
			Provider     *string `json:"provider,omitempty"`
			Queued       *bool   `json:"queued,omitempty"`
		} `json:"queued,omitempty"`
		WorkspaceId *string `json:"workspaceId,omitempty"`
	} `json:"data,omitempty"`
} {
	return r.JSON202
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r RunMigrationImportClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r RunMigrationImportClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r RunMigrationImportClientResponse) GetJSON409() *Conflict {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r RunMigrationImportClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r RunMigrationImportClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r RunMigrationImportClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r RunMigrationImportClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r RunMigrationImportClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r RunMigrationImportClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetMigrationReportClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data *MigrationReport `json:"data,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetMigrationReportClientResponse) GetJSON200() *struct {
	Data *MigrationReport `json:"data,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetMigrationReportClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetMigrationReportClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetMigrationReportClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetMigrationReportClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetMigrationReportClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetMigrationReportClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetMigrationReportClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListPropertiesClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *PropertyListResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListPropertiesClientResponse) GetJSON200() *PropertyListResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListPropertiesClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListPropertiesClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r ListPropertiesClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListPropertiesClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListPropertiesClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListPropertiesClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetPropertyClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Property
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetPropertyClientResponse) GetJSON200() *Property {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetPropertyClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetPropertyClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetPropertyClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r GetPropertyClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetPropertyClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetPropertyClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetPropertyClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetQuoteClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Quote
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetQuoteClientResponse) GetJSON200() *Quote {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetQuoteClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetQuoteClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetQuoteClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetQuoteClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r GetQuoteClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetQuoteClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetQuoteClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetQuoteClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListReservationsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ReservationListResponse
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListReservationsClientResponse) GetJSON200() *ReservationListResponse {
	return r.JSON200
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListReservationsClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListReservationsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r ListReservationsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListReservationsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListReservationsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListReservationsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateReservationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *ReservationCreateResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateReservationClientResponse) GetJSON201() *ReservationCreateResponse {
	return r.JSON201
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateReservationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateReservationClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r CreateReservationClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r CreateReservationClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r CreateReservationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r CreateReservationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateReservationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateReservationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateReservationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetReservationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Reservation
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetReservationClientResponse) GetJSON200() *Reservation {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetReservationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetReservationClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetReservationClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetReservationClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetReservationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetReservationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetReservationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetReservationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetReservationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateReservationClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ReservationUpdateResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *ListingInactive
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateReservationClientResponse) GetJSON200() *ReservationUpdateResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateReservationClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateReservationClientResponse) GetJSON403() *ListingInactive {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateReservationClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateReservationClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r UpdateReservationClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r UpdateReservationClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateReservationClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateReservationClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateReservationClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type AcceptReservationRequestClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Action  AcceptReservationRequest200JSONResponseBodyAction  `json:"action"`
		Channel AcceptReservationRequest200JSONResponseBodyChannel `json:"channel"`

		// ConfirmationCode Example: HM9J2MFR3W
		ConfirmationCode string                                                    `json:"confirmationCode"`
		DeclineReason    *AcceptReservationRequest200JSONResponseBodyDeclineReason `json:"declineReason"`

		// ReservationId Example: 236354
		ReservationId string `json:"reservationId"`

		// Status What Airbnb was asked to do and did not refuse. The reservation itself moves when Airbnb’s own notification lands, usually within seconds — that is when `reservation.request.updated` fires (`requestStatus` `accepted` or `declined`), plus `reservation.created` for an accepted request.
		Status AcceptReservationRequest200JSONResponseBodyStatus `json:"status"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r AcceptReservationRequestClientResponse) GetJSON200() *struct {
	Action  AcceptReservationRequest200JSONResponseBodyAction  `json:"action"`
	Channel AcceptReservationRequest200JSONResponseBodyChannel `json:"channel"`

	// ConfirmationCode Example: HM9J2MFR3W
	ConfirmationCode string                                                    `json:"confirmationCode"`
	DeclineReason    *AcceptReservationRequest200JSONResponseBodyDeclineReason `json:"declineReason"`

	// ReservationId Example: 236354
	ReservationId string `json:"reservationId"`

	// Status What Airbnb was asked to do and did not refuse. The reservation itself moves when Airbnb’s own notification lands, usually within seconds — that is when `reservation.request.updated` fires (`requestStatus` `accepted` or `declined`), plus `reservation.created` for an accepted request.
	Status AcceptReservationRequest200JSONResponseBodyStatus `json:"status"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r AcceptReservationRequestClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r AcceptReservationRequestClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r AcceptReservationRequestClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r AcceptReservationRequestClientResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r AcceptReservationRequestClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r AcceptReservationRequestClientResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r AcceptReservationRequestClientResponse) GetJSON500() *Error {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r AcceptReservationRequestClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r AcceptReservationRequestClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r AcceptReservationRequestClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r AcceptReservationRequestClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r AcceptReservationRequestClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeclineReservationRequestClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Action  DeclineReservationRequest200JSONResponseBodyAction  `json:"action"`
		Channel DeclineReservationRequest200JSONResponseBodyChannel `json:"channel"`

		// ConfirmationCode Example: HM9J2MFR3W
		ConfirmationCode string                                                     `json:"confirmationCode"`
		DeclineReason    *DeclineReservationRequest200JSONResponseBodyDeclineReason `json:"declineReason"`

		// ReservationId Example: 236354
		ReservationId string `json:"reservationId"`

		// Status What Airbnb was asked to do and did not refuse. The reservation itself moves when Airbnb’s own notification lands, usually within seconds — that is when `reservation.request.updated` fires (`requestStatus` `accepted` or `declined`), plus `reservation.created` for an accepted request.
		Status DeclineReservationRequest200JSONResponseBodyStatus `json:"status"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *Error
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *Error
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *Error
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DeclineReservationRequestClientResponse) GetJSON200() *struct {
	Action  DeclineReservationRequest200JSONResponseBodyAction  `json:"action"`
	Channel DeclineReservationRequest200JSONResponseBodyChannel `json:"channel"`

	// ConfirmationCode Example: HM9J2MFR3W
	ConfirmationCode string                                                     `json:"confirmationCode"`
	DeclineReason    *DeclineReservationRequest200JSONResponseBodyDeclineReason `json:"declineReason"`

	// ReservationId Example: 236354
	ReservationId string `json:"reservationId"`

	// Status What Airbnb was asked to do and did not refuse. The reservation itself moves when Airbnb’s own notification lands, usually within seconds — that is when `reservation.request.updated` fires (`requestStatus` `accepted` or `declined`), plus `reservation.created` for an accepted request.
	Status DeclineReservationRequest200JSONResponseBodyStatus `json:"status"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeclineReservationRequestClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r DeclineReservationRequestClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeclineReservationRequestClientResponse) GetJSON404() *Error {
	return r.JSON404
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r DeclineReservationRequestClientResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r DeclineReservationRequestClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r DeclineReservationRequestClientResponse) GetJSON429() *Error {
	return r.JSON429
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r DeclineReservationRequestClientResponse) GetJSON500() *Error {
	return r.JSON500
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r DeclineReservationRequestClientResponse) GetJSON502() *Error {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r DeclineReservationRequestClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeclineReservationRequestClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeclineReservationRequestClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeclineReservationRequestClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListReviewsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *ReviewListResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListReviewsClientResponse) GetJSON200() *ReviewListResponse {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r ListReviewsClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListReviewsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListReviewsClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ListReviewsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r ListReviewsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r ListReviewsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListReviewsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListReviewsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListReviewsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetReviewClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *Review
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetReviewClientResponse) GetJSON200() *Review {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r GetReviewClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetReviewClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetReviewClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetReviewClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetReviewClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetReviewClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetReviewClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetReviewClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetReviewClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ReplyToReviewClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *struct {
		Id       *string `json:"id,omitempty"`
		Platform *string `json:"platform,omitempty"`
		Response *string `json:"response,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *AirbnbWriteRejected
	// JSON429 the response for an HTTP 429 `application/json` response
	JSON429 *AirbnbRateLimited
	// JSON502 the response for an HTTP 502 `application/json` response
	JSON502 *AirbnbUpstreamError
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r ReplyToReviewClientResponse) GetJSON201() *struct {
	Id       *string `json:"id,omitempty"`
	Platform *string `json:"platform,omitempty"`
	Response *string `json:"response,omitempty"`
} {
	return r.JSON201
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ReplyToReviewClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ReplyToReviewClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r ReplyToReviewClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ReplyToReviewClientResponse) GetJSON422() *AirbnbWriteRejected {
	return r.JSON422
}

// GetJSON429 returns the response for an HTTP 429 `application/json` response
func (r ReplyToReviewClientResponse) GetJSON429() *AirbnbRateLimited {
	return r.JSON429
}

// GetJSON502 returns the response for an HTTP 502 `application/json` response
func (r ReplyToReviewClientResponse) GetJSON502() *AirbnbUpstreamError {
	return r.JSON502
}

// GetBody returns the raw response body bytes
func (r ReplyToReviewClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ReplyToReviewClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ReplyToReviewClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ReplyToReviewClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListCustomSchemasClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *CustomSchemaListResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListCustomSchemasClientResponse) GetJSON200() *CustomSchemaListResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r ListCustomSchemasClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ListCustomSchemasClientResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetBody returns the raw response body bytes
func (r ListCustomSchemasClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListCustomSchemasClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListCustomSchemasClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListCustomSchemasClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateCustomSchemaClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *CustomSchemaCreateResponse
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateCustomSchemaClientResponse) GetJSON201() *CustomSchemaCreateResponse {
	return r.JSON201
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r CreateCustomSchemaClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r CreateCustomSchemaClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r CreateCustomSchemaClientResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r CreateCustomSchemaClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r CreateCustomSchemaClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateCustomSchemaClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateCustomSchemaClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateCustomSchemaClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteCustomSchemaClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *CustomSchemaDeleteResponse
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r DeleteCustomSchemaClientResponse) GetJSON200() *CustomSchemaDeleteResponse {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r DeleteCustomSchemaClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r DeleteCustomSchemaClientResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r DeleteCustomSchemaClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r DeleteCustomSchemaClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteCustomSchemaClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteCustomSchemaClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteCustomSchemaClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetCustomSchemaClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *CustomSchema
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetCustomSchemaClientResponse) GetJSON200() *CustomSchema {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetCustomSchemaClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r GetCustomSchemaClientResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r GetCustomSchemaClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetBody returns the raw response body bytes
func (r GetCustomSchemaClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetCustomSchemaClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetCustomSchemaClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetCustomSchemaClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateCustomSchemaClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *CustomSchema
	// JSON400 the response for an HTTP 400 `application/json` response
	JSON400 *BadRequest
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Forbidden
	// JSON404 the response for an HTTP 404 `application/json` response
	JSON404 *NotFound
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateCustomSchemaClientResponse) GetJSON200() *CustomSchema {
	return r.JSON200
}

// GetJSON400 returns the response for an HTTP 400 `application/json` response
func (r UpdateCustomSchemaClientResponse) GetJSON400() *BadRequest {
	return r.JSON400
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r UpdateCustomSchemaClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r UpdateCustomSchemaClientResponse) GetJSON403() *Forbidden {
	return r.JSON403
}

// GetJSON404 returns the response for an HTTP 404 `application/json` response
func (r UpdateCustomSchemaClientResponse) GetJSON404() *NotFound {
	return r.JSON404
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r UpdateCustomSchemaClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r UpdateCustomSchemaClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateCustomSchemaClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateCustomSchemaClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateCustomSchemaClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetUsageLogsClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Data *[]struct {
			CreatedAt     *time.Time `json:"createdAt,omitempty"`
			ErrorCode     *string    `json:"errorCode,omitempty"`
			Id            *string    `json:"id,omitempty"`
			IpAddress     *string    `json:"ipAddress,omitempty"`
			LatencyMs     *int       `json:"latencyMs,omitempty"`
			Method        *string    `json:"method,omitempty"`
			OperationId   *string    `json:"operationId,omitempty"`
			Path          *string    `json:"path,omitempty"`
			RequestBytes  *int       `json:"requestBytes,omitempty"`
			RequestId     *string    `json:"requestId,omitempty"`
			ResponseBytes *int       `json:"responseBytes,omitempty"`
			StatusCode    *int       `json:"statusCode,omitempty"`
			UserAgent     *string    `json:"userAgent,omitempty"`
		} `json:"data,omitempty"`
		Pagination *struct {
			HasMore    *bool   `json:"hasMore,omitempty"`
			NextCursor *string `json:"nextCursor,omitempty"`
			Total      *int    `json:"total,omitempty"`
		} `json:"pagination,omitempty"`
		Range *string `json:"range,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *UnprocessableEntity
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetUsageLogsClientResponse) GetJSON200() *struct {
	Data *[]struct {
		CreatedAt     *time.Time `json:"createdAt,omitempty"`
		ErrorCode     *string    `json:"errorCode,omitempty"`
		Id            *string    `json:"id,omitempty"`
		IpAddress     *string    `json:"ipAddress,omitempty"`
		LatencyMs     *int       `json:"latencyMs,omitempty"`
		Method        *string    `json:"method,omitempty"`
		OperationId   *string    `json:"operationId,omitempty"`
		Path          *string    `json:"path,omitempty"`
		RequestBytes  *int       `json:"requestBytes,omitempty"`
		RequestId     *string    `json:"requestId,omitempty"`
		ResponseBytes *int       `json:"responseBytes,omitempty"`
		StatusCode    *int       `json:"statusCode,omitempty"`
		UserAgent     *string    `json:"userAgent,omitempty"`
	} `json:"data,omitempty"`
	Pagination *struct {
		HasMore    *bool   `json:"hasMore,omitempty"`
		NextCursor *string `json:"nextCursor,omitempty"`
		Total      *int    `json:"total,omitempty"`
	} `json:"pagination,omitempty"`
	Range *string `json:"range,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetUsageLogsClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r GetUsageLogsClientResponse) GetJSON422() *UnprocessableEntity {
	return r.JSON422
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetUsageLogsClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetUsageLogsClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetUsageLogsClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetUsageLogsClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetUsageLogsClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetUsageSummaryClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Breakdown *[]struct {
			AvgLatencyMs *int     `json:"avgLatencyMs,omitempty"`
			ErrorCount   *int     `json:"errorCount,omitempty"`
			ErrorRate    *float32 `json:"errorRate,omitempty"`
			OperationId  *string  `json:"operationId,omitempty"`
			RequestCount *int     `json:"requestCount,omitempty"`
		} `json:"breakdown,omitempty"`
		DailyResetsAt *time.Time `json:"dailyResetsAt,omitempty"`
		Limits        *struct {
			DailyAiRequests *int `json:"dailyAiRequests,omitempty"`
			DailyRequests   *int `json:"dailyRequests,omitempty"`
			MonthlyRequests *int `json:"monthlyRequests,omitempty"`
		} `json:"limits,omitempty"`
		Range     *string `json:"range,omitempty"`
		Remaining *struct {
			Daily   *int `json:"daily,omitempty"`
			DailyAi *int `json:"dailyAi,omitempty"`
			Monthly *int `json:"monthly,omitempty"`
		} `json:"remaining,omitempty"`
		ResetsAt           *time.Time `json:"resetsAt,omitempty"`
		StatusDistribution *struct {
			N2xx *int `json:"2xx,omitempty"`
			N3xx *int `json:"3xx,omitempty"`
			N4xx *int `json:"4xx,omitempty"`
			N5xx *int `json:"5xx,omitempty"`
		} `json:"statusDistribution,omitempty"`
		Tier     *string `json:"tier,omitempty"`
		Timeline *[]struct {
			Day          *string `json:"day,omitempty"`
			ErrorCount   *int    `json:"errorCount,omitempty"`
			RequestCount *int    `json:"requestCount,omitempty"`
		} `json:"timeline,omitempty"`
		Totals *struct {
			AvgLatencyMs *int `json:"avgLatencyMs,omitempty"`
			Errors       *int `json:"errors,omitempty"`
			Requests     *int `json:"requests,omitempty"`
		} `json:"totals,omitempty"`
		Used *struct {
			Daily   *int `json:"daily,omitempty"`
			DailyAi *int `json:"dailyAi,omitempty"`
			Monthly *int `json:"monthly,omitempty"`
		} `json:"used,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetUsageSummaryClientResponse) GetJSON200() *struct {
	Breakdown *[]struct {
		AvgLatencyMs *int     `json:"avgLatencyMs,omitempty"`
		ErrorCount   *int     `json:"errorCount,omitempty"`
		ErrorRate    *float32 `json:"errorRate,omitempty"`
		OperationId  *string  `json:"operationId,omitempty"`
		RequestCount *int     `json:"requestCount,omitempty"`
	} `json:"breakdown,omitempty"`
	DailyResetsAt *time.Time `json:"dailyResetsAt,omitempty"`
	Limits        *struct {
		DailyAiRequests *int `json:"dailyAiRequests,omitempty"`
		DailyRequests   *int `json:"dailyRequests,omitempty"`
		MonthlyRequests *int `json:"monthlyRequests,omitempty"`
	} `json:"limits,omitempty"`
	Range     *string `json:"range,omitempty"`
	Remaining *struct {
		Daily   *int `json:"daily,omitempty"`
		DailyAi *int `json:"dailyAi,omitempty"`
		Monthly *int `json:"monthly,omitempty"`
	} `json:"remaining,omitempty"`
	ResetsAt           *time.Time `json:"resetsAt,omitempty"`
	StatusDistribution *struct {
		N2xx *int `json:"2xx,omitempty"`
		N3xx *int `json:"3xx,omitempty"`
		N4xx *int `json:"4xx,omitempty"`
		N5xx *int `json:"5xx,omitempty"`
	} `json:"statusDistribution,omitempty"`
	Tier     *string `json:"tier,omitempty"`
	Timeline *[]struct {
		Day          *string `json:"day,omitempty"`
		ErrorCount   *int    `json:"errorCount,omitempty"`
		RequestCount *int    `json:"requestCount,omitempty"`
	} `json:"timeline,omitempty"`
	Totals *struct {
		AvgLatencyMs *int `json:"avgLatencyMs,omitempty"`
		Errors       *int `json:"errors,omitempty"`
		Requests     *int `json:"requests,omitempty"`
	} `json:"totals,omitempty"`
	Used *struct {
		Daily   *int `json:"daily,omitempty"`
		DailyAi *int `json:"dailyAi,omitempty"`
		Monthly *int `json:"monthly,omitempty"`
	} `json:"used,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetUsageSummaryClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetUsageSummaryClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetUsageSummaryClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetUsageSummaryClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetUsageSummaryClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetUsageSummaryClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetUsageTierClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		DailyResetsAt *time.Time `json:"dailyResetsAt,omitempty"`
		Limits        *struct {
			DailyAiRequests        *int `json:"dailyAiRequests,omitempty"`
			DailyRequests          *int `json:"dailyRequests,omitempty"`
			DynamicPricingListings *int `json:"dynamicPricingListings,omitempty"`
			MonthlyRequests        *int `json:"monthlyRequests,omitempty"`
		} `json:"limits,omitempty"`
		Remaining *struct {
			Daily                  *int `json:"daily,omitempty"`
			DailyAi                *int `json:"dailyAi,omitempty"`
			DynamicPricingListings *int `json:"dynamicPricingListings,omitempty"`
			Monthly                *int `json:"monthly,omitempty"`
		} `json:"remaining,omitempty"`
		ResetsAt *time.Time `json:"resetsAt,omitempty"`
		Tier     *string    `json:"tier,omitempty"`
		Used     *struct {
			Daily                  *int `json:"daily,omitempty"`
			DailyAi                *int `json:"dailyAi,omitempty"`
			DynamicPricingListings *int `json:"dynamicPricingListings,omitempty"`
			Monthly                *int `json:"monthly,omitempty"`
		} `json:"used,omitempty"`
	}
	// JSON401 the response for an HTTP 401 `application/json` response
	JSON401 *Unauthorized
	// JSON500 the response for an HTTP 500 `application/json` response
	JSON500 *InternalError
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetUsageTierClientResponse) GetJSON200() *struct {
	DailyResetsAt *time.Time `json:"dailyResetsAt,omitempty"`
	Limits        *struct {
		DailyAiRequests        *int `json:"dailyAiRequests,omitempty"`
		DailyRequests          *int `json:"dailyRequests,omitempty"`
		DynamicPricingListings *int `json:"dynamicPricingListings,omitempty"`
		MonthlyRequests        *int `json:"monthlyRequests,omitempty"`
	} `json:"limits,omitempty"`
	Remaining *struct {
		Daily                  *int `json:"daily,omitempty"`
		DailyAi                *int `json:"dailyAi,omitempty"`
		DynamicPricingListings *int `json:"dynamicPricingListings,omitempty"`
		Monthly                *int `json:"monthly,omitempty"`
	} `json:"remaining,omitempty"`
	ResetsAt *time.Time `json:"resetsAt,omitempty"`
	Tier     *string    `json:"tier,omitempty"`
	Used     *struct {
		Daily                  *int `json:"daily,omitempty"`
		DailyAi                *int `json:"dailyAi,omitempty"`
		DynamicPricingListings *int `json:"dynamicPricingListings,omitempty"`
		Monthly                *int `json:"monthly,omitempty"`
	} `json:"used,omitempty"`
} {
	return r.JSON200
}

// GetJSON401 returns the response for an HTTP 401 `application/json` response
func (r GetUsageTierClientResponse) GetJSON401() *Unauthorized {
	return r.JSON401
}

// GetJSON500 returns the response for an HTTP 500 `application/json` response
func (r GetUsageTierClientResponse) GetJSON500() *InternalError {
	return r.JSON500
}

// GetBody returns the raw response body bytes
func (r GetUsageTierClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetUsageTierClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetUsageTierClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetUsageTierClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListWebhooksClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *WebhookListResponse
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListWebhooksClientResponse) GetJSON200() *WebhookListResponse {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r ListWebhooksClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListWebhooksClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListWebhooksClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListWebhooksClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type CreateWebhookClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON201 the response for an HTTP 201 `application/json` response
	JSON201 *WebhookSubscription
}

// GetJSON201 returns the response for an HTTP 201 `application/json` response
func (r CreateWebhookClientResponse) GetJSON201() *WebhookSubscription {
	return r.JSON201
}

// GetBody returns the raw response body bytes
func (r CreateWebhookClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r CreateWebhookClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r CreateWebhookClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r CreateWebhookClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListWebhookEventTypesClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *WebhookEventCatalog
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListWebhookEventTypesClientResponse) GetJSON200() *WebhookEventCatalog {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r ListWebhookEventTypesClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListWebhookEventTypesClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListWebhookEventTypesClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListWebhookEventTypesClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type TestWebhookClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
}

// GetBody returns the raw response body bytes
func (r TestWebhookClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r TestWebhookClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r TestWebhookClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r TestWebhookClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type DeleteWebhookClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
}

// GetBody returns the raw response body bytes
func (r DeleteWebhookClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r DeleteWebhookClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r DeleteWebhookClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r DeleteWebhookClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetWebhookClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *WebhookSubscription
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetWebhookClientResponse) GetJSON200() *WebhookSubscription {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r GetWebhookClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetWebhookClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetWebhookClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetWebhookClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type UpdateWebhookClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *WebhookSubscription
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r UpdateWebhookClientResponse) GetJSON200() *WebhookSubscription {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r UpdateWebhookClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r UpdateWebhookClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r UpdateWebhookClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r UpdateWebhookClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ListWebhookDeliveriesClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *WebhookDeliveryListResponse
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r ListWebhookDeliveriesClientResponse) GetJSON200() *WebhookDeliveryListResponse {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r ListWebhookDeliveriesClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ListWebhookDeliveriesClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ListWebhookDeliveriesClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ListWebhookDeliveriesClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type GetWebhookDeliveryClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *WebhookDeliveryDetail
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r GetWebhookDeliveryClientResponse) GetJSON200() *WebhookDeliveryDetail {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r GetWebhookDeliveryClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r GetWebhookDeliveryClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r GetWebhookDeliveryClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r GetWebhookDeliveryClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type ReplayWebhookDeliveryClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON403 the response for an HTTP 403 `application/json` response
	JSON403 *Error
	// JSON409 the response for an HTTP 409 `application/json` response
	JSON409 *Error
	// JSON422 the response for an HTTP 422 `application/json` response
	JSON422 *Error
}

// GetJSON403 returns the response for an HTTP 403 `application/json` response
func (r ReplayWebhookDeliveryClientResponse) GetJSON403() *Error {
	return r.JSON403
}

// GetJSON409 returns the response for an HTTP 409 `application/json` response
func (r ReplayWebhookDeliveryClientResponse) GetJSON409() *Error {
	return r.JSON409
}

// GetJSON422 returns the response for an HTTP 422 `application/json` response
func (r ReplayWebhookDeliveryClientResponse) GetJSON422() *Error {
	return r.JSON422
}

// GetBody returns the raw response body bytes
func (r ReplayWebhookDeliveryClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r ReplayWebhookDeliveryClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r ReplayWebhookDeliveryClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r ReplayWebhookDeliveryClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type PingWebhookClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
}

// GetBody returns the raw response body bytes
func (r PingWebhookClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r PingWebhookClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PingWebhookClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r PingWebhookClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type RotateWebhookSecretClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	// JSON200 the response for an HTTP 200 `application/json` response
	JSON200 *struct {
		Id        *string    `json:"id,omitempty"`
		RotatedAt *time.Time `json:"rotatedAt,omitempty"`
		Secret    *string    `json:"secret,omitempty"`
	}
}

// GetJSON200 returns the response for an HTTP 200 `application/json` response
func (r RotateWebhookSecretClientResponse) GetJSON200() *struct {
	Id        *string    `json:"id,omitempty"`
	RotatedAt *time.Time `json:"rotatedAt,omitempty"`
	Secret    *string    `json:"secret,omitempty"`
} {
	return r.JSON200
}

// GetBody returns the raw response body bytes
func (r RotateWebhookSecretClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r RotateWebhookSecretClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r RotateWebhookSecretClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r RotateWebhookSecretClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

type TestFireWebhookClientResponse struct {
	Body         []byte
	HTTPResponse *http.Response
}

// GetBody returns the raw response body bytes
func (r TestFireWebhookClientResponse) GetBody() []byte {
	return r.Body
}

// Status returns HTTPResponse.Status
func (r TestFireWebhookClientResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r TestFireWebhookClientResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// ContentType is a convenience method to retrieve the Content-Type value from the HTTP response headers
func (r TestFireWebhookClientResponse) ContentType() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Header.Get("Content-Type")
	}
	return ""
}

// BatchUpdateAvailabilityWithBodyWithResponse Update availability across many properties
//
// Applies ONE settings object across up to 500 properties and pushes the result to every connected channel.
//
// Ownership is checked before anything is written: a batch containing a property from another workspace is refused as a whole and names the offending ids, rather than being partially applied.
//
// Per-property *different* values are separate calls — presenting them as one request would be a false claim about atomicity.
//
// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/availability/batch (the `BatchUpdateAvailability` operationId).
func (c *ClientWithResponses) BatchUpdateAvailabilityWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BatchUpdateAvailabilityClientResponse, error) {
	rsp, err := c.BatchUpdateAvailabilityWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBatchUpdateAvailabilityClientResponse(rsp)
}

// BatchUpdateAvailabilityWithResponse Update availability across many properties
//
// Applies ONE settings object across up to 500 properties and pushes the result to every connected channel.
//
// Ownership is checked before anything is written: a batch containing a property from another workspace is refused as a whole and names the offending ids, rather than being partially applied.
//
// Per-property *different* values are separate calls — presenting them as one request would be a false claim about atomicity.
//
// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/availability/batch (the `BatchUpdateAvailability` operationId).
func (c *ClientWithResponses) BatchUpdateAvailabilityWithResponse(ctx context.Context, body BatchUpdateAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*BatchUpdateAvailabilityClientResponse, error) {
	rsp, err := c.BatchUpdateAvailability(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBatchUpdateAvailabilityClientResponse(rsp)
}

// GetAvailabilityWithResponse Get property availability
//
// Channel-agnostic day-by-day availability calendar for a property over a date window. Returns a thin per-date shape — `{ date, available, price, minNights }` — projected from the property calendar.
//
// The `from` and `to` query params are **required** (ISO `YYYY-MM-DD`, inclusive) — omitting or malforming either returns 422. The window is capped at 366 days; longer ranges are truncated to the first 366 days.
//
// **`days` contains only the dates we actually hold calendar data for.** Requested dates with no calendar row are listed in `coverage.missingDates` — their availability is unknown. Never treat a missing date as bookable: this endpoint deliberately does not synthesise availability, because a fabricated open date can be double-booked. A property with no calendar still returns a real 200 (`days: []`, every date in `coverage.missingDates`), never a 404 — 404 means the property id does not exist or belongs to a different workspace.
//
// This endpoint is read-only, and the projected per-date shape carries **availability, price, and min-nights only** — it does NOT expose max-stay, closed-to-arrival (CTA), closed-to-departure (CTD), or the dedicated stop-sell flag. To read or write that full restriction set on Booking.com use the channel routes: `GET`/`PUT /v1/channels/booking/availability` (with the room + rate ids from `GET /v1/channels/booking/properties/{id}/rooms`). To **write** calendar values use `PUT /v1/availability/{propertyId}` (or `PATCH /v1/availability/batch`), which updates the property calendar and pushes to its connected channels; channel-only settings stay on the channel routes.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/availability/{propertyId} (the `GetAvailability` operationId).
func (c *ClientWithResponses) GetAvailabilityWithResponse(ctx context.Context, propertyId int, params *GetAvailabilityParams, reqEditors ...RequestEditorFn) (*GetAvailabilityClientResponse, error) {
	rsp, err := c.GetAvailability(ctx, propertyId, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAvailabilityClientResponse(rsp)
}

// UpdateAvailabilityWithBodyWithResponse Set prices, block or unblock dates
//
// Writes the calendar for one property AND pushes to every connected channel in the same step. A write that only changed our copy would leave the OTA calendars stale and eventually double-book a guest.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/availability/{propertyId} (the `UpdateAvailability` operationId).
func (c *ClientWithResponses) UpdateAvailabilityWithBodyWithResponse(ctx context.Context, propertyId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAvailabilityClientResponse, error) {
	rsp, err := c.UpdateAvailabilityWithBody(ctx, propertyId, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAvailabilityClientResponse(rsp)
}

// UpdateAvailabilityWithResponse Set prices, block or unblock dates
//
// Writes the calendar for one property AND pushes to every connected channel in the same step. A write that only changed our copy would leave the OTA calendars stale and eventually double-book a guest.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/availability/{propertyId} (the `UpdateAvailability` operationId).
func (c *ClientWithResponses) UpdateAvailabilityWithResponse(ctx context.Context, propertyId int, body UpdateAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAvailabilityClientResponse, error) {
	rsp, err := c.UpdateAvailability(ctx, propertyId, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAvailabilityClientResponse(rsp)
}

// CreateBillingCheckoutWithBodyWithResponse Create checkout session
//
// Redirect user to Stripe checkout.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/billing (the `CreateBillingCheckout` operationId).
func (c *ClientWithResponses) CreateBillingCheckoutWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateBillingCheckoutClientResponse, error) {
	rsp, err := c.CreateBillingCheckoutWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateBillingCheckoutClientResponse(rsp)
}

// CreateBillingCheckoutWithResponse Create checkout session
//
// Redirect user to Stripe checkout.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/billing (the `CreateBillingCheckout` operationId).
func (c *ClientWithResponses) CreateBillingCheckoutWithResponse(ctx context.Context, body CreateBillingCheckoutJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateBillingCheckoutClientResponse, error) {
	rsp, err := c.CreateBillingCheckout(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateBillingCheckoutClientResponse(rsp)
}

// ListAirbnbAlterationsWithResponse List Airbnb alterations
//
// List reservation alteration requests for Airbnb reservations in this workspace. **Pure DB read** from the local `reservation_alterations` mirror — never calls Airbnb upstream — scoped to your workspace via the reservations join.
//
// Default returns only pending alterations; pass `?type=all` for the full history. Filter to a single reservation with `?reservation_code=<confirmation code>`. Every response carries the `dataFreshness` envelope.
//
// Each row carries the proposed change in its `new*` fields. A **listing transfer** shows up as `newListingId` (Repull listing id) and `newAirbnbListingId` (Airbnb's own id); both are `null` when the alteration does not move the reservation.
//
// Alterations of reservations on inactive listings are left out. Filtering by a reservation on an inactive listing (`reservation_code`) returns `403 listing_inactive`.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/alterations (the `ListAirbnbAlterations` operationId).
func (c *ClientWithResponses) ListAirbnbAlterationsWithResponse(ctx context.Context, params *ListAirbnbAlterationsParams, reqEditors ...RequestEditorFn) (*ListAirbnbAlterationsClientResponse, error) {
	rsp, err := c.ListAirbnbAlterations(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbAlterationsClientResponse(rsp)
}

// CreateAirbnbAlterationWithBodyWithResponse Create Airbnb alteration
//
// Propose a change to an existing Airbnb reservation: new dates, a new guest count, a new total price, or a move to a different listing. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host for the workspace, else `404 no_connection`.
//
// The body is validated before anything reaches Airbnb. `confirmation_code` is required and **at least one** of `check_in`, `check_out`, `number_of_guests`, `total_price` or `listing_id` must be sent with it — an alteration that changes nothing is `422 invalid_params`, not a request Airbnb is asked to act on. Unknown fields are refused rather than ignored, so a misspelling can never look like a successful write.
//
// **Listing transfer.** `listing_id` moves the reservation to another listing in your workspace. Send the **Repull** listing id (the `id` from `GET /v1/properties`); Repull checks you own it, that it is active and connected to Airbnb, translates it to the Airbnb listing id and sends it upstream. Callers who hold the Airbnb-side id instead may send `airbnb_listing_id`. **Airbnb decides** whether to honour a listing change on an alteration — Repull sends it and reports Airbnb’s answer; a refusal comes back as `422 airbnb_rejected` carrying Airbnb’s own message.
//
// Returns `403 listing_inactive` when the reservation’s listing, or the listing it is being moved to, is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/alterations (the `CreateAirbnbAlteration` operationId).
func (c *ClientWithResponses) CreateAirbnbAlterationWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateAirbnbAlterationClientResponse, error) {
	rsp, err := c.CreateAirbnbAlterationWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateAirbnbAlterationClientResponse(rsp)
}

// CreateAirbnbAlterationWithResponse Create Airbnb alteration
//
// Propose a change to an existing Airbnb reservation: new dates, a new guest count, a new total price, or a move to a different listing. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host for the workspace, else `404 no_connection`.
//
// The body is validated before anything reaches Airbnb. `confirmation_code` is required and **at least one** of `check_in`, `check_out`, `number_of_guests`, `total_price` or `listing_id` must be sent with it — an alteration that changes nothing is `422 invalid_params`, not a request Airbnb is asked to act on. Unknown fields are refused rather than ignored, so a misspelling can never look like a successful write.
//
// **Listing transfer.** `listing_id` moves the reservation to another listing in your workspace. Send the **Repull** listing id (the `id` from `GET /v1/properties`); Repull checks you own it, that it is active and connected to Airbnb, translates it to the Airbnb listing id and sends it upstream. Callers who hold the Airbnb-side id instead may send `airbnb_listing_id`. **Airbnb decides** whether to honour a listing change on an alteration — Repull sends it and reports Airbnb’s answer; a refusal comes back as `422 airbnb_rejected` carrying Airbnb’s own message.
//
// Returns `403 listing_inactive` when the reservation’s listing, or the listing it is being moved to, is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/alterations (the `CreateAirbnbAlteration` operationId).
func (c *ClientWithResponses) CreateAirbnbAlterationWithResponse(ctx context.Context, body CreateAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateAirbnbAlterationClientResponse, error) {
	rsp, err := c.CreateAirbnbAlteration(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateAirbnbAlterationClientResponse(rsp)
}

// GetAirbnbAlterationWithResponse Get Airbnb alteration
//
// Fetch a single Airbnb reservation alteration by its Airbnb alteration id. **Pure DB read**, workspace-scoped via the reservations join. Returns `404 not_found` when no alteration matches the id in your workspace.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/alterations/{id} (the `GetAirbnbAlteration` operationId).
func (c *ClientWithResponses) GetAirbnbAlterationWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbAlterationClientResponse, error) {
	rsp, err := c.GetAirbnbAlteration(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbAlterationClientResponse(rsp)
}

// AcceptAirbnbAlterationWithBodyWithResponse Accept Airbnb alteration
//
// Accept a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to approve the proposed date / guest-count / price change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/accept (the `AcceptAirbnbAlteration` operationId).
func (c *ClientWithResponses) AcceptAirbnbAlterationWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AcceptAirbnbAlterationClientResponse, error) {
	rsp, err := c.AcceptAirbnbAlterationWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAcceptAirbnbAlterationClientResponse(rsp)
}

// AcceptAirbnbAlterationWithResponse Accept Airbnb alteration
//
// Accept a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to approve the proposed date / guest-count / price change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/accept (the `AcceptAirbnbAlteration` operationId).
func (c *ClientWithResponses) AcceptAirbnbAlterationWithResponse(ctx context.Context, id string, body AcceptAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*AcceptAirbnbAlterationClientResponse, error) {
	rsp, err := c.AcceptAirbnbAlteration(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAcceptAirbnbAlterationClientResponse(rsp)
}

// CancelAirbnbAlterationWithBodyWithResponse Cancel Airbnb alteration
//
// Withdraw an alteration you proposed, before the other side has answered it. **Write-side** — calls Airbnb upstream (`respondToAlteration` with status `canceled`). Use this when you sent the wrong dates, guest count, price or listing: the alteration stops being pending instead of sitting there until the guest acts on it.
//
// This is the third of Airbnb's three answers to a pending alteration, alongside `accept` and `decline`, and behaves identically to them: requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Airbnb decides whether an alteration can still be withdrawn — one that has already been accepted or declined is refused upstream, and Airbnb's own reason comes back in `message`.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/cancel (the `CancelAirbnbAlteration` operationId).
func (c *ClientWithResponses) CancelAirbnbAlterationWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CancelAirbnbAlterationClientResponse, error) {
	rsp, err := c.CancelAirbnbAlterationWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCancelAirbnbAlterationClientResponse(rsp)
}

// CancelAirbnbAlterationWithResponse Cancel Airbnb alteration
//
// Withdraw an alteration you proposed, before the other side has answered it. **Write-side** — calls Airbnb upstream (`respondToAlteration` with status `canceled`). Use this when you sent the wrong dates, guest count, price or listing: the alteration stops being pending instead of sitting there until the guest acts on it.
//
// This is the third of Airbnb's three answers to a pending alteration, alongside `accept` and `decline`, and behaves identically to them: requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Airbnb decides whether an alteration can still be withdrawn — one that has already been accepted or declined is refused upstream, and Airbnb's own reason comes back in `message`.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/cancel (the `CancelAirbnbAlteration` operationId).
func (c *ClientWithResponses) CancelAirbnbAlterationWithResponse(ctx context.Context, id string, body CancelAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*CancelAirbnbAlterationClientResponse, error) {
	rsp, err := c.CancelAirbnbAlteration(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCancelAirbnbAlterationClientResponse(rsp)
}

// DeclineAirbnbAlterationWithBodyWithResponse Decline Airbnb alteration
//
// Decline a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to reject the proposed change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/decline (the `DeclineAirbnbAlteration` operationId).
func (c *ClientWithResponses) DeclineAirbnbAlterationWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*DeclineAirbnbAlterationClientResponse, error) {
	rsp, err := c.DeclineAirbnbAlterationWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeclineAirbnbAlterationClientResponse(rsp)
}

// DeclineAirbnbAlterationWithResponse Decline Airbnb alteration
//
// Decline a pending Airbnb reservation alteration. **Write-side** — calls Airbnb upstream (`respondToAlteration`) to reject the proposed change. Requires a connected Airbnb host for the workspace (else `404 no_connection`) and that the alteration id belongs to a reservation in your workspace (else `404 not_found`). No request body is required.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/alterations/{id}/decline (the `DeclineAirbnbAlteration` operationId).
func (c *ClientWithResponses) DeclineAirbnbAlterationWithResponse(ctx context.Context, id string, body DeclineAirbnbAlterationJSONRequestBody, reqEditors ...RequestEditorFn) (*DeclineAirbnbAlterationClientResponse, error) {
	rsp, err := c.DeclineAirbnbAlteration(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeclineAirbnbAlterationClientResponse(rsp)
}

// GetAirbnbConnectionWithResponse Get Airbnb connection state
//
// Returns the workspace's Airbnb host connection state in one envelope. Use this instead of inferring connection health from per-listing 401s on `GET /v1/channels/airbnb/listings` — that's noisy (every per-listing call has to fail before you know) and ambiguous (a single 5xx looks identical to a deauth).
//
// Pure DB read — does NOT touch Airbnb's API, so it's cheap to poll from a status-page surface.
//
// The response includes one row per Airbnb host the workspace has linked. Each row carries `isConnected`, `lastSyncedAt`, `deactivatedAt`, and `lastDisconnectReason` (most recent non-backfill row in `airbnb_host_events`).
//
// A self-serve `fixUrl` is included whenever `status` is anything other than `connected` — points at the dashboard where the host re-authorizes (or initiates the first OAuth flow for `never_connected` workspaces).
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/connection (the `GetAirbnbConnection` operationId).
func (c *ClientWithResponses) GetAirbnbConnectionWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetAirbnbConnectionClientResponse, error) {
	rsp, err := c.GetAirbnbConnection(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbConnectionClientResponse(rsp)
}

// ListAirbnbListingsWithResponse List Airbnb listings
//
// List every Airbnb listing this workspace has access to via the connected Airbnb account. **Pure DB read — never calls Airbnb upstream.** The connect flow is what populates the local cache; the API serves what's already there. Customers with a disconnected host still see their last-synced data, with the top-level `dataFreshness` envelope flagging the staleness and pointing at the reconnect URL.
//
// Pass `?include=amenities` to enrich each connection with its locally-cached amenity set. Returns `null` per connection when the cache is empty.
//
// Pass `?include=thumbnail` to add `thumbnailUrl` to each listing — one extra column on the query that already runs, so a selection screen renders from a single request instead of one call per listing. `null` when the listing has no thumbnail stored. Combine comma-separated, e.g. `?include=amenities,thumbnail`.
//
// **Can this listing be written to?** Every connection carries `syncCategory` — Airbnb's own per-listing API sync decision (`sync_all`, `sync_rates_and_availability`, or `none`) — and `writable`, which is `false` exactly when that category is `none`. Airbnb authorises sync one listing at a time, so a connected account can still hold listings Airbnb refuses every write to; a write to one of those returns `403 listing_not_api_connected` before anything is sent, and reconnecting the account does not change it (the host must switch the listing on in Airbnb). Check `writable` here before a portfolio-wide push instead of discovering it one 403 at a time.
//
// Inactive listings are left out; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings (the `ListAirbnbListings` operationId).
func (c *ClientWithResponses) ListAirbnbListingsWithResponse(ctx context.Context, params *ListAirbnbListingsParams, reqEditors ...RequestEditorFn) (*ListAirbnbListingsClientResponse, error) {
	rsp, err := c.ListAirbnbListings(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbListingsClientResponse(rsp)
}

// MapAirbnbListingWithBodyWithResponse Map an Airbnb listing to a Repull listing
//
// Link an existing Airbnb listing to a canonical Repull listing/property. **API-key-scoped** (unlike the Booking room mapping, which is Connect-session-scoped).
//
// Discover the `airbnbId` (+ `hostId`) via `GET /v1/channels/airbnb/listings`, then re-point it at the `listingId` of your choice — the dedup / consolidation case where the Airbnb sync auto-created its own listing but you want the inventory under an existing property.
//
// Repoints both the Airbnb record and its platform link to the target listing in one transaction. Idempotent — re-mapping to the same listing is a 200 no-op (`alreadyMapped: true`). Scope is enforced against your workspace on both the target listing and the existing Airbnb record; a listing that already links a different Airbnb listing returns 409.
//
// Returns `403 listing_inactive` when the target listing, or the listing the Airbnb listing is mapped to now, is inactive; nothing is changed.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/listings/map (the `MapAirbnbListing` operationId).
func (c *ClientWithResponses) MapAirbnbListingWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*MapAirbnbListingClientResponse, error) {
	rsp, err := c.MapAirbnbListingWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseMapAirbnbListingClientResponse(rsp)
}

// MapAirbnbListingWithResponse Map an Airbnb listing to a Repull listing
//
// Link an existing Airbnb listing to a canonical Repull listing/property. **API-key-scoped** (unlike the Booking room mapping, which is Connect-session-scoped).
//
// Discover the `airbnbId` (+ `hostId`) via `GET /v1/channels/airbnb/listings`, then re-point it at the `listingId` of your choice — the dedup / consolidation case where the Airbnb sync auto-created its own listing but you want the inventory under an existing property.
//
// Repoints both the Airbnb record and its platform link to the target listing in one transaction. Idempotent — re-mapping to the same listing is a 200 no-op (`alreadyMapped: true`). Scope is enforced against your workspace on both the target listing and the existing Airbnb record; a listing that already links a different Airbnb listing returns 409.
//
// Returns `403 listing_inactive` when the target listing, or the listing the Airbnb listing is mapped to now, is inactive; nothing is changed.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/listings/map (the `MapAirbnbListing` operationId).
func (c *ClientWithResponses) MapAirbnbListingWithResponse(ctx context.Context, body MapAirbnbListingJSONRequestBody, reqEditors ...RequestEditorFn) (*MapAirbnbListingClientResponse, error) {
	rsp, err := c.MapAirbnbListing(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseMapAirbnbListingClientResponse(rsp)
}

// GetAirbnbListingWithResponse Get Airbnb listing
//
// Fetch all Airbnb connection rows for a single Vanio listing id. A property may be linked from multiple Airbnb hosts — every match is returned. Pass `?include=amenities` to enrich each row with its current Airbnb amenities.
//
// Each row carries `syncCategory` — Airbnb's own per-listing API sync decision (`sync_all`, `sync_rates_and_availability`, or `none`) — and `writable`, which is `false` exactly when that category is `none`, meaning Airbnb refuses every write to the listing and Repull returns `403 listing_not_api_connected` without sending anything. `GET /v1/channels/airbnb/listings` reports both fields for the whole portfolio in one call.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id} (the `GetAirbnbListing` operationId).
func (c *ClientWithResponses) GetAirbnbListingWithResponse(ctx context.Context, id string, params *GetAirbnbListingParams, reqEditors ...RequestEditorFn) (*GetAirbnbListingClientResponse, error) {
	rsp, err := c.GetAirbnbListing(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbListingClientResponse(rsp)
}

// AirbnbListingActionWithBodyWithResponse Listing action (delete/push/publish/unlist/relist)
//
// Apply a state action to a listing by id. The path `id` is the canonical Repull listing id.
//
// **`delete` here never touches Airbnb. Read this before you call it.**
//
// | | `action: "delete"` (this endpoint) | `action: "unlist"` (this endpoint) |
// |---|---|---|
// | What it changes | The Repull record | The live Airbnb listing |
// | Calls Airbnb | **No. Never.** | Yes |
// | The guest-facing listing | Stays live and keeps taking bookings | **Goes down** and stops taking bookings |
// | Billing and plan limits | No longer billed, no longer counts toward the cap | Unchanged |
// | API access to the listing | `403 listing_inactive` until reactivated | Unchanged — you can still read and write it |
// | Reverse it with | `PATCH /v1/listings/{id}` `{ "active": true }` | `action: "relist"` |
// | Data kept | Yes, and it keeps syncing | Yes |
//
// Neither one deletes anything on Airbnb. **There is no endpoint on this API that deletes an Airbnb listing** — the word `delete` on this route means "deactivate the Repull record" and nothing else. (Main vanio's internal listing-sync layer has a same-named action that DOES hard-delete on Airbnb; it is not exposed here, by any endpoint, deliberately. If you have read that code, note that the two names do not mean the same thing.)
//
// `delete` is idempotent. To take a listing off the market on every channel at once — Airbnb and Booking.com together — use `POST /v1/listings/{id}/offline`.
//
// `unlist` calls Airbnb and **takes the live listing down**: it is deactivated with a valid deactivation reason and then READ BACK, so "Airbnb accepted the call but the listing is still live" is reported as a failure rather than a success. Requires `airbnbConnectionId` — a listing can be connected to more than one Airbnb listing, and taking down the wrong one is not undoable through this API. `relist` puts it back up (re-enables sync and makes the listing available again); it does not push content.
//
// `relist` goes through the channel-publish billing gate and `unlist` does not, so on a workspace whose subscription has lapsed a listing can be taken down and not put back until billing is sorted out. That refusal comes back as `402` with the action that fixes it — never as an Airbnb error, because retrying and reconnecting Airbnb do nothing for it.
//
// `push` / `publish` push the listing's content to Airbnb via the same host-side sync orchestrator as `POST /v1/listings/{id}/publish/airbnb` — pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create + publish a new one under that host. `force` re-pushes every field, ignoring dirty-field tracking. The result is per-section: see `AirbnbPublishResult`.
//
// Any other action (e.g. `pull`) returns a structured 422 naming the supported actions.
//
// Returns `403 listing_inactive` for `push`/`publish`/`unlist`/`relist` when the listing is inactive. `delete` (deactivation) is always accepted.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/listings/{id} (the `AirbnbListingAction` operationId).
func (c *ClientWithResponses) AirbnbListingActionWithBodyWithResponse(ctx context.Context, id string, params *AirbnbListingActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AirbnbListingActionClientResponse, error) {
	rsp, err := c.AirbnbListingActionWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAirbnbListingActionClientResponse(rsp)
}

// AirbnbListingActionWithResponse Listing action (delete/push/publish/unlist/relist)
//
// Apply a state action to a listing by id. The path `id` is the canonical Repull listing id.
//
// **`delete` here never touches Airbnb. Read this before you call it.**
//
// | | `action: "delete"` (this endpoint) | `action: "unlist"` (this endpoint) |
// |---|---|---|
// | What it changes | The Repull record | The live Airbnb listing |
// | Calls Airbnb | **No. Never.** | Yes |
// | The guest-facing listing | Stays live and keeps taking bookings | **Goes down** and stops taking bookings |
// | Billing and plan limits | No longer billed, no longer counts toward the cap | Unchanged |
// | API access to the listing | `403 listing_inactive` until reactivated | Unchanged — you can still read and write it |
// | Reverse it with | `PATCH /v1/listings/{id}` `{ "active": true }` | `action: "relist"` |
// | Data kept | Yes, and it keeps syncing | Yes |
//
// Neither one deletes anything on Airbnb. **There is no endpoint on this API that deletes an Airbnb listing** — the word `delete` on this route means "deactivate the Repull record" and nothing else. (Main vanio's internal listing-sync layer has a same-named action that DOES hard-delete on Airbnb; it is not exposed here, by any endpoint, deliberately. If you have read that code, note that the two names do not mean the same thing.)
//
// `delete` is idempotent. To take a listing off the market on every channel at once — Airbnb and Booking.com together — use `POST /v1/listings/{id}/offline`.
//
// `unlist` calls Airbnb and **takes the live listing down**: it is deactivated with a valid deactivation reason and then READ BACK, so "Airbnb accepted the call but the listing is still live" is reported as a failure rather than a success. Requires `airbnbConnectionId` — a listing can be connected to more than one Airbnb listing, and taking down the wrong one is not undoable through this API. `relist` puts it back up (re-enables sync and makes the listing available again); it does not push content.
//
// `relist` goes through the channel-publish billing gate and `unlist` does not, so on a workspace whose subscription has lapsed a listing can be taken down and not put back until billing is sorted out. That refusal comes back as `402` with the action that fixes it — never as an Airbnb error, because retrying and reconnecting Airbnb do nothing for it.
//
// `push` / `publish` push the listing's content to Airbnb via the same host-side sync orchestrator as `POST /v1/listings/{id}/publish/airbnb` — pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create + publish a new one under that host. `force` re-pushes every field, ignoring dirty-field tracking. The result is per-section: see `AirbnbPublishResult`.
//
// Any other action (e.g. `pull`) returns a structured 422 naming the supported actions.
//
// Returns `403 listing_inactive` for `push`/`publish`/`unlist`/`relist` when the listing is inactive. `delete` (deactivation) is always accepted.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/listings/{id} (the `AirbnbListingAction` operationId).
func (c *ClientWithResponses) AirbnbListingActionWithResponse(ctx context.Context, id string, params *AirbnbListingActionParams, body AirbnbListingActionJSONRequestBody, reqEditors ...RequestEditorFn) (*AirbnbListingActionClientResponse, error) {
	rsp, err := c.AirbnbListingAction(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAirbnbListingActionClientResponse(rsp)
}

// ListAirbnbListingAmenitiesWithResponse List Airbnb amenities
//
// List an Airbnb listing's amenities. **Pure DB read** from the local `listings_airbnb_amenities` cache — never calls Airbnb upstream. The response splits amenities into `amenities` (regular) and `accessibility_amenities` (step-free access, wide doorways, grab rails, disabled parking, wheelchair, accessible-height fixtures, hoists, etc). Both are arrays (`[]` when none). Consult `dataFreshness` to disambiguate "never synced" from "fresh and genuinely empty". Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/amenities (the `ListAirbnbListingAmenities` operationId).
func (c *ClientWithResponses) ListAirbnbListingAmenitiesWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*ListAirbnbListingAmenitiesClientResponse, error) {
	rsp, err := c.ListAirbnbListingAmenities(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbListingAmenitiesClientResponse(rsp)
}

// UpdateAirbnbListingAmenitiesWithBodyWithResponse Update Airbnb amenities
//
// Set amenities on an Airbnb listing. **Write-side** — calls Airbnb upstream.
//
// **Partial by design**: only the amenities you name change, so turning one off is a one-line body and nothing else on the listing moves. Ids are the `id` values `GET /amenities` returns (e.g. `wireless_internet`, `ac`, `kitchen`); case is ignored. Airbnb refuses ids outside its vocabulary — that comes back as `422 airbnb_rejected` carrying Airbnb's own message.
//
// `accessibility_amenities` go to Airbnb's separate accessibility resource, which has **no read side at all** — Airbnb offers no endpoint to fetch them back, and the combined amenities GET 404s on production listings. What you can read back is our own copy: this endpoint updates it on success, and `GET /amenities` returns it under `accessibilityAmenities`. Airbnb may also hold an accessibility claim for review until photo evidence is attached; pass `photo_ids` to supply it.
//
// Our Airbnb copy is updated on success so a read straight after this write returns the new values. The platform-neutral copy behind `GET /v1/listings/{id}?include=amenities` uses a different amenity vocabulary and is refreshed by the next sync, except where an id happens to be identical in both.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/amenities (the `UpdateAirbnbListingAmenities` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingAmenitiesWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingAmenitiesClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingAmenitiesWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingAmenitiesClientResponse(rsp)
}

// UpdateAirbnbListingAmenitiesWithResponse Update Airbnb amenities
//
// Set amenities on an Airbnb listing. **Write-side** — calls Airbnb upstream.
//
// **Partial by design**: only the amenities you name change, so turning one off is a one-line body and nothing else on the listing moves. Ids are the `id` values `GET /amenities` returns (e.g. `wireless_internet`, `ac`, `kitchen`); case is ignored. Airbnb refuses ids outside its vocabulary — that comes back as `422 airbnb_rejected` carrying Airbnb's own message.
//
// `accessibility_amenities` go to Airbnb's separate accessibility resource, which has **no read side at all** — Airbnb offers no endpoint to fetch them back, and the combined amenities GET 404s on production listings. What you can read back is our own copy: this endpoint updates it on success, and `GET /amenities` returns it under `accessibilityAmenities`. Airbnb may also hold an accessibility claim for review until photo evidence is attached; pass `photo_ids` to supply it.
//
// Our Airbnb copy is updated on success so a read straight after this write returns the new values. The platform-neutral copy behind `GET /v1/listings/{id}?include=amenities` uses a different amenity vocabulary and is refreshed by the next sync, except where an id happens to be identical in both.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/amenities (the `UpdateAirbnbListingAmenities` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingAmenitiesWithResponse(ctx context.Context, id string, body UpdateAirbnbListingAmenitiesJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingAmenitiesClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingAmenities(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingAmenitiesClientResponse(rsp)
}

// GetAirbnbListingAvailabilityWithResponse Get Airbnb availability
//
// Read the per-day availability calendar for an Airbnb listing. Returns one row per day including price overrides, min-stay, and blocked status.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/availability (the `GetAirbnbListingAvailability` operationId).
func (c *ClientWithResponses) GetAirbnbListingAvailabilityWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbListingAvailabilityClientResponse, error) {
	rsp, err := c.GetAirbnbListingAvailability(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbListingAvailabilityClientResponse(rsp)
}

// UpdateAirbnbListingAvailabilityWithBodyWithResponse Update Airbnb availability
//
// Push availability + restrictions to Airbnb. `type: "calendar"` writes per-date restrictions — min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`) — via a batch of operations that each target either a date range or an explicit date list. `type: "rules"` writes listing-level availability rules (default min/max nights, booking lead time, turnover days, seasonal/day-of-week min nights). Restrictions never leak across channels — this endpoint writes only to Airbnb.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
//
// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
//
// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/availability (the `UpdateAirbnbListingAvailability` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingAvailabilityWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingAvailabilityClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingAvailabilityWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingAvailabilityClientResponse(rsp)
}

// UpdateAirbnbListingAvailabilityWithResponse Update Airbnb availability
//
// Push availability + restrictions to Airbnb. `type: "calendar"` writes per-date restrictions — min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`) — via a batch of operations that each target either a date range or an explicit date list. `type: "rules"` writes listing-level availability rules (default min/max nights, booking lead time, turnover days, seasonal/day-of-week min nights). Restrictions never leak across channels — this endpoint writes only to Airbnb.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
//
// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
//
// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/availability (the `UpdateAirbnbListingAvailability` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingAvailabilityWithResponse(ctx context.Context, id string, body UpdateAirbnbListingAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingAvailabilityClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingAvailability(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingAvailabilityClientResponse(rsp)
}

// GetAirbnbBookingSettingsWithResponse Get Airbnb booking settings
//
// Read how an Airbnb listing takes bookings and what happens when a guest cancels: booking mode, Instant Book state, the good-track-record requirement, check-in/check-out times, advance notice, preparation time, booking window, the short-stay and long-stay cancellation policies, and the non-refundable option.
//
// **This is Repull's stored copy, not a live call to Airbnb.** Values come from the local Airbnb mirror that the sync workers fill, so the response always carries `dataFreshness` — check `dataFreshness.stale` (and `dataFreshness.accounts[]` when the workspace has several Airbnb accounts) before treating a value as current.
//
// **Not exposed by Airbnb.** The pre-reservation message and automatic stay extension have no field on Airbnb's `booking_settings` resource, so neither can be read or written here; set the pre-reservation message in the Airbnb host dashboard, and handle extensions through `/v1/channels/airbnb/alterations`. Airbnb also expresses the same-day cutoff only as whole hours of advance notice, so `advanceNotice.hours` is as precise as the cutoff gets.
//
// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive — an inactive listing keeps syncing but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/booking-settings (the `GetAirbnbBookingSettings` operationId).
func (c *ClientWithResponses) GetAirbnbBookingSettingsWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbBookingSettingsClientResponse, error) {
	rsp, err := c.GetAirbnbBookingSettings(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbBookingSettingsClientResponse(rsp)
}

// UpdateAirbnbBookingSettingsWithBodyWithResponse Update Airbnb booking settings
//
// Set any subset of a listing's booking settings on Airbnb. Partial — a field you do not send is left as it is.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id; Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb, so a bad value is a `422` naming the field rather than a failed upstream call. Unknown fields are refused rather than dropped.
//
// **Two groups, applied in order.** Instant Book, check-in/out and the cancellation fields go to Airbnb's booking-settings resource. Advance notice, preparation time and booking window go to Airbnb's availability rules — Airbnb replaces that whole document, so Repull reads the current rules first and merges your change onto them, which is why setting a preparation time does not blank the listing's min/max nights. The response's `applied` array names the groups that were written.
//
// **Non-refundable is a percentage here, a factor on Airbnb.** Airbnb stores `non_refundable_price_factor` between 0.7 and 1.0; send `cancellation.nonRefundable.discountPercent` (0-30) and Repull converts — 10% becomes 0.9. `enabled: false` sets the factor to 1.0.
//
// **Not exposed by Airbnb:** `preReservationMessage` and `automaticStayExtension`. Sending either returns a `422` explaining where to set it instead.
//
// **Errors:** `403 connection_reauth_required` — Airbnb no longer accepts the connection for this listing (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 invalid_params` — the body is wrong; `field` names it. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/booking-settings (the `UpdateAirbnbBookingSettings` operationId).
func (c *ClientWithResponses) UpdateAirbnbBookingSettingsWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbBookingSettingsClientResponse, error) {
	rsp, err := c.UpdateAirbnbBookingSettingsWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbBookingSettingsClientResponse(rsp)
}

// UpdateAirbnbBookingSettingsWithResponse Update Airbnb booking settings
//
// Set any subset of a listing's booking settings on Airbnb. Partial — a field you do not send is left as it is.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id; Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb, so a bad value is a `422` naming the field rather than a failed upstream call. Unknown fields are refused rather than dropped.
//
// **Two groups, applied in order.** Instant Book, check-in/out and the cancellation fields go to Airbnb's booking-settings resource. Advance notice, preparation time and booking window go to Airbnb's availability rules — Airbnb replaces that whole document, so Repull reads the current rules first and merges your change onto them, which is why setting a preparation time does not blank the listing's min/max nights. The response's `applied` array names the groups that were written.
//
// **Non-refundable is a percentage here, a factor on Airbnb.** Airbnb stores `non_refundable_price_factor` between 0.7 and 1.0; send `cancellation.nonRefundable.discountPercent` (0-30) and Repull converts — 10% becomes 0.9. `enabled: false` sets the factor to 1.0.
//
// **Not exposed by Airbnb:** `preReservationMessage` and `automaticStayExtension`. Sending either returns a `422` explaining where to set it instead.
//
// **Errors:** `403 connection_reauth_required` — Airbnb no longer accepts the connection for this listing (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 invalid_params` — the body is wrong; `field` names it. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/booking-settings (the `UpdateAirbnbBookingSettings` operationId).
func (c *ClientWithResponses) UpdateAirbnbBookingSettingsWithResponse(ctx context.Context, id string, body UpdateAirbnbBookingSettingsJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbBookingSettingsClientResponse, error) {
	rsp, err := c.UpdateAirbnbBookingSettings(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbBookingSettingsClientResponse(rsp)
}

// GetAirbnbCheckinGuideWithResponse Get Airbnb check-in guide
//
// Return every published locale variant of an Airbnb listing's check-in guide. **Pure DB read** from `listings_airbnb_check_in_guides`. Pass `?locale=en` to filter to one locale (prefix match). Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/checkin-guide (the `GetAirbnbCheckinGuide` operationId).
func (c *ClientWithResponses) GetAirbnbCheckinGuideWithResponse(ctx context.Context, id string, params *GetAirbnbCheckinGuideParams, reqEditors ...RequestEditorFn) (*GetAirbnbCheckinGuideClientResponse, error) {
	rsp, err := c.GetAirbnbCheckinGuide(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbCheckinGuideClientResponse(rsp)
}

// UpdateAirbnbCheckinGuideWithResponse Upsert Airbnb check-in guide
//
// Upsert the check-in guide for one locale on an Airbnb listing. **Write-side** — calls Airbnb upstream; the DB mirror is reconciled by the sync worker once the upstream call returns. Target the locale with `?locale=en` (defaults to `en`). Requires a connected Airbnb host, else `404 no_connection`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/checkin-guide (the `UpdateAirbnbCheckinGuide` operationId).
func (c *ClientWithResponses) UpdateAirbnbCheckinGuideWithResponse(ctx context.Context, id string, params *UpdateAirbnbCheckinGuideParams, reqEditors ...RequestEditorFn) (*UpdateAirbnbCheckinGuideClientResponse, error) {
	rsp, err := c.UpdateAirbnbCheckinGuide(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbCheckinGuideClientResponse(rsp)
}

// GetAirbnbCheckoutGuideWithResponse Get Airbnb checkout guide
//
// Return the checkout tasks an Airbnb listing shows guests at departure. **Pure DB read** from `listings_airbnb_checkout_tasks`. Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/checkout-guide (the `GetAirbnbCheckoutGuide` operationId).
func (c *ClientWithResponses) GetAirbnbCheckoutGuideWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbCheckoutGuideClientResponse, error) {
	rsp, err := c.GetAirbnbCheckoutGuide(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbCheckoutGuideClientResponse(rsp)
}

// ListAirbnbListingDescriptionsWithResponse List Airbnb descriptions
//
// List an Airbnb listing's per-locale content (name, summary, house rules, etc). **Pure DB read** from `listings_airbnb_descriptions`. Filter to one locale with `?locale=en` (the legacy `?country=` param is accepted as a soft alias). Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/descriptions (the `ListAirbnbListingDescriptions` operationId).
func (c *ClientWithResponses) ListAirbnbListingDescriptionsWithResponse(ctx context.Context, id string, params *ListAirbnbListingDescriptionsParams, reqEditors ...RequestEditorFn) (*ListAirbnbListingDescriptionsClientResponse, error) {
	rsp, err := c.ListAirbnbListingDescriptions(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbListingDescriptionsClientResponse(rsp)
}

// UpdateAirbnbListingDescriptionWithBodyWithResponse Update an Airbnb description for one locale
//
// Write one locale's copy to the live Airbnb listing.
//
// Airbnb keeps a SEPARATE description per locale (`PUT /v2/listing_descriptions/{listingId}/{locale}`), which is why `locale` is part of the request and not a guess: a listing can carry twelve of them, and writing Italian copy into the English row is how a translation gets lost. Only the fields you send are written; Airbnb keeps the rest. `GET /v1/channels/airbnb/listings/{id}/settings?type=locales` lists the locales already synced for the listing.
//
// `description` is not an accepted field: Airbnb composes the public description from the sections (`summary`, `space`, `access`, …) and ignores a directly-supplied one.
//
// **A 200 does not by itself mean the change was applied.** On an established listing Airbnb LOCKS host-managed description fields — the write returns 200, reports them as locked, and applies nothing for them. The response reports `blockedFields`: the fields YOU sent that Airbnb dropped. `blockedFields: []` is what a landed write looks like; a non-empty list is still a 200 (the other fields really were written) with a `message` naming what was not. Reporting that as a clean success is the bug behind "the description does not push to Airbnb".
//
// This writes to AIRBNB. To write Repull's own canonical copy — the content a later publish distributes — use `PUT /v1/listings/{id}/content` with `locale`.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Returns `403 listing_inactive` when the listing is inactive.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/descriptions (the `UpdateAirbnbListingDescription` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingDescriptionWithBodyWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingDescriptionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingDescriptionClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingDescriptionWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingDescriptionClientResponse(rsp)
}

// UpdateAirbnbListingDescriptionWithResponse Update an Airbnb description for one locale
//
// Write one locale's copy to the live Airbnb listing.
//
// Airbnb keeps a SEPARATE description per locale (`PUT /v2/listing_descriptions/{listingId}/{locale}`), which is why `locale` is part of the request and not a guess: a listing can carry twelve of them, and writing Italian copy into the English row is how a translation gets lost. Only the fields you send are written; Airbnb keeps the rest. `GET /v1/channels/airbnb/listings/{id}/settings?type=locales` lists the locales already synced for the listing.
//
// `description` is not an accepted field: Airbnb composes the public description from the sections (`summary`, `space`, `access`, …) and ignores a directly-supplied one.
//
// **A 200 does not by itself mean the change was applied.** On an established listing Airbnb LOCKS host-managed description fields — the write returns 200, reports them as locked, and applies nothing for them. The response reports `blockedFields`: the fields YOU sent that Airbnb dropped. `blockedFields: []` is what a landed write looks like; a non-empty list is still a 200 (the other fields really were written) with a `message` naming what was not. Reporting that as a clean success is the bug behind "the description does not push to Airbnb".
//
// This writes to AIRBNB. To write Repull's own canonical copy — the content a later publish distributes — use `PUT /v1/listings/{id}/content` with `locale`.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Returns `403 listing_inactive` when the listing is inactive.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/descriptions (the `UpdateAirbnbListingDescription` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingDescriptionWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingDescriptionParams, body UpdateAirbnbListingDescriptionJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingDescriptionClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingDescription(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingDescriptionClientResponse(rsp)
}

// GetAirbnbListingDetailsWithResponse Get Airbnb listing details
//
// What kind of property Airbnb thinks this is — property type group and category, room type, capacity — plus the check-in method (`checkInOption`), whether the listing is live (`hasAvailability`), and **`lockedFields`: the attributes Airbnb refuses to change on this listing**.
//
// **Pure DB read** from the local mirror, one entry per Airbnb connection.
//
// Read `lockedFields` before a content write. Airbnb does not refuse a write to a locked attribute: it returns 200, reports the attribute as locked, and applies nothing — which is why a write can look successful and change nothing. 1,180 of 5,917 synced listings carry at least one locked attribute.
//
// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/details (the `GetAirbnbListingDetails` operationId).
func (c *ClientWithResponses) GetAirbnbListingDetailsWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbListingDetailsClientResponse, error) {
	rsp, err := c.GetAirbnbListingDetails(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbListingDetailsClientResponse(rsp)
}

// UpdateAirbnbListingDetailsWithBodyWithResponse Update property type, room type, quiet hours or check-in method
//
// Change what kind of property the Airbnb listing is, when its quiet hours are, or how the guest gets in. Partial: only the fields you send are written. At least one required; an unknown field is refused by name rather than dropped.
//
// This is the UPDATE path for fields that previously had none. `POST /v1/listings` accepts a `propertyType` when a listing is CREATED and nothing could change it afterwards, so a listing mis-typed at import stayed mis-typed; the check-in method was mirrored and never exposed at all.
//
// **A 200 does not by itself mean the change was applied.** `property_type_category`, `property_type_group` and `check_in_option` are among the attributes Airbnb locks on established listings: the write returns 200, and Airbnb applies nothing for the locked ones. The response reports `blockedFields` — the fields YOU sent that Airbnb dropped — and `blockedFields: []` is what a landed write looks like. `GET …/details` reports the same list as `lockedFields` so you can check first.
//
// Canonical property type (the value Repull keeps and republishes) is set with `PUT /v1/listings/{id}/content` under `details`; this endpoint writes straight to Airbnb.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/details (the `UpdateAirbnbListingDetails` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingDetailsWithBodyWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingDetailsParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingDetailsClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingDetailsWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingDetailsClientResponse(rsp)
}

// UpdateAirbnbListingDetailsWithResponse Update property type, room type, quiet hours or check-in method
//
// Change what kind of property the Airbnb listing is, when its quiet hours are, or how the guest gets in. Partial: only the fields you send are written. At least one required; an unknown field is refused by name rather than dropped.
//
// This is the UPDATE path for fields that previously had none. `POST /v1/listings` accepts a `propertyType` when a listing is CREATED and nothing could change it afterwards, so a listing mis-typed at import stayed mis-typed; the check-in method was mirrored and never exposed at all.
//
// **A 200 does not by itself mean the change was applied.** `property_type_category`, `property_type_group` and `check_in_option` are among the attributes Airbnb locks on established listings: the write returns 200, and Airbnb applies nothing for the locked ones. The response reports `blockedFields` — the fields YOU sent that Airbnb dropped — and `blockedFields: []` is what a landed write looks like. `GET …/details` reports the same list as `lockedFields` so you can check first.
//
// Canonical property type (the value Repull keeps and republishes) is set with `PUT /v1/listings/{id}/content` under `details`; this endpoint writes straight to Airbnb.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/details (the `UpdateAirbnbListingDetails` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingDetailsWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingDetailsParams, body UpdateAirbnbListingDetailsJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingDetailsClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingDetails(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingDetailsClientResponse(rsp)
}

// ListAirbnbListingPermitsWithResponse List Airbnb permits and licences
//
// The regulatory permits, licences and registration numbers attached to an Airbnb listing.
//
// **DB-only by default.** `?source=cache` (the default) returns the permits as last mirrored by the sync worker — regulatory body, regulation type, status, permit number — with no upstream call.
//
// **`?source=live` also returns the QUESTIONS.** The mirror stores the RESULT of a permit, not what Airbnb asks for it, so a caller that is about to write needs `?source=live` once: it returns each permit's `flows[]` with the `answer_key`, `type` and `choices` of every question, and the answers already on file. Airbnb refuses an `answer_key` it did not ask for on this listing, so this is not optional guesswork you can skip.
//
// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/permits (the `ListAirbnbListingPermits` operationId).
func (c *ClientWithResponses) ListAirbnbListingPermitsWithResponse(ctx context.Context, id string, params *ListAirbnbListingPermitsParams, reqEditors ...RequestEditorFn) (*ListAirbnbListingPermitsClientResponse, error) {
	rsp, err := c.ListAirbnbListingPermits(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbListingPermitsClientResponse(rsp)
}

// UpdateAirbnbListingPermitsWithBodyWithResponse Answer Airbnb permit questions
//
// Answer the regulatory permit questions for a listing — the licence or registration number a city requires to keep the listing up.
//
// Read the questions first with `GET …/permits?source=live`. For each permit, pick one of its `flows[]` and send its `slug` as `flow_slug`; key every answer by the question's `answer_key`, and let the question's `type` decide the value field (`text_value`, `attestation_value`, `radio_value`, `date_value` or `selected_options_value`). Answers are forwarded verbatim — nothing is defaulted or inferred, because a wrong licence number can take a listing down in a regulated city.
//
// Send `Idempotency-Key`: a timeout here leaves you unable to tell "never arrived" from "arrived, response lost", and this is a compliance filing.
//
// Airbnb refusing the answers (an unknown `answer_key`, a malformed licence number) is `422 airbnb_rejected` carrying Airbnb's own reason. An expired or revoked Airbnb connection is `403 connection_reauth_required`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/permits (the `UpdateAirbnbListingPermits` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingPermitsWithBodyWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingPermitsParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPermitsClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingPermitsWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingPermitsClientResponse(rsp)
}

// UpdateAirbnbListingPermitsWithResponse Answer Airbnb permit questions
//
// Answer the regulatory permit questions for a listing — the licence or registration number a city requires to keep the listing up.
//
// Read the questions first with `GET …/permits?source=live`. For each permit, pick one of its `flows[]` and send its `slug` as `flow_slug`; key every answer by the question's `answer_key`, and let the question's `type` decide the value field (`text_value`, `attestation_value`, `radio_value`, `date_value` or `selected_options_value`). Answers are forwarded verbatim — nothing is defaulted or inferred, because a wrong licence number can take a listing down in a regulated city.
//
// Send `Idempotency-Key`: a timeout here leaves you unable to tell "never arrived" from "arrived, response lost", and this is a compliance filing.
//
// Airbnb refusing the answers (an unknown `answer_key`, a malformed licence number) is `422 airbnb_rejected` carrying Airbnb's own reason. An expired or revoked Airbnb connection is `403 connection_reauth_required`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/permits (the `UpdateAirbnbListingPermits` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingPermitsWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingPermitsParams, body UpdateAirbnbListingPermitsJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPermitsClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingPermits(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingPermitsClientResponse(rsp)
}

// DeleteAirbnbListingPhotoWithResponse Delete an Airbnb photo
//
// Remove a single photo from an Airbnb listing. Pass the Airbnb-side photo id as `?photoId=`. **Write-side** — calls Airbnb upstream.
//
// The photo is proven to belong to the listing named in the path first; a photo from another listing returns `404`. Airbnb refuses to delete a listing's last photo. Both stored copies drop the photo on success — `stored` reports whether that succeeded.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/channels/airbnb/listings/{id}/photos (the `DeleteAirbnbListingPhoto` operationId).
func (c *ClientWithResponses) DeleteAirbnbListingPhotoWithResponse(ctx context.Context, id string, params *DeleteAirbnbListingPhotoParams, reqEditors ...RequestEditorFn) (*DeleteAirbnbListingPhotoClientResponse, error) {
	rsp, err := c.DeleteAirbnbListingPhoto(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteAirbnbListingPhotoClientResponse(rsp)
}

// ListAirbnbListingPhotosWithResponse List Airbnb photos
//
// List photos attached to an Airbnb listing in display order. Returns the public CDN URL plus Airbnb-side metadata (id, caption, room).
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/photos (the `ListAirbnbListingPhotos` operationId).
func (c *ClientWithResponses) ListAirbnbListingPhotosWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*ListAirbnbListingPhotosClientResponse, error) {
	rsp, err := c.ListAirbnbListingPhotos(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbListingPhotosClientResponse(rsp)
}

// UpdateAirbnbListingPhotoWithBodyWithResponse Update an Airbnb photo
//
// Change one photo's caption, its position in the tour, the room it is filed under, or its metadata. **Write-side** — calls Airbnb upstream.
//
// Airbnb's photo endpoints are keyed by photo id alone, so the photo is proven to belong to the listing named in the path before anything is sent; a photo from another listing returns `404`, the same answer a photo that does not exist gets.
//
// On success both stored copies are updated — the Airbnb mirror `GET /photos` serves AND the canonical photo tour behind `GET /v1/listings/{id}` — so a read straight after this write returns the new value instead of waiting for the next sync. `stored` says whether that succeeded; `false` means Airbnb accepted the change but our copy will only catch up at the next sync.
//
// To move several photos at once use `PUT /photos/order`: it is one call instead of N, it validates the whole order before writing anything, and it reports exactly what landed if Airbnb refuses part-way.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/channels/airbnb/listings/{id}/photos (the `UpdateAirbnbListingPhoto` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingPhotoWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPhotoClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingPhotoWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingPhotoClientResponse(rsp)
}

// UpdateAirbnbListingPhotoWithResponse Update an Airbnb photo
//
// Change one photo's caption, its position in the tour, the room it is filed under, or its metadata. **Write-side** — calls Airbnb upstream.
//
// Airbnb's photo endpoints are keyed by photo id alone, so the photo is proven to belong to the listing named in the path before anything is sent; a photo from another listing returns `404`, the same answer a photo that does not exist gets.
//
// On success both stored copies are updated — the Airbnb mirror `GET /photos` serves AND the canonical photo tour behind `GET /v1/listings/{id}` — so a read straight after this write returns the new value instead of waiting for the next sync. `stored` says whether that succeeded; `false` means Airbnb accepted the change but our copy will only catch up at the next sync.
//
// To move several photos at once use `PUT /photos/order`: it is one call instead of N, it validates the whole order before writing anything, and it reports exactly what landed if Airbnb refuses part-way.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/channels/airbnb/listings/{id}/photos (the `UpdateAirbnbListingPhoto` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingPhotoWithResponse(ctx context.Context, id string, body UpdateAirbnbListingPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPhotoClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingPhoto(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingPhotoClientResponse(rsp)
}

// UploadAirbnbListingPhotosWithBodyWithResponse Upload photos to Airbnb
//
// Upload one or more photos to an Airbnb listing.
//
// `image` is base64 image DATA, not a url — a `data:image/jpeg;base64,…` prefix is accepted and stripped, and the decoded image must be under 25 MB. (This operation previously documented public image urls that Airbnb would fetch. It never did: a url arrived at Airbnb as a ~60-byte image.)
//
// Airbnb assigns the photo id and CDN urls, so the newly uploaded photos appear in `GET /photos` after the next sync. Caption, order and room assignment can be set straight away with `PATCH /photos`, `PUT /photos/order` and `PUT /photos/cover`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/listings/{id}/photos (the `UploadAirbnbListingPhotos` operationId).
func (c *ClientWithResponses) UploadAirbnbListingPhotosWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UploadAirbnbListingPhotosClientResponse, error) {
	rsp, err := c.UploadAirbnbListingPhotosWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUploadAirbnbListingPhotosClientResponse(rsp)
}

// UploadAirbnbListingPhotosWithResponse Upload photos to Airbnb
//
// Upload one or more photos to an Airbnb listing.
//
// `image` is base64 image DATA, not a url — a `data:image/jpeg;base64,…` prefix is accepted and stripped, and the decoded image must be under 25 MB. (This operation previously documented public image urls that Airbnb would fetch. It never did: a url arrived at Airbnb as a ~60-byte image.)
//
// Airbnb assigns the photo id and CDN urls, so the newly uploaded photos appear in `GET /photos` after the next sync. Caption, order and room assignment can be set straight away with `PATCH /photos`, `PUT /photos/order` and `PUT /photos/cover`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/listings/{id}/photos (the `UploadAirbnbListingPhotos` operationId).
func (c *ClientWithResponses) UploadAirbnbListingPhotosWithResponse(ctx context.Context, id string, body UploadAirbnbListingPhotosJSONRequestBody, reqEditors ...RequestEditorFn) (*UploadAirbnbListingPhotosClientResponse, error) {
	rsp, err := c.UploadAirbnbListingPhotos(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUploadAirbnbListingPhotosClientResponse(rsp)
}

// SetAirbnbListingCoverPhotoWithBodyWithResponse Set the Airbnb cover photo
//
// Choose which photo leads the listing. **Write-side** — calls Airbnb upstream.
//
// Airbnb has no "cover" field: the cover is the first photo of the tour, so this is a position write. Usually it is a single upstream request — the chosen photo takes a position below the current first one and nothing else moves. When the tour already starts at position 1 and there is no room below it, the tour is renumbered instead, one request per photo whose position actually changes, with the same stop-at-first-failure reporting as `PUT /photos/order` (`applied`, `failed_photo_id`, `not_attempted`; re-send the identical body to finish).
//
// The listing thumbnail — what every list view renders — is repointed at the new cover, so the change is not visible only inside the photo tour.
//
// The photo must already be in our cached copy of the tour; one uploaded since the last sync returns `404`, and `PATCH /photos` can set its position in the meantime.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/cover (the `SetAirbnbListingCoverPhoto` operationId).
func (c *ClientWithResponses) SetAirbnbListingCoverPhotoWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetAirbnbListingCoverPhotoClientResponse, error) {
	rsp, err := c.SetAirbnbListingCoverPhotoWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetAirbnbListingCoverPhotoClientResponse(rsp)
}

// SetAirbnbListingCoverPhotoWithResponse Set the Airbnb cover photo
//
// Choose which photo leads the listing. **Write-side** — calls Airbnb upstream.
//
// Airbnb has no "cover" field: the cover is the first photo of the tour, so this is a position write. Usually it is a single upstream request — the chosen photo takes a position below the current first one and nothing else moves. When the tour already starts at position 1 and there is no room below it, the tour is renumbered instead, one request per photo whose position actually changes, with the same stop-at-first-failure reporting as `PUT /photos/order` (`applied`, `failed_photo_id`, `not_attempted`; re-send the identical body to finish).
//
// The listing thumbnail — what every list view renders — is repointed at the new cover, so the change is not visible only inside the photo tour.
//
// The photo must already be in our cached copy of the tour; one uploaded since the last sync returns `404`, and `PATCH /photos` can set its position in the meantime.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/cover (the `SetAirbnbListingCoverPhoto` operationId).
func (c *ClientWithResponses) SetAirbnbListingCoverPhotoWithResponse(ctx context.Context, id string, body SetAirbnbListingCoverPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*SetAirbnbListingCoverPhotoClientResponse, error) {
	rsp, err := c.SetAirbnbListingCoverPhoto(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetAirbnbListingCoverPhotoClientResponse(rsp)
}

// ReorderAirbnbListingPhotosWithBodyWithResponse Reorder the Airbnb photo tour
//
// Set the order of a listing's photo tour in one call. **Write-side** — calls Airbnb upstream.
//
// Send the photo ids in the order you want them shown, first photo first. Ids you leave out keep their current relative order behind the ones you list, so moving one photo to the front is `{"photo_ids": ["<id>"]}`. Positions are then written as a dense run starting at 1.
//
// Airbnb has no bulk photo endpoint — order is one `sort_order` per photo — so this saves a loop of up to 200 requests against your rate limit and makes the partial-failure case reportable. How much is atomic:
//
// - Everything is validated before anything is written. A duplicate id, an id that is not on this listing, an inactive listing or a missing connection all fail with **zero** upstream writes.
// - Only photos whose position actually changes are written; re-sending the order you already have writes nothing.
// - On the first upstream failure the run stops — nothing after it is attempted. The error carries `applied`, `failed_photo_id` and `not_attempted`, uses Airbnb's own status (`422` rejected / `403` reauth / `429` / `502`), and the operation is idempotent: re-send the identical body to finish the run.
// - Our stored copy records what actually landed, never the intent.
//
// Validation is against our cached copy of the tour, so a listing whose photos have never synced returns `404` — use `PATCH /photos` (which needs no cache) until the first sync lands.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/order (the `ReorderAirbnbListingPhotos` operationId).
func (c *ClientWithResponses) ReorderAirbnbListingPhotosWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReorderAirbnbListingPhotosClientResponse, error) {
	rsp, err := c.ReorderAirbnbListingPhotosWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReorderAirbnbListingPhotosClientResponse(rsp)
}

// ReorderAirbnbListingPhotosWithResponse Reorder the Airbnb photo tour
//
// Set the order of a listing's photo tour in one call. **Write-side** — calls Airbnb upstream.
//
// Send the photo ids in the order you want them shown, first photo first. Ids you leave out keep their current relative order behind the ones you list, so moving one photo to the front is `{"photo_ids": ["<id>"]}`. Positions are then written as a dense run starting at 1.
//
// Airbnb has no bulk photo endpoint — order is one `sort_order` per photo — so this saves a loop of up to 200 requests against your rate limit and makes the partial-failure case reportable. How much is atomic:
//
// - Everything is validated before anything is written. A duplicate id, an id that is not on this listing, an inactive listing or a missing connection all fail with **zero** upstream writes.
// - Only photos whose position actually changes are written; re-sending the order you already have writes nothing.
// - On the first upstream failure the run stops — nothing after it is attempted. The error carries `applied`, `failed_photo_id` and `not_attempted`, uses Airbnb's own status (`422` rejected / `403` reauth / `429` / `502`), and the operation is idempotent: re-send the identical body to finish the run.
// - Our stored copy records what actually landed, never the intent.
//
// Validation is against our cached copy of the tour, so a listing whose photos have never synced returns `404` — use `PATCH /photos` (which needs no cache) until the first sync lands.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/photos/order (the `ReorderAirbnbListingPhotos` operationId).
func (c *ClientWithResponses) ReorderAirbnbListingPhotosWithResponse(ctx context.Context, id string, body ReorderAirbnbListingPhotosJSONRequestBody, reqEditors ...RequestEditorFn) (*ReorderAirbnbListingPhotosClientResponse, error) {
	rsp, err := c.ReorderAirbnbListingPhotos(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReorderAirbnbListingPhotosClientResponse(rsp)
}

// GetAirbnbListingPricingWithResponse Get Airbnb pricing
//
// Read the current pricing config (base price, weekend uplift, length-of-stay discounts, smart-pricing bounds) for an Airbnb listing.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/pricing (the `GetAirbnbListingPricing` operationId).
func (c *ClientWithResponses) GetAirbnbListingPricingWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*GetAirbnbListingPricingClientResponse, error) {
	rsp, err := c.GetAirbnbListingPricing(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbListingPricingClientResponse(rsp)
}

// UpdateAirbnbListingPricingWithBodyWithResponse Update Airbnb pricing
//
// Push pricing changes to Airbnb. The `type` discriminator selects the sub-resource (model, standard settings, LOS, rate-plan, fees, currency, rule, or per-date `calendar`). `type: "calendar"` carries the full per-date restriction set — nightly price, min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`). For settings sub-resources the full object is replaced — GET first, mutate locally, then PUT the whole object.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
//
// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
//
// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/pricing (the `UpdateAirbnbListingPricing` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingPricingWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPricingClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingPricingWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingPricingClientResponse(rsp)
}

// UpdateAirbnbListingPricingWithResponse Update Airbnb pricing
//
// Push pricing changes to Airbnb. The `type` discriminator selects the sub-resource (model, standard settings, LOS, rate-plan, fees, currency, rule, or per-date `calendar`). `type: "calendar"` carries the full per-date restriction set — nightly price, min/max nights, closed-to-arrival, closed-to-departure, and stop-sell (`availability: "unavailable"`). For settings sub-resources the full object is replaced — GET first, mutate locally, then PUT the whole object.
//
// `{id}` is the **Repull listing id** (from `GET /v1/properties` or `GET /v1/channels/airbnb/listings`), not the Airbnb listing id — Repull translates it before calling Airbnb.
//
// The body is validated before anything reaches Airbnb: a malformed body is `422 invalid_params` naming the `field`. Calendar operations accept only the documented fields.
//
// **Blocking dates:** Airbnb requires a `busy_subtype` whenever `availability` is `"unavailable"`. If an operation leaves it out, Repull sends `busy_subtype: "BLOCKED_BY_HOST"`; send `"OUTSIDE_RESERVATION"` for dates held by a booking made on another channel.
//
// **Errors:** `403 listing_not_api_connected` — Airbnb was never told to sync this listing (its `syncCategory` is `none`); the host must switch API sync on for it in Airbnb, reconnecting the account will not help. `403 connection_reauth_required` — Airbnb no longer accepts the connection at all (reconnect; retrying won't help). `403 listing_inactive` — the listing is inactive. `404 not_found` — no Airbnb-connected listing with this id in the workspace. `422 airbnb_rejected` — Airbnb refused the change; `message` carries its reason. `429 airbnb_rate_limited` — back off. `502 airbnb_error` — Airbnb outage or timeout; retry.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/pricing (the `UpdateAirbnbListingPricing` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingPricingWithResponse(ctx context.Context, id string, body UpdateAirbnbListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingPricingClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingPricing(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingPricingClientResponse(rsp)
}

// GetAirbnbListingQualityWithResponse Get Airbnb listing quality
//
// Return an Airbnb listing's quality signals — standards, reservation issues, and monthly quality stats. **Pure DB read** from the local quality mirrors. Scope the response with `?type=all|standards|issues|stats` (default `all`, which returns `{ standards, issues }`). Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/quality (the `GetAirbnbListingQuality` operationId).
func (c *ClientWithResponses) GetAirbnbListingQualityWithResponse(ctx context.Context, id string, params *GetAirbnbListingQualityParams, reqEditors ...RequestEditorFn) (*GetAirbnbListingQualityClientResponse, error) {
	rsp, err := c.GetAirbnbListingQuality(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbListingQualityClientResponse(rsp)
}

// DeleteAirbnbListingRoomWithResponse Delete an Airbnb room
//
// Delete a room from an Airbnb listing, and its beds with it. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=`. Requires a connected Airbnb host, else `404 no_connection`.
//
// The room is proven to belong to the listing named in the path first; a room from another listing returns `404`. Both stored copies drop the room on success — `stored` reports whether that succeeded.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/channels/airbnb/listings/{id}/rooms (the `DeleteAirbnbListingRoom` operationId).
func (c *ClientWithResponses) DeleteAirbnbListingRoomWithResponse(ctx context.Context, id string, params *DeleteAirbnbListingRoomParams, reqEditors ...RequestEditorFn) (*DeleteAirbnbListingRoomClientResponse, error) {
	rsp, err := c.DeleteAirbnbListingRoom(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteAirbnbListingRoomClientResponse(rsp)
}

// ListAirbnbListingRoomsWithResponse List Airbnb rooms
//
// List the rooms configured on an Airbnb listing, ordered by room number, each with its sleeping arrangement in `beds`. **Pure DB read** from `listings_airbnb_rooms` + `listings_airbnb_beds`. Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/rooms (the `ListAirbnbListingRooms` operationId).
func (c *ClientWithResponses) ListAirbnbListingRoomsWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*ListAirbnbListingRoomsClientResponse, error) {
	rsp, err := c.ListAirbnbListingRooms(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbListingRoomsClientResponse(rsp)
}

// CreateAirbnbListingRoomWithBodyWithResponse Create an Airbnb room
//
// Create a new room on an Airbnb listing, with its sleeping arrangement. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host, else `404 no_connection`.
//
// The response is the room object as Airbnb returned it. The new room is also seeded into our own copy, so the very next `GET /rooms` shows it rather than waiting for the sync.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/listings/{id}/rooms (the `CreateAirbnbListingRoom` operationId).
func (c *ClientWithResponses) CreateAirbnbListingRoomWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateAirbnbListingRoomClientResponse, error) {
	rsp, err := c.CreateAirbnbListingRoomWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateAirbnbListingRoomClientResponse(rsp)
}

// CreateAirbnbListingRoomWithResponse Create an Airbnb room
//
// Create a new room on an Airbnb listing, with its sleeping arrangement. **Write-side** — calls Airbnb upstream. Requires a connected Airbnb host, else `404 no_connection`.
//
// The response is the room object as Airbnb returned it. The new room is also seeded into our own copy, so the very next `GET /rooms` shows it rather than waiting for the sync.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/listings/{id}/rooms (the `CreateAirbnbListingRoom` operationId).
func (c *ClientWithResponses) CreateAirbnbListingRoomWithResponse(ctx context.Context, id string, body CreateAirbnbListingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateAirbnbListingRoomClientResponse, error) {
	rsp, err := c.CreateAirbnbListingRoom(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateAirbnbListingRoomClientResponse(rsp)
}

// UpdateAirbnbListingRoomWithBodyWithResponse Update an Airbnb room
//
// Change a room's type, number, privacy or sleeping arrangement. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=` and send only the fields you want to change.
//
// `beds` REPLACES the room's whole arrangement — that is Airbnb's semantics for the field — so send every bed the room has, not just the changed one.
//
// Airbnb's room endpoints are keyed by room id alone, so the room is proven to belong to the listing named in the path before anything is sent; a room from another listing returns `404`, the same answer a room that does not exist gets.
//
// On success both stored copies are rebuilt to match, so a read straight after this write returns the new arrangement. `stored` says whether that succeeded.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/rooms (the `UpdateAirbnbListingRoom` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingRoomWithBodyWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingRoomParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingRoomClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingRoomWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingRoomClientResponse(rsp)
}

// UpdateAirbnbListingRoomWithResponse Update an Airbnb room
//
// Change a room's type, number, privacy or sleeping arrangement. **Write-side** — calls Airbnb upstream. Pass the Airbnb-side room id as `?roomId=` and send only the fields you want to change.
//
// `beds` REPLACES the room's whole arrangement — that is Airbnb's semantics for the field — so send every bed the room has, not just the changed one.
//
// Airbnb's room endpoints are keyed by room id alone, so the room is proven to belong to the listing named in the path before anything is sent; a room from another listing returns `404`, the same answer a room that does not exist gets.
//
// On success both stored copies are rebuilt to match, so a read straight after this write returns the new arrangement. `stored` says whether that succeeded.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/rooms (the `UpdateAirbnbListingRoom` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingRoomWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingRoomParams, body UpdateAirbnbListingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingRoomClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingRoom(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingRoomClientResponse(rsp)
}

// ListAirbnbListingSafetyDisclosuresWithResponse List guest-safety disclosures
//
// What a guest is told about the property before they book — exterior security cameras, a decibel noise monitor, pets on the property, stairs, a pool with no fence, weapons, shared spaces, limited parking. Airbnb calls them `listing_expectations_for_guests` and shows them at booking time.
//
// **Pure DB read** from the local mirror. EVERY supported disclosure type is returned, including the ones this listing has not declared (`value: false`), so "does this property have cameras?" has an answer rather than a missing key — `declared` tells you whether Airbnb holds an explicit answer. Types Airbnb returns that are not in the documented set are passed through rather than dropped.
//
// Where a listing is connected to several Airbnb listings, a disclosure declared on any of them is reported as true of the property.
//
// Returns `404` when the listing has no Airbnb connection in this workspace, and `403 listing_inactive` when the listing is inactive.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/safety-disclosures (the `ListAirbnbListingSafetyDisclosures` operationId).
func (c *ClientWithResponses) ListAirbnbListingSafetyDisclosuresWithResponse(ctx context.Context, id string, reqEditors ...RequestEditorFn) (*ListAirbnbListingSafetyDisclosuresClientResponse, error) {
	rsp, err := c.ListAirbnbListingSafetyDisclosures(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbListingSafetyDisclosuresClientResponse(rsp)
}

// UpdateAirbnbListingSafetyDisclosuresWithBodyWithResponse Update guest-safety disclosures
//
// Declare or retract the guest-safety disclosures on the live Airbnb listing.
//
// **This is a MERGE, not a replacement.** Airbnb keeps one value per disclosure type: a type you leave out keeps the value it has, and to retract one you send it with `value: false`. A full replacement would let a partial request silently un-declare a security camera — a guest-safety statement, not a preference.
//
// Only the disclosures are sent upstream. The same Airbnb endpoint carries the cancellation policy and instant-book settings, and this endpoint never touches them.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Airbnb refusing the change is `422 airbnb_rejected` with Airbnb's own reason; an expired or revoked connection is `403 connection_reauth_required`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/safety-disclosures (the `UpdateAirbnbListingSafetyDisclosures` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingSafetyDisclosuresWithBodyWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingSafetyDisclosuresParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingSafetyDisclosuresClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingSafetyDisclosuresWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingSafetyDisclosuresClientResponse(rsp)
}

// UpdateAirbnbListingSafetyDisclosuresWithResponse Update guest-safety disclosures
//
// Declare or retract the guest-safety disclosures on the live Airbnb listing.
//
// **This is a MERGE, not a replacement.** Airbnb keeps one value per disclosure type: a type you leave out keeps the value it has, and to retract one you send it with `value: false`. A full replacement would let a partial request silently un-declare a security camera — a guest-safety statement, not a preference.
//
// Only the disclosures are sent upstream. The same Airbnb endpoint carries the cancellation policy and instant-book settings, and this endpoint never touches them.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Airbnb refusing the change is `422 airbnb_rejected` with Airbnb's own reason; an expired or revoked connection is `403 connection_reauth_required`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/listings/{id}/safety-disclosures (the `UpdateAirbnbListingSafetyDisclosures` operationId).
func (c *ClientWithResponses) UpdateAirbnbListingSafetyDisclosuresWithResponse(ctx context.Context, id string, params *UpdateAirbnbListingSafetyDisclosuresParams, body UpdateAirbnbListingSafetyDisclosuresJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbListingSafetyDisclosuresClientResponse, error) {
	rsp, err := c.UpdateAirbnbListingSafetyDisclosures(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbListingSafetyDisclosuresClientResponse(rsp)
}

// GetAirbnbListingSettingsWithResponse Get Airbnb listing settings
//
// Return an Airbnb listing's host roles, published locales, and regulatory permits. **Pure DB read** — host roles from `listings_airbnb_details.host_roles`, locales from distinct `listings_airbnb_descriptions.locale`, permits from `listings_airbnb_permits`. Scope with `?type=all|hosts|permits|locales` (default `all`, which returns `{ hosts, locales }`). Returns `404` when the listing has no Airbnb connection in this workspace.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/listings/{id}/settings (the `GetAirbnbListingSettings` operationId).
func (c *ClientWithResponses) GetAirbnbListingSettingsWithResponse(ctx context.Context, id string, params *GetAirbnbListingSettingsParams, reqEditors ...RequestEditorFn) (*GetAirbnbListingSettingsClientResponse, error) {
	rsp, err := c.GetAirbnbListingSettings(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbListingSettingsClientResponse(rsp)
}

// ListAirbnbThreadsWithResponse List Airbnb message threads
//
// List Airbnb message threads (one per guest conversation). Cursor-paginated. Each thread includes a preview of the latest message.
//
// Threads on inactive listings are left out; they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/messaging (the `ListAirbnbThreads` operationId).
func (c *ClientWithResponses) ListAirbnbThreadsWithResponse(ctx context.Context, params *ListAirbnbThreadsParams, reqEditors ...RequestEditorFn) (*ListAirbnbThreadsClientResponse, error) {
	rsp, err := c.ListAirbnbThreads(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbThreadsClientResponse(rsp)
}

// GetAirbnbThreadWithResponse Get Airbnb thread
//
// Fetch a single Airbnb message thread by its Airbnb thread id. **Pure DB read** from the local `message_threads` mirror, workspace-scoped. Returns `404 not_found` when no thread matches. For the messages within a thread use `GET /v1/channels/airbnb/messaging/{threadId}/messages`.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/messaging/{threadId} (the `GetAirbnbThread` operationId).
func (c *ClientWithResponses) GetAirbnbThreadWithResponse(ctx context.Context, threadId string, reqEditors ...RequestEditorFn) (*GetAirbnbThreadClientResponse, error) {
	rsp, err := c.GetAirbnbThread(ctx, threadId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbThreadClientResponse(rsp)
}

// ListAirbnbThreadMessagesWithResponse Get Airbnb messages
//
// Messages stored for an Airbnb thread, as recorded rows (not the unified `Message` shape — use `GET /v1/conversations/{id}/messages` for that). By default returns 50 per page, newest first; walk older pages with `?cursor=` (the `pagination.nextCursor` of the previous page) until `pagination.hasMore` is `false`. `?all=true` returns up to 1000 rows oldest-first in one response, with no `pagination`.
//
// Each row carries `attachments` — photos and other files on that message, inbound or outbound — in the same shape as the unified endpoint.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/messaging/{threadId}/messages (the `ListAirbnbThreadMessages` operationId).
func (c *ClientWithResponses) ListAirbnbThreadMessagesWithResponse(ctx context.Context, threadId string, params *ListAirbnbThreadMessagesParams, reqEditors ...RequestEditorFn) (*ListAirbnbThreadMessagesClientResponse, error) {
	rsp, err := c.ListAirbnbThreadMessages(ctx, threadId, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbThreadMessagesClientResponse(rsp)
}

// SendAirbnbMessageWithBodyWithResponse Send Airbnb message
//
// Send a message in an Airbnb thread as the host. Airbnb enforces content rules (no off-platform contact info, no external URLs) — violating messages are rejected upstream and surface as `422 airbnb_rejected` carrying Airbnb's own reason. Resending the same text is refused again; edit it first. `502 airbnb_error` is the other answer and means something else entirely: Airbnb did not complete the send, so retry it unchanged.
//
// ### Sending a photo or video (`mediaUrl`)
//
// Airbnb only accepts media uploaded to a signed URL it issues, one file per message and no text on the same message. With `mediaUrl`, Repull downloads the file (public `https://` only, 10 MB max), reads its real type from the bytes (JPEG, PNG, GIF, WebP — converted to JPEG — or MP4/QuickTime), uploads it to Airbnb and sends it; `message`, if given, follows as a separate message. This is the same flow as `POST /v1/conversations/{id}/messages` with `attachments` — prefer that endpoint, which also takes several files per request. The response is a `SendMessageResponse`, the send is recorded in the conversation, and failures are the 422 codes documented there (`attachment_type_not_supported`, `attachment_too_large`, `message_not_sent` for a pre-booking thread, …). The thread must already be synced to Repull (`GET /v1/conversations` lists them), otherwise `404`.
//
// Text-only sends (no `mediaUrl`) go straight to Airbnb and return Airbnb's message object.
//
// The `{threadId}` is the Airbnb thread id — the `externalThreadId` field on a unified `Conversation` (`GET /v1/conversations`).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/messaging/{threadId}/messages (the `SendAirbnbMessage` operationId).
func (c *ClientWithResponses) SendAirbnbMessageWithBodyWithResponse(ctx context.Context, threadId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SendAirbnbMessageClientResponse, error) {
	rsp, err := c.SendAirbnbMessageWithBody(ctx, threadId, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSendAirbnbMessageClientResponse(rsp)
}

// SendAirbnbMessageWithResponse Send Airbnb message
//
// Send a message in an Airbnb thread as the host. Airbnb enforces content rules (no off-platform contact info, no external URLs) — violating messages are rejected upstream and surface as `422 airbnb_rejected` carrying Airbnb's own reason. Resending the same text is refused again; edit it first. `502 airbnb_error` is the other answer and means something else entirely: Airbnb did not complete the send, so retry it unchanged.
//
// ### Sending a photo or video (`mediaUrl`)
//
// Airbnb only accepts media uploaded to a signed URL it issues, one file per message and no text on the same message. With `mediaUrl`, Repull downloads the file (public `https://` only, 10 MB max), reads its real type from the bytes (JPEG, PNG, GIF, WebP — converted to JPEG — or MP4/QuickTime), uploads it to Airbnb and sends it; `message`, if given, follows as a separate message. This is the same flow as `POST /v1/conversations/{id}/messages` with `attachments` — prefer that endpoint, which also takes several files per request. The response is a `SendMessageResponse`, the send is recorded in the conversation, and failures are the 422 codes documented there (`attachment_type_not_supported`, `attachment_too_large`, `message_not_sent` for a pre-booking thread, …). The thread must already be synced to Repull (`GET /v1/conversations` lists them), otherwise `404`.
//
// Text-only sends (no `mediaUrl`) go straight to Airbnb and return Airbnb's message object.
//
// The `{threadId}` is the Airbnb thread id — the `externalThreadId` field on a unified `Conversation` (`GET /v1/conversations`).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/messaging/{threadId}/messages (the `SendAirbnbMessage` operationId).
func (c *ClientWithResponses) SendAirbnbMessageWithResponse(ctx context.Context, threadId string, body SendAirbnbMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*SendAirbnbMessageClientResponse, error) {
	rsp, err := c.SendAirbnbMessage(ctx, threadId, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSendAirbnbMessageClientResponse(rsp)
}

// UpdateAirbnbMessageWithBodyWithResponse Edit / react to / mark an Airbnb message
//
// Act on a single message in an Airbnb thread. **Write-side** — calls Airbnb upstream. The `action` discriminator selects the operation:
//
// - `edit` — replace message text (requires `message`).
// - `unsend` — retract the message.
// - `read` — mark the message as read.
// - `react` — add a reaction (requires `reaction`).
//
// Requires a connected Airbnb host, else `404 no_connection`.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/channels/airbnb/messaging/{threadId}/messages/{messageId} (the `UpdateAirbnbMessage` operationId).
func (c *ClientWithResponses) UpdateAirbnbMessageWithBodyWithResponse(ctx context.Context, threadId string, messageId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateAirbnbMessageClientResponse, error) {
	rsp, err := c.UpdateAirbnbMessageWithBody(ctx, threadId, messageId, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbMessageClientResponse(rsp)
}

// UpdateAirbnbMessageWithResponse Edit / react to / mark an Airbnb message
//
// Act on a single message in an Airbnb thread. **Write-side** — calls Airbnb upstream. The `action` discriminator selects the operation:
//
// - `edit` — replace message text (requires `message`).
// - `unsend` — retract the message.
// - `read` — mark the message as read.
// - `react` — add a reaction (requires `reaction`).
//
// Requires a connected Airbnb host, else `404 no_connection`.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/channels/airbnb/messaging/{threadId}/messages/{messageId} (the `UpdateAirbnbMessage` operationId).
func (c *ClientWithResponses) UpdateAirbnbMessageWithResponse(ctx context.Context, threadId string, messageId string, body UpdateAirbnbMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateAirbnbMessageClientResponse, error) {
	rsp, err := c.UpdateAirbnbMessage(ctx, threadId, messageId, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateAirbnbMessageClientResponse(rsp)
}

// WithdrawAirbnbOfferWithResponse Withdraw Airbnb special offer
//
// Withdraw a special offer the guest has not booked. **Write-side** — calls Airbnb upstream. Pass the Airbnb offer id as `?offerId=`. The Repull-id equivalent is `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/channels/airbnb/offers (the `WithdrawAirbnbOffer` operationId).
func (c *ClientWithResponses) WithdrawAirbnbOfferWithResponse(ctx context.Context, params *WithdrawAirbnbOfferParams, reqEditors ...RequestEditorFn) (*WithdrawAirbnbOfferClientResponse, error) {
	rsp, err := c.WithdrawAirbnbOffer(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseWithdrawAirbnbOfferClientResponse(rsp)
}

// GetAirbnbOfferWithResponse Get Airbnb special offer
//
// Read a pre-approval or special offer from Airbnb by its Airbnb id. **Live read** — calls Airbnb upstream. Pass the id as `?offerId=`. The Repull-id equivalent is `GET /v1/conversations/{id}/special-offers/{offerId}`, which also confirms the offer belongs to that conversation.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/offers (the `GetAirbnbOffer` operationId).
func (c *ClientWithResponses) GetAirbnbOfferWithResponse(ctx context.Context, params *GetAirbnbOfferParams, reqEditors ...RequestEditorFn) (*GetAirbnbOfferClientResponse, error) {
	rsp, err := c.GetAirbnbOffer(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbOfferClientResponse(rsp)
}

// CreateAirbnbOfferWithBodyWithResponse Create Airbnb special offer or pre-approval
//
// Create a pre-approval or a special offer on an Airbnb thread, addressed by **Airbnb** ids. **Write-side** — calls Airbnb upstream. The Repull-id equivalents, which also update the inquiry in Vanio, are `POST /v1/conversations/{id}/pre-approval` and `POST /v1/conversations/{id}/special-offers` — prefer those unless you only hold Airbnb ids.
//
// - `type: "preapproval"` — let the guest book the dates and price they asked about. Requires `thread_id`; optional `block_instant_booking`.
// - `type: "offer"` — your own terms. Requires `thread_id`, `listing_id` (the **Airbnb** listing id, as a string), `start_date`, `nights`, `total_price` (whole stay, listing currency) and `guest_details` with `number_of_guests` (or `number_of_adults`; Airbnb counts adults + children).
//
// The body is validated before anything reaches Airbnb (a `422 invalid_params` names the field), and unknown fields are refused. The legacy spellings `threadId` and `blockInstantBooking` still work. The request is sent as the Airbnb account that owns the thread or listing.
//
// Airbnb refusals are mapped rather than returned as a 500: `409 inquiry_no_longer_open` / `inquiry_expired` when the inquiry moved on, `422 airbnb_rejected` with Airbnb’s reason otherwise, `403 connection_reauth_required` when the grant does not allow it.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/offers (the `CreateAirbnbOffer` operationId).
func (c *ClientWithResponses) CreateAirbnbOfferWithBodyWithResponse(ctx context.Context, params *CreateAirbnbOfferParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateAirbnbOfferClientResponse, error) {
	rsp, err := c.CreateAirbnbOfferWithBody(ctx, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateAirbnbOfferClientResponse(rsp)
}

// CreateAirbnbOfferWithResponse Create Airbnb special offer or pre-approval
//
// Create a pre-approval or a special offer on an Airbnb thread, addressed by **Airbnb** ids. **Write-side** — calls Airbnb upstream. The Repull-id equivalents, which also update the inquiry in Vanio, are `POST /v1/conversations/{id}/pre-approval` and `POST /v1/conversations/{id}/special-offers` — prefer those unless you only hold Airbnb ids.
//
// - `type: "preapproval"` — let the guest book the dates and price they asked about. Requires `thread_id`; optional `block_instant_booking`.
// - `type: "offer"` — your own terms. Requires `thread_id`, `listing_id` (the **Airbnb** listing id, as a string), `start_date`, `nights`, `total_price` (whole stay, listing currency) and `guest_details` with `number_of_guests` (or `number_of_adults`; Airbnb counts adults + children).
//
// The body is validated before anything reaches Airbnb (a `422 invalid_params` names the field), and unknown fields are refused. The legacy spellings `threadId` and `blockInstantBooking` still work. The request is sent as the Airbnb account that owns the thread or listing.
//
// Airbnb refusals are mapped rather than returned as a 500: `409 inquiry_no_longer_open` / `inquiry_expired` when the inquiry moved on, `422 airbnb_rejected` with Airbnb’s reason otherwise, `403 connection_reauth_required` when the grant does not allow it.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/offers (the `CreateAirbnbOffer` operationId).
func (c *ClientWithResponses) CreateAirbnbOfferWithResponse(ctx context.Context, params *CreateAirbnbOfferParams, body CreateAirbnbOfferJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateAirbnbOfferClientResponse, error) {
	rsp, err := c.CreateAirbnbOffer(ctx, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateAirbnbOfferClientResponse(rsp)
}

// ListAirbnbReservationsWithResponse List Airbnb reservations
//
// Cursor-paginated list of reservations sourced directly from Airbnb. Use this when you need Airbnb-specific fields (guest payout split, cancellation policy snapshot) that the unified `/v1/reservations` endpoint flattens away.
//
// Walk pages with `?cursor=<pagination.nextCursor>` until `pagination.hasMore` is `false`. The cursor is opaque — never construct or parse it client-side.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. Internally this walks upstream Airbnb cursor pages to skip rows, so deep offsets cost N/limit upstream round-trips; cursor remains the better choice for deep pagination.
//
// When `status` is omitted, all statuses are returned (Airbnb defaults to `accepted` only on its own surface, but this endpoint normalises to "all"). Pass `?status=accepted` to scope.
//
// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/reservations (the `ListAirbnbReservations` operationId).
func (c *ClientWithResponses) ListAirbnbReservationsWithResponse(ctx context.Context, params *ListAirbnbReservationsParams, reqEditors ...RequestEditorFn) (*ListAirbnbReservationsClientResponse, error) {
	rsp, err := c.ListAirbnbReservations(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbReservationsClientResponse(rsp)
}

// GetAirbnbReservationWithResponse Get Airbnb reservation
//
// Fetch a single Airbnb reservation by Airbnb confirmation code (e.g. `HMABCDEF12`).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/reservations/{code} (the `GetAirbnbReservation` operationId).
func (c *ClientWithResponses) GetAirbnbReservationWithResponse(ctx context.Context, code string, reqEditors ...RequestEditorFn) (*GetAirbnbReservationClientResponse, error) {
	rsp, err := c.GetAirbnbReservation(ctx, code, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAirbnbReservationClientResponse(rsp)
}

// AirbnbReservationActionWithBodyWithResponse Accept, decline or cancel an Airbnb reservation
//
// Act on an Airbnb reservation by its Airbnb confirmation code. **Write-side** — calls Airbnb upstream, as the Airbnb account that owns the booking.
//
// - `accept` — accept a pending booking request.
// - `decline` — decline a pending booking request. Requires `reason` (one of Airbnb's decline reasons) and `message` (sent to the guest, at most 500 characters).
// - `cancel` — cancel a confirmed booking as the host. Requires `reason` (one of Airbnb's host-cancellation reasons). **Host cancellations carry Airbnb penalties.**
//
// The body is validated before anything reaches Airbnb; unknown fields are refused. There is no `pre-approve` action: a pre-approval answers an inquiry, which has no confirmation code — use `POST /v1/conversations/{id}/pre-approval`. For accept/decline, `POST /v1/reservations/{id}/accept` and `/decline` do the same by Repull id and keep the reservation in Repull in sync.
//
// Airbnb refusals are mapped rather than returned as a 500: a request that already moved on is `409 request_no_longer_pending` (do not retry), an expired one `409 request_expired`, any other refusal `422 airbnb_rejected` with Airbnb's reason.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/reservations/{code} (the `AirbnbReservationAction` operationId).
func (c *ClientWithResponses) AirbnbReservationActionWithBodyWithResponse(ctx context.Context, code string, params *AirbnbReservationActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AirbnbReservationActionClientResponse, error) {
	rsp, err := c.AirbnbReservationActionWithBody(ctx, code, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAirbnbReservationActionClientResponse(rsp)
}

// AirbnbReservationActionWithResponse Accept, decline or cancel an Airbnb reservation
//
// Act on an Airbnb reservation by its Airbnb confirmation code. **Write-side** — calls Airbnb upstream, as the Airbnb account that owns the booking.
//
// - `accept` — accept a pending booking request.
// - `decline` — decline a pending booking request. Requires `reason` (one of Airbnb's decline reasons) and `message` (sent to the guest, at most 500 characters).
// - `cancel` — cancel a confirmed booking as the host. Requires `reason` (one of Airbnb's host-cancellation reasons). **Host cancellations carry Airbnb penalties.**
//
// The body is validated before anything reaches Airbnb; unknown fields are refused. There is no `pre-approve` action: a pre-approval answers an inquiry, which has no confirmation code — use `POST /v1/conversations/{id}/pre-approval`. For accept/decline, `POST /v1/reservations/{id}/accept` and `/decline` do the same by Repull id and keep the reservation in Repull in sync.
//
// Airbnb refusals are mapped rather than returned as a 500: a request that already moved on is `409 request_no_longer_pending` (do not retry), an expired one `409 request_expired`, any other refusal `422 airbnb_rejected` with Airbnb's reason.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/reservations/{code} (the `AirbnbReservationAction` operationId).
func (c *ClientWithResponses) AirbnbReservationActionWithResponse(ctx context.Context, code string, params *AirbnbReservationActionParams, body AirbnbReservationActionJSONRequestBody, reqEditors ...RequestEditorFn) (*AirbnbReservationActionClientResponse, error) {
	rsp, err := c.AirbnbReservationAction(ctx, code, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAirbnbReservationActionClientResponse(rsp)
}

// ListAirbnbReviewsWithResponse List Airbnb reviews
//
// List reviews left by guests on Airbnb listings in this workspace. Includes both reviews of the host and reviews of the guest (where the host has not yet submitted theirs).
//
// Reviews of inactive listings are left out; they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/reviews (the `ListAirbnbReviews` operationId).
func (c *ClientWithResponses) ListAirbnbReviewsWithResponse(ctx context.Context, params *ListAirbnbReviewsParams, reqEditors ...RequestEditorFn) (*ListAirbnbReviewsClientResponse, error) {
	rsp, err := c.ListAirbnbReviews(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbReviewsClientResponse(rsp)
}

// RespondAirbnbReviewLegacyWithResponse Respond to / submit Airbnb review (legacy)
//
// Legacy action-based shape. Body `{ action: "respond"|"submit", reviewId, response?, review? }`. Kept for backwards compatibility — prefer `PUT /v1/channels/airbnb/reviews/{id}` (edit) and `POST /v1/channels/airbnb/reviews/{id}/respond` (reply) for new integrations.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/reviews (the `RespondAirbnbReviewLegacy` operationId).
//
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *ClientWithResponses) RespondAirbnbReviewLegacyWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*RespondAirbnbReviewLegacyClientResponse, error) {
	rsp, err := c.RespondAirbnbReviewLegacy(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRespondAirbnbReviewLegacyClientResponse(rsp)
}

// EditAirbnbReviewWithBodyWithResponse Edit Airbnb host review
//
// Edit a host-side review for an Airbnb stay. Airbnb collapses POST + PUT into the same upstream call (`PUT /v2/listing_reviews/{id}`), so this endpoint covers both initial submit and subsequent edits while the review window is open.
//
// Body is a partial `AirbnbReview` — pass the fields you want to change (rating, public review, private feedback, category ratings).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/reviews/{id} (the `EditAirbnbReview` operationId).
func (c *ClientWithResponses) EditAirbnbReviewWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*EditAirbnbReviewClientResponse, error) {
	rsp, err := c.EditAirbnbReviewWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseEditAirbnbReviewClientResponse(rsp)
}

// EditAirbnbReviewWithResponse Edit Airbnb host review
//
// Edit a host-side review for an Airbnb stay. Airbnb collapses POST + PUT into the same upstream call (`PUT /v2/listing_reviews/{id}`), so this endpoint covers both initial submit and subsequent edits while the review window is open.
//
// Body is a partial `AirbnbReview` — pass the fields you want to change (rating, public review, private feedback, category ratings).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/airbnb/reviews/{id} (the `EditAirbnbReview` operationId).
func (c *ClientWithResponses) EditAirbnbReviewWithResponse(ctx context.Context, id string, body EditAirbnbReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*EditAirbnbReviewClientResponse, error) {
	rsp, err := c.EditAirbnbReview(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseEditAirbnbReviewClientResponse(rsp)
}

// RespondAirbnbReviewWithBodyWithResponse Respond to Airbnb review
//
// Post a public host response to a guest review. Airbnb allows one response per review — repeated POSTs return 409. Response text is capped at 1000 characters.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/reviews/{id}/respond (the `RespondAirbnbReview` operationId).
func (c *ClientWithResponses) RespondAirbnbReviewWithBodyWithResponse(ctx context.Context, id string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*RespondAirbnbReviewClientResponse, error) {
	rsp, err := c.RespondAirbnbReviewWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRespondAirbnbReviewClientResponse(rsp)
}

// RespondAirbnbReviewWithResponse Respond to Airbnb review
//
// Post a public host response to a guest review. Airbnb allows one response per review — repeated POSTs return 409. Response text is capped at 1000 characters.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/reviews/{id}/respond (the `RespondAirbnbReview` operationId).
func (c *ClientWithResponses) RespondAirbnbReviewWithResponse(ctx context.Context, id string, body RespondAirbnbReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*RespondAirbnbReviewClientResponse, error) {
	rsp, err := c.RespondAirbnbReview(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRespondAirbnbReviewClientResponse(rsp)
}

// ListAirbnbTransactionsWithResponse List Airbnb transactions
//
// List Airbnb host transactions (reservation earnings, payouts, resolution adjustments) for this workspace, newest first. **Pure DB read** — customer-facing reads never call Airbnb upstream; they serve the `airbnb_transactions` mirror. Each row carries the genuine host- and guest-side financial breakdown (accommodation subtotal, cleaning fee, host + guest service fees split base/VAT, tax buckets, expected/actual host payout with settlement status). Trigger a refresh with `POST` on this path. When the mirror is empty or the host disconnected, `dataFreshness.stale = true` with a `reason` (`never_synced`, `host_disconnected_<iso>`, `sync_lag_>_24h`).
//
// Transactions of reservations on inactive listings are left out; payout rows, which belong to no listing, are always included.
//
// **Several Airbnb accounts?** A workspace can connect more than one. By default this returns every connected account's rows; pass `?account_id=<airbnb host id>` to scope to one. Every row carries `accountId` + `accountName` either way, and `dataFreshness.accounts[]` reports each account's freshness separately, so one disconnected host no longer marks the whole response stale.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/airbnb/transactions (the `ListAirbnbTransactions` operationId).
func (c *ClientWithResponses) ListAirbnbTransactionsWithResponse(ctx context.Context, params *ListAirbnbTransactionsParams, reqEditors ...RequestEditorFn) (*ListAirbnbTransactionsClientResponse, error) {
	rsp, err := c.ListAirbnbTransactions(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListAirbnbTransactionsClientResponse(rsp)
}

// SyncAirbnbTransactionsWithBodyWithResponse Sync Airbnb transactions
//
// Refresh the Airbnb transactions mirror for this workspace by pulling from Airbnb upstream and upserting the breakdown that `GET` serves. Optional JSON body `{ start_date, end_date, transaction_type }` (`transaction_type` is `COMPLETED` or `UPCOMING`; both are synced when omitted). Returns `{ synced, count }`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/transactions (the `SyncAirbnbTransactions` operationId).
func (c *ClientWithResponses) SyncAirbnbTransactionsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SyncAirbnbTransactionsClientResponse, error) {
	rsp, err := c.SyncAirbnbTransactionsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSyncAirbnbTransactionsClientResponse(rsp)
}

// SyncAirbnbTransactionsWithResponse Sync Airbnb transactions
//
// Refresh the Airbnb transactions mirror for this workspace by pulling from Airbnb upstream and upserting the breakdown that `GET` serves. Optional JSON body `{ start_date, end_date, transaction_type }` (`transaction_type` is `COMPLETED` or `UPCOMING`; both are synced when omitted). Returns `{ synced, count }`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/airbnb/transactions (the `SyncAirbnbTransactions` operationId).
func (c *ClientWithResponses) SyncAirbnbTransactionsWithResponse(ctx context.Context, body SyncAirbnbTransactionsJSONRequestBody, reqEditors ...RequestEditorFn) (*SyncAirbnbTransactionsClientResponse, error) {
	rsp, err := c.SyncAirbnbTransactions(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSyncAirbnbTransactionsClientResponse(rsp)
}

// GetBookingAvailabilityWithResponse Read current Booking.com rates/availability/restrictions
//
// Read the current rate, availability, and restriction state for a Booking.com property so you can reconcile before writing with the PUT on this path. Keyed by `property_id` (the Booking hotel id), symmetric with the PUT.
//
// Proxies Booking's `getRoomRateAvailability` — the returned fields (price, rooms-to-sell, min/max stay, closed-to-arrival/departure, stop-sell) are whatever Booking.com emits for the window. A listing-id-keyed equivalent is available at `GET /v1/channels/booking/listings/{id}/pricing`.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/booking/availability (the `GetBookingAvailability` operationId).
func (c *ClientWithResponses) GetBookingAvailabilityWithResponse(ctx context.Context, params *GetBookingAvailabilityParams, reqEditors ...RequestEditorFn) (*GetBookingAvailabilityClientResponse, error) {
	rsp, err := c.GetBookingAvailability(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetBookingAvailabilityClientResponse(rsp)
}

// UpdateBookingAvailabilityWithBodyWithResponse Update Booking.com rates/availability
//
// Write rates, availability and restrictions to a Booking.com property. `type` selects the write:
//
// - `rates` — nightly prices, plus any length-of-stay / arrival restrictions sent with them.
// - `availability` — inventory (`availableRooms`), the stop-sell flag (`closed`), and restrictions. Omit `availableRooms` and `closed` for a restriction-only write.
// - `derived-pricing` — occupancy-derived pricing rules.
//
// **Dates are inclusive at both ends.** `{ "start": "2026-11-04", "end": "2026-11-04" }` is exactly one night.
//
// **A rate amount needs an occupancy.** Booking.com stores the amount against the party size the rate plan prices: sent above that number it declines the price in silence, sent below it it answers 400. Send `occupancy`, or omit it and Repull resolves it from Booking.com's own data and echoes the value and its `source` back in `occupancy[]`. If it cannot be resolved the write is refused with `422` naming `updates[N].occupancy`.
//
// **Restrictions are sent in the same call, on their own wire.** A price and a minimum stay are two writes on Booking.com's side. Send them together and the response reports each separately: `price` and `restrictions` carry their own state, their own read-back, and — when refused — Booking.com's own reason. The top-level `applied` is `partial` when they disagree, so a price that landed is never reported as a failure. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` because Booking.com's notification has no element for them — set those on the rate plan in the Extranet. Nothing you send is ever silently ignored.
//
// **Inventory is not part of a rate update.** `roomsToSell` on a `rates` update returns `422 inventory_not_in_rate_update`; send it as `type: "availability"` instead.
//
// **The response says what is known.** Booking.com acknowledges a write with no per-date status, so the nights are read back — prices and restrictions out of the same read: `applied` is `verified`, `mismatch`, `partial`, `rejected` or `unverified` (send `verify: false` to skip the read-back). A bare acknowledgement is never reported as "all updates applied". Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
//
// Restrictions never leak across channels — this endpoint writes only to Booking.com. When Booking.com refuses a write outright, their own reason comes back as `422 booking_rejected` with `booking_ruid`; a genuine outage on their side is `502 booking_error`.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/booking/availability (the `UpdateBookingAvailability` operationId).
func (c *ClientWithResponses) UpdateBookingAvailabilityWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateBookingAvailabilityClientResponse, error) {
	rsp, err := c.UpdateBookingAvailabilityWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateBookingAvailabilityClientResponse(rsp)
}

// UpdateBookingAvailabilityWithResponse Update Booking.com rates/availability
//
// Write rates, availability and restrictions to a Booking.com property. `type` selects the write:
//
// - `rates` — nightly prices, plus any length-of-stay / arrival restrictions sent with them.
// - `availability` — inventory (`availableRooms`), the stop-sell flag (`closed`), and restrictions. Omit `availableRooms` and `closed` for a restriction-only write.
// - `derived-pricing` — occupancy-derived pricing rules.
//
// **Dates are inclusive at both ends.** `{ "start": "2026-11-04", "end": "2026-11-04" }` is exactly one night.
//
// **A rate amount needs an occupancy.** Booking.com stores the amount against the party size the rate plan prices: sent above that number it declines the price in silence, sent below it it answers 400. Send `occupancy`, or omit it and Repull resolves it from Booking.com's own data and echoes the value and its `source` back in `occupancy[]`. If it cannot be resolved the write is refused with `422` naming `updates[N].occupancy`.
//
// **Restrictions are sent in the same call, on their own wire.** A price and a minimum stay are two writes on Booking.com's side. Send them together and the response reports each separately: `price` and `restrictions` carry their own state, their own read-back, and — when refused — Booking.com's own reason. The top-level `applied` is `partial` when they disagree, so a price that landed is never reported as a failure. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` because Booking.com's notification has no element for them — set those on the rate plan in the Extranet. Nothing you send is ever silently ignored.
//
// **Inventory is not part of a rate update.** `roomsToSell` on a `rates` update returns `422 inventory_not_in_rate_update`; send it as `type: "availability"` instead.
//
// **The response says what is known.** Booking.com acknowledges a write with no per-date status, so the nights are read back — prices and restrictions out of the same read: `applied` is `verified`, `mismatch`, `partial`, `rejected` or `unverified` (send `verify: false` to skip the read-back). A bare acknowledgement is never reported as "all updates applied". Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
//
// Restrictions never leak across channels — this endpoint writes only to Booking.com. When Booking.com refuses a write outright, their own reason comes back as `422 booking_rejected` with `booking_ruid`; a genuine outage on their side is `502 booking_error`.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/booking/availability (the `UpdateBookingAvailability` operationId).
func (c *ClientWithResponses) UpdateBookingAvailabilityWithResponse(ctx context.Context, body UpdateBookingAvailabilityJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateBookingAvailabilityClientResponse, error) {
	rsp, err := c.UpdateBookingAvailability(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateBookingAvailabilityClientResponse(rsp)
}

// GetBookingChargesWithResponse Get Booking.com charges
//
// Fetch the extra-charge set (cleaning fee, resort fee, city tax, etc.) configured for a Booking.com property. Pass the Booking.com `property_id` as a query param — required.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/booking/charges (the `GetBookingCharges` operationId).
func (c *ClientWithResponses) GetBookingChargesWithResponse(ctx context.Context, params *GetBookingChargesParams, reqEditors ...RequestEditorFn) (*GetBookingChargesClientResponse, error) {
	rsp, err := c.GetBookingCharges(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetBookingChargesClientResponse(rsp)
}

// UpdateBookingChargesWithBodyWithResponse Set Booking.com charges
//
// Replace the extra-charge set for a Booking.com property. The body carries the target `property_id` and the full `charges` array — Booking treats the write as a full replacement, so include every charge you want to keep.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/booking/charges (the `UpdateBookingCharges` operationId).
func (c *ClientWithResponses) UpdateBookingChargesWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateBookingChargesClientResponse, error) {
	rsp, err := c.UpdateBookingChargesWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateBookingChargesClientResponse(rsp)
}

// UpdateBookingChargesWithResponse Set Booking.com charges
//
// Replace the extra-charge set for a Booking.com property. The body carries the target `property_id` and the full `charges` array — Booking treats the write as a full replacement, so include every charge you want to keep.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/booking/charges (the `UpdateBookingCharges` operationId).
func (c *ClientWithResponses) UpdateBookingChargesWithResponse(ctx context.Context, body UpdateBookingChargesJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateBookingChargesClientResponse, error) {
	rsp, err := c.UpdateBookingCharges(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateBookingChargesClientResponse(rsp)
}

// GetBookingContentWithResponse Get Booking.com content
//
// Read one kind of content for a Booking.com property, straight from Booking.com.
//
// | `type` | What it is |
// |---|---|
// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
// | `policies` | Cancellation and prepayment policies. |
// | `licences` | The region's licence rules and the licence on file. |
// | `checkin_methods` | How guests get in (holiday homes). |
// | `contacts` | Who Booking.com contacts about the property. |
//
// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/booking/content (the `GetBookingContent` operationId).
func (c *ClientWithResponses) GetBookingContentWithResponse(ctx context.Context, params *GetBookingContentParams, reqEditors ...RequestEditorFn) (*GetBookingContentClientResponse, error) {
	rsp, err := c.GetBookingContent(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetBookingContentClientResponse(rsp)
}

// UpdateBookingContentWithBodyWithResponse Update Booking.com content
//
// Write one kind of content to a Booking.com property only. Nothing on the canonical listing or on Airbnb changes. To send the listing's own content to every channel instead, use `PUT /v1/listings/{id}/content` and publish.
//
// | `type` | What it is |
// |---|---|
// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
// | `policies` | Cancellation and prepayment policies. |
// | `licences` | The region's licence rules and the licence on file. |
// | `checkin_methods` | How guests get in (holiday homes). |
// | `contacts` | Who Booking.com contacts about the property. |
//
// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
//
// What each `type` takes:
//
// - `description`: `text`, optional `language` (default `en`).
// - `facilities`: `facilities: [{ facility_id | room_facility_id, state: "PRESENT" | "MISSING", instances? }]`. Facilities you do not send stay as they are.
// - `photos`: `photos: [{ url }]`, uploaded in the background. With `room_id`, send `photo_ids` instead to add photos that have finished processing to that room.
// - `settings`: `settings: { <block>: {…} }`, for example `{ "pets": { "pets_allowed": "PETS_ALLOWED" } }`. Each block is written separately and reported in `results`.
// - `policies`: `policyCode` (152 = free cancellation at any time, 1 = non-refundable, …), optional `prepaymentRequired`; add `policyId` to change an existing policy. A property holds at most 7 policies and none can be deleted.
// - `licences`: `variantId` and `contentData: [{ name, value }]`, from the rules `GET ?type=licences` returns; optional `room_id`.
// - `checkin_methods`: `methods: [{ checkin_method }]`, using a name from `GET ?type=checkin_methods` `available`.
// - `contacts`: `contacts: [...]` in Booking.com's contact shape.
//
// If Booking.com refuses the write, the response is `422 booking_rejected` with Booking.com's reason, even when Booking.com answered HTTP 200. Resending the same body will be refused again.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/content (the `UpdateBookingContent` operationId).
func (c *ClientWithResponses) UpdateBookingContentWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateBookingContentClientResponse, error) {
	rsp, err := c.UpdateBookingContentWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateBookingContentClientResponse(rsp)
}

// UpdateBookingContentWithResponse Update Booking.com content
//
// Write one kind of content to a Booking.com property only. Nothing on the canonical listing or on Airbnb changes. To send the listing's own content to every channel instead, use `PUT /v1/listings/{id}/content` and publish.
//
// | `type` | What it is |
// |---|---|
// | `photos` | The property's photos. Add `room_id` to read one room's gallery. |
// | `facilities` | Property facilities, or a room's with `room_id` (Booking.com's ids — `GET` returns them). |
// | `description` | The property description. Booking.com rewrites what you send into its own multilingual copy; allow about 3 hours to appear. |
// | `settings` | House rules, pets, children, damage deposit, invoice recipient, booking model. |
// | `policies` | Cancellation and prepayment policies. |
// | `licences` | The region's licence rules and the licence on file. |
// | `checkin_methods` | How guests get in (holiday homes). |
// | `contacts` | Who Booking.com contacts about the property. |
//
// `amenities` is accepted as another name for `facilities`, and `descriptions` for `description`.
//
// What each `type` takes:
//
// - `description`: `text`, optional `language` (default `en`).
// - `facilities`: `facilities: [{ facility_id | room_facility_id, state: "PRESENT" | "MISSING", instances? }]`. Facilities you do not send stay as they are.
// - `photos`: `photos: [{ url }]`, uploaded in the background. With `room_id`, send `photo_ids` instead to add photos that have finished processing to that room.
// - `settings`: `settings: { <block>: {…} }`, for example `{ "pets": { "pets_allowed": "PETS_ALLOWED" } }`. Each block is written separately and reported in `results`.
// - `policies`: `policyCode` (152 = free cancellation at any time, 1 = non-refundable, …), optional `prepaymentRequired`; add `policyId` to change an existing policy. A property holds at most 7 policies and none can be deleted.
// - `licences`: `variantId` and `contentData: [{ name, value }]`, from the rules `GET ?type=licences` returns; optional `room_id`.
// - `checkin_methods`: `methods: [{ checkin_method }]`, using a name from `GET ?type=checkin_methods` `available`.
// - `contacts`: `contacts: [...]` in Booking.com's contact shape.
//
// If Booking.com refuses the write, the response is `422 booking_rejected` with Booking.com's reason, even when Booking.com answered HTTP 200. Resending the same body will be refused again.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/content (the `UpdateBookingContent` operationId).
func (c *ClientWithResponses) UpdateBookingContentWithResponse(ctx context.Context, body UpdateBookingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateBookingContentClientResponse, error) {
	rsp, err := c.UpdateBookingContent(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateBookingContentClientResponse(rsp)
}

// MapBookingRoomWithBodyWithResponse Map a Booking.com room to a Repull listing
//
// Link a Booking.com room to a canonical Repull listing — the API-key equivalent of the room mapping the hosted Connect flow performs, and the counterpart of `POST /v1/channels/airbnb/listings/map`.
//
// Discover `roomBookingId` with `GET /v1/channels/booking/properties/{id}/rooms`, which returns every room of a property with the `roomId` this route takes.
//
// Booking.com attaches at the ROOM level: a property is a building and its rooms are what a guest books, so each room maps to one listing. Pass `listingId: null` to unmap a room and remove its channel link.
//
// The room mapping and its channel link are repointed together in one transaction, so a link can never outlive the mapping it describes — a stale link keeps routing that room's reservations to the previous listing. Re-sending a mapping that is already in place writes nothing (`alreadyMapped: true`).
//
// **The property's reservations are pulled as part of the call.** Once the room is mapped, every active reservation Booking.com holds for the property is imported and attached to its listing — `reservationsImported` says how many were processed. One already present is left as it is, so re-sending never duplicates. You do not need a follow-up call: reservations that arrived before the room was mapped are never picked up by the regular sync, so this is the moment they are brought in. It runs on every successful map, including a re-send, so re-sending retries an import that did not run. If the import cannot run, the mapping still stands and `reservationsImported` is `null`. A property with a long booking history can take tens of seconds. Unmapping pulls nothing.
//
// Unlike the Airbnb route, there is no conflict when the target listing already carries another Booking.com room: one listing served by several rooms is a normal arrangement and is not refused.
//
// Scope is enforced on both sides against your workspace — the room's property and the target listing. A room or listing belonging to another workspace returns the same 404 as one that does not exist.
//
// Returns `403 listing_inactive` when the target listing, or the listing the room is mapped to now, is inactive; nothing is changed.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/listings/map (the `MapBookingRoom` operationId).
func (c *ClientWithResponses) MapBookingRoomWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*MapBookingRoomClientResponse, error) {
	rsp, err := c.MapBookingRoomWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseMapBookingRoomClientResponse(rsp)
}

// MapBookingRoomWithResponse Map a Booking.com room to a Repull listing
//
// Link a Booking.com room to a canonical Repull listing — the API-key equivalent of the room mapping the hosted Connect flow performs, and the counterpart of `POST /v1/channels/airbnb/listings/map`.
//
// Discover `roomBookingId` with `GET /v1/channels/booking/properties/{id}/rooms`, which returns every room of a property with the `roomId` this route takes.
//
// Booking.com attaches at the ROOM level: a property is a building and its rooms are what a guest books, so each room maps to one listing. Pass `listingId: null` to unmap a room and remove its channel link.
//
// The room mapping and its channel link are repointed together in one transaction, so a link can never outlive the mapping it describes — a stale link keeps routing that room's reservations to the previous listing. Re-sending a mapping that is already in place writes nothing (`alreadyMapped: true`).
//
// **The property's reservations are pulled as part of the call.** Once the room is mapped, every active reservation Booking.com holds for the property is imported and attached to its listing — `reservationsImported` says how many were processed. One already present is left as it is, so re-sending never duplicates. You do not need a follow-up call: reservations that arrived before the room was mapped are never picked up by the regular sync, so this is the moment they are brought in. It runs on every successful map, including a re-send, so re-sending retries an import that did not run. If the import cannot run, the mapping still stands and `reservationsImported` is `null`. A property with a long booking history can take tens of seconds. Unmapping pulls nothing.
//
// Unlike the Airbnb route, there is no conflict when the target listing already carries another Booking.com room: one listing served by several rooms is a normal arrangement and is not refused.
//
// Scope is enforced on both sides against your workspace — the room's property and the target listing. A room or listing belonging to another workspace returns the same 404 as one that does not exist.
//
// Returns `403 listing_inactive` when the target listing, or the listing the room is mapped to now, is inactive; nothing is changed.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/listings/map (the `MapBookingRoom` operationId).
func (c *ClientWithResponses) MapBookingRoomWithResponse(ctx context.Context, body MapBookingRoomJSONRequestBody, reqEditors ...RequestEditorFn) (*MapBookingRoomClientResponse, error) {
	rsp, err := c.MapBookingRoom(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseMapBookingRoomClientResponse(rsp)
}

// GetBookingListingPricingWithResponse Get Booking.com pricing for a listing
//
// Resolves the Repull listing id to its Booking.com `hotel_id` (via the room mapping the Connect flow records for the authenticated workspace), then proxies Booking's `getRoomRateAvailability` for the requested window. Pricing on Booking is per-room/per-rate-plan, so `room_id` and `room_level` flow through query params unchanged.
//
// Mirrors the per-channel `/listings/{id}/pricing` shape used by Airbnb so SDK consumers can carry a Repull listing id across channels. `id` is a Repull listing id, never a Booking.com hotel id — the hotel-id surface is `/v1/channels/booking/availability`.
//
// A listing can be published under several Booking.com properties. GET uses the oldest and reports the rest in `otherHotelIds`; PUT refuses with `409 ambiguous_booking_mapping` rather than push rates into a property it guessed at. `?hotel_id=` names the property explicitly for either.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/booking/listings/{id}/pricing (the `GetBookingListingPricing` operationId).
func (c *ClientWithResponses) GetBookingListingPricingWithResponse(ctx context.Context, id int, params *GetBookingListingPricingParams, reqEditors ...RequestEditorFn) (*GetBookingListingPricingClientResponse, error) {
	rsp, err := c.GetBookingListingPricing(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetBookingListingPricingClientResponse(rsp)
}

// UpdateBookingListingPricingWithBodyWithResponse Update Booking.com pricing for a listing
//
// Writes nightly prices for a listing's Booking.com room + rate plan. Each update needs `roomId` + `rateId` + `dateRange` + `price` + `currency`; `dateRange` is inclusive at both ends, so `start` equal to `end` writes exactly one night.
//
// **Occupancy.** Booking.com stores a rate amount against the party size the rate plan prices. Send `occupancy` and that is what is used; omit it and it is resolved from Booking.com's own data for that (room, rate plan) and echoed back in `occupancy[]` with its `source`. When it cannot be resolved the write is refused with `422` naming `updates[N].occupancy` — a price is never sent at a guessed party size, because Booking.com declines such an amount without saying so.
//
// **Inventory is a separate write.** `roomsToSell` on a rate update returns `422 inventory_not_in_rate_update`; use `PUT /v1/channels/booking/availability` with `type: "availability"`.
//
// **Restrictions ride along, on their own wire.** Send `restrictions` with the price and Booking.com receives two writes; the response reports each separately in `price` and `restrictions`, each with its own state, read-back and — when refused — Booking.com's own reason. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` (Booking.com's notification has no element for them — set those on the rate plan in the Extranet). Nothing you send is silently ignored.
//
// **The response says what is known.** Booking.com acknowledges a write without per-date status, so the affected nights are read back — prices and restrictions out of the same read — and `applied` reports `verified`, `mismatch`, `partial`, `rejected` or `unverified`. `partial` means one half landed and the other did not, which is never reported as a total failure. Send `verify: false` to skip the read-back; `applied` is then `unverified`. Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
//
// `id` is a Repull listing id. When it is published under several Booking.com properties this returns `409 ambiguous_booking_mapping` and pushes nothing — name the property with `?hotel_id=` instead.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/booking/listings/{id}/pricing (the `UpdateBookingListingPricing` operationId).
func (c *ClientWithResponses) UpdateBookingListingPricingWithBodyWithResponse(ctx context.Context, id int, params *UpdateBookingListingPricingParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateBookingListingPricingClientResponse, error) {
	rsp, err := c.UpdateBookingListingPricingWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateBookingListingPricingClientResponse(rsp)
}

// UpdateBookingListingPricingWithResponse Update Booking.com pricing for a listing
//
// Writes nightly prices for a listing's Booking.com room + rate plan. Each update needs `roomId` + `rateId` + `dateRange` + `price` + `currency`; `dateRange` is inclusive at both ends, so `start` equal to `end` writes exactly one night.
//
// **Occupancy.** Booking.com stores a rate amount against the party size the rate plan prices. Send `occupancy` and that is what is used; omit it and it is resolved from Booking.com's own data for that (room, rate plan) and echoed back in `occupancy[]` with its `source`. When it cannot be resolved the write is refused with `422` naming `updates[N].occupancy` — a price is never sent at a guessed party size, because Booking.com declines such an amount without saying so.
//
// **Inventory is a separate write.** `roomsToSell` on a rate update returns `422 inventory_not_in_rate_update`; use `PUT /v1/channels/booking/availability` with `type: "availability"`.
//
// **Restrictions ride along, on their own wire.** Send `restrictions` with the price and Booking.com receives two writes; the response reports each separately in `price` and `restrictions`, each with its own state, read-back and — when refused — Booking.com's own reason. `minStay`, `maxStay`, `minStayArrival`, `maxStayArrival`, `closedToArrival` and `closedToDeparture` are written; `exactStayArrival`, `minAdvanceRes` and `maxAdvanceRes` are refused with `422 restriction_not_supported` (Booking.com's notification has no element for them — set those on the rate plan in the Extranet). Nothing you send is silently ignored.
//
// **The response says what is known.** Booking.com acknowledges a write without per-date status, so the affected nights are read back — prices and restrictions out of the same read — and `applied` reports `verified`, `mismatch`, `partial`, `rejected` or `unverified`. `partial` means one half landed and the other did not, which is never reported as a total failure. Send `verify: false` to skip the read-back; `applied` is then `unverified`. Booking.com stores a 1-night minimum as no minimum, so `minStay: 1` reads back as `0` and still counts as applied.
//
// `id` is a Repull listing id. When it is published under several Booking.com properties this returns `409 ambiguous_booking_mapping` and pushes nothing — name the property with `?hotel_id=` instead.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/booking/listings/{id}/pricing (the `UpdateBookingListingPricing` operationId).
func (c *ClientWithResponses) UpdateBookingListingPricingWithResponse(ctx context.Context, id int, params *UpdateBookingListingPricingParams, body UpdateBookingListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateBookingListingPricingClientResponse, error) {
	rsp, err := c.UpdateBookingListingPricing(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateBookingListingPricingClientResponse(rsp)
}

// ListBookingConversationsWithResponse List Booking.com conversations
//
// List Booking.com guest conversations. Cursor-paginated. Use the messaging POST to send a reply.
//
// Scoped to this workspace. With `property_id`, the property must be connected to this workspace — any other id returns `404 not_found`. Without it, only messages for this workspace's own Booking.com properties are returned.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/booking/messaging (the `ListBookingConversations` operationId).
func (c *ClientWithResponses) ListBookingConversationsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListBookingConversationsClientResponse, error) {
	rsp, err := c.ListBookingConversations(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListBookingConversationsClientResponse(rsp)
}

// SendBookingMessageWithBodyWithResponse Send Booking.com message
//
// Send a text message in a Booking.com conversation as the host. Booking enforces content rules similar to Airbnb.
//
// **Text only.** To send photos, use `POST /v1/conversations/{id}/messages` with `attachments` (JPEG or PNG, up to 10 MB each, with message text) — it uploads the files to Booking.com and records them in the conversation. Passing `attachments`, `attachment_ids` or `mediaUrl` here returns `422 attachments_not_supported` and sends nothing.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/messaging (the `SendBookingMessage` operationId).
func (c *ClientWithResponses) SendBookingMessageWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SendBookingMessageClientResponse, error) {
	rsp, err := c.SendBookingMessageWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSendBookingMessageClientResponse(rsp)
}

// SendBookingMessageWithResponse Send Booking.com message
//
// Send a text message in a Booking.com conversation as the host. Booking enforces content rules similar to Airbnb.
//
// **Text only.** To send photos, use `POST /v1/conversations/{id}/messages` with `attachments` (JPEG or PNG, up to 10 MB each, with message text) — it uploads the files to Booking.com and records them in the conversation. Passing `attachments`, `attachment_ids` or `mediaUrl` here returns `422 attachments_not_supported` and sends nothing.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/messaging (the `SendBookingMessage` operationId).
func (c *ClientWithResponses) SendBookingMessageWithResponse(ctx context.Context, body SendBookingMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*SendBookingMessageClientResponse, error) {
	rsp, err := c.SendBookingMessage(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSendBookingMessageClientResponse(rsp)
}

// ListBookingPropertiesWithResponse List Booking.com properties
//
// List every Booking.com property this workspace holds. Each property is returned ONCE, with the Repull listings mapped under it.
//
// A Booking.com property is a building; its rooms are what guests book, and each room is mapped to one Repull listing — so one property routinely carries many listings. `listings[].roomBookingId` is the Booking.com room id an ARI write takes.
//
// A property whose rooms are not mapped yet is still listed, with `mappingStatus: "unmapped"` and an empty `listings` array. That is a real mid-onboarding state, not an error: finish `POST /v1/connect/booking/map-rooms` and the listings appear. Such a property used to be dropped silently, which made a mapped-but-unreadable workspace indistinguishable from one with no Booking connection at all.
//
// Inactive listings are left out of `listings`; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/booking/properties (the `ListBookingProperties` operationId).
func (c *ClientWithResponses) ListBookingPropertiesWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListBookingPropertiesClientResponse, error) {
	rsp, err := c.ListBookingProperties(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListBookingPropertiesClientResponse(rsp)
}

// GetBookingPropertyWithResponse Get Booking.com connection for a listing
//
// Return the Booking.com connection record(s) for a Repull listing — the linked Booking hotel id, sync flags, markup, sync category, suspension state, and the Booking room the mapping runs through.
//
// `id` is a **Repull listing id**, not a Booking.com hotel id, despite the `properties` segment. (The hotel-id surface is `/v1/channels/booking/availability`.) The mapping is read from wherever the Connect flow recorded it — `listings_booking_rooms` for anything mapped through `POST /v1/connect/booking/map-rooms`, which is essentially every live mapping.
//
// An ARRAY, because one listing can be published under several Booking.com properties at once; `mappedVia` says which record carries each mapping. A listing with no Booking.com mapping returns 404, and the message says which id space the path takes.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/booking/properties/{id} (the `GetBookingProperty` operationId).
func (c *ClientWithResponses) GetBookingPropertyWithResponse(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*GetBookingPropertyClientResponse, error) {
	rsp, err := c.GetBookingProperty(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetBookingPropertyClientResponse(rsp)
}

// BookingPropertyActionWithBodyWithResponse Take a property off sale / put it back (unlist/relist)
//
// Stop this listing's Booking.com property being sold, or start it again. `id` is a **Repull listing id**, not a Booking.com hotel id, as on the GET.
//
// **Booking.com has no unlist, so this is an availability write.** Airbnb has a real deactivate; Booking.com does not. `unlist` closes the mapped room across the whole forward window, so the property stops selling. `relist` is not its mirror image: it re-syncs the true calendar, so dates that are genuinely blocked (a reservation, an owner stay) stay blocked and only the closure `unlist` wrote lifts. Re-opening everything would sell dates that are not for sale.
//
// **Which property gets closed.** A listing can be mapped to more than one Booking.com property — the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. With exactly one, send nothing. With several, name one with `hotelId` (or `?hotel_id=`); omit it and the request is refused with **`409 ambiguous_booking_mapping`** listing the candidates, and nothing is written. That refusal matters more here than on a publish: writing content into the wrong property is recoverable, closing the wrong property's availability takes real inventory off sale while the property you meant keeps selling. Naming a property this listing is not mapped to is a `404` that names the ones it is.
//
// **This does not change the listing in Repull.** `active` — what Repull bills and serves — is untouched by both actions and is deliberately not echoed in the response, so the two ideas can never be read as one field. To take a listing off the market on every channel at once, use `POST /v1/listings/{id}/offline`.
//
// Any other action returns a structured `422` naming the ones that are supported. To push content use `POST /v1/listings/{id}/publish/booking`; to map rooms use `POST /v1/connect/booking/map-rooms`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/properties/{id} (the `BookingPropertyAction` operationId).
func (c *ClientWithResponses) BookingPropertyActionWithBodyWithResponse(ctx context.Context, id int, params *BookingPropertyActionParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BookingPropertyActionClientResponse, error) {
	rsp, err := c.BookingPropertyActionWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBookingPropertyActionClientResponse(rsp)
}

// BookingPropertyActionWithResponse Take a property off sale / put it back (unlist/relist)
//
// Stop this listing's Booking.com property being sold, or start it again. `id` is a **Repull listing id**, not a Booking.com hotel id, as on the GET.
//
// **Booking.com has no unlist, so this is an availability write.** Airbnb has a real deactivate; Booking.com does not. `unlist` closes the mapped room across the whole forward window, so the property stops selling. `relist` is not its mirror image: it re-syncs the true calendar, so dates that are genuinely blocked (a reservation, an owner stay) stay blocked and only the closure `unlist` wrote lifts. Re-opening everything would sell dates that are not for sale.
//
// **Which property gets closed.** A listing can be mapped to more than one Booking.com property — the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. With exactly one, send nothing. With several, name one with `hotelId` (or `?hotel_id=`); omit it and the request is refused with **`409 ambiguous_booking_mapping`** listing the candidates, and nothing is written. That refusal matters more here than on a publish: writing content into the wrong property is recoverable, closing the wrong property's availability takes real inventory off sale while the property you meant keeps selling. Naming a property this listing is not mapped to is a `404` that names the ones it is.
//
// **This does not change the listing in Repull.** `active` — what Repull bills and serves — is untouched by both actions and is deliberately not echoed in the response, so the two ideas can never be read as one field. To take a listing off the market on every channel at once, use `POST /v1/listings/{id}/offline`.
//
// Any other action returns a structured `422` naming the ones that are supported. To push content use `POST /v1/listings/{id}/publish/booking`; to map rooms use `POST /v1/connect/booking/map-rooms`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/properties/{id} (the `BookingPropertyAction` operationId).
func (c *ClientWithResponses) BookingPropertyActionWithResponse(ctx context.Context, id int, params *BookingPropertyActionParams, body BookingPropertyActionJSONRequestBody, reqEditors ...RequestEditorFn) (*BookingPropertyActionClientResponse, error) {
	rsp, err := c.BookingPropertyAction(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBookingPropertyActionClientResponse(rsp)
}

// ListBookingPropertyRoomsWithResponse List Booking.com rooms + rate-plan ids for a listing
//
// Return every Booking.com room and its rate plans for a listing, each with the `roomId` / `rateId` needed to assemble a restriction write via `PUT /v1/channels/booking/availability`.
//
// `id` is a **Repull listing id**, not a Booking.com hotel id, despite the `properties` segment — resolved to the Booking `hotel_id` through the workspace mapping, read from wherever the Connect flow recorded it (`listings_booking_rooms` for anything mapped through `POST /v1/connect/booking/map-rooms`). A listing with no active Booking.com mapping returns 404, and the message says which id space the path takes. When the listing is published under several properties the oldest is used, the rest come back in `otherHotelIds`, and `?hotel_id=` names a different one. Sourced from Booking's B.XML roomrates feed, which returns rooms and rate plans together (the rooms-unit feed alone omits rate-plan ids). This is the API-key surface for the room/rate ids that were previously only reachable inside the hosted Connect room-mapping flow.
//
// `source` says where the answer came from. `booking` means it was read live just now. If Booking.com returns nothing usable for the property, the rooms and rate plans recorded at the last import are served instead, `source` is `mirror`, and `mirrorReason` names what went wrong live — the ids are Booking.com's own and can be written against, but they can be stale, and `maxPersons`, `policy`, `policyId`, `pricingType` and `isChildRate` come back `null` because only the live feed states them. `rooms` is empty only when Booking.com and the last import both have nothing; a read that failed is an error, never an empty list.
//
// Each rate plan carries `maxPersons` — the party size that rate plan prices, which is the `occupancy` a rate amount must be written at. Each room carries `maxAdults`, Booking.com's capacity for the room, which is what a rate write falls back to when the rate plan states no `maxPersons`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/booking/properties/{id}/rooms (the `ListBookingPropertyRooms` operationId).
func (c *ClientWithResponses) ListBookingPropertyRoomsWithResponse(ctx context.Context, id int, params *ListBookingPropertyRoomsParams, reqEditors ...RequestEditorFn) (*ListBookingPropertyRoomsClientResponse, error) {
	rsp, err := c.ListBookingPropertyRooms(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListBookingPropertyRoomsClientResponse(rsp)
}

// ListBookingReservationsWithResponse List Booking.com reservations
//
// Pull reservations from Booking.com. `type=new` (default) returns un-acknowledged bookings; `type=modified` returns changed bookings. Pass both `reservation_id` and `hotel_id` to fetch a single reservation's full details. Acknowledge processed reservations with the POST so Booking stops re-serving them in the `new` queue.
//
// Scoped to this workspace. `hotel_id` (or its alias `property_id`) must be a property connected to this workspace; any other id returns `404 not_found`, the same as an id that does not exist. Without a hotel, `new`/`modified` cover every Booking.com property this workspace holds (and return `404 not_found` if it holds none). A `reservation_id` that belongs to another workspace returns `404 not_found`.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/booking/reservations (the `ListBookingReservations` operationId).
func (c *ClientWithResponses) ListBookingReservationsWithResponse(ctx context.Context, params *ListBookingReservationsParams, reqEditors ...RequestEditorFn) (*ListBookingReservationsClientResponse, error) {
	rsp, err := c.ListBookingReservations(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListBookingReservationsClientResponse(rsp)
}

// AcknowledgeBookingReservationsWithBodyWithResponse Acknowledge Booking.com reservations
//
// Acknowledge one or more reservations so Booking.com removes them from the `new` queue. The body carries `reservation_ids` (non-empty array). Acknowledge only after you have durably persisted each reservation.
//
// Only reservations that belong to this workspace can be acknowledged. If any id in `reservation_ids` is not one of this workspace's Booking.com reservations (`confirmationCode` on `GET /v1/reservations?platform=booking`), nothing is acknowledged and the response is `404 not_found` naming those ids in `reservation_ids`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/reservations (the `AcknowledgeBookingReservations` operationId).
func (c *ClientWithResponses) AcknowledgeBookingReservationsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*AcknowledgeBookingReservationsClientResponse, error) {
	rsp, err := c.AcknowledgeBookingReservationsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAcknowledgeBookingReservationsClientResponse(rsp)
}

// AcknowledgeBookingReservationsWithResponse Acknowledge Booking.com reservations
//
// Acknowledge one or more reservations so Booking.com removes them from the `new` queue. The body carries `reservation_ids` (non-empty array). Acknowledge only after you have durably persisted each reservation.
//
// Only reservations that belong to this workspace can be acknowledged. If any id in `reservation_ids` is not one of this workspace's Booking.com reservations (`confirmationCode` on `GET /v1/reservations?platform=booking`), nothing is acknowledged and the response is `404 not_found` naming those ids in `reservation_ids`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/reservations (the `AcknowledgeBookingReservations` operationId).
func (c *ClientWithResponses) AcknowledgeBookingReservationsWithResponse(ctx context.Context, body AcknowledgeBookingReservationsJSONRequestBody, reqEditors ...RequestEditorFn) (*AcknowledgeBookingReservationsClientResponse, error) {
	rsp, err := c.AcknowledgeBookingReservations(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAcknowledgeBookingReservationsClientResponse(rsp)
}

// ListBookingReviewsWithResponse List Booking.com reviews
//
// List guest reviews for a Booking.com property. Pass `property_id` (the Booking.com hotel id) as a query param — required.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/booking/reviews (the `ListBookingReviews` operationId).
func (c *ClientWithResponses) ListBookingReviewsWithResponse(ctx context.Context, params *ListBookingReviewsParams, reqEditors ...RequestEditorFn) (*ListBookingReviewsClientResponse, error) {
	rsp, err := c.ListBookingReviews(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListBookingReviewsClientResponse(rsp)
}

// ReplyBookingReviewWithBodyWithResponse Reply to Booking.com review
//
// Post a public host reply to a guest review on Booking.com. Booking allows one host reply per review — repeated POSTs are rejected by upstream.
//
// Booking.com does NOT support host-authored reviews of guests via the API (platform-level limitation), so this endpoint is reply-only.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/reviews (the `ReplyBookingReview` operationId).
func (c *ClientWithResponses) ReplyBookingReviewWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReplyBookingReviewClientResponse, error) {
	rsp, err := c.ReplyBookingReviewWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReplyBookingReviewClientResponse(rsp)
}

// ReplyBookingReviewWithResponse Reply to Booking.com review
//
// Post a public host reply to a guest review on Booking.com. Booking allows one host reply per review — repeated POSTs are rejected by upstream.
//
// Booking.com does NOT support host-authored reviews of guests via the API (platform-level limitation), so this endpoint is reply-only.
//
// `property_id` must be a Booking.com property connected to this workspace (`GET /v1/channels/booking/properties` lists them). Any other id — including one connected to a different workspace — returns `404 not_found`, the same answer as an id that does not exist.
//
// Returns `403 listing_inactive` when any listing mapped to the Booking.com property is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/reviews (the `ReplyBookingReview` operationId).
func (c *ClientWithResponses) ReplyBookingReviewWithResponse(ctx context.Context, body ReplyBookingReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*ReplyBookingReviewClientResponse, error) {
	rsp, err := c.ReplyBookingReview(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReplyBookingReviewClientResponse(rsp)
}

// BookingSetupWithBodyWithResponse Booking.com property setup actions
//
// Action-router for putting a property onto Booking.com — including building one from nothing. Select the step with `action`.
//
// ## Opening a property
//
// - `create-property` — create a NEW Booking.com property for a Repull listing (`listing_id`). Creates the property, its first room with the listing's beds, a rate plan and the room-rate product that makes the room sellable (under the listing's cancellation policy), sets the contact and invoice details and the facilities, seeds availability and rates, syncs the calendar, then runs Booking.com's readiness check and reports what still blocks opening in `warnings`. Send `contact` (`name`, `email`, `phone` in international form); without it the workspace owner is used, and a workspace with no usable contact is refused before anything is created. Returns 201.
// - `add-room` — add another room type (and its sellable product) to a property (`listing_id`, `property_id`). Returns 201.
// - `add-unit` — raise the number of identical units on an existing room (`listing_id`, `property_id`, `room_id`).
// - `advance` — run Booking.com's readiness check for a property (`property_id`) and, when it passes, open it. Returns `checked`, `opened`, `sellable` and `blockers` — Booking.com's own reasons it cannot open yet.
//
// ## Account and policy steps
//
// - `create-legal-entity` — register a legal entity directly (returns 201). Not normally needed: see the legal-entity rules below.
// - `check-legal-status` — always `404`. A legal entity's details are readable for any id on the connectivity-provider credentials every workspace shares, and nothing records which workspace registered which entity, so no entity can be shown to be yours. `create-property` resolves it for you.
// - `check-readiness` — whether a property is ready to open (`property_id`): `ready` and `blockers`, without trying to open it.
// - `open-property` — open the property for sale (`property_id`). Refused with `422 booking_rejected` naming the blockers when it is not ready.
// - `set-contacts` — set property contacts (`property_id`, `contacts` in Booking.com's Contacts API shape; at most one carries the `general` profile).
// - `set-policies` — add a cancellation policy (`property_id`, `policyCode`, optional `prepaymentRequired`). House rules, pets, children and the damage deposit are `POST /v1/channels/booking/content` with `type: "settings"`.
//
// ## Three things about Booking.com that cost real money
//
// **A newly created property is NOT sellable.** Booking.com opens it only when its readiness check passes, and the check names what is missing — a main photo still processing, no availability, a licence the region requires. The response always reports `status: "being_built"` and `sellable: false`, never a guess, with the reasons in `warnings`. Resolve them, then `advance`.
//
// **A room with no ACTIVE rate plan is invisible.** Booking only renders rooms that have at least one active product linkage (room × rate plan). A room can be created successfully, return a `roomId`, and never appear on the property page. If `rateId` comes back `null` from `create-property` or `add-room`, that is exactly what happened: activate a rate plan on the property in the Extranet, then add the room again.
//
// **Room names are Booking.com's.** Travellers see one of Booking.com's standard names ("Two-Bedroom Apartment"), chosen from the listing's bedrooms. The listing's own name is kept as the operator-side reference, never shown to guests.
//
// ## The legal entity is resolved, not asked for
//
// A property is created against the legal entity Booking.com contracts with, invoices and pays. You do not normally send one:
//
// 1. If this workspace already creates properties under a legal entity, that one is reused. A second is never registered.
// 2. If it has none and the request carries `legal_entity` (`company_name`, `legal_contact_name`, `legal_contact_email`), one is registered and used. Booking.com emails the legal contact a contract; creation only succeeds once it is signed.
// 3. If it has none and no `legal_entity`, the request is refused with `422 legal_entity_required` naming the fields — a contracted company is never invented.
//
// `legal_entity_id` overrides all of that. An id that already carries another workspace's properties is refused with `403 legal_entity_not_yours` before anything is created.
//
// ## What you do not control
//
// Properties are created against Booking's **production** target only. A test-target property cannot be sold through and there is no route back from one, so `target` is not a parameter — sending it changes nothing.
//
// The property category comes from the listing's property type (Apartment when it has none; Holiday home, Villa or Chalet when it says so). The initial room count is 1. Latitude and longitude come from the listing and are adjusted slightly to clear Booking.com's duplicate detection — send the property's true position on the listing and do not pre-adjust it yourself.
//
// The listing's name, check-in/check-out times, currency, capacity and price come from the listing. Its postal code is taken from the listing's own `postalCode`; when the listing has none, it falls back to a connected Airbnb listing. A listing with neither is created without a postal code, so set `postalCode` on the listing first.
//
// ## Guards
//
// Every action that takes a `property_id` requires a property connected to this workspace; any other id returns `404 not_found`. Every action that takes a `listing_id` requires a listing in this workspace; any other id returns `404 not_found`.
//
// `create-property` refuses a listing with no coordinates (`422 missing_coordinates`) before anything is created — creating a Booking.com property cannot be undone.
//
// `create-property` is subject to the same published-listing gate as the dashboard: no plan, or the plan's listing limit reached, returns `403 billing_error` with `used` and `limit`, and nothing is created.
//
// Returns `403 listing_inactive` when the listing — or any listing mapped to the Booking.com property — is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// If a property is created and a later step fails, the response is `422 booking_create_partial` carrying `property_id`. The property EXISTS. Do not retry `create-property`, which would open a second one — continue with `add-room` and `advance`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/setup (the `BookingSetup` operationId).
func (c *ClientWithResponses) BookingSetupWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BookingSetupClientResponse, error) {
	rsp, err := c.BookingSetupWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBookingSetupClientResponse(rsp)
}

// BookingSetupWithResponse Booking.com property setup actions
//
// Action-router for putting a property onto Booking.com — including building one from nothing. Select the step with `action`.
//
// ## Opening a property
//
// - `create-property` — create a NEW Booking.com property for a Repull listing (`listing_id`). Creates the property, its first room with the listing's beds, a rate plan and the room-rate product that makes the room sellable (under the listing's cancellation policy), sets the contact and invoice details and the facilities, seeds availability and rates, syncs the calendar, then runs Booking.com's readiness check and reports what still blocks opening in `warnings`. Send `contact` (`name`, `email`, `phone` in international form); without it the workspace owner is used, and a workspace with no usable contact is refused before anything is created. Returns 201.
// - `add-room` — add another room type (and its sellable product) to a property (`listing_id`, `property_id`). Returns 201.
// - `add-unit` — raise the number of identical units on an existing room (`listing_id`, `property_id`, `room_id`).
// - `advance` — run Booking.com's readiness check for a property (`property_id`) and, when it passes, open it. Returns `checked`, `opened`, `sellable` and `blockers` — Booking.com's own reasons it cannot open yet.
//
// ## Account and policy steps
//
// - `create-legal-entity` — register a legal entity directly (returns 201). Not normally needed: see the legal-entity rules below.
// - `check-legal-status` — always `404`. A legal entity's details are readable for any id on the connectivity-provider credentials every workspace shares, and nothing records which workspace registered which entity, so no entity can be shown to be yours. `create-property` resolves it for you.
// - `check-readiness` — whether a property is ready to open (`property_id`): `ready` and `blockers`, without trying to open it.
// - `open-property` — open the property for sale (`property_id`). Refused with `422 booking_rejected` naming the blockers when it is not ready.
// - `set-contacts` — set property contacts (`property_id`, `contacts` in Booking.com's Contacts API shape; at most one carries the `general` profile).
// - `set-policies` — add a cancellation policy (`property_id`, `policyCode`, optional `prepaymentRequired`). House rules, pets, children and the damage deposit are `POST /v1/channels/booking/content` with `type: "settings"`.
//
// ## Three things about Booking.com that cost real money
//
// **A newly created property is NOT sellable.** Booking.com opens it only when its readiness check passes, and the check names what is missing — a main photo still processing, no availability, a licence the region requires. The response always reports `status: "being_built"` and `sellable: false`, never a guess, with the reasons in `warnings`. Resolve them, then `advance`.
//
// **A room with no ACTIVE rate plan is invisible.** Booking only renders rooms that have at least one active product linkage (room × rate plan). A room can be created successfully, return a `roomId`, and never appear on the property page. If `rateId` comes back `null` from `create-property` or `add-room`, that is exactly what happened: activate a rate plan on the property in the Extranet, then add the room again.
//
// **Room names are Booking.com's.** Travellers see one of Booking.com's standard names ("Two-Bedroom Apartment"), chosen from the listing's bedrooms. The listing's own name is kept as the operator-side reference, never shown to guests.
//
// ## The legal entity is resolved, not asked for
//
// A property is created against the legal entity Booking.com contracts with, invoices and pays. You do not normally send one:
//
// 1. If this workspace already creates properties under a legal entity, that one is reused. A second is never registered.
// 2. If it has none and the request carries `legal_entity` (`company_name`, `legal_contact_name`, `legal_contact_email`), one is registered and used. Booking.com emails the legal contact a contract; creation only succeeds once it is signed.
// 3. If it has none and no `legal_entity`, the request is refused with `422 legal_entity_required` naming the fields — a contracted company is never invented.
//
// `legal_entity_id` overrides all of that. An id that already carries another workspace's properties is refused with `403 legal_entity_not_yours` before anything is created.
//
// ## What you do not control
//
// Properties are created against Booking's **production** target only. A test-target property cannot be sold through and there is no route back from one, so `target` is not a parameter — sending it changes nothing.
//
// The property category comes from the listing's property type (Apartment when it has none; Holiday home, Villa or Chalet when it says so). The initial room count is 1. Latitude and longitude come from the listing and are adjusted slightly to clear Booking.com's duplicate detection — send the property's true position on the listing and do not pre-adjust it yourself.
//
// The listing's name, check-in/check-out times, currency, capacity and price come from the listing. Its postal code is taken from the listing's own `postalCode`; when the listing has none, it falls back to a connected Airbnb listing. A listing with neither is created without a postal code, so set `postalCode` on the listing first.
//
// ## Guards
//
// Every action that takes a `property_id` requires a property connected to this workspace; any other id returns `404 not_found`. Every action that takes a `listing_id` requires a listing in this workspace; any other id returns `404 not_found`.
//
// `create-property` refuses a listing with no coordinates (`422 missing_coordinates`) before anything is created — creating a Booking.com property cannot be undone.
//
// `create-property` is subject to the same published-listing gate as the dashboard: no plan, or the plan's listing limit reached, returns `403 billing_error` with `used` and `limit`, and nothing is created.
//
// Returns `403 listing_inactive` when the listing — or any listing mapped to the Booking.com property — is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// If a property is created and a later step fails, the response is `422 booking_create_partial` carrying `property_id`. The property EXISTS. Do not retry `create-property`, which would open a second one — continue with `add-room` and `advance`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/setup (the `BookingSetup` operationId).
func (c *ClientWithResponses) BookingSetupWithResponse(ctx context.Context, body BookingSetupJSONRequestBody, reqEditors ...RequestEditorFn) (*BookingSetupClientResponse, error) {
	rsp, err := c.BookingSetup(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBookingSetupClientResponse(rsp)
}

// DeleteBookingWebhookWithResponse Unsubscribe from a Booking.com notification
//
// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/channels/booking/webhooks (the `DeleteBookingWebhook` operationId).
//
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *ClientWithResponses) DeleteBookingWebhookWithResponse(ctx context.Context, params *DeleteBookingWebhookParams, reqEditors ...RequestEditorFn) (*DeleteBookingWebhookClientResponse, error) {
	rsp, err := c.DeleteBookingWebhook(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteBookingWebhookClientResponse(rsp)
}

// ListBookingWebhooksWithResponse List Booking.com webhook subscriptions
//
// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/booking/webhooks (the `ListBookingWebhooks` operationId).
//
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *ClientWithResponses) ListBookingWebhooksWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListBookingWebhooksClientResponse, error) {
	rsp, err := c.ListBookingWebhooks(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListBookingWebhooksClientResponse(rsp)
}

// CreateBookingWebhookWithBodyWithResponse Subscribe to a Booking.com notification
//
// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/webhooks (the `CreateBookingWebhook` operationId).
//
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *ClientWithResponses) CreateBookingWebhookWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateBookingWebhookClientResponse, error) {
	rsp, err := c.CreateBookingWebhookWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateBookingWebhookClientResponse(rsp)
}

// CreateBookingWebhookWithResponse Subscribe to a Booking.com notification
//
// **Not available through the API — always returns `403 forbidden`.** Booking.com notification subscriptions belong to the Repull platform account that every workspace shares: they are per notification type, not per property, so reading or changing them would affect every workspace. Booking.com events for your own properties are delivered through Repull webhooks — subscribe with `POST /v1/webhooks`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/channels/booking/webhooks (the `CreateBookingWebhook` operationId).
// Deprecated: this operation has been marked as deprecated upstream, but no `x-deprecated-reason` was set
func (c *ClientWithResponses) CreateBookingWebhookWithResponse(ctx context.Context, body CreateBookingWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateBookingWebhookClientResponse, error) {
	rsp, err := c.CreateBookingWebhook(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateBookingWebhookClientResponse(rsp)
}

// GetPlumguideAvailabilityWithResponse Get Plumguide availability
//
// Read the per-day availability calendar for a Plumguide listing. Returns the same row shape as Airbnb availability for SDK convenience.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/plumguide/availability (the `GetPlumguideAvailability` operationId).
func (c *ClientWithResponses) GetPlumguideAvailabilityWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetPlumguideAvailabilityClientResponse, error) {
	rsp, err := c.GetPlumguideAvailability(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetPlumguideAvailabilityClientResponse(rsp)
}

// UpdatePlumguideAvailabilityWithResponse Push availability to Plumguide
//
// Push per-day availability changes to Plumguide. Plumguide accepts only the next 24 months — dates beyond that are silently ignored.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/plumguide/availability (the `UpdatePlumguideAvailability` operationId).
func (c *ClientWithResponses) UpdatePlumguideAvailabilityWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*UpdatePlumguideAvailabilityClientResponse, error) {
	rsp, err := c.UpdatePlumguideAvailability(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdatePlumguideAvailabilityClientResponse(rsp)
}

// ListPlumguideBookingsWithResponse List Plumguide bookings
//
// List Plumguide bookings. Default returns all bookings; pass `listing_id` to filter to one listing, or `booking_code` to fetch a single booking.
//
// Returns `403 listing_inactive` when `listing_id` or `booking_code` resolves to an inactive listing. The unfiltered list is read straight from Plum Guide and is not filtered by listing status.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/plumguide/bookings (the `ListPlumguideBookings` operationId).
func (c *ClientWithResponses) ListPlumguideBookingsWithResponse(ctx context.Context, params *ListPlumguideBookingsParams, reqEditors ...RequestEditorFn) (*ListPlumguideBookingsClientResponse, error) {
	rsp, err := c.ListPlumguideBookings(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListPlumguideBookingsClientResponse(rsp)
}

// ListPlumguideListingsWithResponse List Plumguide listings
//
// List Plumguide listings this workspace has access to. Plumguide is approval-based — listings appear once Plumguide has accepted them.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/plumguide/listings (the `ListPlumguideListings` operationId).
func (c *ClientWithResponses) ListPlumguideListingsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListPlumguideListingsClientResponse, error) {
	rsp, err := c.ListPlumguideListings(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListPlumguideListingsClientResponse(rsp)
}

// GetPlumguidePricingWithResponse Get Plumguide pricing
//
// Read the current pricing for a Plumguide listing (base price, currency, weekend uplift).
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/plumguide/pricing (the `GetPlumguidePricing` operationId).
func (c *ClientWithResponses) GetPlumguidePricingWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetPlumguidePricingClientResponse, error) {
	rsp, err := c.GetPlumguidePricing(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetPlumguidePricingClientResponse(rsp)
}

// UpdatePlumguidePricingWithResponse Push pricing to Plumguide
//
// Push pricing changes to Plumguide. Plumguide rounds all prices to whole units of the listing currency — sub-unit precision is silently truncated.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/plumguide/pricing (the `UpdatePlumguidePricing` operationId).
func (c *ClientWithResponses) UpdatePlumguidePricingWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*UpdatePlumguidePricingClientResponse, error) {
	rsp, err := c.UpdatePlumguidePricing(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdatePlumguidePricingClientResponse(rsp)
}

// DeletePlumguideWebhooksWithResponse Remove Plumguide webhook config
//
// Delete the Plumguide webhook configuration for this workspace. Plumguide stops delivering webhooks until a new config is set.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/channels/plumguide/webhooks (the `DeletePlumguideWebhooks` operationId).
func (c *ClientWithResponses) DeletePlumguideWebhooksWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*DeletePlumguideWebhooksClientResponse, error) {
	rsp, err := c.DeletePlumguideWebhooks(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeletePlumguideWebhooksClientResponse(rsp)
}

// GetPlumguideWebhooksWithResponse Get Plumguide webhook config
//
// Read the current Plumguide webhook configuration for this workspace.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/plumguide/webhooks (the `GetPlumguideWebhooks` operationId).
func (c *ClientWithResponses) GetPlumguideWebhooksWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetPlumguideWebhooksClientResponse, error) {
	rsp, err := c.GetPlumguideWebhooks(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetPlumguideWebhooksClientResponse(rsp)
}

// UpdatePlumguideWebhooksWithBodyWithResponse Replace Plumguide webhook config
//
// Replace the Plumguide webhook configuration. The body carries the full webhook config payload — this is a full replacement, not a patch.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/plumguide/webhooks (the `UpdatePlumguideWebhooks` operationId).
func (c *ClientWithResponses) UpdatePlumguideWebhooksWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdatePlumguideWebhooksClientResponse, error) {
	rsp, err := c.UpdatePlumguideWebhooksWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdatePlumguideWebhooksClientResponse(rsp)
}

// UpdatePlumguideWebhooksWithResponse Replace Plumguide webhook config
//
// Replace the Plumguide webhook configuration. The body carries the full webhook config payload — this is a full replacement, not a patch.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/channels/plumguide/webhooks (the `UpdatePlumguideWebhooks` operationId).
func (c *ClientWithResponses) UpdatePlumguideWebhooksWithResponse(ctx context.Context, body UpdatePlumguideWebhooksJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdatePlumguideWebhooksClientResponse, error) {
	rsp, err := c.UpdatePlumguideWebhooks(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdatePlumguideWebhooksClientResponse(rsp)
}

// ListVrboListingsWithResponse List VRBO listings
//
// List VRBO listings this workspace owns. VRBO is agency-model — Repull reads listings via the public iCal/HTTP feeds.
//
// Inactive listings are left out; they keep syncing and reappear once activated. Use `GET /v1/listings?status=inactive` to find them.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/vrbo/listings (the `ListVrboListings` operationId).
func (c *ClientWithResponses) ListVrboListingsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListVrboListingsClientResponse, error) {
	rsp, err := c.ListVrboListings(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListVrboListingsClientResponse(rsp)
}

// ListVrboReservationsWithResponse List VRBO reservations
//
// Cursor-paginated list of VRBO reservations sourced from the public booking feed. Lag is typically 5-10 minutes vs. Airbnb / Booking.com. `?offset=` is accepted as a first-class alias for `?cursor=` (mutually exclusive; offset capped at 10000).
//
// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/channels/vrbo/reservations (the `ListVrboReservations` operationId).
func (c *ClientWithResponses) ListVrboReservationsWithResponse(ctx context.Context, params *ListVrboReservationsParams, reqEditors ...RequestEditorFn) (*ListVrboReservationsClientResponse, error) {
	rsp, err := c.ListVrboReservations(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListVrboReservationsClientResponse(rsp)
}

// ListConnectionsWithResponse List PMS/OTA connections
//
// Returns all active connections to PMS and OTA platforms.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/connect (the `ListConnections` operationId).
func (c *ClientWithResponses) ListConnectionsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListConnectionsClientResponse, error) {
	rsp, err := c.ListConnections(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListConnectionsClientResponse(rsp)
}

// CreateConnectSessionWithBodyWithResponse Create a multi-channel Connect picker session
//
// Mints a session that lands the user on the channel picker at `connect.repull.dev/{sessionId}` instead of jumping straight to a single provider. The user picks a channel from the registry, the picker page POSTs `selectConnectProvider` to bind the choice, and the per-provider flow takes over.
//
// Use this when you want one entry point for all 13 channels. Use `POST /v1/connect/{provider}` instead when your UI already knows which channel to connect.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect (the `CreateConnectSession` operationId).
func (c *ClientWithResponses) CreateConnectSessionWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateConnectSessionClientResponse, error) {
	rsp, err := c.CreateConnectSessionWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateConnectSessionClientResponse(rsp)
}

// CreateConnectSessionWithResponse Create a multi-channel Connect picker session
//
// Mints a session that lands the user on the channel picker at `connect.repull.dev/{sessionId}` instead of jumping straight to a single provider. The user picks a channel from the registry, the picker page POSTs `selectConnectProvider` to bind the choice, and the per-provider flow takes over.
//
// Use this when you want one entry point for all 13 channels. Use `POST /v1/connect/{provider}` instead when your UI already knows which channel to connect.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect (the `CreateConnectSession` operationId).
func (c *ClientWithResponses) CreateConnectSessionWithResponse(ctx context.Context, body CreateConnectSessionJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateConnectSessionClientResponse, error) {
	rsp, err := c.CreateConnectSession(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateConnectSessionClientResponse(rsp)
}

// SubmitBeds24CredentialsWithBodyWithResponse Submit Beds24 credentials for a Connect session
//
// Completes a credentials-pattern connection for Beds24. API key + prop key from Beds24 → Settings → Apps & Integrations.
//
// The credentials are validated against Beds24 before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/beds24/credentials (the `SubmitBeds24Credentials` operationId).
func (c *ClientWithResponses) SubmitBeds24CredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitBeds24CredentialsClientResponse, error) {
	rsp, err := c.SubmitBeds24CredentialsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitBeds24CredentialsClientResponse(rsp)
}

// SubmitBeds24CredentialsWithResponse Submit Beds24 credentials for a Connect session
//
// Completes a credentials-pattern connection for Beds24. API key + prop key from Beds24 → Settings → Apps & Integrations.
//
// The credentials are validated against Beds24 before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/beds24/credentials (the `SubmitBeds24Credentials` operationId).
func (c *ClientWithResponses) SubmitBeds24CredentialsWithResponse(ctx context.Context, body SubmitBeds24CredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitBeds24CredentialsClientResponse, error) {
	rsp, err := c.SubmitBeds24Credentials(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitBeds24CredentialsClientResponse(rsp)
}

// BookingConnectCallbackWithBodyWithResponse Booking.com connectivity callback
//
// Receives Booking.com's asynchronous confirmation that a property has designated Repull as its connectivity provider, and advances the Connect session. Called by Booking.com, not by integrators.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/booking/callback (the `BookingConnectCallback` operationId).
func (c *ClientWithResponses) BookingConnectCallbackWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BookingConnectCallbackClientResponse, error) {
	rsp, err := c.BookingConnectCallbackWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBookingConnectCallbackClientResponse(rsp)
}

// BookingConnectCallbackWithResponse Booking.com connectivity callback
//
// Receives Booking.com's asynchronous confirmation that a property has designated Repull as its connectivity provider, and advances the Connect session. Called by Booking.com, not by integrators.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/booking/callback (the `BookingConnectCallback` operationId).
func (c *ClientWithResponses) BookingConnectCallbackWithResponse(ctx context.Context, body BookingConnectCallbackJSONRequestBody, reqEditors ...RequestEditorFn) (*BookingConnectCallbackClientResponse, error) {
	rsp, err := c.BookingConnectCallback(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBookingConnectCallbackClientResponse(rsp)
}

// MapConnectBookingRoomsWithBodyWithResponse Submit room→listing mappings for a Booking.com Connect session
//
// Submits the customer's room→listing mapping choices in one transaction. For each mapping, updates `listings_booking_rooms.listing_id` and replaces the corresponding `listing_platform_links` row. Pass `listingId: null` to explicitly unmap a room.
//
// On success the Connect session is marked `completed` and the hosted picker page emits a `repull:connect:completed` postMessage to the embedding window.
//
// No API key required — the `sessionId` in the body is the capability token. Each mapping's `roomId` must belong to the customer's claimed hotel; mismatched IDs are rejected with 403.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/booking/map-rooms (the `MapConnectBookingRooms` operationId).
func (c *ClientWithResponses) MapConnectBookingRoomsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*MapConnectBookingRoomsClientResponse, error) {
	rsp, err := c.MapConnectBookingRoomsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseMapConnectBookingRoomsClientResponse(rsp)
}

// MapConnectBookingRoomsWithResponse Submit room→listing mappings for a Booking.com Connect session
//
// Submits the customer's room→listing mapping choices in one transaction. For each mapping, updates `listings_booking_rooms.listing_id` and replaces the corresponding `listing_platform_links` row. Pass `listingId: null` to explicitly unmap a room.
//
// On success the Connect session is marked `completed` and the hosted picker page emits a `repull:connect:completed` postMessage to the embedding window.
//
// No API key required — the `sessionId` in the body is the capability token. Each mapping's `roomId` must belong to the customer's claimed hotel; mismatched IDs are rejected with 403.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/booking/map-rooms (the `MapConnectBookingRooms` operationId).
func (c *ClientWithResponses) MapConnectBookingRoomsWithResponse(ctx context.Context, body MapConnectBookingRoomsJSONRequestBody, reqEditors ...RequestEditorFn) (*MapConnectBookingRoomsClientResponse, error) {
	rsp, err := c.MapConnectBookingRooms(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseMapConnectBookingRoomsClientResponse(rsp)
}

// ListConnectBookingRoomsWithResponse List Booking.com rooms imported for a Connect session
//
// Returns the rooms imported from the Booking.com hotel claimed in this Connect session, plus the customer's listing options for the mapping dropdowns. Hosted-picker pages poll this endpoint every ~2s after `verifyBookingHotel` succeeds; once rooms appear the page transitions to the mapping UI.
//
// No API key required — the `sessionId` query param is the capability token.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/connect/booking/rooms (the `ListConnectBookingRooms` operationId).
func (c *ClientWithResponses) ListConnectBookingRoomsWithResponse(ctx context.Context, params *ListConnectBookingRoomsParams, reqEditors ...RequestEditorFn) (*ListConnectBookingRoomsClientResponse, error) {
	rsp, err := c.ListConnectBookingRooms(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListConnectBookingRoomsClientResponse(rsp)
}

// VerifyBookingHotelWithBodyWithResponse Verify a Booking.com hotel ID for a Connect session
//
// Manual-paste fallback that closes the Booking.com claim flow. Call this after the customer completes Stage 1 designation in their Booking Extranet (ticking FantasticStay/Repull as their connectivity provider) and pastes their Hotel ID into the hosted picker.
//
// Validates the hotel against Booking's property API, persists the `pms_connections` row, kicks off the room import, and transitions the Connect session to `awaiting_room_mapping`.
//
// No API key required — the `sessionId` is the capability token. Sessions in any terminal state are rejected.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/booking/verify (the `VerifyBookingHotel` operationId).
func (c *ClientWithResponses) VerifyBookingHotelWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*VerifyBookingHotelClientResponse, error) {
	rsp, err := c.VerifyBookingHotelWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseVerifyBookingHotelClientResponse(rsp)
}

// VerifyBookingHotelWithResponse Verify a Booking.com hotel ID for a Connect session
//
// Manual-paste fallback that closes the Booking.com claim flow. Call this after the customer completes Stage 1 designation in their Booking Extranet (ticking FantasticStay/Repull as their connectivity provider) and pastes their Hotel ID into the hosted picker.
//
// Validates the hotel against Booking's property API, persists the `pms_connections` row, kicks off the room import, and transitions the Connect session to `awaiting_room_mapping`.
//
// No API key required — the `sessionId` is the capability token. Sessions in any terminal state are rejected.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/booking/verify (the `VerifyBookingHotel` operationId).
func (c *ClientWithResponses) VerifyBookingHotelWithResponse(ctx context.Context, body VerifyBookingHotelJSONRequestBody, reqEditors ...RequestEditorFn) (*VerifyBookingHotelClientResponse, error) {
	rsp, err := c.VerifyBookingHotel(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseVerifyBookingHotelClientResponse(rsp)
}

// SubmitBookingsyncCredentialsWithBodyWithResponse Submit BookingSync credentials for a Connect session
//
// Completes a credentials-pattern connection for BookingSync. OAuth client credentials issued by BookingSync.
//
// The credentials are validated against BookingSync before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/bookingsync/credentials (the `SubmitBookingsyncCredentials` operationId).
func (c *ClientWithResponses) SubmitBookingsyncCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitBookingsyncCredentialsClientResponse, error) {
	rsp, err := c.SubmitBookingsyncCredentialsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitBookingsyncCredentialsClientResponse(rsp)
}

// SubmitBookingsyncCredentialsWithResponse Submit BookingSync credentials for a Connect session
//
// Completes a credentials-pattern connection for BookingSync. OAuth client credentials issued by BookingSync.
//
// The credentials are validated against BookingSync before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/bookingsync/credentials (the `SubmitBookingsyncCredentials` operationId).
func (c *ClientWithResponses) SubmitBookingsyncCredentialsWithResponse(ctx context.Context, body SubmitBookingsyncCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitBookingsyncCredentialsClientResponse, error) {
	rsp, err := c.SubmitBookingsyncCredentials(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitBookingsyncCredentialsClientResponse(rsp)
}

// SubmitGuestyCredentialsWithBodyWithResponse Submit Guesty credentials for a Connect session
//
// Completes a credentials-pattern connection for Guesty. Client ID + secret from Guesty → Integrations → Open API.
//
// The credentials are validated against Guesty before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/guesty/credentials (the `SubmitGuestyCredentials` operationId).
func (c *ClientWithResponses) SubmitGuestyCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitGuestyCredentialsClientResponse, error) {
	rsp, err := c.SubmitGuestyCredentialsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitGuestyCredentialsClientResponse(rsp)
}

// SubmitGuestyCredentialsWithResponse Submit Guesty credentials for a Connect session
//
// Completes a credentials-pattern connection for Guesty. Client ID + secret from Guesty → Integrations → Open API.
//
// The credentials are validated against Guesty before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/guesty/credentials (the `SubmitGuestyCredentials` operationId).
func (c *ClientWithResponses) SubmitGuestyCredentialsWithResponse(ctx context.Context, body SubmitGuestyCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitGuestyCredentialsClientResponse, error) {
	rsp, err := c.SubmitGuestyCredentials(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitGuestyCredentialsClientResponse(rsp)
}

// SubmitHospitableCredentialsWithBodyWithResponse Submit Hospitable credentials for a Connect session
//
// Completes a credentials-pattern connection for Hospitable. Personal access token from Hospitable → Settings → API.
//
// The credentials are validated against Hospitable before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/hospitable/credentials (the `SubmitHospitableCredentials` operationId).
func (c *ClientWithResponses) SubmitHospitableCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitHospitableCredentialsClientResponse, error) {
	rsp, err := c.SubmitHospitableCredentialsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitHospitableCredentialsClientResponse(rsp)
}

// SubmitHospitableCredentialsWithResponse Submit Hospitable credentials for a Connect session
//
// Completes a credentials-pattern connection for Hospitable. Personal access token from Hospitable → Settings → API.
//
// The credentials are validated against Hospitable before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/hospitable/credentials (the `SubmitHospitableCredentials` operationId).
func (c *ClientWithResponses) SubmitHospitableCredentialsWithResponse(ctx context.Context, body SubmitHospitableCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitHospitableCredentialsClientResponse, error) {
	rsp, err := c.SubmitHospitableCredentials(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitHospitableCredentialsClientResponse(rsp)
}

// SubmitHostawayCredentialsWithBodyWithResponse Submit Hostaway credentials for a Connect session
//
// Completes a credentials-pattern connection for Hostaway. Account ID + API key from Hostaway → Settings → Public API.
//
// The credentials are validated against Hostaway before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/hostaway/credentials (the `SubmitHostawayCredentials` operationId).
func (c *ClientWithResponses) SubmitHostawayCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitHostawayCredentialsClientResponse, error) {
	rsp, err := c.SubmitHostawayCredentialsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitHostawayCredentialsClientResponse(rsp)
}

// SubmitHostawayCredentialsWithResponse Submit Hostaway credentials for a Connect session
//
// Completes a credentials-pattern connection for Hostaway. Account ID + API key from Hostaway → Settings → Public API.
//
// The credentials are validated against Hostaway before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/hostaway/credentials (the `SubmitHostawayCredentials` operationId).
func (c *ClientWithResponses) SubmitHostawayCredentialsWithResponse(ctx context.Context, body SubmitHostawayCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitHostawayCredentialsClientResponse, error) {
	rsp, err := c.SubmitHostawayCredentials(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitHostawayCredentialsClientResponse(rsp)
}

// SubmitIgmsCredentialsWithBodyWithResponse Submit iGMS credentials for a Connect session
//
// Completes a credentials-pattern connection for iGMS. API token from iGMS → Settings → Integrations.
//
// The credentials are validated against iGMS before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/igms/credentials (the `SubmitIgmsCredentials` operationId).
func (c *ClientWithResponses) SubmitIgmsCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitIgmsCredentialsClientResponse, error) {
	rsp, err := c.SubmitIgmsCredentialsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitIgmsCredentialsClientResponse(rsp)
}

// SubmitIgmsCredentialsWithResponse Submit iGMS credentials for a Connect session
//
// Completes a credentials-pattern connection for iGMS. API token from iGMS → Settings → Integrations.
//
// The credentials are validated against iGMS before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/igms/credentials (the `SubmitIgmsCredentials` operationId).
func (c *ClientWithResponses) SubmitIgmsCredentialsWithResponse(ctx context.Context, body SubmitIgmsCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitIgmsCredentialsClientResponse, error) {
	rsp, err := c.SubmitIgmsCredentials(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitIgmsCredentialsClientResponse(rsp)
}

// SubmitLodgifyCredentialsWithBodyWithResponse Submit Lodgify credentials for a Connect session
//
// Completes a credentials-pattern connection for Lodgify. API key from Lodgify → Settings → Public API.
//
// The credentials are validated against Lodgify before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/lodgify/credentials (the `SubmitLodgifyCredentials` operationId).
func (c *ClientWithResponses) SubmitLodgifyCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitLodgifyCredentialsClientResponse, error) {
	rsp, err := c.SubmitLodgifyCredentialsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitLodgifyCredentialsClientResponse(rsp)
}

// SubmitLodgifyCredentialsWithResponse Submit Lodgify credentials for a Connect session
//
// Completes a credentials-pattern connection for Lodgify. API key from Lodgify → Settings → Public API.
//
// The credentials are validated against Lodgify before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/lodgify/credentials (the `SubmitLodgifyCredentials` operationId).
func (c *ClientWithResponses) SubmitLodgifyCredentialsWithResponse(ctx context.Context, body SubmitLodgifyCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitLodgifyCredentialsClientResponse, error) {
	rsp, err := c.SubmitLodgifyCredentials(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitLodgifyCredentialsClientResponse(rsp)
}

// SubmitOwnerrezCredentialsWithBodyWithResponse Submit OwnerRez credentials for a Connect session
//
// Completes a credentials-pattern connection for OwnerRez. Username + API token from OwnerRez → Settings → API.
//
// The credentials are validated against OwnerRez before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/ownerrez/credentials (the `SubmitOwnerrezCredentials` operationId).
func (c *ClientWithResponses) SubmitOwnerrezCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitOwnerrezCredentialsClientResponse, error) {
	rsp, err := c.SubmitOwnerrezCredentialsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitOwnerrezCredentialsClientResponse(rsp)
}

// SubmitOwnerrezCredentialsWithResponse Submit OwnerRez credentials for a Connect session
//
// Completes a credentials-pattern connection for OwnerRez. Username + API token from OwnerRez → Settings → API.
//
// The credentials are validated against OwnerRez before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/ownerrez/credentials (the `SubmitOwnerrezCredentials` operationId).
func (c *ClientWithResponses) SubmitOwnerrezCredentialsWithResponse(ctx context.Context, body SubmitOwnerrezCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitOwnerrezCredentialsClientResponse, error) {
	rsp, err := c.SubmitOwnerrezCredentials(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitOwnerrezCredentialsClientResponse(rsp)
}

// ListConnectProvidersWithResponse List Connect channels
//
// Returns the public registry of every channel the picker supports. No customer-specific data — display metadata only. Cached for 5 minutes at the edge.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/connect/providers (the `ListConnectProviders` operationId).
func (c *ClientWithResponses) ListConnectProvidersWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListConnectProvidersClientResponse, error) {
	rsp, err := c.ListConnectProviders(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListConnectProvidersClientResponse(rsp)
}

// SelectConnectProviderWithBodyWithResponse Bind a picker session to a provider
//
// Called by the hosted picker page once the user clicks a channel card. Validates the provider exists and is permitted by the session's `allowedProviders` whitelist (if any), then returns the next-step URL the picker should navigate to.
//
// No API key required — the session ID is the capability token. The session must still be pending and unexpired.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/sessions/{sessionId}/select-provider (the `SelectConnectProvider` operationId).
func (c *ClientWithResponses) SelectConnectProviderWithBodyWithResponse(ctx context.Context, sessionId string, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SelectConnectProviderClientResponse, error) {
	rsp, err := c.SelectConnectProviderWithBody(ctx, sessionId, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSelectConnectProviderClientResponse(rsp)
}

// SelectConnectProviderWithResponse Bind a picker session to a provider
//
// Called by the hosted picker page once the user clicks a channel card. Validates the provider exists and is permitted by the session's `allowedProviders` whitelist (if any), then returns the next-step URL the picker should navigate to.
//
// No API key required — the session ID is the capability token. The session must still be pending and unexpired.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/sessions/{sessionId}/select-provider (the `SelectConnectProvider` operationId).
func (c *ClientWithResponses) SelectConnectProviderWithResponse(ctx context.Context, sessionId string, body SelectConnectProviderJSONRequestBody, reqEditors ...RequestEditorFn) (*SelectConnectProviderClientResponse, error) {
	rsp, err := c.SelectConnectProvider(ctx, sessionId, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSelectConnectProviderClientResponse(rsp)
}

// SubmitSmoobuCredentialsWithBodyWithResponse Submit Smoobu credentials for a Connect session
//
// Completes a credentials-pattern connection for Smoobu. API key from Smoobu → Settings → For developers.
//
// The credentials are validated against Smoobu before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/smoobu/credentials (the `SubmitSmoobuCredentials` operationId).
func (c *ClientWithResponses) SubmitSmoobuCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitSmoobuCredentialsClientResponse, error) {
	rsp, err := c.SubmitSmoobuCredentialsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitSmoobuCredentialsClientResponse(rsp)
}

// SubmitSmoobuCredentialsWithResponse Submit Smoobu credentials for a Connect session
//
// Completes a credentials-pattern connection for Smoobu. API key from Smoobu → Settings → For developers.
//
// The credentials are validated against Smoobu before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/smoobu/credentials (the `SubmitSmoobuCredentials` operationId).
func (c *ClientWithResponses) SubmitSmoobuCredentialsWithResponse(ctx context.Context, body SubmitSmoobuCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitSmoobuCredentialsClientResponse, error) {
	rsp, err := c.SubmitSmoobuCredentials(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitSmoobuCredentialsClientResponse(rsp)
}

// SubmitVrboCredentialsWithBodyWithResponse Submit Vrbo credentials for a Connect session
//
// Completes a credentials-pattern connection for Vrbo. Activation handshake — Repull mints the Basic-Auth pair the host pastes into Vrbo Partner Central.
//
// The credentials are validated against Vrbo before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/vrbo/credentials (the `SubmitVrboCredentials` operationId).
func (c *ClientWithResponses) SubmitVrboCredentialsWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SubmitVrboCredentialsClientResponse, error) {
	rsp, err := c.SubmitVrboCredentialsWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitVrboCredentialsClientResponse(rsp)
}

// SubmitVrboCredentialsWithResponse Submit Vrbo credentials for a Connect session
//
// Completes a credentials-pattern connection for Vrbo. Activation handshake — Repull mints the Basic-Auth pair the host pastes into Vrbo Partner Central.
//
// The credentials are validated against Vrbo before anything is persisted, so an invalid pair returns `invalid_credentials` rather than creating a dead connection. On success the `pms_connections` row is written and the Connect session moves to its terminal state.
//
// No API key required when called with a `sessionId` — the session is the capability token.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/vrbo/credentials (the `SubmitVrboCredentials` operationId).
func (c *ClientWithResponses) SubmitVrboCredentialsWithResponse(ctx context.Context, body SubmitVrboCredentialsJSONRequestBody, reqEditors ...RequestEditorFn) (*SubmitVrboCredentialsClientResponse, error) {
	rsp, err := c.SubmitVrboCredentials(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSubmitVrboCredentialsClientResponse(rsp)
}

// DeleteConnectionWithResponse Disconnect provider
//
// Disconnect ONE connected account of a provider from this workspace. Supported for `airbnb` and `booking`.
//
// **Which account.** Pass `accountId` — for Airbnb the host id (`accounts[].externalAccountId` from `GET /v1/connect/airbnb`), for Booking.com the hotel id. It is optional only when the workspace has exactly one account for the provider. With several and no `accountId`, the call returns `422` with the account ids in `valid_values` instead of guessing. An `accountId` that is not connected to this workspace returns `404`. Disconnecting one account leaves the others connected.
//
// **What happens.** The account's stored authorization is removed and it stops syncing. Its listings are **deactivated**, not deleted: they stop counting toward your plan's listing limit, their data is kept, and they are returned in `listingsDeactivated`. A listing that is still connected through another account or channel stays active. Reconnect the account, then activate the listings with `POST /v1/listings/status`.
//
// The change is all or nothing. For Airbnb, the host can also revoke access on Airbnb's side (Account → Privacy & sharing → Connected apps); that alone does not update this workspace, so call this endpoint as well.
//
// Other providers return `501 not_implemented` with instructions for disconnecting on the provider's side. That answer depends only on the provider, not on your workspace: an unsupported provider returns `501` whether or not you have a connection to it. `404 not_found` on a supported provider means this workspace has no connection to it (or, with `accountId`, that the account is not connected here).
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/connect/{provider} (the `DeleteConnection` operationId).
func (c *ClientWithResponses) DeleteConnectionWithResponse(ctx context.Context, provider Provider, params *DeleteConnectionParams, reqEditors ...RequestEditorFn) (*DeleteConnectionClientResponse, error) {
	rsp, err := c.DeleteConnection(ctx, provider, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteConnectionClientResponse(rsp)
}

// GetConnectStatusWithResponse Get connection status
//
// Returns the current connection status for a provider, including host metadata (display name + avatar) for Airbnb so clients can render an account-level confirmation UI.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/connect/{provider} (the `GetConnectStatus` operationId).
func (c *ClientWithResponses) GetConnectStatusWithResponse(ctx context.Context, provider Provider, reqEditors ...RequestEditorFn) (*GetConnectStatusClientResponse, error) {
	rsp, err := c.GetConnectStatus(ctx, provider, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetConnectStatusClientResponse(rsp)
}

// CreateConnectionWithBodyWithResponse Connect to PMS/OTA provider
//
// Establish a connection to a PMS or OTA platform. Credentials vary by provider — see docs for each provider.
//
// Airbnb-specific: pass `redirectUrl` (where to send the user after consent) and optionally `accessType`. Three tiers: `read_only` grants read-only scopes; `messaging` grants read scopes plus message read/send but NOT property management, so it can coexist with another app (e.g. an existing PMS) that already holds property management on the same Airbnb account; `full_access` — the default — grants full host scopes including the exclusive property management (only one app per Airbnb account can hold it). The response returns a hosted `url` to redirect the user to.
//
// Booking.com: pass `redirectUrl` (no `accessType`). The response returns a hosted `url` — send the user there to designate FantasticStay in their Booking.com Extranet and paste their Hotel ID. Same response shape as Airbnb (`url`, `sessionId`, `expiresAt`).
//
// PMS providers (api-key based) pass `apiKey` instead; Plumguide passes `clientId`/`clientSecret`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/{provider} (the `CreateConnection` operationId).
func (c *ClientWithResponses) CreateConnectionWithBodyWithResponse(ctx context.Context, provider Provider, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateConnectionClientResponse, error) {
	rsp, err := c.CreateConnectionWithBody(ctx, provider, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateConnectionClientResponse(rsp)
}

// CreateConnectionWithResponse Connect to PMS/OTA provider
//
// Establish a connection to a PMS or OTA platform. Credentials vary by provider — see docs for each provider.
//
// Airbnb-specific: pass `redirectUrl` (where to send the user after consent) and optionally `accessType`. Three tiers: `read_only` grants read-only scopes; `messaging` grants read scopes plus message read/send but NOT property management, so it can coexist with another app (e.g. an existing PMS) that already holds property management on the same Airbnb account; `full_access` — the default — grants full host scopes including the exclusive property management (only one app per Airbnb account can hold it). The response returns a hosted `url` to redirect the user to.
//
// Booking.com: pass `redirectUrl` (no `accessType`). The response returns a hosted `url` — send the user there to designate FantasticStay in their Booking.com Extranet and paste their Hotel ID. Same response shape as Airbnb (`url`, `sessionId`, `expiresAt`).
//
// PMS providers (api-key based) pass `apiKey` instead; Plumguide passes `clientId`/`clientSecret`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/connect/{provider} (the `CreateConnection` operationId).
func (c *ClientWithResponses) CreateConnectionWithResponse(ctx context.Context, provider Provider, body CreateConnectionJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateConnectionClientResponse, error) {
	rsp, err := c.CreateConnection(ctx, provider, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateConnectionClientResponse(rsp)
}

// ListConversationsWithResponse List conversations
//
// Cursor-paginated list of message threads owned by the workspace. Backed by main vanio's `/api/threads/list` which keyset-paginates against `(last_message_at, id)` for constant per-page cost. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// Filters: `platform` (`airbnb`|`booking`|`vrbo`|`website`|`email`), `status` (`open`|`archived` — `archived` is a stable no-op until the bit lands on `message_threads`).
//
// **Inactive listings:** conversations that belong to an inactive listing (by the thread's listing or its reservation's listing) are left out of the page and of `pagination.total`. Inactive listings keep syncing; activate the listing to use it here.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/conversations (the `ListConversations` operationId).
func (c *ClientWithResponses) ListConversationsWithResponse(ctx context.Context, params *ListConversationsParams, reqEditors ...RequestEditorFn) (*ListConversationsClientResponse, error) {
	rsp, err := c.ListConversations(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListConversationsClientResponse(rsp)
}

// GetConversationWithResponse Get conversation detail
//
// Returns one thread (the same shape as the list-row `Conversation`) plus expanded `host` (from `airbnb_hosts` for the thread's `host_id`) and `guest` (resolved via the thread's `reservation_id`, with up to 50 contacts) blocks.
//
// A conversation that belongs to an inactive listing (by the thread's listing or its reservation's listing) returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/conversations/{id} (the `GetConversation` operationId).
func (c *ClientWithResponses) GetConversationWithResponse(ctx context.Context, id int, params *GetConversationParams, reqEditors ...RequestEditorFn) (*GetConversationClientResponse, error) {
	rsp, err := c.GetConversation(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetConversationClientResponse(rsp)
}

// ListConversationMessagesWithResponse List messages in a conversation
//
// Cursor-paginated messages within one thread. Defaults to newest-first (`?order=desc`); pass `?order=asc` for chronological replay. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// A conversation that belongs to an inactive listing returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/conversations/{id}/messages (the `ListConversationMessages` operationId).
func (c *ClientWithResponses) ListConversationMessagesWithResponse(ctx context.Context, id int, params *ListConversationMessagesParams, reqEditors ...RequestEditorFn) (*ListConversationMessagesClientResponse, error) {
	rsp, err := c.ListConversationMessages(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListConversationMessagesClientResponse(rsp)
}

// SendConversationMessageWithBodyWithResponse Send a message to the guest
//
// Sends a message to the guest on this conversation and records it in the thread.
//
// Omit `channel` and the message goes out on whichever channel the conversation already uses (Airbnb, Booking.com, SMS, email or the direct-booking site) — that is the right default. Pass `channel` only to force a specific one.
//
// The message is attributed to the API: it is recorded with `aiGenerated` false so an API send is never counted as an automated reply.
//
// ### Airbnb rewrites links — check `contentRewritten`
//
// Airbnb rejects guest messages containing a link, an email address or a phone number, and names the offending text. When that happens the offending fragment is stripped and the remainder is re-sent once, which means **the guest receives a message that is not the one you wrote**. Reporting that as a plain success would be a lie, so every response carries `contentRewritten`; when it is `true`, `deliveredContent` is the text that actually reached the guest. Check it before assuming your message went out verbatim.
//
// When the text cannot be salvaged (the link is most of the message) nothing is delivered and the call returns `422 message_not_sent` with the channel's verbatim refusal in `statusReason`.
//
// Send `Idempotency-Key` — without it, retrying after a network timeout sends the guest the same message twice.
//
// ### Attachments
//
// Send files with `attachments: [{ url, contentType?, filename? }]` — public `https://` URLs, up to 5 per request, 10 MB each. `message` may be omitted when there are attachments (except on Booking.com). Repull downloads each file, reads its real type from the bytes, keeps a durable copy and delivers it through the channel's own file flow. **Every file is checked before anything is sent**: if one is unreachable, too large or of a type the channel refuses, the call returns 422 naming the file (`index`) and the guest receives nothing.
//
// | Channel | Accepted types | Text | How it arrives |
// |---|---|---|---|
// | Airbnb | JPEG, PNG, GIF, WebP (converted to JPEG), MP4, QuickTime | optional | each file as its own message, then the text as a separate message |
// | Booking.com | JPEG, PNG | **required** | one message carrying the text and every file |
// | SMS, email, direct-booking site chat | — | — | `422 attachments_not_supported`, nothing sent |
//
// Airbnb does not allow files in pre-booking (inquiry) conversations; that refusal comes back as `422 message_not_sent`. Because Airbnb delivers files one message at a time, a later file can be refused after earlier ones arrived — that returns `422 message_partially_sent` with `parts` saying exactly which messages reached the guest; resend only the rest.
//
// The response's `attachments` lists each file's durable `url`, and `parts` lists every channel message the send produced. Read-back (`GET /v1/conversations/{id}/messages`) shows the same files in each message's `attachments`.
//
// **Inactive listings:** a conversation that belongs to an inactive listing returns `403 listing_inactive` and no message is sent. Activate the listing first.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/conversations/{id}/messages (the `SendConversationMessage` operationId).
func (c *ClientWithResponses) SendConversationMessageWithBodyWithResponse(ctx context.Context, id int, params *SendConversationMessageParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SendConversationMessageClientResponse, error) {
	rsp, err := c.SendConversationMessageWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSendConversationMessageClientResponse(rsp)
}

// SendConversationMessageWithResponse Send a message to the guest
//
// Sends a message to the guest on this conversation and records it in the thread.
//
// Omit `channel` and the message goes out on whichever channel the conversation already uses (Airbnb, Booking.com, SMS, email or the direct-booking site) — that is the right default. Pass `channel` only to force a specific one.
//
// The message is attributed to the API: it is recorded with `aiGenerated` false so an API send is never counted as an automated reply.
//
// ### Airbnb rewrites links — check `contentRewritten`
//
// Airbnb rejects guest messages containing a link, an email address or a phone number, and names the offending text. When that happens the offending fragment is stripped and the remainder is re-sent once, which means **the guest receives a message that is not the one you wrote**. Reporting that as a plain success would be a lie, so every response carries `contentRewritten`; when it is `true`, `deliveredContent` is the text that actually reached the guest. Check it before assuming your message went out verbatim.
//
// When the text cannot be salvaged (the link is most of the message) nothing is delivered and the call returns `422 message_not_sent` with the channel's verbatim refusal in `statusReason`.
//
// Send `Idempotency-Key` — without it, retrying after a network timeout sends the guest the same message twice.
//
// ### Attachments
//
// Send files with `attachments: [{ url, contentType?, filename? }]` — public `https://` URLs, up to 5 per request, 10 MB each. `message` may be omitted when there are attachments (except on Booking.com). Repull downloads each file, reads its real type from the bytes, keeps a durable copy and delivers it through the channel's own file flow. **Every file is checked before anything is sent**: if one is unreachable, too large or of a type the channel refuses, the call returns 422 naming the file (`index`) and the guest receives nothing.
//
// | Channel | Accepted types | Text | How it arrives |
// |---|---|---|---|
// | Airbnb | JPEG, PNG, GIF, WebP (converted to JPEG), MP4, QuickTime | optional | each file as its own message, then the text as a separate message |
// | Booking.com | JPEG, PNG | **required** | one message carrying the text and every file |
// | SMS, email, direct-booking site chat | — | — | `422 attachments_not_supported`, nothing sent |
//
// Airbnb does not allow files in pre-booking (inquiry) conversations; that refusal comes back as `422 message_not_sent`. Because Airbnb delivers files one message at a time, a later file can be refused after earlier ones arrived — that returns `422 message_partially_sent` with `parts` saying exactly which messages reached the guest; resend only the rest.
//
// The response's `attachments` lists each file's durable `url`, and `parts` lists every channel message the send produced. Read-back (`GET /v1/conversations/{id}/messages`) shows the same files in each message's `attachments`.
//
// **Inactive listings:** a conversation that belongs to an inactive listing returns `403 listing_inactive` and no message is sent. Activate the listing first.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/conversations/{id}/messages (the `SendConversationMessage` operationId).
func (c *ClientWithResponses) SendConversationMessageWithResponse(ctx context.Context, id int, params *SendConversationMessageParams, body SendConversationMessageJSONRequestBody, reqEditors ...RequestEditorFn) (*SendConversationMessageClientResponse, error) {
	rsp, err := c.SendConversationMessage(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSendConversationMessageClientResponse(rsp)
}

// PreapproveConversationWithBodyWithResponse Pre-approve an inquiry
//
// Pre-approve the Airbnb inquiry on this conversation: the guest who asked about dates may now book them at the listed price, without waiting on you. To change the dates, guests or price, send a special offer instead (`POST /v1/conversations/{id}/special-offers`).
//
// Find inquiries that need an answer with `GET /v1/inquiries` (default `status=open`); each carries the `conversationId` to use here.
//
// **Airbnb only**, and only for listings connected to Airbnb directly. A Booking.com, VRBO or direct-booking conversation, or an Airbnb one relayed through a PMS (Hostaway, Guesty), returns `422 channel_not_supported` and nothing is sent.
//
// The inquiry is marked `pre_approved` everywhere, the same as pre-approving in Airbnb.
//
// An Airbnb refusal is never reported as a success: an inquiry that already moved on is `409 inquiry_no_longer_open`, an expired one `409 inquiry_expired`, a conversation that already has a booking `409 conversation_already_booked`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/conversations/{id}/pre-approval (the `PreapproveConversation` operationId).
func (c *ClientWithResponses) PreapproveConversationWithBodyWithResponse(ctx context.Context, id int, params *PreapproveConversationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PreapproveConversationClientResponse, error) {
	rsp, err := c.PreapproveConversationWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePreapproveConversationClientResponse(rsp)
}

// PreapproveConversationWithResponse Pre-approve an inquiry
//
// Pre-approve the Airbnb inquiry on this conversation: the guest who asked about dates may now book them at the listed price, without waiting on you. To change the dates, guests or price, send a special offer instead (`POST /v1/conversations/{id}/special-offers`).
//
// Find inquiries that need an answer with `GET /v1/inquiries` (default `status=open`); each carries the `conversationId` to use here.
//
// **Airbnb only**, and only for listings connected to Airbnb directly. A Booking.com, VRBO or direct-booking conversation, or an Airbnb one relayed through a PMS (Hostaway, Guesty), returns `422 channel_not_supported` and nothing is sent.
//
// The inquiry is marked `pre_approved` everywhere, the same as pre-approving in Airbnb.
//
// An Airbnb refusal is never reported as a success: an inquiry that already moved on is `409 inquiry_no_longer_open`, an expired one `409 inquiry_expired`, a conversation that already has a booking `409 conversation_already_booked`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/conversations/{id}/pre-approval (the `PreapproveConversation` operationId).
func (c *ClientWithResponses) PreapproveConversationWithResponse(ctx context.Context, id int, params *PreapproveConversationParams, body PreapproveConversationJSONRequestBody, reqEditors ...RequestEditorFn) (*PreapproveConversationClientResponse, error) {
	rsp, err := c.PreapproveConversation(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePreapproveConversationClientResponse(rsp)
}

// CreateConversationSpecialOfferWithBodyWithResponse Send a special offer
//
// Send the guest on this conversation an Airbnb special offer: your own dates, guest count and total price. The guest has 24 hours to book it. Use it to answer an inquiry with different terms, or to make a returning guest a custom price. To accept the guest’s own dates and price as they asked, pre-approve instead (`POST /v1/conversations/{id}/pre-approval`).
//
// `listingId` is optional: omit it to offer the listing the guest asked about. It is a **Repull** listing id; Repull sends Airbnb its own listing id, using the link that belongs to this conversation’s Airbnb account.
//
// `totalPrice` is the whole stay, in the listing’s Airbnb currency — Airbnb does not take a currency on an offer.
//
// **Airbnb only**, and only for listings connected to Airbnb directly; anything else is `422 channel_not_supported` and nothing is sent. The inquiry is marked `special_offer_sent`.
//
// An offer Airbnb refuses is never a `201`: dates that are taken, a price below Airbnb’s minimum, too many guests and the like are `422 airbnb_rejected` with Airbnb’s own reason in `message`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again. Without it, a retry after a timeout can send the guest two offers.
//
// Read or withdraw the offer with `GET` / `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/conversations/{id}/special-offers (the `CreateConversationSpecialOffer` operationId).
func (c *ClientWithResponses) CreateConversationSpecialOfferWithBodyWithResponse(ctx context.Context, id int, params *CreateConversationSpecialOfferParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateConversationSpecialOfferClientResponse, error) {
	rsp, err := c.CreateConversationSpecialOfferWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateConversationSpecialOfferClientResponse(rsp)
}

// CreateConversationSpecialOfferWithResponse Send a special offer
//
// Send the guest on this conversation an Airbnb special offer: your own dates, guest count and total price. The guest has 24 hours to book it. Use it to answer an inquiry with different terms, or to make a returning guest a custom price. To accept the guest’s own dates and price as they asked, pre-approve instead (`POST /v1/conversations/{id}/pre-approval`).
//
// `listingId` is optional: omit it to offer the listing the guest asked about. It is a **Repull** listing id; Repull sends Airbnb its own listing id, using the link that belongs to this conversation’s Airbnb account.
//
// `totalPrice` is the whole stay, in the listing’s Airbnb currency — Airbnb does not take a currency on an offer.
//
// **Airbnb only**, and only for listings connected to Airbnb directly; anything else is `422 channel_not_supported` and nothing is sent. The inquiry is marked `special_offer_sent`.
//
// An offer Airbnb refuses is never a `201`: dates that are taken, a price below Airbnb’s minimum, too many guests and the like are `422 airbnb_rejected` with Airbnb’s own reason in `message`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again. Without it, a retry after a timeout can send the guest two offers.
//
// Read or withdraw the offer with `GET` / `DELETE /v1/conversations/{id}/special-offers/{offerId}`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/conversations/{id}/special-offers (the `CreateConversationSpecialOffer` operationId).
func (c *ClientWithResponses) CreateConversationSpecialOfferWithResponse(ctx context.Context, id int, params *CreateConversationSpecialOfferParams, body CreateConversationSpecialOfferJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateConversationSpecialOfferClientResponse, error) {
	rsp, err := c.CreateConversationSpecialOffer(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateConversationSpecialOfferClientResponse(rsp)
}

// WithdrawConversationSpecialOfferWithResponse Withdraw a special offer
//
// Withdraw a special offer the guest has not booked yet, so it can no longer be booked. An offer the guest already booked cannot be withdrawn — Airbnb refuses with `409 inquiry_no_longer_open`; cancel the booking instead.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/conversations/{id}/special-offers/{offerId} (the `WithdrawConversationSpecialOffer` operationId).
func (c *ClientWithResponses) WithdrawConversationSpecialOfferWithResponse(ctx context.Context, id int, offerId string, reqEditors ...RequestEditorFn) (*WithdrawConversationSpecialOfferClientResponse, error) {
	rsp, err := c.WithdrawConversationSpecialOffer(ctx, id, offerId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseWithdrawConversationSpecialOfferClientResponse(rsp)
}

// GetConversationSpecialOfferWithResponse Get a special offer
//
// Read a special offer on this conversation back from Airbnb — typically to check its `status` (`active` until the guest books it, it expires, or you withdraw it). Read live from Airbnb with the conversation’s own Airbnb account.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/conversations/{id}/special-offers/{offerId} (the `GetConversationSpecialOffer` operationId).
func (c *ClientWithResponses) GetConversationSpecialOfferWithResponse(ctx context.Context, id int, offerId string, reqEditors ...RequestEditorFn) (*GetConversationSpecialOfferClientResponse, error) {
	rsp, err := c.GetConversationSpecialOffer(ctx, id, offerId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetConversationSpecialOfferClientResponse(rsp)
}

// ListGuestsWithResponse List guests
//
// Cursor-paginated list of guests in the workspace. Walks `guests.id ASC` keyset for constant per-page cost regardless of how many guests the customer has. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// Filters: `q` (substring on name/email/phone), `has_reservation` (`true`|`false`), `listing_id` (restrict to guests with at least one reservation on that listing).
//
// **Inactive listings:** a guest whose every reservation is on an inactive listing is left out of the page and of `pagination.total`, and `totalReservations`, `totalRevenue`, `firstStayedAt` / `lastStayedAt`, `has_reservation` and `listing_id` only consider reservations that are not on an inactive listing. Guests with no reservations are tied to no listing and are always listed. Filtering by an inactive `listing_id` returns `403 listing_inactive`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/guests (the `ListGuests` operationId).
func (c *ClientWithResponses) ListGuestsWithResponse(ctx context.Context, params *ListGuestsParams, reqEditors ...RequestEditorFn) (*ListGuestsClientResponse, error) {
	rsp, err := c.ListGuests(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListGuestsClientResponse(rsp)
}

// CreateGuestWithBodyWithResponse Create a guest
//
// Creates a guest in the workspace, with contact normalisation applied (phone digits, email lowercased) and each contact stored as its own record.
//
// **This is find-or-create, and the response tells you which happened.** A guest already on file matching on email — then phone — AND name is returned instead of a duplicate being created. Read the `created` flag rather than inferring from the status: `201` with `created: true` means a new record was written, `200` with `created: false` means an existing guest matched. Quietly handing back an existing record as though it were new is exactly the ambiguity this flag removes.
//
// Field names are camelCase, and an unrecognised field is rejected by name rather than silently dropped.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/guests (the `CreateGuest` operationId).
func (c *ClientWithResponses) CreateGuestWithBodyWithResponse(ctx context.Context, params *CreateGuestParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateGuestClientResponse, error) {
	rsp, err := c.CreateGuestWithBody(ctx, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateGuestClientResponse(rsp)
}

// CreateGuestWithResponse Create a guest
//
// Creates a guest in the workspace, with contact normalisation applied (phone digits, email lowercased) and each contact stored as its own record.
//
// **This is find-or-create, and the response tells you which happened.** A guest already on file matching on email — then phone — AND name is returned instead of a duplicate being created. Read the `created` flag rather than inferring from the status: `201` with `created: true` means a new record was written, `200` with `created: false` means an existing guest matched. Quietly handing back an existing record as though it were new is exactly the ambiguity this flag removes.
//
// Field names are camelCase, and an unrecognised field is rejected by name rather than silently dropped.
//
// Send `Idempotency-Key` to make a retry safe.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/guests (the `CreateGuest` operationId).
func (c *ClientWithResponses) CreateGuestWithResponse(ctx context.Context, params *CreateGuestParams, body CreateGuestJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateGuestClientResponse, error) {
	rsp, err := c.CreateGuest(ctx, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateGuestClientResponse(rsp)
}

// GetGuestWithResponse Get guest profile
//
// Returns the full guest profile — base list-row fields plus contacts, flags, notes, risk metadata, and reservation aggregates. Aggregates main vanio's `GuestService.getGuestProfile()` into the public Repull shape so SDK consumers don't have to learn the internal schema.
//
// **Inactive listings:** a guest whose every reservation is on an inactive listing returns `403 listing_inactive` naming those listings (the guest is kept, so this is not a 404). Otherwise the reservation aggregates exclude reservations on inactive listings. A guest with no reservations is always readable.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/guests/{id} (the `GetGuest` operationId).
func (c *ClientWithResponses) GetGuestWithResponse(ctx context.Context, id int, params *GetGuestParams, reqEditors ...RequestEditorFn) (*GetGuestClientResponse, error) {
	rsp, err := c.GetGuest(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetGuestClientResponse(rsp)
}

// GetHealthWithResponse Health check
//
// Liveness probe. Returns `{ status: "ok", version }` when the API process is running. No auth required. Suitable for uptime monitors, load-balancer health checks, and SDK self-tests.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/health (the `GetHealth` operationId).
func (c *ClientWithResponses) GetHealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetHealthClientResponse, error) {
	rsp, err := c.GetHealth(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetHealthClientResponse(rsp)
}

// GetAuthHealthWithResponse API-key authentication path health
//
// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/health/auth (the `GetAuthHealth` operationId).
func (c *ClientWithResponses) GetAuthHealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetAuthHealthClientResponse, error) {
	rsp, err := c.GetAuthHealth(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetAuthHealthClientResponse(rsp)
}

// GetChannelHealthWithResponse Per-channel connectivity health
//
// Reports reachability and auth state for one channel (`airbnb`, `booking`, `vrbo`, `plumguide`). Use it to tell "the channel is down" apart from "this workspace's connection expired".
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/health/channels/{channel} (the `GetChannelHealth` operationId).
func (c *ClientWithResponses) GetChannelHealthWithResponse(ctx context.Context, channel GetChannelHealthParamsChannel, reqEditors ...RequestEditorFn) (*GetChannelHealthClientResponse, error) {
	rsp, err := c.GetChannelHealth(ctx, channel, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetChannelHealthClientResponse(rsp)
}

// GetMcpHealthWithResponse MCP server reachability
//
// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/health/mcp (the `GetMcpHealth` operationId).
func (c *ClientWithResponses) GetMcpHealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetMcpHealthClientResponse, error) {
	rsp, err := c.GetMcpHealth(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetMcpHealthClientResponse(rsp)
}

// GetWebhooksHealthWithResponse Webhook delivery pipeline health
//
// Component-level probe. `GET /v1/health` reports the API as a whole; this reports one dependency so an incident can be localised without guessing.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/health/webhooks (the `GetWebhooksHealth` operationId).
func (c *ClientWithResponses) GetWebhooksHealthWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetWebhooksHealthClientResponse, error) {
	rsp, err := c.GetWebhooksHealth(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetWebhooksHealthClientResponse(rsp)
}

// ListInquiriesWithResponse List inquiries
//
// Airbnb inquiries — guests asking about dates before booking — newest first. By default only `open` ones: nobody has answered and the stay is still ahead. Answer one with `POST /v1/conversations/{conversationId}/pre-approval` (accept their dates and price) or `POST /v1/conversations/{conversationId}/special-offers` (your own terms).
//
// Booking **requests** are not inquiries: they are reservations with status `pending` — list them with `GET /v1/reservations?status=pending` and answer with `POST /v1/reservations/{id}/accept` or `/decline`.
//
// **Pagination:** pass `pagination.nextCursor` back as `?cursor=` until `pagination.hasMore` is `false`. `?offset=` also works (0..10000). `limit` defaults to 50, max 100.
//
// Inquiries on inactive listings are left out; `?listing_id=` naming an inactive listing returns `403 listing_inactive`. `X-Account-Id` narrows to one connected account.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/inquiries (the `ListInquiries` operationId).
func (c *ClientWithResponses) ListInquiriesWithResponse(ctx context.Context, params *ListInquiriesParams, reqEditors ...RequestEditorFn) (*ListInquiriesClientResponse, error) {
	rsp, err := c.ListInquiries(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListInquiriesClientResponse(rsp)
}

// ClearKvWithResponse Clear KV entries by prefix
//
// Bulk-deletes every key in the project whose name starts with `prefix`. The `prefix` parameter is required — there is no "delete every key in this project" shortcut; pass an empty `prefix` is rejected with 422 to prevent accidental wipes. Returns the number of rows removed.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/kv (the `ClearKv` operationId).
func (c *ClientWithResponses) ClearKvWithResponse(ctx context.Context, params *ClearKvParams, reqEditors ...RequestEditorFn) (*ClearKvClientResponse, error) {
	rsp, err := c.ClearKv(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseClearKvClientResponse(rsp)
}

// ListKvWithResponse List KV entries
//
// Returns every non-expired key-value row in the given project, sorted ascending by key. Use `prefix` to scope to a key namespace (e.g. `prefix=user:42:` to fetch all entries for one user). Hard cap of 1,000 rows per response — for projects approaching that, paginate by walking prefix buckets.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/kv (the `ListKv` operationId).
func (c *ClientWithResponses) ListKvWithResponse(ctx context.Context, params *ListKvParams, reqEditors ...RequestEditorFn) (*ListKvClientResponse, error) {
	rsp, err := c.ListKv(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListKvClientResponse(rsp)
}

// DeleteKvWithResponse Delete a KV entry
//
// Removes a single key. Returns `{ deleted: true }` if the row was present, `{ deleted: false }` if it was already absent — both are 200 (idempotent).
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/kv/{key} (the `DeleteKv` operationId).
func (c *ClientWithResponses) DeleteKvWithResponse(ctx context.Context, key string, params *DeleteKvParams, reqEditors ...RequestEditorFn) (*DeleteKvClientResponse, error) {
	rsp, err := c.DeleteKv(ctx, key, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteKvClientResponse(rsp)
}

// GetKvWithResponse Get a KV entry
//
// Fetches a single key. Returns 404 when the key does not exist OR has expired (rows past `ttl_at` are filtered from reads). Cross-tenant lookups also return 404 — the API never reveals existence of another customer's keys.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/kv/{key} (the `GetKv` operationId).
func (c *ClientWithResponses) GetKvWithResponse(ctx context.Context, key string, params *GetKvParams, reqEditors ...RequestEditorFn) (*GetKvClientResponse, error) {
	rsp, err := c.GetKv(ctx, key, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetKvClientResponse(rsp)
}

// SetKvWithBodyWithResponse Set a KV entry
//
// Upserts a key. The full row is replaced — there is no partial update. Pass `ttl_seconds` (positive integer) to auto-expire the row; omit for no expiry. **Caps:** 64 KiB per row (key bytes + value JSON bytes), 1 MiB per customer (sum across ALL projects/keys). Over either cap returns 413.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/kv/{key} (the `SetKv` operationId).
func (c *ClientWithResponses) SetKvWithBodyWithResponse(ctx context.Context, key string, params *SetKvParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetKvClientResponse, error) {
	rsp, err := c.SetKvWithBody(ctx, key, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetKvClientResponse(rsp)
}

// SetKvWithResponse Set a KV entry
//
// Upserts a key. The full row is replaced — there is no partial update. Pass `ttl_seconds` (positive integer) to auto-expire the row; omit for no expiry. **Caps:** 64 KiB per row (key bytes + value JSON bytes), 1 MiB per customer (sum across ALL projects/keys). Over either cap returns 413.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/kv/{key} (the `SetKv` operationId).
func (c *ClientWithResponses) SetKvWithResponse(ctx context.Context, key string, params *SetKvParams, body SetKvJSONRequestBody, reqEditors ...RequestEditorFn) (*SetKvClientResponse, error) {
	rsp, err := c.SetKv(ctx, key, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetKvClientResponse(rsp)
}

// ListListingsWithResponse List listings
//
// Cursor-paginated list of listings owned by the authenticated workspace. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request to walk the full set. `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. Filters: `q` (substring on name/street/city), `status`, `channel`.
//
// **Optional expansions:** Pass `?include=content` to enrich each row with the rich content slab (summary, description, space, house rules, etc. — sourced from `listings_descriptions` for the `en` locale). Pass `?include=details` for the structural slab (bedrooms, bathrooms, person capacity, check-in window, wifi, house manual, etc.). Both default to `null` per row when the underlying `listings_descriptions` / `listings_details` row is missing — distinct from the field being absent (which signals the expansion was not requested). Pass `?include=thumbnail` to guarantee `thumbnailUrl` on every returned row — including the reduced inactive ones. Combine comma-separated, e.g. `?include=content,thumbnail`. The default response stays lean; consumers must opt in.
//
// **Inactive listings:** by default only active listings are returned. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated, so when `status` asks for inactive ones they carry only `id`, `name`, `status` and `channels` — enough to choose what to activate with `PATCH /v1/listings/{id}`. The `content` and `details` expansions are not applied to them. `?include=thumbnail` is the one exception: it adds `thumbnailUrl` to an inactive row so a single request can render an active/inactive selection screen with pictures, instead of one follow-up call per listing (which an inactive listing would answer with `403 listing_inactive` anyway).
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/listings (the `ListListings` operationId).
func (c *ClientWithResponses) ListListingsWithResponse(ctx context.Context, params *ListListingsParams, reqEditors ...RequestEditorFn) (*ListListingsClientResponse, error) {
	rsp, err := c.ListListings(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListListingsClientResponse(rsp)
}

// CreateListingWithBodyWithResponse Create a Repull listing
//
// Create a new vacation-rental listing under the authenticated workspace. The listing is stored in the canonical Vanio listings tables and can be published to multiple channels (Airbnb, Booking.com) via the publish endpoints.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings (the `CreateListing` operationId).
func (c *ClientWithResponses) CreateListingWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateListingClientResponse, error) {
	rsp, err := c.CreateListingWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateListingClientResponse(rsp)
}

// CreateListingWithResponse Create a Repull listing
//
// Create a new vacation-rental listing under the authenticated workspace. The listing is stored in the canonical Vanio listings tables and can be published to multiple channels (Airbnb, Booking.com) via the publish endpoints.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings (the `CreateListing` operationId).
func (c *ClientWithResponses) CreateListingWithResponse(ctx context.Context, body CreateListingJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateListingClientResponse, error) {
	rsp, err := c.CreateListing(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateListingClientResponse(rsp)
}

// BulkApplyPricingWithBodyWithResponse Bulk apply or decline pricing recommendations
//
// Apply or decline pending Atlas pricing recommendations across many listings in one call. Built for power users with hundreds of listings who would otherwise need 500 sequential single-listing POSTs.
//
// - `items` is capped at 500 entries per request — exceeding returns 422.
// - Per-item failures (stale listing IDs, no pending recs, channel auth blips) DO NOT fail the whole batch — partial success is the norm at this scale and the granular `failed[]` array lets the SDK retry just the bad entries.
// - Tier-limit accounting: this endpoint counts as **1 API call** regardless of how many items the body contains.
//
// Apply path writes the recommended price to each listing's calendar via the calendar service (which fans out to Airbnb/Booking/VRBO) then marks the Atlas recommendation `applied`. Decline path is Atlas-only — fast.
//
// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/pricing/bulk (the `BulkApplyPricing` operationId).
func (c *ClientWithResponses) BulkApplyPricingWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*BulkApplyPricingClientResponse, error) {
	rsp, err := c.BulkApplyPricingWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBulkApplyPricingClientResponse(rsp)
}

// BulkApplyPricingWithResponse Bulk apply or decline pricing recommendations
//
// Apply or decline pending Atlas pricing recommendations across many listings in one call. Built for power users with hundreds of listings who would otherwise need 500 sequential single-listing POSTs.
//
// - `items` is capped at 500 entries per request — exceeding returns 422.
// - Per-item failures (stale listing IDs, no pending recs, channel auth blips) DO NOT fail the whole batch — partial success is the norm at this scale and the granular `failed[]` array lets the SDK retry just the bad entries.
// - Tier-limit accounting: this endpoint counts as **1 API call** regardless of how many items the body contains.
//
// Apply path writes the recommended price to each listing's calendar via the calendar service (which fans out to Airbnb/Booking/VRBO) then marks the Atlas recommendation `applied`. Decline path is Atlas-only — fast.
//
// Returns `403 listing_inactive` naming every inactive listing when any listing in the request is inactive; nothing is written.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/pricing/bulk (the `BulkApplyPricing` operationId).
func (c *ClientWithResponses) BulkApplyPricingWithResponse(ctx context.Context, body BulkApplyPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*BulkApplyPricingClientResponse, error) {
	rsp, err := c.BulkApplyPricing(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseBulkApplyPricingClientResponse(rsp)
}

// SetListingsStatusWithBodyWithResponse Activate or deactivate listings in bulk
//
// Sets up to 500 listings active or inactive in one call. Send `{ "listingIds": ["4118", "4119"], "active": false }` to deactivate them, or `"active": true` to activate them.
//
// An inactive listing is not counted toward your plan's listing limit or billed. It is NOT deleted and the upstream channel (Airbnb / Booking.com / your PMS) is never touched — its data keeps syncing, so it is complete the moment you activate it again. Until then it cannot be read, changed, or receive webhooks.
//
// **All or nothing.** Nothing changes unless the whole request can be applied:
// - If any id is not one of your listings, the call returns `404` naming those ids.
// - If activating would take you over your plan's listing limit, the call returns `402 listings_limit_exceeded`. Only listings that are currently inactive count toward the new total, so re-sending ids that are already active never trips the limit.
//
// Deactivating is always allowed, including when your account is already over its limit — it is how you get back under it.
//
// **Idempotent.** Ids already in the requested state are returned in `unchanged`; ids this call changed are returned in `updated`.
//
// For a single listing, `PATCH /v1/listings/{id}` does the same.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/status (the `SetListingsStatus` operationId).
func (c *ClientWithResponses) SetListingsStatusWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*SetListingsStatusClientResponse, error) {
	rsp, err := c.SetListingsStatusWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetListingsStatusClientResponse(rsp)
}

// SetListingsStatusWithResponse Activate or deactivate listings in bulk
//
// Sets up to 500 listings active or inactive in one call. Send `{ "listingIds": ["4118", "4119"], "active": false }` to deactivate them, or `"active": true` to activate them.
//
// An inactive listing is not counted toward your plan's listing limit or billed. It is NOT deleted and the upstream channel (Airbnb / Booking.com / your PMS) is never touched — its data keeps syncing, so it is complete the moment you activate it again. Until then it cannot be read, changed, or receive webhooks.
//
// **All or nothing.** Nothing changes unless the whole request can be applied:
// - If any id is not one of your listings, the call returns `404` naming those ids.
// - If activating would take you over your plan's listing limit, the call returns `402 listings_limit_exceeded`. Only listings that are currently inactive count toward the new total, so re-sending ids that are already active never trips the limit.
//
// Deactivating is always allowed, including when your account is already over its limit — it is how you get back under it.
//
// **Idempotent.** Ids already in the requested state are returned in `unchanged`; ids this call changed are returned in `updated`.
//
// For a single listing, `PATCH /v1/listings/{id}` does the same.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/status (the `SetListingsStatus` operationId).
func (c *ClientWithResponses) SetListingsStatusWithResponse(ctx context.Context, body SetListingsStatusJSONRequestBody, reqEditors ...RequestEditorFn) (*SetListingsStatusClientResponse, error) {
	rsp, err := c.SetListingsStatus(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseSetListingsStatusClientResponse(rsp)
}

// DeactivateListingWithResponse Deactivate (exclude) a listing
//
// Deactivate a listing — sets it inactive and excludes it from Repull. This is a **soft** operation: the listing row is KEPT (never hard-deleted) and the upstream channel (Airbnb / Hospitable / Booking.com) is NEVER touched. Repull only mutates its own copy.
//
// Equivalent to `PATCH /v1/listings/{id}` with `{ "active": false }`. This is the primary self-serve way for a free-tier customer to trim back under the plan-listings cap — `DELETE` is served even when the account is over the cap (a 402-locked account can still call it). To bring a listing back, use `PATCH` with `{ "active": true }`.
//
// Idempotent: deactivating an already-inactive listing returns 200.
//
// To deactivate many listings at once, use `POST /v1/listings/status` with `{ "active": false }`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/listings/{id} (the `DeactivateListing` operationId).
func (c *ClientWithResponses) DeactivateListingWithResponse(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*DeactivateListingClientResponse, error) {
	rsp, err := c.DeactivateListing(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeactivateListingClientResponse(rsp)
}

// GetListingWithResponse Get a listing
//
// Fetch a single listing by id. Returns the same shape as one element of the `GET /v1/listings` response, so you can bind the result to the same model. Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403, never reveals the listing's existence.
//
// **Optional expansions:** Pass `?include=amenities` to enrich the response with the listing's amenity rows (`[]` when the listing has none). Pass `?include=content` for the rich content slab (summary, description, space, house rules, etc. — sourced from `listings_descriptions` for the `en` locale; `null` when no row is stored). Pass `?include=details` for the structural slab (bedrooms, bathrooms, person capacity, check-in window, wifi, house manual, etc.; `null` when no row is stored). Combine comma-separated, e.g. `?include=amenities,content,details`. The default response stays lean; consumers must opt in.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/listings/{id} (the `GetListing` operationId).
func (c *ClientWithResponses) GetListingWithResponse(ctx context.Context, id int, params *GetListingParams, reqEditors ...RequestEditorFn) (*GetListingClientResponse, error) {
	rsp, err := c.GetListing(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetListingClientResponse(rsp)
}

// UpdateListingActiveWithBodyWithResponse Deactivate or reactivate a listing
//
// Toggle a listing's active state. Send `{ "active": false }` to **deactivate** (exclude the listing from Repull) or `{ "active": true }` to **reactivate** it.
//
// "Deactivate" keeps the listing row — it is NOT a hard delete, and it NEVER touches the upstream channel (Airbnb / Hospitable / Booking.com). Repull only mutates its own copy of the inventory. Deactivating is the self-serve way to get back under the plan-listings cap without paying.
//
// Reactivation respects the plan-listings cap: if activating this listing would push you over the cap for your tier, the call returns `402 listings_limit_exceeded` and the listing stays inactive. Deactivate another listing or upgrade first.
//
// Idempotent: setting a listing to the state it's already in returns 200.
//
// To change many listings at once, all or nothing, use `POST /v1/listings/status`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/listings/{id} (the `UpdateListingActive` operationId).
func (c *ClientWithResponses) UpdateListingActiveWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateListingActiveClientResponse, error) {
	rsp, err := c.UpdateListingActiveWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateListingActiveClientResponse(rsp)
}

// UpdateListingActiveWithResponse Deactivate or reactivate a listing
//
// Toggle a listing's active state. Send `{ "active": false }` to **deactivate** (exclude the listing from Repull) or `{ "active": true }` to **reactivate** it.
//
// "Deactivate" keeps the listing row — it is NOT a hard delete, and it NEVER touches the upstream channel (Airbnb / Hospitable / Booking.com). Repull only mutates its own copy of the inventory. Deactivating is the self-serve way to get back under the plan-listings cap without paying.
//
// Reactivation respects the plan-listings cap: if activating this listing would push you over the cap for your tier, the call returns `402 listings_limit_exceeded` and the listing stays inactive. Deactivate another listing or upgrade first.
//
// Idempotent: setting a listing to the state it's already in returns 200.
//
// To change many listings at once, all or nothing, use `POST /v1/listings/status`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/listings/{id} (the `UpdateListingActive` operationId).
func (c *ClientWithResponses) UpdateListingActiveWithResponse(ctx context.Context, id int, body UpdateListingActiveJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateListingActiveClientResponse, error) {
	rsp, err := c.UpdateListingActive(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateListingActiveClientResponse(rsp)
}

// ListListingCompsWithResponse Comp set for a listing (with daily nightly pricing)
//
// Returns the actual comp set for a listing — the underlying competitor listings (with daily nightly pricing), not just the aggregated `compSummary` from `/pricing`. Each comp comes back with distance, bedrooms, ratings, lat/lng, platform link, and a per-day rate/availability series for the requested window.
//
// Powered by Atlas. Comps with no coordinates are excluded — there's no way to rank them by distance. Listings without coordinates return `data: []` and a `warning` field.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/listings/{id}/comps (the `ListListingComps` operationId).
func (c *ClientWithResponses) ListListingCompsWithResponse(ctx context.Context, id int, params *ListListingCompsParams, reqEditors ...RequestEditorFn) (*ListListingCompsClientResponse, error) {
	rsp, err := c.ListListingComps(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListListingCompsClientResponse(rsp)
}

// UpdateListingContentWithBodyWithResponse Update canonical listing content
//
// Write your PMS's canonical listing content — title, description, amenities, address, occupancy, and policies — into a Repull listing, making it the source of truth. This is the flagship "the PMS owns listing content, Repull distributes it" enabler.
//
// **Partial update:** every field is optional. Only the fields you send are written; absent fields are left untouched. `amenities` is a FULL replacement of the amenity set (omit to leave untouched, send `[]` to clear).
//
// **Multilingual:** send `locale` to say which language this copy is in (`it`, `pt-BR`, …). Canonical content is stored per locale, so each language keeps its own row instead of overwriting the English one. Omit it for English.
//
// **Local write only — NOT a channel publish.** This mutates Repull's own copy of the content. It does NOT push to Airbnb / Booking.com; it marks the channels dirty so a later publish knows what changed. Distribution stays a separate explicit step.
//
// **Photos are deferred:** a provided `photos` array is echoed back in the `deferred` field and NOT persisted (media ingestion is a follow-up).
//
// Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403. This endpoint is served even when the account is over the plan-listings cap, since editing content on a listing you already own never grows the portfolio.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/listings/{id}/content (the `UpdateListingContent` operationId).
func (c *ClientWithResponses) UpdateListingContentWithBodyWithResponse(ctx context.Context, id int, params *UpdateListingContentParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateListingContentClientResponse, error) {
	rsp, err := c.UpdateListingContentWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateListingContentClientResponse(rsp)
}

// UpdateListingContentWithResponse Update canonical listing content
//
// Write your PMS's canonical listing content — title, description, amenities, address, occupancy, and policies — into a Repull listing, making it the source of truth. This is the flagship "the PMS owns listing content, Repull distributes it" enabler.
//
// **Partial update:** every field is optional. Only the fields you send are written; absent fields are left untouched. `amenities` is a FULL replacement of the amenity set (omit to leave untouched, send `[]` to clear).
//
// **Multilingual:** send `locale` to say which language this copy is in (`it`, `pt-BR`, …). Canonical content is stored per locale, so each language keeps its own row instead of overwriting the English one. Omit it for English.
//
// **Local write only — NOT a channel publish.** This mutates Repull's own copy of the content. It does NOT push to Airbnb / Booking.com; it marks the channels dirty so a later publish knows what changed. Distribution stays a separate explicit step.
//
// **Photos are deferred:** a provided `photos` array is echoed back in the `deferred` field and NOT persisted (media ingestion is a follow-up).
//
// Cross-tenant access (a listing that belongs to a different workspace) returns 404 — never 403. This endpoint is served even when the account is over the plan-listings cap, since editing content on a listing you already own never grows the portfolio.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/listings/{id}/content (the `UpdateListingContent` operationId).
func (c *ClientWithResponses) UpdateListingContentWithResponse(ctx context.Context, id int, params *UpdateListingContentParams, body UpdateListingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateListingContentClientResponse, error) {
	rsp, err := c.UpdateListingContent(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateListingContentClientResponse(rsp)
}

// GenerateListingContentWithBodyWithResponse AI-generate listing content
//
// Generate guest-facing copy (title, summary, description, amenities, etc.) for a listing using Repull AI. When `photos` are provided the vision model is used for photo-grounded copy. Persists into the listing by default.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/generate-content (the `GenerateListingContent` operationId).
func (c *ClientWithResponses) GenerateListingContentWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*GenerateListingContentClientResponse, error) {
	rsp, err := c.GenerateListingContentWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGenerateListingContentClientResponse(rsp)
}

// GenerateListingContentWithResponse AI-generate listing content
//
// Generate guest-facing copy (title, summary, description, amenities, etc.) for a listing using Repull AI. When `photos` are provided the vision model is used for photo-grounded copy. Persists into the listing by default.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/generate-content (the `GenerateListingContent` operationId).
func (c *ClientWithResponses) GenerateListingContentWithResponse(ctx context.Context, id int, body GenerateListingContentJSONRequestBody, reqEditors ...RequestEditorFn) (*GenerateListingContentClientResponse, error) {
	rsp, err := c.GenerateListingContent(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGenerateListingContentClientResponse(rsp)
}

// TakeListingOfflineWithBodyWithResponse Take a listing off the market
//
// Stop this listing being sold, on every channel it is connected to, in one call.
//
// What that means differs per channel and you do not have to know which is which. On **Airbnb** the live listing is deactivated with a valid deactivation reason and then READ BACK — Airbnb accepts some deactivations and leaves the listing up, so "we sent the request" is never reported as success. On **Booking.com** there is no unlist at all; the equivalent is closing the room's availability across the whole forward window, which is what happens.
//
// **This is not the same as deactivating the listing in Repull.** The two get confused because both sound like removal, and they have opposite consequences:
//
// | | Take offline (this endpoint) | Deactivate in Repull (`PATCH /v1/listings/{id}` `{"active": false}`) |
// |---|---|---|
// | The guest-facing listing | **Stops taking bookings** | Stays live and keeps taking bookings |
// | Billing and plan limits | Unchanged | No longer billed, no longer counts toward the cap |
// | API access to the listing | Unchanged — you can still read and write it | `403 listing_inactive` until reactivated |
// | Reverse it with | `POST /v1/listings/{id}/online` | `PATCH /v1/listings/{id}` `{"active": true}` |
// | Data kept | Yes | Yes, and it keeps syncing |
//
// Neither one deletes anything, on either side.
//
// **The answer is per channel item.** A listing can sit on several Airbnb connections and a Booking.com property at once; they fail independently and a partial result is the ordinary outcome, so every item reports its own `state`, `code` and `message` and there is no top-level success flag to mislead you. Nothing is rolled back — re-send the same request to retry the items that did not land.
//
// **Booking.com ambiguity is reported, not fanned out.** A listing mapped to more than one active Booking.com property comes back with that item refused (`ambiguous_booking_mapping`) while the Airbnb items still run: closing the wrong property's availability takes real inventory off sale, and taking a listing off Airbnb is not less urgent because its Booking.com mapping is untidy. Name the property with `hotelId` and send it again.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/offline (the `TakeListingOffline` operationId).
func (c *ClientWithResponses) TakeListingOfflineWithBodyWithResponse(ctx context.Context, id int, params *TakeListingOfflineParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*TakeListingOfflineClientResponse, error) {
	rsp, err := c.TakeListingOfflineWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseTakeListingOfflineClientResponse(rsp)
}

// TakeListingOfflineWithResponse Take a listing off the market
//
// Stop this listing being sold, on every channel it is connected to, in one call.
//
// What that means differs per channel and you do not have to know which is which. On **Airbnb** the live listing is deactivated with a valid deactivation reason and then READ BACK — Airbnb accepts some deactivations and leaves the listing up, so "we sent the request" is never reported as success. On **Booking.com** there is no unlist at all; the equivalent is closing the room's availability across the whole forward window, which is what happens.
//
// **This is not the same as deactivating the listing in Repull.** The two get confused because both sound like removal, and they have opposite consequences:
//
// | | Take offline (this endpoint) | Deactivate in Repull (`PATCH /v1/listings/{id}` `{"active": false}`) |
// |---|---|---|
// | The guest-facing listing | **Stops taking bookings** | Stays live and keeps taking bookings |
// | Billing and plan limits | Unchanged | No longer billed, no longer counts toward the cap |
// | API access to the listing | Unchanged — you can still read and write it | `403 listing_inactive` until reactivated |
// | Reverse it with | `POST /v1/listings/{id}/online` | `PATCH /v1/listings/{id}` `{"active": true}` |
// | Data kept | Yes | Yes, and it keeps syncing |
//
// Neither one deletes anything, on either side.
//
// **The answer is per channel item.** A listing can sit on several Airbnb connections and a Booking.com property at once; they fail independently and a partial result is the ordinary outcome, so every item reports its own `state`, `code` and `message` and there is no top-level success flag to mislead you. Nothing is rolled back — re-send the same request to retry the items that did not land.
//
// **Booking.com ambiguity is reported, not fanned out.** A listing mapped to more than one active Booking.com property comes back with that item refused (`ambiguous_booking_mapping`) while the Airbnb items still run: closing the wrong property's availability takes real inventory off sale, and taking a listing off Airbnb is not less urgent because its Booking.com mapping is untidy. Name the property with `hotelId` and send it again.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/offline (the `TakeListingOffline` operationId).
func (c *ClientWithResponses) TakeListingOfflineWithResponse(ctx context.Context, id int, params *TakeListingOfflineParams, body TakeListingOfflineJSONRequestBody, reqEditors ...RequestEditorFn) (*TakeListingOfflineClientResponse, error) {
	rsp, err := c.TakeListingOffline(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseTakeListingOfflineClientResponse(rsp)
}

// TakeListingOnlineWithBodyWithResponse Put a listing back on the market
//
// Put this listing back on sale, on every channel it is connected to. The counterpart of `POST /v1/listings/{id}/offline`, which documents the per-item response and the difference between this and deactivating a listing in Repull.
//
// **It does not push content.** On **Airbnb** it re-enables sync and makes the listing available again; anything that changed while the listing was down is still unpublished, so follow with `POST /v1/listings/{id}/publish/airbnb` if the content moved. On **Booking.com** it re-syncs the true calendar rather than opening everything: dates that are genuinely blocked — a reservation, an owner stay — stay blocked, and only the closure `offline` wrote lifts. The two directions are not mirror images, and that is deliberate.
//
// **One asymmetry worth planning for.** Taking a listing down passes no billing gate; putting it back up goes through the channel-publish gate. So on a workspace whose subscription has lapsed, `offline` still works and this endpoint answers `402 payment_required` — a listing can be left off the market until billing is sorted out. That refusal is reported as a billing refusal with the action that fixes it, never as a channel error: retrying, or reconnecting the channel, does nothing for it.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/online (the `TakeListingOnline` operationId).
func (c *ClientWithResponses) TakeListingOnlineWithBodyWithResponse(ctx context.Context, id int, params *TakeListingOnlineParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*TakeListingOnlineClientResponse, error) {
	rsp, err := c.TakeListingOnlineWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseTakeListingOnlineClientResponse(rsp)
}

// TakeListingOnlineWithResponse Put a listing back on the market
//
// Put this listing back on sale, on every channel it is connected to. The counterpart of `POST /v1/listings/{id}/offline`, which documents the per-item response and the difference between this and deactivating a listing in Repull.
//
// **It does not push content.** On **Airbnb** it re-enables sync and makes the listing available again; anything that changed while the listing was down is still unpublished, so follow with `POST /v1/listings/{id}/publish/airbnb` if the content moved. On **Booking.com** it re-syncs the true calendar rather than opening everything: dates that are genuinely blocked — a reservation, an owner stay — stay blocked, and only the closure `offline` wrote lifts. The two directions are not mirror images, and that is deliberate.
//
// **One asymmetry worth planning for.** Taking a listing down passes no billing gate; putting it back up goes through the channel-publish gate. So on a workspace whose subscription has lapsed, `offline` still works and this endpoint answers `402 payment_required` — a listing can be left off the market until billing is sorted out. That refusal is reported as a billing refusal with the action that fixes it, never as a channel error: retrying, or reconnecting the channel, does nothing for it.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/online (the `TakeListingOnline` operationId).
func (c *ClientWithResponses) TakeListingOnlineWithResponse(ctx context.Context, id int, params *TakeListingOnlineParams, body TakeListingOnlineJSONRequestBody, reqEditors ...RequestEditorFn) (*TakeListingOnlineClientResponse, error) {
	rsp, err := c.TakeListingOnline(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseTakeListingOnlineClientResponse(rsp)
}

// DeleteListingPhotoWithBodyWithResponse Delete a stored listing photo
//
// Deletes a single stored photo by its storage `path` (as returned by `GET /v1/listings/{id}/photos` or `POST /v1/listings/{id}/photos/upload-url`).
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/listings/{id}/photos (the `DeleteListingPhoto` operationId).
func (c *ClientWithResponses) DeleteListingPhotoWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*DeleteListingPhotoClientResponse, error) {
	rsp, err := c.DeleteListingPhotoWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteListingPhotoClientResponse(rsp)
}

// DeleteListingPhotoWithResponse Delete a stored listing photo
//
// Deletes a single stored photo by its storage `path` (as returned by `GET /v1/listings/{id}/photos` or `POST /v1/listings/{id}/photos/upload-url`).
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/listings/{id}/photos (the `DeleteListingPhoto` operationId).
func (c *ClientWithResponses) DeleteListingPhotoWithResponse(ctx context.Context, id int, body DeleteListingPhotoJSONRequestBody, reqEditors ...RequestEditorFn) (*DeleteListingPhotoClientResponse, error) {
	rsp, err := c.DeleteListingPhoto(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteListingPhotoClientResponse(rsp)
}

// ListListingPhotosWithResponse List a listing's stored photos
//
// Returns the photo set currently stored for this listing.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/listings/{id}/photos (the `ListListingPhotos` operationId).
func (c *ClientWithResponses) ListListingPhotosWithResponse(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*ListListingPhotosClientResponse, error) {
	rsp, err := c.ListListingPhotos(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListListingPhotosClientResponse(rsp)
}

// CreateListingPhotoUploadUrlWithBodyWithResponse Mint a direct-to-storage photo upload URL
//
// Mints a short-lived signed upload URL + token for a listing photo. **The client PUTs the raw file bytes directly to the returned `uploadUrl` — the file bytes never pass through the Repull API or main vanio.** This endpoint only mints the URL; do not POST the file itself here, it will not be accepted.
//
// Flow: (1) POST here with `fileName`/`fileType`/optional `fileSize` to get `{ uploadUrl, token, path, publicUrl, expiresIn }`; (2) PUT the raw file bytes to `uploadUrl` from the client; (3) `publicUrl` is the durable URL for the uploaded photo — attach it to the listing via `PUT /v1/listings/{id}/content` (`photos` field) or list it back via `GET /v1/listings/{id}/photos`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/photos/upload-url (the `CreateListingPhotoUploadUrl` operationId).
func (c *ClientWithResponses) CreateListingPhotoUploadUrlWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateListingPhotoUploadUrlClientResponse, error) {
	rsp, err := c.CreateListingPhotoUploadUrlWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateListingPhotoUploadUrlClientResponse(rsp)
}

// CreateListingPhotoUploadUrlWithResponse Mint a direct-to-storage photo upload URL
//
// Mints a short-lived signed upload URL + token for a listing photo. **The client PUTs the raw file bytes directly to the returned `uploadUrl` — the file bytes never pass through the Repull API or main vanio.** This endpoint only mints the URL; do not POST the file itself here, it will not be accepted.
//
// Flow: (1) POST here with `fileName`/`fileType`/optional `fileSize` to get `{ uploadUrl, token, path, publicUrl, expiresIn }`; (2) PUT the raw file bytes to `uploadUrl` from the client; (3) `publicUrl` is the durable URL for the uploaded photo — attach it to the listing via `PUT /v1/listings/{id}/content` (`photos` field) or list it back via `GET /v1/listings/{id}/photos`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/photos/upload-url (the `CreateListingPhotoUploadUrl` operationId).
func (c *ClientWithResponses) CreateListingPhotoUploadUrlWithResponse(ctx context.Context, id int, body CreateListingPhotoUploadUrlJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateListingPhotoUploadUrlClientResponse, error) {
	rsp, err := c.CreateListingPhotoUploadUrl(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateListingPhotoUploadUrlClientResponse(rsp)
}

// GetListingPricingWithResponse Get pricing recommendations
//
// Returns date-by-date pricing recommendations for a listing's upcoming calendar window, plus the listing's base-price context and a 5km comp summary. Recommendations come from the Atlas pricing model — pre-computed nightly and stored in `pricing_recommendations`. Use POST to apply or decline pending recommendations.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/listings/{id}/pricing (the `GetListingPricing` operationId).
func (c *ClientWithResponses) GetListingPricingWithResponse(ctx context.Context, id int, params *GetListingPricingParams, reqEditors ...RequestEditorFn) (*GetListingPricingClientResponse, error) {
	rsp, err := c.GetListingPricing(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetListingPricingClientResponse(rsp)
}

// ApplyListingPricingWithBodyWithResponse Apply or decline pricing recommendations
//
// Apply: writes the recommended price to the listing's calendar for the given dates and triggers the platform fan-out (Airbnb / Booking.com / VRBO). Decline: marks the recommendation as `declined` so it stops surfacing — the model can re-recommend on the next training cycle.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/pricing (the `ApplyListingPricing` operationId).
func (c *ClientWithResponses) ApplyListingPricingWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ApplyListingPricingClientResponse, error) {
	rsp, err := c.ApplyListingPricingWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseApplyListingPricingClientResponse(rsp)
}

// ApplyListingPricingWithResponse Apply or decline pricing recommendations
//
// Apply: writes the recommended price to the listing's calendar for the given dates and triggers the platform fan-out (Airbnb / Booking.com / VRBO). Decline: marks the recommendation as `declined` so it stops surfacing — the model can re-recommend on the next training cycle.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/pricing (the `ApplyListingPricing` operationId).
func (c *ClientWithResponses) ApplyListingPricingWithResponse(ctx context.Context, id int, body ApplyListingPricingJSONRequestBody, reqEditors ...RequestEditorFn) (*ApplyListingPricingClientResponse, error) {
	rsp, err := c.ApplyListingPricing(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseApplyListingPricingClientResponse(rsp)
}

// GetListingPricingHistoryWithResponse Pricing recommendation audit trail
//
// Cursor-paginated audit trail of pricing recommendations vs applied prices for a listing across a date window. Use `pagination.nextCursor` from one response as the `cursor` query param of the next request.
//
// Defaults to ±90 days from today. Cursor is a keyset on `date ASC` — stable even if rows are added during a partner's pagination walk. `limit` is capped at 500 — exceeding returns 422.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/listings/{id}/pricing/history (the `GetListingPricingHistory` operationId).
func (c *ClientWithResponses) GetListingPricingHistoryWithResponse(ctx context.Context, id int, params *GetListingPricingHistoryParams, reqEditors ...RequestEditorFn) (*GetListingPricingHistoryClientResponse, error) {
	rsp, err := c.GetListingPricingHistory(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetListingPricingHistoryClientResponse(rsp)
}

// GetListingPricingStrategyWithResponse Get pricing strategy
//
// Returns the strategy that constrains how the Atlas pricing model behaves for this listing. If no strategy row exists yet, returns sane defaults flagged with `isDefault: true`.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/listings/{id}/pricing/strategy (the `GetListingPricingStrategy` operationId).
func (c *ClientWithResponses) GetListingPricingStrategyWithResponse(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*GetListingPricingStrategyClientResponse, error) {
	rsp, err := c.GetListingPricingStrategy(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetListingPricingStrategyClientResponse(rsp)
}

// UpdateListingPricingStrategyWithBodyWithResponse Update pricing strategy
//
// Upserts the strategy on `(listing_id, customer_id)` — repeated PUTs are idempotent. Send only the fields you want to change; omitted fields take server-side defaults.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/listings/{id}/pricing/strategy (the `UpdateListingPricingStrategy` operationId).
func (c *ClientWithResponses) UpdateListingPricingStrategyWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateListingPricingStrategyClientResponse, error) {
	rsp, err := c.UpdateListingPricingStrategyWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateListingPricingStrategyClientResponse(rsp)
}

// UpdateListingPricingStrategyWithResponse Update pricing strategy
//
// Upserts the strategy on `(listing_id, customer_id)` — repeated PUTs are idempotent. Send only the fields you want to change; omitted fields take server-side defaults.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PUT /v1/listings/{id}/pricing/strategy (the `UpdateListingPricingStrategy` operationId).
func (c *ClientWithResponses) UpdateListingPricingStrategyWithResponse(ctx context.Context, id int, body UpdateListingPricingStrategyJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateListingPricingStrategyClientResponse, error) {
	rsp, err := c.UpdateListingPricingStrategy(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateListingPricingStrategyClientResponse(rsp)
}

// GetListingPublishStatusWithResponse Per-channel publish status
//
// Returns connection state and sync activity per channel. `channels` is sync activity (empty until first push). `connections` is connection state (populated as soon as a channel is linked). Recommended polling cadence: at most once per 30s per listing — for bulk views, prefer `GET /v1/listings` and filter client-side.
//
// **When a push fails, this endpoint says why.** `channels[].pushError` carries the channel's own reason for the last failed push, verbatim — `"Links and contact info can't be shared"`, `"Check-in start time must be before end time"`, `"property_type_group must be one of […]"`. It is free text written by the channel, so render it next to the retry button rather than parsing it. `null` when the last push succeeded or none has run; pair it with `pushStatus` to tell those two apart.
//
// **It also says what you will not be allowed to change.** The `airbnb` entry in `connections` carries `lockedFields` — attributes Airbnb has locked on this listing. Airbnb does not refuse a write to one: it answers 200, reports the field as locked, and applies nothing, so a locked write is indistinguishable from a successful one unless you looked first. Read it before you let someone edit. Airbnb-only; no other channel has the concept, and no other entry carries the field.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/listings/{id}/publish-status (the `GetListingPublishStatus` operationId).
func (c *ClientWithResponses) GetListingPublishStatusWithResponse(ctx context.Context, id int, reqEditors ...RequestEditorFn) (*GetListingPublishStatusClientResponse, error) {
	rsp, err := c.GetListingPublishStatus(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetListingPublishStatusClientResponse(rsp)
}

// PublishListingToAirbnbWithBodyWithResponse Publish a listing to Airbnb
//
// Push a Repull listing's canonical content to Airbnb. Pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create a brand-new Airbnb listing under that host.
//
// **A publish is not one call to Airbnb.** It is up to eight independent ones — details, description, amenities, rooms, policies, photos, pricing, checkout_tasks — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Airbnb's own reason, per section, for the ones that did not. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Publish again once you have fixed the failing sections — a re-publish of an unchanged section is harmless.
//
// `result.live` is a different question from `result.published`. `published` is about CONTENT — every attempted section landed. `live` is about whether the listing takes bookings: it is true only when activation was actually performed and succeeded. A create can land all eight sections and still leave the listing inactive, because activation is skipped when instant-booking cannot be confirmed to be off — so `published: true` with `live: false` is a real and common outcome, and `result.warnings` says why. `live` is ABSENT, not `false`, when activation was never part of the operation: publishing to an already-mapped listing updates content and activates nothing. Only treat a listing as not-live when `live` is present and false.
//
// `result.warnings[]` lists steps that failed WITHOUT failing the publish — optional work the push carried on past. They were previously swallowed, so the only sign of one was a listing that was somehow not quite right afterwards. A publish can be `published: true` and still carry warnings; read them before concluding nothing needs doing.
//
// `result.lockedFields` names the fields Airbnb will not let this listing change at all. They are not retryable by anyone: Airbnb answers 200 and applies nothing. `GET /v1/channels/airbnb/listings/{id}` reports the same list up front.
//
// **Which fields this pushes** — title, description sections and house rules (English/primary locale), amenities, rooms and beds, photos, nightly price and fees, cancellation policy and guest controls, check-in/out times, quiet hours, property and room type, checkout tasks. **Not pushed by this endpoint:** non-primary locales (`PUT /v1/channels/airbnb/listings/{id}/descriptions`), guest-safety disclosures (`PUT …/safety-disclosures`), check-in method (`PUT …/details`), permits (`PUT …/permits`), and the calendar (`PUT …/availability`).
//
// `force: true` re-pushes every section, ignoring dirty-field tracking. Without it only the sections changed since the last successful publish are sent.
//
// Send `Idempotency-Key` to make a retry safe: a timeout on a publish otherwise leaves you unable to tell whether it ran.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/publish/airbnb (the `PublishListingToAirbnb` operationId).
func (c *ClientWithResponses) PublishListingToAirbnbWithBodyWithResponse(ctx context.Context, id int, params *PublishListingToAirbnbParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PublishListingToAirbnbClientResponse, error) {
	rsp, err := c.PublishListingToAirbnbWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePublishListingToAirbnbClientResponse(rsp)
}

// PublishListingToAirbnbWithResponse Publish a listing to Airbnb
//
// Push a Repull listing's canonical content to Airbnb. Pass `airbnbConnectionId` to update an already-mapped Airbnb listing, or `hostId` to create a brand-new Airbnb listing under that host.
//
// **A publish is not one call to Airbnb.** It is up to eight independent ones — details, description, amenities, rooms, policies, photos, pricing, checkout_tasks — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Airbnb's own reason, per section, for the ones that did not. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Publish again once you have fixed the failing sections — a re-publish of an unchanged section is harmless.
//
// `result.live` is a different question from `result.published`. `published` is about CONTENT — every attempted section landed. `live` is about whether the listing takes bookings: it is true only when activation was actually performed and succeeded. A create can land all eight sections and still leave the listing inactive, because activation is skipped when instant-booking cannot be confirmed to be off — so `published: true` with `live: false` is a real and common outcome, and `result.warnings` says why. `live` is ABSENT, not `false`, when activation was never part of the operation: publishing to an already-mapped listing updates content and activates nothing. Only treat a listing as not-live when `live` is present and false.
//
// `result.warnings[]` lists steps that failed WITHOUT failing the publish — optional work the push carried on past. They were previously swallowed, so the only sign of one was a listing that was somehow not quite right afterwards. A publish can be `published: true` and still carry warnings; read them before concluding nothing needs doing.
//
// `result.lockedFields` names the fields Airbnb will not let this listing change at all. They are not retryable by anyone: Airbnb answers 200 and applies nothing. `GET /v1/channels/airbnb/listings/{id}` reports the same list up front.
//
// **Which fields this pushes** — title, description sections and house rules (English/primary locale), amenities, rooms and beds, photos, nightly price and fees, cancellation policy and guest controls, check-in/out times, quiet hours, property and room type, checkout tasks. **Not pushed by this endpoint:** non-primary locales (`PUT /v1/channels/airbnb/listings/{id}/descriptions`), guest-safety disclosures (`PUT …/safety-disclosures`), check-in method (`PUT …/details`), permits (`PUT …/permits`), and the calendar (`PUT …/availability`).
//
// `force: true` re-pushes every section, ignoring dirty-field tracking. Without it only the sections changed since the last successful publish are sent.
//
// Send `Idempotency-Key` to make a retry safe: a timeout on a publish otherwise leaves you unable to tell whether it ran.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/publish/airbnb (the `PublishListingToAirbnb` operationId).
func (c *ClientWithResponses) PublishListingToAirbnbWithResponse(ctx context.Context, id int, params *PublishListingToAirbnbParams, body PublishListingToAirbnbJSONRequestBody, reqEditors ...RequestEditorFn) (*PublishListingToAirbnbClientResponse, error) {
	rsp, err := c.PublishListingToAirbnb(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePublishListingToAirbnbClientResponse(rsp)
}

// PublishListingToBookingWithBodyWithResponse Publish a listing to Booking.com
//
// Push a Repull listing's content to Booking.com. The listing must already be mapped to a Booking.com property + room — claim the hotel through the Connect Booking flow, then map its rooms with `POST /v1/connect/booking/map-rooms`.
//
// **Which property the content lands in.** A listing can be mapped to more than one Booking.com property; the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. When the listing has exactly one property you need send nothing. When it has several, name one with `hotelId` in the body (or `?hotel_id=` — the same value, accepted either way, body wins if you send both). Omit it on such a listing and the push is refused with **`409 ambiguous_booking_mapping`**, listing the candidate ids: content pushed into a property chosen for you lands on the wrong listing and reports success, which is worse than a refusal. `GET /v1/channels/booking/properties` lists every property with the listings mapped under it. Naming a property this listing is not mapped to is a `404` that names the ones it is.
//
// The property that actually received the content comes back as `result.hotelId`.
//
// **A publish is not one call to Booking.com.** It is several independent Content API calls — details, description, amenities, rooms, photos, pricing — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Booking.com's own reason, per section, for the ones that did not. A property whose Content API credentials do not cover a section answers 403 for that section alone. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Fix the failing sections and publish again — re-publishing an unchanged section is harmless.
//
// A listing with no Booking.com property mapped at all is not an error: the call returns `result.published: false` with `result.reason` and `result.hotelId: null`, and nothing is pushed.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/publish/booking (the `PublishListingToBooking` operationId).
func (c *ClientWithResponses) PublishListingToBookingWithBodyWithResponse(ctx context.Context, id int, params *PublishListingToBookingParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PublishListingToBookingClientResponse, error) {
	rsp, err := c.PublishListingToBookingWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePublishListingToBookingClientResponse(rsp)
}

// PublishListingToBookingWithResponse Publish a listing to Booking.com
//
// Push a Repull listing's content to Booking.com. The listing must already be mapped to a Booking.com property + room — claim the hotel through the Connect Booking flow, then map its rooms with `POST /v1/connect/booking/map-rooms`.
//
// **Which property the content lands in.** A listing can be mapped to more than one Booking.com property; the same unit re-listed under a new property keeps its old mapping, and workspaces routinely sit on five or six. When the listing has exactly one property you need send nothing. When it has several, name one with `hotelId` in the body (or `?hotel_id=` — the same value, accepted either way, body wins if you send both). Omit it on such a listing and the push is refused with **`409 ambiguous_booking_mapping`**, listing the candidate ids: content pushed into a property chosen for you lands on the wrong listing and reports success, which is worse than a refusal. `GET /v1/channels/booking/properties` lists every property with the listings mapped under it. Naming a property this listing is not mapped to is a `404` that names the ones it is.
//
// The property that actually received the content comes back as `result.hotelId`.
//
// **A publish is not one call to Booking.com.** It is several independent Content API calls — details, description, amenities, rooms, photos, pricing — and each can fail on its own. `result.published` is true only when every attempted section landed; `result.sections` lists the ones that did and `result.errors[]` carries Booking.com's own reason, per section, for the ones that did not. A property whose Content API credentials do not cover a section answers 403 for that section alone. **A partial publish is normal and is not rolled back**: what succeeded stays applied. Fix the failing sections and publish again — re-publishing an unchanged section is harmless.
//
// A listing with no Booking.com property mapped at all is not an error: the call returns `result.published: false` with `result.reason` and `result.hotelId: null`, and nothing is pushed.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/publish/booking (the `PublishListingToBooking` operationId).
func (c *ClientWithResponses) PublishListingToBookingWithResponse(ctx context.Context, id int, params *PublishListingToBookingParams, body PublishListingToBookingJSONRequestBody, reqEditors ...RequestEditorFn) (*PublishListingToBookingClientResponse, error) {
	rsp, err := c.PublishListingToBooking(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePublishListingToBookingClientResponse(rsp)
}

// PullListingFromAirbnbWithBodyWithResponse Refresh a listing from Airbnb
//
// Re-read this listing from Airbnb and update your stored copy, then report what was refreshed and when. The mirror image of `POST /v1/listings/{id}/publish/airbnb`.
//
// Every other Airbnb read on this API is served from our database. This endpoint is the one that goes and asks Airbnb — use it after a push, to see the values Airbnb actually kept, or when a host has changed something in the Airbnb app.
//
// **What it refreshes:** basic listing facts (property type, bedrooms, beds, bathrooms, capacity), descriptions, photos, rooms and beds, amenities, booking settings (check-in/check-out windows, guest controls, cancellation policy), stay rules (min/max nights, advance-booking window, turnover buffer), pricing settings and standard fees, permits, checkout tasks and the check-in guide. After it returns, those values are what `GET /v1/listings/{id}?include=content,details` and the `/v1/channels/airbnb/**` routes serve.
//
// **What it does NOT refresh:** the calendar (nightly rates and availability — see `GET /v1/channels/airbnb/listings/{id}/availability`), reservations, messages, reviews or payouts. Those arrive continuously through the channel's own sync and never need a manual pull.
//
// **Runs synchronously** — the response is the result, not a job id. Expect several seconds.
//
// **One pull per listing per 15 minutes.** A pull is roughly a dozen Airbnb calls; a second call inside the window returns `429 rate_limit_exceeded` with `Retry-After` and `nextPullAvailableAt`, and makes no Airbnb calls. Two simultaneous calls cannot both run.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/pull/airbnb (the `PullListingFromAirbnb` operationId).
func (c *ClientWithResponses) PullListingFromAirbnbWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PullListingFromAirbnbClientResponse, error) {
	rsp, err := c.PullListingFromAirbnbWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePullListingFromAirbnbClientResponse(rsp)
}

// PullListingFromAirbnbWithResponse Refresh a listing from Airbnb
//
// Re-read this listing from Airbnb and update your stored copy, then report what was refreshed and when. The mirror image of `POST /v1/listings/{id}/publish/airbnb`.
//
// Every other Airbnb read on this API is served from our database. This endpoint is the one that goes and asks Airbnb — use it after a push, to see the values Airbnb actually kept, or when a host has changed something in the Airbnb app.
//
// **What it refreshes:** basic listing facts (property type, bedrooms, beds, bathrooms, capacity), descriptions, photos, rooms and beds, amenities, booking settings (check-in/check-out windows, guest controls, cancellation policy), stay rules (min/max nights, advance-booking window, turnover buffer), pricing settings and standard fees, permits, checkout tasks and the check-in guide. After it returns, those values are what `GET /v1/listings/{id}?include=content,details` and the `/v1/channels/airbnb/**` routes serve.
//
// **What it does NOT refresh:** the calendar (nightly rates and availability — see `GET /v1/channels/airbnb/listings/{id}/availability`), reservations, messages, reviews or payouts. Those arrive continuously through the channel's own sync and never need a manual pull.
//
// **Runs synchronously** — the response is the result, not a job id. Expect several seconds.
//
// **One pull per listing per 15 minutes.** A pull is roughly a dozen Airbnb calls; a second call inside the window returns `429 rate_limit_exceeded` with `Retry-After` and `nextPullAvailableAt`, and makes no Airbnb calls. Two simultaneous calls cannot both run.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/listings/{id}/pull/airbnb (the `PullListingFromAirbnb` operationId).
func (c *ClientWithResponses) PullListingFromAirbnbWithResponse(ctx context.Context, id int, body PullListingFromAirbnbJSONRequestBody, reqEditors ...RequestEditorFn) (*PullListingFromAirbnbClientResponse, error) {
	rsp, err := c.PullListingFromAirbnb(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePullListingFromAirbnbClientResponse(rsp)
}

// GetListingSegmentsWithResponse Atlas DNA segment intelligence for a listing
//
// Aggregates Atlas DNA segment signal (quality tier, design style, bedrooms) across the listing's geographic neighborhood (default: 5km radius) or the whole city, so consumers can answer:
// - What segments dominate my market?
// - Which segment does my listing match best?
// - What's the ADR uplift for moving up a tier?
//
// DNA coverage is still ramping — segments are scored asynchronously. Cities and radii without scored comps return `totalCompsAnalyzed: 0` plus a `low_dna_coverage` recommendation rather than fabricated data.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/listings/{id}/segments (the `GetListingSegments` operationId).
func (c *ClientWithResponses) GetListingSegmentsWithResponse(ctx context.Context, id int, params *GetListingSegmentsParams, reqEditors ...RequestEditorFn) (*GetListingSegmentsClientResponse, error) {
	rsp, err := c.GetListingSegments(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetListingSegmentsClientResponse(rsp)
}

// ListMarketsWithResponse List markets the customer operates in
//
// Returns per-city KPIs across every market the authenticated customer has listings in (market share, ADR vs market, occupancy, ratings) plus a lightweight `browse` discovery summary (top-50 featured markets, country categories, total catalog size). For the full paginated discovery catalog with search, call `GET /v1/markets/browse`. Each `markets[]` entry is enriched with `subscribed` + `source` from the customer's market subscriptions.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/markets (the `ListMarkets` operationId).
func (c *ClientWithResponses) ListMarketsWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListMarketsClientResponse, error) {
	rsp, err := c.ListMarkets(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListMarketsClientResponse(rsp)
}

// ListMarketBrowseWithResponse Paginated discovery catalog
//
// Cursor-paginated, search-filterable catalog of every Atlas-tracked market the customer could expand into. Backed by the precomputed `market_summaries` table (>=5 active comps per city). Supports fuzzy `q` substring search (trigram-indexed), `country` (ISO 3166-1 alpha-2) filter, and `sort` (`listings_desc` | `name_asc`). Use the `nextCursor` from `pagination` to walk pages — the cursor is an opaque base64 token; do not parse it.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// `pagination.total` is the count of markets matching the current `q`/`country`/`min_listings` filter (across all pages) — same shape as every other list endpoint.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/markets/browse (the `ListMarketBrowse` operationId).
func (c *ClientWithResponses) ListMarketBrowseWithResponse(ctx context.Context, params *ListMarketBrowseParams, reqEditors ...RequestEditorFn) (*ListMarketBrowseClientResponse, error) {
	rsp, err := c.ListMarketBrowse(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListMarketBrowseClientResponse(rsp)
}

// GetMarketWithResponse Deep-dive on a single market
//
// Detailed market view for one city — price distribution, bedroom mix, property types, upcoming events, Wheelhouse demand, monthly benchmarks, customer health rollup, top comps (proximity-sorted, paginated), customer's percentile position, capacity-mix gap, and a 6-month supply trend.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/markets/{city} (the `GetMarket` operationId).
func (c *ClientWithResponses) GetMarketWithResponse(ctx context.Context, city string, params *GetMarketParams, reqEditors ...RequestEditorFn) (*GetMarketClientResponse, error) {
	rsp, err := c.GetMarket(ctx, city, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetMarketClientResponse(rsp)
}

// GetMarketCalendarWithResponse Calendar-level market view
//
// Date-by-date market view for a city — market avg / min / max nightly rate, occupancy %, Wheelhouse demand, events touching the date, and (when `listingId` is supplied) an overlay of the customer's own pricing + availability.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/markets/{city}/calendar (the `GetMarketCalendar` operationId).
func (c *ClientWithResponses) GetMarketCalendarWithResponse(ctx context.Context, city string, params *GetMarketCalendarParams, reqEditors ...RequestEditorFn) (*GetMarketCalendarClientResponse, error) {
	rsp, err := c.GetMarketCalendar(ctx, city, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetMarketCalendarClientResponse(rsp)
}

// ListMigrationsWithResponse List migrations
//
// Every property manager your workspace has moved through Repull Migrate, newest first, each with where it stands. Page with `pagination.nextCursor`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/migrations (the `ListMigrations` operationId).
func (c *ClientWithResponses) ListMigrationsWithResponse(ctx context.Context, params *ListMigrationsParams, reqEditors ...RequestEditorFn) (*ListMigrationsClientResponse, error) {
	rsp, err := c.ListMigrations(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListMigrationsClientResponse(rsp)
}

// DeleteMigrationWithResponse End a migration
//
// Disconnects the source (it stops syncing) and deactivates the migration's workspace. The imported data is kept and stays readable.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/migrations/{workspaceId} (the `DeleteMigration` operationId).
func (c *ClientWithResponses) DeleteMigrationWithResponse(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*DeleteMigrationClientResponse, error) {
	rsp, err := c.DeleteMigration(ctx, workspaceId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteMigrationClientResponse(rsp)
}

// GetMigrationWithResponse Get a migration
//
// Where one migration stands: its state, the source connections with their last import run, and how much data has landed.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/migrations/{workspaceId} (the `GetMigration` operationId).
func (c *ClientWithResponses) GetMigrationWithResponse(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*GetMigrationClientResponse, error) {
	rsp, err := c.GetMigration(ctx, workspaceId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetMigrationClientResponse(rsp)
}

// GetMigrationChannelMapWithResponse Get channel links
//
// The Airbnb, Booking.com and VRBO listing each migrated property is linked to in the source PMS, read live. Use it to link each listing to the right property when the channels are reconnected in the destination — channel connections themselves cannot be moved between PMSs.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/migrations/{workspaceId}/channel-map (the `GetMigrationChannelMap` operationId).
func (c *ClientWithResponses) GetMigrationChannelMapWithResponse(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*GetMigrationChannelMapClientResponse, error) {
	rsp, err := c.GetMigrationChannelMap(ctx, workspaceId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetMigrationChannelMapClientResponse(rsp)
}

// CutoverMigrationWithResponse Cut over
//
// The property manager has switched: disconnect the source so it stops syncing. The imported data stays readable. Idempotent.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/migrations/{workspaceId}/cutover (the `CutoverMigration` operationId).
func (c *ClientWithResponses) CutoverMigrationWithResponse(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*CutoverMigrationClientResponse, error) {
	rsp, err := c.CutoverMigration(ctx, workspaceId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCutoverMigrationClientResponse(rsp)
}

// CheckMigrationCutoverWithBodyWithResponse Check reservations before switching
//
// Send every upcoming reservation the destination holds; get back what is missing, extra, or on different dates compared with the source. Read-only — run it as often as you like before cutover.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/migrations/{workspaceId}/cutover-check (the `CheckMigrationCutover` operationId).
func (c *ClientWithResponses) CheckMigrationCutoverWithBodyWithResponse(ctx context.Context, workspaceId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CheckMigrationCutoverClientResponse, error) {
	rsp, err := c.CheckMigrationCutoverWithBody(ctx, workspaceId, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCheckMigrationCutoverClientResponse(rsp)
}

// CheckMigrationCutoverWithResponse Check reservations before switching
//
// Send every upcoming reservation the destination holds; get back what is missing, extra, or on different dates compared with the source. Read-only — run it as often as you like before cutover.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/migrations/{workspaceId}/cutover-check (the `CheckMigrationCutover` operationId).
func (c *ClientWithResponses) CheckMigrationCutoverWithResponse(ctx context.Context, workspaceId int, body CheckMigrationCutoverJSONRequestBody, reqEditors ...RequestEditorFn) (*CheckMigrationCutoverClientResponse, error) {
	rsp, err := c.CheckMigrationCutover(ctx, workspaceId, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCheckMigrationCutoverClientResponse(rsp)
}

// RunMigrationImportWithBodyWithResponse Run the import again
//
// Queue another import from the source PMS — for example messages after the first pass, or only reservations changed since a date. Progress shows on `GET /v1/migrations/{workspaceId}`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/migrations/{workspaceId}/import (the `RunMigrationImport` operationId).
func (c *ClientWithResponses) RunMigrationImportWithBodyWithResponse(ctx context.Context, workspaceId int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*RunMigrationImportClientResponse, error) {
	rsp, err := c.RunMigrationImportWithBody(ctx, workspaceId, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRunMigrationImportClientResponse(rsp)
}

// RunMigrationImportWithResponse Run the import again
//
// Queue another import from the source PMS — for example messages after the first pass, or only reservations changed since a date. Progress shows on `GET /v1/migrations/{workspaceId}`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/migrations/{workspaceId}/import (the `RunMigrationImport` operationId).
func (c *ClientWithResponses) RunMigrationImportWithResponse(ctx context.Context, workspaceId int, body RunMigrationImportJSONRequestBody, reqEditors ...RequestEditorFn) (*RunMigrationImportClientResponse, error) {
	rsp, err := c.RunMigrationImport(ctx, workspaceId, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRunMigrationImportClientResponse(rsp)
}

// GetMigrationReportWithResponse Get a migration report
//
// What came across and what needs a decision in the destination: properties without an address, upcoming reservations with no guest contact, channel reservations that must not be re-created, and anything the source PMS cannot carry. Includes the source's capability matrix.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/migrations/{workspaceId}/report (the `GetMigrationReport` operationId).
func (c *ClientWithResponses) GetMigrationReportWithResponse(ctx context.Context, workspaceId int, reqEditors ...RequestEditorFn) (*GetMigrationReportClientResponse, error) {
	rsp, err := c.GetMigrationReport(ctx, workspaceId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetMigrationReportClientResponse(rsp)
}

// ListPropertiesWithResponse List properties
//
// Cursor-paginated list of properties for the authenticated workspace. Walk pages with `?cursor=<pagination.nextCursor>`; stop when `pagination.hasMore` is `false`. Cursor is opaque base64 — do not parse it.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// Filters: `q` (substring on name/street/city), `status` (active|inactive|all), `lifecycle_status` (exact match on the listing's lifecycle state). Other unknown params (e.g. `?search=` or `?propertyId=`) are rejected with 422 — no silent unfiltered results.
//
// **Incremental sync (only changes since last poll):** pass `?updated_since=<ISO8601>` to receive only properties changed at or after that instant. Each property carries `updatedAt` — the last row of the final page is your next watermark. `updated_since` changes the page ordering to `updatedAt ASC, id ASC` (and the cursor with it); see the parameter description. `GET /v1/listings` does NOT yet accept `updated_since` — use this endpoint for property-side incremental sync.
//
// **Inactive properties:** an inactive property keeps syncing, but cannot be read or changed through the API until it is activated. They are only listed when `status` asks for them, and then with `id`, `name`, `status`, `lifecycleStatus`, `channels` and `updatedAt` only — enough to choose what to activate with `PATCH /v1/listings/{id}`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/properties (the `ListProperties` operationId).
func (c *ClientWithResponses) ListPropertiesWithResponse(ctx context.Context, params *ListPropertiesParams, reqEditors ...RequestEditorFn) (*ListPropertiesClientResponse, error) {
	rsp, err := c.ListProperties(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListPropertiesClientResponse(rsp)
}

// GetPropertyWithResponse Get property details
//
// Fetch a single property by Repull id. Property ids are workspace-scoped — an id from one workspace is not valid in another. 404 means the id does not exist OR belongs to a different workspace.
//
// **Optional expansions:** Pass `?include=amenities` to enrich the response with the property's amenities (sourced from the unified `listings_amenities` table). Returns `[]` when the property has no amenity rows. The default response stays lean; consumers must opt in.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/properties/{id} (the `GetProperty` operationId).
func (c *ClientWithResponses) GetPropertyWithResponse(ctx context.Context, id int, params *GetPropertyParams, reqEditors ...RequestEditorFn) (*GetPropertyClientResponse, error) {
	rsp, err := c.GetProperty(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetPropertyClientResponse(rsp)
}

// GetQuoteWithResponse Price a stay
//
// Returns the full price breakdown for a stay — nightly total, length-of-stay discount, cleaning fee, pet and other fees, taxes, and the total.
//
// A quote is priced against a booking website, because the markup, custom fees and tax overrides that decide what a guest is actually charged live there. A workspace with no booking site receives `422 quote_unavailable` rather than a number computed from different rules than the ones applied at checkout.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/quotes (the `GetQuote` operationId).
func (c *ClientWithResponses) GetQuoteWithResponse(ctx context.Context, params *GetQuoteParams, reqEditors ...RequestEditorFn) (*GetQuoteClientResponse, error) {
	rsp, err := c.GetQuote(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetQuoteClientResponse(rsp)
}

// ListReservationsWithResponse List reservations
//
// Cursor-paginated list of reservations across all connected PMS platforms. Filter by platform, status, listing, or check-in date range.
//
// **Pagination:** Walk pages with `?cursor=` — pass `pagination.nextCursor` from one response back as `?cursor=` on the next request. Stop when `pagination.hasMore` is `false`. `limit` defaults to 50, max 100; requesting more returns 422 (no silent truncation).
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`. For deep pagination cursor remains O(1) per page; offset > 10000 returns 422 with a docs link.
//
// **Incremental sync (only changes since last poll):** pass `?updated_since=<ISO8601>` to receive only reservations amended, cancelled, or created at or after that instant — no full re-walk. Each row carries `updatedAt`; the last row of the final page is your next watermark. Note that `updated_since` changes the page ordering to `updatedAt ASC, id ASC` (and the cursor with it) so mid-walk amendments cannot be skipped — see the parameter description for the full contract.
//
// Reservations on inactive listings are left out (counts and cursors included); they keep syncing and reappear once the listing is activated. Filtering by an inactive listing (`listing_id`) returns `403 listing_inactive`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/reservations (the `ListReservations` operationId).
func (c *ClientWithResponses) ListReservationsWithResponse(ctx context.Context, params *ListReservationsParams, reqEditors ...RequestEditorFn) (*ListReservationsClientResponse, error) {
	rsp, err := c.ListReservations(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListReservationsClientResponse(rsp)
}

// CreateReservationWithBodyWithResponse Create a reservation
//
// Creates a reservation and everything that hangs off one: the guest, the conversation thread, the dashboard item, the calendar block, and the `reservation.created` fan-out that issues the door code and starts the messaging automations.
//
// **Platform is restricted to `direct`, `website` and `owner`.** Reservations on Airbnb, Booking.com and Vrbo are owned by the channel and arrive through sync — creating one here would mint a local booking the channel has never heard of, which then fights the next sync. Create those on the channel.
//
// **Dates are validated** (`YYYY-MM-DD`, and `checkOut` must be after `checkIn`), and an unrecognised field is rejected by name rather than silently ignored.
//
// **This endpoint does not set the price.** There is no `totalPrice` field: the reservation pipeline derives the price breakdown from the property's own rates and overwrites anything supplied, so accepting a total would be taking a value and discarding it. A reservation created here is priced by that engine (`0` when the property has no rates for the range). `currency` IS honoured. Quote a stay with `GET /v1/quotes` before booking if you need the figure up front.
//
// **Availability is NOT checked.** This creates the reservation you asked for even if the dates overlap an existing booking. Call `GET /v1/availability/{propertyId}` first if that matters.
//
// Send `Idempotency-Key` — a network timeout here is exactly the case it exists for: without it, a retry books the guest twice.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/reservations (the `CreateReservation` operationId).
func (c *ClientWithResponses) CreateReservationWithBodyWithResponse(ctx context.Context, params *CreateReservationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateReservationClientResponse, error) {
	rsp, err := c.CreateReservationWithBody(ctx, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateReservationClientResponse(rsp)
}

// CreateReservationWithResponse Create a reservation
//
// Creates a reservation and everything that hangs off one: the guest, the conversation thread, the dashboard item, the calendar block, and the `reservation.created` fan-out that issues the door code and starts the messaging automations.
//
// **Platform is restricted to `direct`, `website` and `owner`.** Reservations on Airbnb, Booking.com and Vrbo are owned by the channel and arrive through sync — creating one here would mint a local booking the channel has never heard of, which then fights the next sync. Create those on the channel.
//
// **Dates are validated** (`YYYY-MM-DD`, and `checkOut` must be after `checkIn`), and an unrecognised field is rejected by name rather than silently ignored.
//
// **This endpoint does not set the price.** There is no `totalPrice` field: the reservation pipeline derives the price breakdown from the property's own rates and overwrites anything supplied, so accepting a total would be taking a value and discarding it. A reservation created here is priced by that engine (`0` when the property has no rates for the range). `currency` IS honoured. Quote a stay with `GET /v1/quotes` before booking if you need the figure up front.
//
// **Availability is NOT checked.** This creates the reservation you asked for even if the dates overlap an existing booking. Call `GET /v1/availability/{propertyId}` first if that matters.
//
// Send `Idempotency-Key` — a network timeout here is exactly the case it exists for: without it, a retry books the guest twice.
//
// Returns `403 listing_inactive` when the listing is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/reservations (the `CreateReservation` operationId).
func (c *ClientWithResponses) CreateReservationWithResponse(ctx context.Context, params *CreateReservationParams, body CreateReservationJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateReservationClientResponse, error) {
	rsp, err := c.CreateReservation(ctx, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateReservationClientResponse(rsp)
}

// GetReservationWithResponse Get reservation details
//
// Returns the full record for a single reservation, scoped to the authenticated workspace. Response shape is identical to a single row in `GET /v1/reservations` so SDK consumers can use the same type for both. Returns **404** if the id does not exist OR belongs to a different workspace — the API never differentiates the two so caller can't enumerate other workspaces' ids.
//
// Returns `403 listing_inactive` when the listing this resolves to is inactive. An inactive listing keeps syncing, but cannot be read or changed through the API until it is activated.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/reservations/{id} (the `GetReservation` operationId).
func (c *ClientWithResponses) GetReservationWithResponse(ctx context.Context, id int, params *GetReservationParams, reqEditors ...RequestEditorFn) (*GetReservationClientResponse, error) {
	rsp, err := c.GetReservation(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetReservationClientResponse(rsp)
}

// UpdateReservationWithBodyWithResponse Update a reservation
//
// Changes the dates, the occupancy, or the unit. Drives the same command path the dashboard does, so the side effects come with it: the change audit is appended, bound task due dates re-sync, the old calendar dates unblock and the new ones block, the conversation's cached listing is invalidated, and `reservation.updated` fires — which is what revokes and re-issues the door code.
//
// Supply at least one field; an empty body returns 422 rather than a 200 that changed nothing.
//
// **Moving and re-dating in one call is one operation.** Send `listingId` together with `checkIn`/`checkOut` and it is applied as a single move, so the access code is re-issued once rather than twice.
//
// ### Fields this endpoint deliberately does NOT accept
//
// Each is rejected by name with the reason, never accepted and ignored:
//
// | Field | Why |
// |---|---|
// | `guest` / `guestDetails` | Guest name, email and phone live on the guest record. The underlying command has no branch for them, so accepting them would return a success that changed nothing. |
// | `pricing` / `totalPrice` / `currency` | Repricing writes the price breakdown, the pricing row and a pricing-history entry. It belongs to its own endpoint. |
// | `status` | Not a field. Cancelling, confirming and checking out are separate operations with materially different side effects — cancellation issues a credit refund and revokes access codes. |
// | `platform` | Immutable: it records where the booking actually originated. |
// | `notes` | `internal_notes` is an append-only audit trail the system writes on every change. |
//
// **Availability is NOT checked.** A date change that overlaps another booking will be written. Call `GET /v1/availability/{propertyId}` first if that matters.
//
// Returns `403 listing_inactive` when the reservation is on an inactive listing, or when a `listingId` move targets one; nothing is changed.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/reservations/{id} (the `UpdateReservation` operationId).
func (c *ClientWithResponses) UpdateReservationWithBodyWithResponse(ctx context.Context, id int, params *UpdateReservationParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateReservationClientResponse, error) {
	rsp, err := c.UpdateReservationWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateReservationClientResponse(rsp)
}

// UpdateReservationWithResponse Update a reservation
//
// Changes the dates, the occupancy, or the unit. Drives the same command path the dashboard does, so the side effects come with it: the change audit is appended, bound task due dates re-sync, the old calendar dates unblock and the new ones block, the conversation's cached listing is invalidated, and `reservation.updated` fires — which is what revokes and re-issues the door code.
//
// Supply at least one field; an empty body returns 422 rather than a 200 that changed nothing.
//
// **Moving and re-dating in one call is one operation.** Send `listingId` together with `checkIn`/`checkOut` and it is applied as a single move, so the access code is re-issued once rather than twice.
//
// ### Fields this endpoint deliberately does NOT accept
//
// Each is rejected by name with the reason, never accepted and ignored:
//
// | Field | Why |
// |---|---|
// | `guest` / `guestDetails` | Guest name, email and phone live on the guest record. The underlying command has no branch for them, so accepting them would return a success that changed nothing. |
// | `pricing` / `totalPrice` / `currency` | Repricing writes the price breakdown, the pricing row and a pricing-history entry. It belongs to its own endpoint. |
// | `status` | Not a field. Cancelling, confirming and checking out are separate operations with materially different side effects — cancellation issues a credit refund and revokes access codes. |
// | `platform` | Immutable: it records where the booking actually originated. |
// | `notes` | `internal_notes` is an append-only audit trail the system writes on every change. |
//
// **Availability is NOT checked.** A date change that overlaps another booking will be written. Call `GET /v1/availability/{propertyId}` first if that matters.
//
// Returns `403 listing_inactive` when the reservation is on an inactive listing, or when a `listingId` move targets one; nothing is changed.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/reservations/{id} (the `UpdateReservation` operationId).
func (c *ClientWithResponses) UpdateReservationWithResponse(ctx context.Context, id int, params *UpdateReservationParams, body UpdateReservationJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateReservationClientResponse, error) {
	rsp, err := c.UpdateReservation(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateReservationClientResponse(rsp)
}

// AcceptReservationRequestWithResponse Accept a booking request
//
// Accept a pending Airbnb booking request — a reservation with status `pending`, made on a listing without Instant Book. Find them with `GET /v1/reservations?status=pending`. Airbnb expires a request the host has not answered within 24 hours.
//
// Airbnb confirms asynchronously: the reservation’s status moves to confirmed, and a `reservation.updated` webhook fires, when Airbnb’s notification lands (usually within seconds). The response reports what Airbnb was asked to do.
//
// **Airbnb only**, and only for listings connected to Airbnb directly: other channels have no request step (`422 channel_not_supported`). A reservation that is not pending is refused before Airbnb is contacted (`409 reservation_not_pending`); one Airbnb says already moved on is `409 request_no_longer_pending`. Neither is worth retrying.
//
// Takes no body.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/reservations/{id}/accept (the `AcceptReservationRequest` operationId).
func (c *ClientWithResponses) AcceptReservationRequestWithResponse(ctx context.Context, id int, params *AcceptReservationRequestParams, reqEditors ...RequestEditorFn) (*AcceptReservationRequestClientResponse, error) {
	rsp, err := c.AcceptReservationRequest(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseAcceptReservationRequestClientResponse(rsp)
}

// DeclineReservationRequestWithBodyWithResponse Decline a booking request
//
// Decline a pending Airbnb booking request (a reservation with status `pending`; find them with `GET /v1/reservations?status=pending`).
//
// `reason` must be one of Airbnb’s own decline reasons. `message` is required: Airbnb sends it to the guest with the decline (at most 500 characters). It is not defaulted — a canned message would put words in your mouth.
//
// Airbnb confirms asynchronously; the reservation’s status moves, and `reservation.updated` fires, when its notification lands. Same channel and status rules as `POST /v1/reservations/{id}/accept`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/reservations/{id}/decline (the `DeclineReservationRequest` operationId).
func (c *ClientWithResponses) DeclineReservationRequestWithBodyWithResponse(ctx context.Context, id int, params *DeclineReservationRequestParams, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*DeclineReservationRequestClientResponse, error) {
	rsp, err := c.DeclineReservationRequestWithBody(ctx, id, params, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeclineReservationRequestClientResponse(rsp)
}

// DeclineReservationRequestWithResponse Decline a booking request
//
// Decline a pending Airbnb booking request (a reservation with status `pending`; find them with `GET /v1/reservations?status=pending`).
//
// `reason` must be one of Airbnb’s own decline reasons. `message` is required: Airbnb sends it to the guest with the decline (at most 500 characters). It is not defaulted — a canned message would put words in your mouth.
//
// Airbnb confirms asynchronously; the reservation’s status moves, and `reservation.updated` fires, when its notification lands. Same channel and status rules as `POST /v1/reservations/{id}/accept`.
//
// Send `Idempotency-Key`: a repeat with the same key replays the first response instead of acting twice (a `409 idempotency_key_in_use` while the first is still running). A 5xx, a `429 airbnb_rate_limited` or a `403 connection_reauth_required` is not stored — nothing was done — so retrying with the same key reaches Airbnb again.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/reservations/{id}/decline (the `DeclineReservationRequest` operationId).
func (c *ClientWithResponses) DeclineReservationRequestWithResponse(ctx context.Context, id int, params *DeclineReservationRequestParams, body DeclineReservationRequestJSONRequestBody, reqEditors ...RequestEditorFn) (*DeclineReservationRequestClientResponse, error) {
	rsp, err := c.DeclineReservationRequest(ctx, id, params, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeclineReservationRequestClientResponse(rsp)
}

// ListReviewsWithResponse List reviews
//
// Cursor-paginated guest + host review stream for the workspace. Backed by main vanio's unified `reviews` table (populated by per-channel backfill crons), so this surface returns the complete cross-channel history — separate from `/v1/channels/airbnb/reviews` which hits Airbnb live.
//
// `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — see the `offset` parameter below. Mutually exclusive with `cursor`.
//
// Filters: `platform` (`airbnb`|`booking`|`vrbo`), `listing_id` (internal Repull listing id), `rating_min` / `rating_max` (inclusive bounds, 0..5), `status` (`responded`|`unanswered`|`all`), `reviewer_role` (`guest` (default) | `host` | `all`).
//
// **Inactive listings:** reviews of inactive listings are left out of the page and of `pagination.total`. Filtering by an inactive `listing_id` returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/reviews (the `ListReviews` operationId).
func (c *ClientWithResponses) ListReviewsWithResponse(ctx context.Context, params *ListReviewsParams, reqEditors ...RequestEditorFn) (*ListReviewsClientResponse, error) {
	rsp, err := c.ListReviews(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListReviewsClientResponse(rsp)
}

// GetReviewWithResponse Get review
//
// Returns one review (the bare `Review` object — NOT wrapped in `{ data: ... }`). Scoped to the authenticated workspace via the listings join — reviews that don't belong to the workspace return 404 (we don't differentiate to avoid leaking other customers' ids).
//
// A review of an inactive listing returns `403 listing_inactive`. Inactive listings keep syncing; activate the listing to use it here.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/reviews/{id} (the `GetReview` operationId).
func (c *ClientWithResponses) GetReviewWithResponse(ctx context.Context, id int, params *GetReviewParams, reqEditors ...RequestEditorFn) (*GetReviewClientResponse, error) {
	rsp, err := c.GetReview(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetReviewClientResponse(rsp)
}

// ReplyToReviewWithBodyWithResponse Reply to a review on any channel
//
// Resolves the review, reads its channel and dispatches the reply. Channel-neutral: you do not need to know where the review came from.
//
// Replies are available on Airbnb today; a review from a channel without a reply API returns `422 unsupported_channel` naming the channels that do work.
//
// **Inactive listings:** a review of an inactive listing returns `403 listing_inactive` and no reply reaches the channel. Activate the listing first.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/reviews/{id}/reply (the `ReplyToReview` operationId).
func (c *ClientWithResponses) ReplyToReviewWithBodyWithResponse(ctx context.Context, id int, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReplyToReviewClientResponse, error) {
	rsp, err := c.ReplyToReviewWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReplyToReviewClientResponse(rsp)
}

// ReplyToReviewWithResponse Reply to a review on any channel
//
// Resolves the review, reads its channel and dispatches the reply. Channel-neutral: you do not need to know where the review came from.
//
// Replies are available on Airbnb today; a review from a channel without a reply API returns `422 unsupported_channel` naming the channels that do work.
//
// **Inactive listings:** a review of an inactive listing returns `403 listing_inactive` and no reply reaches the channel. Activate the listing first.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/reviews/{id}/reply (the `ReplyToReview` operationId).
func (c *ClientWithResponses) ReplyToReviewWithResponse(ctx context.Context, id int, body ReplyToReviewJSONRequestBody, reqEditors ...RequestEditorFn) (*ReplyToReviewClientResponse, error) {
	rsp, err := c.ReplyToReview(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReplyToReviewClientResponse(rsp)
}

// ListCustomSchemasWithResponse List custom schemas
//
// Returns every custom schema owned by the workspace, including inactive ones.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/schema/custom (the `ListCustomSchemas` operationId).
func (c *ClientWithResponses) ListCustomSchemasWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListCustomSchemasClientResponse, error) {
	rsp, err := c.ListCustomSchemas(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListCustomSchemasClientResponse(rsp)
}

// CreateCustomSchemaWithBodyWithResponse Create a custom schema
//
// Create a workspace-scoped field-mapping schema. The schema reshapes the `native` response payload into your app's preferred field names. After creation, set `X-Schema: <name>` on any read endpoint to apply it.
//
// **Reserved names:** `calry`, `calry-v1`, `native` are built-in schemas and cannot be used as a custom name.
//
// **Mapping safety:** Each mapping value is parsed by an internal expression engine — `eval`, `Function`, `process`, and other unsafe keywords are rejected up front. Field names are capped at 100 chars and expressions at 500 chars.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/schema/custom (the `CreateCustomSchema` operationId).
func (c *ClientWithResponses) CreateCustomSchemaWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateCustomSchemaClientResponse, error) {
	rsp, err := c.CreateCustomSchemaWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateCustomSchemaClientResponse(rsp)
}

// CreateCustomSchemaWithResponse Create a custom schema
//
// Create a workspace-scoped field-mapping schema. The schema reshapes the `native` response payload into your app's preferred field names. After creation, set `X-Schema: <name>` on any read endpoint to apply it.
//
// **Reserved names:** `calry`, `calry-v1`, `native` are built-in schemas and cannot be used as a custom name.
//
// **Mapping safety:** Each mapping value is parsed by an internal expression engine — `eval`, `Function`, `process`, and other unsafe keywords are rejected up front. Field names are capped at 100 chars and expressions at 500 chars.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/schema/custom (the `CreateCustomSchema` operationId).
func (c *ClientWithResponses) CreateCustomSchemaWithResponse(ctx context.Context, body CreateCustomSchemaJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateCustomSchemaClientResponse, error) {
	rsp, err := c.CreateCustomSchema(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateCustomSchemaClientResponse(rsp)
}

// DeleteCustomSchemaWithResponse Delete a custom schema
//
// Hard-delete a custom schema. Subsequent requests carrying its name in `X-Schema` fall back to `native`. There is no undelete.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/schema/custom/{id} (the `DeleteCustomSchema` operationId).
func (c *ClientWithResponses) DeleteCustomSchemaWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*DeleteCustomSchemaClientResponse, error) {
	rsp, err := c.DeleteCustomSchema(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteCustomSchemaClientResponse(rsp)
}

// GetCustomSchemaWithResponse Get a custom schema
//
// Fetch a single custom schema by id. Scoped to the authenticated workspace — schemas that belong to other workspaces return 404.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/schema/custom/{id} (the `GetCustomSchema` operationId).
func (c *ClientWithResponses) GetCustomSchemaWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*GetCustomSchemaClientResponse, error) {
	rsp, err := c.GetCustomSchema(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetCustomSchemaClientResponse(rsp)
}

// UpdateCustomSchemaWithBodyWithResponse Update a custom schema
//
// Patch the description, mappings, or active flag of a custom schema. The schema `name` is immutable — create a new schema and migrate consumers if you need to rename. Mapping updates are revalidated for safety.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/schema/custom/{id} (the `UpdateCustomSchema` operationId).
func (c *ClientWithResponses) UpdateCustomSchemaWithBodyWithResponse(ctx context.Context, id openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateCustomSchemaClientResponse, error) {
	rsp, err := c.UpdateCustomSchemaWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateCustomSchemaClientResponse(rsp)
}

// UpdateCustomSchemaWithResponse Update a custom schema
//
// Patch the description, mappings, or active flag of a custom schema. The schema `name` is immutable — create a new schema and migrate consumers if you need to rename. Mapping updates are revalidated for safety.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/schema/custom/{id} (the `UpdateCustomSchema` operationId).
func (c *ClientWithResponses) UpdateCustomSchemaWithResponse(ctx context.Context, id openapi_types.UUID, body UpdateCustomSchemaJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateCustomSchemaClientResponse, error) {
	rsp, err := c.UpdateCustomSchema(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateCustomSchemaClientResponse(rsp)
}

// GetUsageLogsWithResponse List API request logs
//
// Cursor-paginated raw API request log for the authenticated workspace, newest first. Filter by time `range`, `operation` id(s), status class, or free-text `q`. Walk pages with `cursor` from `pagination.nextCursor` until `pagination.hasMore` is `false`; `offset` is accepted as a shallow alias (deep walks must use `cursor`).
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/usage/logs (the `GetUsageLogs` operationId).
func (c *ClientWithResponses) GetUsageLogsWithResponse(ctx context.Context, params *GetUsageLogsParams, reqEditors ...RequestEditorFn) (*GetUsageLogsClientResponse, error) {
	rsp, err := c.GetUsageLogs(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetUsageLogsClientResponse(rsp)
}

// GetUsageSummaryWithResponse Get usage summary
//
// Aggregated usage over the requested `range` — tier + plan limits, quota used/remaining, next reset, a per-operation breakdown (request/error counts, error rate, avg latency), a daily timeline, status-class distribution, and range totals. Two request quotas are reported and they reset at different times: `dailyRequests` is the daily circuit breaker that stops runaway client loops (resets at `dailyResetsAt`, the next UTC midnight) and `monthlyRequests` is the billing quota (resets at `resetsAt`). `null` limits mean unlimited on that dimension.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/usage/summary (the `GetUsageSummary` operationId).
func (c *ClientWithResponses) GetUsageSummaryWithResponse(ctx context.Context, params *GetUsageSummaryParams, reqEditors ...RequestEditorFn) (*GetUsageSummaryClientResponse, error) {
	rsp, err := c.GetUsageSummary(ctx, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetUsageSummaryClientResponse(rsp)
}

// GetUsageTierWithResponse Get tier and quota
//
// Lightweight current-tier snapshot for status badges and quota meters — plan limits (daily requests, monthly requests, daily AI requests, dynamic-pricing listings), the amount used, the amount remaining, and the next reset. Two request quotas are reported and they reset at different times: `dailyRequests` is the daily circuit breaker that stops runaway client loops (resets at `dailyResetsAt`, the next UTC midnight) and `monthlyRequests` is the billing quota (resets at `resetsAt`). Exceeding the daily cap returns 429 `daily_limit_exceeded`; exceeding the monthly one returns 429 `rate_limit_exceeded`. `null` limits mean unlimited on that dimension.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/usage/tier (the `GetUsageTier` operationId).
func (c *ClientWithResponses) GetUsageTierWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*GetUsageTierClientResponse, error) {
	rsp, err := c.GetUsageTier(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetUsageTierClientResponse(rsp)
}

// ListWebhooksWithResponse List webhook subscriptions
//
// List every webhook subscription registered for this workspace. Each row includes the destination URL, subscribed event types, and the most recent delivery summary.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/webhooks (the `ListWebhooks` operationId).
func (c *ClientWithResponses) ListWebhooksWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListWebhooksClientResponse, error) {
	rsp, err := c.ListWebhooks(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListWebhooksClientResponse(rsp)
}

// CreateWebhookWithBodyWithResponse Create webhook subscription
//
// Register a new endpoint. Returns the plaintext signing secret ONCE — capture it from the response and store it securely. After this call the secret is masked everywhere; mint a new one with `POST /v1/webhooks/{id}/rotate-secret` if you lose it. See `GET /v1/webhooks/event-types` for the full list of subscribable events. Events about an inactive listing (reservations, messages, alterations, reviews, payments, calendar and listing events) are not delivered. The data keeps syncing while the listing is inactive, but its events are never sent — including after you reactivate it; webhooks resume for events that happen from reactivation on. Account-level events are always delivered.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/webhooks (the `CreateWebhook` operationId).
func (c *ClientWithResponses) CreateWebhookWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*CreateWebhookClientResponse, error) {
	rsp, err := c.CreateWebhookWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateWebhookClientResponse(rsp)
}

// CreateWebhookWithResponse Create webhook subscription
//
// Register a new endpoint. Returns the plaintext signing secret ONCE — capture it from the response and store it securely. After this call the secret is masked everywhere; mint a new one with `POST /v1/webhooks/{id}/rotate-secret` if you lose it. See `GET /v1/webhooks/event-types` for the full list of subscribable events. Events about an inactive listing (reservations, messages, alterations, reviews, payments, calendar and listing events) are not delivered. The data keeps syncing while the listing is inactive, but its events are never sent — including after you reactivate it; webhooks resume for events that happen from reactivation on. Account-level events are always delivered.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/webhooks (the `CreateWebhook` operationId).
func (c *ClientWithResponses) CreateWebhookWithResponse(ctx context.Context, body CreateWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*CreateWebhookClientResponse, error) {
	rsp, err := c.CreateWebhook(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseCreateWebhookClientResponse(rsp)
}

// ListWebhookEventTypesWithResponse List webhook event types
//
// The canonical catalog of every event the API can deliver, grouped by domain, with realistic sample payloads.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/webhooks/event-types (the `ListWebhookEventTypes` operationId).
func (c *ClientWithResponses) ListWebhookEventTypesWithResponse(ctx context.Context, reqEditors ...RequestEditorFn) (*ListWebhookEventTypesClientResponse, error) {
	rsp, err := c.ListWebhookEventTypes(ctx, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListWebhookEventTypesClientResponse(rsp)
}

// TestWebhookWithBodyWithResponse [Legacy] Send test webhook to a URL
//
// Deprecated: prefer creating a subscription then calling `POST /v1/webhooks/{id}/ping`. Kept for back-compat.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/webhooks/test (the `TestWebhook` operationId).
func (c *ClientWithResponses) TestWebhookWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*TestWebhookClientResponse, error) {
	rsp, err := c.TestWebhookWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseTestWebhookClientResponse(rsp)
}

// TestWebhookWithResponse [Legacy] Send test webhook to a URL
//
// Deprecated: prefer creating a subscription then calling `POST /v1/webhooks/{id}/ping`. Kept for back-compat.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/webhooks/test (the `TestWebhook` operationId).
func (c *ClientWithResponses) TestWebhookWithResponse(ctx context.Context, body TestWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*TestWebhookClientResponse, error) {
	rsp, err := c.TestWebhook(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseTestWebhookClientResponse(rsp)
}

// DeleteWebhookWithResponse Delete webhook subscription
//
// Delete a webhook subscription. In-flight deliveries already on the queue are still attempted; new events stop firing immediately.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with DELETE /v1/webhooks/{id} (the `DeleteWebhook` operationId).
func (c *ClientWithResponses) DeleteWebhookWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*DeleteWebhookClientResponse, error) {
	rsp, err := c.DeleteWebhook(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseDeleteWebhookClientResponse(rsp)
}

// GetWebhookWithResponse Get webhook subscription
//
// Fetch a single webhook subscription by id. Use the `deliveries` sub-resource to list recent attempts, and `ping` to send a test event.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/webhooks/{id} (the `GetWebhook` operationId).
func (c *ClientWithResponses) GetWebhookWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*GetWebhookClientResponse, error) {
	rsp, err := c.GetWebhook(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetWebhookClientResponse(rsp)
}

// UpdateWebhookWithBodyWithResponse Update webhook subscription
//
// Update url, description, events, or status (active|paused). Re-enabling clears `consecutiveFailures` and `disabledAt`.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/webhooks/{id} (the `UpdateWebhook` operationId).
func (c *ClientWithResponses) UpdateWebhookWithBodyWithResponse(ctx context.Context, id openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*UpdateWebhookClientResponse, error) {
	rsp, err := c.UpdateWebhookWithBody(ctx, id, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateWebhookClientResponse(rsp)
}

// UpdateWebhookWithResponse Update webhook subscription
//
// Update url, description, events, or status (active|paused). Re-enabling clears `consecutiveFailures` and `disabledAt`.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with PATCH /v1/webhooks/{id} (the `UpdateWebhook` operationId).
func (c *ClientWithResponses) UpdateWebhookWithResponse(ctx context.Context, id openapi_types.UUID, body UpdateWebhookJSONRequestBody, reqEditors ...RequestEditorFn) (*UpdateWebhookClientResponse, error) {
	rsp, err := c.UpdateWebhook(ctx, id, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseUpdateWebhookClientResponse(rsp)
}

// ListWebhookDeliveriesWithResponse List webhook deliveries
//
// Cursor-paginated history of every delivery attempt for this subscription. Walk pages with `?cursor=<pagination.nextCursor>`; stop when `pagination.hasMore` is `false`. The cursor is opaque base64 — do not parse it. `?offset=` is also accepted as a first-class alias for shallow paging (0..10000) — mutually exclusive with `cursor`.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/webhooks/{id}/deliveries (the `ListWebhookDeliveries` operationId).
func (c *ClientWithResponses) ListWebhookDeliveriesWithResponse(ctx context.Context, id openapi_types.UUID, params *ListWebhookDeliveriesParams, reqEditors ...RequestEditorFn) (*ListWebhookDeliveriesClientResponse, error) {
	rsp, err := c.ListWebhookDeliveries(ctx, id, params, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseListWebhookDeliveriesClientResponse(rsp)
}

// GetWebhookDeliveryWithResponse Get webhook delivery
//
// Full request + response capture for one delivery attempt.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with GET /v1/webhooks/{id}/deliveries/{delivery_id} (the `GetWebhookDelivery` operationId).
func (c *ClientWithResponses) GetWebhookDeliveryWithResponse(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, reqEditors ...RequestEditorFn) (*GetWebhookDeliveryClientResponse, error) {
	rsp, err := c.GetWebhookDelivery(ctx, id, deliveryId, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseGetWebhookDeliveryClientResponse(rsp)
}

// ReplayWebhookDeliveryWithBodyWithResponse Replay webhook delivery
//
// Re-sends the original payload (same eventId, fresh deliveryId, attempt + 1).
//
// A delivery may be replayed at most **3 times per rolling 60 minutes**; the 4th inside that window answers `409 replay_limit_reached` and names the time the next one is allowed. The limit is charged to the original delivery, so replaying the delivery a replay produced draws on the same budget. It is not a lifetime cap — a delivery that has not been replayed for an hour starts fresh.
//
// A delivery your endpoint already accepted is not re-sent (it would be a duplicate) and answers `409 delivery_already_succeeded`; send `{"force": true}` to replay it anyway, which still counts against the limit.
//
// A delivery about a listing that is inactive now is not re-sent and answers `403 listing_inactive`; activate the listing first.
//
// Takes any type of body and a specified content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/webhooks/{id}/deliveries/{delivery_id}/replay (the `ReplayWebhookDelivery` operationId).
func (c *ClientWithResponses) ReplayWebhookDeliveryWithBodyWithResponse(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*ReplayWebhookDeliveryClientResponse, error) {
	rsp, err := c.ReplayWebhookDeliveryWithBody(ctx, id, deliveryId, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReplayWebhookDeliveryClientResponse(rsp)
}

// ReplayWebhookDeliveryWithResponse Replay webhook delivery
//
// Re-sends the original payload (same eventId, fresh deliveryId, attempt + 1).
//
// A delivery may be replayed at most **3 times per rolling 60 minutes**; the 4th inside that window answers `409 replay_limit_reached` and names the time the next one is allowed. The limit is charged to the original delivery, so replaying the delivery a replay produced draws on the same budget. It is not a lifetime cap — a delivery that has not been replayed for an hour starts fresh.
//
// A delivery your endpoint already accepted is not re-sent (it would be a duplicate) and answers `409 delivery_already_succeeded`; send `{"force": true}` to replay it anyway, which still counts against the limit.
//
// A delivery about a listing that is inactive now is not re-sent and answers `403 listing_inactive`; activate the listing first.
//
// Takes a body of the `application/json` content type, and returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/webhooks/{id}/deliveries/{delivery_id}/replay (the `ReplayWebhookDelivery` operationId).
func (c *ClientWithResponses) ReplayWebhookDeliveryWithResponse(ctx context.Context, id openapi_types.UUID, deliveryId openapi_types.UUID, body ReplayWebhookDeliveryJSONRequestBody, reqEditors ...RequestEditorFn) (*ReplayWebhookDeliveryClientResponse, error) {
	rsp, err := c.ReplayWebhookDelivery(ctx, id, deliveryId, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseReplayWebhookDeliveryClientResponse(rsp)
}

// PingWebhookWithResponse Send ping event
//
// Fires a synthetic `repull.ping` at the subscription URL and returns the full delivery cycle inline. Used by dashboards and health-check probes.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/webhooks/{id}/ping (the `PingWebhook` operationId).
func (c *ClientWithResponses) PingWebhookWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*PingWebhookClientResponse, error) {
	rsp, err := c.PingWebhook(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePingWebhookClientResponse(rsp)
}

// RotateWebhookSecretWithResponse Rotate signing secret
//
// Mints a fresh signing secret and returns the plaintext ONCE. After this response the secret is masked everywhere — capture and store it now.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/webhooks/{id}/rotate-secret (the `RotateWebhookSecret` operationId).
func (c *ClientWithResponses) RotateWebhookSecretWithResponse(ctx context.Context, id openapi_types.UUID, reqEditors ...RequestEditorFn) (*RotateWebhookSecretClientResponse, error) {
	rsp, err := c.RotateWebhookSecret(ctx, id, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseRotateWebhookSecretClientResponse(rsp)
}

// TestFireWebhookWithResponse Send test event of a specific type
//
// Delivers a realistic fixture payload of the requested event type to the subscription URL.
//
// Returns a wrapper object for the known response body format(s).
//
// Corresponds with POST /v1/webhooks/{id}/test/{event_type} (the `TestFireWebhook` operationId).
func (c *ClientWithResponses) TestFireWebhookWithResponse(ctx context.Context, id openapi_types.UUID, eventType WebhookEventType, reqEditors ...RequestEditorFn) (*TestFireWebhookClientResponse, error) {
	rsp, err := c.TestFireWebhook(ctx, id, eventType, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParseTestFireWebhookClientResponse(rsp)
}

// ParseBatchUpdateAvailabilityClientResponse parses an HTTP response from a BatchUpdateAvailabilityWithResponse call
func ParseBatchUpdateAvailabilityClientResponse(rsp *http.Response) (*BatchUpdateAvailabilityClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &BatchUpdateAvailabilityClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AvailabilityWriteResult
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseGetAvailabilityClientResponse parses an HTTP response from a GetAvailabilityWithResponse call
func ParseGetAvailabilityClientResponse(rsp *http.Response) (*GetAvailabilityClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAvailabilityClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest PropertyAvailability
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseUpdateAvailabilityClientResponse parses an HTTP response from a UpdateAvailabilityWithResponse call
func ParseUpdateAvailabilityClientResponse(rsp *http.Response) (*UpdateAvailabilityClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAvailabilityClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AvailabilityWriteResult
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseCreateBillingCheckoutClientResponse parses an HTTP response from a CreateBillingCheckoutWithResponse call
func ParseCreateBillingCheckoutClientResponse(rsp *http.Response) (*CreateBillingCheckoutClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateBillingCheckoutClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	return response, nil
}

// ParseListAirbnbAlterationsClientResponse parses an HTTP response from a ListAirbnbAlterationsWithResponse call
func ParseListAirbnbAlterationsClientResponse(rsp *http.Response) (*ListAirbnbAlterationsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbAlterationsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data []AirbnbAlteration `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest AirbnbAccountNotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseCreateAirbnbAlterationClientResponse parses an HTTP response from a CreateAirbnbAlterationWithResponse call
func ParseCreateAirbnbAlterationClientResponse(rsp *http.Response) (*CreateAirbnbAlterationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateAirbnbAlterationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest AirbnbAlteration
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetAirbnbAlterationClientResponse parses an HTTP response from a GetAirbnbAlterationWithResponse call
func ParseGetAirbnbAlterationClientResponse(rsp *http.Response) (*GetAirbnbAlterationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbAlterationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Data An Airbnb reservation alteration request (date change, guest-count change, price change, or a move to another listing), mirrored locally in `reservation_alterations`. Fields prefixed `original*` describe the reservation as it stands today; `new*` fields describe the proposed change. Compare them to render a diff and decide whether to accept (`POST .../{id}/accept`) or decline (`POST .../{id}/decline`) — or, for one you proposed yourself, to withdraw it (`POST .../{id}/cancel`).
			Data AirbnbAlteration `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseAcceptAirbnbAlterationClientResponse parses an HTTP response from a AcceptAirbnbAlterationWithResponse call
func ParseAcceptAirbnbAlterationClientResponse(rsp *http.Response) (*AcceptAirbnbAlterationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &AcceptAirbnbAlterationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseCancelAirbnbAlterationClientResponse parses an HTTP response from a CancelAirbnbAlterationWithResponse call
func ParseCancelAirbnbAlterationClientResponse(rsp *http.Response) (*CancelAirbnbAlterationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CancelAirbnbAlterationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseDeclineAirbnbAlterationClientResponse parses an HTTP response from a DeclineAirbnbAlterationWithResponse call
func ParseDeclineAirbnbAlterationClientResponse(rsp *http.Response) (*DeclineAirbnbAlterationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeclineAirbnbAlterationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetAirbnbConnectionClientResponse parses an HTTP response from a GetAirbnbConnectionWithResponse call
func ParseGetAirbnbConnectionClientResponse(rsp *http.Response) (*GetAirbnbConnectionClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbConnectionClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbConnectionResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListAirbnbListingsClientResponse parses an HTTP response from a ListAirbnbListingsWithResponse call
func ParseListAirbnbListingsClientResponse(rsp *http.Response) (*ListAirbnbListingsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbListingsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbListingListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest AirbnbAccountNotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseMapAirbnbListingClientResponse parses an HTTP response from a MapAirbnbListingWithResponse call
func ParseMapAirbnbListingClientResponse(rsp *http.Response) (*MapAirbnbListingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &MapAirbnbListingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest MapAirbnbListingResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseGetAirbnbListingClientResponse parses an HTTP response from a GetAirbnbListingWithResponse call
func ParseGetAirbnbListingClientResponse(rsp *http.Response) (*GetAirbnbListingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbListingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbListing
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseAirbnbListingActionClientResponse parses an HTTP response from a AirbnbListingActionWithResponse call
func ParseAirbnbListingActionClientResponse(rsp *http.Response) (*AirbnbListingActionClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &AirbnbListingActionClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbListingAction200JSONResponseBody
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 402:
		var dest PublishBillingRefused
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON402 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseListAirbnbListingAmenitiesClientResponse parses an HTTP response from a ListAirbnbListingAmenitiesWithResponse call
func ParseListAirbnbListingAmenitiesClientResponse(rsp *http.Response) (*ListAirbnbListingAmenitiesClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbListingAmenitiesClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data struct {
				AccessibilityAmenities *[]AirbnbAmenity `json:"accessibility_amenities,omitempty"`
				Amenities              *[]AirbnbAmenity `json:"amenities,omitempty"`
			} `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbListingAmenitiesClientResponse parses an HTTP response from a UpdateAirbnbListingAmenitiesWithResponse call
func ParseUpdateAirbnbListingAmenitiesClientResponse(rsp *http.Response) (*UpdateAirbnbListingAmenitiesClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbListingAmenitiesClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data struct {
				// AccessibilityAmenities How many accessibility amenities were written.
				AccessibilityAmenities *int `json:"accessibilityAmenities,omitempty"`

				// Amenities How many regular amenities were written.
				Amenities *int `json:"amenities,omitempty"`
			} `json:"data"`

			// Stored Whether our own copy was brought in line with the change.
			Stored bool `json:"stored"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetAirbnbListingAvailabilityClientResponse parses an HTTP response from a GetAirbnbListingAvailabilityWithResponse call
func ParseGetAirbnbListingAvailabilityClientResponse(rsp *http.Response) (*GetAirbnbListingAvailabilityClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbListingAvailabilityClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbListingAvailabilityClientResponse parses an HTTP response from a UpdateAirbnbListingAvailabilityWithResponse call
func ParseUpdateAirbnbListingAvailabilityClientResponse(rsp *http.Response) (*UpdateAirbnbListingAvailabilityClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbListingAvailabilityClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetAirbnbBookingSettingsClientResponse parses an HTTP response from a GetAirbnbBookingSettingsWithResponse call
func ParseGetAirbnbBookingSettingsClientResponse(rsp *http.Response) (*GetAirbnbBookingSettingsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbBookingSettingsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data struct {
				// AdvanceNotice How much notice a booking needs.
				AdvanceNotice *struct {
					// AllowRequestToBook Whether a guest may still REQUEST to book inside the notice window.
					AllowRequestToBook *bool `json:"allowRequestToBook,omitempty"`

					// Hours Whole hours of notice. `0` allows same-day bookings.
					Hours *int `json:"hours,omitempty"`

					// SameDayBookingsAllowed Derived: `hours === 0`.
					SameDayBookingsAllowed *bool `json:"sameDayBookingsAllowed,omitempty"`
				} `json:"advanceNotice,omitempty"`

				// BookingMode Derived from `instantBook`. `request_to_book` means every booking needs the host to approve it.
				BookingMode *GetAirbnbBookingSettings200JSONResponseBodyDataBookingMode `json:"bookingMode,omitempty"`

				// BookingWindow How far ahead guests may book.
				BookingWindow *struct {
					// Days `null` when unlimited.
					Days      *int  `json:"days,omitempty"`
					Unlimited *bool `json:"unlimited,omitempty"`
				} `json:"bookingWindow,omitempty"`
				Cancellation *struct {
					// LongStayPolicy Airbnb's long-term-stay policy id, for stays of 28+ nights. Opaque.
					LongStayPolicy *string `json:"longStayPolicy,omitempty"`
					NonRefundable  *struct {
						// DiscountPercent Whole-percent discount a guest gets for giving up refundability.
						DiscountPercent *int  `json:"discountPercent,omitempty"`
						Enabled         *bool `json:"enabled,omitempty"`

						// PriceFactor Airbnb's own representation: `1 - discountPercent/100`.
						PriceFactor *float32 `json:"priceFactor,omitempty"`
					} `json:"nonRefundable,omitempty"`

					// ShortStayPolicy Policy for stays under 28 nights.
					ShortStayPolicy *GetAirbnbBookingSettings200JSONResponseBodyDataCancellationShortStayPolicy `json:"shortStayPolicy,omitempty"`
				} `json:"cancellation,omitempty"`

				// CheckIn Check-in window as hours of the day, or `FLEXIBLE`.
				CheckIn *struct {
					End   *GetAirbnbBookingSettings200JSONResponseBody_Data_CheckIn_End   `json:"end,omitempty"`
					Start *GetAirbnbBookingSettings200JSONResponseBody_Data_CheckIn_Start `json:"start,omitempty"`
				} `json:"checkIn,omitempty"`
				CheckOut *struct {
					Time *int `json:"time,omitempty"`
				} `json:"checkOut,omitempty"`

				// InstantBook Instant Book state. All three fields describe ONE Airbnb value, `instant_booking_allowed_category`.
				InstantBook *struct {
					// Enabled `false` when the category is `off` — the listing is request-to-book.
					Enabled *bool `json:"enabled,omitempty"`

					// GuestCategory Which guests may Instant Book.
					GuestCategory *GetAirbnbBookingSettings200JSONResponseBodyDataInstantBookGuestCategory `json:"guestCategory,omitempty"`

					// RequiresGoodTrackRecord `true` for `experienced_guests_only` / `recommended_guests_only` — Airbnb calls this a good track record.
					RequiresGoodTrackRecord *bool `json:"requiresGoodTrackRecord,omitempty"`
				} `json:"instantBook,omitempty"`

				// PreparationTime Airbnb's `turnover_days` — nights blocked between two stays.
				PreparationTime *struct {
					Nights *int `json:"nights,omitempty"`
				} `json:"preparationTime,omitempty"`
			} `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbBookingSettingsClientResponse parses an HTTP response from a UpdateAirbnbBookingSettingsWithResponse call
func ParseUpdateAirbnbBookingSettingsClientResponse(rsp *http.Response) (*UpdateAirbnbBookingSettingsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbBookingSettingsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data struct {
				// Applied Which upstream groups this request wrote.
				Applied  []UpdateAirbnbBookingSettings200JSONResponseBodyDataApplied `json:"applied"`
				Settings struct {
					// AdvanceNotice How much notice a booking needs.
					AdvanceNotice *struct {
						// AllowRequestToBook Whether a guest may still REQUEST to book inside the notice window.
						AllowRequestToBook *bool `json:"allowRequestToBook,omitempty"`

						// Hours Whole hours of notice. `0` allows same-day bookings.
						Hours *int `json:"hours,omitempty"`

						// SameDayBookingsAllowed Derived: `hours === 0`.
						SameDayBookingsAllowed *bool `json:"sameDayBookingsAllowed,omitempty"`
					} `json:"advanceNotice,omitempty"`

					// BookingMode Derived from `instantBook`. `request_to_book` means every booking needs the host to approve it.
					BookingMode *UpdateAirbnbBookingSettings200JSONResponseBodyDataSettingsBookingMode `json:"bookingMode,omitempty"`

					// BookingWindow How far ahead guests may book.
					BookingWindow *struct {
						// Days `null` when unlimited.
						Days      *int  `json:"days,omitempty"`
						Unlimited *bool `json:"unlimited,omitempty"`
					} `json:"bookingWindow,omitempty"`
					Cancellation *struct {
						// LongStayPolicy Airbnb's long-term-stay policy id, for stays of 28+ nights. Opaque.
						LongStayPolicy *string `json:"longStayPolicy,omitempty"`
						NonRefundable  *struct {
							// DiscountPercent Whole-percent discount a guest gets for giving up refundability.
							DiscountPercent *int  `json:"discountPercent,omitempty"`
							Enabled         *bool `json:"enabled,omitempty"`

							// PriceFactor Airbnb's own representation: `1 - discountPercent/100`.
							PriceFactor *float32 `json:"priceFactor,omitempty"`
						} `json:"nonRefundable,omitempty"`

						// ShortStayPolicy Policy for stays under 28 nights.
						ShortStayPolicy *UpdateAirbnbBookingSettings200JSONResponseBodyDataSettingsCancellationShortStayPolicy `json:"shortStayPolicy,omitempty"`
					} `json:"cancellation,omitempty"`

					// CheckIn Check-in window as hours of the day, or `FLEXIBLE`.
					CheckIn *struct {
						End   *UpdateAirbnbBookingSettings200JSONResponseBody_Data_Settings_CheckIn_End   `json:"end,omitempty"`
						Start *UpdateAirbnbBookingSettings200JSONResponseBody_Data_Settings_CheckIn_Start `json:"start,omitempty"`
					} `json:"checkIn,omitempty"`
					CheckOut *struct {
						Time *int `json:"time,omitempty"`
					} `json:"checkOut,omitempty"`

					// InstantBook Instant Book state. All three fields describe ONE Airbnb value, `instant_booking_allowed_category`.
					InstantBook *struct {
						// Enabled `false` when the category is `off` — the listing is request-to-book.
						Enabled *bool `json:"enabled,omitempty"`

						// GuestCategory Which guests may Instant Book.
						GuestCategory *UpdateAirbnbBookingSettings200JSONResponseBodyDataSettingsInstantBookGuestCategory `json:"guestCategory,omitempty"`

						// RequiresGoodTrackRecord `true` for `experienced_guests_only` / `recommended_guests_only` — Airbnb calls this a good track record.
						RequiresGoodTrackRecord *bool `json:"requiresGoodTrackRecord,omitempty"`
					} `json:"instantBook,omitempty"`

					// PreparationTime Airbnb's `turnover_days` — nights blocked between two stays.
					PreparationTime *struct {
						Nights *int `json:"nights,omitempty"`
					} `json:"preparationTime,omitempty"`
				} `json:"settings"`
			} `json:"data"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetAirbnbCheckinGuideClientResponse parses an HTTP response from a GetAirbnbCheckinGuideWithResponse call
func ParseGetAirbnbCheckinGuideClientResponse(rsp *http.Response) (*GetAirbnbCheckinGuideClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbCheckinGuideClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data []map[string]interface{} `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbCheckinGuideClientResponse parses an HTTP response from a UpdateAirbnbCheckinGuideWithResponse call
func ParseUpdateAirbnbCheckinGuideClientResponse(rsp *http.Response) (*UpdateAirbnbCheckinGuideClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbCheckinGuideClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetAirbnbCheckoutGuideClientResponse parses an HTTP response from a GetAirbnbCheckoutGuideWithResponse call
func ParseGetAirbnbCheckoutGuideClientResponse(rsp *http.Response) (*GetAirbnbCheckoutGuideClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbCheckoutGuideClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data []map[string]interface{} `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListAirbnbListingDescriptionsClientResponse parses an HTTP response from a ListAirbnbListingDescriptionsWithResponse call
func ParseListAirbnbListingDescriptionsClientResponse(rsp *http.Response) (*ListAirbnbListingDescriptionsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbListingDescriptionsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data []map[string]interface{} `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbListingDescriptionClientResponse parses an HTTP response from a UpdateAirbnbListingDescriptionWithResponse call
func ParseUpdateAirbnbListingDescriptionClientResponse(rsp *http.Response) (*UpdateAirbnbListingDescriptionClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbListingDescriptionClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbContentWriteResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetAirbnbListingDetailsClientResponse parses an HTTP response from a GetAirbnbListingDetailsWithResponse call
func ParseGetAirbnbListingDetailsClientResponse(rsp *http.Response) (*GetAirbnbListingDetailsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbListingDetailsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data []AirbnbListingDetailsResponse `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbListingDetailsClientResponse parses an HTTP response from a UpdateAirbnbListingDetailsWithResponse call
func ParseUpdateAirbnbListingDetailsClientResponse(rsp *http.Response) (*UpdateAirbnbListingDetailsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbListingDetailsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbContentWriteResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseListAirbnbListingPermitsClientResponse parses an HTTP response from a ListAirbnbListingPermitsWithResponse call
func ParseListAirbnbListingPermitsClientResponse(rsp *http.Response) (*ListAirbnbListingPermitsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbListingPermitsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data AirbnbPermitsResponse `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbListingPermitsClientResponse parses an HTTP response from a UpdateAirbnbListingPermitsWithResponse call
func ParseUpdateAirbnbListingPermitsClientResponse(rsp *http.Response) (*UpdateAirbnbListingPermitsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbListingPermitsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AirbnbListingId *string `json:"airbnbListingId,omitempty"`
			ListingId       *string `json:"listingId,omitempty"`

			// Permits The permit flows as Airbnb reports them after the write, including each flow's new `status`.
			Permits *[]map[string]interface{} `json:"permits,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseDeleteAirbnbListingPhotoClientResponse parses an HTTP response from a DeleteAirbnbListingPhotoWithResponse call
func ParseDeleteAirbnbListingPhotoClientResponse(rsp *http.Response) (*DeleteAirbnbListingPhotoClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteAirbnbListingPhotoClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Deleted Example: true
			Deleted *bool `json:"deleted,omitempty"`

			// Stored Whether our own copy dropped the photo too.
			Stored *bool `json:"stored,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListAirbnbListingPhotosClientResponse parses an HTTP response from a ListAirbnbListingPhotosWithResponse call
func ParseListAirbnbListingPhotosClientResponse(rsp *http.Response) (*ListAirbnbListingPhotosClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbListingPhotosClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbListingPhotoClientResponse parses an HTTP response from a UpdateAirbnbListingPhotoWithResponse call
func ParseUpdateAirbnbListingPhotoClientResponse(rsp *http.Response) (*UpdateAirbnbListingPhotoClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbListingPhotoClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Data The photo as Airbnb returned it.
			Data map[string]interface{} `json:"data"`

			// Stored Whether our own copy was brought in line with the change.
			Stored bool `json:"stored"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseUploadAirbnbListingPhotosClientResponse parses an HTTP response from a UploadAirbnbListingPhotosWithResponse call
func ParseUploadAirbnbListingPhotosClientResponse(rsp *http.Response) (*UploadAirbnbListingPhotosClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UploadAirbnbListingPhotosClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 201:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseSetAirbnbListingCoverPhotoClientResponse parses an HTTP response from a SetAirbnbListingCoverPhotoWithResponse call
func ParseSetAirbnbListingCoverPhotoClientResponse(rsp *http.Response) (*SetAirbnbListingCoverPhotoClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SetAirbnbListingCoverPhotoClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data struct {
				// Applied The photos this call actually moved. Empty when the photo was already the cover.
				Applied      *[]AirbnbPhotoPosition `json:"applied,omitempty"`
				CoverPhotoId *string                `json:"coverPhotoId,omitempty"`

				// Order Present only when the whole tour had to be renumbered.
				Order *[]AirbnbPhotoPosition `json:"order,omitempty"`
			} `json:"data"`

			// Stored Whether our own copy (tour order and listing thumbnail) was brought in line.
			Stored bool `json:"stored"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseReorderAirbnbListingPhotosClientResponse parses an HTTP response from a ReorderAirbnbListingPhotosWithResponse call
func ParseReorderAirbnbListingPhotosClientResponse(rsp *http.Response) (*ReorderAirbnbListingPhotosClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ReorderAirbnbListingPhotosClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data struct {
				// Applied The photos this call actually moved.
				Applied *[]AirbnbPhotoPosition `json:"applied,omitempty"`

				// Order The resulting tour order, including photos you did not name.
				Order *[]AirbnbPhotoPosition `json:"order,omitempty"`

				// Unchanged Photos that were already in the right position.
				Unchanged *int `json:"unchanged,omitempty"`
			} `json:"data"`

			// Stored Whether our own copy was brought in line with what landed.
			Stored bool `json:"stored"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetAirbnbListingPricingClientResponse parses an HTTP response from a GetAirbnbListingPricingWithResponse call
func ParseGetAirbnbListingPricingClientResponse(rsp *http.Response) (*GetAirbnbListingPricingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbListingPricingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbListingPricingClientResponse parses an HTTP response from a UpdateAirbnbListingPricingWithResponse call
func ParseUpdateAirbnbListingPricingClientResponse(rsp *http.Response) (*UpdateAirbnbListingPricingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbListingPricingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetAirbnbListingQualityClientResponse parses an HTTP response from a GetAirbnbListingQualityWithResponse call
func ParseGetAirbnbListingQualityClientResponse(rsp *http.Response) (*GetAirbnbListingQualityClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbListingQualityClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Data Shape depends on `type`: an object `{ standards, issues }` for `all`/`standards`, or an array for `issues`/`stats`.
			Data interface{} `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseDeleteAirbnbListingRoomClientResponse parses an HTTP response from a DeleteAirbnbListingRoomWithResponse call
func ParseDeleteAirbnbListingRoomClientResponse(rsp *http.Response) (*DeleteAirbnbListingRoomClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteAirbnbListingRoomClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Deleted Example: true
			Deleted *bool `json:"deleted,omitempty"`

			// Stored Whether our own copy dropped the room too.
			Stored *bool `json:"stored,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListAirbnbListingRoomsClientResponse parses an HTTP response from a ListAirbnbListingRoomsWithResponse call
func ParseListAirbnbListingRoomsClientResponse(rsp *http.Response) (*ListAirbnbListingRoomsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbListingRoomsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data []map[string]interface{} `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseCreateAirbnbListingRoomClientResponse parses an HTTP response from a CreateAirbnbListingRoomWithResponse call
func ParseCreateAirbnbListingRoomClientResponse(rsp *http.Response) (*CreateAirbnbListingRoomClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateAirbnbListingRoomClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 201:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbListingRoomClientResponse parses an HTTP response from a UpdateAirbnbListingRoomWithResponse call
func ParseUpdateAirbnbListingRoomClientResponse(rsp *http.Response) (*UpdateAirbnbListingRoomClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbListingRoomClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Data The room as Airbnb returned it.
			Data map[string]interface{} `json:"data"`

			// Stored Whether our own copy was brought in line with the change.
			Stored bool `json:"stored"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListAirbnbListingSafetyDisclosuresClientResponse parses an HTTP response from a ListAirbnbListingSafetyDisclosuresWithResponse call
func ParseListAirbnbListingSafetyDisclosuresClientResponse(rsp *http.Response) (*ListAirbnbListingSafetyDisclosuresClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbListingSafetyDisclosuresClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data AirbnbSafetyDisclosuresResponse `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbListingSafetyDisclosuresClientResponse parses an HTTP response from a UpdateAirbnbListingSafetyDisclosuresWithResponse call
func ParseUpdateAirbnbListingSafetyDisclosuresClientResponse(rsp *http.Response) (*UpdateAirbnbListingSafetyDisclosuresClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbListingSafetyDisclosuresClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AirbnbListingId *string `json:"airbnbListingId,omitempty"`

			// Disclosures The set as Airbnb reports it after the write.
			Disclosures *[]AirbnbSafetyDisclosure `json:"disclosures,omitempty"`
			ListingId   *string                   `json:"listingId,omitempty"`

			// Result Airbnb's raw booking-settings response.
			Result *map[string]interface{} `json:"result,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest AirbnbWriteForbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetAirbnbListingSettingsClientResponse parses an HTTP response from a GetAirbnbListingSettingsWithResponse call
func ParseGetAirbnbListingSettingsClientResponse(rsp *http.Response) (*GetAirbnbListingSettingsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbListingSettingsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Data Shape depends on `type`: `{ hosts, locales }` for `all`, or an array for `hosts`/`permits`/`locales`.
			Data interface{} `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListAirbnbThreadsClientResponse parses an HTTP response from a ListAirbnbThreadsWithResponse call
func ParseListAirbnbThreadsClientResponse(rsp *http.Response) (*ListAirbnbThreadsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbThreadsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbThreadListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest AirbnbAccountNotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseGetAirbnbThreadClientResponse parses an HTTP response from a GetAirbnbThreadWithResponse call
func ParseGetAirbnbThreadClientResponse(rsp *http.Response) (*GetAirbnbThreadClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbThreadClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Data An Airbnb message thread.
			Data AirbnbThread `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListAirbnbThreadMessagesClientResponse parses an HTTP response from a ListAirbnbThreadMessagesWithResponse call
func ParseListAirbnbThreadMessagesClientResponse(rsp *http.Response) (*ListAirbnbThreadMessagesClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbThreadMessagesClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data *[]struct {
				Attachments *[]ConversationMessageAttachment `json:"attachments,omitempty"`

				// Channel Example: airbnb
				Channel   *string    `json:"channel,omitempty"`
				CreatedAt *time.Time `json:"createdAt,omitempty"`

				// ExternalCreatedAt When Airbnb recorded the message.
				ExternalCreatedAt *time.Time `json:"externalCreatedAt,omitempty"`

				// ExternalMessageId Airbnb's message id.
				ExternalMessageId *string `json:"externalMessageId,omitempty"`

				// Id Repull message id.
				Id *string `json:"id,omitempty"`

				// Message Message text. Empty for a file-only message.
				Message       *string `json:"message,omitempty"`
				ReservationId *string `json:"reservationId,omitempty"`

				// SenderType `guest`, `host`, `user`, `system`, …
				SenderType *string `json:"senderType,omitempty"`

				// ThreadId The Airbnb thread id.
				ThreadId          *string    `json:"threadId,omitempty"`
				TranslatedMessage *string    `json:"translatedMessage,omitempty"`
				UpdatedAt         *time.Time `json:"updatedAt,omitempty"`

				// UserId Airbnb user id of the sender.
				UserId *string `json:"userId,omitempty"`
			} `json:"data,omitempty"`

			// DataFreshness When this workspace's Airbnb data was last synced.
			DataFreshness *map[string]interface{} `json:"dataFreshness,omitempty"`

			// Pagination Absent when `?all=true`.
			Pagination *struct {
				HasMore    *bool   `json:"hasMore,omitempty"`
				NextCursor *string `json:"nextCursor,omitempty"`
			} `json:"pagination,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseSendAirbnbMessageClientResponse parses an HTTP response from a SendAirbnbMessageWithResponse call
func ParseSendAirbnbMessageClientResponse(rsp *http.Response) (*SendAirbnbMessageClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SendAirbnbMessageClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest SendAirbnbMessage201JSONResponseBody
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseUpdateAirbnbMessageClientResponse parses an HTTP response from a UpdateAirbnbMessageWithResponse call
func ParseUpdateAirbnbMessageClientResponse(rsp *http.Response) (*UpdateAirbnbMessageClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateAirbnbMessageClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseWithdrawAirbnbOfferClientResponse parses an HTTP response from a WithdrawAirbnbOfferWithResponse call
func ParseWithdrawAirbnbOfferClientResponse(rsp *http.Response) (*WithdrawAirbnbOfferClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &WithdrawAirbnbOfferClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			CreatedAt    *time.Time              `json:"createdAt,omitempty"`
			ExpiresAt    *time.Time              `json:"expiresAt,omitempty"`
			GuestDetails *map[string]interface{} `json:"guestDetails,omitempty"`

			// Id Airbnb special-offer id.
			Id *string `json:"id,omitempty"`

			// ListingId Airbnb listing id (special offers only).
			ListingId *string                                          `json:"listingId,omitempty"`
			Nights    *int                                             `json:"nights,omitempty"`
			OfferType *WithdrawAirbnbOffer200JSONResponseBodyOfferType `json:"offerType,omitempty"`
			StartDate *openapi_types.Date                              `json:"startDate,omitempty"`
			Status    *WithdrawAirbnbOffer200JSONResponseBodyStatus    `json:"status,omitempty"`

			// ThreadId Airbnb thread id the offer was sent on.
			ThreadId   *string  `json:"threadId,omitempty"`
			TotalPrice *float32 `json:"totalPrice,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetAirbnbOfferClientResponse parses an HTTP response from a GetAirbnbOfferWithResponse call
func ParseGetAirbnbOfferClientResponse(rsp *http.Response) (*GetAirbnbOfferClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbOfferClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			CreatedAt    *time.Time              `json:"createdAt,omitempty"`
			ExpiresAt    *time.Time              `json:"expiresAt,omitempty"`
			GuestDetails *map[string]interface{} `json:"guestDetails,omitempty"`

			// Id Airbnb special-offer id.
			Id *string `json:"id,omitempty"`

			// ListingId Airbnb listing id (special offers only).
			ListingId *string                                     `json:"listingId,omitempty"`
			Nights    *int                                        `json:"nights,omitempty"`
			OfferType *GetAirbnbOffer200JSONResponseBodyOfferType `json:"offerType,omitempty"`
			StartDate *openapi_types.Date                         `json:"startDate,omitempty"`
			Status    *GetAirbnbOffer200JSONResponseBodyStatus    `json:"status,omitempty"`

			// ThreadId Airbnb thread id the offer was sent on.
			ThreadId   *string  `json:"threadId,omitempty"`
			TotalPrice *float32 `json:"totalPrice,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseCreateAirbnbOfferClientResponse parses an HTTP response from a CreateAirbnbOfferWithResponse call
func ParseCreateAirbnbOfferClientResponse(rsp *http.Response) (*CreateAirbnbOfferClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateAirbnbOfferClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest struct {
			CreatedAt    *time.Time              `json:"createdAt,omitempty"`
			ExpiresAt    *time.Time              `json:"expiresAt,omitempty"`
			GuestDetails *map[string]interface{} `json:"guestDetails,omitempty"`

			// Id Airbnb special-offer id.
			Id *string `json:"id,omitempty"`

			// ListingId Airbnb listing id (special offers only).
			ListingId *string                                        `json:"listingId,omitempty"`
			Nights    *int                                           `json:"nights,omitempty"`
			OfferType *CreateAirbnbOffer201JSONResponseBodyOfferType `json:"offerType,omitempty"`
			StartDate *openapi_types.Date                            `json:"startDate,omitempty"`
			Status    *CreateAirbnbOffer201JSONResponseBodyStatus    `json:"status,omitempty"`

			// ThreadId Airbnb thread id the offer was sent on.
			ThreadId   *string  `json:"threadId,omitempty"`
			TotalPrice *float32 `json:"totalPrice,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseListAirbnbReservationsClientResponse parses an HTTP response from a ListAirbnbReservationsWithResponse call
func ParseListAirbnbReservationsClientResponse(rsp *http.Response) (*ListAirbnbReservationsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbReservationsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbReservationListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest AirbnbAccountNotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseGetAirbnbReservationClientResponse parses an HTTP response from a GetAirbnbReservationWithResponse call
func ParseGetAirbnbReservationClientResponse(rsp *http.Response) (*GetAirbnbReservationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAirbnbReservationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbReservation
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseAirbnbReservationActionClientResponse parses an HTTP response from a AirbnbReservationActionWithResponse call
func ParseAirbnbReservationActionClientResponse(rsp *http.Response) (*AirbnbReservationActionClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &AirbnbReservationActionClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			ConfirmationCode *string `json:"confirmationCode,omitempty"`
			StatusType       *string `json:"statusType,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseListAirbnbReviewsClientResponse parses an HTTP response from a ListAirbnbReviewsWithResponse call
func ParseListAirbnbReviewsClientResponse(rsp *http.Response) (*ListAirbnbReviewsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbReviewsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbReviewListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest AirbnbAccountNotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseRespondAirbnbReviewLegacyClientResponse parses an HTTP response from a RespondAirbnbReviewLegacyWithResponse call
func ParseRespondAirbnbReviewLegacyClientResponse(rsp *http.Response) (*RespondAirbnbReviewLegacyClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &RespondAirbnbReviewLegacyClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case rsp.StatusCode == 201:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseEditAirbnbReviewClientResponse parses an HTTP response from a EditAirbnbReviewWithResponse call
func ParseEditAirbnbReviewClientResponse(rsp *http.Response) (*EditAirbnbReviewClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &EditAirbnbReviewClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbReview
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseRespondAirbnbReviewClientResponse parses an HTTP response from a RespondAirbnbReviewWithResponse call
func ParseRespondAirbnbReviewClientResponse(rsp *http.Response) (*RespondAirbnbReviewClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &RespondAirbnbReviewClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest AirbnbReview
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseListAirbnbTransactionsClientResponse parses an HTTP response from a ListAirbnbTransactionsWithResponse call
func ParseListAirbnbTransactionsClientResponse(rsp *http.Response) (*ListAirbnbTransactionsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListAirbnbTransactionsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data []AirbnbTransaction `json:"data"`

			// DataFreshness Top-level freshness indicator for any DB-backed Airbnb read. Tells consumers WHY a column may be `null` or stale without sprinkling per-row error envelopes through the response. The endpoint always returns 200 + DB data; this field is the single signal for "should I prompt the user to reconnect / wait for sync?".
			//
			// A workspace can connect several Airbnb accounts, so the answer has two levels. `accounts[]` carries the verdict per account; the top-level fields aggregate it. Scope a request with `?account_id=` and `accounts[]` holds exactly that account, with the top-level fields mirroring it.
			DataFreshness AirbnbDataFreshness `json:"dataFreshness"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest AirbnbAccountNotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseSyncAirbnbTransactionsClientResponse parses an HTTP response from a SyncAirbnbTransactionsWithResponse call
func ParseSyncAirbnbTransactionsClientResponse(rsp *http.Response) (*SyncAirbnbTransactionsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SyncAirbnbTransactionsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Count Number of transactions upserted.
			Count  int  `json:"count"`
			Synced bool `json:"synced"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetBookingAvailabilityClientResponse parses an HTTP response from a GetBookingAvailabilityWithResponse call
func ParseGetBookingAvailabilityClientResponse(rsp *http.Response) (*GetBookingAvailabilityClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetBookingAvailabilityClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BookingAvailabilityStateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest BookingUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseUpdateBookingAvailabilityClientResponse parses an HTTP response from a UpdateBookingAvailabilityWithResponse call
func ParseUpdateBookingAvailabilityClientResponse(rsp *http.Response) (*UpdateBookingAvailabilityClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateBookingAvailabilityClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BookingPricingUpdateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest BookingWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest BookingRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest BookingUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetBookingChargesClientResponse parses an HTTP response from a GetBookingChargesWithResponse call
func ParseGetBookingChargesClientResponse(rsp *http.Response) (*GetBookingChargesClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetBookingChargesClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseUpdateBookingChargesClientResponse parses an HTTP response from a UpdateBookingChargesWithResponse call
func ParseUpdateBookingChargesClientResponse(rsp *http.Response) (*UpdateBookingChargesClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateBookingChargesClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetBookingContentClientResponse parses an HTTP response from a GetBookingContentWithResponse call
func ParseGetBookingContentClientResponse(rsp *http.Response) (*GetBookingContentClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetBookingContentClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseUpdateBookingContentClientResponse parses an HTTP response from a UpdateBookingContentWithResponse call
func ParseUpdateBookingContentClientResponse(rsp *http.Response) (*UpdateBookingContentClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateBookingContentClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseMapBookingRoomClientResponse parses an HTTP response from a MapBookingRoomWithResponse call
func ParseMapBookingRoomClientResponse(rsp *http.Response) (*MapBookingRoomClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &MapBookingRoomClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest MapBookingRoomResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseGetBookingListingPricingClientResponse parses an HTTP response from a GetBookingListingPricingWithResponse call
func ParseGetBookingListingPricingClientResponse(rsp *http.Response) (*GetBookingListingPricingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetBookingListingPricingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BookingPricingResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest BookingUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseUpdateBookingListingPricingClientResponse parses an HTTP response from a UpdateBookingListingPricingWithResponse call
func ParseUpdateBookingListingPricingClientResponse(rsp *http.Response) (*UpdateBookingListingPricingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateBookingListingPricingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BookingPricingUpdateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest BookingWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest BookingRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest BookingUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseListBookingConversationsClientResponse parses an HTTP response from a ListBookingConversationsWithResponse call
func ParseListBookingConversationsClientResponse(rsp *http.Response) (*ListBookingConversationsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListBookingConversationsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BookingConversationListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseSendBookingMessageClientResponse parses an HTTP response from a SendBookingMessageWithResponse call
func ParseSendBookingMessageClientResponse(rsp *http.Response) (*SendBookingMessageClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SendBookingMessageClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 201:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListBookingPropertiesClientResponse parses an HTTP response from a ListBookingPropertiesWithResponse call
func ParseListBookingPropertiesClientResponse(rsp *http.Response) (*ListBookingPropertiesClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListBookingPropertiesClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BookingPropertyListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseGetBookingPropertyClientResponse parses an HTTP response from a GetBookingPropertyWithResponse call
func ParseGetBookingPropertyClientResponse(rsp *http.Response) (*GetBookingPropertyClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetBookingPropertyClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseBookingPropertyActionClientResponse parses an HTTP response from a BookingPropertyActionWithResponse call
func ParseBookingPropertyActionClientResponse(rsp *http.Response) (*BookingPropertyActionClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &BookingPropertyActionClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BookingPropertyActionResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 402:
		var dest PublishBillingRefused
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON402 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest BookingWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest BookingUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest ChannelActionUnavailable
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseListBookingPropertyRoomsClientResponse parses an HTTP response from a ListBookingPropertyRoomsWithResponse call
func ParseListBookingPropertyRoomsClientResponse(rsp *http.Response) (*ListBookingPropertyRoomsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListBookingPropertyRoomsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BookingRoomsRatesResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListBookingReservationsClientResponse parses an HTTP response from a ListBookingReservationsWithResponse call
func ParseListBookingReservationsClientResponse(rsp *http.Response) (*ListBookingReservationsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListBookingReservationsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListBookingReservations200JSONResponseBody
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseAcknowledgeBookingReservationsClientResponse parses an HTTP response from a AcknowledgeBookingReservationsWithResponse call
func ParseAcknowledgeBookingReservationsClientResponse(rsp *http.Response) (*AcknowledgeBookingReservationsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &AcknowledgeBookingReservationsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListBookingReviewsClientResponse parses an HTTP response from a ListBookingReviewsWithResponse call
func ParseListBookingReviewsClientResponse(rsp *http.Response) (*ListBookingReviewsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListBookingReviewsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseReplyBookingReviewClientResponse parses an HTTP response from a ReplyBookingReviewWithResponse call
func ParseReplyBookingReviewClientResponse(rsp *http.Response) (*ReplyBookingReviewClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ReplyBookingReviewClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Success *bool `json:"success,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseBookingSetupClientResponse parses an HTTP response from a BookingSetupWithResponse call
func ParseBookingSetupClientResponse(rsp *http.Response) (*BookingSetupClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &BookingSetupClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case rsp.StatusCode == 201:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseDeleteBookingWebhookClientResponse parses an HTTP response from a DeleteBookingWebhookWithResponse call
func ParseDeleteBookingWebhookClientResponse(rsp *http.Response) (*DeleteBookingWebhookClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteBookingWebhookClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseListBookingWebhooksClientResponse parses an HTTP response from a ListBookingWebhooksWithResponse call
func ParseListBookingWebhooksClientResponse(rsp *http.Response) (*ListBookingWebhooksClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListBookingWebhooksClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseCreateBookingWebhookClientResponse parses an HTTP response from a CreateBookingWebhookWithResponse call
func ParseCreateBookingWebhookClientResponse(rsp *http.Response) (*CreateBookingWebhookClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateBookingWebhookClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseGetPlumguideAvailabilityClientResponse parses an HTTP response from a GetPlumguideAvailabilityWithResponse call
func ParseGetPlumguideAvailabilityClientResponse(rsp *http.Response) (*GetPlumguideAvailabilityClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetPlumguideAvailabilityClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseUpdatePlumguideAvailabilityClientResponse parses an HTTP response from a UpdatePlumguideAvailabilityWithResponse call
func ParseUpdatePlumguideAvailabilityClientResponse(rsp *http.Response) (*UpdatePlumguideAvailabilityClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdatePlumguideAvailabilityClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseListPlumguideBookingsClientResponse parses an HTTP response from a ListPlumguideBookingsWithResponse call
func ParseListPlumguideBookingsClientResponse(rsp *http.Response) (*ListPlumguideBookingsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListPlumguideBookingsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListPlumguideListingsClientResponse parses an HTTP response from a ListPlumguideListingsWithResponse call
func ParseListPlumguideListingsClientResponse(rsp *http.Response) (*ListPlumguideListingsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListPlumguideListingsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest PlumguideListingListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseGetPlumguidePricingClientResponse parses an HTTP response from a GetPlumguidePricingWithResponse call
func ParseGetPlumguidePricingClientResponse(rsp *http.Response) (*GetPlumguidePricingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetPlumguidePricingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseUpdatePlumguidePricingClientResponse parses an HTTP response from a UpdatePlumguidePricingWithResponse call
func ParseUpdatePlumguidePricingClientResponse(rsp *http.Response) (*UpdatePlumguidePricingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdatePlumguidePricingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseDeletePlumguideWebhooksClientResponse parses an HTTP response from a DeletePlumguideWebhooksWithResponse call
func ParseDeletePlumguideWebhooksClientResponse(rsp *http.Response) (*DeletePlumguideWebhooksClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeletePlumguideWebhooksClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetPlumguideWebhooksClientResponse parses an HTTP response from a GetPlumguideWebhooksWithResponse call
func ParseGetPlumguideWebhooksClientResponse(rsp *http.Response) (*GetPlumguideWebhooksClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetPlumguideWebhooksClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseUpdatePlumguideWebhooksClientResponse parses an HTTP response from a UpdatePlumguideWebhooksWithResponse call
func ParseUpdatePlumguideWebhooksClientResponse(rsp *http.Response) (*UpdatePlumguideWebhooksClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdatePlumguideWebhooksClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListVrboListingsClientResponse parses an HTTP response from a ListVrboListingsWithResponse call
func ParseListVrboListingsClientResponse(rsp *http.Response) (*ListVrboListingsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListVrboListingsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest VrboListingListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseListVrboReservationsClientResponse parses an HTTP response from a ListVrboReservationsWithResponse call
func ParseListVrboReservationsClientResponse(rsp *http.Response) (*ListVrboReservationsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListVrboReservationsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest VrboReservationListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseListConnectionsClientResponse parses an HTTP response from a ListConnectionsWithResponse call
func ParseListConnectionsClientResponse(rsp *http.Response) (*ListConnectionsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListConnectionsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ConnectionListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseCreateConnectSessionClientResponse parses an HTTP response from a CreateConnectSessionWithResponse call
func ParseCreateConnectSessionClientResponse(rsp *http.Response) (*CreateConnectSessionClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateConnectSessionClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest ConnectSession
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	}

	return response, nil
}

// ParseSubmitBeds24CredentialsClientResponse parses an HTTP response from a SubmitBeds24CredentialsWithResponse call
func ParseSubmitBeds24CredentialsClientResponse(rsp *http.Response) (*SubmitBeds24CredentialsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SubmitBeds24CredentialsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
			Ok          *bool                   `json:"ok,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseBookingConnectCallbackClientResponse parses an HTTP response from a BookingConnectCallbackWithResponse call
func ParseBookingConnectCallbackClientResponse(rsp *http.Response) (*BookingConnectCallbackClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &BookingConnectCallbackClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	}

	return response, nil
}

// ParseMapConnectBookingRoomsClientResponse parses an HTTP response from a MapConnectBookingRoomsWithResponse call
func ParseMapConnectBookingRoomsClientResponse(rsp *http.Response) (*MapConnectBookingRoomsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &MapConnectBookingRoomsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest MapConnectBookingRoomsResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 410:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON410 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 425:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON425 = &dest

	}

	return response, nil
}

// ParseListConnectBookingRoomsClientResponse parses an HTTP response from a ListConnectBookingRoomsWithResponse call
func ParseListConnectBookingRoomsClientResponse(rsp *http.Response) (*ListConnectBookingRoomsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListConnectBookingRoomsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BookingConnectRoomsResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 410:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON410 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 425:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON425 = &dest

	}

	return response, nil
}

// ParseVerifyBookingHotelClientResponse parses an HTTP response from a VerifyBookingHotelWithResponse call
func ParseVerifyBookingHotelClientResponse(rsp *http.Response) (*VerifyBookingHotelClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &VerifyBookingHotelClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BookingVerifyHotelResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 410:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON410 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 412:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON412 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParseSubmitBookingsyncCredentialsClientResponse parses an HTTP response from a SubmitBookingsyncCredentialsWithResponse call
func ParseSubmitBookingsyncCredentialsClientResponse(rsp *http.Response) (*SubmitBookingsyncCredentialsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SubmitBookingsyncCredentialsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
			Ok          *bool                   `json:"ok,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseSubmitGuestyCredentialsClientResponse parses an HTTP response from a SubmitGuestyCredentialsWithResponse call
func ParseSubmitGuestyCredentialsClientResponse(rsp *http.Response) (*SubmitGuestyCredentialsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SubmitGuestyCredentialsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
			Ok          *bool                   `json:"ok,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseSubmitHospitableCredentialsClientResponse parses an HTTP response from a SubmitHospitableCredentialsWithResponse call
func ParseSubmitHospitableCredentialsClientResponse(rsp *http.Response) (*SubmitHospitableCredentialsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SubmitHospitableCredentialsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
			Ok          *bool                   `json:"ok,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseSubmitHostawayCredentialsClientResponse parses an HTTP response from a SubmitHostawayCredentialsWithResponse call
func ParseSubmitHostawayCredentialsClientResponse(rsp *http.Response) (*SubmitHostawayCredentialsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SubmitHostawayCredentialsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
			Ok          *bool                   `json:"ok,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseSubmitIgmsCredentialsClientResponse parses an HTTP response from a SubmitIgmsCredentialsWithResponse call
func ParseSubmitIgmsCredentialsClientResponse(rsp *http.Response) (*SubmitIgmsCredentialsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SubmitIgmsCredentialsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
			Ok          *bool                   `json:"ok,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseSubmitLodgifyCredentialsClientResponse parses an HTTP response from a SubmitLodgifyCredentialsWithResponse call
func ParseSubmitLodgifyCredentialsClientResponse(rsp *http.Response) (*SubmitLodgifyCredentialsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SubmitLodgifyCredentialsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
			Ok          *bool                   `json:"ok,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseSubmitOwnerrezCredentialsClientResponse parses an HTTP response from a SubmitOwnerrezCredentialsWithResponse call
func ParseSubmitOwnerrezCredentialsClientResponse(rsp *http.Response) (*SubmitOwnerrezCredentialsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SubmitOwnerrezCredentialsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
			Ok          *bool                   `json:"ok,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseListConnectProvidersClientResponse parses an HTTP response from a ListConnectProvidersWithResponse call
func ParseListConnectProvidersClientResponse(rsp *http.Response) (*ListConnectProvidersClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListConnectProvidersClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ConnectProviderListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseSelectConnectProviderClientResponse parses an HTTP response from a SelectConnectProviderWithResponse call
func ParseSelectConnectProviderClientResponse(rsp *http.Response) (*SelectConnectProviderClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SelectConnectProviderClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SelectProviderResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseSubmitSmoobuCredentialsClientResponse parses an HTTP response from a SubmitSmoobuCredentialsWithResponse call
func ParseSubmitSmoobuCredentialsClientResponse(rsp *http.Response) (*SubmitSmoobuCredentialsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SubmitSmoobuCredentialsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
			Ok          *bool                   `json:"ok,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseSubmitVrboCredentialsClientResponse parses an HTTP response from a SubmitVrboCredentialsWithResponse call
func ParseSubmitVrboCredentialsClientResponse(rsp *http.Response) (*SubmitVrboCredentialsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SubmitVrboCredentialsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			AccountInfo *map[string]interface{} `json:"accountInfo,omitempty"`
			Ok          *bool                   `json:"ok,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseDeleteConnectionClientResponse parses an HTTP response from a DeleteConnectionWithResponse call
func ParseDeleteConnectionClientResponse(rsp *http.Response) (*DeleteConnectionClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteConnectionClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// AccountId The account that was disconnected. `null` only when the workspace had a stale connection record with no account to name, which was cleared.
			AccountId *string `json:"accountId"`

			// Disconnected Always `true` on success.
			Disconnected bool `json:"disconnected"`

			// ListingsDeactivated Ids of the listings this call deactivated. Listings still connected through another account or channel are not included and stay active.
			ListingsDeactivated []string `json:"listingsDeactivated"`

			// Provider The provider the account belonged to.
			Provider string `json:"provider"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 501:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON501 = &dest

	}

	return response, nil
}

// ParseGetConnectStatusClientResponse parses an HTTP response from a GetConnectStatusWithResponse call
func ParseGetConnectStatusClientResponse(rsp *http.Response) (*GetConnectStatusClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetConnectStatusClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ConnectStatus
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseCreateConnectionClientResponse parses an HTTP response from a CreateConnectionWithResponse call
func ParseCreateConnectionClientResponse(rsp *http.Response) (*CreateConnectionClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateConnectionClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Connection
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseListConversationsClientResponse parses an HTTP response from a ListConversationsWithResponse call
func ParseListConversationsClientResponse(rsp *http.Response) (*ListConversationsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListConversationsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ConversationListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetConversationClientResponse parses an HTTP response from a GetConversationWithResponse call
func ParseGetConversationClientResponse(rsp *http.Response) (*GetConversationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetConversationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ConversationDetail
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListConversationMessagesClientResponse parses an HTTP response from a ListConversationMessagesWithResponse call
func ParseListConversationMessagesClientResponse(rsp *http.Response) (*ListConversationMessagesClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListConversationMessagesClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest MessageListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseSendConversationMessageClientResponse parses an HTTP response from a SendConversationMessageWithResponse call
func ParseSendConversationMessageClientResponse(rsp *http.Response) (*SendConversationMessageClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SendConversationMessageClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest SendMessageResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 503:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON503 = &dest

	}

	return response, nil
}

// ParsePreapproveConversationClientResponse parses an HTTP response from a PreapproveConversationWithResponse call
func ParsePreapproveConversationClientResponse(rsp *http.Response) (*PreapproveConversationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PreapproveConversationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest struct {
			// BlockInstantBooking Example: false
			BlockInstantBooking bool `json:"blockInstantBooking"`

			// ConversationId Example: 164743
			ConversationId string `json:"conversationId"`

			// ExpiresAt When the guest can no longer book on the pre-approval, if Airbnb reported it.
			ExpiresAt *time.Time                                      `json:"expiresAt"`
			Status    PreapproveConversation201JSONResponseBodyStatus `json:"status"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseCreateConversationSpecialOfferClientResponse parses an HTTP response from a CreateConversationSpecialOfferWithResponse call
func ParseCreateConversationSpecialOfferClientResponse(rsp *http.Response) (*CreateConversationSpecialOfferClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateConversationSpecialOfferClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest struct {
			// AirbnbListingId Airbnb listing id the offer is for (a string — it exceeds 2^53).
			//
			// Example: 955656266214757921
			AirbnbListingId *string `json:"airbnbListingId,omitempty"`

			// CheckIn Example: 2026-10-01
			CheckIn *openapi_types.Date `json:"checkIn"`

			// CheckOut Example: 2026-10-05
			CheckOut *openapi_types.Date `json:"checkOut"`

			// ConversationId Repull conversation id the offer was sent on.
			//
			// Example: 164743
			ConversationId string     `json:"conversationId"`
			CreatedAt      *time.Time `json:"createdAt,omitempty"`

			// ExpiresAt When the guest can no longer book the offer (Airbnb gives them 24 hours).
			ExpiresAt *time.Time `json:"expiresAt,omitempty"`

			// Guests Guests on the offer. Airbnb counts adults + children as guests; infants and pets are extra.
			Guests *struct {
				// Adults Example: 2
				Adults *int `json:"adults,omitempty"`

				// Children Example: 1
				Children *int `json:"children,omitempty"`

				// Infants Example: 0
				Infants *int `json:"infants,omitempty"`

				// Pets Example: 0
				Pets *int `json:"pets,omitempty"`

				// Total Example: 3
				Total *int `json:"total,omitempty"`
			} `json:"guests,omitempty"`

			// Id Airbnb special-offer id. Use it to read or withdraw the offer.
			//
			// Example: 1459920384
			Id *string `json:"id"`

			// ListingId Repull listing id, when known.
			//
			// Example: 23892
			ListingId *string `json:"listingId,omitempty"`

			// Nights Example: 4
			Nights *int `json:"nights"`

			// Status Airbnb’s status for the offer: `active` (the guest can book it), `accepted`, `declined`, `expired` or `voided` (withdrawn).
			//
			// Example: active
			Status *string `json:"status"`

			// TotalPrice Total for the stay, in the listing’s Airbnb currency.
			//
			// Example: 880
			TotalPrice *float32 `json:"totalPrice"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseWithdrawConversationSpecialOfferClientResponse parses an HTTP response from a WithdrawConversationSpecialOfferWithResponse call
func ParseWithdrawConversationSpecialOfferClientResponse(rsp *http.Response) (*WithdrawConversationSpecialOfferClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &WithdrawConversationSpecialOfferClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// ConversationId Example: 164743
			ConversationId string `json:"conversationId"`

			// Id Example: 1459920384
			Id     string                                                    `json:"id"`
			Status WithdrawConversationSpecialOffer200JSONResponseBodyStatus `json:"status"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetConversationSpecialOfferClientResponse parses an HTTP response from a GetConversationSpecialOfferWithResponse call
func ParseGetConversationSpecialOfferClientResponse(rsp *http.Response) (*GetConversationSpecialOfferClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetConversationSpecialOfferClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// AirbnbListingId Airbnb listing id the offer is for (a string — it exceeds 2^53).
			//
			// Example: 955656266214757921
			AirbnbListingId *string `json:"airbnbListingId,omitempty"`

			// CheckIn Example: 2026-10-01
			CheckIn *openapi_types.Date `json:"checkIn"`

			// CheckOut Example: 2026-10-05
			CheckOut *openapi_types.Date `json:"checkOut"`

			// ConversationId Repull conversation id the offer was sent on.
			//
			// Example: 164743
			ConversationId string     `json:"conversationId"`
			CreatedAt      *time.Time `json:"createdAt,omitempty"`

			// ExpiresAt When the guest can no longer book the offer (Airbnb gives them 24 hours).
			ExpiresAt *time.Time `json:"expiresAt,omitempty"`

			// Guests Guests on the offer. Airbnb counts adults + children as guests; infants and pets are extra.
			Guests *struct {
				// Adults Example: 2
				Adults *int `json:"adults,omitempty"`

				// Children Example: 1
				Children *int `json:"children,omitempty"`

				// Infants Example: 0
				Infants *int `json:"infants,omitempty"`

				// Pets Example: 0
				Pets *int `json:"pets,omitempty"`

				// Total Example: 3
				Total *int `json:"total,omitempty"`
			} `json:"guests,omitempty"`

			// Id Airbnb special-offer id. Use it to read or withdraw the offer.
			//
			// Example: 1459920384
			Id *string `json:"id"`

			// ListingId Repull listing id, when known.
			//
			// Example: 23892
			ListingId *string `json:"listingId,omitempty"`

			// Nights Example: 4
			Nights *int `json:"nights"`

			// Status Airbnb’s status for the offer: `active` (the guest can book it), `accepted`, `declined`, `expired` or `voided` (withdrawn).
			//
			// Example: active
			Status *string `json:"status"`

			// TotalPrice Total for the stay, in the listing’s Airbnb currency.
			//
			// Example: 880
			TotalPrice *float32 `json:"totalPrice"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseListGuestsClientResponse parses an HTTP response from a ListGuestsWithResponse call
func ParseListGuestsClientResponse(rsp *http.Response) (*ListGuestsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListGuestsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest GuestListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseCreateGuestClientResponse parses an HTTP response from a CreateGuestWithResponse call
func ParseCreateGuestClientResponse(rsp *http.Response) (*CreateGuestClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateGuestClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest GuestCreateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest GuestCreateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetGuestClientResponse parses an HTTP response from a GetGuestWithResponse call
func ParseGetGuestClientResponse(rsp *http.Response) (*GetGuestClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetGuestClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest GuestProfile
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetHealthClientResponse parses an HTTP response from a GetHealthWithResponse call
func ParseGetHealthClientResponse(rsp *http.Response) (*GetHealthClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetHealthClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Status Example: ok
			Status *string `json:"status,omitempty"`

			// Version Example: 1.0.0
			Version *string `json:"version,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseGetAuthHealthClientResponse parses an HTTP response from a GetAuthHealthWithResponse call
func ParseGetAuthHealthClientResponse(rsp *http.Response) (*GetAuthHealthClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetAuthHealthClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest map[string]interface{}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseGetChannelHealthClientResponse parses an HTTP response from a GetChannelHealthWithResponse call
func ParseGetChannelHealthClientResponse(rsp *http.Response) (*GetChannelHealthClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetChannelHealthClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest map[string]interface{}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseGetMcpHealthClientResponse parses an HTTP response from a GetMcpHealthWithResponse call
func ParseGetMcpHealthClientResponse(rsp *http.Response) (*GetMcpHealthClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetMcpHealthClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest map[string]interface{}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseGetWebhooksHealthClientResponse parses an HTTP response from a GetWebhooksHealthWithResponse call
func ParseGetWebhooksHealthClientResponse(rsp *http.Response) (*GetWebhooksHealthClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetWebhooksHealthClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest map[string]interface{}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseListInquiriesClientResponse parses an HTTP response from a ListInquiriesWithResponse call
func ParseListInquiriesClientResponse(rsp *http.Response) (*ListInquiriesClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListInquiriesClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data []struct {
				// Channel Example: airbnb
				Channel string `json:"channel"`

				// CheckIn Example: 2026-09-23
				CheckIn *openapi_types.Date `json:"checkIn"`

				// CheckOut Example: 2026-10-11
				CheckOut *openapi_types.Date `json:"checkOut"`

				// ConversationId Repull conversation id — pass it to `POST /v1/conversations/{id}/pre-approval` or `/special-offers`.
				//
				// Example: 164743
				ConversationId *string    `json:"conversationId"`
				CreatedAt      *time.Time `json:"createdAt"`

				// ExpectedPayout What Airbnb quoted the host for the stay the guest asked about.
				ExpectedPayout struct {
					// Amount Example: 2152.6
					Amount *float32 `json:"amount,omitempty"`

					// Currency Example: USD
					Currency *string `json:"currency,omitempty"`
				} `json:"expectedPayout"`
				Guests struct {
					// Adults Example: 2
					Adults *int `json:"adults,omitempty"`

					// Children Example: 0
					Children *int `json:"children,omitempty"`

					// Infants Example: 0
					Infants *int `json:"infants,omitempty"`

					// Pets Example: 0
					Pets *int `json:"pets,omitempty"`

					// Total Example: 2
					Total *int `json:"total,omitempty"`
				} `json:"guests"`

				// Id Repull inquiry id.
				//
				// Example: 25173
				Id string `json:"id"`

				// ListingId Example: 23892
				ListingId *string `json:"listingId"`

				// RelayedBy A PMS (e.g. `hostaway`, `guesty`) this inquiry arrives through. When set, it cannot be pre-approved or offered from Repull — act on it in that PMS.
				RelayedBy *string `json:"relayedBy"`

				// ReservationId The reservation the inquiry became, once booked.
				ReservationId *string `json:"reservationId"`

				// RespondBy Airbnb’s response deadline for the host (it counts toward response rate).
				RespondBy   *time.Time `json:"respondBy"`
				RespondedAt *time.Time `json:"respondedAt"`

				// Status `open` — nobody has answered and the stay is still ahead; `pre_approved`; `special_offer_sent` (from the API, a connected app, or Airbnb’s own app); `booked` — the guest booked (`reservationId`); `expired` — the stay has started or Airbnb expired it; `declined`; `not_possible` — Airbnb says the dates cannot be booked.
				Status    ListInquiries200JSONResponseBodyDataStatus `json:"status"`
				UpdatedAt *time.Time                                 `json:"updatedAt"`
			} `json:"data"`

			// Pagination Canonical cursor-based pagination envelope. Pass `nextCursor` back as `?cursor=` to fetch the next page; stop when `hasMore` is `false`. The cursor is opaque base64 — do not parse or construct it by hand.
			Pagination Pagination `json:"pagination"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseClearKvClientResponse parses an HTTP response from a ClearKvWithResponse call
func ParseClearKvClientResponse(rsp *http.Response) (*ClearKvClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ClearKvClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Deleted *int `json:"deleted,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseListKvClientResponse parses an HTTP response from a ListKvWithResponse call
func ParseListKvClientResponse(rsp *http.Response) (*ListKvClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListKvClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data *[]struct {
				Key       *string     `json:"key,omitempty"`
				TtlAt     *time.Time  `json:"ttl_at,omitempty"`
				UpdatedAt *time.Time  `json:"updated_at,omitempty"`
				Value     interface{} `json:"value,omitempty"`
			} `json:"data,omitempty"`
			Pagination *struct {
				HasMore *bool `json:"hasMore,omitempty"`
				Total   *int  `json:"total,omitempty"`
			} `json:"pagination,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseDeleteKvClientResponse parses an HTTP response from a DeleteKvWithResponse call
func ParseDeleteKvClientResponse(rsp *http.Response) (*DeleteKvClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteKvClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Deleted *bool `json:"deleted,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	}

	return response, nil
}

// ParseGetKvClientResponse parses an HTTP response from a GetKvWithResponse call
func ParseGetKvClientResponse(rsp *http.Response) (*GetKvClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetKvClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Key       *string     `json:"key,omitempty"`
			TtlAt     *time.Time  `json:"ttl_at,omitempty"`
			UpdatedAt *time.Time  `json:"updated_at,omitempty"`
			Value     interface{} `json:"value,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseSetKvClientResponse parses an HTTP response from a SetKvWithResponse call
func ParseSetKvClientResponse(rsp *http.Response) (*SetKvClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SetKvClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Key       *string     `json:"key,omitempty"`
			TtlAt     *time.Time  `json:"ttl_at,omitempty"`
			UpdatedAt *time.Time  `json:"updated_at,omitempty"`
			Value     interface{} `json:"value,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 413:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON413 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseListListingsClientResponse parses an HTTP response from a ListListingsWithResponse call
func ParseListListingsClientResponse(rsp *http.Response) (*ListListingsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListListingsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseCreateListingClientResponse parses an HTTP response from a CreateListingWithResponse call
func ParseCreateListingClientResponse(rsp *http.Response) (*CreateListingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateListingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest ListingCreateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 402:
		var dest PaymentRequired
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON402 = &dest

	}

	return response, nil
}

// ParseBulkApplyPricingClientResponse parses an HTTP response from a BulkApplyPricingWithResponse call
func ParseBulkApplyPricingClientResponse(rsp *http.Response) (*BulkApplyPricingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &BulkApplyPricingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest BulkPricingResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseSetListingsStatusClientResponse parses an HTTP response from a SetListingsStatusWithResponse call
func ParseSetListingsStatusClientResponse(rsp *http.Response) (*SetListingsStatusClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &SetListingsStatusClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingStatusBatchResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 402:
		var dest PaymentRequired
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON402 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseDeactivateListingClientResponse parses an HTTP response from a DeactivateListingWithResponse call
func ParseDeactivateListingClientResponse(rsp *http.Response) (*DeactivateListingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeactivateListingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingActiveResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseGetListingClientResponse parses an HTTP response from a GetListingWithResponse call
func ParseGetListingClientResponse(rsp *http.Response) (*GetListingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetListingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Listing
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseUpdateListingActiveClientResponse parses an HTTP response from a UpdateListingActiveWithResponse call
func ParseUpdateListingActiveClientResponse(rsp *http.Response) (*UpdateListingActiveClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateListingActiveClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingActiveResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 402:
		var dest PaymentRequired
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON402 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseListListingCompsClientResponse parses an HTTP response from a ListListingCompsWithResponse call
func ParseListListingCompsClientResponse(rsp *http.Response) (*ListListingCompsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListListingCompsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingCompsResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseUpdateListingContentClientResponse parses an HTTP response from a UpdateListingContentWithResponse call
func ParseUpdateListingContentClientResponse(rsp *http.Response) (*UpdateListingContentClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateListingContentClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingContentUpdateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseGenerateListingContentClientResponse parses an HTTP response from a GenerateListingContentWithResponse call
func ParseGenerateListingContentClientResponse(rsp *http.Response) (*GenerateListingContentClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GenerateListingContentClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingGenerateContentResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case rsp.StatusCode == 502:
		break // No content-type

	}

	return response, nil
}

// ParseTakeListingOfflineClientResponse parses an HTTP response from a TakeListingOfflineWithResponse call
func ParseTakeListingOfflineClientResponse(rsp *http.Response) (*TakeListingOfflineClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &TakeListingOfflineClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingMarketStateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 402:
		var dest PublishBillingRefused
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON402 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest TooManyRequests
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers TakeListingOfflineClientResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Limit"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Limit", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.XRateLimitLimit = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Remaining"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Remaining", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.XRateLimitRemaining = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Reset"); len(values) > 0 {
			var value time.Time
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Reset", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: "date-time"}); err != nil {
				return nil, err
			}
			headers.XRateLimitReset = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseTakeListingOnlineClientResponse parses an HTTP response from a TakeListingOnlineWithResponse call
func ParseTakeListingOnlineClientResponse(rsp *http.Response) (*TakeListingOnlineClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &TakeListingOnlineClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingMarketStateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 402:
		var dest PublishBillingRefused
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON402 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest TooManyRequests
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers TakeListingOnlineClientResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Limit"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Limit", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.XRateLimitLimit = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Remaining"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Remaining", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.XRateLimitRemaining = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Reset"); len(values) > 0 {
			var value time.Time
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Reset", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: "date-time"}); err != nil {
				return nil, err
			}
			headers.XRateLimitReset = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseDeleteListingPhotoClientResponse parses an HTTP response from a DeleteListingPhotoWithResponse call
func ParseDeleteListingPhotoClientResponse(rsp *http.Response) (*DeleteListingPhotoClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteListingPhotoClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingPhotoDeleteResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseListListingPhotosClientResponse parses an HTTP response from a ListListingPhotosWithResponse call
func ParseListListingPhotosClientResponse(rsp *http.Response) (*ListListingPhotosClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListListingPhotosClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingPhotosResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseCreateListingPhotoUploadUrlClientResponse parses an HTTP response from a CreateListingPhotoUploadUrlWithResponse call
func ParseCreateListingPhotoUploadUrlClientResponse(rsp *http.Response) (*CreateListingPhotoUploadUrlClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateListingPhotoUploadUrlClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingPhotoUploadUrlResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseGetListingPricingClientResponse parses an HTTP response from a GetListingPricingWithResponse call
func ParseGetListingPricingClientResponse(rsp *http.Response) (*GetListingPricingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetListingPricingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingPricingResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case rsp.StatusCode == 502:
		break // No content-type

	}

	return response, nil
}

// ParseApplyListingPricingClientResponse parses an HTTP response from a ApplyListingPricingWithResponse call
func ParseApplyListingPricingClientResponse(rsp *http.Response) (*ApplyListingPricingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ApplyListingPricingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingPricingApplyResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseGetListingPricingHistoryClientResponse parses an HTTP response from a GetListingPricingHistoryWithResponse call
func ParseGetListingPricingHistoryClientResponse(rsp *http.Response) (*GetListingPricingHistoryClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetListingPricingHistoryClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingPricingHistoryResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetListingPricingStrategyClientResponse parses an HTTP response from a GetListingPricingStrategyWithResponse call
func ParseGetListingPricingStrategyClientResponse(rsp *http.Response) (*GetListingPricingStrategyClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetListingPricingStrategyClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingPricingStrategy
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseUpdateListingPricingStrategyClientResponse parses an HTTP response from a UpdateListingPricingStrategyWithResponse call
func ParseUpdateListingPricingStrategyClientResponse(rsp *http.Response) (*UpdateListingPricingStrategyClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateListingPricingStrategyClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Ok *bool `json:"ok,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseGetListingPublishStatusClientResponse parses an HTTP response from a GetListingPublishStatusWithResponse call
func ParseGetListingPublishStatusClientResponse(rsp *http.Response) (*GetListingPublishStatusClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetListingPublishStatusClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingPublishStatusResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest TooManyRequests
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers GetListingPublishStatusClientResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Limit"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Limit", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.XRateLimitLimit = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Remaining"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Remaining", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.XRateLimitRemaining = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Reset"); len(values) > 0 {
			var value time.Time
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Reset", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: "date-time"}); err != nil {
				return nil, err
			}
			headers.XRateLimitReset = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParsePublishListingToAirbnbClientResponse parses an HTTP response from a PublishListingToAirbnbWithResponse call
func ParsePublishListingToAirbnbClientResponse(rsp *http.Response) (*PublishListingToAirbnbClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PublishListingToAirbnbClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingPublishAirbnbResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 402:
		var dest PublishBillingRefused
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON402 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParsePublishListingToBookingClientResponse parses an HTTP response from a PublishListingToBookingWithResponse call
func ParsePublishListingToBookingClientResponse(rsp *http.Response) (*PublishListingToBookingClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PublishListingToBookingClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingPublishBookingResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 402:
		var dest PublishBillingRefused
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON402 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	}

	return response, nil
}

// ParsePullListingFromAirbnbClientResponse parses an HTTP response from a PullListingFromAirbnbWithResponse call
func ParsePullListingFromAirbnbClientResponse(rsp *http.Response) (*PullListingFromAirbnbClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PullListingFromAirbnbClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingPullResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest TooManyRequests
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	switch {
	case rsp.StatusCode == 429:
		var headers PullListingFromAirbnbClientResponse429Headers
		if values := rsp.Header.Values("Retry-After"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "Retry-After", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.RetryAfter = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Limit"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Limit", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.XRateLimitLimit = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Remaining"); len(values) > 0 {
			var value int
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Remaining", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "integer", Format: ""}); err != nil {
				return nil, err
			}
			headers.XRateLimitRemaining = &value
		}
		if values := rsp.Header.Values("X-RateLimit-Reset"); len(values) > 0 {
			var value time.Time
			if err := runtime.BindStyledParameterWithOptions("simple", "X-RateLimit-Reset", values[0], &value, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationHeader, Explode: false, Required: false, Type: "string", Format: "date-time"}); err != nil {
				return nil, err
			}
			headers.XRateLimitReset = &value
		}
		response.Headers429 = &headers
	}

	return response, nil
}

// ParseGetListingSegmentsClientResponse parses an HTTP response from a GetListingSegmentsWithResponse call
func ParseGetListingSegmentsClientResponse(rsp *http.Response) (*GetListingSegmentsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetListingSegmentsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ListingSegmentsResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseListMarketsClientResponse parses an HTTP response from a ListMarketsWithResponse call
func ParseListMarketsClientResponse(rsp *http.Response) (*ListMarketsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListMarketsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest MarketsOverviewResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case rsp.StatusCode == 502:
		break // No content-type

	}

	return response, nil
}

// ParseListMarketBrowseClientResponse parses an HTTP response from a ListMarketBrowseWithResponse call
func ParseListMarketBrowseClientResponse(rsp *http.Response) (*ListMarketBrowseClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListMarketBrowseClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest MarketBrowseResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseGetMarketClientResponse parses an HTTP response from a GetMarketWithResponse call
func ParseGetMarketClientResponse(rsp *http.Response) (*GetMarketClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetMarketClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest MarketDetailResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case rsp.StatusCode == 502:
		break // No content-type

	}

	return response, nil
}

// ParseGetMarketCalendarClientResponse parses an HTTP response from a GetMarketCalendarWithResponse call
func ParseGetMarketCalendarClientResponse(rsp *http.Response) (*GetMarketCalendarClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetMarketCalendarClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest MarketCalendarResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case rsp.StatusCode == 502:
		break // No content-type

	}

	return response, nil
}

// ParseListMigrationsClientResponse parses an HTTP response from a ListMigrationsWithResponse call
func ParseListMigrationsClientResponse(rsp *http.Response) (*ListMigrationsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListMigrationsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data       *[]Migration `json:"data,omitempty"`
			Pagination *struct {
				HasMore    *bool   `json:"hasMore,omitempty"`
				NextCursor *string `json:"nextCursor,omitempty"`
				Total      *int    `json:"total,omitempty"`
			} `json:"pagination,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseDeleteMigrationClientResponse parses an HTTP response from a DeleteMigrationWithResponse call
func ParseDeleteMigrationClientResponse(rsp *http.Response) (*DeleteMigrationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteMigrationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data *struct {
				Deactivated   *bool      `json:"deactivated,omitempty"`
				DeactivatedAt *time.Time `json:"deactivatedAt,omitempty"`
				WorkspaceId   *string    `json:"workspaceId,omitempty"`
			} `json:"data,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetMigrationClientResponse parses an HTTP response from a GetMigrationWithResponse call
func ParseGetMigrationClientResponse(rsp *http.Response) (*GetMigrationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetMigrationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Data One migration: a property manager moved through Repull Migrate, living in its own workspace.
			Data *Migration `json:"data,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetMigrationChannelMapClientResponse parses an HTTP response from a GetMigrationChannelMapWithResponse call
func ParseGetMigrationChannelMapClientResponse(rsp *http.Response) (*GetMigrationChannelMapClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetMigrationChannelMapClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data *MigrationChannelMap `json:"data,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseCutoverMigrationClientResponse parses an HTTP response from a CutoverMigrationWithResponse call
func ParseCutoverMigrationClientResponse(rsp *http.Response) (*CutoverMigrationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CutoverMigrationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			// Data One migration: a property manager moved through Repull Migrate, living in its own workspace.
			Data *Migration `json:"data,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseCheckMigrationCutoverClientResponse parses an HTTP response from a CheckMigrationCutoverWithResponse call
func ParseCheckMigrationCutoverClientResponse(rsp *http.Response) (*CheckMigrationCutoverClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CheckMigrationCutoverClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data *MigrationCutoverCheck `json:"data,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseRunMigrationImportClientResponse parses an HTTP response from a RunMigrationImportWithResponse call
func ParseRunMigrationImportClientResponse(rsp *http.Response) (*RunMigrationImportClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &RunMigrationImportClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 202:
		var dest struct {
			Data *struct {
				Queued *[]struct {
					ConnectionId *string `json:"connectionId,omitempty"`
					Error        *string `json:"error,omitempty"`
					Provider     *string `json:"provider,omitempty"`
					Queued       *bool   `json:"queued,omitempty"`
				} `json:"queued,omitempty"`
				WorkspaceId *string `json:"workspaceId,omitempty"`
			} `json:"data,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON202 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Conflict
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetMigrationReportClientResponse parses an HTTP response from a GetMigrationReportWithResponse call
func ParseGetMigrationReportClientResponse(rsp *http.Response) (*GetMigrationReportClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetMigrationReportClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data *MigrationReport `json:"data,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListPropertiesClientResponse parses an HTTP response from a ListPropertiesWithResponse call
func ParseListPropertiesClientResponse(rsp *http.Response) (*ListPropertiesClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListPropertiesClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest PropertyListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseGetPropertyClientResponse parses an HTTP response from a GetPropertyWithResponse call
func ParseGetPropertyClientResponse(rsp *http.Response) (*GetPropertyClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetPropertyClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Property
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseGetQuoteClientResponse parses an HTTP response from a GetQuoteWithResponse call
func ParseGetQuoteClientResponse(rsp *http.Response) (*GetQuoteClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetQuoteClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Quote
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseListReservationsClientResponse parses an HTTP response from a ListReservationsWithResponse call
func ParseListReservationsClientResponse(rsp *http.Response) (*ListReservationsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListReservationsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ReservationListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseCreateReservationClientResponse parses an HTTP response from a CreateReservationWithResponse call
func ParseCreateReservationClientResponse(rsp *http.Response) (*CreateReservationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateReservationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest ReservationCreateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetReservationClientResponse parses an HTTP response from a GetReservationWithResponse call
func ParseGetReservationClientResponse(rsp *http.Response) (*GetReservationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetReservationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Reservation
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseUpdateReservationClientResponse parses an HTTP response from a UpdateReservationWithResponse call
func ParseUpdateReservationClientResponse(rsp *http.Response) (*UpdateReservationClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateReservationClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ReservationUpdateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest ListingInactive
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseAcceptReservationRequestClientResponse parses an HTTP response from a AcceptReservationRequestWithResponse call
func ParseAcceptReservationRequestClientResponse(rsp *http.Response) (*AcceptReservationRequestClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &AcceptReservationRequestClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Action  AcceptReservationRequest200JSONResponseBodyAction  `json:"action"`
			Channel AcceptReservationRequest200JSONResponseBodyChannel `json:"channel"`

			// ConfirmationCode Example: HM9J2MFR3W
			ConfirmationCode string                                                    `json:"confirmationCode"`
			DeclineReason    *AcceptReservationRequest200JSONResponseBodyDeclineReason `json:"declineReason"`

			// ReservationId Example: 236354
			ReservationId string `json:"reservationId"`

			// Status What Airbnb was asked to do and did not refuse. The reservation itself moves when Airbnb’s own notification lands, usually within seconds — that is when `reservation.request.updated` fires (`requestStatus` `accepted` or `declined`), plus `reservation.created` for an accepted request.
			Status AcceptReservationRequest200JSONResponseBodyStatus `json:"status"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseDeclineReservationRequestClientResponse parses an HTTP response from a DeclineReservationRequestWithResponse call
func ParseDeclineReservationRequestClientResponse(rsp *http.Response) (*DeclineReservationRequestClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeclineReservationRequestClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Action  DeclineReservationRequest200JSONResponseBodyAction  `json:"action"`
			Channel DeclineReservationRequest200JSONResponseBodyChannel `json:"channel"`

			// ConfirmationCode Example: HM9J2MFR3W
			ConfirmationCode string                                                     `json:"confirmationCode"`
			DeclineReason    *DeclineReservationRequest200JSONResponseBodyDeclineReason `json:"declineReason"`

			// ReservationId Example: 236354
			ReservationId string `json:"reservationId"`

			// Status What Airbnb was asked to do and did not refuse. The reservation itself moves when Airbnb’s own notification lands, usually within seconds — that is when `reservation.request.updated` fires (`requestStatus` `accepted` or `declined`), plus `reservation.created` for an accepted request.
			Status DeclineReservationRequest200JSONResponseBodyStatus `json:"status"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseListReviewsClientResponse parses an HTTP response from a ListReviewsWithResponse call
func ParseListReviewsClientResponse(rsp *http.Response) (*ListReviewsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListReviewsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest ReviewListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetReviewClientResponse parses an HTTP response from a GetReviewWithResponse call
func ParseGetReviewClientResponse(rsp *http.Response) (*GetReviewClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetReviewClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest Review
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseReplyToReviewClientResponse parses an HTTP response from a ReplyToReviewWithResponse call
func ParseReplyToReviewClientResponse(rsp *http.Response) (*ReplyToReviewClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ReplyToReviewClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest struct {
			Id       *string `json:"id,omitempty"`
			Platform *string `json:"platform,omitempty"`
			Response *string `json:"response,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest AirbnbWriteRejected
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 429:
		var dest AirbnbRateLimited
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON429 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 502:
		var dest AirbnbUpstreamError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON502 = &dest

	}

	return response, nil
}

// ParseListCustomSchemasClientResponse parses an HTTP response from a ListCustomSchemasWithResponse call
func ParseListCustomSchemasClientResponse(rsp *http.Response) (*ListCustomSchemasClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListCustomSchemasClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest CustomSchemaListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	}

	return response, nil
}

// ParseCreateCustomSchemaClientResponse parses an HTTP response from a CreateCustomSchemaWithResponse call
func ParseCreateCustomSchemaClientResponse(rsp *http.Response) (*CreateCustomSchemaClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateCustomSchemaClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest CustomSchemaCreateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseDeleteCustomSchemaClientResponse parses an HTTP response from a DeleteCustomSchemaWithResponse call
func ParseDeleteCustomSchemaClientResponse(rsp *http.Response) (*DeleteCustomSchemaClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteCustomSchemaClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest CustomSchemaDeleteResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseGetCustomSchemaClientResponse parses an HTTP response from a GetCustomSchemaWithResponse call
func ParseGetCustomSchemaClientResponse(rsp *http.Response) (*GetCustomSchemaClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetCustomSchemaClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest CustomSchema
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	}

	return response, nil
}

// ParseUpdateCustomSchemaClientResponse parses an HTTP response from a UpdateCustomSchemaWithResponse call
func ParseUpdateCustomSchemaClientResponse(rsp *http.Response) (*UpdateCustomSchemaClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateCustomSchemaClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest CustomSchema
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 400:
		var dest BadRequest
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON400 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Forbidden
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 404:
		var dest NotFound
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON404 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParseGetUsageLogsClientResponse parses an HTTP response from a GetUsageLogsWithResponse call
func ParseGetUsageLogsClientResponse(rsp *http.Response) (*GetUsageLogsClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetUsageLogsClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Data *[]struct {
				CreatedAt     *time.Time `json:"createdAt,omitempty"`
				ErrorCode     *string    `json:"errorCode,omitempty"`
				Id            *string    `json:"id,omitempty"`
				IpAddress     *string    `json:"ipAddress,omitempty"`
				LatencyMs     *int       `json:"latencyMs,omitempty"`
				Method        *string    `json:"method,omitempty"`
				OperationId   *string    `json:"operationId,omitempty"`
				Path          *string    `json:"path,omitempty"`
				RequestBytes  *int       `json:"requestBytes,omitempty"`
				RequestId     *string    `json:"requestId,omitempty"`
				ResponseBytes *int       `json:"responseBytes,omitempty"`
				StatusCode    *int       `json:"statusCode,omitempty"`
				UserAgent     *string    `json:"userAgent,omitempty"`
			} `json:"data,omitempty"`
			Pagination *struct {
				HasMore    *bool   `json:"hasMore,omitempty"`
				NextCursor *string `json:"nextCursor,omitempty"`
				Total      *int    `json:"total,omitempty"`
			} `json:"pagination,omitempty"`
			Range *string `json:"range,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest UnprocessableEntity
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetUsageSummaryClientResponse parses an HTTP response from a GetUsageSummaryWithResponse call
func ParseGetUsageSummaryClientResponse(rsp *http.Response) (*GetUsageSummaryClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetUsageSummaryClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Breakdown *[]struct {
				AvgLatencyMs *int     `json:"avgLatencyMs,omitempty"`
				ErrorCount   *int     `json:"errorCount,omitempty"`
				ErrorRate    *float32 `json:"errorRate,omitempty"`
				OperationId  *string  `json:"operationId,omitempty"`
				RequestCount *int     `json:"requestCount,omitempty"`
			} `json:"breakdown,omitempty"`
			DailyResetsAt *time.Time `json:"dailyResetsAt,omitempty"`
			Limits        *struct {
				DailyAiRequests *int `json:"dailyAiRequests,omitempty"`
				DailyRequests   *int `json:"dailyRequests,omitempty"`
				MonthlyRequests *int `json:"monthlyRequests,omitempty"`
			} `json:"limits,omitempty"`
			Range     *string `json:"range,omitempty"`
			Remaining *struct {
				Daily   *int `json:"daily,omitempty"`
				DailyAi *int `json:"dailyAi,omitempty"`
				Monthly *int `json:"monthly,omitempty"`
			} `json:"remaining,omitempty"`
			ResetsAt           *time.Time `json:"resetsAt,omitempty"`
			StatusDistribution *struct {
				N2xx *int `json:"2xx,omitempty"`
				N3xx *int `json:"3xx,omitempty"`
				N4xx *int `json:"4xx,omitempty"`
				N5xx *int `json:"5xx,omitempty"`
			} `json:"statusDistribution,omitempty"`
			Tier     *string `json:"tier,omitempty"`
			Timeline *[]struct {
				Day          *string `json:"day,omitempty"`
				ErrorCount   *int    `json:"errorCount,omitempty"`
				RequestCount *int    `json:"requestCount,omitempty"`
			} `json:"timeline,omitempty"`
			Totals *struct {
				AvgLatencyMs *int `json:"avgLatencyMs,omitempty"`
				Errors       *int `json:"errors,omitempty"`
				Requests     *int `json:"requests,omitempty"`
			} `json:"totals,omitempty"`
			Used *struct {
				Daily   *int `json:"daily,omitempty"`
				DailyAi *int `json:"dailyAi,omitempty"`
				Monthly *int `json:"monthly,omitempty"`
			} `json:"used,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseGetUsageTierClientResponse parses an HTTP response from a GetUsageTierWithResponse call
func ParseGetUsageTierClientResponse(rsp *http.Response) (*GetUsageTierClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetUsageTierClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			DailyResetsAt *time.Time `json:"dailyResetsAt,omitempty"`
			Limits        *struct {
				DailyAiRequests        *int `json:"dailyAiRequests,omitempty"`
				DailyRequests          *int `json:"dailyRequests,omitempty"`
				DynamicPricingListings *int `json:"dynamicPricingListings,omitempty"`
				MonthlyRequests        *int `json:"monthlyRequests,omitempty"`
			} `json:"limits,omitempty"`
			Remaining *struct {
				Daily                  *int `json:"daily,omitempty"`
				DailyAi                *int `json:"dailyAi,omitempty"`
				DynamicPricingListings *int `json:"dynamicPricingListings,omitempty"`
				Monthly                *int `json:"monthly,omitempty"`
			} `json:"remaining,omitempty"`
			ResetsAt *time.Time `json:"resetsAt,omitempty"`
			Tier     *string    `json:"tier,omitempty"`
			Used     *struct {
				Daily                  *int `json:"daily,omitempty"`
				DailyAi                *int `json:"dailyAi,omitempty"`
				DynamicPricingListings *int `json:"dynamicPricingListings,omitempty"`
				Monthly                *int `json:"monthly,omitempty"`
			} `json:"used,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 401:
		var dest Unauthorized
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON401 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 500:
		var dest InternalError
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON500 = &dest

	}

	return response, nil
}

// ParseListWebhooksClientResponse parses an HTTP response from a ListWebhooksWithResponse call
func ParseListWebhooksClientResponse(rsp *http.Response) (*ListWebhooksClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListWebhooksClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest WebhookListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseCreateWebhookClientResponse parses an HTTP response from a CreateWebhookWithResponse call
func ParseCreateWebhookClientResponse(rsp *http.Response) (*CreateWebhookClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &CreateWebhookClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 201:
		var dest WebhookSubscription
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON201 = &dest

	}

	return response, nil
}

// ParseListWebhookEventTypesClientResponse parses an HTTP response from a ListWebhookEventTypesWithResponse call
func ParseListWebhookEventTypesClientResponse(rsp *http.Response) (*ListWebhookEventTypesClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListWebhookEventTypesClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest WebhookEventCatalog
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseTestWebhookClientResponse parses an HTTP response from a TestWebhookWithResponse call
func ParseTestWebhookClientResponse(rsp *http.Response) (*TestWebhookClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &TestWebhookClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	return response, nil
}

// ParseDeleteWebhookClientResponse parses an HTTP response from a DeleteWebhookWithResponse call
func ParseDeleteWebhookClientResponse(rsp *http.Response) (*DeleteWebhookClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &DeleteWebhookClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	return response, nil
}

// ParseGetWebhookClientResponse parses an HTTP response from a GetWebhookWithResponse call
func ParseGetWebhookClientResponse(rsp *http.Response) (*GetWebhookClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetWebhookClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest WebhookSubscription
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseUpdateWebhookClientResponse parses an HTTP response from a UpdateWebhookWithResponse call
func ParseUpdateWebhookClientResponse(rsp *http.Response) (*UpdateWebhookClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &UpdateWebhookClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest WebhookSubscription
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseListWebhookDeliveriesClientResponse parses an HTTP response from a ListWebhookDeliveriesWithResponse call
func ParseListWebhookDeliveriesClientResponse(rsp *http.Response) (*ListWebhookDeliveriesClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ListWebhookDeliveriesClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest WebhookDeliveryListResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseGetWebhookDeliveryClientResponse parses an HTTP response from a GetWebhookDeliveryWithResponse call
func ParseGetWebhookDeliveryClientResponse(rsp *http.Response) (*GetWebhookDeliveryClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &GetWebhookDeliveryClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest WebhookDeliveryDetail
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseReplayWebhookDeliveryClientResponse parses an HTTP response from a ReplayWebhookDeliveryWithResponse call
func ParseReplayWebhookDeliveryClientResponse(rsp *http.Response) (*ReplayWebhookDeliveryClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &ReplayWebhookDeliveryClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case rsp.StatusCode == 200:
		break // No content-type

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 403:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON403 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 409:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON409 = &dest

	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 422:
		var dest Error
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON422 = &dest

	}

	return response, nil
}

// ParsePingWebhookClientResponse parses an HTTP response from a PingWebhookWithResponse call
func ParsePingWebhookClientResponse(rsp *http.Response) (*PingWebhookClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PingWebhookClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	return response, nil
}

// ParseRotateWebhookSecretClientResponse parses an HTTP response from a RotateWebhookSecretWithResponse call
func ParseRotateWebhookSecretClientResponse(rsp *http.Response) (*RotateWebhookSecretClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &RotateWebhookSecretClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest struct {
			Id        *string    `json:"id,omitempty"`
			RotatedAt *time.Time `json:"rotatedAt,omitempty"`
			Secret    *string    `json:"secret,omitempty"`
		}
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}

// ParseTestFireWebhookClientResponse parses an HTTP response from a TestFireWebhookWithResponse call
func ParseTestFireWebhookClientResponse(rsp *http.Response) (*TestFireWebhookClientResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &TestFireWebhookClientResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	return response, nil
}
